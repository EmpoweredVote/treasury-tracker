---
phase: 81-towns-virginia-data-model-linking
plan: 03
type: execute
wave: 3
depends_on: [81-02]
files_modified:
  - src/components/EntitySwitcher.tsx
  - src/components/CitiesInCountyPanel.tsx
  - src/components/CountiesInStatePanel.tsx
  - src/App.tsx
requirements: [VALINK-01]
must_haves:
  truths:
    - "The EntitySwitcher picker shows state/federal navigation nodes even when they have no datasets, so 'Virginia' is selectable from the top-level 'State Governments' list despite carrying no budget data; ordinary localities still require data to appear (CONTEXT D-09)"
    - "The Virginia hub page surfaces its cities, counties, AND towns: CitiesInStatePanel already lists cities+towns; a counties affordance (CountiesInStatePanel or a counties section) makes all VA counties reachable from the state page (CONTEXT D-10)"
    - "A county page lists its towns: CitiesInCountyPanel includes entity_type 'town' (not just 'city'), so towns appear in their county's localities panel (VALINK-01)"
    - "Navigation reads US → Virginia → locality: an independent city renders standalone (US→Virginia→City, no county level); a county renders standalone (US→Virginia→County); a town shows US→Virginia→<County> County→<Town> via the existing breadcrumb resolver (county_id set in 81-02)"
    - "No regression: existing CA/MA/federal/county/city pages render as before; the data-less-node relaxation is scoped to state/federal only"
  artifacts:
    - path: "src/components/EntitySwitcher.tsx"
      provides: "Picker that always includes state/federal nav nodes regardless of datasets"
      contains: "entity_type"
    - path: "src/components/CitiesInCountyPanel.tsx"
      provides: "County localities panel that includes towns as well as cities"
      contains: "town"
    - path: "src/components/CountiesInStatePanel.tsx"
      provides: "Counties affordance on the state hub page (or an equivalent section)"
      contains: "county"
    - path: "src/App.tsx"
      provides: "State page renders the counties affordance alongside CitiesInStatePanel"
      contains: "CountiesInStatePanel"
  key_links:
    - from: "Virginia state node (no datasets) + linked towns/cities/counties"
      to: "US → Virginia → locality navigation: pickable hub + reachable cities/counties/towns + town breadcrumb"
      via: "relax picker data-filter for nav nodes; counties panel on state page; towns in county panel; existing breadcrumb resolver"
      pattern: "EntitySwitcher / CitiesInStatePanel / CountiesInStatePanel / CitiesInCountyPanel"
---

<objective>
Make the Virginia navigation model usable in the app (the UI half of VALINK-01): let citizens **select Virginia** from the picker (even though it has no budget data), reach **its cities, counties, and towns** from the hub page, see a county's **towns** in that county's localities panel, and get correct **US → Virginia → locality** breadcrumbs. Then verify the phase's success criteria in the live app and write 81-VERIFICATION.md. The data model (state node, county_id links) is already in place from 81-02; the breadcrumb resolver and most panels already exist — this plan is small, surgical frontend edits.
</objective>

<task type="execute">
<name>Show data-less state/federal nav nodes in the picker</name>
<read_first>
- src/components/EntitySwitcher.tsx (the `withData` filter ~70 and the federal/state pre-filter ~72–79; totalCount ~100)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-09 — Chris-confirmed)
</read_first>
<action>
In EntitySwitcher.tsx, change the `withData` defensive filter so it keeps a municipality when it has datasets **OR** its entity_type is 'state' or 'federal' (navigation hubs are not data leaves). Concretely: `const withData = filtered.filter(m => (m.available_datasets && m.available_datasets.length > 0) || m.entity_type === 'state' || m.entity_type === 'federal')`. Ordinary localities (city/county/town/…) still require datasets. Leave `cityEntities` building (which already excludes state/federal) and the `byState` grouping unchanged so a data-less Virginia node lands only in the stateEntities list, not in a city group. Keep `totalCount` (the "N jurisdictions with data" label) counting only data-bearing rows so the headline number doesn't inflate.
</action>
<acceptance_criteria>
- src/components/EntitySwitcher.tsx withData filter includes `m.entity_type === 'state' || m.entity_type === 'federal'`
- A state node with `available_datasets: []` appears in the State Governments section of the rendered picker (Virginia is selectable)
- CA/MA state nodes and the federal node still appear; data-less ordinary localities (city/county/town) still do NOT appear
</acceptance_criteria>
</task>

<task type="execute">
<name>Add a counties affordance to the Virginia hub page</name>
<read_first>
- src/components/CitiesInStatePanel.tsx (the available/coming-soon + filter-box pattern to mirror; it excludes counties)
- src/components/CitiesInCountyPanel.tsx (the simplest panel shape)
- src/App.tsx (the state-page render block ~1290 where CitiesInStatePanel is rendered)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-10)
</read_first>
<action>
Create `src/components/CountiesInStatePanel.tsx` mirroring CitiesInStatePanel but filtering `m.state === state.state && m.entity_type === 'county'` (sorted; available-now vs coming-soon by `available_datasets.length`; show the filter box above the 24-count threshold since VA has 95 counties). Heading "Counties in {state.name}". Clicking a county calls the same `onCityClick`/`handleEntityChange` handler. In src/App.tsx, render `<CountiesInStatePanel ... />` in the same `navigationPath.length === 0 && selectedEntity?.entity_type === 'state'` block, alongside CitiesInStatePanel (counties section first or after — pick a sensible order). This makes all 95 VA counties reachable from the Virginia hub while CitiesInStatePanel continues to surface cities + towns.
</action>
<acceptance_criteria>
- src/components/CountiesInStatePanel.tsx exists and filters entity_type === 'county' for the state
- App.tsx renders CountiesInStatePanel on state pages (navigationPath.length === 0 && entity_type === 'state')
- On the Virginia page, both a counties panel and the cities/towns panel render; counties have a filter box (95 > threshold)
- `npm run build` (or the project's typecheck/build) passes
</acceptance_criteria>
</task>

<task type="execute">
<name>Include towns in the county localities panel</name>
<read_first>
- src/components/CitiesInCountyPanel.tsx (line ~17: `m.county_id === county.id && m.entity_type === 'city'`)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-10 — towns appear in their county's localities panel)
</read_first>
<action>
In CitiesInCountyPanel.tsx, broaden the membership filter so it includes towns as well as cities: `m.county_id === county.id && (m.entity_type === 'city' || m.entity_type === 'town')`. This makes a Virginia county page list its linked towns (county_id set in 81-02) in the same localities panel. No other change — the available/coming-soon split and click handler stay as-is. (Existing CA county pages are unaffected: their linked rows are cities; no towns are linked to them.)
</action>
<acceptance_criteria>
- CitiesInCountyPanel.tsx filter includes `entity_type === 'town'`
- A VA county with linked towns (e.g. Loudoun County → Leesburg/Purcellville) lists those towns in its localities panel
- Existing CA county pages (e.g. LA County) still list their cities and render unchanged
</acceptance_criteria>
</task>

<task type="execute">
<name>Verify success criteria in the live app and write 81-VERIFICATION.md</name>
<read_first>
- .planning/ROADMAP.md (Phase 81 success criteria 1–4)
- .planning/phases/80-city-county-loads/80-VERIFICATION.md (the inline verification format to mirror)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (specifics — the four proofs: per-capita-for-towns, linking, standalone, hub)
</read_first>
<action>
Run the app (or its preview/build) and walk the four success criteria, capturing evidence: (1) all ~37 towns load with sourced datasets + ≥1 town shows per-capita; (2) "Virginia" is selectable in the picker and its page lists cities, counties, and towns; (3) an independent city (Alexandria) renders standalone (US→Virginia→Alexandria, no county), a county (Fairfax County) renders standalone and lists its cities+towns, and a town (e.g. Vienna→Fairfax County, Leesburg→Loudoun County) shows the US→Virginia→<County> County→<Town> breadcrumb and appears in its county's localities panel; (4) re-running the loaders/seed changes nothing (idempotent). Write `.planning/phases/81-towns-virginia-data-model-linking/81-VERIFICATION.md` (frontmatter `status: passed|failed`, requirements [VALOAD-03, VALINK-01], method inline) with a success-criteria table + evidence, mirroring 80-VERIFICATION.md. Note this is the phase-level check; the milestone-wide source-chain audit + UAT sign-off is Phase 83.
</action>
<acceptance_criteria>
- 81-VERIFICATION.md exists with a per-criterion table and concrete evidence (town counts, a town per-capita value, breadcrumb screenshots/notes, picker visibility, idempotency result)
- All four ROADMAP Phase 81 success criteria are marked met (or any gap is documented + accepted, Phase-80-style)
- The build/typecheck passes and no existing CA/MA/federal page regressed
</acceptance_criteria>
</task>

<verification>
- Picker: Virginia selectable (data-less) under State Governments; CA/MA/federal unchanged; data-less ordinary localities still hidden
- Virginia hub page lists cities, counties (filterable), and towns
- County page lists its towns; town shows US→Virginia→County→Town breadcrumb; city/county render standalone
- 81-VERIFICATION.md written with status + evidence; build passes
</verification>
