---
phase: 95-mn-history-oh-va-re-do-sgfs-02-sgfs-03
plan: 05
type: execute
wave: 2
depends_on: ["95-03", "95-04"]
files_modified:
  - scripts/cleanupOHVAEstimateRows.mjs
autonomous: true
requirements: [SGFS-03]
must_haves:
  truths:
    - "No FY2026 (or any future/no-actual) estimate budget row remains for the Ohio or Virginia state node"
    - "No remaining OH/VA state-node budget row carries an estimate-grade source (lsc.ohio.gov, dpb.virginia.gov) or a NULL source stamp"
    - "OH data_sources rows carry fiscal_years = [2020,2021,2022,2023,2024,2025] and VA = [2022,2023,2024,2025], both pointing at the ACFR landing pages"
  artifacts:
    - path: "scripts/cleanupOHVAEstimateRows.mjs"
      provides: "One-shot idempotent cleanup: deletes orphaned FY2026 OH/VA estimate budget rows + asserts the post-state"
      contains: "fiscal_year"
  key_links:
    - from: "scripts/cleanupOHVAEstimateRows.mjs"
      to: "treasury.budgets (OH/VA FY2026 rows)"
      via: "targeted DELETE by municipality_id + fiscal_year"
      pattern: "delete"
---

<objective>
Close out the OH/VA false-provenance remediation: delete the orphaned FY2026 (and any other future/no-published-ACFR) estimate budget rows that the ACFR loaders did NOT overwrite, correct the OH/VA data_sources metadata, and prove cohort-clean state for both nodes.

Purpose: SGFS-03 — the ACFR loaders (Plans 03/04) only upsert the years they are called for (the RPC keys on muni+FY+dataset_type), so the old FY2026 estimate rows survive untouched with their false-looking source URLs (Pitfall 4). This plan removes them and verifies no estimate/NULL row remains for OH or VA.
Output: `scripts/cleanupOHVAEstimateRows.mjs`; Ohio budgets contain only the FY2020-FY2025 GAAP ACFR rows and Virginia only the FY2022-FY2025 GAAP ACFR rows; data_sources metadata corrected.

NOTE: OH and VA now have DIFFERENT keep-windows — OH was extended to FY2020-FY2025 (Plan 03, 6 years / 12 rows) while VA stays FY2022-FY2025 (Plan 04, 4 years / 8 rows). The cleanup window filter MUST be per-state, or it would wrongly delete Ohio's FY2020/FY2021 rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-CONTEXT.md
@.planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-RESEARCH.md
@.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md
@scripts/processMN.js

<interfaces>
Reuse the processMN.js env-load + supabase client shape. Operations (all under schema('treasury')):

Per-state keep-windows (CRITICAL): OH_KEEP = [2020,2021,2022,2023,2024,2025]; VA_KEEP = [2022,2023,2024,2025].
muni lookup: from('municipalities').select('id').eq('name', 'Ohio'|'Virginia').eq('state', 'OH'|'VA').single()
DELETE orphan rows: for Ohio, delete any budgets row whose fiscal_year is NOT in OH_KEEP (catches FY2026 + any stray); for Virginia, delete any budgets row whose fiscal_year is NOT in VA_KEEP (catches FY2026 + FY2021/FY2020 estimate strays + any other). Both dataset_types. Use the state's own keep-list — do NOT apply one shared window to both states.
data_source correction: from('data_sources').update({ base_url: <ACFR landing>, fiscal_years: <state keep-list> }).eq('name', ...): Ohio rows ('Ohio General Fund Operating Budget' | 'Ohio General Fund Revenue') get fiscal_years = OH_KEEP; Virginia rows ('Virginia General Fund Operating Budget' | 'Virginia General Fund Revenue') get fiscal_years = VA_KEEP. (The ACFR loaders already set these; this is an idempotent re-assert.)

Use a --dry-run flag (print rows that WOULD be deleted, no write) and a live mode. Idempotent: a second live run deletes 0 rows.
ACFR landing pages: OH = https://obm.ohio.gov/reports-and-resources/01-acfr-and-pafr ; VA = https://www.doa.virginia.gov/reports/ACFReport/
Prereq: Plans 03 and 04 loaded the FY2022-FY2025 ACFR rows for OH and VA (depends_on).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write cleanupOHVAEstimateRows.mjs — delete orphaned FY2026/non-window estimate rows (dry-run first)</name>
  <read_first>
    - scripts/processMN.js (env loader + supabase client + schema('treasury') usage to copy)
    - .planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-RESEARCH.md "Runtime State Inventory" (OH/VA estimate-row inventory + data_source names) + Pitfall 4 (FY2026 survives RPC) + "Loader Strategy Recommendation" (separate cleanup script preferred)
    - .planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md (P1 actuals-only window, P6 idempotency, targeted-write-only)
  </read_first>
  <behavior>
    - --dry-run lists every OH budgets row outside OH_KEEP [2020-2025] and every VA budgets row outside VA_KEEP [2022-2025] (expected: OH FY2026 operating+revenue, VA FY2026 operating+revenue) without deleting — and does NOT flag OH FY2020/FY2021 (those are now valid actuals).
    - Live mode deletes exactly those rows and re-asserts the OH/VA data_sources base_url + per-state fiscal_years.
    - A second live run deletes 0 rows and re-asserts identically (idempotent).
  </behavior>
  <action>
    Create scripts/cleanupOHVAEstimateRows.mjs reusing the processMN.js env-load + createClient shape. Define OH_KEEP = [2020,2021,2022,2023,2024,2025] and VA_KEEP = [2022,2023,2024,2025]. For each of Ohio (OH, keep-list OH_KEEP) and Virginia (VA, keep-list VA_KEEP): look up the state-node municipality_id; select all treasury.budgets rows for that municipality_id and report fiscal_year + dataset_type + source_url + data_source; identify rows whose fiscal_year is NOT in that state's keep-list (the orphaned estimate years, primarily FY2026). In --dry-run print them and exit without writing. In live mode DELETE those out-of-window rows via targeted from('budgets').delete().eq('municipality_id', muniId).eq('fiscal_year', fy) per row, then UPDATE the four data_sources rows (Ohio/Virginia x operating/revenue, by name) setting base_url to the ACFR landing page and fiscal_years to the state's keep-list (Ohio → OH_KEEP, Virginia → VA_KEEP). Never use treasury_sync_city_budget; targeted writes only. Make all operations idempotent (re-run deletes 0).
  </action>
  <verify>
    <automated>node scripts/cleanupOHVAEstimateRows.mjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - --dry-run exits 0 and lists the OH FY2026 + VA FY2026 rows (operating and revenue) as deletion candidates, listing NO OH FY2020-FY2025 row and NO VA FY2022-FY2025 row.
    - The script contains no reference to treasury_sync_city_budget and never sets data_source_id.
    - grep -n "fiscal_year\|OH_KEEP\|VA_KEEP" scripts/cleanupOHVAEstimateRows.mjs shows the per-state windows OH=[2020..2025] and VA=[2022..2025].
  </acceptance_criteria>
  <done>cleanupOHVAEstimateRows.mjs exists; dry-run correctly identifies the orphaned FY2026 OH/VA rows and no in-window rows.</done>
</task>

<task type="auto">
  <name>Task 2: Run cleanup live + verify OH/VA nodes are estimate-free and cohort-clean</name>
  <read_first>
    - scripts/cleanupOHVAEstimateRows.mjs (from Task 1)
    - .planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-VALIDATION.md (0-NULL invariant + no-FY2026 probes)
    - feedback_supabase_migration_mcp (use mcp__supabase-local execute_sql for the DB probes)
  </read_first>
  <action>
    Run node scripts/cleanupOHVAEstimateRows.mjs (live). Confirm it deleted the OH FY2026 + VA FY2026 rows and re-asserted the four data_sources rows. Then via supabase-local execute_sql, probe the OH and VA state nodes: (a) count any budgets row with fiscal_year = 2026 for either node (must be 0); (b) count any OH/VA budgets row with NULL source_url/source_date/data_source (must be 0); (c) count any OH/VA budgets row whose source_url contains 'lsc.ohio.gov' or 'dpb.virginia.gov', or whose data_source contains 'estimated' (must be 0); (d) confirm the OH node has exactly 12 rows (FY2020-FY2025 x operating+revenue) and the VA node has exactly 8 rows (FY2022-FY2025 x operating+revenue). Re-run the cleanup script once more and confirm it deletes 0 rows (idempotent).
  </action>
  <verify>
    <automated>node scripts/cleanupOHVAEstimateRows.mjs --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - DB probe (a): count of OH+VA state-node budgets rows with fiscal_year = 2026 returns 0.
    - DB probe (b): count of OH+VA budgets rows with NULL source_url OR source_date OR data_source returns 0.
    - DB probe (c): count of OH+VA budgets rows with source_url LIKE '%lsc.ohio.gov%' OR '%dpb.virginia.gov%' OR data_source LIKE '%estimated%' returns 0.
    - DB probe (d): OH node has 12 budgets rows (fiscal_year in 2020-2025) and VA node has 8 budgets rows (fiscal_year in 2022-2025).
    - The final --dry-run reports 0 remaining deletion candidates (idempotent post-state).
  </acceptance_criteria>
  <done>OH/VA FY2026 estimate rows deleted; Ohio holds only the 12 FY2020-FY2025 GAAP ACFR rows and Virginia only the 8 FY2022-FY2025 GAAP ACFR rows; 0 NULL stamps, 0 estimate/LSC/DPB references; idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleanup script -> treasury.budgets DELETE | A too-broad delete could remove valid FY2022-FY2025 ACFR rows; window filter must be exact |
| residual estimate row -> live app | The whole SGFS-03 point: any surviving FY2026/estimate/LSC/DPB row keeps showing false-sourced data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-15 | Tampering (over-deletion) | budgets DELETE window | mitigate | Targeted delete keyed on per-state keep-list (OH NOT IN [2020..2025], VA NOT IN [2022..2025]) — guards Ohio's FY2020/FY2021 actuals; --dry-run lists candidates before any write; Task 2 probe (d) asserts 12 OH / 8 VA rows survive |
| T-95-16 | Info disclosure (residual false provenance) | OH/VA estimate rows | mitigate | Task 2 probes (a)(c) assert 0 FY2026 rows and 0 lsc.ohio.gov/dpb.virginia.gov/estimated references remain |
| T-95-17 | Repudiation | 0-NULL invariant | mitigate | Task 2 probe (b) asserts 0 NULL source stamps across both nodes |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Dry-run identifies only the orphaned FY2026 OH/VA rows (Task 1).
- After live cleanup: 0 FY2026 rows, 0 NULL stamps, 0 estimate/LSC/DPB references, exactly 12 OH rows and 8 VA rows (Task 2 probes).
- Idempotent: second run deletes 0.
</verification>

<success_criteria>
- The OH state node contains ONLY real GAAP ACFR rows (FY2020-FY2025 operating + revenue); the VA state node ONLY FY2022-FY2025 operating + revenue.
- No falsely-sourced or future-estimate row survives for either node (SGFS-03 fully closed for OH/VA).
- data_sources metadata reflects the ACFR source.
</success_criteria>

<output>
Create `.planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-05-SUMMARY.md` when done.
</output>
