---
phase: 96-remaining-states-sgfs-04
plan: 07
type: execute
wave: 5
depends_on: ["96-02", "96-03", "96-04", "96-05", "96-06"]
files_modified:
  - .planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md
autonomous: true
requirements: [SGFS-04]
must_haves:
  truths:
    - "Cohort unsourced revenue rows are deleted — no cohort state node shows a 'Money In' tab with unsourced data (D-96-03 / ground rule)"
    - "Cohort out-of-window operating estimate rows (FY2022/FY2025/FY2026) are deleted"
    - "All 46 cohort states + GA carry sourced NASBO operating rows for FY2023 + FY2024 with 0-NULL source_url/source_date/data_source"
    - "AL/MI source_date = FY-09-30, TX = FY-08-31, NY = FY-03-31 in the loaded rows"
    - "Re-running the loader is idempotent (no net change)"
  artifacts:
    - path: ".planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md"
      provides: "Per-state load + probe results, residual revenue deferral note, FY2022 acknowledged-gap note"
      contains: "0-NULL"
  key_links:
    - from: "scripts/loadStateGF.mjs (live)"
      to: "treasury.budgets (cohort operating rows)"
      via: "treasury_sync_budget_tree RPC + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Run the Wave 0 cleanup live, run the full-cohort NASBO operating load live, and DB-probe-verify that the operating side of SGFS-04 is satisfied: every cohort state + GA has sourced FY2023+FY2024 operating rows (0-NULL), no displayed unsourced revenue rows remain, and the non-June-30 FY-ends are correct. Then write the load log documenting the residual revenue deferral (D-96-01/03) and the acknowledged FY2022 gap.

Purpose: This is the only plan that writes to production. It is gated on the full-cohort dry-run passing (Plan 06) and the cleanup script + dry-run passing (Plan 02). Order matters: cleanup first (remove displayed unsourced data before/with the load so the ground rule is never violated mid-flight), then the operating load, then verification.
Output: Production cohort state nodes carrying real, sourced NASBO operating actuals; a 96-07-LOAD-LOG.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-remaining-states-sgfs-04/96-CONTEXT.md
@.planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md
@.planning/phases/96-remaining-states-sgfs-04/96-VALIDATION.md
@.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md

<interfaces>
<!-- Run commands + DB-probe shape. Use mcp__supabase-local execute_sql for probes (per Chris's memory). -->

Cleanup (Plan 02 artifact): scripts/cleanupStateEstimates.mjs
  node scripts/cleanupStateEstimates.mjs --dry-run        # audit
  node scripts/cleanupStateEstimates.mjs --confirm        # live delete

Load (Plans 03-06 populated STATES): scripts/loadStateGF.mjs
  node scripts/loadStateGF.mjs --dry-run                  # full-cohort revalidate (94 state-years)
  node scripts/loadStateGF.mjs                            # full-cohort live load
  node scripts/loadStateGF.mjs --state XX --fy 2023       # per-state if a retry is needed

DB probe (treasury.budgets): for each cohort municipality_id, expect
  - operating rows ONLY for fiscal_year IN (2023, 2024), all with non-NULL source_url/source_date/data_source
  - NO revenue rows
  - source_date pattern: AL/MI '*-09-30', TX '*-08-31', NY '*-03-31', others '*-06-30'

46 cohort IDs + GA ID + the 4 non-June-30 states: 96-RESEARCH.md §"The 46-State Cohort".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run cleanup + full-cohort live load</name>
  <files>scripts/loadStateGF.mjs, scripts/cleanupStateEstimates.mjs</files>
  <read_first>
    - scripts/cleanupStateEstimates.mjs (the Plan-02 cleanup script — --dry-run then --confirm contract)
    - scripts/loadStateGF.mjs (the full-cohort loader; RPC + post-RPC source-stamp; idempotency)
    - .planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md §"D-96-03 Revenue Display Finding" (delete revenue before load), Pitfall 5 (FY2025/2026 operating), §"Loader Scaling" (Option A full run)
    - .planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md (P4 0-NULL stamp; P6 idempotent; never treasury_sync_city_budget)
  </read_first>
  <action>
    Ensure SUPABASE_SERVICE_KEY is present in .env/.env.local (the loader exits 2 without it for live runs). Step 1: re-run `node scripts/cleanupStateEstimates.mjs --dry-run` and confirm the per-state summary matches expectation (cohort revenue rows + FY2022/2025/2026 operating rows only — no MN/OH/VA/GA rows), then run `node scripts/cleanupStateEstimates.mjs --confirm` to delete them (D-96-03). Step 2: re-run the full-cohort `node scripts/loadStateGF.mjs --dry-run` as a final gate (all 94 state-years tie:PASS) — abort if any FAIL. Step 3: run the live full-cohort load `node scripts/loadStateGF.mjs` (no flags). The loader resolves each state node, find-or-updates its nasbo-ser operating data_source, upserts the operating tree via treasury_sync_budget_tree (keyed muni+fy+dataset_type), and stamps source_url/source_date/data_source via the targeted post-RPC UPDATE (P4). GA FY2023 (already loaded) re-runs idempotently (P6). If any single state-year errors, re-run that state with `--state XX --fy YYYY` after fixing; never hand-edit budgets.
  </action>
  <verify>
    <automated>node scripts/loadStateGF.mjs --dry-run</automated>
  </verify>
  <done>Cleanup --confirm deleted the cohort revenue + out-of-window operating rows; the live load reports all cohort state-years loaded; the final full dry-run still prints 94× tie:PASS.</done>
</task>

<task type="auto">
  <name>Task 2: DB-probe verification + load log (0-NULL, FY-ends, no unsourced revenue)</name>
  <files>.planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md</files>
  <read_first>
    - .planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md §"The 46-State Cohort" (IDs to probe + the 4 non-June-30 states), §"D-96-03 Revenue Display Finding", §"Pitfall 5" (no FY2025/2026 operating)
    - .planning/phases/96-remaining-states-sgfs-04/96-VALIDATION.md §"Manual-Only Verifications" + §"Validation Sign-Off"
    - .planning/phases/95-mn-history-oh-va-re-do-sgfs-02-sgfs-03/95-05-SUMMARY.md (the 95-05 cohort-clean probe pattern to mirror, if present)
  </read_first>
  <action>
    Using mcp__supabase-local execute_sql (per Chris's DB-migration preference — no Dashboard checkpoint), run probes against treasury.budgets for the 46 cohort IDs + GA: (1) COUNT of operating rows per state by fiscal_year — expect exactly FY2023 + FY2024 (no FY2022/2025/2026); (2) COUNT of operating rows with NULL source_url OR NULL source_date OR NULL data_source across the cohort — expect 0 (the 0-NULL invariant, P4); (3) COUNT of revenue rows across the cohort — expect 0 (D-96-03 resolved); (4) source_date spot-check for AL, MI (expect *-09-30), TX (*-08-31), NY (*-03-31), and one June-30 state (e.g. CA *-06-30). Then write `.planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md` recording: per-state load status, the 4 probe results, the AL/MI/TX/NY FY-end confirmations, an explicit "residual revenue deferral" note (cohort revenue-by-source remains deferred to the future ACFR upgrade per D-96-01; the unsourced revenue rows were DELETED, not replaced, so nothing unsourced is displayed — D-96-03 honest resolution), and an "acknowledged FY2022 gap" note (FY2022 actuals exist in the 2024 SER but are out of scope for the 2025-SER window per D-96-02; FY2022 estimate rows were deleted, leaving no row rather than an estimate — honest per P5). Any state that genuinely could not be sourced (none expected per D-96-04) is documented here, not left as an estimate.
  </action>
  <verify>
    <automated>node scripts/loadStateGF.mjs --dry-run</automated>
    <human-check>96-07-LOAD-LOG.md records: 0 NULL-provenance operating rows, 0 cohort revenue rows, operating years = {2023,2024} only, AL/MI=09-30 TX=08-31 NY=03-31 source_dates, and the residual revenue + FY2022 deferral notes. Open one cohort state node in the live app and confirm no unsourced 'Money In' renders.</human-check>
  </verify>
  <done>All four DB probes pass (0 NULL provenance, 0 revenue rows, only FY2023+FY2024 operating, correct FY-ends); 96-07-LOAD-LOG.md written with the residual revenue + FY2022 deferral notes; idempotent re-run confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Loader/cleanup script → treasury.budgets (production) | The only production-writing plan; a wrong write path or order could display unsourced data or destroy real rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-14 | Information disclosure (ground-rule breach) | leftover unsourced revenue rows | mitigate | Cleanup runs --confirm BEFORE the load; Probe (3) verifies 0 cohort revenue rows; human app check confirms no 'Money In' unsourced render — BLOCKING if non-zero |
| T-96-15 | Tampering (data integrity) | live operating load | mitigate | Full --dry-run (94× tie:PASS) is a hard gate before the live run; any FAIL aborts the load (P5) |
| T-96-16 | Tampering (false provenance) | 0-NULL source stamp + FY-ends | mitigate | Probe (2) requires 0 NULL provenance; Probe (4) verifies AL/MI/TX/NY FY-ends — BLOCKING if wrong |
| T-96-17 | Tampering (wrong write path) | DB write | mitigate | Load uses treasury_sync_budget_tree + targeted UPDATE only; cleanup uses targeted DELETE only; never treasury_sync_city_budget, never data_source_id (P4) |
| T-96-SC | Tampering | npm installs | mitigate | No package installs this phase |
</threat_model>

<verification>
- Probe 1: every cohort state + GA has operating rows for exactly {2023, 2024}.
- Probe 2: 0 cohort operating rows with NULL source_url/source_date/data_source.
- Probe 3: 0 cohort revenue rows.
- Probe 4: AL/MI source_date *-09-30, TX *-08-31, NY *-03-31, CA *-06-30.
- Idempotency: a second `node scripts/loadStateGF.mjs` run reports no net change.
- 96-07-LOAD-LOG.md contains the residual revenue deferral + FY2022 acknowledged-gap notes.
</verification>

<success_criteria>
SGFS-04 (operating side) is satisfied: all 46 cohort states + GA carry real, sourced NASBO operating actuals for FY2023+FY2024 with 0-NULL provenance and correct fiscal-year-end source_dates; no unsourced estimate operating rows and no displayed unsourced revenue rows remain for the cohort; the residual revenue-by-source deferral and the FY2022 gap are documented (not silent). Phase 97 (SGFS-05) takes the cohort-wide audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/96-remaining-states-sgfs-04/96-07-SUMMARY.md` when done.
</output>
