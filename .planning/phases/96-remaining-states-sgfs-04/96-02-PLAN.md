---
phase: 96-remaining-states-sgfs-04
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/cleanupStateEstimates.mjs
autonomous: true
requirements: [SGFS-04]
must_haves:
  truths:
    - "A dry-run lists exactly the cohort revenue rows + out-of-window operating rows it would delete, by state + dataset_type + fiscal_year, with row counts"
    - "Live run deletes the 46 cohort states' revenue estimate rows (dataset_type='revenue')"
    - "Live run deletes the 46 cohort states' operating estimate rows OUTSIDE the FY2023-2024 actual window (FY2022, FY2025, FY2026)"
    - "The script never calls treasury_sync_city_budget and never touches budgets.data_source_id"
    - "Re-running after deletion is idempotent (0 further deletes)"
  artifacts:
    - path: "scripts/cleanupStateEstimates.mjs"
      provides: "Targeted DELETE of unsourced cohort revenue + out-of-window operating estimate rows; dry-run default"
      contains: "dataset_type"
  key_links:
    - from: "scripts/cleanupStateEstimates.mjs"
      to: "treasury.budgets"
      via: "targeted DELETE by municipality_id + dataset_type + fiscal_year"
      pattern: "from\\('budgets'\\)"
---

<objective>
Create `scripts/cleanupStateEstimates.mjs` (NEW) — the Wave 0 cleanup that removes every cohort row that would display unsourced data: the 46 cohort states' revenue estimate rows (deferred per D-96-01, never replaced this phase) and their out-of-window operating estimate rows (FY2022/FY2025/FY2026) that the NASBO actual-year load (FY2023+FY2024 only) will not overwrite.

Purpose: D-96-03 (BLOCKING) — RESEARCH confirmed the cohort's unsourced revenue rows ARE displayed in the live "Money In" tab, violating the ground rule "never display unsourced data". The NASBO loader only writes operating FY2023+FY2024, so revenue rows and FY2022/2025/2026 operating estimate rows persist as displayed false data unless explicitly deleted. This reconciles Chris's "defer revenue, don't build it" with not displaying false data — we delete the display, we do not fabricate a replacement.
Output: An idempotent, dry-run-first cleanup script. It is RUN (live) in Plan 07, not here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-remaining-states-sgfs-04/96-CONTEXT.md
@.planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md
@.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md

<interfaces>
<!-- Reuse the env-loading + supabase-client pattern from loadStateGF.mjs. Do NOT re-invent. -->

From scripts/loadStateGF.mjs (copy these patterns):
  loadEnv()  // reads ../.env.local then ../.env into process.env
  const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
  supabase.schema('treasury').from('budgets')...   // schema is 'treasury'
  parseArgs({ options: { 'dry-run': { type: 'boolean', default: false } } })

Cohort IDs: the 46 (municipality_id, abbr, name, FY-end) rows are listed in
  96-RESEARCH.md §"The 46-State Cohort" (table with DB ID column). These are the ONLY IDs
  the cleanup may touch. MN/OH/VA/GA are EXCLUDED (they carry real actuals).

treasury.budgets columns: municipality_id, fiscal_year, dataset_type, total_budget,
  source_url, source_date, data_source (see prior-phase loader contracts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build cleanupStateEstimates.mjs (dry-run default, targeted DELETE)</name>
  <files>scripts/cleanupStateEstimates.mjs</files>
  <read_first>
    - scripts/loadStateGF.mjs (loadEnv, createClient, schema('treasury'), parseArgs, exit-2-on-error patterns to copy)
    - .planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md §"The 46-State Cohort" (the 46 municipality_id values — hardcode these), §"D-96-03 Revenue Display Finding" (what to delete + why), §"Revenue + out-of-window operating cleanup SQL" (the exact delete predicates), Pitfall 5 (FY2025/2026 operating must be deleted)
    - .planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md (P4: never treasury_sync_city_budget, never budgets.data_source_id; P6 idempotent)
  </read_first>
  <action>
    Create scripts/cleanupStateEstimates.mjs. Embed a COHORT array of the 46 municipality_id UUIDs from 96-RESEARCH.md §"The 46-State Cohort" (paired with abbr+name as inline comments for auditability). Reuse loadStateGF.mjs's loadEnv() + createClient + parseArgs (`--dry-run` boolean, default false → so an unflagged run is LIVE; require an explicit confirmation, see below). The script performs two targeted DELETEs against supabase.schema('treasury').from('budgets'): (a) WHERE municipality_id IN COHORT AND dataset_type = 'revenue' (the deferred unsourced revenue rows, D-96-03); (b) WHERE municipality_id IN COHORT AND dataset_type = 'operating' AND fiscal_year IN (2022, 2025, 2026) (out-of-window operating estimate rows the NASBO FY2023+FY2024 load will not overwrite, Pitfall 5). Before deleting, SELECT the matching rows and print a per-state summary (abbr, dataset_type, fiscal_year, count, current source_url null/value) so the dry-run is auditable. In dry-run mode print the summary and exit without writing. In live mode, gate the actual DELETE behind an explicit `--confirm` flag (refuse to delete without it, printing the dry-run summary instead) so a bare run cannot silently wipe rows. NEVER call treasury_sync_city_budget; NEVER write budgets.data_source_id (P4). Idempotent: a second run finds 0 matching rows and reports 0 deletes (P6). Exit 2 on any DB error.
  </action>
  <verify>
    <automated>node scripts/cleanupStateEstimates.mjs --dry-run</automated>
  </verify>
  <done>Dry-run prints a per-state summary of cohort revenue rows + FY2022/2025/2026 operating rows that WOULD be deleted, makes no writes, and exits 0; a bare (no --confirm) run also refuses to write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleanup script → treasury.budgets | A DELETE with a wrong municipality_id list could destroy real (MN/OH/VA/GA) sourced rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Denial of service / data loss | DELETE predicate municipality_id IN COHORT | mitigate | COHORT is the explicit 46-ID list from RESEARCH (MN/OH/VA/GA excluded by construction); dry-run + `--confirm` gate; per-state summary printed before any write |
| T-96-05 | Information disclosure (ground-rule breach) | leftover unsourced revenue rows | mitigate | This script is the control that removes the displayed unsourced revenue rows (D-96-03); Plan 07 verifies 0 revenue rows remain for the cohort |
| T-96-06 | Tampering (wrong write path) | DB write | mitigate | Targeted `from('budgets').delete()` only; never treasury_sync_city_budget, never data_source_id (P4) — reviewer confirms by grep |
| T-96-SC | Tampering | npm installs | mitigate | No package installs (RESEARCH §"No New Packages") |
</threat_model>

<verification>
- `node scripts/cleanupStateEstimates.mjs --dry-run` — prints summary, 0 writes, exit 0.
- Grep confirms the script contains NO `treasury_sync_city_budget` and NO `data_source_id`.
- Grep confirms exactly 46 UUIDs in the COHORT list and that none match the MN/OH/VA/GA IDs (those four are not in RESEARCH's cohort table).
- `--confirm` gate present (bare live run refuses to delete).
</verification>

<success_criteria>
A dry-run-first, idempotent cleanup script exists that targets only the 46 cohort states' revenue rows and out-of-window (FY2022/2025/2026) operating estimate rows, via targeted DELETE, with a confirmation gate and never the unsafe sync path. Not yet run live.
</success_criteria>

<output>
Create `.planning/phases/96-remaining-states-sgfs-04/96-02-SUMMARY.md` when done.
</output>
