---
phase: 96-remaining-states-sgfs-04
plan: 03
type: execute
wave: 1
depends_on: ["96-01"]
files_modified:
  - scripts/loadStateGF.mjs
autonomous: true
requirements: [SGFS-04]
must_haves:
  truths:
    - "STATES contains FY2023 + FY2024 actual entries for the 12 Batch-A states (AK AL AR AZ CA CO CT DE FL HI IA ID)"
    - "Every Batch-A state-year uses the 6-function 2025-SER taxonomy (no Public Assistance line)"
    - "Every Batch-A state-year passes the dual checksum on --dry-run (function-sum ties Table 1 GF within 0.5%)"
  artifacts:
    - path: "scripts/loadStateGF.mjs"
      provides: "Batch-A state-year data objects in STATES"
      contains: "Alabama"
  key_links:
    - from: "scripts/loadStateGF.mjs STATES"
      to: "validateAgainstControl"
      via: "controlTotalGF cross-check per state-year"
      pattern: "controlTotalGF"
---

<objective>
Transcribe NASBO 2025 SER General Fund spending-by-function actuals (FY2023 + FY2024) for the first 12 cohort states into the `STATES` object in `loadStateGF.mjs`, and prove every state-year passes the dual checksum via `--dry-run`. No production writes.

Batch A (12 states): AK, AL, AR, AZ, CA, CO, CT, DE, FL, HI, IA, ID.

Purpose: This is the data-entry core of the phase. Each state-year is six GF function values + a Table-1 GF control total, transcribed from the 2025 SER via `pdftotext -table`, and gated by the loader's `validateAgainstControl()` (function-sum must tie Table 1 GF). The Phase-94 Georgia load (total $29.266B, 0.03% tie, 0-NULL) is the quality bar.
Output: 12 states × 2 FYs = 24 validated state-year entries in STATES; all dry-run checksums PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/96-remaining-states-sgfs-04/96-CONTEXT.md
@.planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md
@.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md

<interfaces>
<!-- STATES entry shape (one per state). Alabama is fully worked in RESEARCH §"State entry structure". -->

STATES[ABBR] = {
  name: '<State>', abbr: 'ABBR', population: <int>,
  operating: {
    2023: { confidence: 'actual', controlTotalGF: <Table1 GF $>, categories: [ {name,total} ×6 ] },
    2024: { confidence: 'actual', controlTotalGF: <Table1 GF $>, categories: [ {name,total} ×6 ] },
  },
};

The 6 categories (2025 SER, verbatim names, NO Public Assistance):
  Elementary & Secondary Education, Higher Education, Medicaid, Corrections, Transportation, All Other

DB IDs + names + FY-ends for all 12 Batch-A states: 96-RESEARCH.md §"The 46-State Cohort" table.
NASBO PDF: cache/nasbo-2025-ser.pdf (already downloaded). Extract with: pdftotext -table.
Table line-number map (Table 1 GF control + T5/T9/T13/T16/T21/T26 per-function): RESEARCH §"Extraction Technique".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract + transcribe Batch-A states (AK AL AR AZ CA CO) FY2023+FY2024</name>
  <files>scripts/loadStateGF.mjs</files>
  <read_first>
    - scripts/loadStateGF.mjs (the STATES object + the GA entry as the structural template; validateAgainstControl)
    - .planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md §"Extraction Technique — pdftotext -table" (table line map + column order: GF is the FIRST numeric column per FY group), §"State entry structure (6-function, 2025 SER)" (the fully-worked Alabama example), §"Critical Taxonomy Change" (no Public Assistance), Pitfalls 1/2/3 (always -table; never the FY2025 estimated column; 6 categories not 7)
    - cache/nasbo-2025-ser.pdf (the source PDF)
  </read_first>
  <action>
    Run `pdftotext -table cache/nasbo-2025-ser.pdf` and locate Table 1 (GF control totals) + the six per-function tables (T5 Elementary & Secondary Education, T9 Higher Education, T13 Medicaid, T16 Corrections, T21 Transportation, T26 All Other) using the line-number map in RESEARCH §"Extraction Technique". For each of AK, AL, AR, AZ, CA, CO: read the state's row in each function table, taking the GENERAL FUND column (the first numeric column) for the Actual FY2023 group, then again for the Actual FY2024 group — NEVER the Estimated FY2025 group (Pitfall 2). Read the same state's Table 1 General Fund value for FY2023 and FY2024 as controlTotalGF. Add each state to STATES using the 6-function structure (no Public Assistance line — Pitfall 3), verbatim category names, population from RESEARCH's cohort table. Alabama's verified figures are already in RESEARCH §"State entry structure" — use them directly. After adding these 6 states, run the dry-run for each (below); each function-sum must tie Table 1 GF within 0.5% (in practice 0 diff). Sanity-check magnitudes: California GF is the largest in the cohort; a value 2×/0.5× off the old round-number estimate signals a misread (Pitfall 1) — re-extract.
  </action>
  <verify>
    <automated>node scripts/loadStateGF.mjs --state AL --dry-run; node scripts/loadStateGF.mjs --state CA --dry-run</automated>
  </verify>
  <done>AK AL AR AZ CA CO each have FY2023+FY2024 entries; `--dry-run` for each prints tie:PASS for both FYs; no Public Assistance line; no FY2025 data.</done>
</task>

<task type="auto">
  <name>Task 2: Extract + transcribe Batch-A states (CT DE FL HI IA ID) + batch dry-run sweep</name>
  <files>scripts/loadStateGF.mjs</files>
  <read_first>
    - scripts/loadStateGF.mjs (STATES — append after Task 1's additions; validateAgainstControl)
    - .planning/phases/96-remaining-states-sgfs-04/96-RESEARCH.md §"Extraction Technique", §"The 46-State Cohort" (DB IDs + populations for CT DE FL HI IA ID), Pitfalls 1/2/3
    - cache/nasbo-2025-ser.pdf
  </read_first>
  <action>
    Repeat the Task-1 extraction for CT, DE, FL, HI, IA, ID: GF column, FY2023 + FY2024 actual groups only, six categories, Table 1 GF as controlTotalGF, population from the cohort table. Add each to STATES. Then run a full Batch-A dry-run sweep (all 12 states, both FYs = 24 state-years) per the per-batch sampling rate in 96-VALIDATION.md — every state-year must print tie:PASS before this plan is done. Any FAIL → re-extract that state-year from the PDF; never load an unvalidated figure (P5/D-96-04).
  </action>
  <verify>
    <automated>node scripts/loadStateGF.mjs --state AK --dry-run && node scripts/loadStateGF.mjs --state AL --dry-run && node scripts/loadStateGF.mjs --state AR --dry-run && node scripts/loadStateGF.mjs --state AZ --dry-run && node scripts/loadStateGF.mjs --state CA --dry-run && node scripts/loadStateGF.mjs --state CO --dry-run && node scripts/loadStateGF.mjs --state CT --dry-run && node scripts/loadStateGF.mjs --state DE --dry-run && node scripts/loadStateGF.mjs --state FL --dry-run && node scripts/loadStateGF.mjs --state HI --dry-run && node scripts/loadStateGF.mjs --state IA --dry-run && node scripts/loadStateGF.mjs --state ID --dry-run</automated>
  </verify>
  <done>All 12 Batch-A states present; the sweep prints tie:PASS for all 24 state-years; offline `node --test scripts/loadStateGF.test.mjs` still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NASBO SER PDF → STATES data objects | Hand-transcribed figures; a transposition or wrong-column read fabricates a state total |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07 | Tampering (data integrity) | transcribed GF function values | mitigate | Dual checksum (function-sum vs Table 1 GF) gates every state-year on --dry-run; FAIL = not loaded (P5) |
| T-96-08 | Tampering (wrong-year load) | FY2025 estimated column | mitigate | Parse by labeled FY group, not column position (Pitfall 2); only FY2023+FY2024 entered |
| T-96-09 | Tampering (fabricated category) | Public Assistance line | mitigate | 6-function taxonomy only (Pitfall 3); checksum closes without a PA line |
| T-96-SC | Tampering | npm installs | mitigate | No package installs this phase |
</threat_model>

<verification>
- Per-state `--dry-run` prints tie:PASS for FY2023 and FY2024 for all 12 Batch-A states.
- No STATES entry for these states contains a "Public Assistance" category.
- No FY2025 key appears under any state's `operating`.
- `node --test scripts/loadStateGF.test.mjs` green.
</verification>

<success_criteria>
The 12 Batch-A states each carry validated FY2023 + FY2024 NASBO operating entries in STATES (6-function taxonomy, dual-checksum PASS), matching the Georgia quality bar. No production writes.
</success_criteria>

<output>
Create `.planning/phases/96-remaining-states-sgfs-04/96-03-SUMMARY.md` when done.
</output>
