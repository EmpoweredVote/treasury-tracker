---
phase: 31-anaheim-santa-ana-ca-data-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedAnaheimSantaAnaCA.js
autonomous: true
requirements: [POPUL-02]
must_haves:
  truths:
    - "Anaheim municipality row exists in DB with state=CA, population~344000, population_year=2024, county_id NULL"
    - "Santa Ana municipality row exists in DB with state=CA, population~312000, population_year=2024, county_id NULL"
    - "Four canonical data_source rows exist (Anaheim GF operating + revenue, Santa Ana GF operating + revenue)"
    - "treasury_list_source_ids RPC returns all four canonical data_source names"
  artifacts:
    - path: "scripts/seedAnaheimSantaAnaCA.js"
      provides: "Idempotent two-city seeder for Anaheim + Santa Ana municipalities and 4 data_source rows"
      contains: "Santa Ana General Fund Operating Budget"
  key_links:
    - from: "scripts/seedAnaheimSantaAnaCA.js"
      to: "treasury_list_source_ids"
      via: "RPC verification block asserting all four expected names"
      pattern: "treasury_list_source_ids"
    - from: "scripts/seedAnaheimSantaAnaCA.js"
      to: "treasury.municipalities"
      via: "upsertMunicipality by name+state"
      pattern: "upsertMunicipality"
---

<objective>
Seed Anaheim and Santa Ana CA municipality rows (population + population_year) and four canonical data_source rows (General Fund operating + revenue per city), then verify all four data_source names resolve via the treasury_list_source_ids RPC.

This implements POPUL-02 (both cities seeded with 2024 population data) and creates the data_source contract rows that Plan 02 (Anaheim processor) and Plan 03 (Santa Ana processor) look up by exact name.

Purpose: Establish the DB foundation so the per-city PDF processors have municipality IDs and correctly-named data_source rows to write budget trees against.
Output: scripts/seedAnaheimSantaAnaCA.js; two municipality rows + four data_source rows in the DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md
@scripts/seedFresnoRiversideCA.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/seedAnaheimSantaAnaCA.js (two-city seeder)</name>
  <read_first>
    - scripts/seedFresnoRiversideCA.js (canonical analog — copy structure verbatim, change city names/IDs/values)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md (seedAnaheimSantaAnaCA.js section, lines 23-124 — exact payload shapes)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md (Population Data section + Seeder Template code example)
  </read_first>
  <action>
    Create scripts/seedAnaheimSantaAnaCA.js by adapting scripts/seedFresnoRiversideCA.js. Copy verbatim: the import block, the loadEnv() function (reads ../.env.local then ../.env, skips ENOENT silently, never logs key values), the Supabase client init (SUPABASE_URL fallback + SUPABASE_SERVICE_KEY/SUPABASE_SERVICE_ROLE_KEY, exit(2) if missing), upsertMunicipality() (SELECT by name+state then INSERT-or-UPDATE preserving id/created_at), and upsertDataSourceByName() (SELECT by name then INSERT-or-UPDATE).

    Set the MUNICIPALITIES array to two entries:
      1. name='Anaheim', state='CA', entity_type='city', population=344000, population_year=2024 (county_id omitted — stays NULL; Orange County not loaded, per Pitfall 7 in RESEARCH.md).
      2. name='Santa Ana', state='CA', entity_type='city', population=312000, population_year=2024 (county_id omitted — stays NULL).
    These population values are the round figures from the Census sub-est2024_06.csv 2024 annual estimates (Anaheim 344,521; Santa Ana 312,534) — NOT the ~348K/~335K approximations in REQUIREMENTS.md (per Pitfall 5 in RESEARCH.md).

    Define the four data_source rows with these EXACT names (these are the contract that Plan 02/03 processors look up — must match character-for-character):
      - name='Anaheim General Fund Operating Budget', api_type='pdf_download', dataset_type='operating', dataset_id='anaheim-gf-operating', base_url='https://www.anaheim.net/271/Operating-Budget-CIP', municipality_id=anaheimId
      - name='Anaheim General Fund Revenue Budget', api_type='pdf_download', dataset_type='revenue', dataset_id='anaheim-gf-revenue', base_url='https://www.anaheim.net/271/Operating-Budget-CIP', municipality_id=anaheimId
      - name='Santa Ana General Fund Operating Budget', api_type='pdf_download', dataset_type='operating', dataset_id='santa-ana-gf-operating', base_url='https://www.santa-ana.org/budget/', municipality_id=santaAnaId
      - name='Santa Ana General Fund Revenue Budget', api_type='pdf_download', dataset_type='revenue', dataset_id='santa-ana-gf-revenue', base_url='https://www.santa-ana.org/budget/', municipality_id=santaAnaId
    Note: base_url is NOT NULL on the data_sources table (discovered in Phase 30 Plan 01 — include it on every row).

    Set the main() flow: upsert Anaheim → capture anaheimId; upsert Santa Ana → capture santaAnaId; build the dataSources array with both IDs; loop upsertDataSourceByName over the four; then run the verification block (Task 2 covers running it).

    Set the expectedNames verification array to the four exact data_source names above. Do not insert an Orange County municipality row.
  </action>
  <verify>
    <automated>node -c scripts/seedAnaheimSantaAnaCA.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/seedAnaheimSantaAnaCA.js` exits 0 (valid syntax)
    - File contains the literal strings "Anaheim General Fund Operating Budget", "Anaheim General Fund Revenue Budget", "Santa Ana General Fund Operating Budget", "Santa Ana General Fund Revenue Budget"
    - MUNICIPALITIES array sets population=344000 for Anaheim and population=312000 for Santa Ana, both with population_year=2024
    - No county_id is assigned to either municipality (field omitted or explicitly NULL)
    - File contains a loadEnv() function and never logs SUPABASE_SERVICE_KEY value
    - File calls treasury_list_source_ids RPC in a verification block
  </acceptance_criteria>
  <done>scripts/seedAnaheimSantaAnaCA.js exists, passes node -c, contains all four canonical data_source names and correct population values, and includes the treasury_list_source_ids verification block.</done>
</task>

<task type="auto">
  <name>Task 2: Run seeder live + verify all four data_source rows in DB</name>
  <read_first>
    - scripts/seedAnaheimSantaAnaCA.js (the file written in Task 1)
    - scripts/seedFresnoRiversideCA.js (reference for expected console output: "inserted"/"updated existing" + OK lines)
  </read_first>
  <action>
    Run `node scripts/seedAnaheimSantaAnaCA.js`. Confirm it exits 0, upserts both municipality rows (Anaheim + Santa Ana), upserts all four data_source rows, and the treasury_list_source_ids verification block prints an OK line for each of the four expected names. If the seeder exits non-zero due to a missing NOT NULL column (as happened in Phase 30 with base_url), add the required field using the official city budget page URLs already specified and re-run. Then run the seeder a SECOND time to confirm idempotency — the second run must show "updated existing" (or equivalent) for all rows rather than creating duplicates.
  </action>
  <verify>
    <automated>node scripts/seedAnaheimSantaAnaCA.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/seedAnaheimSantaAnaCA.js` exits 0
    - Console output shows an OK / verified line for all four data_source names from treasury_list_source_ids
    - A second consecutive run exits 0 and shows update-in-place behavior (no duplicate rows created)
    - Anaheim and Santa Ana municipality rows are present in treasury.municipalities with state=CA after the run
  </acceptance_criteria>
  <done>Seeder runs clean (exit 0), all four data_source names verified via treasury_list_source_ids, idempotency confirmed by a clean second run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase | Service-role key used to write municipality + data_source rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information disclosure | SUPABASE_SERVICE_KEY in seeder | mitigate | loadEnv() loads key from .env; key value never written to console (copy verbatim from seedFresnoRiversideCA.js) |
| T-31-02 | Tampering / integrity | data_source name mismatch | mitigate | treasury_list_source_ids verification block asserts all four canonical names exist; exit non-zero on MISSING (Pitfall 6) |
| T-31-SC | Tampering | npm/pip installs | mitigate | No package installs in this plan; all dependencies already present (RESEARCH.md Package Legitimacy Audit — no new packages) |
</threat_model>

<verification>
- `node -c scripts/seedAnaheimSantaAnaCA.js` exits 0
- `node scripts/seedAnaheimSantaAnaCA.js` exits 0 with four OK verification lines
- Second consecutive run is idempotent (no duplicates)
</verification>

<success_criteria>
Anaheim and Santa Ana municipality rows seeded (correct 2024 population, county_id NULL); four canonical data_source rows created and verified via treasury_list_source_ids; seeder is idempotent. POPUL-02 population foundation in place.
</success_criteria>

<output>
Create `.planning/phases/31-anaheim-santa-ana-ca-data-load/31-01-SUMMARY.md` when done.
</output>
