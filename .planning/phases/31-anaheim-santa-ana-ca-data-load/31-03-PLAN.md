---
phase: 31-anaheim-santa-ana-ca-data-load
plan: 03
type: execute
wave: 3
depends_on: ["31-01", "31-02"]
files_modified:
  - scripts/extractSantaAna.py
  - scripts/processSantaAna.js
  - docs/Santa Ana/
autonomous: true
requirements: [DATA-09]
must_haves:
  truths:
    - "Santa Ana General Fund operating budget loaded for at least one fiscal year (target FY2022-23 through FY2025-26)"
    - "Santa Ana enterprise funds (Water, Sewer, Refuse, Sanitation, Parking, Transportation Center, Federal Clean Water Protection) excluded at extraction time"
    - "Santa Ana operating total within sanity band ~$350M-$450M (General Fund scope, not ~$734M all-funds)"
    - "Santa Ana revenue loaded if a clean GF revenue section exists, else deferred per best-effort pattern"
  artifacts:
    - path: "scripts/extractSantaAna.py"
      provides: "pdfplumber extractor for Santa Ana adopted budget PDFs; extraction-time General Fund filter; full dollars"
      contains: "General Fund"
    - path: "scripts/processSantaAna.js"
      provides: "Node.js processor; $350M-$450M sanity band; resolvePdfDir() worktree-safe; 16MB maxBuffer; treasury_sync_budget_tree RPC loader"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/processSantaAna.js"
      to: "scripts/extractSantaAna.py"
      via: "execSync python call"
      pattern: "extractSantaAna\\.py"
    - from: "scripts/processSantaAna.js"
      to: "treasury_sync_budget_tree"
      via: "RPC per fiscal year"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Load Santa Ana's General Fund operating budget into the DB. Download Santa Ana adopted budget PDFs into docs/Santa Ana/, write extractSantaAna.py (pdfplumber, General-Fund-only filter at extraction time, handles inconsistent filenames) and processSantaAna.js (sanity band + 16MB maxBuffer + treasury_sync_budget_tree RPC loader), verify with --dry-run, then live-load. Best-effort revenue per the established pattern.

This implements DATA-09 (Santa Ana CA operating + revenue budget loaded and visible; General Fund scope; enterprise funds filtered).

Runs in Wave 3 (after Plan 02 Anaheim) so the single-year Anaheim pipeline confirms the pattern before tackling Santa Ana's larger (~19-20MB) PDFs and inconsistent filenames.

Purpose: Santa Ana becomes selectable in the app with correct General Fund totals (not enterprise-inflated all-funds totals).
Output: scripts/extractSantaAna.py, scripts/processSantaAna.js, docs/Santa Ana/*.pdf; Santa Ana GF operating budget_items rows in DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-01-SUMMARY.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-02-SUMMARY.md
@scripts/extractFresno.py
@scripts/processFresno.js
@scripts/processRiverside.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Santa Ana PDFs + inspect General Fund section structure</name>
  <read_first>
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md (Santa Ana Budget Profile table, Santa Ana PDF Direct Download URLs table, Open Question 2, Pitfall 3, Anti-Pattern "Adopted not Proposed")
    - scripts/extractFresno.py (analog — structural facts the extractor needs)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-02-SUMMARY.md (Anaheim Task 1 findings — the structural-inspection approach to replicate)
  </read_first>
  <action>
    Create the docs/Santa Ana/ directory (with the space in the name — must match resolvePdfDir's 'Santa Ana' string). Download the ADOPTED budget book PDF (NOT the Proposed version — see Anti-Pattern in RESEARCH.md) for each available fiscal year from the Santa Ana GCS Direct Download URLs in RESEARCH.md. Target FY2022-23 through FY2025-26 as primary depth (the four confirmed GCS URLs); FY2020-21 and FY2021-22 are stretch via the santa-ana.org archive pages. Santa Ana filenames are inconsistent (V6, V13, V26 version suffixes — Pitfall 3); save each downloaded PDF with a normalized fy-prefixed filename so the FY detector can parse it (e.g., fy2024-25-adopted-budget.pdf using the four-digit start year).

    Then inspect the FY2024-25 PDF with pdfplumber: identify (a) the EXACT General Fund section label / GF total row string, (b) the enterprise section-boundary string(s) (Water, Sewer, Refuse Collections, Sanitation, Parking, Transportation Center, Federal Clean Water Protection), (c) the adopted-amount column position, and (d) the amount scale (spot-check one department against the ~$407M FY2024-25 GF total). Record these four facts in the SUMMARY. Note the PDFs are ~19-20MB — pdfplumber reading them in Python is fine, but the processor's execSync maxBuffer must be raised for the JSON output (Task 2 handles this).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='docs/Santa Ana';const pdfs=fs.existsSync(d)?fs.readdirSync(d).filter(f=>f.toLowerCase().endsWith('.pdf')):[];if(pdfs.length<1){console.error('No Santa Ana PDFs found');process.exit(1)}console.log(pdfs.join('\n'))"</automated>
  </verify>
  <acceptance_criteria>
    - docs/Santa Ana/ directory exists (with the space) and contains at least one ADOPTED budget book PDF (not a Proposed version)
    - At least the FY2024-25 PDF is present, named with a normalized fy-prefixed filename
    - SUMMARY records the four structural facts: General Fund section/total label string, enterprise section boundary string(s), adopted-amount column position, and amount scale
    - The downloaded document is the comprehensive adopted budget book (department-level detail), confirmed by inspection showing multiple GF department line items
  </acceptance_criteria>
  <done>docs/Santa Ana/ contains the adopted budget book PDFs (FY2024-25 plus available years); the General Fund section label, enterprise stop boundary, adopted-column position, and amount scale are documented for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write extractSantaAna.py + processSantaAna.js; dry-run confirms GF totals within band</name>
  <read_first>
    - scripts/extractFresno.py (primary extractor analog — single-year pattern; copy parse_money, GF section filter, row output shape, dedup, stderr/stdout discipline)
    - scripts/processFresno.js (processor analog — loadEnv, resolvePdfDir, buildTree, ensureMunicipality, upsertDataSource, loadFiscalYear, sanity band halt, main parseArgs)
    - scripts/processRiverside.js (secondary processor analog cited in PATTERNS.md — same single-FY approach applies; do NOT copy its biennial FY-detection code)
    - docs/Santa Ana/*.pdf (downloaded PDFs — must match Task 1 structural facts)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md (extractSantaAna.py + processSantaAna.js sections, lines 315-393 — including the 16MB maxBuffer note and the multi-pattern detect_fy_from_filename)
  </read_first>
  <action>
    Write scripts/extractSantaAna.py adapting scripts/extractFresno.py (single-year — do NOT use Riverside biennial logic). Copy verbatim: parse_money() (Fresno variant with space-in-number handling and [1-9] leading-digit guard), the row output shape (each row = {department, fund:'General Fund', adopted_amount, fiscal_year, page_num}), (department, fiscal_year) dedup, argparse main with positional pdf_path + --mode operating|revenue. Use the MULTI-PATTERN detect_fy_from_filename from PATTERNS.md lines 325-339 (handles fy25-26, fy2025-26, and /20NN/ path-component fallback; returns the ENDING year — FY2024-25 → 2025). Implement the extraction-time fund filter using the EXACT General Fund section label and enterprise section-boundary strings recorded in Task 1 (Water, Sewer, Refuse Collections, Sanitation, Parking, Transportation Center, Federal Clean Water Protection) — only emit General Fund rows; log every skipped non-GF boundary to sys.stderr. If Task 1 found thousands scale, apply ×1000. ONLY the final JSON array to stdout; all diagnostics to stderr. For --mode revenue: extract a clean GF revenue/sources section if present, else emit an empty JSON array (deferred — best-effort).

    Write scripts/processSantaAna.js adapting scripts/processFresno.js. Set GF_BAND_MIN=350_000_000 and GF_BAND_MAX=450_000_000 (Santa Ana GF ~$404M-$424M across FY2022-26, per RESEARCH.md). Set resolvePdfDir() to use 'Santa Ana' (with the space; git-common-dir worktree fallback copied verbatim). Set extractPDF() to call extractSantaAna.py with maxBuffer = 16*1024*1024 (raised from 8MB because Santa Ana PDFs are ~19-20MB — per PATTERNS.md lines 375-387) and double-quoted paths. Set ensureMunicipality() to SELECT name='Santa Ana', state='CA' and exit(2) with "run seedAnaheimSantaAnaCA.js first" if missing. Set upsertDataSource() to use the 'santa-ana' prefix in dataset_id/name/base_url. Keep the sanity band halt (process.exit(3) before any DB write). Keep loadFiscalYear() calling treasury_sync_budget_tree with all seven params. Keep main parseArgs with --dry-run, --pdf, --revenue.

    Run `node scripts/processSantaAna.js --dry-run` and confirm each FY's GF operating total lands within $350M-$450M (FY2024-25 ≈ $407M). If the total is ~1.5-2x expected, the filter is leaking enterprise rows — fix filter strings, do NOT widen the band. If the band is verifiably wrong against correct extracted data, adjust it and document in the SUMMARY with original values in a comment.
  </action>
  <verify>
    <automated>node scripts/processSantaAna.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/processSantaAna.js` exits 0 and `python -c "import ast; ast.parse(open('scripts/extractSantaAna.py').read())"` exits 0
    - processSantaAna.js sets maxBuffer to 16*1024*1024 (16MB) on the extractPDF execSync call
    - extractSantaAna.py emits only fund='General Fund' rows; non-GF boundaries logged to stderr
    - `node scripts/processSantaAna.js --dry-run` exits 0 with at least one fiscal year and every printed GF operating total inside $350M-$450M (FY2024-25 ≈ $407M)
    - Running extractSantaAna.py directly produces valid JSON on stdout with no diagnostic text mixed in
  </acceptance_criteria>
  <done>extractSantaAna.py and processSantaAna.js exist and pass syntax checks; --dry-run shows GF operating totals within $350M-$450M for all available fiscal years; enterprise funds filtered at extraction time; 16MB maxBuffer set.</done>
</task>

<task type="auto">
  <name>Task 3: Live-load Santa Ana GF operating (+ revenue if available)</name>
  <read_first>
    - scripts/processSantaAna.js (the file written in Task 2)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-03-PLAN.md Task 2 (band expectations)
  </read_first>
  <action>
    Run `node scripts/processSantaAna.js` (no --dry-run) to live-load Santa Ana General Fund operating data for all available fiscal years via treasury_sync_budget_tree. Confirm each FY reports "Inserted: N rows" and exits 0. Then run `node scripts/processSantaAna.js --revenue`: load rows if returned, else document revenue as deferred (not a gap — best-effort per RESEARCH.md). Run the operating load a SECOND time to confirm idempotency (same inserted row counts).
  </action>
  <verify>
    <automated>node scripts/processSantaAna.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processSantaAna.js` exits 0 and reports "Inserted: N rows" for at least one fiscal year
    - All loaded GF operating totals are within $350M-$450M (no exit(3) halt)
    - A second consecutive operating run exits 0 with the same per-FY inserted row counts (idempotent)
    - Revenue is either loaded or explicitly documented as deferred in the SUMMARY with the reason
  </acceptance_criteria>
  <done>Santa Ana General Fund operating budget loaded into the DB for all available fiscal years (idempotent); revenue loaded or documented as deferred. DATA-09 operating scope satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file → extractor | Untrusted PDF content parsed by pdfplumber |
| extractor stdout → processor | JSON crosses Python→Node via execSync |
| processor → Supabase | Service-role key writes budget_items via RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-08 | Tampering / integrity | Enterprise fund bleed (Santa Ana ~$327M non-GF) | mitigate | Extraction-time General Fund filter in extractSantaAna.py + $350M-$450M sanity band halt (exit 3) before DB write |
| T-31-09 | Tampering / integrity | Wrong amount scale (thousands vs full dollars) | mitigate | Task 1 spot-checks scale against ~$407M FY2024-25 GF; ×1000 applied if needed; sanity band catches gross errors |
| T-31-10 | Denial of service | Large PDF (~19-20MB) stdout exceeds execSync buffer | mitigate | maxBuffer raised to 16MB on extractPDF execSync (PATTERNS.md) |
| T-31-11 | Tampering | Command injection via PDF path | mitigate | PDF paths from readdirSync(pdfDir) controlled directory, double-quoted in execSync |
| T-31-12 | Tampering / integrity | Wrong document loaded (Proposed not Adopted, or wrong FY from inconsistent filename) | mitigate | Use only confirmed Adopted GCS URLs; normalize filenames; FY verified against PDF header during Task 1 inspection |
| T-31-13 | Information disclosure | SUPABASE_SERVICE_KEY logging | mitigate | loadEnv() pattern; key never logged |
| T-31-SC | Tampering | npm/pip installs | mitigate | No package installs; pdfplumber + @supabase/supabase-js already present (RESEARCH.md audit) |
</threat_model>

<verification>
- `node -c scripts/processSantaAna.js` and Python AST parse of extractSantaAna.py both exit 0
- processSantaAna.js uses 16MB maxBuffer
- `node scripts/processSantaAna.js --dry-run` shows all GF operating totals within $350M-$450M
- `node scripts/processSantaAna.js` live-loads at least one FY, idempotent on second run
- Enterprise funds filtered at extraction time; revenue loaded or deferred
</verification>

<success_criteria>
Santa Ana General Fund operating budget loaded for at least one fiscal year (target FY2022-23 through FY2025-26); enterprise funds excluded at extraction time; totals within band (FY2024-25 ≈ $407M); revenue loaded or documented as deferred. DATA-09 operating scope satisfied.
</success_criteria>

<output>
Create `.planning/phases/31-anaheim-santa-ana-ca-data-load/31-03-SUMMARY.md` when done.
</output>
