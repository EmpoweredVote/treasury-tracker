---
phase: 31-anaheim-santa-ana-ca-data-load
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - scripts/extractAnaheim.py
  - scripts/processAnaheim.js
  - docs/Anaheim/
autonomous: true
requirements: [DATA-08]
must_haves:
  truths:
    - "Anaheim General Fund operating budget loaded for at least one fiscal year (target FY2022-23 through FY2025-26)"
    - "Anaheim enterprise funds (Public Utilities, Convention/Sports, golf) excluded at extraction time"
    - "Anaheim operating total within sanity band ~$350M-$550M (General Fund scope, not ~$2.3B all-funds)"
    - "Anaheim revenue loaded if a clean GF revenue section exists, else deferred per best-effort pattern"
  artifacts:
    - path: "scripts/extractAnaheim.py"
      provides: "pdfplumber extractor for Anaheim adopted budget PDFs; extraction-time General Fund filter; full dollars"
      contains: "General Fund"
    - path: "scripts/processAnaheim.js"
      provides: "Node.js processor; $350M-$550M sanity band; resolvePdfDir() worktree-safe; treasury_sync_budget_tree RPC loader"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/processAnaheim.js"
      to: "scripts/extractAnaheim.py"
      via: "execSync python call"
      pattern: "extractAnaheim\\.py"
    - from: "scripts/processAnaheim.js"
      to: "treasury_sync_budget_tree"
      via: "RPC per fiscal year"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Load Anaheim's General Fund operating budget into the DB. Download Anaheim adopted budget PDFs into docs/Anaheim/, write extractAnaheim.py (pdfplumber, General-Fund-only filter at extraction time) and processAnaheim.js (sanity band + treasury_sync_budget_tree RPC loader), verify with --dry-run, then live-load. Best-effort revenue per the established pattern (extract if a clean GF revenue section exists; defer otherwise).

This implements DATA-08 (Anaheim CA operating + revenue budget loaded and visible; General Fund scope; enterprise utility funds filtered).

Purpose: Anaheim becomes selectable in the app with correct General Fund totals (not enterprise-inflated all-funds totals).
Output: scripts/extractAnaheim.py, scripts/processAnaheim.js, docs/Anaheim/*.pdf; Anaheim GF operating budget_items rows in DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md
@.planning/phases/31-anaheim-santa-ana-ca-data-load/31-01-SUMMARY.md
@scripts/extractFresno.py
@scripts/processFresno.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Anaheim PDFs + inspect General Fund section structure</name>
  <read_first>
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-RESEARCH.md (Anaheim Budget Profile table, Anaheim PDF Archive URLs table, Open Question 1, Pitfall 4)
    - scripts/extractFresno.py (analog — to know what structural facts the extractor needs: section header string, stop boundary, column order, amount scale)
  </read_first>
  <action>
    Create the docs/Anaheim/ directory. Download the FULL adopted budget PDF (NOT the Budget In Brief summary — see Pitfall 4) for each available fiscal year from the Anaheim Archive.aspx URLs in RESEARCH.md. Target FY2022-23 through FY2025-26 as primary depth (ADID=876, 907, 926, 964); FY2020-21 and FY2021-22 (ADID=821, 843) are stretch if the format is consistent. Each Archive.aspx?ADID=NNN URL serves the full adopted budget PDF; if a URL returns an HTML page rather than a PDF, navigate it to find the actual PDF download link. Save each PDF with an fy-prefixed filename so detect_fy_from_filename can parse it (e.g., fy2024-25-adopted-budget.pdf — use the four-digit start year so the existing fy(\d{4})- regex works).

    Then inspect the FY2024-25 PDF with pdfplumber (first ~15 pages and any "Appropriations Summary" / "General Fund" pages): identify (a) the EXACT string that labels the General Fund section or the General Fund total/subtotal row, (b) the section-boundary string(s) where enterprise funds begin (Public Utilities, Convention/Sports & Entertainment, Golf), (c) the column order (which column is the adopted amount for the budget year), and (d) the amount scale (full dollars vs thousands — spot-check one known department against the ~$491M FY2024-25 GF total: if Police shows ~$200 instead of ~$200,000,000, amounts are in thousands). Record these four facts in the SUMMARY — they drive Task 2's filter logic.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='docs/Anaheim';const pdfs=fs.existsSync(d)?fs.readdirSync(d).filter(f=>f.toLowerCase().endsWith('.pdf')):[];if(pdfs.length<1){console.error('No Anaheim PDFs found');process.exit(1)}console.log(pdfs.join('\n'))"</automated>
  </verify>
  <acceptance_criteria>
    - docs/Anaheim/ directory exists and contains at least one full adopted budget PDF (not a Budget In Brief summary)
    - At least the FY2024-25 PDF is present, named with an fy-prefixed filename parseable by detect_fy_from_filename
    - SUMMARY records the four structural facts: General Fund section/total label string, enterprise section boundary string(s), adopted-amount column position, and amount scale (full dollars vs thousands)
    - Each downloaded PDF is a department-detail document (not a ~400KB summary): inspection shows multiple General Fund department line items, not just 2-3 summary rows (Pitfall 4)
  </acceptance_criteria>
  <done>docs/Anaheim/ contains the full adopted budget PDFs (FY2024-25 plus available years); the General Fund section label, enterprise stop boundary, adopted-column position, and amount scale are documented for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write extractAnaheim.py + processAnaheim.js; dry-run confirms GF totals within band</name>
  <read_first>
    - scripts/extractFresno.py (primary extractor analog — single-year pattern; copy parse_money, detect_fy_from_filename, GF section filter, row output shape, dedup, stderr/stdout discipline)
    - scripts/processFresno.js (primary processor analog — copy loadEnv, resolvePdfDir, extractPDF execSync, buildTree, ensureMunicipality, upsertDataSource, loadFiscalYear, sanity band halt, main parseArgs)
    - docs/Anaheim/*.pdf (the downloaded PDFs — must match the structural facts recorded in Task 1)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-PATTERNS.md (extractAnaheim.py + processAnaheim.js sections, lines 127-311)
  </read_first>
  <action>
    Write scripts/extractAnaheim.py adapting scripts/extractFresno.py (single-year, NOT the Riverside biennial pattern). Copy verbatim: parse_money() (Fresno variant with re.sub(r'[$()\s,]','',s) space-in-number handling and the [1-9] not [0-9] leading-digit guard), detect_fy_from_filename() (four-digit fy(\d{4})- preferred, two-digit fallback), the row output shape (each emitted row = {department, fund:'General Fund', adopted_amount, fiscal_year, page_num}), the (department, fiscal_year) deduplication, and the argparse main with positional pdf_path + --mode operating|revenue. Implement the extraction-time fund filter using the EXACT General Fund section label string and enterprise section-boundary string(s) recorded in Task 1 — only emit General Fund rows; log every skipped non-GF section boundary to sys.stderr. If Task 1 found amounts are in thousands, multiply adopted_amount by 1000 (toFullDollars pattern). All diagnostic output goes to stderr; ONLY the final JSON array goes to stdout. For --mode revenue: if Task 1 found a clean GF revenue/sources-of-funds section, extract it; otherwise emit an empty JSON array (revenue deferred — best-effort per RESEARCH.md, same as Phase 30 D-07).

    Write scripts/processAnaheim.js adapting scripts/processFresno.js. Set GF_BAND_MIN=350_000_000 and GF_BAND_MAX=550_000_000 (Anaheim GF ~$360M-$527M across FY2020-26, per RESEARCH.md). Set resolvePdfDir() to use 'Anaheim' (with the git-common-dir worktree fallback copied verbatim). Set extractPDF() to call extractAnaheim.py with maxBuffer 8*1024*1024 and double-quoted paths. Set ensureMunicipality() to SELECT name='Anaheim', state='CA' and exit(2) with "run seedAnaheimSantaAnaCA.js first" if missing. Set upsertDataSource() to use the 'anaheim' prefix in dataset_id/name/base_url. Keep the sanity band halt (process.exit(3) before any DB write if total outside band) with the four console.error lines copied verbatim. Keep loadFiscalYear() calling treasury_sync_budget_tree with all seven params. Keep main parseArgs with --dry-run, --pdf, --revenue.

    Run `node scripts/processAnaheim.js --dry-run` and confirm each fiscal year's GF operating total lands within the $350M-$550M band. The FY2024-25 total should be roughly ~$491M (RESEARCH.md). If the dry-run total is ~2-5x expected, the fund filter is leaking enterprise rows — fix the filter strings, do NOT widen the band. If the band itself is verifiably wrong against correct extracted data (as occurred in Phase 30), adjust the band to the verified actual range and document the change in the SUMMARY with the original values preserved in a comment.
  </action>
  <verify>
    <automated>node scripts/processAnaheim.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/processAnaheim.js` exits 0 and `python -c "import ast; ast.parse(open('scripts/extractAnaheim.py').read())"` exits 0
    - extractAnaheim.py emits only rows with fund='General Fund'; non-GF boundaries are logged to stderr (verify stderr shows [skip] lines for enterprise sections)
    - `node scripts/processAnaheim.js --dry-run` exits 0 with at least one fiscal year and every printed GF operating total inside $350M-$550M (FY2024-25 ≈ $491M)
    - Running extractAnaheim.py directly produces valid JSON on stdout with NO diagnostic text mixed in (stdout parses as a JSON array)
    - Dry-run does NOT write to the DB (no treasury_sync_budget_tree call executed under --dry-run)
  </acceptance_criteria>
  <done>extractAnaheim.py and processAnaheim.js exist and pass syntax checks; --dry-run shows GF operating totals within the sanity band for all available fiscal years; enterprise funds are filtered at extraction time (confirmed by stderr skip logs).</done>
</task>

<task type="auto">
  <name>Task 3: Live-load Anaheim GF operating (+ revenue if available)</name>
  <read_first>
    - scripts/processAnaheim.js (the file written in Task 2)
    - .planning/phases/31-anaheim-santa-ana-ca-data-load/31-02-PLAN.md Task 2 (band expectations)
  </read_first>
  <action>
    Run `node scripts/processAnaheim.js` (no --dry-run) to live-load Anaheim General Fund operating data for all available fiscal years via the treasury_sync_budget_tree RPC. Confirm each FY reports "Inserted: N rows" and exits 0. Then run `node scripts/processAnaheim.js --revenue`: if extractAnaheim.py --mode revenue returns rows, load them; if it returns an empty array, revenue is deferred (document as deferred in the SUMMARY, not a gap — best-effort per RESEARCH.md). Run the operating load a SECOND time to confirm idempotency (same inserted row counts — the RPC delete+reinserts per data_source_id+fiscal_year pair).
  </action>
  <verify>
    <automated>node scripts/processAnaheim.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processAnaheim.js` exits 0 and reports "Inserted: N rows" for at least one fiscal year
    - All loaded GF operating totals are within the $350M-$550M band (no exit(3) halt occurred)
    - A second consecutive operating run exits 0 with the same per-FY inserted row counts (idempotent)
    - Revenue is either loaded (rows inserted) or explicitly documented as deferred in the SUMMARY with the reason (no clean GF revenue section)
  </acceptance_criteria>
  <done>Anaheim General Fund operating budget loaded into the DB for all available fiscal years (idempotent); revenue loaded or documented as deferred. DATA-08 operating scope satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file → extractor | Untrusted PDF content parsed by pdfplumber |
| extractor stdout → processor | JSON crosses Python→Node via execSync |
| processor → Supabase | Service-role key writes budget_items via RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Tampering / integrity | Enterprise fund bleed (Anaheim ~$923M enterprise) | mitigate | Extraction-time General Fund filter in extractAnaheim.py (never emit non-GF rows) + $350M-$550M sanity band halt (exit 3) before DB write |
| T-31-04 | Tampering / integrity | Wrong amount scale (thousands vs full dollars) | mitigate | Task 1 spot-checks scale against ~$491M FY2024-25 GF; toFullDollars ×1000 applied if needed; sanity band catches gross scale errors |
| T-31-05 | Denial of service | Large PDF stdout exceeds execSync buffer | mitigate | maxBuffer 8MB cap on execSync (Anaheim PDFs ~9MB on disk; JSON output is small) |
| T-31-06 | Tampering | Command injection via PDF path | mitigate | PDF paths from readdirSync(pdfDir) controlled directory, double-quoted in execSync shell string |
| T-31-07 | Information disclosure | SUPABASE_SERVICE_KEY logging | mitigate | loadEnv() pattern; key never logged |
| T-31-SC | Tampering | npm/pip installs | mitigate | No package installs; pdfplumber + @supabase/supabase-js already present (RESEARCH.md audit — no new packages) |
</threat_model>

<verification>
- `node -c scripts/processAnaheim.js` and Python AST parse of extractAnaheim.py both exit 0
- `node scripts/processAnaheim.js --dry-run` shows all GF operating totals within $350M-$550M
- `node scripts/processAnaheim.js` live-loads at least one FY (Inserted: N rows), idempotent on second run
- Enterprise funds filtered at extraction time (stderr [skip] logs); revenue loaded or deferred
</verification>

<success_criteria>
Anaheim General Fund operating budget loaded for at least one fiscal year (target FY2022-23 through FY2025-26); enterprise funds excluded at extraction time; totals within band (FY2024-25 ≈ $491M); revenue loaded or documented as deferred. DATA-08 operating scope satisfied.
</success_criteria>

<output>
Create `.planning/phases/31-anaheim-santa-ana-ca-data-load/31-02-SUMMARY.md` when done.
</output>
