---
phase: 39-ma-population-state-budget-and-enrichment
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processMA.js
  - scripts/loadMaGFExcel.js
autonomous: true
requirements: [STATE-01]
must_haves:
  truths:
    - "The MA state government entity shows General Fund Expenditures sourced from real MA FY2025 enacted budget figures, not round-number estimates"
    - "Each FY in processMA.js carries a confidence value of 'enacted' or 'actual' (not 'estimated') and a cited source"
    - "Category sums validate against the FY total (validate() passes for every loaded year)"
    - "scripts/loadMaGFExcel.js is tracked in git (no longer untracked)"
  artifacts:
    - path: "scripts/processMA.js"
      provides: "MA state entity General Fund budget loader with real enacted/actual figures"
      contains: "EXPENDITURES"
    - path: "scripts/loadMaGFExcel.js"
      provides: "MA city-level GF Excel loader (committed to git as-is)"
  key_links:
    - from: "scripts/processMA.js"
      to: "treasury_sync_budget_tree"
      via: "RPC call with p_dataset_type='operating', p_triggered_by='bulk_load'"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processMA.js"
      to: "treasury.municipalities (Massachusetts, entity_type=state)"
      via: "lookup by name='Massachusetts' state='MA'"
      pattern: "Massachusetts"
---

<objective>
Upgrade the Massachusetts state government budget from hardcoded round-number estimates to real published MA General Fund figures, and commit the untracked `loadMaGFExcel.js` helper (STATE-01).

Purpose: The MA state entity currently shows `confidence: 'estimated'` round numbers (e.g., FY2025 total $36B) created by `processMA.js`. STATE-01 requires real published figures from the MA budget so citizens see authoritative state budget data, consistent with how CA state was loaded in Phase 33.
Output: Updated dollar amounts and confidence labels inside the `EXPENDITURES` constant in `scripts/processMA.js`, the MA state entity reloaded with real data, and `scripts/loadMaGFExcel.js` added to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research real MA FY2021–FY2025 General Fund figures and update processMA.js constants</name>
  <read_first>
    - scripts/processMA.js (the exact edit target — the EXPENDITURES object spans lines 27-188; validate() at lines 190-198 requires category sums to match cat.total within $1M and the category total sum to match the FY total within $10M; buildTree() at lines 200-204; main() RPC pattern at line 240; do NOT touch validate/buildTree/main structure or the category NAMES, only the dollar amounts and confidence labels)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md (processMA.js MODIFY section: explicit "What stays the same" vs "What changes" lists)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (STATE-01 section: Interpretation A is correct — update processMA.js with real figures from budget.digital.mass.gov; MA State Government Budget Known Facts; Pitfall 6; Open Question 1; Assumption A4)
  </read_first>
  <action>
    Look up real Massachusetts state government General Fund / General Appropriations Act figures for FY2021 through FY2025 from the MA budget website (https://budget.digital.mass.gov or https://www.mass.gov/lists/budget-information). Use Final GAA (enacted) or actual final-spending figures — record which you used per year. MA fiscal year ends June 30.

    Then update the dollar amounts inside `EXPENDITURES` in `scripts/processMA.js` to match the real published figures. Specifically:
    - Update each FY's top-level `total` to the real GF/GAA total for that year.
    - Distribute the real total across the EXISTING category names (Health and Human Services, Education, Local Government Aid, Debt Service, General Government, Corrections and Public Safety, Other Programs) using the real published category breakdown where available; where a published sub-breakdown is unavailable, allocate proportionally and document the allocation basis in a comment. Do NOT rename, add, or remove categories — only change amounts.
    - Ensure each category's `lineItems` amounts sum to that category's `total` within $1,000,000 (validate() tolerance), and all category totals sum to the FY `total` within $10,000,000.
    - Change each FY's `confidence` from `'estimated'` to `'enacted'` (for GAA figures) or `'actual'` (for final audited figures), matching the source you used.
    - Update the file header comment block (lines 2-7) to cite the specific source URL and the figure basis (enacted vs actual), and replace "Confidence: estimated for all years."

    Scope note: the roadmap and STATE.md focus STATE-01 on real FY2025 data; update FY2021–FY2025 where real figures are available and keep FY2026 as-is (or update if a real enacted FY2026 GAA exists). The `POPULATION` constant (7,029,917) and the per-capita print logic stay unchanged. Per-capita should land in a plausible range (~$5,000–$8,500/person for a $36B–$58B GF); flag if outside (Pitfall 6 warning signs).

    Do NOT run live yet — validate via dry-run only in this task.
  </action>
  <verify>
    <automated>node scripts/processMA.js --dry-run</automated>
  </verify>
  <done>`node scripts/processMA.js --dry-run` prints "validation: PASS" for every fiscal year, shows the updated real totals and category breakdown, prints a plausible per-capita figure for each year, and the confidence label printed is 'enacted' or 'actual' (not 'estimated').</done>
  <acceptance_criteria>
    - `node scripts/processMA.js --dry-run` exits 0 and prints "FY{year} validation: PASS" for every year in the loop
    - No occurrence of `confidence: 'estimated'` remains in `scripts/processMA.js` for the years being upgraded (grep `grep -v '^#' scripts/processMA.js` shows updated confidence values 'enacted' or 'actual')
    - The file header comment cites a real MA budget source URL (budget.digital.mass.gov or mass.gov budget-information)
    - FY2025 `total` reflects a real published MA GF/GAA figure (not the prior round-number 36_000_000_000) and the printed per-capita for FY2025 falls in the $5,000–$8,500/person range
    - Category names in EXPENDITURES are unchanged from the original (same 7 category names per year)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live-reload MA state budget and commit loadMaGFExcel.js</name>
  <read_first>
    - scripts/processMA.js (confirm Task 1 dry-run passed before live run; main() upserts the data_source row by name and calls treasury_sync_budget_tree per FY)
    - scripts/loadMaGFExcel.js (the untracked file to commit as-is — read to confirm no code changes are needed; per PATTERNS.md it is committed unchanged)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (STATE-01 Test Map: open app → Massachusetts state entity → verify totals plausible)
  </read_first>
  <action>
    Run the live MA state budget reload: `node scripts/processMA.js` (no `--dry-run`). This re-upserts the "Massachusetts General Fund Operating Budget" data_source and calls `treasury_sync_budget_tree` for each fiscal year with the real figures. The RPC is idempotent and `p_triggered_by` is already `'bulk_load'` (the value required by the sync_logs check constraint per Phase 38 SUMMARY) — confirm this value is unchanged.

    After the run, verify in the DB via the Supabase MCP tool (`mcp__supabase-local__execute_sql`): query `treasury.budgets` joined to the MA state municipality (`name='Massachusetts' AND entity_type='state'`) and confirm the operating totals match the real figures you loaded (not the prior estimates) for FY2025 and the other updated years.

    Then commit the untracked `scripts/loadMaGFExcel.js` as-is (`git add scripts/loadMaGFExcel.js`) — no code changes. Per RESEARCH, this file is the city-level GF Excel loader; STATE-01 commits it as a tracked asset. Do NOT run loadMaGFExcel.js in this plan (it loads city-level data, which is out of scope for the state-entity upgrade and would touch the 351-city rows owned by other workstreams).

    The actual git commit of plan files and these script changes is handled by the planner/execute-phase commit step; this task's git action is limited to staging `loadMaGFExcel.js` so it is no longer untracked.
  </action>
  <verify>
    <automated>node scripts/processMA.js</automated>
  </verify>
  <done>The live run loads every updated FY without RPC error (no `r?.error`). DB query confirms the MA state entity's operating budget total for FY2025 equals the real figure loaded (not 36_000_000_000). `scripts/loadMaGFExcel.js` is staged in git (git status no longer shows it as `??` untracked).</done>
  <acceptance_criteria>
    - `node scripts/processMA.js` completes with "Done." and no "RPC error" line
    - DB: the MA state entity (`name='Massachusetts'`, `entity_type='state'`) has an operating budget row for FY2025 whose total equals the real figure loaded in Task 1 (confirmed via execute_sql against treasury.budgets)
    - `git status` no longer lists `scripts/loadMaGFExcel.js` as untracked (it is staged/tracked)
    - No regression: existing CA state entity budget rows are untouched (this plan only writes to the Massachusetts entity)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- DB: MA state entity FY2025 operating total equals the real loaded figure (not the prior $36B estimate)
- processMA.js confidence labels are 'enacted'/'actual', header cites a real source URL
- `scripts/loadMaGFExcel.js` is tracked in git
- validate() passes for every loaded FY (dry-run printed "PASS")
</verification>

<success_criteria>
- MA state government entity shows real General Fund figures, not hardcoded estimates (STATE-01)
- Per-capita figure for the MA state entity is plausible (~$5,000–$8,500/person)
- `loadMaGFExcel.js` committed as a tracked file
</success_criteria>

<output>
Create `.planning/phases/39-ma-population-state-budget-and-enrichment/39-02-SUMMARY.md` when done
</output>
