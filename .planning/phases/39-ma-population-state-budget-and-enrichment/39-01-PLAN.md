---
phase: 39-ma-population-state-budget-and-enrichment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadMAPopulation.js
autonomous: true
requirements: [MA-04]
must_haves:
  truths:
    - "All 351 MA municipalities have population > 0 and population_year = 2024"
    - "MA city pages show a per-capita ($/resident) figure (auto-activated by population > 0)"
    - "MA towns at Census SUMLEV=061 are matched (not SUMLEV=162, which would miss ~325 towns)"
  artifacts:
    - path: "scripts/loadMAPopulation.js"
      provides: "Census 2024 population loader for all MA municipalities (dynamic DB-driven city list)"
      contains: "sub-est2024_25.csv"
      min_lines: 120
  key_links:
    - from: "scripts/loadMAPopulation.js"
      to: "treasury.municipalities"
      via: "Supabase UPDATE population, population_year=2024 keyed by id"
      pattern: "\\.update\\(\\{\\s*population"
    - from: "scripts/loadMAPopulation.js"
      to: "Census sub-est2024_25.csv"
      via: "https download + SUMLEV=061 filter"
      pattern: "061"
---

<objective>
Load 2024 Census population data for all 351 Massachusetts municipalities so that per-capita ($/resident) spending displays on every MA city page (MA-04).

Purpose: Every MA municipality currently has `population = 0` and `population_year = NULL`, which suppresses the per-capita display in `PlainLanguageSummary.tsx`. Loading population is the single blocker for per-capita context on MA city pages.
Output: A new `scripts/loadMAPopulation.js` script (copy of `loadORPopulation.js` with six MA-specific changes), and 351 MA municipality rows updated with real population values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/loadMAPopulation.js from the loadORPopulation.js analog</name>
  <read_first>
    - scripts/loadORPopulation.js (the exact analog to copy; note SUMLEV=162 filter at line 83, hardcoded EXPECTED_CITIES at line 16, CSV_URL at line 11, normalizeCensusName at lines 25-27, downloadFile redirect-safe version at lines 29-48, column-position guard at lines 73-77, idempotence check at lines 129-140)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md (Pattern 1 section: the six MA-specific changes with exact code snippets, plus Shared Patterns: loadEnv, Supabase client init, CSV column validation, parseArgs, key guard)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (MA-04 Census Data Deep Dive: SUMLEV analysis table, name normalization, known-good sanity values, Pitfalls 1 and 2)
  </read_first>
  <action>
    Create `scripts/loadMAPopulation.js` as a full copy of `scripts/loadORPopulation.js`, then apply these six MA-specific changes:

    1. CSV_URL = `https://www2.census.gov/programs-surveys/popest/datasets/2020-2024/cities/totals/sub-est2024_25.csv` (FIPS 25 = Massachusetts, replacing the `_41` Oregon URL). CSV_PATH = `path.join(tmpdir(), 'sub-est2024_25.csv')`.
    2. SUMLEV filter: change `if (cols[0] !== '162') continue;` to `if (cols[0] !== '061') continue;`. MA towns appear ONLY at SUMLEV=061; using 162 would match only ~26 of 351 cities (Pitfall 1).
    3. Replace the hardcoded `EXPECTED_CITIES` array with a dynamic Supabase query: `SELECT id, name FROM municipalities WHERE state='MA'` (use the treasury schema). Build a `dbNames` Map of `name -> id` (expected size 351). The Supabase client must be created BEFORE the parse loop because the city list now comes from the DB — move the `createClient(SUPABASE_URL, SUPABASE_KEY, { db: { schema: 'treasury' } })` call up, and exit with an error if the query returns 0 rows.
    4. Extend `normalizeCensusName()` to add `.replace(/-/g, ' ')` (hyphen to space) followed by `.replace(/\b\w/g, c => c.toUpperCase())` (title-case each word), AFTER the existing ` city`/` town`/` village` suffix stripping. This converts Census `"Manchester-by-the-Sea town"` to `"Manchester By The Sea"` to match the DLS-stored DB name (Pitfall 2).
    5. Change the matching/UPDATE loop to iterate over the parsed `cityMap` (normalized Census name -> population) and look up `dbNames.get(censusName)` to obtain the municipality UUID; UPDATE by `.eq('id', muniId)` (not by `.eq('name', city).eq('state', 'OR')`). Keep the idempotence check (skip if `population` and `population_year` already match). Track `updated`, `skipped`, `failed`, and a `missingInDb` array for Census rows with no DB match.
    6. Replace the OR `KNOWN_VALUES` hardcoded spot-checks with MA values: Boston ~695000, Worcester ~215000, Cambridge ~118000, Springfield ~155000. Keep the ±1% drift WARNING behavior (warn only, do not exit), since these are ASSUMED values per RESEARCH A6.

    Preserve from the OR analog unchanged: the redirect-safe `downloadFile()`, the CSV column-position guard (`header[0]==='SUMLEV'`, `header[8]==='NAME'`, `header[15]==='POPESTIMATE2024'`), the `--dry-run` parseArgs flag, the SUPABASE_KEY guard, and `main().catch(...)` entry point. Add the `loadEnv()` helper from processMA.js (lines 17-22) so the script reads `.env`/`.env.local` for `SUPABASE_SERVICE_KEY`.

    Replace the OR "all expected cities found or exit(1)" behavior with non-fatal reporting: log a warning listing any DB municipalities with no Census match (`dbMissingFromCensus`) and any Census rows with no DB match (`missingInDb`), then print a summary line. Exit non-zero only on actual UPDATE `failed > 0`.
  </action>
  <verify>
    <automated>node scripts/loadMAPopulation.js --dry-run</automated>
  </verify>
  <done>`node scripts/loadMAPopulation.js --dry-run` runs without error, reports the DB municipality count as 351 (or close), lists "would UPDATE" lines for matched cities including Boston/Worcester/Cambridge with plausible populations, and reports any unmatched names without crashing. No DB writes occur in dry-run.</done>
  <acceptance_criteria>
    - File `scripts/loadMAPopulation.js` exists and contains the literal string `sub-est2024_25.csv`
    - File contains the SUMLEV filter `cols[0] !== '061'` (NOT `'162'`)
    - `normalizeCensusName` contains `.replace(/-/g, ' ')` for hyphen normalization
    - The script queries `municipalities` with `.eq('state', 'MA')` rather than a hardcoded city array (no `EXPECTED_CITIES = ['Portland'...]` remnant)
    - `node scripts/loadMAPopulation.js --dry-run` exits 0 and prints a dry-run preview including Boston with a population in the 650K–740K range (±5% of ~695K)
    - Dry-run output reports DB municipality count and any unmatched names; "Manchester By The Sea" does NOT appear in the unmatched/missing list
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live-load population for all 351 MA municipalities</name>
  <read_first>
    - scripts/loadMAPopulation.js (the script created in Task 1 — confirm dry-run output looked correct before live run)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (MA-04 → Test Map: the exact verification SQL `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population > 0` expecting 351; Pitfall 1 warning signs)
  </read_first>
  <action>
    Run the live population load: `node scripts/loadMAPopulation.js` (no `--dry-run`). This downloads the Census CSV (or uses the cached copy in tmpdir), filters SUMLEV=061 rows, normalizes names, and UPDATEs `population` + `population_year=2024` for each matched MA municipality keyed by id.

    After the run, verify in the DB using the Supabase MCP tool (`mcp__supabase-local__execute_sql`, per project memory: use MCP for DB ops, skip Dashboard checkpoint): run `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population IS NOT NULL AND population > 0`. The expected result is 351. If the count is materially below 351 (e.g., 26), this indicates the SUMLEV filter regressed to 162 (Pitfall 1) — stop and fix the filter. Also spot-check three known values: `SELECT name, population FROM treasury.municipalities WHERE state='MA' AND name IN ('Boston','Worcester','Cambridge')` and confirm Boston ~695K, Worcester ~215K, Cambridge ~118K (±5%).

    If a small number of cities (1–6) remain unmatched, inspect the script's "DB municipalities with no Census match" warning output, resolve any name-normalization mismatch by extending the one-off override in `normalizeCensusName`, and re-run (the idempotence check makes re-runs safe). Do not hardcode population values manually.
  </action>
  <verify>
    <automated>node scripts/loadMAPopulation.js</automated>
  </verify>
  <done>The script reports a Summary line with Failed: 0. DB query `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population > 0` returns 351 (or, if a documented handful of Census-absent municipalities exist, the count is logged with the exact missing names and confirmed acceptable). Boston/Worcester/Cambridge show populations within ±5% of the known-good values.</done>
  <acceptance_criteria>
    - Script run completes with `Failed: 0` in the summary line
    - DB query `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population > 0` returns 351 (any shortfall is logged with the exact unmatched municipality names and an explicit note)
    - `SELECT population FROM treasury.municipalities WHERE state='MA' AND name='Boston'` returns a value in 660,000–730,000
    - A second run of `node scripts/loadMAPopulation.js` reports the majority as SKIP (idempotence holds — no duplicate writes, Failed: 0)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- DB: `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population > 0` = 351 (or documented shortfall with named exceptions)
- DB: Boston/Worcester/Cambridge/Springfield populations within ±5% of known-good Census 2024 values
- Idempotence: re-running the script produces SKIP for already-loaded cities and Failed: 0
</verification>

<success_criteria>
- `scripts/loadMAPopulation.js` exists, uses the MA Census URL (FIPS 25), SUMLEV=061 filter, dynamic DB city list, and hyphen-aware normalization
- All 351 MA municipalities have `population > 0` and `population_year = 2024` (per-capita display auto-activates on city pages)
- Re-runs are idempotent
</success_criteria>

<output>
Create `.planning/phases/39-ma-population-state-budget-and-enrichment/39-01-SUMMARY.md` when done
</output>
