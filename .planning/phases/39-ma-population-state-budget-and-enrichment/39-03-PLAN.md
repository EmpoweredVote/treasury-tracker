---
phase: 39-ma-population-state-budget-and-enrichment
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ENRICH-01]
user_setup:
  - service: anthropic
    why: "enrichCategories.js calls the Anthropic API to generate plain-language category descriptions"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: ".env (already present per RESEARCH Environment Availability)"
must_haves:
  truths:
    - "14 universal MA DLS category enrichment rows exist with municipality_id IS NULL (9 operating + 5 revenue)"
    - "The same enrichment description is served to different MA cities for the same category name (universal reuse, not per-city)"
    - "No duplicate universal rows were created for any of the 14 name_keys"
  artifacts: []
  key_links:
    - from: "treasury.category_enrichment (14 universal rows)"
      to: "all 351 MA city pages"
      via: "getBudgetById e_univ LEFT JOIN on name_key with municipality_id IS NULL"
      pattern: "municipality_id IS NULL"
    - from: "enrichCategories.js --city Boston"
      to: "treasury.category_enrichment"
      via: "saveEnrichment upsert keyed by (name_key, municipality_id)"
      pattern: "category_enrichment"
---

<objective>
Create 14 universal plain-language enrichment descriptions for the MA DLS category names (9 operating + 5 revenue) by enriching ONE MA city (Boston) and then universalizing those rows so all 351 MA cities reuse them (ENRICH-01).

Purpose: MA DLS uses identical category column names across all 351 cities. Per-city enrichment would create 4,914 redundant rows at ~$1.40; the universal approach creates 14 rows at ~$0.004 that serve every MA city via the backend's `e_univ` JOIN.
Output: 14 rows in `treasury.category_enrichment` with `municipality_id IS NULL` covering the exact MA DLS name_keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich Boston's 14 MA DLS categories via enrichCategories.js</name>
  <read_first>
    - scripts/enrichCategories.js (invoke as-is, no code changes; CLI flags --city/--state/--year at lines ~64-75; saveEnrichment at lines 381-403 writes name_key = normalize(name) with municipality_id = the city's UUID and upserts onConflict 'name_key,municipality_id')
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (ENRICH-01 section: the exact 14 confirmed name_keys in two tables — 9 operating from special-revenue, 5 revenue from revenue-by-source; Pitfalls 3, 4; Assumption A3 Boston has FY2025 operating data)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md (enrichCategories.js INVOKE section: invocation pattern + saveEnrichment behavior)
  </read_first>
  <action>
    Confirm `ANTHROPIC_API_KEY` is present in `.env` before running (RESEARCH Environment Availability flags this as the only blocking dependency). Estimated cost is ~$0.004 (14 categories × ~$0.0002/call) — well under the $5 project threshold, so no cost gate is required.

    Run exactly ONE city enrichment: `node scripts/enrichCategories.js --city "Boston" --state MA --year 2025`. Use Boston specifically because it has federal grant data in all 5 FYs and will have FY2025 operating + revenue categories (Pitfall 3 — other cities may log "No budget categories for year 2025"). If the run reports "No budget categories for year 2025" for Boston, fall back to `--year 2024`.

    Do NOT run `--all --state MA` (Pitfall 4 — that enriches 351 cities, 4,914 calls, ~$1.40, and creates per-city rows that defeat universalization). Do NOT enrich a second city before universalizing (Pitfall: two cities with non-NULL municipality_id break the universalize step).

    After the run, verify via Supabase MCP (`mcp__supabase-local__execute_sql`): `SELECT name_key, plain_name, municipality_id FROM treasury.category_enrichment WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='Boston' AND state='MA') ORDER BY name_key`. Confirm 14 rows exist, all with Boston's UUID, covering the 14 confirmed name_keys: federal general government grants, federal public safety grants, federal public works grants, federal education grants, federal emergency management agency, federal culture and recreation grants, federal community development block grants, other federal housing and urban development grants, other federal grants, tax levy, state aid, local receipts, all other, enterprise & cpa funds.

    If fewer than 14 name_keys are present (e.g., a category had zero data for the chosen year), note which are missing and re-run with `--year 2024` to fill gaps. Confirm all 14 exist before proceeding.
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city "Boston" --state MA --year 2025</automated>
  </verify>
  <done>14 enrichment rows exist in `treasury.category_enrichment` with Boston's municipality_id, covering all 14 confirmed MA DLS name_keys, each with a non-empty plain_name and description.</done>
  <acceptance_criteria>
    - The enrichment run completes without throwing; console shows at most 14 categories processed (NOT 351 cities — Pitfall 4 guard)
    - DB query returns 14 rows for Boston's municipality_id covering exactly the 14 confirmed name_keys (federal general government grants ... enterprise & cpa funds)
    - Each of the 14 rows has a non-null, non-empty `plain_name` and `description`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Universalize the 14 enrichment rows (municipality_id = NULL) with duplicate guard</name>
  <read_first>
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-PATTERNS.md (enrichCategories.js Post-invocation SQL: the 3-step universalize SQL — Step 1 pre-existing-universal check, Step 2 UPDATE, Step 3 verify)
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (ENRICH-01 universalize SQL; Pitfall 5: the (name_key, municipality_id) unique constraint — a pre-existing universal row for any name_key would make the UPDATE violate uniqueness; Assumption A5)
  </read_first>
  <action>
    Run all DB operations via the Supabase MCP tool (`mcp__supabase-local__execute_sql`).

    Step 1 — Pitfall 5 duplicate guard (MANDATORY, run BEFORE the UPDATE): `SELECT name_key, COUNT(*) FROM treasury.category_enrichment WHERE municipality_id IS NULL AND name_key IN ('federal general government grants','federal public safety grants','federal public works grants','federal education grants','federal emergency management agency','federal culture and recreation grants','federal community development block grants','other federal housing and urban development grants','other federal grants','tax levy','state aid','local receipts','all other','enterprise & cpa funds') GROUP BY name_key`. If this returns ANY rows, a universal row already exists for those name_keys — for each such name_key, DELETE the Boston-owned duplicate row instead of updating it (updating to NULL would violate the `category_enrichment_name_key_municipality_id_key` unique constraint). Only run the Step 2 UPDATE for name_keys that have NO pre-existing universal row.

    Step 2 — Universalize: `UPDATE treasury.category_enrichment SET municipality_id = NULL WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='Boston' AND state='MA') AND name_key IN (<the 14 name_keys above>)`. Expected: 14 rows updated (fewer if any were handled as duplicates in Step 1).

    Step 3 — Verify universalization: `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id IS NULL AND name_key IN (<the 14 name_keys>)`. Expected: 14. Also confirm no Boston-owned rows remain for these name_keys: `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='Boston' AND state='MA') AND name_key IN (<the 14 name_keys>)` should return 0.
  </action>
  <verify>
    <automated>MISSING — DB-state verification via mcp__supabase-local__execute_sql: SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id IS NULL AND name_key IN (the 14 MA DLS name_keys) must return 14</automated>
  </verify>
  <done>Exactly 14 rows in `treasury.category_enrichment` have `municipality_id IS NULL` for the MA DLS name_keys, and 0 Boston-owned rows remain for those name_keys. No unique-constraint violation occurred.</done>
  <acceptance_criteria>
    - Step 1 duplicate-guard query was run and its result recorded before any UPDATE
    - `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id IS NULL AND name_key IN (<14 name_keys>)` returns 14
    - `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id = (Boston id) AND name_key IN (<14 name_keys>)` returns 0
    - No `category_enrichment_name_key_municipality_id_key` unique-constraint error occurred during the UPDATE
  </acceptance_criteria>
</task>

</tasks>

<verification>
- DB: 14 universal rows (`municipality_id IS NULL`) for the MA DLS name_keys
- DB: 0 Boston-owned rows remain for those name_keys
- Pitfall 5 duplicate guard run before the UPDATE; no unique-constraint violation
- Cost stayed under $5 (single-city enrichment, ~$0.004)
</verification>

<success_criteria>
- All 14 universal MA DLS category enrichments exist and serve all 351 MA cities via the e_univ JOIN (ENRICH-01)
- Enrichment is universal (NULL municipality_id), not per-city — confirmed by row count and Boston-row absence
</success_criteria>

<output>
Create `.planning/phases/39-ma-population-state-budget-and-enrichment/39-03-SUMMARY.md` when done
</output>
