---
phase: 39-ma-population-state-budget-and-enrichment
plan: "04"
type: execute
wave: 2
depends_on: ["39-01", "39-02", "39-03"]
files_modified: []
autonomous: false
requirements: [MA-04, STATE-01, ENRICH-01]
must_haves:
  truths:
    - "A human confirms per-capita ($/resident) appears on MA city pages"
    - "A human confirms the MA state entity shows real (not estimated) General Fund data"
    - "A human confirms enrichment descriptions appear and are identical across two different MA cities for the same category"
  artifacts: []
  key_links:
    - from: "loaded population/state-budget/enrichment data"
      to: "live app at treasurytracker.empowered.vote"
      via: "ev-accounts-api getCities/getBudgetById serving population + e_univ enrichment"
      pattern: "per person"
---

<objective>
Human verification that all three Phase 39 workstreams are visible and correct in the live app: per-capita on MA city pages (MA-04), real MA state budget (STATE-01), and universal enrichment descriptions shared across cities (ENRICH-01).

Purpose: All three workstreams ran autonomously against the DB. This checkpoint confirms the data surfaces correctly in the live app before the phase is marked complete, satisfying the phase success criteria that require visual confirmation.
Output: Human approval (or recorded issues) and `39-04-SUMMARY.md` / verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-checkpoint DB verification of all three workstreams</name>
  <read_first>
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (Phase Requirements → Test Map: the exact DB count queries for MA-04, STATE-01, ENRICH-01)
  </read_first>
  <action>
    Before the human checkpoint, run these confirmation queries via the Supabase MCP tool (`mcp__supabase-local__execute_sql`) to ensure the data is actually present (so the human is not asked to verify something that silently failed):

    1. MA-04: `SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND population > 0` — expect 351 (or documented shortfall).
    2. STATE-01: `SELECT b.fiscal_year, b.total_amount FROM treasury.budgets b JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.name='Massachusetts' AND m.entity_type='state' AND b.dataset_type='operating' ORDER BY b.fiscal_year` — confirm FY2025 total is the real figure (not 36_000_000_000).
    3. ENRICH-01: `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id IS NULL AND name_key IN ('tax levy','state aid','federal general government grants')` — expect 3 (confirms universals exist).

    Record the three query results. If any fails its expectation, report it as a blocker for the corresponding Wave 1 plan rather than proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>MISSING — DB-state verification via mcp__supabase-local__execute_sql: the three queries above must return 351 / real-FY2025-total / 3 respectively</automated>
  </verify>
  <done>All three DB queries return their expected values (351 populated MA cities; MA state FY2025 real total; 3 sample universal enrichment rows present).</done>
  <acceptance_criteria>
    - Query 1 returns 351 (or a shortfall with named exceptions documented)
    - Query 2 shows the MA state entity FY2025 operating total ≠ 36_000_000_000 (real loaded figure)
    - Query 3 returns 3
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human spot-check of MA per-capita, state budget, and universal enrichment in the live app</name>
  <read_first>
    - .planning/phases/39-ma-population-state-budget-and-enrichment/39-RESEARCH.md (Phase Requirements → Test Map: the manual verification rows for MA-04, STATE-01, ENRICH-01)
  </read_first>
  <action>
    This is a human-verification checkpoint. The executor pauses here and asks the user to perform the live-app checks below, then resumes on the user's signal. Do not auto-approve.
  </action>
  <what-built>
    All three Phase 39 workstreams loaded data into the live DB:
    - MA-04: 2024 Census population for all 351 MA municipalities (per-capita auto-activates when population > 0).
    - STATE-01: MA state government General Fund budget upgraded from estimates to real published figures.
    - ENRICH-01: 14 universal plain-language category descriptions shared across all 351 MA cities.
  </what-built>
  <how-to-verify>
    Open the live app at https://treasurytracker.empowered.vote and confirm:

    1. (MA-04) Open any MA city page (e.g., Boston, then Worcester). Confirm a per-capita figure appears alongside the budget total — wording like "That's roughly $X per person." (Money Out) and "$X per resident" (Money In). Confirm the per-resident number is plausible for the city's size.
    2. (STATE-01) Open the Massachusetts STATE entity (under "State Governments" in the entity picker). Confirm the General Fund total looks like a real published figure for FY2025 (not an obvious round-number estimate), and the per-capita is plausible (~$5,000–$8,500/person).
    3. (ENRICH-01) On a MA city page, open the Money Out and Money In tabs and confirm category descriptions appear (e.g., "Tax Levy", "State Aid", "Federal Public Safety Grants" each show a plain-language explanation — not blank).
    4. (ENRICH-01 universality) Open a SECOND MA city (different from the first) and confirm the SAME category (e.g., "State Aid") shows the IDENTICAL description text as on the first city — proving universal reuse.

    If any of the four checks fails, describe which one and what you saw.
  </how-to-verify>
  <verify>
    <human-check>User confirms all four live-app checks pass, or reports specific failures.</human-check>
  </verify>
  <done>User types "approved" after all four checks pass, or reports specific issues that become gap-closure items.</done>
  <resume-signal>Type "approved" if all four checks pass, or describe the issues found.</resume-signal>
</task>

</tasks>

<verification>
- Automated DB queries pass (351 / real MA FY2025 total / 3 universal rows)
- Human confirms per-capita on MA city pages (MA-04)
- Human confirms real MA state budget figures (STATE-01)
- Human confirms enrichment descriptions appear and are identical across two MA cities (ENRICH-01)
</verification>

<success_criteria>
- All five Phase 39 ROADMAP success criteria confirmed: per-capita visible, MA state real data, 9 operating + 5 revenue category descriptions present, identical across cities
- Human approval recorded; write 39-VERIFICATION.md if any gaps surface
</success_criteria>

<output>
Create `.planning/phases/39-ma-population-state-budget-and-enrichment/39-04-SUMMARY.md` when done. If gaps are found, also write `39-VERIFICATION.md` listing them.
</output>
