---
phase: 120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47
plan: "120-03"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processNHAcfr.js
  - scripts/processNHRevenueAcfr.js
  - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-03-NH-LOADLOG.md
autonomous: true
requirements: [ACFR-45]
user_setup: []

must_haves:
  truths:
    - "New Hampshire state node (c54f6dbd-3f2a-453e-b0b9-259e377aef67) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2017–FY2024), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NH units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL column, Total revenues line): FY2024 = $6,377,159K, FY2017 = $4,207,160K (store ×1,000 raw dollars)"
    - "GENERAL column is the 1st of the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (General | Highway | Education | Non-Major Governmental | Total) — NOT the Statement of Activities, NOT the biennial appropriations document"
    - "das.nh.gov is AKAMAI-EDGE-BLOCKED for automated curl/fetch (HTTP 403 for all UA/header variants — harder than the tn.gov precedent). The loader MUST fetch via the Wayback Machine mirror (web.archive.org/web/{ts}if_/https://www.das.nh.gov/...) as a durable, re-fetchable proxy, OR a browser-download step — direct das.nh.gov fetch WILL fail"
    - "Source URL naming has 3 eras: FY2021/FY2022/FY2024 = fy_{YYYY}_annual_comprehensive_financial_report.pdf; FY2023 (exception) adds _acfr suffix = fy_2023_annual_comprehensive_financial_report_acfr.pdf; FY2017–FY2020 (pre-rename) = fy_{YYYY}_comprehensive_annual_financial_report.pdf. Directory segment = fy%20{YY} (2-digit year, URL-encoded space)"
    - "Pre-existing NASBO operating rows (NH FY2023 $2,136,000K, FY2024 $1,981,000K) are REPLACED in place at the same (c54f6dbd, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (NH ACFR GF ~3.22× NASBO — the LARGEST in the batch; 'Federal Government' = $3.07B = 48% of FY2024 GF revenue + 'Special Taxes' = $1.79B (Medicaid Enhancement Tax + business taxes, NH has no broad sales/income tax); TX-trap mechanism) recorded at load; GAAP basis label verified on live rows"
    - "No negative GF line items observed at either bookend; the P2 clamp path stays wired for any interior year, checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on NH once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNHAcfr.js"
      provides: "NH GF spending-by-function loader (dataset_type='operating'), UNITS=1000, Wayback/browser fetch path, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNHRevenueAcfr.js"
      provides: "NH GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-03-NH-LOADLOG.md"
      provides: "Per-FY load disposition, the Akamai-block/Wayback-proxy fetch note, NASBO-replacement confirmation, ~3.22× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNHAcfr.js / processNHRevenueAcfr.js"
      to: "treasury.budgets (NH state node c54f6dbd)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['NH']"
      to: "scripts/processNHAcfr.js + scripts/processNHRevenueAcfr.js"
      via: "gen_state.py code generation from nh/nh_all.json + 3-era naming-exception SOURCES map (Wayback-proxied URLs)"
      pattern: "General"
---

<objective>
Upgrade the **New Hampshire** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processNHAcfr.js` (GF spending-by-function, `operating`) + `scripts/processNHRevenueAcfr.js` (GF revenue-by-source, `revenue`) — fetching NH ACFR PDFs FY2017–FY2024 **via the Wayback Machine mirror** into gitignored `_acfr-work/nh/`, extracting the **GENERAL column** (1st of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**NH is the fetch-mechanism deviation of Batch 3.** `das.nh.gov` returns HTTP 403 (Akamai edge-block) to all automated `curl`/fetch — harder than the tn.gov precedent (browser-header spoofing was insufficient). The loader **MUST fetch through the Wayback Machine mirror** `web.archive.org/web/{ts}if_/https://www.das.nh.gov/...` (itself a durable, re-fetchable URL) or a browser-download step. Bookend guards: **FY2024 $6,377,159K, FY2017 $4,207,160K** (GF Total revenues, ×1,000). 3-era filename map (see source_facts). Scope note: NH ACFR GF ~3.22× NASBO — largest divergence in the batch (Federal 48% + Special Taxes; NH has no broad sales/income tax) — accept-and-relabel honestly (TX precedent).

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `120-03-NH-LOADLOG.md` with the load disposition + the Wayback-fetch note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['NH']` entry (mirror the 'MD'/'NC' multi-column + naming-exception-map entries): name='New Hampshire', abbr='NH', population=1_377_529, node='c54f6dbd-3f2a-453e-b0b9-259e377aef67', years=[the FYs that cleanly tie, FY2017–FY2024 target], tolerance (small; NH tied at $0), data='nh/nh_all.json', out='NH', base_url='https://www.das.nh.gov/accounting/', sources={ 3-era naming map, Wayback-proxied per-FY URLs }, UNITS=1000. Use `head_note` for the ~3.22× TX-trap scope note + the Akamai-block/Wayback-proxy fetch note. Then `py -3 gen_state.py NH` emits scripts/processNHAcfr.js + processNHRevenueAcfr.js.
- **Fetch deviation:** the standard generated loader/download step uses plain `curl` against the source. For NH this WILL 403. Fetch each PDF via the Wayback Machine mirror instead: resolve the archived snapshot of the das.nh.gov original (`https://web.archive.org/web/{timestamp}if_/https://www.das.nh.gov/accounting/fy%20{YY}/{filename}`), which is NOT blocked, and store the Wayback URL as the durable source_url in the SOURCES map. (Recon verified both bookends this way.) If a browser-download step is preferred over Wayback, follow the `followups/ca-acfr-reconciliation.md` precedent — but the Wayback proxy is the lower-effort durable path.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For NH, capture the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds nh/nh_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'nh-acfr-gf-operating' (name 'New Hampshire General Fund Operating Budget'), 'nh-acfr-gf-revenue' (name 'New Hampshire General Fund Revenue'), api_type='pdf_download', base_url = the DAS accounting landing page.
- NASBO baseline (live-confirmed): NH has 2 NASBO operating rows only — FY2023 $2,136,000K, FY2024 $1,981,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH3-SOURCES.md NH Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = 1st column of 5 (General | Highway | Education | Non-Major Governmental | Total). Units = THOUSANDS. FY-end June 30.
- Clean window FY2017–FY2024 (8 yr; Wayback history back to FY1997 if deeper wanted later).
- **ACCESS: das.nh.gov / www.das.nh.gov / www.nh.gov all return HTTP 403 (Akamai `errors.edgesuite.net`) to automated curl — all UA/Accept/Referer/sec-fetch header variants blocked. Fetch via Wayback mirror `web.archive.org/web/{ts}if_/{original-url}` (NOT blocked; Wayback re-crawled das.nh.gov/accounting/ through 2026-06/07).**
- URL naming (3 eras), dir segment `fy%20{YY}`:
  - FY2021, FY2022, FY2024: `https://www.das.nh.gov/accounting/fy%20{YY}/fy_{YYYY}_annual_comprehensive_financial_report.pdf`
  - FY2023 (exception, `_acfr` suffix): `.../fy%2023/fy_2023_annual_comprehensive_financial_report_acfr.pdf`
  - FY2017–FY2020 (pre-ACFR-rename): `.../fy%20{YY}/fy_{YYYY}_comprehensive_annual_financial_report.pdf`
  - Landing: `https://www.das.nh.gov/accounting/` (blocked; verify via Wayback)
- Bookend ties (GF Total revenues, via Wayback-mirrored originals): FY2024 $6,377,159K = line-item sum (diff $0); FY2017 $4,207,160K = line-item sum (diff $0).
- No negative GF line items observed at either bookend.
- Scope vs NASBO ~3.22× — "Federal Government" = $3,065,572K in FY2024 = 48% of GF revenue + "Special Taxes" = $1,792,670K (Medicaid Enhancement Tax + business taxes, NH has no broad sales/income tax). Largest divergence in the batch. Accept-and-relabel honestly (TX precedent).
</source_facts>
</context>

<tasks>

<task id="120-03-01" type="auto">
  <name>Task 1: Generate both NH loaders (UNITS=1000, 3-era Wayback SOURCES map) via gen_state.py + Wayback-fetch/extract FY2017–FY2024 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/nh/ (gitignored PDFs + pdftotext + nh_all.json), scripts/processNHAcfr.js, scripts/processNHRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a 'MD'/'NC' multi-column + naming-exception entry; confirm how UNITS + a per-FY sources map + tolerance + head_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how the revenues and expenditures sections are each emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (NH Detail Block: the Akamai-block/Wayback-proxy access finding, the 3-era filename map, units=thousands, bookend ties)
    - scripts/processMDAcfr.js (a recent gen_state.py naming-exception output — the generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). **Do NOT curl das.nh.gov directly — it 403s (Akamai).** For each FY across the durable window (FY2017–FY2024), resolve the Wayback Machine snapshot of the das.nh.gov original (per the 3-era filename map in source_facts) and fetch the archived bytes via `https://web.archive.org/web/{timestamp}if_/https://www.das.nh.gov/accounting/fy%20{YY}/{filename}` into `_acfr-work/nh/NH{YYYY}.pdf`. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page / Wayback error page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL column (1st of 5) for BOTH the revenues section and the expenditures section. Assemble `_acfr-work/nh/nh_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names (thousands, unscaled at this stage); document any hand-patch inline with printed-row evidence. Add a `CONFIGS['NH']` entry to `_acfr-work/gen_state.py` (node c54f6dbd…, years=[the tied FYs], UNITS=1000, tolerance small, data='nh/nh_all.json', out='NH', base_url the DAS accounting landing page, sources=the 3-era Wayback-proxied per-FY URL map, head_note recording the ~3.22× TX-trap accept-relabel + the Akamai-block/Wayback-proxy fetch note). Run `py -3 gen_state.py NH` to emit `scripts/processNHAcfr.js` + `scripts/processNHRevenueAcfr.js` (dataset_ids nh-acfr-gf-operating / nh-acfr-gf-revenue). Cross-check FY2024 revenue stored = 6,377,159×1000 and FY2017 = 4,207,160×1000 — if either bookend does not match, STOP and re-inspect column/statement selection before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie or lacks a usable Wayback snapshot, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processNHAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNHAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNHRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '6,377,159,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nh/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY, fetched via the Wayback mirror; HTML/Wayback-error pages rejected.
    - `node scripts/processNHAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2024` prints TOTAL 6,377,159,000 and `--fy 2017` prints 4,207,160,000 (×1,000).
    - Both loaders carry `UNITS = 1000`; the SOURCES map stores the durable Wayback URLs and covers all 3 naming eras (incl. the FY2023 `_acfr` exception); `_acfr-work/nh/nh_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("New Hampshire State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='nh-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both NH loaders exist (generated by gen_state.py) with UNITS=1000, the 3-era Wayback-proxied SOURCES map, nh_all.json holds every tied FY, both bookends (6,377,159K / 4,207,160K) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="120-03-02" type="auto">
  <name>Task 2: Live-load NH (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNHAcfr.js, scripts/processNHRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNHAcfr.js + scripts/processNHRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; NH node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the NH node (c54f6dbd) and RECORD the pre-load NASBO operating rows (FY2023 $2,136,000K, FY2024 $1,981,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processNHAcfr.js --fy <YYYY>` then `node scripts/processNHRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (c54f6dbd, fy, 'operating') where a NASBO year overlaps; revenue inserts net-new), stamp source_url/source_date/data_source (GAAP label — source_url = the durable Wayback URL), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded NH GF operating + revenue totals against the pre-load NASBO totals to confirm the ~3.22× accept-relabel divergence is the expected federal+special-taxes-inside-GAAP-GF mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "NH row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NH node has, per loaded FY, an operating row "New Hampshire State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url (the Wayback URL) + source_date.
    - NH FY2024 revenue row total = 6,377,159,000 and FY2017 = 4,207,160,000 (×1,000).
    - NH FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (NH, fy), GAAP-labelled; no "NASBO" label remains on the NH node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~3.22× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed NH FY has operating + revenue rows in treasury.budgets, thousands-scaled (UNITS=1000), GAAP-labelled + per-year-sourced (Wayback URLs); FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="120-03-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-03-NH-LOADLOG.md</files>
  <read_first>
    - scripts/processNHAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NH --fy 2024 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded NH FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) NH has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (NH, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — NH has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for NH; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 120-03-NH-LOADLOG.md: FYs loaded, FYs skipped (+reason), the Akamai-block/Wayback-proxy fetch note (record that source_url values are Wayback URLs and why), pre-load-NASBO-vs-loaded-ACFR totals with the ~3.22× accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than NH.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 120-03-NH-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of NH --fy 2024 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded NH FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; NH has ZERO remaining "NASBO" operating rows; exactly one operating row per (NH, fy).
    - NH has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for NH (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 120-03-NH-LOADLOG.md has a finalized "Load Disposition" section documenting the Wayback-proxy fetch.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms NH fully ACFR-sourced (operating + revenue, GAAP-labelled via durable Wayback URLs, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 120-03-NH-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wayback-mirrored das.nh.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the NH node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-03-A | Denial of Service | loader silently fails/hangs on every fetch because das.nh.gov 403s (Akamai) | mitigate | fetch via the Wayback mirror (not blocked); recon verified both bookends this way; %PDF magic guard catches an error page |
| T-120-03-B | Tampering | units mis-scale (applying/omitting the ×1,000) inflates or deflates NH 1000× | mitigate | UNITS=1000 hard-set; bookend totals (6,377,159K / 4,207,160K) asserted in dry-run + live |
| T-120-03-C | Tampering | wrong naming-era filename (missing the FY2023 `_acfr` suffix or the pre-FY2021 word order) → 404 / wrong-year PDF | mitigate | 3-era SOURCES map special-cases each era + FY2023; %PDF magic + size guard; honest hole if a year 404s |
| T-120-03-D | Tampering | extracting the wrong statement/column (Highway/Education/Non-Major/Total instead of GENERAL, or the biennial appropriations doc / Statement of Activities) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-120-03-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (NH, fy), zero NASBO labels |
| T-120-03-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-120-03-G | Info Disclosure | silently inflating NH (~3.22×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-120-03-H | Tampering | write to a state other than NH | mitigate | loader resolves only the NH node id; Task 3 spot-checks cohort unchanged |
| T-120-03-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both NH loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processNHRevenueAcfr.js --dry-run --fy 2024` → 6,377,159,000; `--fy 2017` → 4,207,160,000.
- Fetch path uses the Wayback mirror (das.nh.gov direct fetch 403s); source_url values are durable Wayback URLs.
- Live: NH node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (NH, fy)).
- Re-run NH --fy 2024 → "Loaded 0 rows"; 0 data_sources residue for NH (LOAD-01).
- NH ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~3.22×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 120-03-NH-LOADLOG.md finalized (incl. the Wayback-fetch note).
</verification>

<success_criteria>
The New Hampshire state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2017–FY2024, honest holes logged), fetched via the durable Wayback Machine mirror to defeat the Akamai edge-block, stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~3.22× accept-relabel divergence recorded at load, the P2 clamp wired, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands NH to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-03-SUMMARY.md` when done — record the NH FY window actually transcribed, the Wayback-proxy fetch note (why direct das.nh.gov failed), any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~3.22×), the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
