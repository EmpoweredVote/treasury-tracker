---
phase: 120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47
plan: "120-05"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processNDAcfr.js
  - scripts/processNDRevenueAcfr.js
  - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-05-ND-LOADLOG.md
autonomous: true
requirements: [ACFR-47]
user_setup: []

must_haves:
  truths:
    - "North Dakota state node (e84aafe0-eeaa-470a-8fd3-708c88af2a80) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2021–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "ND units are DOLLARS: UNITS=1 (NOT thousands — the ND units trap). Bookend ties (GENERAL column, Total revenues line): FY2025 = $4,510,201,793, FY2021 = $3,955,670,947 (store raw dollars, no ×1,000)"
    - "GENERAL column is the 1st of the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (General | Federal [Special Revenue] | State [Special Revenue] | Nonmajor Governmental Funds | Total) — this is the ANNUAL GAAP statement, NOT ND's biennial appropriations/budget document, NOT the Statement of Activities"
    - "Source URLs are DERIVABLE: https://www.omb.nd.gov/sites/www/files/documents/financial-transparency/cafr/{YYYY}-acfr.pdf for FY2022–FY2025; FY2021 EXCEPTION adds an -nd suffix = 2021-acfr-nd.pdf. Landing: omb.nd.gov/financial-transparency/annual-comprehensive-financial-reports-acfr lists all 5 years"
    - "Pre-existing NASBO operating rows (ND FY2023 $2,436,000K, FY2024 $2,876,000K) are REPLACED in place at the same (e84aafe0, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (ND ACFR GF ~1.57× NASBO — the mildest structural divergence in the batch; GF dominated by own-source Sales & Use Tax $1.35B + Oil/Gas/Coal Taxes $750M, while most federal intergovernmental revenue $2.68B is booked to the separate Federal special-revenue column, NOT GF) recorded at load; GAAP basis label verified on live rows"
    - "No negative GF line items observed at either bookend; the P2 clamp path stays wired for any interior year, checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on ND once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNDAcfr.js"
      provides: "ND GF spending-by-function loader (dataset_type='operating'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNDRevenueAcfr.js"
      provides: "ND GF revenue-by-source loader (dataset_type='revenue'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-05-ND-LOADLOG.md"
      provides: "Per-FY load disposition, the FY2021 -nd-suffix note, NASBO-replacement confirmation, ~1.57× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNDAcfr.js / processNDRevenueAcfr.js"
      to: "treasury.budgets (ND state node e84aafe0)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['ND']"
      to: "scripts/processNDAcfr.js + scripts/processNDRevenueAcfr.js"
      via: "gen_state.py code generation from nd/nd_all.json + derivable per-FY SOURCES map (FY2021 -nd exception), UNITS=1"
      pattern: "General"
---

<objective>
Upgrade the **North Dakota** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processNDAcfr.js` (GF spending-by-function, `operating`) + `scripts/processNDRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading ND ACFR PDFs FY2021–FY2025 into gitignored `_acfr-work/nd/`, extracting the **GENERAL column** (1st of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**ND is DOLLARS, not thousands — UNITS=1 (the ND units trap).** Bookend guards: **FY2025 $4,510,201,793, FY2021 $3,955,670,947** (GF Total revenues, raw dollars). URL is derivable `.../cafr/{YYYY}-acfr.pdf` with one exception — **FY2021 adds an `-nd` suffix (`2021-acfr-nd.pdf`)**. This is the audited **annual** GAAP statement, distinct from ND's biennial appropriations cycle (the D-03 biennial concern does NOT apply to the ACFR). Scope note: ND ACFR GF ~1.57× NASBO — the mildest divergence in the batch (own-source sales/oil-gas in GF; federal in the separate Federal column) — accept-and-relabel honestly (NE/OH/VA near-parity case).

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `120-05-ND-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['ND']` entry (mirror the recent 'CO'/'IN' clean-derivable entries, with a one-year filename exception like MA FY2017): name='North Dakota', abbr='ND', population=779_094, node='e84aafe0-eeaa-470a-8fd3-708c88af2a80', years=[the FYs that cleanly tie, FY2021–FY2025 target], tolerance (small; ND tied at $0), data='nd/nd_all.json', out='ND', base_url='https://www.omb.nd.gov/financial-transparency/annual-comprehensive-financial-reports-acfr', sources={ derivable per-FY URL map, FY2021 -nd exception }, **UNITS=1 (dollars)**. Use `head_note` for the ~1.57× mild-divergence scope note + the annual-vs-biennial clarification. Then `py -3 gen_state.py ND` emits scripts/processNDAcfr.js + processNDRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For ND, capture the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds nd/nd_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Values are in DOLLARS already; do NOT scale.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'nd-acfr-gf-operating' (name 'North Dakota General Fund Operating Budget'), 'nd-acfr-gf-revenue' (name 'North Dakota General Fund Revenue'), api_type='pdf_download', base_url = the OMB ACFR landing page.
- NASBO baseline (live-confirmed): ND has 2 NASBO operating rows only — FY2023 $2,436,000K, FY2024 $2,876,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH3-SOURCES.md ND Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = 1st column of 5 (General | Federal [Special Revenue] | State [Special Revenue] | Nonmajor Governmental Funds | Total). Units = DOLLARS (not thousands). FY-end June 30. CONFIRMED annual GAAP statement — the biennial-budget concern does NOT apply to the audited ACFR.
- Clean window FY2021–FY2025 (5 yr, all listed on the landing page; reaches FY2025, beyond the recency floor).
- URL — DERIVABLE: `https://www.omb.nd.gov/sites/www/files/documents/financial-transparency/cafr/{YYYY}-acfr.pdf` for FY2022–FY2025. **FY2021 exception:** `.../cafr/2021-acfr-nd.pdf` (adds `-nd` suffix). Landing: `https://www.omb.nd.gov/financial-transparency/annual-comprehensive-financial-reports-acfr`.
- Bookend ties (GF Total revenues, DOLLARS): FY2025 $4,510,201,793 = line-item sum (diff $0); FY2021 $3,955,670,947 = line-item sum (diff $0).
- No negative GF line items observed at either bookend.
- Scope vs NASBO ~1.57× — Sales & Use Taxes $1,346,955,054 + Oil, Gas, and Coal Taxes $750,043,102 booked directly into GF, while most federal intergovernmental revenue $2,678,818,384 is booked to the separate "Federal" special-revenue column, NOT GF. Mildest divergence in the batch. Accept-and-relabel honestly (NE/OH/VA near-parity case).
</source_facts>
</context>

<tasks>

<task id="120-05-01" type="auto">
  <name>Task 1: Generate both ND loaders (UNITS=1 dollars, FY2021 -nd exception) via gen_state.py + download/extract FY2021–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/nd/ (gitignored PDFs + pdftotext + nd_all.json), scripts/processNDAcfr.js, scripts/processNDRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a 'CO'/'IN' clean-derivable entry + a one-year filename exception; confirm how UNITS + sources + tolerance + head_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how the revenues and expenditures sections are each emitted; note ND values are already in dollars)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (ND Detail Block: derivable per-year URL + FY2021 -nd exception, units=DOLLARS, bookend ties, annual-vs-biennial note)
    - scripts/processCOAcfr.js (a recent gen_state.py clean-derivable output — the generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). For each FY across the durable window (FY2021–FY2025), derive the URL `https://www.omb.nd.gov/sites/www/files/documents/financial-transparency/cafr/{YYYY}-acfr.pdf` — **special-case FY2021 to `2021-acfr-nd.pdf`** — and download to `_acfr-work/nd/ND{YYYY}.pdf`. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the **annual** Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (NOT the biennial appropriations doc, NOT the Statement of Activities), and run `extract_gf.py` to isolate the GENERAL column (1st of 5) for BOTH the revenues section and the expenditures section. **Values are in DOLLARS — set UNITS=1, do NOT multiply by 1000.** Assemble `_acfr-work/nd/nd_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names; document any hand-patch inline with printed-row evidence. Add a `CONFIGS['ND']` entry to `_acfr-work/gen_state.py` (node e84aafe0…, years=[the tied FYs], **UNITS=1**, tolerance small, data='nd/nd_all.json', out='ND', base_url the OMB ACFR landing page, sources=derivable per-FY URL map with the FY2021 `-nd` exception, head_note recording the ~1.57× mild accept-relabel + the annual-vs-biennial clarification). Run `py -3 gen_state.py ND` to emit `scripts/processNDAcfr.js` + `scripts/processNDRevenueAcfr.js` (dataset_ids nd-acfr-gf-operating / nd-acfr-gf-revenue). Cross-check FY2025 revenue stored = 4,510,201,793 and FY2021 = 3,955,670,947 — if either bookend does not match, STOP and re-inspect column selection AND unit scaling before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processNDAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNDAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNDRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '4,510,201,793'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nd/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected; FY2021 fetched via the `-nd`-suffix URL.
    - `node scripts/processNDAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints TOTAL 4,510,201,793 and `--fy 2021` prints 3,955,670,947 (raw dollars, NOT ×1,000).
    - Both loaders carry `UNITS = 1`; `_acfr-work/gen_state.py` has a `CONFIGS['ND']` entry whose SOURCES map special-cases FY2021's `-nd` suffix; `_acfr-work/nd/nd_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("North Dakota State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='nd-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both ND loaders exist (generated by gen_state.py) with UNITS=1 (dollars), derivable SOURCES map incl. the FY2021 `-nd` exception, nd_all.json holds every tied FY, both bookends (4,510,201,793 / 3,955,670,947) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="120-05-02" type="auto">
  <name>Task 2: Live-load ND (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNDAcfr.js, scripts/processNDRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNDAcfr.js + scripts/processNDRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; ND node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the ND node (e84aafe0) and RECORD the pre-load NASBO operating rows (FY2023 $2,436,000K, FY2024 $2,876,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processNDAcfr.js --fy <YYYY>` then `node scripts/processNDRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (e84aafe0, fy, 'operating') where a NASBO year overlaps — FY2023/FY2024; revenue inserts net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded ND GF operating + revenue totals against the pre-load NASBO totals to confirm the ~1.57× accept-relabel divergence is the expected mild own-source-GF mechanism, not a units bug.
  </action>
  <verify>
    <automated>echo "ND row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the ND node has, per loaded FY, an operating row "North Dakota State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - ND FY2025 revenue row total = 4,510,201,793 and FY2021 = 3,955,670,947 (raw dollars).
    - ND FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (ND, fy), GAAP-labelled; no "NASBO" label remains on the ND node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.57× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed ND FY has operating + revenue rows in treasury.budgets, dollar-scaled (UNITS=1), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="120-05-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-05-ND-LOADLOG.md</files>
  <read_first>
    - scripts/processNDAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. ND --fy 2025 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded ND FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) ND has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (ND, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — ND has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for ND; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 120-05-ND-LOADLOG.md: FYs loaded, FYs skipped (+reason), the FY2021 `-nd`-suffix note, pre-load-NASBO-vs-loaded-ACFR totals with the ~1.57× accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than ND.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 120-05-ND-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of ND --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded ND FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; ND has ZERO remaining "NASBO" operating rows; exactly one operating row per (ND, fy).
    - ND has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for ND (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 120-05-ND-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms ND fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 120-05-ND-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| omb.nd.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the ND node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-05-A | Tampering | units mis-scale — applying a spurious ×1,000 to ND's already-dollars figures inflates ND 1000× | mitigate | UNITS=1 hard-set (dollars); bookend totals (4,510,201,793 / 3,955,670,947) asserted in dry-run + live |
| T-120-05-B | Tampering | FY2021 404s because the `-nd` suffix exception is missed | mitigate | SOURCES map special-cases FY2021 = `2021-acfr-nd.pdf`; %PDF magic + size guard; honest hole if it still 404s |
| T-120-05-C | Tampering | extracting the wrong statement (the biennial appropriations doc or Statement of Activities) or the wrong column (Federal/State/Nonmajor/Total instead of GENERAL) | mitigate | extract_gf.py isolates the 1st column of the ANNUAL Governmental Funds statement; bookend ties gate before transcription |
| T-120-05-D | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (ND, fy), zero NASBO labels |
| T-120-05-E | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-120-05-F | Info Disclosure | silently inflating ND (~1.57×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-120-05-G | Tampering | write to a state other than ND | mitigate | loader resolves only the ND node id; Task 3 spot-checks cohort unchanged |
| T-120-05-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both ND loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1 (dollars).
- `processNDRevenueAcfr.js --dry-run --fy 2025` → 4,510,201,793; `--fy 2021` → 3,955,670,947.
- FY2021 fetched via the `-nd`-suffix URL.
- Live: ND node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (ND, fy)).
- Re-run ND --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for ND (LOAD-01).
- ND ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.57×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 120-05-ND-LOADLOG.md finalized.
</verification>

<success_criteria>
The North Dakota state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2021–FY2025, honest holes logged), stored in dollars (UNITS=1), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.57× mild accept-relabel divergence recorded at load, the P2 clamp wired, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands ND to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-05-SUMMARY.md` when done — record the ND FY window actually transcribed, the FY2021 `-nd`-suffix note, any FY omitted as a non-tying honest hole, the two bookend tie confirmations (dollars), the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~1.57×), the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
