---
phase: 120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47
plan: "120-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processNEAcfr.js
  - scripts/processNERevenueAcfr.js
  - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-01-NE-LOADLOG.md
autonomous: true
requirements: [ACFR-43]
user_setup: []

must_haves:
  truths:
    - "Nebraska state node (ccfb8751-ae32-4974-96a9-d8c8ea85a898) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2020–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NE units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL FUND column, Total revenues line): FY2025 = $6,308,910K, FY2020 = $4,993,719K (store ×1,000 raw dollars)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General Fund | Highway | Federal | Health and Social Services | Permanent School | Nonmajor | Totals) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are FULLY DERIVABLE: https://das.nebraska.gov/accounting/docs/NE_DAS_Accounting-Annual_Reports_Annual_Comprehensive_Financial_Report_ACFR_{YYYY}.pdf (confirmed FY2020–FY2025); no naming exceptions — landing table das.nebraska.gov/accounting/financial_reports.php"
    - "Pre-existing NASBO operating rows (NE FY2023 $5,154,000K, FY2024 $5,314,000K) are REPLACED in place at the same (ccfb8751, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (NE ACFR GF ~1.19× NASBO — the SMALLEST divergence in the batch; NE GF is almost entirely own-source, Income Tax $3.09B + Sales/Use $2.62B = 91%, federal flows sit in the separate Federal Fund column not GF) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows"
    - "FY2020 'Other Taxes' = -$193K (minor) renders via the P2 clamp (slice→0, signed magnitude in label, parent nets it) if FY2020 is loaded; FY2025 has no negative GF lines; interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on NE once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNEAcfr.js"
      provides: "NE GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNERevenueAcfr.js"
      provides: "NE GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-01-NE-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~1.19× accept-relabel record, FY2020 clamp note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNEAcfr.js / processNERevenueAcfr.js"
      to: "treasury.budgets (NE state node ccfb8751)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['NE']"
      to: "scripts/processNEAcfr.js + scripts/processNERevenueAcfr.js"
      via: "gen_state.py code generation from ne/ne_all.json + derivable per-FY SOURCES map"
      pattern: "General Fund"
---

<objective>
Upgrade the **Nebraska** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processNEAcfr.js` (GF spending-by-function, `operating`) + `scripts/processNERevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading NE ACFR PDFs FY2020–FY2025 into gitignored `_acfr-work/ne/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**NE is the simplest state in Batch 3** — fully derivable per-year URL, no naming exceptions, thousands units, one immaterial negative (FY2020 "Other Taxes" = -$193K). Bookend guards: **FY2025 $6,308,910K, FY2020 $4,993,719K** (GF Total revenues, ×1,000). Scope note: NE ACFR GF ~1.19× NASBO — the near-parity case in this batch (OH/VA precedent), federal flows are booked to NE's separate Federal Fund column, not GF — accept-and-relabel honestly.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `120-01-NE-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['NE']` entry (mirror the recent 'CO'/'IN'/'AR' clean-derivable entries): name='Nebraska', abbr='NE', population=1_961_504, node='ccfb8751-ae32-4974-96a9-d8c8ea85a898', years=[the FYs that cleanly tie], tolerance (small; NE tied at $0 so a few ×1,000 units is safe), data='ne/ne_all.json', out='NE', base_url='https://das.nebraska.gov/accounting/financial_reports.php', sources={ derivable per-FY URL map }, UNITS=1000. Use `head_note` for the ~1.19× near-parity scope note; `neg_note` for the FY2020 "Other Taxes" -$193K clamp note. Then `py -3 gen_state.py NE` emits scripts/processNEAcfr.js + processNERevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor (anchors on the Total-revenues row's right edge). For NE, capture the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds ne/ne_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline with printed-row evidence.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle (delete-stale-by-dataset_id → insert → RPC/stamp → delete-by-id at end — leaves 0 residue). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ne-acfr-gf-operating' (name 'Nebraska General Fund Operating Budget'), 'ne-acfr-gf-revenue' (name 'Nebraska General Fund Revenue'), api_type='pdf_download', base_url = the DAS financial-reports landing page.
- NASBO baseline (live-confirmed): NE has 2 NASBO operating rows only — FY2023 $5,154,000K, FY2024 $5,314,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH3-SOURCES.md NE Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = 1st column of 7 (General Fund | Highway | Federal | Health and Social Services | Permanent School | Nonmajor | Totals). Units = THOUSANDS. FY-end June 30.
- Clean window FY2020–FY2025 (6 yr, all listed directly on the financial-reports page; pre-FY2020 not checked — out of scope, not a blocker).
- URL — FULLY DERIVABLE: `https://das.nebraska.gov/accounting/docs/NE_DAS_Accounting-Annual_Reports_Annual_Comprehensive_Financial_Report_ACFR_{YYYY}.pdf`. Landing: `https://das.nebraska.gov/accounting/financial_reports.php`. All URLs confirmed `application/pdf` (not soft-404).
- Bookend ties (GF Total revenues): FY2025 $6,308,910K = line-item sum (diff $0); FY2020 $4,993,719K = line-item sum (diff $0).
- FY2020 "Other Taxes" = -$193K (minor, immaterial P2 clamp candidate). FY2025 has no negative GF lines.
- Scope vs NASBO ~1.19× (FY2025 ACFR ~$6.31B vs FY2024 NASBO ~$5.31B) — smallest divergence in the batch; Federal Grants and Contracts is only ~$157K (~0.002%) of NE's GF, the fund is ~91% own-source (Income Tax $3.09B + Sales/Use $2.62B); federal flows are booked to the separate Federal Fund column, not GF. Accept-and-relabel honestly (near-parity, OH/VA precedent).
</source_facts>
</context>

<tasks>

<task id="120-01-01" type="auto">
  <name>Task 1: Generate both NE loaders (UNITS=1000) via gen_state.py + download/extract FY2020–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ne/ (gitignored PDFs + pdftotext + ne_all.json), scripts/processNEAcfr.js, scripts/processNERevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'CO'/'IN'/'AR' clean-derivable entry pattern; confirm how UNITS + sources + tolerance + head_note/neg_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how the revenues section and expenditures section are each emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (NE Detail Block: derivable per-year URL, units=thousands, bookend ties, FY2020 negative)
    - scripts/processCOAcfr.js (a recent gen_state.py clean-derivable output — the exact generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). For each FY across the durable window (FY2020–FY2025), derive the URL `https://das.nebraska.gov/accounting/docs/NE_DAS_Accounting-Annual_Reports_Annual_Comprehensive_Financial_Report_ACFR_{YYYY}.pdf` and download to `_acfr-work/ne/NE{YYYY}.pdf`. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL FUND column (1st of 7) for BOTH the revenues section and the expenditures section. Assemble `_acfr-work/ne/ne_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names (thousands, unscaled at this stage); document any hand-patch inline with printed-row evidence. Add a `CONFIGS['NE']` entry to `_acfr-work/gen_state.py` (node ccfb8751…, years=[the FYs that cleanly tie], UNITS=1000, tolerance small, data='ne/ne_all.json', out='NE', base_url the DAS financial-reports landing page, sources=derivable per-FY URL map, head_note recording the ~1.19× near-parity accept-relabel, neg_note the FY2020 "Other Taxes" -$193K clamp note). Run `py -3 gen_state.py NE` to emit `scripts/processNEAcfr.js` + `scripts/processNERevenueAcfr.js` (dataset_ids ne-acfr-gf-operating / ne-acfr-gf-revenue). Cross-check FY2025 revenue stored = 6,308,910×1000 and FY2020 = 4,993,719×1000 — if either bookend does not match, STOP and re-inspect column/statement selection before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processNEAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNEAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNERevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '6,308,910,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ne/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected.
    - `node scripts/processNEAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints TOTAL 6,308,910,000 and `--fy 2020` prints 4,993,719,000 (×1,000).
    - Both loaders carry `UNITS = 1000`; `_acfr-work/gen_state.py` has a `CONFIGS['NE']` entry; `_acfr-work/ne/ne_all.json` exists with per-FY operating + revenue trees.
    - FY2020 "Other Taxes" -$193K routes through clampForRender (slice→0, signed magnitude in label) if FY2020 is present.
    - dataSource labels are GAAP-basis-labelled ("Nebraska State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='ne-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both NE loaders exist (generated by gen_state.py) with UNITS=1000, derivable SOURCES map, ne_all.json holds every tied FY, both bookends (6,308,910K / 4,993,719K) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="120-01-02" type="auto">
  <name>Task 2: Live-load NE (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNEAcfr.js, scripts/processNERevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNEAcfr.js + scripts/processNERevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; NE node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the NE node (ccfb8751) and RECORD the pre-load NASBO operating rows (FY2023 $5,154,000K, FY2024 $5,314,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processNEAcfr.js --fy <YYYY>` then `node scripts/processNERevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (ccfb8751, fy, 'operating') where a NASBO year overlaps; revenue inserts net-new at (ccfb8751, fy, 'revenue')), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded NE GF operating + revenue totals against the pre-load NASBO totals to confirm the ~1.19× accept-relabel divergence is the expected near-parity own-source-GF mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "NE row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NE node has, per loaded FY, an operating row "Nebraska State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - NE FY2025 revenue row total = 6,308,910,000 and FY2020 = 4,993,719,000 (×1,000).
    - NE FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (NE, fy), GAAP-labelled; no "NASBO" label remains on the NE node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.19× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed NE FY has operating + revenue rows in treasury.budgets, thousands-scaled (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="120-01-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-01-NE-LOADLOG.md</files>
  <read_first>
    - scripts/processNEAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NE --fy 2025 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded NE FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) NE has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (NE, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — NE has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for NE; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged (row counts + a couple data_source labels). Append a "Load Disposition" section to 120-01-NE-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the ~1.19× accept-relabel note, the FY2020 clamp note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than NE.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 120-01-NE-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of NE --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded NE FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; NE has ZERO remaining "NASBO" operating rows; exactly one operating row per (NE, fy).
    - NE has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for NE (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 120-01-NE-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms NE fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 120-01-NE-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| das.nebraska.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the NE node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-01-A | Tampering | units mis-scale (applying/omitting the ×1,000) inflates or deflates NE 1000× | mitigate | UNITS=1000 hard-set; bookend totals (6,308,910K / 4,993,719K) asserted in dry-run + live |
| T-120-01-B | Tampering | extracting the wrong statement/column (Statement of Activities, or Highway/Federal/Nonmajor/Totals instead of GENERAL FUND) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-120-01-C | Tampering | wrong derivable-URL year → wrong-year or 404 PDF | mitigate | %PDF magic + size guard; honest hole if a year 404s; landing table cross-check |
| T-120-01-D | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (NE, fy), zero NASBO labels |
| T-120-01-E | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-120-01-F | Info Disclosure | silently inflating NE (~1.19×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-120-01-G | Tampering | write to a state other than NE | mitigate | loader resolves only the NE node id; Task 3 spot-checks cohort unchanged |
| T-120-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both NE loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processNERevenueAcfr.js --dry-run --fy 2025` → 6,308,910,000; `--fy 2020` → 4,993,719,000.
- Live: NE node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (NE, fy)).
- Re-run NE --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for NE (LOAD-01).
- NE ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.19×) recorded against pre-load NASBO totals; FY2020 clamp note recorded.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 120-01-NE-LOADLOG.md finalized.
</verification>

<success_criteria>
The Nebraska state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2020–FY2025, honest holes logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.19× near-parity accept-relabel divergence recorded at load, the FY2020 P2 clamp wired, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands NE to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-01-SUMMARY.md` when done — record the NE FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~1.19×), the FY2020 clamp note, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
