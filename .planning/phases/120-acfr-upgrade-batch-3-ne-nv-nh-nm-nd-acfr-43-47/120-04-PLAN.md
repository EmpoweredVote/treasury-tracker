---
phase: 120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47
plan: "120-04"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processNMAcfr.js
  - scripts/processNMRevenueAcfr.js
  - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-04-NM-LOADLOG.md
autonomous: true
requirements: [ACFR-46]
user_setup: []

must_haves:
  truths:
    - "New Mexico state node (1e60ff76-c9fa-48d0-9442-042f61cd40ea) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2019–FY2024), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NM units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL FUND column, Total revenues line): FY2024 = $30,530,269K, FY2019 = $15,358,087K (store ×1,000 raw dollars)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General Fund | Severance Tax/Debt Service Fund | Land Grant/Capital Projects Fund) — NOT the Statement of Activities, NOT budgetary"
    - "Source URLs are OPAQUE WordPress-upload slugs (NO shared pattern) under nmdfa.state.nm.us — explicit per-FY SOURCES map required: FY2024=wp-content/uploads/2025/04/FINAL-341a-State-of-New-Mexico-FY24-ACFR.pdf, FY2022=wp-content/uploads/2023/07/Agency-341-A-SoNM-FY22-ACFR-Final.pdf, FY2019=wp-content/uploads/2021/01/Final-Version-State-of-New-Mexico-CAFR-2019-Audit-05-07-20.pdf"
    - "FY2022 numeric table is IMAGE-ONLY (raster, not pdftotext-extractable — KY FY2023 precedent). If FY2022 is loaded, its GF figures must be EMBEDDED as static/transcribed data (NJ Phase 115 embedded-data precedent), already hand-verified in recon: GF Total revenues = $26,161,736K, GF Total expenditures = $20,159,689K (both tie exactly). FY2022 'Investment Income (Loss)' = -$91,222K → P2 clamp REQUIRED if FY2022 loaded"
    - "FY2023 URL was NOT located in recon (opaque slugs non-guessable). The loader MUST crawl nmdfa.state.nm.us / its statewide-financial-reporting-accountability-bureau page live to find the FY2023 URL before deciding to load or honest-hole it — do not silently skip without attempting discovery"
    - "Pre-existing NASBO operating rows (NM FY2023 $8,682,000K, FY2024 $9,975,000K) are REPLACED in place at the same (1e60ff76, fy, 'operating') key where an ACFR year overlaps — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (NM ACFR GF ~3.06× NASBO — 'Federal Revenue' = $11.69B = 38% of FY2024 GF + 'General and Selective Taxes' $8.02B + 'Rentals and Royalties' (oil & gas) $5.35B, federal-passthrough + own-source royalty stream both consolidated into GF; TX-trap mechanism) recorded at load; GAAP basis label verified on live rows"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on NM once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNMAcfr.js"
      provides: "NM GF spending-by-function loader (dataset_type='operating'), UNITS=1000, FY2022 embedded-data path, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNMRevenueAcfr.js"
      provides: "NM GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-04-NM-LOADLOG.md"
      provides: "Per-FY load disposition, the FY2022 image-only/embedded-data note + FY2022 clamp, the FY2023 live-discovery result, NASBO-replacement confirmation, ~3.06× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNMAcfr.js / processNMRevenueAcfr.js"
      to: "treasury.budgets (NM state node 1e60ff76)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['NM']"
      to: "scripts/processNMAcfr.js + scripts/processNMRevenueAcfr.js"
      via: "gen_state.py code generation from nm/nm_all.json (FY2022 embedded) + enumerated opaque WordPress-slug SOURCES map"
      pattern: "General Fund"
---

<objective>
Upgrade the **New Mexico** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processNMAcfr.js` (GF spending-by-function, `operating`) + `scripts/processNMRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading NM ACFR PDFs FY2019–FY2024 into gitignored `_acfr-work/nm/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**NM has two load-time findings.** (1) **FY2022's numeric table is image-only** (raster, not `pdftotext`-extractable — KY FY2023 precedent); if FY2022 is loaded, embed its hand-verified GF figures as static/transcribed data (NJ Phase 115 embedded-data precedent): GF Total revenues **$26,161,736K**, expenditures **$20,159,689K**, and apply a **P2 clamp** to FY2022's "Investment Income (Loss)" = -$91,222K. (2) **FY2023's URL was not located** (opaque WordPress slugs, non-guessable); crawl `nmdfa.state.nm.us` live to find it before loading or honest-holing it. Bookend guards: **FY2024 $30,530,269K, FY2019 $15,358,087K** (thousands, ×1,000). Scope note: NM ACFR GF ~3.06× NASBO (Federal 38% + oil/gas royalties) — accept-and-relabel honestly (TX precedent).

Output: both loaders live-loaded across the tied window (FY2022 embedded, FY2023 per live-discovery, honest holes logged), NASBO replaced in place, Money In auto-enabled; `120-04-NM-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['NM']` entry (mirror the 'GA'/'NC' opaque-filename entries + the NJ/CT embedded-data pattern for the one image-only year): name='New Mexico', abbr='NM', population=2_117_522, node='1e60ff76-c9fa-48d0-9442-042f61cd40ea', years=[the FYs that cleanly tie], tolerance (small; NM tied at $0), data='nm/nm_all.json', out='NM', base_url='https://www.nmdfa.state.nm.us/financial-control/statewide-financial-reporting-accountability-bureau/', sources={ enumerated opaque WordPress-slug per-FY URL map }, UNITS=1000. Use `head_note` for the ~3.06× TX-trap scope note + the FY2022-embedded + FY2023-discovery notes; `neg_note` for the FY2022 investment-income clamp. Then `py -3 gen_state.py NM` emits scripts/processNMAcfr.js + processNMRevenueAcfr.js.
- **FY2022 embedded-data deviation:** FY2022's Statement pages render as a raster image — `pdftotext -table`/`-layout` return zero numeric content. Do NOT run FY2022 through the automated extract; embed the hand-verified FY2022 GF trees directly into nm/nm_all.json (NJ Phase 115 precedent), with printed-row evidence documented inline. FY2022 GF Total revenues = $26,161,736K, expenditures = $20,159,689K; "Investment Income (Loss)" = -$91,222K needs the P2 clamp.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor for the machine-readable years (FY2019, FY2024, and FY2023 if its URL is found and it extracts). Captures the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds nm/nm_all.json.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'nm-acfr-gf-operating' (name 'New Mexico General Fund Operating Budget'), 'nm-acfr-gf-revenue' (name 'New Mexico General Fund Revenue'), api_type='pdf_download', base_url = the NM DFA statewide-financial-reporting page.
- NASBO baseline (live-confirmed): NM has 2 NASBO operating rows only — FY2023 $8,682,000K, FY2024 $9,975,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH3-SOURCES.md NM Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = 1st column of 3 (General Fund | Severance Tax/Debt Service Fund | Land Grant/Capital Projects Fund). Units = THOUSANDS. FY-end June 30.
- Clean window FY2019–FY2024 (6 yr; FY2022 image-only caveat; FY2023 URL gap-logged).
- URLs — OPAQUE WordPress-upload slugs, NO shared pattern, explicit enumeration required:
  - FY2024 `https://www.nmdfa.state.nm.us/wp-content/uploads/2025/04/FINAL-341a-State-of-New-Mexico-FY24-ACFR.pdf`
  - FY2022 `https://www.nmdfa.state.nm.us/wp-content/uploads/2023/07/Agency-341-A-SoNM-FY22-ACFR-Final.pdf`
  - FY2019 `https://www.nmdfa.state.nm.us/wp-content/uploads/2021/01/Final-Version-State-of-New-Mexico-CAFR-2019-Audit-05-07-20.pdf`
  - FY2023 not located in recon — crawl `nmdfa.state.nm.us` live at load time
  - Landing: `https://www.nmdfa.state.nm.us/financial-control/statewide-financial-reporting-accountability-bureau/` (does not itself link the current ACFR — found via Wayback CDX)
- Bookend ties (GF Total revenues): FY2024 $30,530,269K = line-item sum (diff $0, clean pdftotext); FY2019 $15,358,087K = line-item sum (diff $0, clean pdftotext).
- FY2022 (mid-window, IMAGE-ONLY, hand-verified): GF Total revenues $26,161,736K, GF Total expenditures $20,159,689K (both tie exactly). "Investment Income (Loss)" = -$91,222K → P2 clamp required if loaded.
- Scope vs NASBO ~3.06× — "Federal Revenue" = $11,691,941K in FY2024 (38%) + "Rentals and Royalties" (oil & gas) $5,353,926K + "General and Selective Taxes" $8.02B, all consolidated into GF. Accept-and-relabel honestly (TX precedent).
</source_facts>
</context>

<tasks>

<task id="120-04-01" type="auto">
  <name>Task 1: Generate both NM loaders (UNITS=1000, opaque SOURCES map, FY2022 embedded) via gen_state.py + download/extract + FY2023 live-discovery + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/nm/ (gitignored PDFs + pdftotext + nm_all.json), scripts/processNMAcfr.js, scripts/processNMRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a 'GA'/'NC' opaque-filename entry; confirm how UNITS + the explicit sources map + tolerance + head_note/neg_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how the revenues and expenditures sections are each emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (NM Detail Block: enumerated opaque slugs, units=thousands, bookend ties, the FY2022 image-only finding + hand-verified figures, the FY2023 URL gap)
    - scripts/processGAAcfr.js (a recent gen_state.py opaque-filename output — the generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). Download the OPAQUE per-year URLs (enumerated in source_facts) for FY2024 and FY2019 to `_acfr-work/nm/NM{YYYY}.pdf`. **Attempt FY2023 URL discovery**: crawl `nmdfa.state.nm.us` (and its statewide-financial-reporting-accountability-bureau page / Wayback CDX of `nmdfa.state.nm.us/wp-content/uploads/`) to locate the FY2023 ACFR PDF; if found, download + extract it too; if not found within a reasonable effort, honest-hole FY2023 and note it. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on the machine-readable years, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL FUND column (1st of 3) for BOTH the revenues section and the expenditures section. **FY2022 is image-only** — do NOT run it through `pdftotext`; instead EMBED the hand-verified FY2022 GF trees directly into nm/nm_all.json (revenues total $26,161,736K, expenditures total $20,159,689K, "Investment Income (Loss)" = -$91,222K), with the printed-row evidence documented inline (NJ Phase 115 precedent). Assemble `_acfr-work/nm/nm_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, unscaled at this stage). Add a `CONFIGS['NM']` entry to `_acfr-work/gen_state.py` (node 1e60ff76…, years=[the tied FYs], UNITS=1000, tolerance small, data='nm/nm_all.json', out='NM', base_url the NM DFA page, sources=enumerated opaque slug map (+ FY2023 if discovered), head_note recording the ~3.06× TX-trap accept-relabel + FY2022-embedded + FY2023-discovery notes, neg_note the FY2022 investment-income clamp). Run `py -3 gen_state.py NM` to emit `scripts/processNMAcfr.js` + `scripts/processNMRevenueAcfr.js` (dataset_ids nm-acfr-gf-operating / nm-acfr-gf-revenue). Cross-check FY2024 revenue stored = 30,530,269×1000 and FY2019 = 15,358,087×1000; FY2022 (if loaded) revenue = 26,161,736×1000 — if any bookend does not match, STOP and re-inspect. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processNMAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNMAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNMRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '30,530,269,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nm/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY (FY2024, FY2019, + FY2023 if discovered); soft-404/HTML rejected.
    - `node scripts/processNMAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2024` prints TOTAL 30,530,269,000 and `--fy 2019` prints 15,358,087,000 (×1,000); if FY2022 loaded, `--fy 2022` prints 26,161,736,000.
    - FY2022 GF figures are EMBEDDED (not from an automated pdftotext pass); FY2022 "Investment Income (Loss)" -$91,222K routes through clampForRender.
    - Both loaders carry `UNITS = 1000`; `_acfr-work/gen_state.py` has a `CONFIGS['NM']` entry with the enumerated opaque SOURCES map; `_acfr-work/nm/nm_all.json` exists with per-FY operating + revenue trees.
    - The FY2023 live-discovery result is noted (URL found → loaded; not found → honest hole).
    - dataSource labels are GAAP-basis-labelled ("New Mexico State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='nm-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both NM loaders exist (generated by gen_state.py) with UNITS=1000, enumerated opaque SOURCES map, nm_all.json holds every tied FY (FY2022 embedded + clamped), both bookends (30,530,269K / 15,358,087K) dry-run-tie, the FY2023 discovery attempt is done, and no live write occurred.</done>
</task>

<task id="120-04-02" type="auto">
  <name>Task 2: Live-load NM (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNMAcfr.js, scripts/processNMRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNMAcfr.js + scripts/processNMRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; NM node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the NM node (1e60ff76) and RECORD the pre-load NASBO operating rows (FY2023 $8,682,000K, FY2024 $9,975,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processNMAcfr.js --fy <YYYY>` then `node scripts/processNMRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (1e60ff76, fy, 'operating') where a NASBO year overlaps — e.g. FY2024, and FY2023 if its ACFR is loaded; revenue inserts net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. Confirm the embedded FY2022 year loads with its clamp applied. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded NM GF operating + revenue totals against the pre-load NASBO totals to confirm the ~3.06× accept-relabel divergence is the expected federal+royalty-inside-GAAP-GF mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "NM row existence + NASBO-replacement + FY2022-embedded-clamp + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NM node has, per loaded FY, an operating row "New Mexico State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - NM FY2024 revenue row total = 30,530,269,000 and FY2019 = 15,358,087,000 (×1,000); if FY2022 loaded, its revenue row = 26,161,736,000 with the negative Investment-Income category clamped.
    - Any NM NASBO operating year overlapped by a loaded ACFR year is GONE (replaced in place): exactly ONE operating row per (NM, fy), GAAP-labelled. (If FY2023 was honest-holed, its NASBO row may legitimately remain — record which.)
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~3.06× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed NM FY has operating + revenue rows in treasury.budgets, thousands-scaled (UNITS=1000), GAAP-labelled + per-year-sourced; overlapping NASBO operating replaced in place (no duplicates); FY2022 embedded + clamped; accept-relabel divergence recorded.</done>
</task>

<task id="120-04-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-04-NM-LOADLOG.md</files>
  <read_first>
    - scripts/processNMAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NM --fy 2024 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded NM FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) every NM NASBO operating year overlapped by a loaded ACFR year has NO remaining "NASBO" row (exactly one operating row per (NM, fy)); (c) honest-hole FYs (incl. FY2023 if not discovered) ABSENT from ACFR — record which NASBO rows, if any, legitimately remain; (d) Money In auto-enable — NM has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for NM; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 120-04-NM-LOADLOG.md: FYs loaded, FYs skipped (+reason), the FY2022 image-only/embedded-data note + FY2022 clamp, the FY2023 live-discovery result, pre-load-NASBO-vs-loaded-ACFR totals with the ~3.06× accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than NM.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 120-04-NM-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of NM --fy 2024 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded NM FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; every ACFR-overlapped year has ZERO "NASBO" operating rows; exactly one operating row per (NM, fy).
    - NM has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for NM (LOAD-01 holds).
    - Honest-hole FYs (incl. FY2023 if not discovered) documented; any legitimately-remaining NASBO row recorded explicitly.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 120-04-NM-LOADLOG.md has a finalized "Load Disposition" section documenting the FY2022 embedded-data + FY2023 discovery outcomes.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms NM fully ACFR-sourced across the loaded window (operating + revenue, GAAP-labelled, overlapping NASBO replaced, FY2022 embedded+clamped, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 120-04-NM-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nmdfa.state.nm.us PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| hand-transcribed FY2022 figures → embedded data | manually keyed values become displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the NM node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-04-A | Tampering | FY2022 image-only page silently yields a $0/blank GF row if run through the automated pdftotext pass | mitigate | FY2022 embedded as hand-verified static data (NJ Phase 115 precedent); dry-run asserts FY2022 revenue = 26,161,736,000 |
| T-120-04-B | Tampering | FY2022 negative "Investment Income (Loss)" -$91,222K displayed as negative revenue in the UI | mitigate | P2 clamp (slice→0, signed magnitude in label, parent nets it); neg_note wired into the generated loader |
| T-120-04-C | Tampering | units mis-scale (applying/omitting the ×1,000) inflates or deflates NM 1000× | mitigate | UNITS=1000 hard-set; bookend totals (30,530,269K / 15,358,087K) asserted in dry-run + live |
| T-120-04-D | Tampering | extracting the wrong statement/column (Severance/Land Grant instead of GENERAL FUND, or the Statement of Activities) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-120-04-E | Tampering | wrong opaque WordPress slug → wrong-year or 404 PDF; FY2023 silently skipped without a discovery attempt | mitigate | each per-FY URL verified live; %PDF magic + size guard; FY2023 requires an explicit live crawl before honest-holing |
| T-120-04-F | Tampering | NASBO row left alongside an overlapping ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (NM, fy) |
| T-120-04-G | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-120-04-H | Info Disclosure | silently inflating NM (~3.06×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-120-04-I | Tampering | write to a state other than NM | mitigate | loader resolves only the NM node id; Task 3 spot-checks cohort unchanged |
| T-120-04-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both NM loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processNMRevenueAcfr.js --dry-run --fy 2024` → 30,530,269,000; `--fy 2019` → 15,358,087,000; `--fy 2022` (if loaded) → 26,161,736,000 with the negative Investment-Income category clamped.
- FY2022 GF figures are embedded (not automated pdftotext); FY2023 live-discovery attempted.
- Live: NM node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; every ACFR-overlapped NASBO year replaced in place (one operating row per (NM, fy)).
- Re-run NM --fy 2024 → "Loaded 0 rows"; 0 data_sources residue for NM (LOAD-01).
- NM ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~3.06×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 120-04-NM-LOADLOG.md finalized.
</verification>

<success_criteria>
The New Mexico state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2019–FY2024, FY2022 embedded from hand-verified figures + clamped, FY2023 per live-discovery, honest holes logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, overlapping NASBO operating replaced in place, the ~3.06× accept-relabel divergence recorded at load, the FY2022 P2 clamp wired, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands NM to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-04-SUMMARY.md` when done — record the NM FY window actually transcribed, the FY2022 image-only/embedded-data note + FY2022 clamp, the FY2023 live-discovery result, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~3.06×), the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
