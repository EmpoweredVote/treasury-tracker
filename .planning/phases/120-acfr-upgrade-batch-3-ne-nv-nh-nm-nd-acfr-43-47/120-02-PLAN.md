---
phase: 120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47
plan: "120-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processNVAcfr.js
  - scripts/processNVRevenueAcfr.js
  - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-02-NV-LOADLOG.md
autonomous: true
requirements: [ACFR-44]
user_setup: []

must_haves:
  truths:
    - "Nevada state node (d0879e45-0b72-41ee-bdbd-a214a4f2a1d5) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its confirmed durable window (FY2019–FY2023), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NV units are DOLLARS: UNITS=1 (NOT thousands — this is the #1 load risk for NV). Bookend ties (GENERAL FUND column, Total revenues line): FY2023 = $15,153,168,081, FY2019 = $10,411,179,917 (store raw dollars, no ×1,000)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (General Fund | State Education Fund | Nonmajor Governmental Funds | Total Governmental Funds) — NOT the Statement of Activities, NOT budgetary"
    - "Source URLs are OPAQUE / NON-DERIVABLE — explicit per-FY SOURCES map required under https://www.controller.nv.gov/siteassets/content/financialrpts/acfr/: FY2023=2023-acfr-report.pdf, FY2022=2022_ACFR_Report.pdf, FY2021=FY21_ACFR.pdf, FY2020=ACFR_FY2020.pdf, FY2019=CAFR_Web_2019.pdf"
    - "PARTIAL-WINDOW load (D-07): NV has NOT published a FY2024/FY2025 ACFR (controller.nv.gov states docs are 'currently being remediated'). Re-check controller.nv.gov at load time; if FY2024 still absent, load FY2019–FY2023 on ACFR and LEAVE the FY2024 NASBO operating row in place with its honest NASBO label — do NOT fabricate or omit the latest covered year"
    - "Pre-existing NASBO operating rows: NV FY2023 $4,742,000K is REPLACED in place by ACFR (FY2023 is in the ACFR window); NV FY2024 $5,273,000K STAYS as a NASBO-sourced row (ACFR unavailable) unless a FY2024 ACFR is found at load time"
    - "Accept-and-relabel scope divergence (NV ACFR GF ~2.87× NASBO — GAAP GF consolidates federal Medicaid/grant pass-through: 'Intergovernmental' = $8.94B = 59% of FY2023 GF revenue; TX/NC-trap mechanism) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows"
    - "No negative GF line items observed at either bookend (Investment Income positive in FY2019 and FY2023); the P2 clamp path stays wired for any interior year, checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on NV once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNVAcfr.js"
      provides: "NV GF spending-by-function loader (dataset_type='operating'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNVRevenueAcfr.js"
      provides: "NV GF revenue-by-source loader (dataset_type='revenue'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-02-NV-LOADLOG.md"
      provides: "Per-FY load disposition, FY2024-NASBO-retained note, FY2024 re-check result, NASBO-replacement confirmation, ~2.87× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNVAcfr.js / processNVRevenueAcfr.js"
      to: "treasury.budgets (NV state node d0879e45)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['NV']"
      to: "scripts/processNVAcfr.js + scripts/processNVRevenueAcfr.js"
      via: "gen_state.py code generation from nv/nv_all.json + enumerated opaque per-FY SOURCES map, UNITS=1"
      pattern: "General Fund"
---

<objective>
Upgrade the **Nevada** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processNVAcfr.js` (GF spending-by-function, `operating`) + `scripts/processNVRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading NV ACFR PDFs FY2019–FY2023 into gitignored `_acfr-work/nv/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO FY2023 replaced in place, FY2024 NASBO retained, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**NV is DOLLARS, not thousands — UNITS=1 (the #1 NV load risk).** Bookend guards: **FY2023 $15,153,168,081, FY2019 $10,411,179,917** (GF Total revenues, raw dollars). **URLs are opaque / non-derivable** — enumerate the explicit per-FY SOURCES map. **PARTIAL-WINDOW (D-07):** NV has not published a FY2024/FY2025 ACFR (site under "remediation"); re-check at load time, else load FY2019–FY2023 on ACFR and leave FY2024 NASBO-sourced with its honest label. Scope note: NV ACFR GF ~2.87× NASBO — Intergovernmental (federal Medicaid pass-through) is 59% of GF — accept-and-relabel honestly (TX precedent).

Output: both loaders live-loaded across FY2019–FY2023 (or wider if a newer ACFR is found), FY2023 NASBO replaced in place / FY2024 NASBO retained, Money In auto-enabled; `120-02-NV-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['NV']` entry (mirror the 'GA'/'NC' opaque-filename entries): name='Nevada', abbr='NV', population=3_104_614, node='d0879e45-0b72-41ee-bdbd-a214a4f2a1d5', years=[the FYs that cleanly tie, FY2019–FY2023 target], tolerance (small; NV tied at $0), data='nv/nv_all.json', out='NV', base_url='https://controller.nv.gov/financial-reports/annual-comprehensive-financial-report-acfr/', sources={ enumerated per-FY opaque URL map }, **UNITS=1 (dollars)**. Use `head_note` for the ~2.87× TX-trap scope note + the partial-window/FY2024-NASBO-retained note. Then `py -3 gen_state.py NV` emits scripts/processNVAcfr.js + processNVRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For NV, capture the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds nv/nv_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Values are in DOLLARS already; do NOT scale.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle (delete-stale-by-dataset_id → insert → RPC/stamp → delete-by-id at end — leaves 0 residue). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'nv-acfr-gf-operating' (name 'Nevada General Fund Operating Budget'), 'nv-acfr-gf-revenue' (name 'Nevada General Fund Revenue'), api_type='pdf_download', base_url = the NV Controller ACFR landing page.
- NASBO baseline (live-confirmed): NV has 2 NASBO operating rows only — FY2023 $4,742,000K, FY2024 $5,273,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node). FY2023 is inside the ACFR window (replaced); FY2024 is NOT (retained).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH3-SOURCES.md NV Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = 1st column of 4 (General Fund | State Education Fund | Nonmajor Governmental Funds | Total Governmental Funds). Units = DOLLARS (not thousands). FY-end June 30.
- Clean window FY2019–FY2023 (5 yr confirmed). FY2024/FY2025 NOT found under any tested naming variant — genuine current-publication gap (site under "remediation").
- URLs — OPAQUE, non-derivable, explicit enumeration required, base `https://www.controller.nv.gov/siteassets/content/financialrpts/acfr/`:
  - FY2023 `2023-acfr-report.pdf` (also mirrored at the stable alias `annual-comprehensive-financial-report.pdf`)
  - FY2022 `2022_ACFR_Report.pdf`
  - FY2021 `FY21_ACFR.pdf`
  - FY2020 `ACFR_FY2020.pdf`
  - FY2019 `CAFR_Web_2019.pdf`
  - Landing: `https://controller.nv.gov/financial-reports/annual-comprehensive-financial-report-acfr/`
- Bookend ties (GF Total revenues, DOLLARS): FY2023 $15,153,168,081 = line-item sum (diff $0); FY2019 $10,411,179,917 = line-item sum (diff $0).
- No negative GF line items observed at either bookend (Investment Income positive in FY2019 and FY2023).
- Scope vs NASBO ~2.87× — "Intergovernmental" = $8,940,557,604 in FY2023 = 59% of GF revenue (federal Medicaid/grant pass-through booked directly into GF). Accept-and-relabel honestly (TX precedent).
</source_facts>
</context>

<tasks>

<task id="120-02-01" type="auto">
  <name>Task 1: Generate both NV loaders (UNITS=1 dollars, opaque SOURCES map) via gen_state.py + download/extract FY2019–FY2023 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/nv/ (gitignored PDFs + pdftotext + nv_all.json), scripts/processNVAcfr.js, scripts/processNVRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a 'GA'/'NC' opaque-filename entry pattern; confirm how UNITS + the explicit sources map + tolerance + head_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how the revenues and expenditures sections are each emitted; note NV values are already in dollars)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (NV Detail Block: enumerated opaque per-year URLs, units=DOLLARS, bookend ties, FY2024 gap)
    - scripts/processGAAcfr.js (a recent gen_state.py opaque-filename output — the exact generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). **Re-check `controller.nv.gov` for a FY2024 (and FY2025) ACFR before finalizing the window** (D-07); if a newer ACFR has since posted, add it to the SOURCES map and window. Otherwise use the confirmed FY2019–FY2023 window. For each FY, download the OPAQUE per-year URL (enumerated in source_facts) to `_acfr-work/nv/NV{YYYY}.pdf`. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL FUND column (1st of 4) for BOTH the revenues section and the expenditures section. **Values are in DOLLARS — set UNITS=1, do NOT multiply by 1000.** Assemble `_acfr-work/nv/nv_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names; document any hand-patch inline with printed-row evidence. Add a `CONFIGS['NV']` entry to `_acfr-work/gen_state.py` (node d0879e45…, years=[the tied FYs], **UNITS=1**, tolerance small, data='nv/nv_all.json', out='NV', base_url the NV Controller ACFR landing page, sources=enumerated opaque URL map, head_note recording the ~2.87× TX-trap accept-relabel + the partial-window/FY2024-NASBO-retained note). Run `py -3 gen_state.py NV` to emit `scripts/processNVAcfr.js` + `scripts/processNVRevenueAcfr.js` (dataset_ids nv-acfr-gf-operating / nv-acfr-gf-revenue). Cross-check FY2023 revenue stored = 15,153,168,081 and FY2019 = 10,411,179,917 — if either bookend does not match, STOP and re-inspect column selection AND unit scaling before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processNVAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNVAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNVRevenueAcfr.js --dry-run --fy 2023 2>&1 | grep -q '15,153,168,081'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nv/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected; each opaque URL verified live.
    - `node scripts/processNVAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2023` prints TOTAL 15,153,168,081 and `--fy 2019` prints 10,411,179,917 (raw dollars, NOT ×1,000).
    - Both loaders carry `UNITS = 1`; `_acfr-work/gen_state.py` has a `CONFIGS['NV']` entry with the enumerated opaque SOURCES map; `_acfr-work/nv/nv_all.json` exists with per-FY operating + revenue trees.
    - The FY2024 re-check result is noted (found → added; absent → partial window FY2019–FY2023 confirmed).
    - dataSource labels are GAAP-basis-labelled ("Nevada State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='nv-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both NV loaders exist (generated by gen_state.py) with UNITS=1 (dollars), enumerated opaque SOURCES map, nv_all.json holds every tied FY, both bookends (15,153,168,081 / 10,411,179,917) dry-run-tie, the P2 clamp path is wired, the FY2024 re-check is done, and no live write occurred.</done>
</task>

<task id="120-02-02" type="auto">
  <name>Task 2: Live-load NV (operating + revenue) FY2019–FY2023, FY2023 NASBO replaced in place, FY2024 NASBO retained</name>
  <files>(runs scripts/processNVAcfr.js, scripts/processNVRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNVAcfr.js + scripts/processNVRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; NV node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the NV node (d0879e45) and RECORD the pre-load NASBO operating rows (FY2023 $4,742,000K, FY2024 $5,273,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (FY2019–FY2023, minus honest holes), run `node scripts/processNVAcfr.js --fy <YYYY>` then `node scripts/processNVRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (d0879e45, 2023, 'operating'); revenue inserts net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. **Do NOT touch the FY2024 NASBO operating row** unless a FY2024 ACFR was found in Task 1 — it stays as the honest NASBO-sourced latest-year row. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded NV GF operating + revenue totals against the pre-load NASBO totals to confirm the ~2.87× accept-relabel divergence is the expected federal-Medicaid-inside-GAAP-GF mechanism, not a units bug.
  </action>
  <verify>
    <automated>echo "NV row existence + FY2023-NASBO-replacement + FY2024-NASBO-retained + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NV node has, per loaded ACFR FY (FY2019–FY2023), an operating row "Nevada State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - NV FY2023 revenue row total = 15,153,168,081 and FY2019 = 10,411,179,917 (raw dollars).
    - NV FY2023 NASBO operating row is GONE (replaced in place by ACFR); the NV FY2024 NASBO operating row is STILL PRESENT with its honest "NASBO…" label (no ACFR available for FY2024).
    - Exactly ONE operating row per (NV, fy) for every FY.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~2.87× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed NV ACFR FY (FY2019–FY2023) has operating + revenue rows in treasury.budgets, dollar-scaled (UNITS=1), GAAP-labelled + per-year-sourced; FY2023 NASBO operating replaced in place, FY2024 NASBO retained; accept-relabel divergence recorded.</done>
</task>

<task id="120-02-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-02-NV-LOADLOG.md</files>
  <read_first>
    - scripts/processNVAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NV --fy 2023 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded NV ACFR FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) NV FY2019–FY2023 have NO remaining "NASBO State Expenditure Report" operating rows; **the FY2024 NASBO row is intentionally still present** (record it explicitly so it isn't mistaken for a miss); exactly one operating row per (NV, fy); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — NV has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for NV; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 120-02-NV-LOADLOG.md: FYs loaded, FYs skipped (+reason), the FY2024 re-check result + FY2024-NASBO-retained rationale, pre-load-NASBO-vs-loaded-ACFR totals with the ~2.87× accept-relabel note, the NASBO-replacement confirmation (FY2023 replaced / FY2024 retained), and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than NV.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 120-02-NV-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of NV --fy 2023 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded NV ACFR FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; NV FY2019–FY2023 have ZERO "NASBO" operating rows; the FY2024 NASBO row is present-and-accounted-for; exactly one operating row per (NV, fy).
    - NV has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for NV (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 120-02-NV-LOADLOG.md has a finalized "Load Disposition" section documenting the partial-window decision.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms NV FY2019–FY2023 fully ACFR-sourced (operating + revenue, GAAP-labelled, FY2023 NASBO replaced, FY2024 NASBO deliberately retained, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 120-02-NV-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller.nv.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the NV node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-120-02-A | Tampering | units mis-scale — applying a spurious ×1,000 to NV's already-dollars figures inflates NV 1000× | mitigate | UNITS=1 hard-set (dollars); bookend totals (15,153,168,081 / 10,411,179,917) asserted in dry-run + live; recon flags NV as the units trap of the batch |
| T-120-02-B | Tampering | extracting the wrong statement/column (State Education Fund/Nonmajor/Total instead of GENERAL FUND, or the Statement of Activities) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-120-02-C | Tampering | wrong opaque filename → wrong-year or 404 PDF | mitigate | each per-FY URL verified live against the enumerated map; %PDF magic + size guard; honest hole if a year 404s |
| T-120-02-D | Info Disclosure | dropping the FY2024 NASBO row (stranding the latest covered year) OR fabricating a FY2024 ACFR | mitigate | partial-window rule: load FY2019–FY2023 ACFR, retain FY2024 NASBO with honest label; Task 3 records FY2024 explicitly |
| T-120-02-E | Tampering | FY2023 NASBO row left alongside the FY2023 ACFR row (duplicate) | mitigate | RPC keyed (muni, 2023, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (NV, fy) |
| T-120-02-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-120-02-G | Info Disclosure | silently inflating NV (~2.87×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-120-02-H | Tampering | write to a state other than NV | mitigate | loader resolves only the NV node id; Task 3 spot-checks cohort unchanged |
| T-120-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both NV loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1 (dollars).
- `processNVRevenueAcfr.js --dry-run --fy 2023` → 15,153,168,081; `--fy 2019` → 10,411,179,917.
- Live: NV FY2019–FY2023 have operating + revenue rows, GAAP-labelled, non-null sources; FY2023 NASBO replaced in place; FY2024 NASBO row retained (honest label); one operating row per (NV, fy).
- Re-run NV --fy 2023 → "Loaded 0 rows"; 0 data_sources residue for NV (LOAD-01).
- NV ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~2.87×) recorded against pre-load NASBO totals; FY2024 re-check result recorded.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 120-02-NV-LOADLOG.md finalized.
</verification>

<success_criteria>
The Nevada state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its confirmed durable window (FY2019–FY2023, or wider if a FY2024 ACFR is found at load time), stored in dollars (UNITS=1), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, the FY2023 NASBO operating replaced in place while the FY2024 NASBO row is honestly retained (no fabricated latest year), the ~2.87× accept-relabel divergence recorded at load, the P2 clamp wired, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands NV to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-02-SUMMARY.md` when done — record the NV FY window actually transcribed, the FY2024 re-check result + FY2024-NASBO-retained decision, any FY omitted as a non-tying honest hole, the two bookend tie confirmations (dollars), the NASBO-replacement confirmation (FY2023 replaced / FY2024 retained), the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~2.87×), the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
