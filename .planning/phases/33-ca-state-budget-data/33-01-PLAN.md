---
phase: 33-ca-state-budget-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedCAState.js
  - docs/California/Historical_Expenditures.xlsx
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "California exists in treasury.municipalities with entity_type='state', population=39500000, state='CA'"
    - "A data_source row named 'California General Fund Operating Budget' exists with api_type='xlsx_download', dataset_id='ca-lao-gf-operating', municipality_id pointing to the CA state row"
    - "docs/California/Historical_Expenditures.xlsx exists locally and is readable by openpyxl"
  artifacts:
    - path: "scripts/seedCAState.js"
      provides: "Idempotent seeder for CA state municipality + data_source"
      exports: ["main"]
    - path: "docs/California/Historical_Expenditures.xlsx"
      provides: "LAO historical expenditures data file (5.5MB)"
  key_links:
    - from: "seedCAState.js"
      to: "treasury.municipalities"
      via: "upsertMunicipality() -> supabase.schema('treasury').from('municipalities')"
      pattern: "upsertMunicipality"
    - from: "seedCAState.js"
      to: "treasury.data_sources"
      via: "upsertDataSourceByName() -> supabase.schema('treasury').from('data_sources')"
      pattern: "upsertDataSourceByName"
---

<objective>
Seed California as a state-level entity and download the LAO Excel file so Wave 2 can begin immediately.

Purpose: DATA-01 (municipality + data_source rows) and the Excel download are independent blocking prerequisites for all subsequent plans. Completing them in Wave 1 maximizes parallel opportunity.

Output: scripts/seedCAState.js (idempotent, exits 0), docs/California/Historical_Expenditures.xlsx (5.5 MB), CA state municipality row in DB with entity_type='state', one data_source row with api_type='xlsx_download'.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-ca-state-budget-data/33-RESEARCH.md
@.planning/phases/33-ca-state-budget-data/33-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/California/ and download LAO Excel</name>
  <files>docs/California/Historical_Expenditures.xlsx</files>
  <read_first>
    - .planning/phases/33-ca-state-budget-data/33-RESEARCH.md (URL, expected file size, sheet names)
  </read_first>
  <action>
    Create the directory docs/California/ if it does not exist. Download the LAO Historical Expenditures Excel file from https://lao.ca.gov/sections/state-budget/econ_fiscal/Historical_Expenditures.xlsx using Node.js fetch or PowerShell Invoke-WebRequest. Save to docs/California/Historical_Expenditures.xlsx. After download, run a quick openpyxl check to confirm the 'Pivot Table Data' sheet is present and readable: `python -c "import openpyxl; wb = openpyxl.load_workbook('docs/California/Historical_Expenditures.xlsx', read_only=True, data_only=True); print(wb.sheetnames)"` — expected output includes both 'Pivot Table' and 'Pivot Table Data'. Do not write any loader code in this task; download only.
  </action>
  <verify>
    <automated>python -c "import openpyxl; wb = openpyxl.load_workbook('C:/treasury-tracker/docs/California/Historical_Expenditures.xlsx', read_only=True, data_only=True); assert 'Pivot Table Data' in wb.sheetnames, 'Sheet missing'; print('OK:', wb.sheetnames)"</automated>
  </verify>
  <acceptance_criteria>
    - docs/California/Historical_Expenditures.xlsx exists and is larger than 1 MB
    - `python -c "import openpyxl; wb = openpyxl.load_workbook('docs/California/Historical_Expenditures.xlsx', read_only=True, data_only=True); print(wb.sheetnames)"` outputs a list that includes 'Pivot Table Data'
    - No error from openpyxl on load
  </acceptance_criteria>
  <done>docs/California/Historical_Expenditures.xlsx is locally present and openpyxl can open the 'Pivot Table Data' sheet without error.</done>
</task>

<task type="auto">
  <name>Task 2: Write and run seedCAState.js</name>
  <files>scripts/seedCAState.js</files>
  <read_first>
    - scripts/seedAnaheimSantaAnaCA.js (exact analog — copy upsertMunicipality, upsertDataSourceByName, loadEnv, Supabase init, main structure verbatim)
    - .planning/phases/33-ca-state-budget-data/33-PATTERNS.md (municipality payload, data_source payload, verification pattern)
    - .planning/phases/33-ca-state-budget-data/33-RESEARCH.md (Pattern 3: seedCAState.js)
  </read_first>
  <action>
    Create scripts/seedCAState.js modeled exactly on scripts/seedAnaheimSantaAnaCA.js. Use ESM (import/export). Copy loadEnv(), Supabase init (SUPABASE_URL defaulting to 'https://kxsdzaojfaibhuzmclfq.supabase.co', SUPABASE_KEY from SUPABASE_SERVICE_KEY or SUPABASE_SERVICE_ROLE_KEY), upsertMunicipality(), and upsertDataSourceByName() verbatim from the analog — only the payload constants change.

    Municipality payload (CALIFORNIA constant):
    - name: 'California'
    - state: 'CA'
    - entity_type: 'state'
    - population: 39500000
    - population_year: 2024
    - county_id: null (do not include county_id field; states don't belong to counties)

    Data source payload (DATA_SOURCE constant, built after caStateId is known):
    - name: 'California General Fund Operating Budget'
    - api_type: 'xlsx_download'
    - dataset_type: 'operating'
    - dataset_id: 'ca-lao-gf-operating'
    - base_url: 'https://lao.ca.gov/sections/state-budget/econ_fiscal/Historical_Expenditures.xlsx'
    - municipality_id: caStateId (from upsertMunicipality result)
    - fiscal_years: [2022, 2023, 2024, 2025, 2026]

    All table reads use .schema('treasury'). RPC calls do NOT use .schema(). Verification step at end: call supabase.rpc('treasury_list_source_ids') and confirm 'California General Fund Operating Budget' appears in the result with api_type='xlsx_download'. Exit 0 on success.

    Run the seeder after writing: `node scripts/seedCAState.js`
  </action>
  <verify>
    <automated>node scripts/seedCAState.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/seedCAState.js` exits 0
    - Script output includes the line `OK: California General Fund Operating Budget (api_type=xlsx_download, type=operating)`
    - Running the script a second time (idempotency check) also exits 0 with "(updated existing municipality row ...)" and "(updated existing row ...)" in output
    - DB query `SELECT name, entity_type, population, state FROM treasury.municipalities WHERE name='California' AND state='CA'` returns 1 row: name=California, entity_type=state, population=39500000, state=CA
  </acceptance_criteria>
  <done>seedCAState.js exits 0, is idempotent, and the CA state municipality + data_source rows are confirmed present in the DB via treasury_list_source_ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → Supabase | Service role key from .env crosses here; if leaked, full DB write access |
| Script → LAO.ca.gov | Downloading a public Excel file; no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | SUPABASE_SERVICE_KEY in .env | mitigate | Key loaded from .env via loadEnv(), never hardcoded or logged. .env is in .gitignore. |
| T-33-02 | Tampering | Downloaded Excel from LAO.ca.gov | accept | File is from the official California LAO domain (lao.ca.gov), publicly accessible, no auth token. Wave 2 adds a $150B-$300B sanity check on extracted totals. |
| T-33-03 | Tampering | entity_type='state' DB write | mitigate | Phase 32 CHECK constraint already validates 'state' is an accepted value. Seeder uses parameterized Supabase client calls, not raw SQL. |
</threat_model>

<verification>
After both tasks complete:
1. `node scripts/seedCAState.js` exits 0 (second run — idempotency)
2. DB check: `SELECT name, entity_type, population FROM treasury.municipalities WHERE state='CA' AND entity_type='state'` returns exactly 1 row with name='California', entity_type='state', population=39500000
3. `python -c "import openpyxl; wb = openpyxl.load_workbook('docs/California/Historical_Expenditures.xlsx', read_only=True, data_only=True); print('Sheets:', wb.sheetnames)"` includes 'Pivot Table Data'
</verification>

<success_criteria>
- California municipality row in DB: entity_type='state', population=39500000, state='CA'
- Data source row in DB: name='California General Fund Operating Budget', api_type='xlsx_download', dataset_id='ca-lao-gf-operating'
- docs/California/Historical_Expenditures.xlsx present and openpyxl-readable
- seedCAState.js is idempotent (run twice, exits 0 both times)
</success_criteria>

<output>
Create .planning/phases/33-ca-state-budget-data/33-01-SUMMARY.md when done
</output>
