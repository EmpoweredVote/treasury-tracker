---
phase: 33-ca-state-budget-data
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - scripts/enrichCategories.js
autonomous: false
requirements:
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "enrichCategories.js buildEntityContext() has a 'state' case that returns state-level policy framing (not city-government language)"
    - "CA state budget categories have enrichment descriptions in the DB using state-level framing"
    - "The California entity is visible in the entity picker under a 'State Governments' section at treasurytracker.empowered.vote"
    - "Clicking California shows Money Out tab with General Fund total approximately $228B for FY2025-26"
    - "Per-capita display shows approximately $5,782 per resident for FY2025-26"
    - "Year selector offers at least FY2022-2026 as selectable years"
  artifacts:
    - path: "scripts/enrichCategories.js"
      provides: "Modified enrichment pipeline with 'state' case in buildEntityContext()"
      contains: "case 'state':"
    - path: ".planning/phases/33-ca-state-budget-data/33-VERIFICATION.md"
      provides: "Phase 33 verification record"
  key_links:
    - from: "enrichCategories.js buildEntityContext()"
      to: "treasury.category_enrichment"
      via: "enrichCategories.js --city 'California' --state CA --year 2026"
      pattern: "case 'state':"
---

<objective>
Modify enrichCategories.js to add state-level framing, run enrichment for CA state, verify the live app, and write the phase VERIFICATION.md.

Purpose: DATA-03 (state enrichment framing) and DATA-04 (live app functional check). The enrichment modification is a one-case addition to a switch statement. This plan completes Phase 33 end-to-end.

Output: Modified enrichCategories.js with 'state' case; category_enrichment rows in DB for California FY2026; 33-VERIFICATION.md confirming all 5 success criteria.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-ca-state-budget-data/33-RESEARCH.md
@.planning/phases/33-ca-state-budget-data/33-PATTERNS.md
@.planning/phases/33-ca-state-budget-data/33-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'state' case to buildEntityContext() in enrichCategories.js, then run enrichment</name>
  <files>scripts/enrichCategories.js</files>
  <read_first>
    - scripts/enrichCategories.js lines 283-315 (the full buildEntityContext() switch block — read this before editing to confirm current state and exact insertion point)
    - .planning/phases/33-ca-state-budget-data/33-PATTERNS.md (enrichCategories.js section: current switch block and required modification)
    - .planning/phases/33-ca-state-budget-data/33-RESEARCH.md (Pattern 4: 'state' case text, cost estimate ~$0.002)
  </read_first>
  <action>
    Edit scripts/enrichCategories.js. Locate the buildEntityContext() function switch statement (currently has cases for 'township', 'county', 'school_district', and 'city'/default). Insert a new case block immediately before the `case 'city':` line (which is `default:` equivalent — the last case before default). The new case text:

      case 'state':
        return `This is a state government budget. The California state budget covers
policy programs funded through the General Fund — primarily K-12 and higher education,
health and human services (Medi-Cal), corrections and rehabilitation, and government
operations. Programs are organized by the Department of Finance's agency groupings.
Amounts are in the hundreds of millions to tens of billions of dollars. Frame
descriptions as state policy programs visible to residents statewide, not as
local city departments.`;

    No other changes to enrichCategories.js. The CLI invocation `--city "California" --state CA` already works because getMunicipality uses .ilike('name', 'California').eq('state', 'CA') which will return the row with entity_type='state' that was seeded in Plan 01.

    After editing, verify with dry-run first:
      `node scripts/enrichCategories.js --city "California" --state CA --year 2026 --dry-run`
    Confirm the dry-run output references state-level framing (not "city government"). Estimated cost: ~$0.002 (12 categories × $0.0002/call) — well under the $5 threshold. Then run live enrichment:
      `node scripts/enrichCategories.js --city "California" --state CA --year 2026`
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city "California" --state CA --year 2026 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/enrichCategories.js contains the text `case 'state':` between the school_district case and the city/default case
    - scripts/enrichCategories.js contains the phrase `state government budget` inside the 'state' case return string
    - `node scripts/enrichCategories.js --city "California" --state CA --year 2026 --dry-run` exits 0 and outputs context text that includes "state government" or "General Fund" (not "city government with a mayor")
    - `node scripts/enrichCategories.js --city "California" --state CA --year 2026` exits 0
    - DB query: `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='California' AND state='CA')` returns a count greater than 0
    - At least one enrichment description in the DB for California does NOT contain the phrase "city council" or "mayor" (confirming state framing was used)
  </acceptance_criteria>
  <done>enrichCategories.js has the 'state' case; dry-run confirms state framing; live enrichment exits 0 and inserts category_enrichment rows in the DB for California.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: App spot-check and VERIFICATION.md</name>
  <what-built>
    California state budget is now fully loaded: municipality seeded (entity_type='state'), 5 fiscal years of General Fund operating data (~$228B FY2025-26), and AI-enriched category descriptions using state-level policy framing. The EntitySwitcher UI fix from Phase 32 displays CA state under "State Governments" above city groups.
  </what-built>
  <how-to-verify>
    Open https://treasurytracker.empowered.vote in a browser and verify all 5 success criteria:

    1. ENTITY PICKER: Entity picker shows a "State Governments" section. "California" appears in that section (above city groups, not nested inside "CALIFORNIA" city group).

    2. MONEY OUT TAB: Click California. The Money Out tab loads and shows a General Fund total in the $220B-$240B range for the latest year (FY2025-26). The number should be approximately $228 billion.

    3. PER-CAPITA: Per-capita display shows approximately $5,700-$5,900 per resident (correct figure is ~$5,782 for FY2025-26 at 39.5M population).

    4. ENRICHMENT: Click on the top spending category ("Health and Human Services" or "K-12 Education"). Enrichment description is visible and uses state-level policy language — should reference "Medi-Cal" or "state programs" or "General Fund", not "city council" or "mayor".

    5. YEAR SELECTOR: Year selector shows at least FY2022-2026 as selectable options. Switching years updates the totals correctly.

    If any criterion fails, note it precisely and type "issues: [description]". If all pass, type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe any failures precisely</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichCategories.js → Anthropic API | ANTHROPIC_API_KEY from .env; prompts contain budget category names (not PII) |
| enrichCategories.js → Supabase | Service role key from .env; writes to category_enrichment table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Tampering | Enrichment prompt injection via category names from DB | accept | Category names are LAO agency/department names (e.g., "Health and Human Services") — static government taxonomy, not user input. No injection surface. |
| T-33-10 | Information Disclosure | ANTHROPIC_API_KEY logged | mitigate | enrichCategories.js loads key from .env; existing pattern never logs key values. Cost is ~$0.002 — no risk of surprise charges. |
| T-33-11 | Repudiation | Enrichment descriptions wrong framing (city language for state entity) | mitigate | 'state' case added to buildEntityContext() before running; dry-run confirms correct framing before live API calls. |
</threat_model>

<verification>
After Task 1 completes and Task 2 (human checkpoint) is approved:
1. `grep -n "case 'state':" scripts/enrichCategories.js` returns at least 1 match
2. DB: `SELECT COUNT(*) FROM treasury.category_enrichment WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='California' AND state='CA')` returns count > 0
3. Live app: all 5 success criteria verified by human (entity picker, total ~$228B, per-capita ~$5,782, state-framed enrichment, year selector FY2022-2026)
4. 33-VERIFICATION.md written with pass/fail for each success criterion
</verification>

<success_criteria>
- buildEntityContext() in enrichCategories.js has a 'state' case returning state-level policy framing
- Category enrichment rows exist in DB for California with state-level descriptions (no "city council" or "mayor" language)
- Live app: California visible in "State Governments" section of entity picker
- Live app: Money Out tab shows ~$228B for FY2025-26
- Live app: Per-capita ~$5,782 per resident
- Live app: Year selector shows FY2022-2026
- 33-VERIFICATION.md confirms all DATA-01 through DATA-04 requirements are met
</success_criteria>

<output>
Create .planning/phases/33-ca-state-budget-data/33-03-SUMMARY.md when done.
After human approval in Task 2, write .planning/phases/33-ca-state-budget-data/33-VERIFICATION.md recording pass/fail for each of the 5 success criteria.
</output>
