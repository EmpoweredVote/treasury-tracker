---
phase: 75-bank-truth-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/<timestamp>_create_org_financial_summary.sql
autonomous: false
requirements: [EVDATA-04, EVDATA-05]
user_setup:
  - service: Supabase (treasury schema, production project kxsdzaojfaibhuzmclfq)
    why: "The summary record is a new table the Phase 76 donor-facing view reads via the ev-accounts API. DDL must land on the live project."
    dashboard_config:
      - task: "Apply the migration via mcp__supabase-local__apply_migration (DDL), then verify with mcp__supabase-local__execute_sql / list_migrations. Do NOT ask Chris to paste into the Dashboard (per feedback_supabase_migration_mcp)."
        location: "MCP supabase-local tools"
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env — present locally; not needed for the MCP migration path, used by the loaders in 75-02/75-03."

must_haves:
  truths:
    - "A new table treasury.org_financial_summary holds one sourced row per (municipality_id, fiscal_year) with cash balance + as-of date, monthly burn + runway, income gross/fees/net (+ per-source breakdown), and reconciliation variance (+ per-source breakdown) and unmatched deposits"
    - "The table is upsertable by (municipality_id, fiscal_year) — re-running a loader overwrites the single FY row, never accumulates (idempotent)"
    - "Every figure carries sourcing columns (source_name, source_url, source_date) consistent with the always-sourced standard used by treasury.federal_context_metrics"
    - "service_role + anon read grants mirror the federal context tables so the ev-accounts API can expose the row"
  artifacts:
    - path: "supabase/migrations/<timestamp>_create_org_financial_summary.sql"
      provides: "DDL for treasury.org_financial_summary + grants"
      contains: "org_financial_summary"
  key_links:
    - from: "org_financial_summary row"
      to: "ev-accounts API → Phase 76 donor view"
      via: "upsert keyed on (municipality_id, fiscal_year); API exposes the EV nonprofit row"
      pattern: "org_financial_summary"
---

<objective>
Create the single data home for Empowered Vote's reconciled financial summary: a new `treasury.org_financial_summary` table, one sourced row per (municipality_id, fiscal_year), holding the scalar cash/runway truth (balance, as-of, monthly burn, runway), the income gross→net story (totals + per-source JSON), and the reconciliation result (variance + per-source JSON + unmatched deposits). This is the record CONTEXT.md D-02 calls for — the one source of truth the Phase 76 donor-facing view reads through the ev-accounts API.

Purpose: 75-02 (reconcile/balance/fees) and 75-03 (manual/interest) both upsert into this table. It must exist first, with the right shape and grants, so the loaders have a stable target and the API can surface it. No data is written here — schema only.
Output: one Supabase migration applied to the live project + verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md
@supabase/migrations/20260612110100_create_federal_context_tables.sql
@supabase/migrations/20260612110200_grant_federal_tables.sql

<interfaces>
<!-- Precedent: treasury.federal_context_metrics is the existing org-level keyed-metrics table the API already exposes.
     This table mirrors its sourcing-column + grant pattern, but is scoped to a municipality + fiscal year and holds
     a richer per-FY summary (scalar fields + jsonb breakdowns) rather than one scalar per key. -->

EV entity: treasury.municipalities row name='Empowered Vote', entity_type='nonprofit'.
Sourcing-column convention (from federal_context_metrics): source_name text, source_url text, source_date date, updated_at timestamptz.
Grants convention: see 20260612110200_grant_federal_tables.sql (GRANT SELECT to anon + authenticated; full to service_role).

Live FY2026 values this row will eventually hold (from 75-02): balance 1706.77 @ 2026-06-17; trailing burn ~ $344–385/mo; runway ~4–5 months; income gross/fees/net per source; recon variance per source.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the org_financial_summary migration</name>
  <files>supabase/migrations/&lt;timestamp&gt;_create_org_financial_summary.sql</files>
  <read_first>
    - supabase/migrations/20260612110100_create_federal_context_tables.sql (sourcing-column shape, COMMENT style, dollars-not-millions convention)
    - supabase/migrations/20260612110200_grant_federal_tables.sql (the exact GRANT statements to mirror)
    - .planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md (D-02 record fields, D-05 recon variance, D-11 per-source gross→net, D-07 unmatched deposits)
  </read_first>
  <action>
    Create `supabase/migrations/&lt;timestamp&gt;_create_org_financial_summary.sql` (use a UTC timestamp newer than the latest existing migration, format `YYYYMMDDHHMMSS`). Define:

    `CREATE TABLE treasury.org_financial_summary (`
      `id uuid PRIMARY KEY DEFAULT gen_random_uuid(),`
      `municipality_id uuid NOT NULL REFERENCES treasury.municipalities(id) ON DELETE CASCADE,`
      `fiscal_year smallint NOT NULL,`
      `balance numeric NOT NULL,`
      `balance_as_of date NOT NULL,`
      `monthly_burn numeric NOT NULL,`
      `burn_window_months smallint NOT NULL DEFAULT 3,`
      `runway_months numeric,`  -- nullable: NULL when burn ≈ 0 (D-03 says this won't occur, but allow it rather than store Infinity)
      `income_gross numeric NOT NULL DEFAULT 0,`
      `income_fees numeric NOT NULL DEFAULT 0,`
      `income_net numeric NOT NULL DEFAULT 0,`
      `income_by_source jsonb NOT NULL DEFAULT '[]'::jsonb,`  -- [{source,gross,fee,net}]
      `recon_variance numeric,`
      `recon_explanation text,`
      `recon_by_source jsonb NOT NULL DEFAULT '[]'::jsonb,`   -- [{source,platform_net,bank_deposits,variance}]
      `unmatched_deposits jsonb NOT NULL DEFAULT '[]'::jsonb,`-- [{date,amount,description}]
      `source_name text NOT NULL,`
      `source_url text,`
      `source_date date NOT NULL,`
      `updated_at timestamptz NOT NULL DEFAULT now(),`
      `UNIQUE (municipality_id, fiscal_year)`
    `);`

    Add a `COMMENT ON TABLE` explaining: v2.6 EV financial summary — one row per (municipality, fiscal year); bank-authoritative balance/expenses, platform-authoritative income; amounts in DOLLARS; upsert by (municipality_id, fiscal_year). Add COMMENTs on `runway_months` (NULL = burn≈0, never display Infinity), `income_by_source` / `recon_by_source` (JSON array shapes above), and `unmatched_deposits` (deposits matching no platform descriptor and not interest — flagged for manual classification, D-07).

    Mirror the GRANT statements from `20260612110200_grant_federal_tables.sql` for this table: `GRANT SELECT ON treasury.org_financial_summary TO anon, authenticated;` and `GRANT ALL ON treasury.org_financial_summary TO service_role;` (match whatever roles that file grants — copy its exact pattern).
  </action>
  <verify>
    <automated>test -f supabase/migrations/*_create_org_financial_summary.sql &amp;&amp; grep -q "UNIQUE (municipality_id, fiscal_year)" supabase/migrations/*_create_org_financial_summary.sql</automated>
  </verify>
  <acceptance_criteria>
    - A migration file matching `supabase/migrations/*_create_org_financial_summary.sql` exists with a timestamp newer than every other migration in that directory.
    - It creates `treasury.org_financial_summary` with all columns above, the FK to `treasury.municipalities(id)`, and `UNIQUE (municipality_id, fiscal_year)`.
    - It contains `income_by_source`, `recon_by_source`, and `unmatched_deposits` as `jsonb`, and `source_name`/`source_date` as NOT NULL.
    - It contains GRANT statements mirroring `20260612110200_grant_federal_tables.sql`.
  </acceptance_criteria>
  <done>Migration authored with the full summary shape, FK, unique key, sourcing columns, and grants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply the migration to the live project + verify</name>
  <files>supabase/migrations/&lt;timestamp&gt;_create_org_financial_summary.sql</files>
  <read_first>
    - the migration authored in Task 1
    - C:/Users/Chris/.claude/projects/C--treasury-tracker/memory/feedback_supabase_migration_mcp.md (use mcp__supabase-local for DDL; skip the Dashboard checkpoint)
  </read_first>
  <action>
    Apply the DDL with `mcp__supabase-local__apply_migration` (name e.g. `create_org_financial_summary`, body = the SQL from Task 1). Then verify with `mcp__supabase-local__execute_sql`:
    `SELECT column_name, data_type FROM information_schema.columns WHERE table_schema='treasury' AND table_name='org_financial_summary' ORDER BY ordinal_position;`
    Confirm the column set + types match Task 1, and confirm the unique constraint exists:
    `SELECT conname FROM pg_constraint WHERE conrelid='treasury.org_financial_summary'::regclass;`
    Do not write any rows (75-02 does that).
  </action>
  <verify>
    <human-check>execute_sql shows treasury.org_financial_summary with the expected columns and a UNIQUE(municipality_id, fiscal_year) constraint; no errors on apply.</human-check>
  </verify>
  <acceptance_criteria>
    - `mcp__supabase-local__apply_migration` returns success (no error).
    - The information_schema query lists every column from Task 1 with the right types.
    - A unique constraint over (municipality_id, fiscal_year) is present.
  </acceptance_criteria>
  <done>treasury.org_financial_summary exists on the live project, verified by query; ready for 75-02 to upsert.</done>
  <resume-signal>Type "applied" once the table exists and the column check passes, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migration → Supabase (DDL) | Schema change to the production treasury schema via MCP, single-operator. |
| org_financial_summary → ev-accounts API (read) | New table exposed read-only to anon/authenticated for the public donor view; holds only aggregates (no donor PII). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01-01 | Information Disclosure | new public-readable table | mitigate | Table stores only org-level aggregates (balance, burn, per-source totals) — no donor names/emails/per-donation rows. anon/authenticated get SELECT only. |
| T-75-01-02 | Tampering / data integrity | accidental row accumulation | mitigate | UNIQUE(municipality_id, fiscal_year) forces upsert semantics; loaders cannot create duplicate FY rows. |
| T-75-01-SC | Tampering | npm installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- Migration file present, newer timestamp, correct shape (FK + unique key + jsonb + sourcing columns + grants).
- Applied to the live project via MCP; information_schema confirms columns and the unique constraint.
- No rows written (data is 75-02's job).
</verification>

<success_criteria>
- treasury.org_financial_summary exists with the D-02/D-05/D-07/D-11 fields, keyed for idempotent per-FY upsert, sourced, and API-readable.
</success_criteria>

<output>
Create `.planning/phases/75-bank-truth-reconciliation/75-01-SUMMARY.md` when done — record the final column list, the unique-key/upsert decision, the grant set, and the applied migration name/timestamp.
</output>
