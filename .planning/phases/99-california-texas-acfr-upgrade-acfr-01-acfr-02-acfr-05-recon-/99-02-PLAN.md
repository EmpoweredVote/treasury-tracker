---
phase: 99-california-texas-acfr-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - scripts/processCA.js
  - scripts/processCARevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: false
requirements: [ACFR-01, ACFR-05, RECON-03]

must_haves:
  truths:
    - "User sees California GF revenue-by-source on the CA state node (ACFR-sourced, GAAP basis-labelled)"
    - "User sees California GAAP spending-by-function on the CA state node, replacing the prior NASBO operating rows"
    - "Each loaded CA FY ties to its ACFR GF column printed total"
    - "Re-running the CA loaders writes 0 net-new rows (idempotent in-place update)"
    - "The 2 stale CA data_sources (ca-lao-gf-operating, ca-dof-gf-revenue) are deleted; fresh ca-acfr-* exist"
    - "Negative CA categories render clamped to 0 with signed magnitude in the label"
  artifacts:
    - path: "scripts/processCA.js"
      provides: "Live CA operating load — treasury.budgets (muni e1007bf5, dataset_type operating, FY2020-2025)"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processCARevenueAcfr.js"
      provides: "Live CA revenue insert — treasury.budgets (dataset_type revenue, FY2020-2025)"
      contains: "dataset_type"
  key_links:
    - from: "scripts/processCA.js"
      to: "CA state node operating rows (municipality_id e1007bf5, fy, 'operating')"
      via: "treasury_sync_budget_tree updates in place → NASBO replaced by ACFR GAAP"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processCARevenueAcfr.js"
      to: "CA state node revenue rows (new dataset_type='revenue')"
      via: "pure insert (no prior NASBO revenue)"
      pattern: "p_dataset_type.*revenue|dataset_type.*revenue"
---

<objective>
Run the CA loaders from 99-01 against the LIVE production treasury table: load CA GF revenue-by-source + GAAP spending-by-function for FY2020-2025, replacing the existing NASBO operating rows on the CA state node idempotently (RECON-03), delete the 2 stale CA `data_sources`, and verify each FY ties to its ACFR printed total + a re-run is a no-op.

Purpose: Deliver ACFR-01 (CA upgraded to full ACFR GAAP node) on production, proving the per-state upgrade path Phase 100 reuses.
Output: Live CA operating (replaced) + revenue (new) rows on node e1007bf5; stale CA data_sources removed; fresh ca-acfr-* data_sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-CONTEXT.md
@.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-01-SUMMARY.md

<interfaces>
<!-- Data model the live write touches. -->
- CA state node: treasury.municipalities id = e1007bf5-bac9-4b1c-878e-f6834885f850 (entity_type='state').
- budgets keyed (municipality_id, fiscal_year, dataset_type); columns total_budget, hierarchy (text[]), data_source, source_url, source_date. Probe with total_budget (NOT total) and hierarchy (NOT tree); there is no row_count column.
- Operating replace (D-11): treasury_sync_budget_tree with p_dataset_type='operating' on the SAME (muni,fy,'operating') key NASBO used → RPC UPDATES IN PLACE → NASBO row replaced by ACFR GAAP row. Then targeted post-RPC UPDATE of {source_url, source_date, data_source}. NEVER treasury_sync_city_budget (not source-safe).
- Revenue insert (D-12): dataset_type='revenue' is new on this node → pure insert.
- Pre-existing CA NASBO operating rows: FY2023 $195.189B, FY2024 $205.671B (both replaced by ACFR FY2023/FY2024). Window FY2020-2025 fully covers them → no orphan FY (D-13).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve first live CA production write</name>
  <action>PAUSE for human approval before the first production write to treasury.budgets. Present the dry-run results below; do not run the live CA load until the operator types "approved".</action>
  <what-built>Two dry-run-clean CA loaders (99-01) ready to write to the LIVE production treasury.budgets table on the CA state node (e1007bf5).</what-built>
  <how-to-verify>
    This is the first production DB write of the phase. Before approving:
    1. Run `node scripts/processCA.js --dry-run` and `node scripts/processCARevenueAcfr.js --dry-run`; confirm every FY2020-2025 prints "validation: PASS" and the bookends tie (FY2020 rev $155,923,876K, FY2025 $221,591,201K).
    2. Confirm this writes ONLY to muni e1007bf5 (California state node) — no other state touched.
    3. Confirm the operating load will REPLACE NASBO FY2023 ($195.189B) + FY2024 ($205.671B) with ACFR GAAP (expected — one basis per state-FY).
    4. Acknowledge the CA node total/basis will change to ACFR GAAP and Money In becomes a real revenue view.
  </how-to-verify>
  <resume-signal>Type "approved" to run the live CA load, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Live-load CA operating + revenue (FY2020-2025) and replace NASBO operating</name>
  <files>scripts/processCA.js, scripts/processCARevenueAcfr.js</files>
  <read_first>
    - scripts/processCA.js, scripts/processCARevenueAcfr.js (built + dry-run-validated in 99-01)
    - .planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-01-SUMMARY.md (per-FY extracted totals to reconcile against)
  </read_first>
  <action>
    Source the env (SUPABASE_URL + SUPABASE_SERVICE_KEY) then run the live loads (no --dry-run): `node scripts/processCA.js` (operating, all FY2020-2025) and `node scripts/processCARevenueAcfr.js` (revenue, all FY2020-2025). Each writes via treasury_sync_budget_tree keyed (e1007bf5, fy, dataset_type) then the targeted post-RPC UPDATE stamps {source_url=per-FY ACFR URL, source_date=FY-06-30, data_source=GAAP-labelled string}.
    The operating writes hit the SAME (muni,fy,'operating') key as the NASBO rows → the RPC updates in place, so NASBO FY2023/FY2024 are REPLACED by ACFR GAAP (D-11, RECON-03); FY2020-2022+FY2025 are new operating rows. Revenue rows are pure inserts (dataset_type='revenue' new on this node, D-12). The per-FY tie-check gate (built in 99-01) still runs and will exit 2 before writing any FY that does not tie to its printed total. If the SCO download is flaky, drive per-`--fy` in a retry loop.
    Do NOT use treasury_sync_city_budget. Do NOT touch any non-CA node.
  </action>
  <verify>
    <automated>node scripts/processCA.js --fy 2025 --dry-run && node scripts/processCARevenueAcfr.js --fy 2025 --dry-run</automated>
  </verify>
  <done>treasury.budgets has CA (e1007bf5) operating rows for FY2020-2025 carrying ACFR GAAP data_source strings (NASBO FY2023/FY2024 strings gone), and revenue rows for FY2020-2025; FY2020 revenue total_budget = $155,923,876,000 and FY2025 = $221,591,201,000; each row's source_url points at the per-year SCO ACFR PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale CA data_sources, create fresh ca-acfr-*, verify idempotency + clamp</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs, scripts/processCA.js, scripts/processCARevenueAcfr.js</files>
  <read_first>
    - scripts/cleanupStaleStateGFDataSources.mjs (built + dry-run-validated in 99-01)
  </read_first>
  <action>
    Run the live cleanup for CA: `node scripts/cleanupStaleStateGFDataSources.mjs --state CA` — deletes ca-lao-gf-operating + ca-dof-gf-revenue (D-04) after re-asserting each backs 0 budgets rows (the script exits 2 rather than delete a row-backed data_source). The fresh `ca-acfr-gf-operating` + `ca-acfr-gf-revenue` data_sources are created by the loaders' find-or-create in Task 1 — confirm they exist and that the deleted stale ones are gone.
    Verify IDEMPOTENCY (D-14, SC#2): re-run `node scripts/processCA.js` and `node scripts/processCARevenueAcfr.js` once more and confirm 0 net-new budgets rows (RPC updates the same (muni,fy,dataset) rows in place); re-run the cleanup and confirm 0 deleted. Verify the P2 clamp (D-15, ACFR-05): if any CA FY has a negative GF category (investment income), confirm it renders area 0 with the signed magnitude in the hierarchy label and the FY's total_budget preserves the net (un-clamped) control total. Confirm no NASBO/other-state node changed (un-upgraded states untouched, D-14).
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --state CA --dry-run</automated>
  </verify>
  <done>treasury.data_sources for CA: ca-lao-gf-operating + ca-dof-gf-revenue gone; ca-acfr-gf-operating + ca-acfr-gf-revenue present. Second loader run = 0 net-new rows; second cleanup run = 0 deleted. Any negative CA category renders clamped-to-0 with signed label; FY total preserved. No other state node altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets (CA node e1007bf5) | production finance table mutation crosses here |
| cleanup → treasury.data_sources | production metadata deletion crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Tampering | RPC writing wrong-keyed/over-broad rows (e.g. via treasury_sync_city_budget) | mitigate | use only treasury_sync_budget_tree keyed (e1007bf5, fy, dataset_type); explicit muni-id scope; never the city RPC |
| T-99-02-02 | Information disclosure | a FY loaded that does not tie to the ACFR printed total | mitigate | per-FY tie-check gate (≤$10M) exits 2 before write; bookends asserted |
| T-99-02-03 | Tampering | deleting a data_source that still backs live rows | mitigate | cleanup pre-asserts 0 budgets rows; exits 2 otherwise; only ca-lao/ca-dof targeted |
| T-99-02-04 | Repudiation | unsourced/ mislabelled basis shown as real | mitigate | post-RPC stamp of data_source (GAAP) + source_url + source_date per FY |
| T-99-02-05 | Denial of service | flaky SCO download aborts a multi-FY run mid-way | accept | per-FY retry loop; idempotent re-run is safe (0 net-new) |
| T-99-02-SC | Tampering | npm installs | accept | no new packages; uses vendored @supabase/supabase-js + system pdftotext |
</threat_model>

<verification>
- CA operating rows FY2020-2025 carry ACFR GAAP data_source; NASBO FY2023/FY2024 replaced (RECON-03).
- CA revenue rows FY2020-2025 inserted; Money In now has data (ACFR-01).
- Bookends tie: FY2020 $155,923,876,000; FY2025 $221,591,201,000.
- Re-run = 0 net-new rows (idempotent, SC#2/D-14).
- Stale ca-lao-gf-operating + ca-dof-gf-revenue deleted; ca-acfr-* present (D-04).
- Negative category (if any) clamped (ACFR-05/SC#3).
- No other state node touched.
</verification>

<success_criteria>
CA state node (e1007bf5) shows ACFR-sourced GF revenue-by-source + GAAP spending-by-function, GAAP basis-labelled, each FY tying to the ACFR printed total, NASBO operating replaced in place, idempotent re-run a no-op, stale CA data_sources cleaned up, negatives clamped, un-upgraded states untouched. ACFR-01 + RECON-03 (CA) + ACFR-05 (CA) satisfied.
</success_criteria>

<output>
Create `.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-02-SUMMARY.md` when done — record CA per-FY loaded totals + tie results, the NASBO→ACFR replace confirmation, idempotency proof, the data_sources before/after, and any clamped negative category.
</output>
