---
phase: 99-california-texas-acfr-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - scripts/processTX.js
  - scripts/processTXRevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: false
requirements: [ACFR-02, ACFR-05, RECON-03]

must_haves:
  truths:
    - "User sees Texas GF revenue-by-source on the TX state node (ACFR-sourced, GAAP basis-labelled, honestly named General Revenue Fund)"
    - "User sees Texas GAAP spending-by-function on the TX state node, replacing the prior NASBO operating rows"
    - "Each loaded TX FY ties to its ACFR General Revenue Fund column printed total"
    - "Re-running the TX loaders writes 0 net-new rows (idempotent)"
    - "FY2016 is either loaded (alt file-id) or dropped with a gap-log note"
    - "The 2 stale TX data_sources (tx-gf-operating, tx-gf-revenue) are deleted; fresh tx-acfr-* exist"
    - "Negative TX categories render clamped to 0 with signed magnitude in the label"
  artifacts:
    - path: "scripts/processTX.js"
      provides: "Live TX operating load — treasury.budgets (TX state node, dataset_type operating, FY2015-2024 -FY2016)"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processTXRevenueAcfr.js"
      provides: "Live TX revenue insert — treasury.budgets (dataset_type revenue, FY2015-2024 -FY2016)"
      contains: "dataset_type"
  key_links:
    - from: "scripts/processTX.js"
      to: "TX state node operating rows (municipality_id, fy, 'operating')"
      via: "treasury_sync_budget_tree updates in place → NASBO replaced by ACFR GAAP (GR Fund)"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processTXRevenueAcfr.js"
      to: "TX state node revenue rows (new dataset_type='revenue')"
      via: "pure insert (no prior NASBO revenue)"
      pattern: "p_dataset_type.*revenue|dataset_type.*revenue"
---

<objective>
Run the TX loaders from 99-01 against the LIVE production treasury table: load TX GF revenue-by-source + GAAP spending-by-function for FY2015-2024 (-FY2016) from the "General Revenue Fund" column, replacing the existing NASBO operating rows on the TX state node idempotently (RECON-03), honestly relabelling the basis (the ~3x GR-Fund scope, D-06), handling the FY2016 gap, deleting the 2 stale TX `data_sources`, and verifying each FY ties + a re-run is a no-op.

Purpose: Deliver ACFR-02 (TX upgraded to full ACFR GAAP node) on production. Independent of 99-02 (different state node, different scripts) — both Wave 2 plans may run in parallel.
Output: Live TX operating (replaced) + revenue (new) rows; stale TX data_sources removed; fresh tx-acfr-* data_sources; FY2016 disposition recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-CONTEXT.md
@.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-01-SUMMARY.md

<interfaces>
<!-- Data model the live write touches. -->
- TX state node: treasury.municipalities WHERE name='Texas' AND state='TX' AND entity_type='state'. TX FYE = Aug 31 → source_date '{FY}-08-31'.
- budgets keyed (municipality_id, fiscal_year, dataset_type); total_budget, hierarchy (text[]), data_source, source_url, source_date. Probe with total_budget + hierarchy; no row_count column.
- Operating replace (D-11): treasury_sync_budget_tree p_dataset_type='operating' on the SAME (muni,fy,'operating') key NASBO used → RPC updates in place → NASBO replaced by ACFR GAAP. Targeted post-RPC UPDATE for {source_url, source_date, data_source}. NEVER treasury_sync_city_budget.
- Revenue insert (D-12): dataset_type='revenue' new on this node → pure insert.
- Pre-existing TX NASBO operating: FY2023 + FY2024 (~$50.5B). The ACFR General Revenue Fund is ~3x (FY2024 $161.4B) — a broader consolidated operating fund. Accept + relabel honestly (D-06); the node total visibly jumps; that is correct + sourced. Window FY2015-2024 fully covers the NASBO FY2023/FY2024 → no orphan FY (D-13).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve live TX production write (GR-Fund basis jump)</name>
  <action>PAUSE for human approval before the production write to the TX state node. Present the dry-run results + the deliberate ~3x GR-Fund basis jump below; do not run the live TX load until the operator types "approved".</action>
  <what-built>Two dry-run-clean TX loaders (99-01) ready to write to the LIVE production treasury.budgets on the TX state node — including the deliberate ~3x General-Revenue-Fund basis change.</what-built>
  <how-to-verify>
    Production DB write. Before approving:
    1. Run `node scripts/processTX.js --dry-run` and `node scripts/processTXRevenueAcfr.js --dry-run`; confirm each available FY prints "validation: PASS" and the bookends tie (FY2015 rev $95,574,830K, FY2024 $161,416,562K).
    2. ACKNOWLEDGE the deliberate basis change (D-06): the TX node total will jump from ~$50.5B (NASBO GF) to ~$161B (ACFR General Revenue Fund) because the GR Fund is a broader consolidated operating fund. This is correct, sourced, and honestly relabelled — NOT an error.
    3. Confirm the FY2016 disposition from 99-01 (loaded via alt file-id, or dropped with a gap-log note).
    4. Confirm this writes ONLY to the TX state node — no other state touched.
  </how-to-verify>
  <resume-signal>Type "approved" to run the live TX load (acknowledging the GR-Fund basis jump), or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Live-load TX operating + revenue (FY2015-2024 -FY2016) and replace NASBO operating</name>
  <files>scripts/processTX.js, scripts/processTXRevenueAcfr.js</files>
  <read_first>
    - scripts/processTX.js, scripts/processTXRevenueAcfr.js (built + dry-run-validated in 99-01)
    - .planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-01-SUMMARY.md (per-FY extracted totals + FY2016 disposition)
  </read_first>
  <action>
    Source the env then run the live loads (no --dry-run): `node scripts/processTX.js` (operating) and `node scripts/processTXRevenueAcfr.js` (revenue) for the available FY2015-2024 window (FY2016 included only if 99-01 located its alt file-id; otherwise skipped per the gap log). Each writes via treasury_sync_budget_tree keyed (TX-muni-id, fy, dataset_type) then the post-RPC UPDATE stamps {source_url=per-FY ACFR URL, source_date=FY-08-31, data_source=`Texas State ACFR — General Revenue Fund...` GAAP string}.
    Operating writes hit the SAME (muni,fy,'operating') key as NASBO → RPC updates in place → NASBO FY2023/FY2024 REPLACED by ACFR GAAP (D-11, RECON-03). The replacement intentionally raises the node total ~3x (GR-Fund scope, D-06) — the data_source string names "General Revenue Fund" so the basis is honest. Revenue rows are pure inserts (D-12). The per-FY tie-check gate exits 2 before writing any FY that does not tie to its printed "General Revenue Fund" Total.
    Do NOT use treasury_sync_city_budget. Do NOT touch any non-TX node.
  </action>
  <verify>
    <automated>node scripts/processTX.js --fy 2024 --dry-run && node scripts/processTXRevenueAcfr.js --fy 2024 --dry-run</automated>
  </verify>
  <done>treasury.budgets has TX operating rows for the FY2015-2024 (-FY2016) window carrying ACFR GR-Fund GAAP data_source strings (NASBO FY2023/FY2024 strings gone), and revenue rows for the same window; FY2015 revenue total_budget = $95,574,830,000 and FY2024 = $161,416,562,000; each row's source_url points at the per-year `96-471.pdf` (or FY2016 alt); FY2016 loaded or gap-logged.</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale TX data_sources, create fresh tx-acfr-*, verify idempotency + clamp</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs, scripts/processTX.js, scripts/processTXRevenueAcfr.js</files>
  <read_first>
    - scripts/cleanupStaleStateGFDataSources.mjs (built + dry-run-validated in 99-01)
  </read_first>
  <action>
    Run the live cleanup for TX: `node scripts/cleanupStaleStateGFDataSources.mjs --state TX` — deletes tx-gf-operating + tx-gf-revenue (D-07) after re-asserting each backs 0 budgets rows (exit 2 rather than delete a row-backed data_source). Confirm the fresh `tx-acfr-gf-operating` + `tx-acfr-gf-revenue` data_sources created by the loaders' find-or-create in Task 1 exist and the stale ones are gone.
    Verify IDEMPOTENCY (D-14, SC#2): re-run `node scripts/processTX.js` and `node scripts/processTXRevenueAcfr.js` once more → 0 net-new budgets rows; re-run cleanup → 0 deleted. Verify the P2 clamp (D-15, ACFR-05): if any TX FY has a negative GF category, confirm it renders area 0 with the signed magnitude in the label and the FY total_budget preserves the net control total. Confirm no other state node changed (D-14).
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --state TX --dry-run</automated>
  </verify>
  <done>treasury.data_sources for TX: tx-gf-operating + tx-gf-revenue gone; tx-acfr-gf-operating + tx-acfr-gf-revenue present. Second loader run = 0 net-new rows; second cleanup run = 0 deleted. Any negative TX category clamped-to-0 with signed label, FY total preserved. No other state node altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets (TX node) | production finance table mutation crosses here |
| cleanup → treasury.data_sources | production metadata deletion crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-03-01 | Tampering | RPC writing wrong-keyed/over-broad rows | mitigate | only treasury_sync_budget_tree keyed (TX-muni, fy, dataset_type); explicit TX node scope; never the city RPC |
| T-99-03-02 | Information disclosure | a FY loaded that does not tie to the GR-Fund printed total | mitigate | per-FY tie-check gate (≤$10M) exits 2 before write; bookends asserted |
| T-99-03-03 | Information disclosure | GR-Fund (~3x) shown without disclosing it is broader than NASBO GF | mitigate | data_source string names "General Revenue Fund" + GAAP basis; honest relabel (D-06) |
| T-99-03-04 | Tampering | deleting a data_source that still backs live rows | mitigate | cleanup pre-asserts 0 budgets rows; exits 2 otherwise; only tx-gf-operating/tx-gf-revenue targeted |
| T-99-03-05 | Denial of service | flaky download / FY2016 404 aborts the multi-FY run | accept | FY2016 gap-logged, per-FY retry; idempotent re-run safe (0 net-new) |
| T-99-03-SC | Tampering | npm installs | accept | no new packages; uses vendored @supabase/supabase-js + system pdftotext |
</threat_model>

<verification>
- TX operating rows FY2015-2024 (-FY2016) carry ACFR GR-Fund GAAP data_source; NASBO FY2023/FY2024 replaced (RECON-03).
- TX revenue rows inserted; Money In now has data (ACFR-02).
- Bookends tie: FY2015 $95,574,830,000; FY2024 $161,416,562,000.
- Node total jump to ~$161B is present, sourced, and basis-labelled "General Revenue Fund".
- FY2016 loaded or gap-logged.
- Re-run = 0 net-new rows (idempotent, SC#2/D-14).
- Stale tx-gf-operating + tx-gf-revenue deleted; tx-acfr-* present (D-07).
- Negative category (if any) clamped (ACFR-05/SC#3). No other state node touched.
</verification>

<success_criteria>
TX state node shows ACFR-sourced GF revenue-by-source + GAAP spending-by-function (honestly labelled General Revenue Fund, GAAP basis), each FY tying to the ACFR printed total, NASBO operating replaced in place, idempotent re-run a no-op, FY2016 handled, stale TX data_sources cleaned up, negatives clamped, un-upgraded states untouched. ACFR-02 + RECON-03 (TX) + ACFR-05 (TX) satisfied.
</success_criteria>

<output>
Create `.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-03-SUMMARY.md` when done — record TX per-FY loaded totals + tie results, the FY2016 disposition, the NASBO→ACFR GR-Fund replace + basis-jump confirmation, idempotency proof, data_sources before/after, and any clamped negative category.
</output>
