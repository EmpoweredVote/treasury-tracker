---
phase: 34-3-level-tree-infrastructure-ev-accounts-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/test/treasury-3level.test.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TREE-01, TREE-02, TREE-03]
must_haves:
  truths:
    - "A 3-level tree (c→c→i) submitted to treasury_sync_budget_tree lands as budget_categories rows at depths 0, 1, and 2"
    - "getBudgetById returns a 3-level BudgetCategory[] (subcategories→subcategories→lineItems) for the test budget"
    - "At least 3 existing 2-level city budgets (Portland, San Jose, Dallas) still return a 2-level tree with no depth-2 subcategories"
    - "The full ev-accounts-api vitest suite passes (npm test green)"
    - "The test cleans up after itself — no test budget rows remain in treasury.budgets after the run"
    - "REQUIREMENTS.md marks TREE-01, TREE-02, TREE-03 as satisfied by existing infrastructure + verification test"
  artifacts:
    - path: "C:/EV-Accounts/backend/test/treasury-3level.test.ts"
      provides: "Integration + regression test covering TREE-01/02/03"
      contains: "treasury_sync_budget_tree"
      min_lines: 80
  key_links:
    - from: "C:/EV-Accounts/backend/test/treasury-3level.test.ts"
      to: "treasury_sync_budget_tree RPC"
      via: "supabase.rpc('treasury_sync_budget_tree', ...)"
      pattern: "rpc\\(['\"]treasury_sync_budget_tree['\"]"
    - from: "C:/EV-Accounts/backend/test/treasury-3level.test.ts"
      to: "getBudgetById in treasuryService.ts"
      via: "import + direct call"
      pattern: "getBudgetById"
---

<objective>
Phase 34 verifies that the existing `treasury_sync_budget_tree` RPC and the `/api/treasury/budgets/:id/categories` endpoint (via `getBudgetById`) already support 3-level trees end-to-end, while confirming zero regressions for the 30+ existing 2-level city/county budgets.

Live inspection during research (34-RESEARCH.md) established that **no ev-accounts-api code changes and no treasury-tracker source changes are required** — the RPC already accepts the `c → c → i` shape and creates depth-0/1/2 `budget_categories` rows, and `getBudgetById` builds an N-level tree from `parent_id`. Bloomington IN already runs at depth 4 in production. This phase proves that infrastructure is correct with an automated test and a human spot-check, then records TREE-01/02/03 as satisfied.

Purpose: Lock in the 3-level infrastructure with a regression-proof test before Phase 35 reloads CA state as a genuine 3-level tree against it.
Output: One new vitest integration/regression test file (`treasury-3level.test.ts`), a green full suite, a human-verified spot-check of 3 city pages, and updated REQUIREMENTS.md traceability.

Scope guardrails (from 34-RESEARCH.md Common Pitfalls — do NOT violate):
- Do NOT modify `C:/EV-Accounts/backend/src/lib/treasuryService.ts` or `C:/EV-Accounts/backend/src/routes/treasury.ts`. They already handle N-level depth.
- Do NOT search for, create, or edit a `.sql` file for `treasury_sync_budget_tree`. It is a live Postgres function with no source file in either repo. Verify behavior via live RPC calls only.
- Do NOT add a `department` (or `category`/`subcategory`) column to `budget_line_items`. The ARCHITECTURE.md schema description is superseded — hierarchy lives in `budget_categories.parent_id`.
- Do NOT modify `C:/treasury-tracker/scripts/processCA.js` in this phase. Emitting a genuine 3-level CA tree is Phase 35 scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-RESEARCH.md
@.planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-PATTERNS.md
@.planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Day-1 inspections + scaffold treasury-3level.test.ts with TREE-01 (3-level RPC submit) and TREE-02 (3-level API response)</name>
  <files>C:/EV-Accounts/backend/test/treasury-3level.test.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/test/arcgis-sources-coverage.test.ts (primary analog: pg.Pool beforeAll/afterAll lifecycle, describe/it structure, DELETE cleanup in afterAll)
    - C:/treasury-tracker/scripts/processCA.js (RPC call signature for treasury_sync_budget_tree — lines ~197-210; loadEnv() pattern lines ~45-55; supabase client init lines ~59-62)
    - C:/EV-Accounts/backend/src/lib/treasuryService.ts (getBudgetById export name + NestedCategory interface lines ~479-508 and buildTree lines ~644-664 — READ ONLY, do not modify)
    - C:/EV-Accounts/backend/vitest.config.ts (test discovery glob test/**/*.{test,spec}.{ts,js}; testTimeout 30000; pool 'forks')
    - .planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-PATTERNS.md (full pattern map for this test file)
  </read_first>
  <action>
    First perform the two Day-1 mandatory inspections from the phase goal and record results inline as code comments at the top of the test file:
    (a) Run a live query `SELECT id, parent_id, name, depth FROM treasury.budget_categories ORDER BY depth DESC LIMIT 5` to confirm depth-2+ rows exist (research recorded depth 0/1/2/3/4 distributions). There is NO `department`/`category`/`subcategory` column on `budget_line_items` — confirm the columns are `id, category_id, description, approved_amount, actual_amount, ... fund, expense_category, external_id, source` before asserting against them.
    (b) Confirm `treasury_sync_budget_tree` exists as a live RPC by the successful submit below — do NOT search for a .sql definition (none exists; pitfall 5).

    Create `C:/EV-Accounts/backend/test/treasury-3level.test.ts`. Import `{ describe, it, expect, beforeAll, afterAll }` from `vitest`, `pg` from `pg`, `{ createClient }` from `@supabase/supabase-js`, and `{ getBudgetById }` from the service module (resolve the import path against `treasuryService.ts`; the `@backend` alias resolves to `./src` per vitest.config.ts). Add a `loadEnv()` helper mirroring processCA.js that reads `../../.env.local` and `../../.env` relative to `__dirname` (test dir is `C:/EV-Accounts/backend/test/`, so `../../.env*` reaches `C:/EV-Accounts/.env*`); only set vars not already in `process.env`.

    Use a single top-level `describe('treasury 3-level tree infrastructure (TREE-01/02/03)', ...)`. In `beforeAll`, instantiate `pool = new pg.Pool({ connectionString: process.env.DATABASE_URL })` and `supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_KEY)` using `process.env.SUPABASE_URL` (fallback `https://kxsdzaojfaibhuzmclfq.supabase.co`) and `process.env.SUPABASE_SERVICE_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY`. Declare `let testBudgetId: string | null = null` at describe scope.

    Pick an existing `data_source_id` for the submit. Query one valid id at runtime: `SELECT id FROM treasury.data_sources WHERE dataset_type = 'operating' LIMIT 1` (do not hardcode a UUID). Store as `dataSourceId`.

    TREE-01 test ('3-level tree c→c→i submits and creates depth 0/1/2 budget_categories'): Build the exact `threeLevel` array from 34-PATTERNS.md (Health and Human Services → Dept of Health Care Services → Medi-Cal, with one leaf line item `{ d: "Medi-Cal Managed Care", a: 40000000000, aa: null, f: null, e: null }`). Use a sentinel fiscal year unlikely to collide (e.g. `9999`) so the test budget is identifiable and isolated. Call `supabase.rpc('treasury_sync_budget_tree', { p_data_source_id: dataSourceId, p_fiscal_year: 9999, p_dataset_type: 'operating', p_total: 87139490000, p_tree: threeLevel, p_row_count: 1, p_triggered_by: 'bulk_load' })`. Assert no `error` and no `data.error`, assert `data.status === 'success'`. Set `testBudgetId = data.budget_id`. Then query `SELECT depth, count(*) FROM treasury.budget_categories WHERE budget_id = $1 GROUP BY depth ORDER BY depth` and assert exactly depths 0, 1, 2 are present (one row each).

    TREE-02 test ('getBudgetById returns a 3-level BudgetCategory[] for depth-2 data'): Call `await getBudgetById(testBudgetId)`. Assert not null. Walk `result.categories[0]` → `.subcategories[0]` → `.subcategories[0]` and assert the deepest node has `lineItems` defined with length >= 1 and its `subcategories` is undefined. Assert the names chain matches "Health and Human Services" / "Dept of Health Care Services" / "Medi-Cal".

    In `afterAll`: if `testBudgetId` is set, `await pool.query('DELETE FROM treasury.budgets WHERE id = $1', [testBudgetId])` (FK cascade removes categories + line items — this satisfies threat T-34-01), then `await pool.end()`. Include `expect(...)` failure messages on every assertion for diagnosis.

    Do not edit any `src/` file. If `getBudgetById` cannot be imported cleanly into the test, fall back to verifying TREE-02 via the recursive `parent_id` shape using direct SQL (build the tree in-test) — but attempt the direct import first since vitest resolves TS via its own loader.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npm test -- treasury-3level</automated>
  </verify>
  <acceptance_criteria>
    - File `C:/EV-Accounts/backend/test/treasury-3level.test.ts` exists and contains the literal string `treasury_sync_budget_tree`.
    - Running `cd C:/EV-Accounts/backend && npm test -- treasury-3level` reports the TREE-01 and TREE-02 `it()` tests passing.
    - TREE-01 assertion proves depths 0, 1, and 2 each exist for the inserted test budget (one category row per depth).
    - TREE-02 assertion proves the API tree is `subcategories → subcategories → lineItems` (3 levels), with the leaf node carrying `lineItems` and no `subcategories`.
    - No source file under `C:/EV-Accounts/backend/src/` is modified (git status in that repo shows only the new test file changed).
    - After the run, `SELECT count(*) FROM treasury.budgets WHERE id = '<testBudgetId>'` returns 0 (cleanup confirmed by afterAll).
  </acceptance_criteria>
  <done>TREE-01 and TREE-02 are proven by passing automated tests; the test inserts a 3-level tree, verifies depth 0/1/2 in budget_categories and a 3-level API response, and deletes the test budget on teardown. No src/ files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add TREE-03 backward-compat regression tests for 3 existing 2-level cities + green full suite</name>
  <files>C:/EV-Accounts/backend/test/treasury-3level.test.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/test/treasury-3level.test.ts (the file created in Task 1 — append, do not rewrite)
    - C:/EV-Accounts/backend/test/essentialsService-finance-summary.test.ts (model for asserting shape properties on a getBudgetById result — lines ~37-43)
    - .planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-PATTERNS.md (TREE-03 backward-compat test pattern, lines ~234-253)
  </read_first>
  <action>
    Append TREE-03 regression tests to the existing describe block in `treasury-3level.test.ts` (do not create a new file).

    Resolve the budget UUIDs for three known 2-level cities at runtime via SQL rather than hardcoding — query the operating budget for each by joining municipalities → data_sources → budgets. For each of Portland (OR), San Jose (CA), and Dallas (TX): `SELECT b.id FROM treasury.budgets b JOIN treasury.data_sources ds ON ds.id = b.data_source_id JOIN treasury.municipalities m ON m.id = ds.municipality_id WHERE m.name = $1 AND ds.dataset_type = 'operating' ORDER BY b.fiscal_year DESC LIMIT 1` (confirm the exact join columns by reading the columns returned in Task 1's data_sources query; adjust the municipality linkage if the schema differs). If a city has no operating budget row, substitute another confirmed 2-level city from STATE.md's seeded list (e.g. Sacramento, Long Beach, Gresham) and note the substitution in a comment.

    Write one `it()` per city: 'Portland OR operating budget returns a 2-level tree (no depth-2 subcategories)'. In each: `const result = await getBudgetById(cityBudgetId)`; assert `result` not null; `const root = result.categories[0]`; assert `root.subcategories` is defined (Level 2 exists); assert `root.subcategories[0].subcategories` is `undefined` (NO Level 3 for these 2-level cities — this is the backward-compat proof); assert `root.subcategories[0].lineItems` is defined (leaf carries line items at depth 1). These are READ-ONLY queries — they must not insert or delete any rows.

    Because CA state (loaded in Phase 33) currently emits a 2-level tree per 34-RESEARCH.md (processCA.js unchanged until Phase 35), do NOT assert CA state is 3-level here — that is Phase 35. Only the synthetic test budget from Task 1 is 3-level in this phase.

    After appending, run the FULL ev-accounts-api suite (not just the filtered file) to confirm zero regressions across all 4 pre-existing test files plus the new one.
  </action>
  <verify>
    <automated>cd C:/EV-Accounts/backend && npm test</automated>
  </verify>
  <acceptance_criteria>
    - The full `cd C:/EV-Accounts/backend && npm test` run is green (all test files pass, including arcgis-sources-coverage and the 3 essentialsService tests).
    - The TREE-03 `it()` tests for all three cities pass, each asserting `root.subcategories[0].subcategories === undefined` and `root.subcategories[0].lineItems` defined.
    - The TREE-03 tests perform only SELECT queries (no INSERT/UPDATE/DELETE against the named city budgets).
    - `treasury-3level.test.ts` contains TREE-01, TREE-02, and TREE-03 coverage in a single describe block (file still contains `treasury_sync_budget_tree` and now also `getBudgetById` for at least 4 `it()` blocks total).
    - `cd C:/EV-Accounts/backend && npm run typecheck` exits 0 (no type errors introduced in src/; note test files are not typechecked by tsc but typecheck must still pass for the repo).
  </acceptance_criteria>
  <done>TREE-03 is proven: three existing 2-level city budgets return a 2-level tree with no depth-2 subcategories, and the full ev-accounts-api test suite passes green with zero regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check 3 city pages in live app + record TREE traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (TREE-01/02/03 checklist items + Traceability table rows)
    - .planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-VALIDATION.md (Manual-Only Verifications section)
  </read_first>
  <what-built>
    An automated vitest integration + regression test (`treasury-3level.test.ts`) proving TREE-01 (3-level RPC submit), TREE-02 (3-level API response), and TREE-03 (existing 2-level cities unchanged). No ev-accounts-api or treasury-tracker source code was changed — the infrastructure was already correct. The full ev-accounts-api test suite is green.
  </what-built>
  <how-to-verify>
    1. Open https://treasurytracker.empowered.vote in a browser.
    2. Select **Portland** (Oregon group). Confirm the Money Out / Budget tab loads and the icicle renders categories that drill down normally (no errors, totals look like prior runs).
    3. Select **San Jose** (California group). Confirm the same — page loads, icicle drills down, totals present.
    4. Select **Dallas** (Texas group). Confirm the same.
    5. Confirm none of the three pages show a broken layout, missing categories, or a console error in the Money Out tab.
    Expected outcome: all three existing 2-level city pages render identically to before — no regression introduced by the verification work (which made no code changes, so any regression would be unexpected).
  </how-to-verify>
  <action>
    After human approval, update `.planning/REQUIREMENTS.md`:
    - Change `- [ ] **TREE-01**` to `- [x] **TREE-01**` and similarly for TREE-02 and TREE-03 in the TREE section.
    - Append to each TREE line a short note: "(satisfied by existing infrastructure + treasury-3level.test.ts, Phase 34)".
    - In the Traceability table, change the Status cell for TREE-01, TREE-02, TREE-03 rows from `Pending` to `Complete`.
    Do not alter any DATA/INFRA/ICICLE/RETROFIT rows.
  </action>
  <verify>
    <human-check>User confirms all three city pages (Portland, San Jose, Dallas) render correctly with no regression in the live app.</human-check>
  </verify>
  <acceptance_criteria>
    - User has typed approval (or described issues, which would block).
    - REQUIREMENTS.md shows `- [x]` for TREE-01, TREE-02, TREE-03.
    - REQUIREMENTS.md Traceability table shows `Complete` for TREE-01, TREE-02, TREE-03 rows.
    - No DATA/INFRA/ICICLE/RETROFIT requirement lines were modified.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all three city pages render correctly, or describe any rendering issue.</resume-signal>
  <done>Human has verified the 3 city pages render correctly in the live app, and REQUIREMENTS.md marks TREE-01/02/03 as complete with a note that they are satisfied by existing infrastructure plus the verification test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production Supabase DB | The test inserts a real (sentinel FY 9999) budget row into the live treasury schema via the RPC and reads existing production city budgets |
| client → categories API | `/api/treasury/budgets/:id/categories` accepts a path UUID from any caller (optionalAuth, public read) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | test inserts a budget row into production treasury.budgets | mitigate | `afterAll` issues `DELETE FROM treasury.budgets WHERE id = $1` for the test budget id (FK cascade removes categories + line items); sentinel FY 9999 makes any leaked row identifiable; acceptance criterion asserts post-run count = 0 |
| T-34-02 | Tampering | TREE-03 regression tests run against real production city budgets | mitigate | TREE-03 tests are SELECT-only via getBudgetById; acceptance criterion forbids INSERT/UPDATE/DELETE against named city budgets |
| T-34-03 | Tampering | UUID injection in `/budgets/:id/categories` route path | accept | Already mitigated upstream — `UUID_REGEX` validates the param at the route in treasury.ts (no code change in this phase); read-only public endpoint, low value target |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (34-RESEARCH.md Package Legitimacy Audit: zero new packages); slopcheck N/A — nothing to verify |
</threat_model>

<verification>
- `cd C:/EV-Accounts/backend && npm test` is green (full suite, all 5 test files including new treasury-3level).
- `cd C:/EV-Accounts/backend && npm run typecheck` exits 0.
- `treasury-3level.test.ts` exercises a live 3-level RPC submit, a 3-level API response, and three 2-level regression checks, and cleans up its inserted budget.
- Human spot-check confirms Portland, San Jose, and Dallas render correctly in the live app.
- REQUIREMENTS.md TREE-01/02/03 marked complete.
</verification>

<success_criteria>
1. A test 3-level tree submitted to `treasury_sync_budget_tree` lands as depth-0/1/2 `budget_categories` rows (TREE-01).
2. The categories API (`getBudgetById`) returns a 3-level `BudgetCategory[]` for that test data — Level 3 node visible with leaf `lineItems` (TREE-02).
3. Portland, San Jose, and Dallas return correct 2-level trees and render identically in the live app — no regressions (TREE-03).
4. The full ev-accounts-api vitest suite passes; the test budget is removed on teardown; REQUIREMENTS.md TREE-01/02/03 marked complete.
</success_criteria>

<output>
Create `.planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-01-SUMMARY.md` when done.
</output>
