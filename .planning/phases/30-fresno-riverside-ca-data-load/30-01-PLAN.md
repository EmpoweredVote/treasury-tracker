---
phase: 30-fresno-riverside-ca-data-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedFresnoRiversideCA.js
autonomous: true
requirements: [POPUL-01]
must_haves:
  truths:
    - "Fresno municipality row exists in treasury.municipalities with state=CA, population=550000, population_year=2024"
    - "Riverside municipality row exists in treasury.municipalities with state=CA, population=324000, population_year=2024"
    - "Four data_source rows exist with canonical names matching what processors look up"
  artifacts:
    - path: "scripts/seedFresnoRiversideCA.js"
      provides: "Idempotent two-city seeder for Fresno + Riverside municipalities and 4 data_source rows"
      contains: "Fresno General Fund Operating Budget"
  key_links:
    - from: "scripts/seedFresnoRiversideCA.js"
      to: "treasury_list_source_ids"
      via: "RPC verification of the four canonical data_source names"
      pattern: "treasury_list_source_ids"
---

<objective>
Seed the Fresno and Riverside municipality rows (with 2024 Census population) and the four canonical data_source rows that the Fresno and Riverside processors will look up by name. This establishes the DB foundation so the city picker and per-capita display will work automatically once budget data is loaded.

Purpose: POPUL-01 (Fresno + Riverside) — both cities seeded with 2024 population; per-capita ($/resident) display depends on `population` being correct. The data_source rows must exist with exact canonical names before Plan 2 / Plan 3 processors run (Pitfall 5: name mismatch breaks processor lookup).

Output: `scripts/seedFresnoRiversideCA.js` (new) plus 2 municipality rows and 4 data_source rows in the DB, verified via `treasury_list_source_ids`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-fresno-riverside-ca-data-load/30-CONTEXT.md
@.planning/phases/30-fresno-riverside-ca-data-load/30-PATTERNS.md
@scripts/seedLongBeachBakersfieldCA.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/seedFresnoRiversideCA.js adapting the Long Beach + Bakersfield two-city seeder</name>
  <read_first>
    - scripts/seedLongBeachBakersfieldCA.js (canonical two-city seeder template — copy loadEnv(), Supabase client setup, upsertMunicipality(), upsertDataSourceByName(), and the treasury_list_source_ids verification block verbatim)
    - .planning/phases/30-fresno-riverside-ca-data-load/30-PATTERNS.md (seeder pattern lines 23–243: exact payload shapes and verbatim-copy guidance)
  </read_first>
  <action>
    Create `scripts/seedFresnoRiversideCA.js` as a near-verbatim adaptation of `scripts/seedLongBeachBakersfieldCA.js`.

    Copy verbatim: the imports block, `loadEnv()`, the `SUPABASE_URL`/`SUPABASE_KEY` config + client creation (with the `process.exit(1)` guard on missing key), `upsertMunicipality()`, and `upsertDataSourceByName()`.

    Change the `MUNICIPALITIES` array to exactly two entries:
    - Fresno: `{ name: 'Fresno', state: 'CA', entity_type: 'city', population: 550000, population_year: 2024 }` — do NOT set `county_id` (Fresno County not loaded; stays NULL per Deferred Ideas).
    - Riverside: `{ name: 'Riverside', state: 'CA', entity_type: 'city', population: 324000, population_year: 2024 }` — do NOT set `county_id` (Riverside County not loaded; stays NULL per Deferred Ideas). This is the City of Riverside, NOT Riverside County (Pitfall 6).

    Define the four `DATA_SOURCES` rows with these EXACT canonical names (these strings are the contract that Plan 2 / Plan 3 processors look up — they must match character-for-character):
    - `name: 'Fresno General Fund Operating Budget'`, `api_type: 'pdf_download'`, `dataset_type: 'operating'`, `dataset_id: 'fresno-gf-operating'`, `municipality_id: fresnoId`
    - `name: 'Fresno General Fund Revenue Budget'`, `api_type: 'pdf_download'`, `dataset_type: 'revenue'`, `dataset_id: 'fresno-gf-revenue'`, `municipality_id: fresnoId`
    - `name: 'Riverside General Fund Operating Budget'`, `api_type: 'pdf_download'`, `dataset_type: 'operating'`, `dataset_id: 'riverside-gf-operating'`, `municipality_id: riversideId`
    - `name: 'Riverside General Fund Revenue Budget'`, `api_type: 'pdf_download'`, `dataset_type: 'revenue'`, `dataset_id: 'riverside-gf-revenue'`, `municipality_id: riversideId`

    Set the `expectedNames` verification array to those four name strings. Keep the `treasury_list_source_ids` RPC verification block verbatim, and keep the `process.exit(1)`/`process.exit(2)` error-handling conventions. The seeder must be idempotent (select-then-update-or-insert) so it is safe to re-run if either municipality row already exists (Assumption A7).
  </action>
  <verify>
    <automated>node -c scripts/seedFresnoRiversideCA.js</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seedFresnoRiversideCA.js` exists and passes `node -c` (syntax check)
    - File contains the four exact strings: `Fresno General Fund Operating Budget`, `Fresno General Fund Revenue Budget`, `Riverside General Fund Operating Budget`, `Riverside General Fund Revenue Budget`
    - File contains `population: 550000` (Fresno) and `population: 324000` (Riverside)
    - File contains `population_year: 2024` for both
    - File does NOT set `county_id` for either city
    - File calls `supabase.rpc('treasury_list_source_ids')` for verification
  </acceptance_criteria>
  <done>Seeder script written, syntactically valid, with correct populations and the four canonical data_source names.</done>
</task>

<task type="auto">
  <name>Task 2: Run the seeder live and verify rows in DB</name>
  <read_first>
    - scripts/seedFresnoRiversideCA.js (the file created in Task 1)
  </read_first>
  <action>
    Run `node scripts/seedFresnoRiversideCA.js`. The script loads env from `.env.local`/`.env`, upserts both municipality rows, upserts the four data_source rows, and verifies all four canonical names appear in `treasury_list_source_ids`.

    Confirm the run prints `OK: Fresno General Fund Operating Budget`, `OK: Fresno General Fund Revenue Budget`, `OK: Riverside General Fund Operating Budget`, `OK: Riverside General Fund Revenue Budget` (one OK line per source) and exits 0. If the script exits non-zero (missing key, MISSING source, or write error), stop and report — do not proceed to Plan 2/3.
  </action>
  <verify>
    <automated>node scripts/seedFresnoRiversideCA.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/seedFresnoRiversideCA.js` exits 0
    - Output contains `OK:` lines for all four canonical data_source names and no `MISSING:` lines
    - Re-running the script a second time also exits 0 (idempotency — second run shows "updated existing" rather than failing on duplicate)
  </acceptance_criteria>
  <done>Both municipalities and all four data_source rows present in DB; verification RPC confirms all four names; idempotent on re-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase | Service-role key writes to treasury schema; key never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | SUPABASE_SERVICE_KEY in seedFresnoRiversideCA.js | mitigate | Key loaded via loadEnv() from .env; never console.log'd (established pattern, copied verbatim) |
| T-30-02 | Tampering | Wrong-government data (Riverside County vs City) | mitigate | Municipality row hard-codes entity_type='city', population 324K (city), name 'Riverside' (Pitfall 6) |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `node scripts/seedFresnoRiversideCA.js` exits 0 with four OK lines
- Fresno and Riverside rows queryable in treasury.municipalities with correct CA state and 2024 population
</verification>

<success_criteria>
Fresno (pop 550000) and Riverside (pop 324000) municipality rows and four canonical data_source rows exist in the DB and pass `treasury_list_source_ids` verification. Seeder is idempotent.
</success_criteria>

<output>
Create `.planning/phases/30-fresno-riverside-ca-data-load/30-01-SUMMARY.md` when done
</output>
