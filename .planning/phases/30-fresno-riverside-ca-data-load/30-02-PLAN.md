---
phase: 30-fresno-riverside-ca-data-load
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - scripts/extractFresno.py
  - scripts/processFresno.js
  - docs/Fresno/
autonomous: true
requirements: [DATA-05]
must_haves:
  truths:
    - "Fresno General Fund operating budget total is ~$483M (within $383M–$583M band), enterprise funds excluded"
    - "Fresno operating budget rows loaded for at least one fiscal year (target FY2020–2026 depth per available PDFs)"
    - "Fresno revenue rows loaded if a clean revenue section exists, else deferred (best-effort per D-07)"
  artifacts:
    - path: "scripts/extractFresno.py"
      provides: "pdfplumber extractor, General-Fund-only filter at extraction time"
      contains: "General Fund"
    - path: "scripts/processFresno.js"
      provides: "Node processor with $383M–$583M sanity band, treasury_sync_budget_tree loader"
      contains: "383_000_000"
  key_links:
    - from: "scripts/processFresno.js"
      to: "scripts/extractFresno.py"
      via: "execSync python call"
      pattern: "extractFresno.py"
    - from: "scripts/processFresno.js"
      to: "treasury_sync_budget_tree"
      via: "RPC per fiscal year"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Build the Fresno PDF pipeline — download adopted budget PDFs into `docs/Fresno/`, write `scripts/extractFresno.py` (single-year, General-Fund-only filter at extraction time), write `scripts/processFresno.js` (sanity band + RPC loader), dry-run to confirm totals, then live-load operating (and best-effort revenue) into the DB.

Purpose: DATA-05 — Fresno operating + revenue budget loaded and visible in app. The critical correctness constraint (Pitfall 1): enterprise/proprietary funds (~$899M) exceed General Fund (~$483M), so the filter MUST be applied at extraction time (D-04/D-06) — a bleed would nearly double the reported total. Fresno runs before Riverside (D-09) to establish the single-year baseline.

Output: `scripts/extractFresno.py`, `scripts/processFresno.js`, `docs/Fresno/*.pdf`, and Fresno General Fund operating (~$483M/FY) budget rows in the DB. Revenue loaded if cleanly available, else documented as deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-fresno-riverside-ca-data-load/30-CONTEXT.md
@.planning/phases/30-fresno-riverside-ca-data-load/30-RESEARCH.md
@.planning/phases/30-fresno-riverside-ca-data-load/30-PATTERNS.md
@scripts/extractLongBeach.py
@scripts/processLongBeach.js
@scripts/processFremont.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Fresno adopted budget PDFs and inspect General Fund table structure</name>
  <read_first>
    - .planning/phases/30-fresno-riverside-ca-data-load/30-RESEARCH.md (Open Question 1: Fresno table structure unknown; Pitfall 3: amount scale; Environment Availability: docs/Fresno/ must be created)
    - scripts/extractLongBeach.py (single-year extractor template — the structure you will adapt depends on what you see in the PDF)
  </read_first>
  <action>
    Create the `docs/Fresno/` directory. Download City of Fresno adopted budget PDFs from the official Fresno city site (fresno.gov budget/finance pages). Target FY2020–FY2026 depth per D-01: download as many adopted budget PDFs with consistent structure as are available back to FY2020; if the format changes significantly before FY2022, stop at FY2022 (D-01 permits this). Name files with an `fy<YEAR>-` prefix so `detect_fy_from_filename()` can parse the fiscal year (e.g., `fy2025-adopted-budget.pdf`).

    Open the FY2025 adopted budget PDF and identify the General Fund summary table: (a) whether rows are department-level (like Oakland) or expenditure-category-level (like Long Beach), (b) the exact page-level marker text that identifies the General Fund section, (c) the exact fund-label string used for General Fund rows so the extraction-time filter can match it, and (d) the amount scale — spot-check one known department: if it shows `$483` rather than `$483,000,000`, the PDF uses thousands and `processFresno.js` will need `toFullDollars()` (Pitfall 3 / Assumption A1).

    Record these four findings (structure, page marker, fund label string, amount scale) in the SUMMARY — they drive Task 2 extraction logic.
  </action>
  <verify>
    <automated>node -e "const {readdirSync}=require('fs'); const f=readdirSync('docs/Fresno').filter(x=>x.toLowerCase().endsWith('.pdf')); if(f.length<1){console.error('no PDFs in docs/Fresno');process.exit(1)} console.log('PDFs:',f.join(', '))"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/Fresno/` directory exists and contains at least one `.pdf` file
    - PDF filenames carry an `fy<YEAR>` token parseable by `detect_fy_from_filename()`
    - SUMMARY records: table structure (dept vs category level), General Fund page marker text, the exact General Fund fund-label string, and amount scale (full dollars vs thousands)
  </acceptance_criteria>
  <done>Fresno PDFs downloaded to docs/Fresno/; General Fund table structure, fund-label string, and amount scale documented for use in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write extractFresno.py and processFresno.js, dry-run to confirm ~$483M General Fund total</name>
  <read_first>
    - scripts/extractLongBeach.py (analog: copy parse_money(), detect_fy_from_filename(), _extract_label_and_last_value(), dedup block, argparse main verbatim; adapt extract_from_page row-detection and the General-Fund filter)
    - scripts/processLongBeach.js (analog: copy loadEnv, client, extractPDF(), buildTree(), ensureMunicipality(), upsertDataSource(), loadFiscalYear(), parseArgs verbatim; adapt city names, sanity band, --revenue flag)
    - scripts/processFremont.js (toFullDollars() helper — only wire in if Task 1 found amounts in thousands)
    - docs/Fresno/ PDFs (the actual files to extract)
    - .planning/phases/30-fresno-riverside-ca-data-load/30-PATTERNS.md (processFresno.js / extractFresno.py adaptation guidance, lines 247–519)
  </read_first>
  <action>
    Write `scripts/extractFresno.py` adapted from `scripts/extractLongBeach.py`. Copy `parse_money()`, `detect_fy_from_filename()`, `_extract_label_and_last_value()`, the dedup-by-`(department, fiscal_year)` block, and the argparse main (`pdf_path` + `--mode operating|revenue`) verbatim. Adapt `extract_from_page()` row-detection to the Fresno General Fund table structure found in Task 1.

    Enforce the extraction-time General Fund filter (D-04/D-06, Pitfall 1): only `results.append(...)` rows whose fund label explicitly matches the Fresno General Fund label string identified in Task 1. For every non-General-Fund row, emit `print(f'  [skip] Non-GF row excluded: ...', file=sys.stderr)`. Never emit enterprise/proprietary fund rows. Each emitted row is a dict with keys `department`, `fund` (set to `'General Fund'`), `adopted_amount`, `fiscal_year`, `page_num`. Revenue `--mode revenue` detects a `Revenues:`/`Resources:` section best-effort (D-07).

    Write `scripts/processFresno.js` adapted from `scripts/processLongBeach.js`. Copy `loadEnv()`, the Supabase client block, `buildTree(rows, datasetType)`, `loadFiscalYear()` (calls `treasury_sync_budget_tree`), and the `parseArgs` block (`--dry-run`, `--pdf`, `--revenue`) verbatim. Set `extractPDF()` to call `scripts/extractFresno.py`. Set `resolvePdfDir()` to use `docs/Fresno`. Set `ensureMunicipality()` to look up `name='Fresno', state='CA'` and error with "run seedFresnoRiversideCA.js first" if absent. In `upsertDataSource()`, build names as `Fresno General Fund Operating Budget FY${fy}` / `Fresno General Fund Revenue Budget FY${fy}` and `dataset_id` prefixed `fresno-`. Set the sanity band constants to `GF_BAND_MIN = 383_000_000` and `GF_BAND_MAX = 583_000_000` (±$100M around the $483M target — do NOT use the ~$2.0B all-funds figure as the ceiling; that would let enterprise bleed pass). If Task 1 found amounts in thousands, add `toFullDollars()` from processFremont.js and apply it where `adopted_amount` is consumed.

    Run `node scripts/processFresno.js --dry-run` and confirm each FY's General Fund total lands in the $383M–$583M band (expect ~$483M for FY2025). Run `node scripts/processFresno.js --dry-run --revenue` to check whether a clean revenue section exists. If the dry-run total is outside the band, fix the extractor filter before any live load. If revenue dry-run yields 0 rows or implausible totals, mark revenue deferred (D-07 / Pitfall 4) — do not block.
  </action>
  <verify>
    <automated>node scripts/processFresno.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extractFresno.py` exists; `python scripts/extractFresno.py "<a docs/Fresno PDF>"` emits valid JSON to stdout
    - extractFresno.py contains a `file=sys.stderr` skip log for non-General-Fund rows (proves extraction-time filter exists)
    - `scripts/processFresno.js` passes `node -c` and contains `383_000_000` and `583_000_000`
    - `node scripts/processFresno.js --dry-run` exits 0 and prints at least one `fiscal_year=` line with a total inside $383M–$583M (does NOT hit the SCALE MISMATCH exit 3)
    - processFresno.js `ensureMunicipality()` looks up `'Fresno'` / `'CA'`
    - Revenue dry-run result recorded (loaded vs deferred) for SUMMARY
  </acceptance_criteria>
  <done>extractFresno.py + processFresno.js written; dry-run confirms General Fund total ~$483M within band; revenue availability determined.</done>
</task>

<task type="auto">
  <name>Task 3: Live-load Fresno operating (and best-effort revenue) into the DB</name>
  <read_first>
    - scripts/processFresno.js (the file from Task 2 — the live path is the same code minus --dry-run)
    - .planning/phases/30-fresno-riverside-ca-data-load/30-RESEARCH.md (Pitfall 4: revenue best-effort)
  </read_first>
  <action>
    Run `node scripts/processFresno.js` (no `--dry-run`) to live-load all available Fresno General Fund operating fiscal years via `treasury_sync_budget_tree`. The RPC deletes+reinserts per (data_source_id, fiscal_year) so re-runs are idempotent. Confirm each FY reports inserted rows and a total within the $383M–$583M band.

    If Task 2 confirmed a clean revenue section, run `node scripts/processFresno.js --revenue` to load revenue rows. If revenue was determined unavailable in Task 2, skip the revenue live-load and record it as deferred per D-07 in the SUMMARY (operating-only ship is acceptable; do not block the phase).

    Do not run enrichment here — that is Plan 4 behind the combined $0.10 cost gate.
  </action>
  <verify>
    <automated>node scripts/processFresno.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processFresno.js` (live) exits 0 and reports `Inserted:` rows for at least one Fresno operating fiscal year
    - No FY triggers the SCALE MISMATCH exit-3 halt
    - Revenue either loaded (Inserted rows reported) or explicitly recorded as deferred per D-07 in SUMMARY
    - Re-running the live load exits 0 (idempotent delete+reinsert)
  </acceptance_criteria>
  <done>Fresno General Fund operating rows loaded in DB (~$483M/FY); revenue loaded or documented deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF files → Python extractor | PDF paths from controlled docs/Fresno readdir, not user input |
| Python stdout → Node processor | JSON parsed; size-capped via maxBuffer |
| Node processor → Supabase | Service-role RPC writes; key never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | Enterprise fund bleed inflating total to ~$2.0B | mitigate | Extraction-time General Fund filter (D-06) + $383M–$583M sanity band halt (exit 3) before any DB write |
| T-30-04 | Denial of Service | Oversized PDF buffer in execSync | mitigate | maxBuffer 8MB cap copied verbatim from processLongBeach.js |
| T-30-05 | Tampering | Command injection via PDF path | mitigate | PDF path from readdirSync(docs/Fresno), double-quoted in execSync string (established pattern) |
| T-30-06 | Information Disclosure | SUPABASE_SERVICE_KEY logging | mitigate | Key loaded via loadEnv(); never console.log'd |
</threat_model>

<verification>
- `node scripts/processFresno.js --dry-run` total within $383M–$583M band
- extractFresno.py emits stderr skip logs for non-General-Fund rows (filter proven active)
- Live load reports inserted rows for ≥1 Fresno operating FY
</verification>

<success_criteria>
Fresno General Fund operating budget (~$483M/FY, enterprise funds excluded) loaded into the DB for at least one fiscal year via the new extractor/processor pipeline. Revenue loaded if cleanly available, else documented as deferred per D-07.
</success_criteria>

<output>
Create `.planning/phases/30-fresno-riverside-ca-data-load/30-02-SUMMARY.md` when done
</output>
