---
phase: 28-oakland-san-jose-ca-data-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedOaklandSanJoseCA.js
autonomous: true
requirements: [POPUL-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "Oakland municipality row exists in treasury.municipalities with state=CA and population=444000"
    - "San Jose municipality row exists in treasury.municipalities with state=CA and population=997000"
    - "Four data_source rows exist with correct GPF/General Fund names for Oakland and San Jose operating + revenue"
    - "Running the seeder a second time updates rather than duplicates rows (idempotent)"
  artifacts:
    - path: "scripts/seedOaklandSanJoseCA.js"
      provides: "Idempotent municipality + data_source seeder for Oakland and San Jose"
      contains: "upsertMunicipality"
      min_lines: 80
  key_links:
    - from: "scripts/seedOaklandSanJoseCA.js"
      to: "treasury.municipalities"
      via: "upsertMunicipality SELECT-then-INSERT/UPDATE"
      pattern: "from\\('municipalities'\\)"
    - from: "scripts/seedOaklandSanJoseCA.js"
      to: "treasury_list_source_ids"
      via: "verification RPC"
      pattern: "treasury_list_source_ids"
---

<objective>
Seed Oakland and San Jose municipality rows (with 2024 population) and the four data_source rows the city processors will look up. This is the foundation plan: Plans 02 and 03 cannot load budget data until these rows exist and verify via `treasury_list_source_ids`.

Purpose: Satisfy POPUL-01 (population for per-capita display) and create the named data_source rows that DATA-02 (Oakland) and DATA-03 (San Jose) processors depend on.
Output: `scripts/seedOaklandSanJoseCA.js` — idempotent, run once, verifies its own writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-CONTEXT.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seedOaklandSanJoseCA.js with municipality + data_source upserts</name>
  <files>scripts/seedOaklandSanJoseCA.js</files>
  <read_first>
    - scripts/seedCaliforniaCities.js (PRIMARY analog — copy upsertMunicipality lines 47-95, upsertDataSourceByName lines 117-164, verification lines 293-322 verbatim, adapt payloads)
    - scripts/seedSacramentoCA.js (copy loadEnv() lines 41-52 verbatim; single-city verification structure)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-PATTERNS.md (seeder section, lines 23-198 — exact code excerpts and line citations)
  </read_first>
  <action>
    Create scripts/seedOaklandSanJoseCA.js as an ES module (project uses `import`). Copy the `loadEnv()` helper from seedSacramentoCA.js (reads `../.env.local` then `../.env`, sets process.env keys not already present) and call it before creating the Supabase client. Initialize the client with `SUPABASE_URL` (default `https://kxsdzaojfaibhuzmclfq.supabase.co`) and `SUPABASE_KEY = process.env.SUPABASE_SERVICE_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY`; exit(1) with "Missing SUPABASE_SERVICE_KEY" if absent.

    Define a MUNICIPALITIES array with two payloads: `{ name: 'Oakland', state: 'CA', entity_type: 'city', population: 444000, population_year: 2024 }` and `{ name: 'San Jose', state: 'CA', entity_type: 'city', population: 997000, population_year: 2024 }` (per POPUL-01). Leave `county_id` unset/NULL for both (county linking is deferred per CONTEXT.md Deferred Ideas).

    Copy `upsertMunicipality(m)` verbatim from seedCaliforniaCities.js: SELECT id by name+state via `.maybeSingle()`, UPDATE if existing else INSERT, return the row id. This handles Pitfall 7 (pre-existing rows) — never use raw INSERT. Capture the returned `oaklandId` and `sanJoseId`.

    Copy `upsertDataSourceByName(src)` verbatim from seedCaliforniaCities.js (SELECT by name → UPDATE-or-INSERT). Define and upsert these four data_source rows with EXACT names (the processors look these up by name and by municipality_id):
    - `{ name: 'Oakland General Purpose Fund Operating Budget', api_type: 'pdf_download', dataset_type: 'operating', dataset_id: 'oakland-gpf-operating', base_url: 'https://cao-94612.s3.us-west-2.amazonaws.com/documents/', municipality_id: oaklandId }`
    - `{ name: 'Oakland General Purpose Fund Revenue Budget', api_type: 'pdf_download', dataset_type: 'revenue', dataset_id: 'oakland-gpf-revenue', base_url: 'https://cao-94612.s3.us-west-2.amazonaws.com/documents/', municipality_id: oaklandId }`
    - `{ name: 'San Jose General Fund Operating Budget', api_type: 'pdf_download', dataset_type: 'operating', dataset_id: 'sanjose-gf-operating', base_url: 'https://www.sanjoseca.gov/', municipality_id: sanJoseId }`
    - `{ name: 'San Jose General Fund Revenue Budget', api_type: 'pdf_download', dataset_type: 'revenue', dataset_id: 'sanjose-gf-revenue', base_url: 'https://www.sanjoseca.gov/', municipality_id: sanJoseId }`

    Oakland fund label invariant (D-06): the string "General Purpose Fund" — NEVER "General Fund" — must appear in both Oakland data_source names. San Jose uses "General Fund" (D-03).

    Copy the verification block from seedCaliforniaCities.js (lines 293-322): call `supabase.rpc('treasury_list_source_ids')`, then assert all four exact names appear in the listing; print `OK: {name}` for each hit and `MISSING: {name}` otherwise; exit(1) if any missing. End with `main().catch(e => { console.error('Fatal:', e); process.exit(2); })`.

    Do NOT create per-FY data_source rows here — the processors (Plans 02/03) create per-FY pdf_download rows internally via the `(municipality_id, api_type, dataset_id, dataset_type)` upsert pattern. This seeder creates the canonical named rows that verification and the app rely on.
  </action>
  <verify>
    <automated>node scripts/seedOaklandSanJoseCA.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seedOaklandSanJoseCA.js contains `function upsertMunicipality`
    - scripts/seedOaklandSanJoseCA.js contains the string `General Purpose Fund` (Oakland, D-06) and the string `San Jose General Fund Operating Budget`
    - scripts/seedOaklandSanJoseCA.js contains `population: 444000` and `population: 997000`
    - scripts/seedOaklandSanJoseCA.js contains `treasury_list_source_ids`
    - scripts/seedOaklandSanJoseCA.js does NOT contain the string `Oakland General Fund` (would violate D-06)
    - `node scripts/seedOaklandSanJoseCA.js` exits 0 and prints `OK:` for all four expected data_source names
  </acceptance_criteria>
  <done>Seeder runs to completion, prints OK for all four data_source names, and exits 0. Oakland and San Jose municipality rows exist with correct population.</done>
</task>

<task type="auto">
  <name>Task 2: Verify idempotency and DB state via second run</name>
  <files>scripts/seedOaklandSanJoseCA.js</files>
  <read_first>
    - scripts/seedOaklandSanJoseCA.js (the file just created — confirm UPDATE branch logs)
    - scripts/seedCaliforniaCities.js (idempotency reference — `(updated existing ...)` log lines)
  </read_first>
  <action>
    Run the seeder a SECOND time. On the second run, every upsert must take the UPDATE branch (existing row found), not INSERT. Confirm the console output shows `(updated existing municipality row ...)` for both cities and `(updated existing row ...)` for all four data_sources — proving idempotency. If any second-run line says `(inserted new ...)`, that indicates a SELECT-key mismatch (name or state); fix the lookup keys before continuing.

    Then query the DB to confirm exactly one row per city and per data_source name (no duplicates). Use the Supabase MCP tools (mcp__supabase-local) per the project memory rule — run `execute_sql` against `treasury.municipalities` filtered to name IN ('Oakland','San Jose') AND state='CA', and against `treasury.data_sources` filtered to the four exact names. Confirm population_year=2024 and population values 444000 / 997000 are persisted. Do not ask the user to paste into the Supabase Dashboard.
  </action>
  <verify>
    <automated>node scripts/seedOaklandSanJoseCA.js</automated>
  </verify>
  <acceptance_criteria>
    - Second `node scripts/seedOaklandSanJoseCA.js` run exits 0 and output contains `(updated existing` (not `(inserted new`) for municipality and data_source upserts
    - `execute_sql` on treasury.municipalities returns exactly 1 Oakland row (population=444000, population_year=2024) and 1 San Jose row (population=997000, population_year=2024)
    - `execute_sql` on treasury.data_sources returns exactly 4 rows matching the four exact names, no duplicates
  </acceptance_criteria>
  <done>Second run is idempotent (all UPDATE branches), and DB queries confirm exactly one row per municipality and per data_source name with correct population values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase | Service-role key writes to treasury schema; key sourced from local .env only |
| local .env → process | Secrets loaded from .env/.env.local, never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information disclosure | SUPABASE_SERVICE_KEY in seeder | mitigate | Key read from process.env via loadEnv() (.env/.env.local only); never console.logged; .env is gitignored (established Sacramento pattern) |
| T-28-02 | Tampering | Duplicate / mislabeled municipality rows | mitigate | upsertMunicipality() SELECT-then-UPDATE by (name,state) prevents duplicates; idempotency verified in Task 2 (Pitfall 7) |
| T-28-03 | Denial of resource | Excessive Claude API spend | accept | No AI calls in this plan; enrichment cost gate handled in Plan 04 |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all libraries are pre-existing project dependencies (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `node scripts/seedOaklandSanJoseCA.js` exits 0 and verification block prints OK for all four data_source names
- Second run is fully idempotent (UPDATE branches only)
- DB confirms exactly one municipality row per city with correct 2024 population, and four data_source rows with exact names
</verification>

<success_criteria>
Oakland and San Jose municipality rows exist (state=CA, populations 444000 / 997000, year 2024), and the four canonically-named data_source rows exist and verify via treasury_list_source_ids. Re-running the seeder makes no duplicate rows.
</success_criteria>

<output>
Create `.planning/phases/28-oakland-san-jose-ca-data-load/28-01-SUMMARY.md` when done
</output>
