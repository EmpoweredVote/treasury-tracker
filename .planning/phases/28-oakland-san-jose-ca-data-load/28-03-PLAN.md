---
phase: 28-oakland-san-jose-ca-data-load
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - scripts/extractSanJose.py
  - scripts/processSanJose.js
  - docs/SanJose/
autonomous: false
requirements: [DATA-03]
user_setup: []
must_haves:
  truths:
    - "San Jose Adopted Operating Budget PDFs are downloaded into docs/SanJose/ and the exact General Fund label is identified"
    - "extractSanJose.py filters to the General Fund only and excludes enterprise funds at extraction time (D-03)"
    - "extractSanJose.py uses early-exit page skipping so a 400+ page PDF extracts in well under 2 minutes (Pitfall 3)"
    - "processSanJose.js dry-run produces a General Fund total in the ~$1.7-1.9B range"
    - "San Jose General Fund operating loads into treasury budgets; revenue loaded if a clean section exists, else deferred per D-05"
  artifacts:
    - path: "scripts/extractSanJose.py"
      provides: "pdfplumber General Fund extractor with enterprise-fund filter and large-PDF early exit"
      contains: "General Fund"
      min_lines: 80
    - path: "scripts/processSanJose.js"
      provides: "San Jose processor building operating + best-effort revenue trees, calling treasury_sync_budget_tree"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
  key_links:
    - from: "scripts/processSanJose.js"
      to: "scripts/extractSanJose.py"
      via: "execSync python extraction"
      pattern: "extractSanJose"
    - from: "scripts/processSanJose.js"
      to: "treasury_sync_budget_tree"
      via: "Supabase RPC per fiscal year"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Build the San Jose extraction-to-load pipeline: download the annual Adopted Operating Budget PDFs, write extractSanJose.py (General-Fund-only with enterprise-fund filter at extraction time and large-PDF early exit, D-03/D-04), write processSanJose.js (operating + best-effort revenue, D-05), dry-run to validate the ~$1.7-1.9B General Fund total, then live-load.

Purpose: Satisfy DATA-03 — San Jose General Fund operating budget visible in the app, enterprise funds excluded.
Output: scripts/extractSanJose.py, scripts/processSanJose.js, downloaded PDFs in docs/SanJose/, loaded San Jose budget rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-CONTEXT.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-RESEARCH.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download San Jose PDFs, identify General Fund label, write extractSanJose.py</name>
  <files>scripts/extractSanJose.py, docs/SanJose/</files>
  <read_first>
    - scripts/extractFremont.py (PRIMARY analog — copy parse_money lines 28-37, header/column detection lines 86-132, revenue+expenditure section parsing lines 161-206, main block lines 209-215; adapt the page-level fund filter)
    - scripts/extractPortland.py (parse_fy lines 54-66 — reuse verbatim for San Jose FY YYYY-YY labels)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-PATTERNS.md (extractSanJose.py section lines 521-655; San Jose-Specific Pattern Notes lines 968-975)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-RESEARCH.md (San Jose Budget PDF Availability; Pitfalls 3,4; Open Question 5; Pattern 2 enterprise-fund filter)
  </read_first>
  <action>
    Create docs/SanJose/ (mkdir -p docs/SanJose). Download the Adopted Operating Budget PDFs from the sanjoseca.gov budget pages for FY2022-23, FY2023-24, FY2024-25, and FY2025-26 (each page has an "Entire Document (PDF)" link; sanjoseca.gov returns 403 for direct automated URL access per A4, so fetch via the page link). Save as fy2022-23-adopted-operating-budget.pdf through fy2025-26-adopted-operating-budget.pdf. Minimum viable is one fiscal year, but load all that download cleanly (D-04 — researcher determines FY range).

    Before writing the filter, inspect FY2024-25 (Open Question 5 / Pitfall 4): run a pdfplumber text scan over the first ~50 pages to identify (a) the EXACT General Fund label string as it appears as a fund header, (b) the exact enterprise fund names to exclude (Airport, Wastewater, Water, Environmental Services and any others present), and (c) the General Fund summary table column structure (Adopted / Estimated Actual / Proposed). Record the exact strings in the SUMMARY.

    Create scripts/extractSanJose.py with a python3 shebang and imports sys, json, re, pdfplumber. Copy parse_money from extractFremont.py (lines 28-37) and parse_fy from extractPortland.py (lines 54-66). Define GENERAL_FUND_MARKERS with the exact General Fund string and EXCLUDED_FUNDS with the confirmed enterprise fund names. Implement extract_budget(pdf_path) per Pattern 2: for each page, FIRST apply the early-exit `if 'General Fund' not in text[:200]: continue` (Pitfall 3 — critical for 400+ page performance), then require Total Revenues / Total Expenditures markers, then detect the page fund and `continue` if it is in EXCLUDED_FUNDS or not in GENERAL_FUND_MARKERS. The enterprise-fund filter MUST run at extraction time, not in the processor (D-03). For accepted General Fund pages, parse revenue items (between a Revenues header and Total Revenues) and expenditure items (between an Expenditures header and Total Expenditures) using the Fremont line-parsing logic (lines 161-206), emitting amounts arrays plus column metadata. Tag each result with fiscal_year via parse_fy. Emit a stderr WARNING for None fiscal_year rows. The main block parses one pdf_path arg and prints json.dumps(data, indent=2) to stdout.

    Performance target (Pitfall 3): extraction of one 400+ page PDF completes in under 30 seconds and under 2 minutes hard ceiling. If it exceeds 2 minutes, the early-exit guard is not firing — fix before proceeding.
  </action>
  <verify>
    <automated>python scripts/extractSanJose.py "docs/SanJose/fy2024-25-adopted-operating-budget.pdf"</automated>
  </verify>
  <acceptance_criteria>
    - docs/SanJose/ contains at least fy2024-25-adopted-operating-budget.pdf
    - scripts/extractSanJose.py contains `EXCLUDED_FUNDS` and the early-exit guard `text[:200]`
    - scripts/extractSanJose.py contains the literal string `General Fund`
    - Running extractSanJose.py on the FY2024-25 PDF prints non-empty JSON, exits 0, and completes in under 2 minutes
    - The emitted output contains no rows attributed to Airport, Wastewater, or Water funds (enterprise funds filtered at extraction, D-03)
  </acceptance_criteria>
  <done>San Jose PDFs downloaded; extractSanJose.py extracts General-Fund-only revenue + expenditure items, excludes enterprise funds at extraction time, and runs within the performance ceiling. Exact GF label and excluded fund names recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Write processSanJose.js and dry-run validate the General Fund total</name>
  <files>scripts/processSanJose.js</files>
  <read_first>
    - scripts/processFremont.js (PRIMARY analog — copy imports/ROOT lines 26-34, Supabase init lines 37-40, extractPDF lines 49-56, toFullDollars lines 59-61, buildOperatingTree lines 64-82, buildRevenueTree lines 85-115, per-FY load loop lines 255-286, main lines 290-327; change Fremont to SanJose)
    - scripts/seedSacramentoCA.js (loadEnv lines 41-52 — copy verbatim)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-PATTERNS.md (processSanJose.js section lines 659-820; best-effort revenue note lines 974-975)
    - scripts/extractSanJose.py (the extractor from Task 1 — confirm output shape: revenue_items, expenditure_items, amounts arrays, column indexes)
  </read_first>
  <action>
    Create scripts/processSanJose.js as an ES module. Copy from processFremont.js: imports + ROOT (lines 26-34), loadEnv (from seedSacramentoCA.js lines 41-52), Supabase init (lines 37-40, exit 2 if no key), extractPDF (lines 49-56) pointing pyScript at scripts/extractSanJose.py with maxBuffer 8*1024*1024 (Security T-28-04) and double-quoted path (Security T-28-05), toFullDollars (lines 59-61 — apply only if Task 1 found San Jose amounts in thousands), buildOperatingTree (lines 64-82) with each line item fund field f set to General Fund, buildRevenueTree (lines 85-115) splitting Taxes vs Non-Tax Revenue, the per-FY operating + best-effort revenue load loop (lines 255-286) which MUST check revenue_items.length > 0 before building/loading revenue and log "revenue deferred per D-05" when empty (do NOT fail), the treasury_sync_budget_tree RPC call with p_dataset_type operating/revenue and p_triggered_by bulk_load, and main() (lines 290-327) with --dry-run boolean and optional --pdf string, pointing pdfDir at docs/SanJose.

    Resolve the data_source ids by the names created in Plan 01: "San Jose General Fund Operating Budget" and "San Jose General Fund Revenue Budget" (or the (municipality_id, api_type=pdf_download, dataset_id=fy{year}, dataset_type) upsert pattern from processFremont.js lines 152-180 — match whichever lookup processFremont.js uses).

    Add a per-FY sanity check: print the computed General Fund operating total and flag if it falls outside ~$1.6B-$2.0B (Pitfall — General Fund target is ~$1.7-1.9B, NOT the ~$5.3B all-funds figure). If the dry-run total lands near $5B (enterprise bleed — filter failed) or near $1.7M (thousands not converted), STOP and surface to the user before live-loading. End with main().catch(e => { console.error('Fatal:', e); process.exit(2); }).

    Run the dry-run across all downloaded PDFs and capture per-FY operating + revenue totals in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/processSanJose.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/processSanJose.js contains `treasury_sync_budget_tree` and `extractSanJose`
    - scripts/processSanJose.js contains `maxBuffer` set to an 8MB cap
    - scripts/processSanJose.js checks `revenue_items.length` before loading revenue and logs a deferral message when empty (D-05)
    - `node scripts/processSanJose.js --dry-run` exits 0 and prints a General Fund operating total per fiscal year
    - Each printed dry-run operating total falls within ~$1.6B-$2.0B OR the script explicitly halts with a scale/enterprise-bleed warning for the user
  </acceptance_criteria>
  <done>processSanJose.js dry-runs cleanly, producing per-FY General Fund operating totals in the ~$1.7-1.9B range (or halting on scale/enterprise mismatch). Revenue handled best-effort. No DB writes yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    San Jose General Fund operating (and best-effort revenue, D-05) data live-loaded into treasury budgets via treasury_sync_budget_tree for all downloaded fiscal years. Enterprise funds excluded at extraction time (D-03).
  </what-built>
  <how-to-verify>
    The executor must FIRST run the live load (remove --dry-run) for operating, then revenue if available, then perform these DB checks before pausing:
    1. execute_sql (mcp__supabase-local) on treasury.budgets joined to data_sources confirms San Jose General Fund operating rows exist for at least 1 fiscal_year, total in ~$1.7-1.9B (well below the ~$5.3B all-funds figure — confirms enterprise funds excluded).
    2. execute_sql confirms no loaded category names correspond to Airport/Wastewater/Water enterprise funds.
    3. If revenue was loaded, confirm at least one fiscal_year of dataset_type=revenue exists; otherwise SUMMARY notes revenue deferred per D-05.
    Then the human verifies in the app at treasurytracker.empowered.vote:
    1. "San Jose" appears in the city picker under "California".
    2. San Jose operating budget total is in the ~$1.7-1.9B General Fund range (not ~$5.3B).
    3. Money In / revenue tab shows data if revenue was loaded.
  </how-to-verify>
  <resume-signal>Type "approved" if San Jose shows a ~$1.7-1.9B General Fund operating total with enterprise funds excluded, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file path → execSync(python) | Path from controlled readdir of docs/SanJose/, not user CLI input |
| script → Supabase | Service-role key writes budget rows; key from local .env only |
| remote sanjoseca.gov PDF → local disk | Untrusted 400+ page PDF parsed by pdfplumber (data only, no execution) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Denial of service | Large 400+ page PDF buffer in execSync | mitigate | maxBuffer 8*1024*1024 cap; extractor early-exit page skipping keeps stdout small (Pitfall 3) |
| T-28-05 | Tampering | Command injection via PDF path in execSync | mitigate | PDF path from readdirSync(docs/SanJose/) only; path double-quoted in execSync (processFremont.js / T-17-03 pattern) |
| T-28-06 | Information disclosure | SUPABASE_SERVICE_KEY in processor | mitigate | Key read from process.env via loadEnv; never logged; .env gitignored |
| T-28-08 | Spoofing / integrity | All-funds total loaded instead of General Fund (~$5.3B vs ~$1.8B) | mitigate | Enterprise-fund filter at extraction time (D-03) plus dry-run sanity band ~$1.6B-$2.0B with explicit halt before live load |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs — pdfplumber and supabase-js pre-existing (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- python scripts/extractSanJose.py on FY2024-25 emits non-empty JSON, no enterprise-fund rows, completes under 2 minutes
- node scripts/processSanJose.js --dry-run prints per-FY General Fund operating totals in ~$1.7-1.9B (or halts on mismatch)
- After live load: treasury.budgets has San Jose General Fund operating rows; total well below all-funds ~$5.3B
- Human confirms San Jose visible in app under California with ~$1.7-1.9B General Fund total
</verification>

<success_criteria>
DATA-03 satisfied: San Jose General Fund operating budget loaded with a total in the ~$1.7-1.9B range, enterprise funds excluded at extraction time (D-03), visible in the app. Revenue loaded if available, otherwise documented as deferred (D-05).
</success_criteria>

<output>
Create `.planning/phases/28-oakland-san-jose-ca-data-load/28-03-SUMMARY.md` when done. Record: PDFs downloaded, the exact General Fund label and excluded enterprise fund names, extraction runtime, per-FY operating + revenue totals, and the revenue loaded-or-deferred decision.
</output>
