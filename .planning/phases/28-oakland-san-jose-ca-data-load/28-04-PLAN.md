---
phase: 28-oakland-san-jose-ca-data-load
plan: 04
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified: []
autonomous: false
requirements: [ENRICH-01, DATA-02, DATA-03, POPUL-01]
user_setup: []
must_haves:
  truths:
    - "Enrichment cost is estimated via dry-run BEFORE any live Claude call; combined Oakland + San Jose estimate is confirmed under $0.10 (D-07)"
    - "category_enrichment rows are created for Oakland and San Jose top operating (and revenue) categories"
    - "Oakland and San Jose appear in the app city picker under California with correct totals"
    - "Per-capita ($/resident) displays correctly using Oakland 444K and San Jose 997K populations"
    - "Enrichment descriptions are visible (not empty) for top categories in both cities"
  artifacts: []
  key_links:
    - from: "enrichCategories.js"
      to: "treasury.category_enrichment"
      via: "name_key upsert"
      pattern: "category_enrichment"
---

<objective>
Run AI category enrichment for Oakland and San Jose behind the $0.10 cost gate (D-07), then verify the full phase success criteria end-to-end in the app: both cities visible under California, correct operating totals, per-capita display, and enrichment descriptions present.

Purpose: Satisfy ENRICH-01 (Oakland + San Jose) and confirm DATA-02, DATA-03, POPUL-01 are observably true in the running app.
Output: category_enrichment rows for both cities; a written verification record for the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-CONTEXT.md
@.planning/phases/28-oakland-san-jose-ca-data-load/28-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Approve running enrichment after confirming the combined cost estimate is under $0.10 (D-07).</decision>
  <context>
    D-07 sets a tightened $0.10 cost gate for this phase (vs the project-wide $5 threshold). The executor MUST run enrichCategories.js in --dry-run mode for each city/year FIRST to obtain the estimated row count and token cost, then sum the Oakland + San Jose estimate. RESEARCH.md estimates ~$0.04 combined, but the actual estimate depends on the number of loaded categories (which depends on what Plans 02/03 loaded). Present the actual summed estimate before any live call.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with enrichment</name>
      <pros>Combined dry-run estimate is under $0.10; enrichment descriptions become visible (ENRICH-01).</pros>
      <cons>Spends a small amount of Claude API budget (expected ~$0.04).</cons>
    </option>
    <option id="halt">
      <name>Halt — estimate approaches or exceeds $0.10</name>
      <pros>Respects the D-07 gate and the unfunded-nonprofit cost discipline.</pros>
      <cons>ENRICH-01 not satisfied this run; needs user direction on scope reduction.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed (estimate confirmed under $0.10) or halt (estimate too high — give direction).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Run enrichment for Oakland and San Jose (post-gate)</name>
  <files>scripts/enrichCategories.js</files>
  <read_first>
    - scripts/enrichCategories.js (reused unchanged — confirm --city, --state, --year, --dry-run flags and name_key idempotency)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-RESEARCH.md (Enrichment invocation lines 416-427; D-07 cost gate)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-02-SUMMARY.md (Oakland loaded fiscal years)
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-03-SUMMARY.md (San Jose loaded fiscal years)
  </read_first>
  <action>
    Only after the decision checkpoint approved "proceed": run enrichCategories.js live for each loaded city/year. Use the latest loaded fiscal year per city as the primary enrichment target (and additional years if the combined estimate stays under $0.10). Invoke `node scripts/enrichCategories.js --city Oakland --state CA --year {YYYY}` and `node scripts/enrichCategories.js --city "San Jose" --state CA --year {YYYY}` (San Jose name MUST be quoted — two words). enrichCategories.js is idempotent via name_key upsert, so re-running is safe. Do NOT modify enrichCategories.js — it is fully reusable.

    After running, confirm via execute_sql (mcp__supabase-local) that treasury.category_enrichment has non-empty description rows for both Oakland and San Jose top operating categories. Record the actual cost (or estimate, since live cost is small) and the enriched row counts in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city Oakland --state CA --year 2024 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - The combined Oakland + San Jose --dry-run cost estimate printed before live calls is under $0.10 (D-07)
    - After live runs, execute_sql on treasury.category_enrichment returns 1+ non-empty description rows for Oakland and 1+ for San Jose
    - Re-running enrichCategories.js for a city/year already enriched updates via name_key (no duplicate enrichment rows)
  </acceptance_criteria>
  <done>Enrichment ran under the $0.10 gate; category_enrichment has non-empty descriptions for Oakland and San Jose top categories; re-run is idempotent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 28 outcome: Oakland (GPF operating, 2+ FYs) and San Jose (General Fund operating, ~$1.7-1.9B) loaded and enriched, both with 2024 population for per-capita display.
  </what-built>
  <how-to-verify>
    Verify all six Phase 28 success criteria at treasurytracker.empowered.vote:
    1. "Oakland" and "San Jose" appear in the city picker under "California".
    2. Oakland operating budget shows data for at least 2 fiscal years (biennial); totals in the GPF band (~$800M/year GPF; note the $2.1B requirement figure refers to all-funds per 28-02-SUMMARY Open Question 1 resolution).
    3. San Jose operating budget tab shows a total in the ~$1.7-1.9B General Fund range (enterprise funds filtered).
    4. Both cities show Revenue / Money In tabs with at least one fiscal year populated, OR revenue is documented as deferred per D-05 in the SUMMARYs.
    5. Per-capita ($/resident) displays correctly for Oakland (~444K) and San Jose (~997K).
    6. Enrichment descriptions are visible (not empty) for top categories in both cities.
    For Oakland, also confirm the primary fund label reads "General Purpose Fund" (D-06), not "General Fund".
  </how-to-verify>
  <resume-signal>Type "approved" if all six criteria pass (with Oakland showing GPF label and any deferred revenue documented), or list which criteria fail.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write phase verification record</name>
  <files>.planning/phases/28-oakland-san-jose-ca-data-load/28-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-02-SUMMARY.md
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-03-SUMMARY.md
    - .planning/ROADMAP.md (Phase 28 Success Criteria, the six items)
  </read_first>
  <action>
    Write .planning/phases/28-oakland-san-jose-ca-data-load/28-VERIFICATION.md recording the outcome of each of the six Phase 28 success criteria (pass / deferred / fail), the human-verify result from the prior checkpoint, the resolution of Open Question 1 (Oakland $2.1B all-funds vs ~$800M GPF), the revenue loaded-or-deferred status for each city (D-05), and the enrichment cost actually incurred vs the $0.10 gate (D-07). Note any deferred items (skipped Oakland biennials, deferred revenue) so they carry forward to STATE.md.
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('.planning/phases/28-oakland-san-jose-ca-data-load/28-VERIFICATION.md')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/28-oakland-san-jose-ca-data-load/28-VERIFICATION.md exists
    - It records a pass/deferred/fail status for all six ROADMAP Phase 28 success criteria
    - It records the Open Question 1 resolution, per-city revenue status (D-05), and enrichment cost vs the $0.10 gate (D-07)
  </acceptance_criteria>
  <done>28-VERIFICATION.md exists and documents all six success criteria, the GPF/all-funds resolution, revenue status, and enrichment cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Claude API (Anthropic) | Enrichment sends category names; cost-bounded by D-07 gate |
| script → Supabase | Service-role key writes category_enrichment rows; key from local .env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Denial of resource | Excessive Claude API spend on enrichment | mitigate | Mandatory --dry-run cost estimate before any live call; blocking decision checkpoint enforces the $0.10 combined gate (D-07); checkpoint is never auto-approvable |
| T-28-06 | Information disclosure | SUPABASE_SERVICE_KEY / ANTHROPIC_API_KEY in enrichment | mitigate | Keys read from process.env (.env/.env.local); never logged; .env gitignored |
| T-28-09 | Tampering | Duplicate enrichment rows on re-run | mitigate | enrichCategories.js upserts by name_key (idempotent); verified by re-run check |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs — enrichCategories.js and its deps pre-existing (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Combined Oakland + San Jose enrichment --dry-run estimate confirmed under $0.10 before any live call (D-07)
- treasury.category_enrichment has non-empty descriptions for Oakland and San Jose top categories
- Human confirms all six Phase 28 success criteria in the app
- 28-VERIFICATION.md exists and records every criterion plus deferred items
</verification>

<success_criteria>
ENRICH-01 satisfied: Oakland and San Jose have AI-generated enrichment within the $0.10 gate. All six Phase 28 success criteria verified true in the app (both cities visible, correct totals, per-capita display, enrichment visible), with Oakland showing "General Purpose Fund" (D-06) and any deferred revenue documented (D-05).
</success_criteria>

<output>
Create `.planning/phases/28-oakland-san-jose-ca-data-load/28-04-SUMMARY.md` and `.planning/phases/28-oakland-san-jose-ca-data-load/28-VERIFICATION.md` when done.
</output>
