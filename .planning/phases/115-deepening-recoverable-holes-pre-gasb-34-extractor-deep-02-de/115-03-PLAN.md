---
phase: 115-deepening-recoverable-holes-pre-gasb-34-extractor
plan: 03
type: execute
wave: 2
depends_on: [115-02]
files_modified:
  - scripts/maAcfrExtract.mjs
  - scripts/processMAAcfr.js
  - scripts/processMARevenueAcfr.js
  - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-03-MA-LOADLOG.md
autonomous: true
requirements: [DEEP-03]

must_haves:
  truths:
    - "MA's six remaining holes (FY2001, FY2002, FY2004, FY2005, FY2014, FY2021) are each recovered with the year tying exactly (±TOL) to its printed General Fund totals — or documented unrecoverable in the loadlog with the specific extraction failure"
    - "The two interior holes (FY2014, FY2021 — '-table' anchor breakage per 108-02 loadlog) are attacked first: per-year parser handling in maAcfrExtract.mjs (alternate anchors / column-merge tolerance), the highest-value recoveries since they interrupt an otherwise-contiguous FY2003–FY2025 series"
    - "For FY2001–FY2005 era years: the executor determines per year whether the statement is GASB-34 Governmental-Funds format (GAAP label) or pre-GASB-34 Combined-Statement format — pre-34-format years route through scripts/pre34Extract.mjs (from plan 115-02) and carry the distinct 'pre-GASB-34 combined statement basis' label"
    - "Already-loaded MA years (19: FY2003, 2006–2013, 2015–2020, 2022–2025) are untouched: pre/post row counts + FY2024 spot total identical — parser changes MUST NOT alter any currently-tying year's extraction (regression-gated in dry-run before any live write)"
    - "Idempotent never-overwrite with 0 data_sources residue: re-run of one recovered FY = 0 net change; 0 rows remain for ma-acfr-% dataset_ids"
  artifacts:
    - path: "scripts/maAcfrExtract.mjs"
      provides: "MA extractor with per-year handling for the FY2014/FY2021 anchor variants (and any 2001–2005-era adjustments)"
      contains: "extractMAGeneralFund"
    - path: "scripts/processMAAcfr.js"
      provides: "MA operating loader with recovered years loading and (if pre-34 years recovered) the distinct basis label"
      contains: "clampForRender"
    - path: ".planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-03-MA-LOADLOG.md"
      provides: "Per-FY disposition for all six holes, per-year format determination (GAAP vs pre-34), regression evidence for the 19 existing years, idempotency + 0-residue evidence"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/maAcfrExtract.mjs (+ scripts/pre34Extract.mjs for pre-34-format years)"
      to: "processMAAcfr.js / processMARevenueAcfr.js"
      via: "extractMAGeneralFund per-year variants; pre34Extract import only if a 2001–2005 year proves pre-34-format"
      pattern: "extractMAGeneralFund"
    - from: "MA loaders"
      to: "treasury.budgets (MA state node)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Recover the six **Massachusetts** honest holes (FY2001/02/04/05 old-format + FY2014/FY2021 interior anchor-breaks) by extending `scripts/maAcfrExtract.mjs` with per-year handling, reusing plan 115-02's `pre34Extract.mjs` for any year that proves pre-GASB-34-format, and live-loading only the recovered years — the 19 existing MA years provably untouched.

All source material is already cached (`_acfr-work/ma/MA2001–MA2025 .pdf + .txt`). The 108-02 loadlog pins the failure modes: FY2014/FY2021 = `-table` column-merge breaks the REVENUES/Total-revenues anchors; FY2001–2005 = older statement layout the generic parser doesn't isolate (FY2003 + FY2006 were already recovered by a previous generalization, so FY2002/04/05 are likely near-misses of the modern format, FY2001 possibly pre-34).

Output: MA series as contiguous as extraction honestly allows, `115-03-MA-LOADLOG.md` with per-FY disposition + regression evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md
@.planning/milestones/v2.13-phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-MA-LOADLOG.md

<interfaces>
From scripts/processMAAcfr.js (read fully first):
  - Runtime-extraction loader: YEARS = 2001..2025 attempted; loadYear() downloads (urlFor: `macomptroller.org/wp-content/uploads/acfr_fy-{YYYY}.pdf`, FY2017 special-case) → pdftotext -table → extractMAGeneralFund → null on no-tie (hole-tolerant loop skips nulls).
  - UNITS = 1_000; TOL = 5; POPULATION = 7_029_917; dataSource(fy) = "Massachusetts State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - Ephemeral data_sources lifecycle present (Phase 111); dataset_ids ma-acfr-gf-operating / ma-acfr-gf-revenue.
  - Predates Phase 114 hardening (parseArgs strict:false at line ~87) — apply the same guards while touching (strict parseArgs, --fy value validation against YEARS, try/finally cleanup, surfaced select errors).
From scripts/maAcfrExtract.mjs: token-order + positional variants; returns {found, ...} result consumed by both loaders. FY2014/FY2021 fail inside this file — fix HERE, not in the loaders.
From plan 115-02 (dependency): scripts/pre34Extract.mjs with extractPre34GeneralFund(text) + the "pre-GASB-34 combined statement basis" label convention — import ONLY if a 2001–2005 year's statement proves to be the Combined-Statement format.
Regression contract: before ANY live write, full dry-run must show all 19 currently-loaded years still tying with IDENTICAL totals to the current production values (capture them from DB first) — a parser change that shifts an existing year's numbers is a defect, not a recovery.
DB verification: mcp__supabase-local__execute_sql if available, else read-only node select scripts with repo .env credentials.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix FY2014/FY2021 anchor breakage in maAcfrExtract.mjs (regression-gated)</name>
  <files>scripts/maAcfrExtract.mjs</files>
  <read_first>
    - scripts/maAcfrExtract.mjs (current anchor + variant logic)
    - _acfr-work/ma/MA2014.txt and _acfr-work/ma/MA2021.txt (the actual failing text — note the OCR-ish run-together tokens like "ofRevenues" seen in MA2014)
    - _acfr-work/ma/MA2015.txt (a tying neighbor year — the layout the parser expects)
    - .planning/milestones/v2.13-phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-MA-LOADLOG.md (pinned failure descriptions)
  </read_first>
  <action>
    Diagnose exactly why extractMAGeneralFund returns not-found/no-tie on MA2014.txt and MA2021.txt (per loadlog: `-table` column merge breaks the REVENUES/Total-revenues anchor detection; MA2014 also shows run-together tokens like "Statement ofRevenues"). Add per-year-tolerant handling — e.g. whitespace-insensitive anchor regexes, an alternate re-extraction pass with `pdftotext -layout` for years where `-table` merges columns, or a column-position fallback — WITHOUT changing the extraction result for any currently-tying year. If a year needs `-layout` re-extraction, regenerate that year's txt into a separate file (MA2014_layout.txt) so the cached -table text for other years is untouched. Gate: dry-run both MA loaders across ALL years; FY2014 + FY2021 must now tie; the 19 existing years must still tie with unchanged totals.
  </action>
  <verify>
    <automated>node scripts/processMAAcfr.js --dry-run --fy 2014 2>&1 | grep -qi "PASS" && node scripts/processMAAcfr.js --dry-run --fy 2021 2>&1 | grep -qi "PASS" && node scripts/processMAAcfr.js --dry-run --fy 2015 2>&1 | grep -qi "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - FY2014 and FY2021 dry-run tie in both loaders (or the loadlog documents the specific irreducible failure)
    - All 19 currently-loaded years still tie with totals identical to captured production values
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Recover the FY2001–FY2005 era holes with per-year format determination</name>
  <files>scripts/maAcfrExtract.mjs, scripts/processMAAcfr.js, scripts/processMARevenueAcfr.js</files>
  <read_first>
    - _acfr-work/ma/MA2002.txt, MA2004.txt, MA2005.txt, MA2001.txt (the failing-era text — determine per year: modern Governmental-Funds format vs pre-34 Combined-Statement format)
    - _acfr-work/ma/MA2003.txt (the already-recovered same-era year — what generalization already handles)
    - scripts/pre34Extract.mjs (from plan 115-02 — import if any year is pre-34-format)
  </read_first>
  <action>
    For each of FY2001/2002/2004/2005: identify the statement format in the cached text. Modern-format years → extend maAcfrExtract per-year handling (same discipline as Task 1). Pre-34 Combined-Statement years → route through extractPre34GeneralFund with the "Massachusetts State CAFR — General Fund (FY${fy} actual, pre-GASB-34 combined statement basis)" label (loader plumbing: per-year label override where basis differs). Every recovered year must tie both printed totals (±TOL); years that cannot tie are left as holes with the specific failure logged. Apply Phase-114 hardening to both MA loader files while touching them. Re-run the full regression dry-run (all years) after each parser change.
  </action>
  <verify>
    <automated>node scripts/processMAAcfr.js --dry-run 2>&1 | tail -8 (exit 0; recovered-year count visible); node scripts/processMARevenueAcfr.js --dry-run 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Each of FY2001/2002/2004/2005 either ties in both loaders or has a specific documented failure in the loadlog
    - Any pre-34-format year carries the distinct basis label (grep the dry-run output for "pre-GASB-34" on that year)
    - Full dry-run exit 0 with the 19 existing years' totals unchanged
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live-load recovered years per-FY, verify untouched/idempotent/0-residue, loadlog + SUMMARY + commits</name>
  <files>treasury.budgets (MA node), 115-03-MA-LOADLOG.md, 115-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§6 never-overwrite contract)
  </read_first>
  <action>
    Capture MA baseline (row counts per dataset, FY list, FY2024 operating total as spot-hash + ALL 19 existing years' totals). Live-load ONLY the recovered years per-`--fy`, both datasets. Post-load: (a) all 19 pre-existing years' totals byte-identical, (b) each recovered year present with correct basis label + source stamps, (c) re-run one recovered FY = 0 net change, (d) 0 data_sources residue for ma-acfr-%. Write `115-03-MA-LOADLOG.md`: per-FY disposition for all six holes (recovered / unrecoverable+reason), per-year format determination, regression table (19 years before/after), idempotency + residue evidence. Commit extractor+loaders (feat) and loadlog (docs) atomically; write 115-03-SUMMARY.md.
  </action>
  <verify>
    <automated>grep -q "Load Disposition" .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-03-MA-LOADLOG.md && git log --oneline --grep="115-03" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - DB: recovered years present with correct labels; 19 existing years' totals identical before/after (regression table in loadlog)
    - Re-run of one recovered FY = 0 net change; 0 residue for ma-acfr-%
    - Loadlog has a disposition row for each of FY2001/2002/2004/2005/2014/2021
    - feat + docs commits exist; 115-03-SUMMARY.md exists with Self-Check
  </acceptance_criteria>
</task>

</tasks>

<verification>
- MA dry-runs exit 0; FY2014/FY2021 (and as many of FY2001–05 as honestly tie) recovered.
- 19 existing MA years' production totals unchanged (regression table).
- Idempotency + 0 residue proven; loadlog dispositions cover all six holes.
</verification>
