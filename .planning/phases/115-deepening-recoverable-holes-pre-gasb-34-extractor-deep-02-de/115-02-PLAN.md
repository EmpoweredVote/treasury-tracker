---
phase: 115-deepening-recoverable-holes-pre-gasb-34-extractor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pre34Extract.mjs
  - scripts/processCTAcfr.js
  - scripts/processCTRevenueAcfr.js
  - scripts/processWIAcfr.js
  - scripts/processWIRevenueAcfr.js
  - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-02-CT-WI-LOADLOG.md
autonomous: true
requirements: [DEEP-02, DEEP-03, DEEP-04]

must_haves:
  truths:
    - "A committed pre-GASB-34 extractor (scripts/pre34Extract.mjs) parses the 'Combined Statement of Revenues, Expenditures, and Changes in Fund Balances — All Governmental Fund Types' format, anchoring on that exact title (NOT the Higher-Education or proprietary 'Combined Statement' variants in the same CAFRs) and taking the General (Fund) column = 1st numeric column"
    - "Every pre-GASB-34 row in the DB carries a distinct honest basis label — '{State} State CAFR — General Fund (FY{fy} actual, pre-GASB-34 combined statement basis)' — visibly different from the GAAP label on the same node's modern years (DEEP-02, success criterion 1)"
    - "CT pre-34 years (FY1988–FY2001 candidates, cached in _acfr-work/ct/) and WI pre-34 years (FY2000–FY2001, cached in _acfr-work/wi/) load as deep as extraction ties allow — every loaded year ties (±TOL) to its printed General column Total revenues / Total expenditures, non-tying years logged as honest holes (DEEP-04)"
    - "CT FY2006 (scanned, no text layer) is recovered via the free OCR path (pdftoppm + Tesseract 5.4 at C:\\Program Files\\Tesseract-OCR\\tesseract.exe) with the transcription tying to the printed GF totals, and carries the normal GAAP basis label (FY2006 is GASB-34-era) — or is documented unrecoverable with the OCR evidence (DEEP-03)"
    - "Existing CT FY2002–FY2025 (23yr) and WI FY2002–FY2025 (24yr) modern rows are untouched: pre/post row counts + spot-year totals identical"
    - "Idempotent never-overwrite with 0 data_sources residue: re-running one deepened FY per state = 0 net change; 0 rows remain for ct-acfr-% / wi-acfr-% dataset_ids"
    - "WI pre-FY2000 (4-section multi-file era) is NOT attempted — out of scope, recorded in the loadlog"
  artifacts:
    - path: "scripts/pre34Extract.mjs"
      provides: "Reusable pre-GASB-34 Combined-Statement GF-column extractor (committed, mirrors maAcfrExtract.mjs placement)"
      contains: "All Governmental Fund Types"
      min_lines: 60
    - path: "scripts/processCTAcfr.js"
      provides: "CT operating loader extended: pre-34 years via pre34Extract + distinct basis label + FY2006 OCR-sourced entry"
      contains: "pre-GASB-34"
    - path: "scripts/processWIAcfr.js"
      provides: "WI operating loader extended: FY2000–FY2001 via pre34Extract + distinct basis label"
      contains: "pre-GASB-34"
    - path: ".planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-02-CT-WI-LOADLOG.md"
      provides: "Per-FY disposition for CT FY1988–2001 + FY2006 + WI FY2000–2001, OCR evidence, basis-label proof, untouched-modern evidence"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/pre34Extract.mjs"
      to: "processCTAcfr.js / processCTRevenueAcfr.js / processWIAcfr.js / processWIRevenueAcfr.js"
      via: "imported for pre-34 years only; modern years keep their existing extractors"
      pattern: "pre34Extract"
    - from: "CT/WI loaders"
      to: "treasury.budgets (CT + WI state nodes)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Build the **pre-GASB-34 extractor** (DEEP-02) as committed `scripts/pre34Extract.mjs`, wire it into the CT and WI loaders for their pre-FY2002 years with a **distinct honest basis label**, load CT FY1988–FY2001 + WI FY2000–FY2001 as deep as ties allow (DEEP-04), and recover **CT FY2006** via the free OCR path (DEEP-03) — all per-`--fy` live loads, modern rows provably untouched.

The cached source text already exists (`_acfr-work/ct/CT1988+.txt`, `_acfr-work/wi/WI2000/01.txt` — clean pdftotext output; only CT2006 is scanned). Verified structure: General = 1st column, thousands, "Total Revenues"/"Total Expenditures" rows present (research §3).

Output: pre-34 extractor committed; CT deepened to its earliest tying year + FY2006 filled (or documented unrecoverable); WI deepened FY2000–2001; `115-02-CT-WI-LOADLOG.md` with full disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md
@.planning/milestones/v2.13-phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-02-CT-LOADLOG.md
@.planning/milestones/v2.13-phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-03-WI-LOADLOG.md

<interfaces>
From scripts/processCTAcfr.js / processWIAcfr.js (files being extended — read fully first):
  - Runtime-extraction loaders; per-year exact `SRC = {fy: url}` maps; UNITS = 1_000; TOL = 5; token-order-first extraction with positional fallback, gated per-dataset.
  - CT URLs FY1988–FY2001: `https://osc.ct.gov/reports/oldcafrpdfs/CT_CAFR_FY{YYYY}.pdf` (already downloaded + text-extracted to _acfr-work/ct/). CT needs browser UA (constant already in file).
  - WI FY2000–2001: cached in _acfr-work/wi/ (WI2000.txt Combined Statement at line ~1426 verified).
  - Ephemeral data_sources lifecycle (Phase 111) present — keep verbatim. dataset_ids ct-acfr-gf-operating / ct-acfr-gf-revenue / wi-acfr-gf-operating / wi-acfr-gf-revenue.
  - Modern label: "{State} State ACFR — General Fund (FY{fy} actual, GAAP basis)". Pre-34 label MUST differ: "{State} State CAFR — General Fund (FY{fy} actual, pre-GASB-34 combined statement basis)". CT2006 keeps the GAAP label.
  - These loaders predate Phase 114 hardening: apply the same guards while touching (strict parseArgs, --fy value validation, try/finally data_sources cleanup, surfaced select errors) — fix-while-touching only.
Pre-34 statement facts (verified in cache, research §3):
  - Title anchor: "Combined Statement of Revenues, Expenditures, and Changes in Fund Balances" + "All Governmental Fund Types" within the following ~3 lines. Do NOT match the Higher-Education/University-Hospital or proprietary variants (CT1995.txt:198 vs the real one at :2513).
  - General = 1st numeric column; column lineups vary by year/state (Special Revenue | Debt Service | Capital Projects [| Expendable Trust | Memorandum Totals]); thousands.
  - Negative-value risk: scan every transcribed year; clampForRender path stays wired (P2 clamp, parent total preserved).
OCR tooling (CT FY2006 only): pdftoppm on PATH (Poppler 25.07); tesseract at `C:\Program Files\Tesseract-OCR\tesseract.exe` (NOT on PATH — invoke by full path). CT2006.pdf cached (4.8MB). FY2005/FY2007 statement page positions bracket the search window. Start 300dpi; escalate to 400dpi/--psm 6 if the tie fails. The tie gate is the OCR-error defense: no tie → iterate or document unrecoverable.
DB verification: mcp__supabase-local__execute_sql if available, else read-only node select scripts with repo .env credentials.
POPULATION sanity: CT 3_605_944, WI 5_893_718 (per-capita GF ~$2k–$5k for 1990s years — smaller than modern, but same order).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/pre34Extract.mjs + wire into CT/WI loaders with distinct basis label + dry-run tie all pre-34 years</name>
  <files>scripts/pre34Extract.mjs, scripts/processCTAcfr.js, scripts/processCTRevenueAcfr.js, scripts/processWIAcfr.js, scripts/processWIRevenueAcfr.js</files>
  <read_first>
    - scripts/maAcfrExtract.mjs (the committed-extractor precedent: export shape, token-order + positional variants, found/tie result object)
    - scripts/processCTAcfr.js + scripts/processCTRevenueAcfr.js (SRC map, loadYear, tie gate, label plumbing)
    - scripts/processWIAcfr.js + scripts/processWIRevenueAcfr.js (same)
    - _acfr-work/ct/CT1995.txt lines ~2513-2600 and _acfr-work/wi/WI2000.txt lines ~1426-1530 (real Combined-Statement layouts to parse)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§3 structure + title-anchor caution)
  </read_first>
  <action>
    Create `scripts/pre34Extract.mjs` exporting `extractPre34GeneralFund(text)` returning `{found, revenues:[{name,total}], revTotal, expenditures:[{name,total}], expTotal}` (mirror maAcfrExtract.mjs's result shape). Anchor ONLY on the "Combined Statement of Revenues, Expenditures, and Changes in Fund Balances" title with "All Governmental Fund Types" within 3 lines; take label + FIRST numeric token per leaf row (parens = negative); capture "Total Revenues" and "Total Expenditures" printed totals for the tie. Handle sub-headers (Current:, Debt Service:) the way extract_gf.py does — propagate then clear at the next top-level line. Extend CT loaders: add FY1988–FY2001 to the SRC map (oldcafrpdfs URLs), route years < 2002 through pre34Extract with dataSource label "Connecticut State CAFR — General Fund (FY${fy} actual, pre-GASB-34 combined statement basis)". Extend WI loaders identically for FY2000–FY2001 (cached; URLs from 109-03 loadlog's DEBFCapitalFinance family — enumerate from the archive page if the cached copy needs re-downloading). Apply Phase-114 hardening to all four loader files while touching them. Dry-run all four loaders: every pre-34 year either prints a PASS tie against BOTH printed totals or is omitted from the map + logged; all modern years still tie unchanged.
  </action>
  <verify>
    <automated>node scripts/processCTAcfr.js --dry-run --fy 1995 2>&1 | grep -qi "PASS" && node scripts/processWIAcfr.js --dry-run --fy 2000 2>&1 | grep -qi "PASS" && node scripts/processCTAcfr.js --dry-run --fy 2024 2>&1 | grep -qi "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/pre34Extract.mjs exists, exports extractPre34GeneralFund, contains the "All Governmental Fund Types" anchor string
    - CT dry-run FY1995 and WI dry-run FY2000 tie; CT FY2024 (modern regression) still ties
    - Pre-34 dataSource label contains "pre-GASB-34 combined statement basis"; modern label unchanged
    - Full CT + WI dry-runs exit 0 with per-year dispositions printed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CT FY2006 OCR recovery</name>
  <files>scripts/processCTAcfr.js, scripts/processCTRevenueAcfr.js, _acfr-work/ct/ (gitignored OCR artifacts)</files>
  <read_first>
    - _acfr-work/ct/CT2005.txt and _acfr-work/ct/CT2007.txt (bracket the FY2006 statement location + give the expected modern-format row labels)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§4 OCR path)
  </read_first>
  <action>
    Locate the Governmental Funds statement pages in CT2006.pdf (use FY2005/FY2007 page positions as the search window; render candidate pages at low res to find the statement title visually via OCR text match). Render the statement pages with `pdftoppm -r 300 -png`, OCR with `"C:\Program Files\Tesseract-OCR\tesseract.exe" <img> stdout`, and transcribe the General Fund column (revenues + expenditures, all leaf rows + printed totals) into the CT loaders as an embedded FY2006 entry (OCR-transcribed years are embedded data, not runtime-parsed — the OCR output is not stable enough to re-parse on every run; comment the entry with the OCR provenance + page numbers). Tie gate: category sums must tie the printed totals (±TOL); if digits are ambiguous, re-render at 400dpi / --psm 6 and cross-check each column value against the row's Total column arithmetic where visible. FY2006 gets the normal GAAP basis label. If after escalation the transcription cannot tie, remove the FY2006 entry and document unrecoverable-with-evidence in the loadlog (OCR sample + which rows failed). Dry-run: CT loaders show FY2006 PASS tie (or the year is absent + logged).
  </action>
  <verify>
    <automated>node scripts/processCTAcfr.js --dry-run --fy 2006 2>&1 | grep -qiE "PASS|not a loadable" (PASS if recovered; loadable-years rejection if documented unrecoverable)</automated>
  </verify>
  <acceptance_criteria>
    - Either: CT FY2006 dry-run ties for both datasets AND the loader entry carries an OCR-provenance comment with page numbers
    - Or: the loadlog contains an "FY2006 unrecoverable" section with OCR evidence and the specific failure
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live-load per-FY, verify basis labels + modern-untouched + idempotency + 0 residue, loadlog + SUMMARY + commits</name>
  <files>treasury.budgets (CT + WI nodes), 115-02-CT-WI-LOADLOG.md, 115-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§6 never-overwrite contract)
  </read_first>
  <action>
    Capture pre-load baselines for CT and WI (row counts per dataset, FY list, CT FY2024 + WI FY2024 operating totals as spot-hashes). Live-load ONLY the new years per-`--fy` (CT pre-34 years + FY2006 if recovered; WI FY2000–2001), both datasets. Post-load selects: (a) all modern rows unchanged (counts + spot totals), (b) every deepened year present with data_source containing "pre-GASB-34 combined statement basis" (FY2006: "GAAP basis"), source_url/source_date stamped, (c) re-run one deepened FY per state = 0 net change, (d) 0 data_sources residue for ct-acfr-% and wi-acfr-%. Write `115-02-CT-WI-LOADLOG.md`: per-FY Load Disposition for CT FY1988–2001+2006 and WI FY2000–2001, WI pre-FY2000 out-of-scope note, OCR evidence, basis-label spot-check output, baseline-vs-post evidence. Commit loaders+extractor (feat) and loadlog (docs) atomically; write 115-02-SUMMARY.md.
  </action>
  <verify>
    <automated>grep -q "Load Disposition" .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-02-CT-WI-LOADLOG.md && git log --oneline --grep="115-02" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - DB: every deepened year present with the pre-GASB-34 basis label (FY2006 GAAP if recovered); CT FY2002–2025 and WI FY2002–2025 counts + spot totals identical before/after
    - Re-run of one deepened FY per state = 0 net change; 0 residue for both states' dataset_ids
    - Loadlog has a disposition row for every candidate year (CT FY1988–2001, FY2006; WI FY2000–2001) + the WI pre-FY2000 exclusion note
    - feat + docs commits exist; 115-02-SUMMARY.md exists with Self-Check
  </acceptance_criteria>
</task>

</tasks>

<verification>
- pre34Extract.mjs committed and anchored on "All Governmental Fund Types".
- CT/WI dry-runs exit 0 (pre-34 + modern years all tie or are logged holes).
- DB basis labels: pre-34 rows visibly distinct from GAAP rows on the same node.
- Modern rows untouched; idempotency + 0 residue proven; loadlog dispositions complete.
</verification>
