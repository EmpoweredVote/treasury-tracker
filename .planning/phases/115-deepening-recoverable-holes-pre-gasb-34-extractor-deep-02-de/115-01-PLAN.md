---
phase: 115-deepening-recoverable-holes-pre-gasb-34-extractor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processNJAcfr.js
  - scripts/processNJRevenueAcfr.js
  - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md
autonomous: true
requirements: [DEEP-03]

must_haves:
  truths:
    - "New Jersey's state node carries ACFR-sourced GF operating + revenue rows for recovered pre-FY2020 years (target FY2003±–FY2019, as deep as the GASB-34 Governmental Funds statement format holds), every recovered FY tying exactly (±TOL) to its printed General Fund column totals — or the year is documented unrecoverable in the gap log with the reason"
    - "The executor STOPS at NJ's GASB-34 boundary: any year whose statement is the pre-GASB-34 Combined-Statement format is NOT loaded (pre-34 NJ is out of scope — DEEP-04 names only CT/WI) and is logged as such"
    - "Units are verified per era against the statement's printed units note — NJ's modern ACFR is DOLLARS (UNITS=1); if a deep era is in thousands, that era gets an explicit per-year units override, and the tie gate + per-capita sanity (~$2k–$8k/person) proves the choice"
    - "Existing NJ FY2020–FY2025 rows are untouched: pre/post DB assertions show identical row counts and an identical spot-year total_budget"
    - "Idempotent never-overwrite with 0 data_sources residue: re-running one recovered FY produces 0 net change AND 0 rows remain in treasury.data_sources for nj-acfr dataset_ids"
    - "Every recovered row is GAAP-basis-labelled ('New Jersey State ACFR — General Fund (FY{fy} actual, GAAP basis)') with per-year source_url/source_date stamped"
  artifacts:
    - path: "scripts/processNJAcfr.js"
      provides: "NJ GF operating loader extended with a pre-FY2020 per-year URL map + era-aware extraction"
      contains: "fr.shtml"
    - path: "scripts/processNJRevenueAcfr.js"
      provides: "NJ GF revenue loader with the same pre-FY2020 extension"
      contains: "clampForRender"
    - path: ".planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md"
      provides: "Per-FY disposition (loaded / unrecoverable+reason / pre-34 boundary), units verification per era, idempotency + 0-residue + modern-rows-untouched evidence"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNJAcfr.js / processNJRevenueAcfr.js"
      to: "treasury.budgets (NJ state node)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "pre-FY2020 URL map"
      to: "nj.gov/treasury/omb/publications era-named PDFs"
      via: "URLs enumerated from the fr.shtml landing page (never derived blindly)"
      pattern: "nj.gov/treasury/omb"
---

<objective>
Deepen the **New Jersey** state node from FY2020–FY2025 (6 years) back through the pre-FY2020 archive (candidates FY2002–FY2019) by extending the existing runtime-extraction loaders `scripts/processNJAcfr.js` + `scripts/processNJRevenueAcfr.js` with a per-year URL map enumerated from `nj.gov/treasury/omb/fr.shtml`, downloading each year to gitignored `_acfr-work/nj/`, extracting the GENERAL FUND column of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, tying every year to its printed totals, and live-loading ONLY the new years per-`--fy` — stopping honestly at NJ's GASB-34 format boundary.

Output: NJ deepened as far as the modern statement format allows (each year tied or logged unrecoverable), modern FY2020–2025 rows provably untouched, `115-01-NJ-LOADLOG.md` with the full disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md
@.planning/milestones/v2.13-phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md

<interfaces>
From scripts/processNJAcfr.js (the file being extended — read fully first):
  - Runtime-extraction loader: downloads PDF → `pdftotext -table` → in-file parser → tie gate → RPC.
  - **UNITS = 1 (DOLLARS — the only non-thousands state).** Verify per deep era against the printed units note; per-capita sanity ~$2k–$8k/person (POPULATION = 9_288_994).
  - NJ_BASE = 'https://www.nj.gov/treasury/omb/publications'; current URL special-cases live near the top of the file.
  - Ephemeral data_sources lifecycle (Phase 111): delete-by-dataset_id → insert fresh → RPC per year → delete-by-id at end. Keep verbatim; dataset_ids 'nj-acfr-gf-operating' / 'nj-acfr-gf-revenue'.
  - dataSource(fy) = "New Jersey State ACFR — General Fund (FY${fy} actual, GAAP basis)"; source_date = `${fy}-06-30`.
  - These loaders predate Phase 114 hardening: while touching them, apply the same guards added in commits abb37f5/c9d9aaf/fba8977/97dfa1c (parseArgs strict, --fy value validation against the URL map, try/finally data_sources cleanup, surfaced select errors) — fix-while-touching only, no fleet sweep.
Known URL anchors (HEAD-probed 200 OK on 2026-07-03): `19fr/NJFR2019 Complete.pdf` (10.0MB), `18fr/FR 2018 Secured Final.pdf` (8.7MB), `10fr/pdf/fullfr2010.pdf` (12.1MB). Era pattern ~FY2002–2017: `{YY}fr/pdf/fullfr{YYYY}.pdf` — but ENUMERATE every year's exact URL from the fr.shtml landing page, never derive (project memory: recon URL details unreliable at load).
DB verification: mcp__supabase-local__execute_sql if available, else read-only node select scripts with repo .env credentials.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate pre-FY2020 URLs, download/extract, extend both NJ loaders, dry-run tie every recoverable year</name>
  <files>scripts/processNJAcfr.js, scripts/processNJRevenueAcfr.js, _acfr-work/nj/ (gitignored)</files>
  <read_first>
    - scripts/processNJAcfr.js (current parser, URL handling, tie gate, RPC + stamp)
    - scripts/processNJRevenueAcfr.js (revenue twin incl. clampForRender)
    - .planning/milestones/v2.13-phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md (deferred-history note, URL era patterns)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§5 NJ, §6 idempotency contract)
  </read_first>
  <action>
    Fetch `https://www.nj.gov/treasury/omb/fr.shtml` and enumerate the exact per-year PDF URL for every listed pre-FY2020 year (candidates FY2002–FY2019). Download each to `_acfr-work/nj/NJ{YYYY}.pdf` with the soft-404 guard (%PDF magic + >500KB). Run `pdftotext -table`. For each year, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances; if the year's statement is instead the pre-GASB-34 Combined-Statement format ("All Governmental Fund Types" columns), STOP the descent there — record the boundary year in the loadlog and do not load it or anything older. Verify the units note per era ("expressed in dollars" vs "(In Thousands)"); if an era is thousands, add a per-year UNITS override keyed off the URL map. Extend both loaders with the enumerated URL map (`SRC`-style per-year exact URLs) and any per-era parser adjustments needed for the older statement layouts (mirror the token-order + positional fallback discipline used by processCTAcfr.js). Apply the Phase-114 hardening pattern to both files while touching them (strict parseArgs, --fy value validation, try/finally data_sources cleanup, surfaced select errors). Dry-run both loaders across all newly mapped years: every recoverable year prints a PASS tie (±TOL) against BOTH Total revenues and Total expenditures for the General Fund column; years that cannot tie are OMITTED from the map (honest hole) and logged with the reason.
  </action>
  <verify>
    <automated>node scripts/processNJAcfr.js --dry-run 2>&1 | tail -5 (exit 0, no FY fails); node scripts/processNJRevenueAcfr.js --dry-run 2>&1 | tail -5 (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - scripts/processNJAcfr.js contains a per-year URL map covering the recovered pre-FY2020 years with exact fr.shtml-enumerated URLs
    - Both loaders --dry-run exit 0 with every mapped year printing a PASS tie
    - A `--fy` value not in the map exits 2 with the loadable-years message
    - The GASB-34 boundary year (first pre-34-format year encountered) is identified and recorded in a code comment + carried to the loadlog
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Capture pre-load baseline, live-load new years per-FY, verify modern rows untouched + idempotency + 0 residue</name>
  <files>treasury.budgets (NJ node), .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md</files>
  <read_first>
    - scripts/processNJAcfr.js (as extended in Task 1)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-RESEARCH.md (§6 never-overwrite contract)
  </read_first>
  <action>
    BEFORE loading: capture the NJ node's baseline via read-only select — row count per dataset_type, the FY list, and FY2024 operating total_budget as the spot-hash. Live-load ONLY the new pre-FY2020 years, one `--fy` at a time, for both operating and revenue. AFTER loading: re-select — (a) all FY2020–FY2025 rows unchanged (count + FY2024 spot total identical), (b) each new year present with the printed-total-derived total_budget, GAAP basis label, source_url + source_date stamped, (c) re-run ONE recovered FY (both loaders) and show 0 net change, (d) `treasury.data_sources` has 0 rows for `nj-acfr-%` dataset_ids, (e) Money In already enabled (revenue rows pre-existed — confirm new revenue years render into the same series). Write `115-01-NJ-LOADLOG.md`: per-FY Load Disposition table (loaded/unrecoverable+reason/boundary), units verification per era, baseline-vs-post evidence, idempotency + residue results.
  </action>
  <verify>
    <automated>grep -q "Load Disposition" .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md</automated>
  </verify>
  <acceptance_criteria>
    - DB select shows NJ rows for every recovered year in both datasets with data_source containing "GAAP basis"
    - FY2024 operating total_budget identical before/after (exact value recorded in loadlog)
    - Re-run of one recovered FY = 0 net change (loadlog shows the evidence)
    - data_sources residue count for nj-acfr-% = 0
    - Loadlog contains a per-FY disposition row for EVERY candidate year FY2002–FY2019 (loaded, unrecoverable+reason, or pre-34 boundary)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Commit loaders + loadlog, SUMMARY</name>
  <files>scripts/processNJAcfr.js, scripts/processNJRevenueAcfr.js, 115-01-NJ-LOADLOG.md, 115-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md
  </read_first>
  <action>
    Commit the loader extensions (feat) and the loadlog (docs) atomically per the executor commit protocol. Write 115-01-SUMMARY.md per the summary template, recording the recovered window, boundary year, any unrecoverable years with reasons, and the untouched-modern-rows evidence.
  </action>
  <verify>
    <automated>git log --oneline --grep="115-01" | head -3 (>=2 commits)</automated>
  </verify>
  <acceptance_criteria>
    - feat + docs commits exist for 115-01
    - 115-01-SUMMARY.md exists with Self-Check section
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Both NJ loaders --dry-run exit 0; every mapped year ties.
- DB: recovered years present + GAAP-labelled; FY2020–2025 untouched; 1 re-run FY = 0 net change; 0 nj-acfr-% data_sources residue.
- Loadlog dispositions cover all of FY2002–FY2019.
</verification>
