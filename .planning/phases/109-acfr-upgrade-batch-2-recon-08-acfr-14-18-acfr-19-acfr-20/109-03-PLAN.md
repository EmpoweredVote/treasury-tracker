---
phase: 109-acfr-upgrade-batch-2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processWIAcfr.js
  - scripts/processWIRevenueAcfr.js
  - .planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-03-WI-LOADLOG.md
autonomous: true
requirements: [ACFR-16, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Wisconsin state node (15fe5240-19d9-4fef-b785-d624b0a39a2a) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across the verified FY2019–FY2025 window PLUS every deep-history year (FY2000+, enumerated from the doa.wi.gov Financial-Reporting-Archive) that extracts + ties (D-01), every loaded FY tying exactly to the printed GENERAL FUND column total (D-04), every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "WI units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to 38,655,598K and FY2019 to 27,866,801K — extracted via the shared parser extractGovFundGeneralColumn (D-02) from the GENERAL FUND column ONLY (1st of General | Transportation | Nonmajor Governmental | Total), NOT a multi-column sum"
    - "The pre-existing NASBO operating rows (WI FY2023 $18,864M, FY2024 $22,280M) are REPLACED in place by the ACFR operating rows (same (15fe5240, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite (D-09): a second live run of a representative FY produces 0 net change; the 14 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (D-07) (WI ACFR GF ~1.74× NASBO — $14.4B Intergovernmental revenue, nearly all federal, inside the GAAP GF; the MA ~1.73× analog) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (D-06, ACFR-20); WI investment income positive in confirmed bookends, all attempted years checked at load"
    - "Money In auto-enables on WI once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processWIAcfr.js"
      provides: "WI GF spending-by-function loader (operating), parser-based, thousands×1000, tie-verified FY2019–FY2025 + deep-history attempts"
      contains: "treasury_sync_budget_tree"
      min_lines: 100
    - path: "scripts/processWIRevenueAcfr.js"
      provides: "WI GF revenue-by-source loader (revenue), parser-based, thousands×1000, tie-verified FY2019–FY2025 + deep-history attempts"
      contains: "clampForRender"
      min_lines: 100
    - path: ".planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-03-WI-LOADLOG.md"
      provides: "Per-FY load disposition incl. deep-history attempts, path-split URL record, NASBO-replacement, accept-relabel divergence, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processWIAcfr.js / processWIRevenueAcfr.js"
      to: "treasury.budgets (WI state node 15fe5240)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processWIAcfr.js / processWIRevenueAcfr.js"
      to: "scripts/maAcfrExtract.mjs"
      via: "import { extractGovFundGeneralColumn } — the 108-proven shared GF-column parser (D-02)"
      pattern: "extractGovFundGeneralColumn"
    - from: "SOURCES map (per-year URLs across the three doa.wi.gov path families)"
      to: "doa.wi.gov ACFR/CAFR PDFs"
      via: "explicit per-year URLs — /budget/ (FY2024–25, FY2018–2021), /budget/SCO/ (FY2022–23), /DEBFCapitalFinance/{YYYY}/ (FY2000–2017)"
      pattern: "doa.wi.gov"
---

<objective>
Upgrade the **Wisconsin** state node from NASBO operating-only to full State-ACFR GAAP by building two parser-based loaders on the NC mold — `scripts/processWIAcfr.js` (`operating`) and `scripts/processWIRevenueAcfr.js` (`revenue`) — downloading the WI ACFR/CAFR PDFs across the verified **FY2019–FY2025** window and then **pushing into the deep enumerable history (FY2000+)** from the doa.wi.gov Financial-Reporting-Archive into gitignored `_acfr-work/wi/`, extracting the **GENERAL FUND column** (1st of General | Transportation | Nonmajor Governmental | Total — the tranche's cleanest 3-column layout) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + the shared `extractGovFundGeneralColumn` parser, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**URL path split is the WI-specific trap:** `/budget/` (FY2024–25 `FY%20{YYYY}%20ACFR%20Final.pdf`, FY2018–2021 `CAFR{YYYY}.pdf`/`ACFR2021.pdf`), `/budget/SCO/` (FY2022–23), and `/DEBFCapitalFinance/{YYYY}/{YYYY}CAFR[_Linked].pdf` (FY2000–2017, per-year variants) — enumerate every URL explicitly from the archive page, no derived pattern. Deep years (FY2000–FY2017) load only if they extract + tie; failures become logged honest holes (D-01), never forced.

This is one of five independent per-state plans (D-08) — a failure or hold here blocks only WI, not the other four states.

Output: both WI loaders live-loaded across the verified window + every deep year that ties (holes logged per D-01/D-05), NASBO replaced in place, Money In auto-enabled; `109-03-WI-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- WI loaders mirror the NC loaders file-for-file (108's cleanest shared-parser reuse). -->
From scripts/processNCAcfr.js (spend-by-function analog → processWIAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Wisconsin', state='WI', entity_type='state' (.single()). Node id = 15fe5240-19d9-4fef-b785-d624b0a39a2a (assert match).
  - import { extractGovFundGeneralColumn } from './maAcfrExtract.mjs' — parse each FY's pdftotext -table text (D-02).
  - UNITS = 1_000 (thousands → ×1,000). Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands.
  - SOURCES = { [fy]: { url, date } } — explicit per-year URLs across the three path families; date=`${fy}-06-30`; dataSource(fy)="Wisconsin State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES/REVENUE maps + validate() exit-2 + buildTree ×UNITS + root {n:'Wisconsin General Fund Budget'} + RPC + source-stamp, as in NC.
  - data_sources.dataset_id='wi-acfr-gf-operating' / 'wi-acfr-gf-revenue'; names 'Wisconsin General Fund Operating Budget' / 'Wisconsin General Fund Revenue'; base_url=https://doa.wi.gov/Pages/StateFinances/Financial-Reporting-Archive.aspx.
  - flags: --dry-run, --fy YYYY.
From scripts/processNCRevenueAcfr.js (revenue analog → processWIRevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender + "(net loss — shown at 0)" + signed-net root (ACFR-20).
POPULATION = 5_893_718 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): WI has 2 NASBO operating rows only — FY2023 $18,864M, FY2024 $22,280M, data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$38.7B FY2025) is ~1.74× ($14.4B Intergovernmental — nearly all federal — inside GAAP GF, MA analog), accept-and-relabel per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH2-SOURCES.md WI Detail Block — USE these, but re-verify URLs at load (108 lesson). -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds (PDF page ~45 of FY2025 ACFR). NOT Statement of Activities, NOT budgetary comparison.
- GF column = 1st of (General | Transportation | Nonmajor Governmental | Total). Parser takes the FIRST numeric token — do NOT sum across columns.
- Units = thousands. FY-end June 30. Window: FY2019–FY2025 extraction-verified; FY2000+ enumerable from the archive page.
- Recon-confirmed explicit URLs (re-verify at load — three path families):
  - FY2025: https://doa.wi.gov/budget/FY%202025%20ACFR%20Final.pdf / FY2024: https://doa.wi.gov/budget/FY%202024%20ACFR%20Final.pdf
  - FY2023: https://doa.wi.gov/budget/SCO/FY%202023%20ACFR%20Final.pdf / FY2022: https://doa.wi.gov/budget/SCO/FY%202022%20ACFR.pdf
  - FY2021: https://doa.wi.gov/budget/ACFR2021.pdf / FY2020: https://doa.wi.gov/budget/CAFR2020.pdf / FY2019: https://doa.wi.gov/budget/CAFR2019.pdf / FY2018: https://doa.wi.gov/budget/CAFR2018.pdf
  - FY2017: https://doa.wi.gov/DEBFCapitalFinance/2017/2017_CAFR_Linked.pdf / FY2016: https://doa.wi.gov/DEBFCapitalFinance/2016/2016_CAFR_Linked.pdf
  - FY2000–FY2015: https://doa.wi.gov/DEBFCapitalFinance/{YYYY}/{YYYY}CAFR[_Linked].pdf (per-year variants; enumerate from archive page)
  - Archive: https://doa.wi.gov/Pages/StateFinances/Financial-Reporting-Archive.aspx
- Bookend ties (GENERAL FUND Total revenues, thousands): FY2025 = 38,655,598K (diff $0); FY2019 = 27,866,801K (diff $0).
- Investment and Interest Income positive in FY2025 (+$431,703K) and FY2019 (+$29,649K); check older years at load (zero-rate era FY2008–FY2012 near-zero but unlikely negative — P2 clamp safety net, ACFR-20).
- Scope vs NASBO ~1.74× — accept-and-relabel honestly (ACFR-19).
- Pre-FY2000: archive lists back to FY1995 but split across multiple files (4-section format) — OUT of scope (deferred).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both WI loaders (NC parser-based mold) + enumerate URLs across the three path families + download/extract FY2019–FY2025 then deep FY2000+ (GF column via shared parser) + dry-run tie</name>
  <files>scripts/processWIAcfr.js, scripts/processWIRevenueAcfr.js, _acfr-work/wi/</files>
  <read_first>
    - scripts/processNCAcfr.js (the exact parser-based spend-by-function template)
    - scripts/processNCRevenueAcfr.js (the exact parser-based revenue template incl. clampForRender)
    - scripts/maAcfrExtract.mjs (extractGovFundGeneralColumn — the shared GF-column parser, D-02)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md (WI Detail Block: three URL path families, bookend ties, deep-history note)
  </read_first>
  <action>
    Download each WI ACFR/CAFR to `_acfr-work/wi/WI{YYYY}.pdf` using the recon-confirmed explicit URLs for FY2018–FY2025 and the `/DEBFCapitalFinance/{YYYY}/` per-year variants for FY2000–FY2017 (enumerate the exact `[_Linked]` naming per year from the Financial-Reporting-Archive page — no derived pattern), with a soft-404 guard (%PDF magic + size >~500KB; reject HTML). If any URL 404s, re-enumerate from the archive page (load-time recon-correction expected). Work newest→oldest: verified FY2019–FY2025 first, then deep years FY2018 → FY2000 — each deep year that fails download/extraction/tie becomes a logged honest hole (D-01 guard), never a forced load. Pre-FY2000 (multi-file 4-section format) is OUT of scope. Run `pdftotext -table` per year, locate the Governmental Funds Statement of Rev/Exp/Changes, and feed the text to `extractGovFundGeneralColumn` to isolate the GENERAL FUND column (1st numeric token) for BOTH revenues and expenditures. DO NOT sum across columns. Copy processNCAcfr.js → scripts/processWIAcfr.js and processNCRevenueAcfr.js → scripts/processWIRevenueAcfr.js; set STATE_NAME='Wisconsin', STATE_ABBR='WI', POPULATION=5_893_718, UNITS=1_000, node id assert 15fe5240-19d9-4fef-b785-d624b0a39a2a. Build SOURCES with the explicit enumerated URLs (source_date=`${fy}-06-30`). Populate EXPENDITURES + REVENUE maps from the parser output (verbatim names, thousands), each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('wi-acfr-gf-operating' / 'wi-acfr-gf-revenue'); base_url=the doa.wi.gov archive page. Keep clampForRender in the revenue loader (ACFR-20). Add a header comment recording the accept-and-relabel scope note (WI ~1.74× NASBO — $14.4B federal intergovernmental inside GAAP GF, ACFR-19), the GF-column-only caution, and the three-path-family URL note. Cross-check FY2025 Total revenues = 38,655,598K and FY2019 = 27,866,801K before processing the rest. Run both loaders `--dry-run`; confirm every present FY PASS.
  </action>
  <verify>
    <automated>node scripts/processWIAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processWIRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '38,655,598,000' && node scripts/processWIRevenueAcfr.js --dry-run --fy 2019 2>&1 | grep -q '27,866,801,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/wi/` holds a real PDF (%PDF, >~500KB) per extractable FY; soft-404 HTML rejected.
    - `node scripts/processWIAcfr.js --dry-run` prints "validation: PASS" for every present FY; `processWIRevenueAcfr.js --dry-run --fy 2025` → 38,655,598,000; `--fy 2019` → 27,866,801,000.
    - Both loaders import extractGovFundGeneralColumn from './maAcfrExtract.mjs' (D-02).
    - SOURCES covers at minimum FY2019–FY2025 (7 explicit doa.wi.gov URLs across the correct path families) plus every deep year that tied; deep years that failed are ABSENT from SOURCES and logged.
    - Header comment records the GF-column-only caution and the ~1.74× accept-relabel note (ACFR-19); UNITS=1_000.
  </acceptance_criteria>
  <done>Both WI loaders exist on the NC parser-based mold with explicit URLs across the three path families, every present FY dry-run-ties to its printed GENERAL FUND total (GF column only) with the FY2025 38,655,598K / FY2019 27,866,801K bookends, deep-history attempts logged (loads + holes), and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load WI (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processWIAcfr.js, scripts/processWIRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processWIAcfr.js + scripts/processWIRevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; WI node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the WI node (15fe5240) and RECORD the pre-load NASBO operating rows (FY2023 $18,864M, FY2024 $22,280M) — never-overwrite baseline + accept-relabel comparator. Then for each WI FY present after Task 1, run `node scripts/processWIAcfr.js --fy <YYYY>` then `node scripts/processWIRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (15fe5240, fy, 'operating') for FY2023/FY2024; all other FYs operating + all revenue net-new. Each stamps source_url/source_date/data_source (GAAP label). Abort+log any FY failing the live tie; never force-write. Record loaded WI GF totals against pre-load NASBO totals to confirm the ~1.74× accept-relabel divergence (federal intergovernmental inside GAAP GF — ACFR-19).
  </action>
  <verify>
    <automated>echo "WI row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the WI node has, per loaded FY, an operating row "Wisconsin State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - WI FY2025 revenue row total = 38,655,598,000; FY2019 = 27,866,801,000.
    - WI FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (WI, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.74× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>Each transcribed WI FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO rows replaced in place (no duplicates); the accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-03-WI-LOADLOG.md</files>
  <read_first>
    - scripts/processWIAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. WI --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded WI FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) WI has NO remaining "NASBO" operating rows (one operating row per (WI, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — WI ≥1 revenue row; (e) cohort untouched (RECON-08): the 14 existing ACFR nodes + a sample un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 109-03-WI-LOADLOG.md: FYs loaded (verified window + deep-history results with the deepest year reached), FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than WI.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 109-03-WI-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of WI --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded WI FY has both operating + revenue rows, GAAP-labelled, non-null sources; WI has ZERO remaining "NASBO" operating rows; one operating row per (WI, fy).
    - WI ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 109-03-WI-LOADLOG.md has a finalized "Load Disposition" section incl. the deepest loaded year.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms WI fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, Money In enabled), honest holes absent, cohort unchanged; 109-03-WI-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doa.wi.gov PDF → parser extraction | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the WI node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-03-A | Tampering | multi-column sum — summing General + Transportation + Nonmajor overshoots the GF total | mitigate | shared parser takes ONLY the 1st numeric token (D-02); bookend ties (38,655,598K / 27,866,801K) gate before load; validate() exit-2 |
| T-109-03-B | Tampering | wrong statement (Statement of Activities / budgetary) | mitigate | Task 1 targets the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate |
| T-109-03-C | Tampering | wrong path family (/budget/ vs /budget/SCO/ vs /DEBFCapitalFinance/) → 404 or wrong-year PDF | mitigate | explicit per-year URLs enumerated from the archive page; %PDF magic + size guard; per-FY tie catches wrong-year content |
| T-109-03-D | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-109-03-E | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (WI, fy), zero NASBO |
| T-109-03-F | Info Disclosure | silently inflating WI (~1.74×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-109-03-G | Tampering | write to a state other than WI | mitigate | loader resolves only WI node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-109-03-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both WI loaders `--dry-run`: every present FY "validation: PASS"; GF column via shared parser only; UNITS=1_000.
- `processWIRevenueAcfr.js --dry-run --fy 2025` → 38,655,598,000; `--fy 2019` → 27,866,801,000.
- Deep-history attempts (FY2018 → FY2000) each resolved to loaded-and-tied OR logged hole — no forced loads.
- Live: WI node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run WI --fy 2025 → "Loaded 0 rows" (idempotent).
- WI ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.74×) recorded (ACFR-19).
- 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 109-03-WI-LOADLOG.md finalized.
</verification>

<success_criteria>
The Wisconsin state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across the verified FY2019–FY2025 window plus every deep-history year (FY2000+) that extracted + tied (honest holes logged), GF column via the shared parser only, thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place (RECON-08), the ~1.74× accept-relabel divergence recorded (ACFR-19), the P2 clamp wired (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands WI to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-03-SUMMARY.md` when done — record the WI FY window actually loaded (verified 7 yrs + deepest deep-history year reached), FYs omitted as honest holes, the two bookend tie confirmations, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
