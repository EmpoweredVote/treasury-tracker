---
phase: 87-enrichment-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ohioEnrichment87.mjs
  - scripts/loadOhioEnrichment87.mjs
  - scripts/loadOhioEnrichment87.test.mjs
autonomous: false
requirements: [OHENR-01]
must_haves:
  truths:
    - "data/ohioEnrichment87.mjs exports OHIO_ENRICHMENT (keyed by exact name_key → {plain_name, short_description, description, tags, confidence}) + EXPECTED_KEYS covering ALL live Ohio depth-0 keys (52: 35 operating + 17 revenue, the city+county+GAAP/CASH/MOD union); every description is concept-level, entity- AND state-neutral, bleed-safe (no locality names, no $ figures, no Ohio-specific facts)"
    - "scripts/loadOhioEnrichment87.mjs mirrors loadVAEnrichment82.mjs: derives the live worklist from the DB (OH city+county municipalities → budgets → budget_categories depth 0, operating+revenue), builds universal (municipality_id NULL) rows, and ABORTS if any live key is missing from the map (100% coverage gate, NO fallback)"
    - "Two pre-write guards abort the run: a $-figure leak guard and an OH-locality-name guard (against the live OH municipalities.name list, minus a common-English-word skip-set incl. 'ohio'); writes use DELETE-THEN-INSERT over the authored keys (NULLS DISTINCT index — upsert would duplicate universal rows)"
    - "--apply writes the universal rows to treasury.category_enrichment; a dry-run (no --apply) derives the worklist + asserts coverage + runs both guards with ZERO DB writes; the loader exports pure helpers (buildRows, findDollarLeaks, findLocalityLeaks) for offline tests"
    - "Offline node --test cases pass: buildRows over the 52 EXPECTED_KEYS yields 0 missing; a simulated live key not in the map is reported as missing (gate works); findDollarLeaks/findLocalityLeaks catch a seeded $-figure and a seeded locality name"
  artifacts:
    - path: "data/ohioEnrichment87.mjs"
      provides: "The hand-authored, state-neutral Ohio enrichment map (52 keys) + EXPECTED_KEYS coverage list"
      contains: "OHIO_ENRICHMENT"
    - path: "scripts/loadOhioEnrichment87.mjs"
      provides: "The Ohio enrichment loader — live-worklist coverage gate + leak guards + delete-then-insert universal writes"
      contains: "findLocalityLeaks"
    - path: "scripts/loadOhioEnrichment87.test.mjs"
      provides: "Offline tests for coverage gate + leak guards + buildRows"
      contains: "buildRows"
  key_links:
    - from: "The fixed live Ohio category vocabulary (52 depth-0 keys, city+county, all bases)"
      to: "universal (NULL municipality_id) category_enrichment rows the app/API join renders as plain-language descriptions"
      via: "OHIO_ENRICHMENT map → coverage gate + leak guards → delete-then-insert into treasury.category_enrichment"
      pattern: "loadOhioEnrichment87"
---

<objective>
Author standardized, bleed-safe, state-neutral plain-language enrichment for the FULL Ohio category vocabulary (52 live depth-0 keys: 35 operating + 17 revenue, spanning cities + counties + GAAP/CASH/MOD), at $0 inline (no paid API), and load it as universal `category_enrichment` rows — with a 100% coverage gate (abort on any unmapped live key) and delete-then-insert writes (NULLS-DISTINCT-safe). Delivers OHENR-01.

Mirror VA Phase 82 (`scripts/loadVAEnrichment82.mjs` + `data/vaEnrichment82.mjs` + `loadVAEnrichment82.test.mjs`) almost verbatim — swap the map contents for Ohio's vocabulary, the state filter to 'OH', and the locality-name skip-set. Ohio trees are FLAT, so all keys are depth-0 plain (no `parent|child` composites).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-enrichment-parity/87-CONTEXT.md
@scripts/loadVAEnrichment82.mjs
@scripts/loadVAEnrichment82.test.mjs
@data/vaEnrichment82.mjs

<interfaces>
<!-- The loader + map + test are a near-verbatim copy of the VA Phase 82 trio. -->

Production target: SUPABASE_URL=https://kxsdzaojfaibhuzmclfq.supabase.co (.env) + service key. Prefer mcp__supabase-local for read-only verification probes (feedback_supabase_migration_mcp).

category_enrichment row shape (confirmed): { name_key, municipality_id (NULL for universal), plain_name, short_description, description, tags (text[]), source ('ai'), confidence, evidence_summary, generated_at }. Index on (name_key, municipality_id) is NULLS DISTINCT → upsert would INSERT duplicate universal rows; delete-then-insert is the only idempotent overwrite-correct path.

Live worklist derivation (mirror VA loader, but Ohio is flat = depth 0 only): OH municipalities where entity_type IN ('city','county') → their budgets (operating, revenue) → budget_categories depth 0 → distinct lower(link_key || name), excluding 'total'. Paginate the 1000-row cap as the VA loader does.

The 52 expected keys are enumerated in 87-CONTEXT.md (revenue 17 + operating 35) with the synonym clusters to author consistently. EXPECTED_KEYS in the map must equal this set; the loader's coverage gate is the enforcement.

Locality-name guard: build from the live OH municipalities.name list; skip common-English-word names so generic civic text doesn't false-positive — at minimum {'ohio','marion','union','clinton','green','mentor','oberlin','independence','springfield','franklin','lebanon','milford','reading','amherst','seven hills','huber heights'} plus any others that are common words or the state-node name (verify against the actual OH name list at runtime and extend the skip-set as needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author data/ohioEnrichment87.mjs (52 state-neutral keys + EXPECTED_KEYS)</name>
  <files>data/ohioEnrichment87.mjs</files>
  <read_first>
    - data/vaEnrichment82.mjs (the exact export shape + the CONCEPTS voice + tags/confidence conventions)
    - .planning/phases/87-enrichment-parity/87-CONTEXT.md (the 52-key list + synonym clusters + D-02 neutrality rules)
  </read_first>
  <action>
    Create `data/ohioEnrichment87.mjs` exporting `SOURCE='ai'`, `OHIO_ENRICHMENT` (object keyed by each of the 52 exact name_keys → {plain_name, short_description, description, tags:[...], confidence}), and `EXPECTED_KEYS` (the 52-key array — the single source of truth for the coverage gate + tests). Author each description concept-level, entity- and state-neutral, bleed-safe: no locality names, no $ figures, no Ohio-specific facts; prefer "the local government" / "local government" over "the city". Author the synonym-cluster keys (87-CONTEXT) consistently — e.g. `police` and `security of persons and property police` describe the same concept; `payment in lieu of taxes`/`receipts in lieu of taxes`/`revenue in lieu of taxes` are the same; the various `other*` are honest catch-alls (NOT "Information Technology"). Each key gets its own row even when text is near-identical to a synonym (the coverage gate requires it).
  </action>
  <acceptance_criteria>
    - `node -e "import('./data/ohioEnrichment87.mjs').then(m=>console.log(m.EXPECTED_KEYS.length, Object.keys(m.OHIO_ENRICHMENT).length))"` prints `52 52`
    - Every EXPECTED_KEYS entry has a matching OHIO_ENRICHMENT row with non-empty plain_name + short_description + description + tags
    - No description contains a `$<digit>` figure or an Ohio locality name; voice is state-neutral ("local government", not "Ohio"/"the city of X")
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build scripts/loadOhioEnrichment87.mjs + offline tests (mirror VA loader)</name>
  <files>scripts/loadOhioEnrichment87.mjs, scripts/loadOhioEnrichment87.test.mjs</files>
  <read_first>
    - scripts/loadVAEnrichment82.mjs (buildRows/findDollarLeaks/findLocalityLeaks; the live-worklist derivation + coverage gate + delete-then-insert; the entry-guard idiom so tests can import pure helpers)
    - scripts/loadVAEnrichment82.test.mjs (the offline test structure)
  </read_first>
  <action>
    Create `scripts/loadOhioEnrichment87.mjs` mirroring loadVAEnrichment82.mjs: import OHIO_ENRICHMENT + EXPECTED_KEYS; export pure `buildRows(liveKeys)` (→ {rows, missing}), `findDollarLeaks(rows)`, `findLocalityLeaks(rows, localityNames)` (with the OH skip-set incl. 'ohio'); `main()` derives the live OH worklist (entity_type IN city,county; operating+revenue; depth 0; paginated), builds rows, prints the coverage report, ABORTS on any missing key / $-leak / locality-leak, and on `--apply` does delete-then-insert of the universal rows into treasury.category_enrichment. Guard `main()` with the entry-script idiom so tests import the pure helpers without hitting the DB. EVIDENCE string = an Ohio-parity analog of the VA one.

    Create `scripts/loadOhioEnrichment87.test.mjs` (node:test): buildRows over EXPECTED_KEYS → 0 missing, 52 rows; buildRows over [...EXPECTED_KEYS,'__fake_key__'] → missing=['__fake_key__'] (gate fires); findDollarLeaks catches a seeded row with "$5"; findLocalityLeaks catches a seeded row containing a sample OH city name (and does NOT flag a skip-set word like "Ohio"/"Marion").
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadOhioEnrichment87.test.mjs` exits 0, all cases pass
    - `node -e "import('./scripts/loadOhioEnrichment87.mjs').then(m=>console.log(typeof m.buildRows, typeof m.findDollarLeaks, typeof m.findLocalityLeaks))"` prints `function function function`
    - The loader contains a delete-then-insert path (no upsert) and a coverage-gate abort with no fallback
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Dry-run coverage + guards, then live --apply + verify</name>
  <files>scripts/loadOhioEnrichment87.mjs</files>
  <read_first>
    - scripts/loadOhioEnrichment87.mjs (dry-run vs --apply paths)
    - reference_category_enrichment_nulls_distinct (why delete-then-insert)
  </read_first>
  <action>
    With .env sourced: first run the DRY-RUN (`node scripts/loadOhioEnrichment87.mjs`) and confirm the live worklist derives the 52 keys, MISSING coverage = 0, $-leaks = 0, locality-leaks = 0, zero writes. If the live worklist surfaces a key not in the map, ADD it to data/ohioEnrichment87.mjs (state-neutral) and re-run — never fall back. Once clean, run `--apply` to delete-then-insert the universal rows. Then verify (mcp__supabase-local): for the 52 keys, count universal (municipality_id IS NULL) category_enrichment rows = 52 with no duplicate (name_key) among universal rows; spot-read 2-3 rows (e.g. 'police', 'sales taxes', 'human services') for state-neutral text; confirm an Ohio city page key (e.g. 'income taxes') and a county-only key (e.g. 'human services') both resolve to a universal row.
  </action>
  <acceptance_criteria>
    - Dry-run: 52 live keys, 0 missing, 0 $-leaks, 0 locality-leaks, 0 writes
    - After --apply: exactly 52 universal (NULL municipality_id) rows exist for the authored keys, with NO duplicate name_key among universal rows (delete-then-insert worked)
    - Spot-checked rows ('police', 'sales taxes', 'human services') have state-neutral, $-free, locality-free text
    - Re-running --apply is idempotent (still 52 universal rows, no duplicates)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadOhioEnrichment87.test.mjs` → all pass (coverage gate + leak guards + buildRows).
- Dry-run derives the 52 live Ohio keys with 0 missing / 0 leaks / 0 writes; --apply writes exactly 52 universal category_enrichment rows via delete-then-insert (no duplicates, NULLS-DISTINCT-safe).
- Every authored row is state-neutral, $-free, locality-free (bleed-safe by construction + enforced by guards); the coverage gate aborts on any unmapped live key (no fallback) — OHENR-01 met.
- Out of scope (noted): Ironton population backfill (F-1) and the county "Charges For Services" duplicate-column display quirk — neither blocks enrichment.
</verification>
