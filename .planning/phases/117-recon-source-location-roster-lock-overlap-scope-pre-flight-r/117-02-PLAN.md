---
phase: 117-recon-source-location-roster-lock-overlap-scope-pre-flight-r
plan: "117-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md
autonomous: true
requirements: [RECON-11]
user_setup: []

must_haves:
  truths:
    - "Each Batch-2 state (IA, KS, ME, MS, MT) gets a D-03 triage first: does a GAAP Governmental Funds ACFR with a splittable GENERAL FUND column even exist? — recorded before any deep URL-spelunking"
    - "For each passing Batch-2 state, the ACFR Governmental Funds Statement of Rev/Exp/Changes GENERAL FUND column is located, with the durable per-year URL pattern and the cleanly pdftotext -table-extractable clean window recorded"
    - "Each passing Batch-2 state's clean window is bookend-tie-confirmed (oldest + latest FY) with actual dollar GF Total revenues figures tied to the printed page total (D-05)"
    - "Each Batch-2 state has its four risk facts pinned (units / negative-category years / exact GF column header + statement / FY-end month) per D-08 — non-June FY-ends explicitly flagged"
    - "Each passing Batch-2 state's clean window covers FY2023 + FY2024 (recency floor D-07) OR is flagged as a blocker/decision for the load phase"
    - "Each Batch-2 state's ACFR GF scope vs its NASBO GF is compared with magnitude; where materially broader/narrower a UT/AL/LA/TX-style accept-relabel is recommended (D-09)"
    - "Any Batch-2 state that fails triage is recorded as a stay-NASBO-exception candidate (D-01) with an accept-relabel option surfaced and fed to the 117-06 NASBO-served list — NOT silently dropped (D-11)"
    - "Each passing Batch-2 state is mapped to the closest existing loader template (extract_gf.py + gen_state.py, or the process{...}Acfr.js family) by GF-statement layout"
    - "A per-state gap log records every Batch-2 FY that does not cleanly extract or lacks a durable URL, with reason (D-04 effort-budget stops, D-06 durable-URL exclusions)"
  artifacts:
    - path: ".planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md"
      provides: "Per-state D-03 triage + ACFR source location + bookend ties + four risk facts + recency-floor verdict + scope-vs-NASBO + loader mapping + gap log for IA, KS, ME, MS, MT"
      contains: "Iowa"
  key_links:
    - from: "117-BATCH2-SOURCES.md per-state block"
      to: "Phase 119 SOURCES-map + loader clone (extract_gf.py + gen_state.py)"
      via: "durable per-year URL pattern + GF column index + units + template mapping"
      pattern: "General Fund"
---

<objective>
Recon the Batch-2 states of the v2.15 final tail — **IA, KS, ME, MS, MT** — for the State-ACFR upgrade. For each state, FIRST run the D-03 triage, then — for states that pass — locate the ACFR Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances* → **GENERAL FUND column** (GAAP), find its durable per-year URL pattern, establish the cleanly `pdftotext -table`-extractable clean window, bookend-tie-confirm it (oldest + latest FY), pin the four risk facts (D-08), check the recency floor (D-07), compare ACFR GF scope vs NASBO (D-09), map the closest loader template, and gap-log every year that doesn't cleanly extract or lacks a durable URL. States that fail triage are recorded as stay-NASBO-exception candidates (D-01) — never silently dropped (D-11).

Purpose: Input contract for Phase 119 (Batch 2 loads: ACFR-38..42). **Documentation only — no DB writes, no NASBO mutations, no loader code, no frontend, $0 (`pdftotext` only, no AI).**
Output: `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md` — the per-state source doc mirroring the Phase 98/103/107/112 shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/milestones/v2.11-phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md
@.planning/followups/ca-acfr-reconciliation.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public state ACFR site → local disk | untrusted PDF/HTML fetched over the network into a local working dir |

Documentation-only, read-only recon: fetch public ACFR PDFs, run `pdftotext`, write local markdown. No DB, no secrets, no user input, no network writes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Spoofing/Tampering | a fetched "ACFR" PDF that is actually a soft-404 HTML page (HTTP 200 + HTML body) mistaken for a real document | mitigate | Filter every download by Content-Type `application/pdf` or size (>~1MB), never by HTTP status alone. Confirm the exact statement title + "General Fund" column header + units before trusting any figure. |
| T-117-02 | Tampering | extracting the wrong statement (Statement of Activities or a budgetary schedule) instead of the Governmental Funds GENERAL FUND column | mitigate | Confirm the statement title + General Fund column header; tie the extracted GF Total revenues to the printed total before recording (D-05/D-08.3). Budgetary-only → D-01/D-09 accept-relabel candidate, not GAAP. |
| T-117-03 | Tampering | recording a non-durable (Wayback/one-off) URL that will rot | mitigate | Reject non-durable-only years to the gap log (D-06); only a stable re-fetchable per-year URL pattern is recorded as loadable; a tie never overrides durability. |

No package installs → no supply-chain checkpoint needed.
</threat_model>

<tasks>

<task id="117-02-00" type="auto">
  <name>Task 0: Workspace + doc skeleton + D-03 triage for all five Batch-2 states</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-01..D-12 + canonical refs; D-03 triage-first + D-01 fill policy)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (the per-state SOURCES doc shape + table columns to mirror)
- .gitignore (confirm `_acfr-work/` is gitignored — it is, lines 108/133)
  </read_first>
  <action>
Create/reuse the gitignored `_acfr-work/{ia,ks,me,ms,mt}/` working directory. Confirm `pdftotext -v` runs (poppler); if absent, halt and note the install requirement. Scaffold `117-BATCH2-SOURCES.md` with the same 8-section skeleton used in Batch 1 (D-03 triage table; per-state source table; bookend tie-confirmations; four-risk-facts D-08; scope-vs-NASBO D-09; recency-floor D-07; consolidated gap log; loader-template mapping + Phase-119 load notes). Then perform the **D-03 triage** for IA, KS, ME, MS, MT: confirm whether each state's Comptroller/Auditor/Treasurer/Finance office publishes a GAAP ACFR with a Governmental Funds *Statement of Rev/Exp/Changes* that has a distinct General Fund column, vs a biennial/budgetary-only document or no clean GF split. Record each verdict (RECON = full recon in Tasks 1–2; STAY-NASBO-candidate = surface for D-01 with the available basis + accept-relabel option).
  </action>
  <verify>
    <automated>pdftotext -v 2>&1 | Select-String -Pattern "pdftotext" ; Test-Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md</automated>
  </verify>
  <acceptance_criteria>
- `pdftotext -v` runs (poppler available); if not, the plan halts with the install requirement recorded
- `_acfr-work/` exists and is gitignored (no PDFs committed)
- `117-BATCH2-SOURCES.md` exists with the 8-section skeleton
- The D-03 triage table has a verdict (RECON or STAY-NASBO-candidate) for all five of IA, KS, ME, MS, MT
  </acceptance_criteria>
  <done>Workspace ready, poppler confirmed, doc skeleton in place with all 8 sections, and every Batch-2 state has a D-03 triage verdict recorded.</done>
</task>

<task id="117-02-01" type="auto">
  <name>Task 1: Recon IA + KS + ME — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-04 effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-01 stay-NASBO fill policy)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- .planning/followups/ca-acfr-reconciliation.md (browser-download fallback for a 403/soft-404 — not a hard blocker)
- _acfr-work/extract_gf.py (the v2.14 loader-template GF extractor — confirms the located statement layout matches the clone target)
  </read_first>
  <action>
For each of **Iowa**, **Kansas**, and **Maine** whose D-03 verdict is RECON — each within the ~15-20 min/state D-04 budget: (1) Locate its ACFR Governmental Funds *Statement of Rev/Exp/Changes in Fund Balances* → **GENERAL FUND column** (GAAP — NOT the government-wide Statement of Activities, NOT a budgetary/forecast schedule). (2) Discover the durable per-year URL pattern; download the latest + oldest cleanly-reachable FY into `_acfr-work/{ia,ks,me}/`, filtering by Content-Type `application/pdf`/size (never HTTP status). (3) `pdftotext -table` both bookend PDFs; confirm GF "Total revenues" ties to the printed page total (D-05) with actual dollar figures. (4) Pin the four risk facts (D-08): units; any negative GF category (P2 clamp anticipation); exact GF column header + Gov-Funds-statement confirmation; FY-end month — **flag non-June FY-ends** (ME is one to watch; confirm each). (5) Recency floor (D-07): FY2023 + FY2024 covered, or flag a blocker. (6) Scope vs NASBO (D-09) with magnitude + accept-relabel recommendation where materially broader/narrower. (7) Map the closest loader template (`extract_gf.py` + `gen_state.py`, or a `process{...}Acfr.js` layout). Gap-log any FY that 404s/soft-404s/won't tie/lacks a durable URL; stop at the effort budget. If a state has no clean GAAP GF ACFR, disposition it STAY-NASBO per D-01 with the available basis + accept-relabel option and note for 117-06. Transcribe all findings into `117-BATCH2-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md -Pattern "Iowa|Kansas|Maine" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- IA, KS, ME each have a per-state block (statement/page, GF column header, units, FY-end month with non-June flagged, durable URL pattern, clean window) — OR a recorded STAY-NASBO-exception disposition per D-01
- Each RECON-verdict state has its oldest + latest FY bookend GF Total revenues recorded with a dollar figure + printed-total tie check (D-05)
- Each has all four risk facts pinned (D-08); recency-floor verdict recorded (D-07); scope-vs-NASBO magnitude + accept-relabel rec (D-09); loader-template mapping
- Every non-extracting / non-durable / non-tying FY is in the gap log with a reason; no soft-404 HTML mistaken for a PDF
  </acceptance_criteria>
  <done>IA, KS, ME fully reconned (or dispositioned STAY-NASBO per D-01) and transcribed into 117-BATCH2-SOURCES.md.</done>
</task>

<task id="117-02-02" type="auto">
  <name>Task 2: Recon MS + MT — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-04 effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-01 stay-NASBO fill policy)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- _acfr-work/gen_state.py (the v2.14 per-state loader generator — confirms the located statement maps onto the clone target)
  </read_first>
  <action>
For each of **Mississippi** and **Montana** whose D-03 verdict is RECON — each within the ~15-20 min/state D-04 budget, same procedure as Task 1 (locate Gov-Funds *Statement of Rev/Exp/Changes* → GENERAL FUND column; durable per-year URL pattern; download + `pdftotext -table` latest + oldest cleanly-reachable FY filtered by Content-Type/size; bookend-tie GF Total revenues to the printed total with dollar figures per D-05; pin four risk facts per D-08 incl. non-June FY-end flag; recency floor per D-07; scope vs NASBO per D-09; loader-template mapping; gap-log). **Montana note:** MT runs a biennial budget cycle — its GAAP ACFR is still annual, but confirm the Governmental Funds statement is annual (not a biennial-budget schedule) before recording; if only a biennial/budgetary basis is publishable, treat it as a D-01/D-09 accept-relabel candidate. If MS or MT has no clean GAAP GF ACFR, disposition STAY-NASBO per D-01 with basis + accept-relabel option, noted for 117-06. Transcribe all findings into the MS and MT rows of every table in `117-BATCH2-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md -Pattern "Mississippi|Montana" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- MS and MT each have a per-state block (statement/page, GF column header, units, FY-end month with non-June flagged, durable URL pattern, clean window) — OR a recorded STAY-NASBO-exception disposition per D-01
- Each RECON-verdict state has its oldest + latest FY bookend GF Total revenues recorded with a dollar figure + printed-total tie check (D-05)
- Each has all four risk facts pinned (D-08); recency-floor verdict recorded (D-07); scope-vs-NASBO magnitude + accept-relabel rec (D-09); loader-template mapping
- MT's annual-vs-biennial GF statement is confirmed (annual GAAP ACFR, not a biennial-budget schedule) or flagged as a D-01/D-09 accept-relabel candidate
- Every non-extracting / non-durable / non-tying FY for MS/MT is in the gap log with a reason
- Batch-2 SOURCES doc complete for all 5 states across all 8 sections
  </acceptance_criteria>
  <done>MS and MT fully reconned (or dispositioned STAY-NASBO per D-01); Batch-2 SOURCES doc complete for all 5 states across all 8 sections.</done>
</task>

</tasks>

<verification>
- `117-BATCH2-SOURCES.md` has a D-03 triage verdict + a complete per-state block for all 5 Batch-2 states (IA, KS, ME, MS, MT) across all 8 sections.
- Every RECON-verdict state's oldest + latest bookend FY GF Total revenues is recorded with a dollar figure and tied to its printed page total via `pdftotext -table`.
- Every state's four risk facts (units / negatives / exact GF column+statement / FY-end month with non-June flagged) are pinned; recency-floor (FY2023+FY2024) verdict recorded per state.
- Any state failing triage is recorded as a STAY-NASBO-exception candidate per D-01, never silently dropped (D-11); the gap log records every deferred/non-durable/non-tying FY with a reason; durable-URL-mandatory honored (D-06); no soft-404 HTML mistaken for a PDF.
- No DB writes, no loader edits, no NASBO mutations, no frontend changes; $0 spend.
</verification>

<success_criteria>
All 5 Batch-2 states triaged (D-03) and, where a clean GAAP GF ACFR exists, located + bookend-tie-confirmed with dollar figures, four risk facts pinned, recency floor checked, scope-vs-NASBO compared, loader template mapped, and gap log written; any no-clean-ACFR state surfaced as a stay-NASBO-exception candidate (D-01) — the Phase 119 input contract for IA/KS/ME/MS/MT is complete and documentation-only.
</success_criteria>

<output>
Create `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-02-SUMMARY.md` when done.
</output>
