---
phase: 117-recon-source-location-roster-lock-overlap-scope-pre-flight-r
plan: "117-06"
type: execute
wave: 2
depends_on: ["117-01", "117-02", "117-03", "117-04", "117-05"]
files_modified:
  - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
autonomous: true
requirements: [RECON-11]
user_setup: []

must_haves:
  truths:
    - "A read-only DB probe confirms which of the 21 roster states carry a non-NASBO / custom-source state node (D-10 overlap) and recommends in-place upgrade (no duplicate node, Phase 98 CA precedent) for any found, else records 'none found'"
    - "The read-only probe confirms the 29 existing ACFR nodes + all un-upgraded states are left undisturbed by Phases 118–122 (the untouched-nodes contract — noted, not executed)"
    - "The final roster is LOCKED, drawn only from the named 21 (D-11 no backfill); each state marked RECON (loadable with clean window) or STAY-NASBO-exception (D-01, with the basis available + accept-relabel option); the count floats down honestly with no substitution"
    - "The 118–121 batch split is CONFIRMED per D-12 (118 = AK/AR/DE/HI/ID; 119 = IA/KS/ME/MS/MT; 120 = NE/NV/NH/NM/ND; 121 = OK/RI/SD/VT/WV/WY), rebalanced around any STAY-NASBO-exception survivors"
    - "An explicit 'nodes remaining NASBO-served after this milestone' list is produced (all STAY-NASBO-exception states from D-01) — the input contract for Phase 123 (NASBORT-01)"
    - "Non-June FY-ends, GF-alone scope divergences (D-09 accept-relabel recs), and units traps are rolled up cross-batch into an Open Risks section for the load phases"
    - "117-RECON.md consolidates the four batch SOURCES docs + the deepening doc into the decision-ready Phase 118–123 handoff mirroring the Phase 98/103/107 RECON shape, with $0 spend and no-DB-write confirmed"
  artifacts:
    - path: ".planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md"
      provides: "Consolidated recon handoff: locked roster, confirmed batch split, overlap resolutions, untouched-nodes contract, NASBO-served list for Phase 123, per-state summary + loader mapping, DEEP-05 deepening summary, open risks"
      contains: "Roster Lock"
  key_links:
    - from: "117-RECON.md locked roster + confirmed batch split"
      to: "Phases 118/119/120/121 loads"
      via: "per-state window + template mapping + STAY-NASBO dispositions"
      pattern: "Batch 1|Batch 2|Batch 3|Batch 4"
    - from: "117-RECON.md NASBO-served list (D-01)"
      to: "Phase 123 (NASBORT-01)"
      via: "explicit list of nodes still rendering NASBO after the milestone"
      pattern: "NASBO-served"
---

<objective>
Close Phase 117 by resolving prior-load overlaps (D-10), locking the final roster (D-11), confirming the batch split (D-12), producing the **"nodes remaining NASBO-served after this milestone" list** (D-01 — the Phase 123 input contract), and consolidating the four batch SOURCES docs + the deepening doc into the decision-ready Phase 118–123 handoff. Uses a **read-only** DB probe to confirm which roster states carry a non-NASBO custom-source node and to confirm the 29 existing ACFR nodes + un-upgraded states are left undisturbed.

Purpose: The locked roster + confirmed batch split + NASBO-served list + overlap resolutions are the final input contract Phases 118–123 inherit. **Documentation + read-only DB queries only — no DB writes, no NASBO mutations, no loader code, no frontend, $0.**
Output: `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md` — the consolidated handoff mirroring the Phase 98/103/107 RECON shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase (read-only) → recon doc | read-only `execute_sql` results transcribed into local markdown |

Documentation + read-only-query plan: run read-only `SELECT` probes against `treasury.municipalities` / `treasury.budgets` / `treasury.data_sources` and write local markdown. **No DB writes, no schema changes, no NASBO mutations, no secrets, no frontend.**

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-08 | Tampering | an accidental non-SELECT statement mutates the DB during the overlap probe | mitigate | Use only read-only `SELECT` queries via the Supabase read path; no INSERT/UPDATE/DELETE/DDL in this plan; the whole phase is documentation-only per the CONTEXT boundary. |
| T-117-09 | Information disclosure / Repudiation | mis-identifying which nodes are ACFR vs NASBO leads Phases 118–122 to disturb one of the 29 existing ACFR nodes | mitigate | Enumerate the 29 existing ACFR nodes explicitly from the DB and record the untouched-nodes contract; confirm the 21 roster states + the 4 DEEP-05 nodes are the only nodes touched. |
| T-117-10 | Repudiation | the NASBO-served list omits a STAY-NASBO-exception state, so Phase 123 demotes NASBO out from under a live node, blanking it | mitigate | Build the NASBO-served list directly from every batch doc's STAY-NASBO-exception dispositions (D-01); cross-check the count against roster-minus-loadable; each exception carries its honest basis label so it keeps rendering after NASBO demotion. |

No package installs → no supply-chain checkpoint needed.
</threat_model>

<tasks>

<task id="117-06-01" type="auto">
  <name>Task 1: Read-only overlap probe (D-10) + untouched-nodes contract</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-10 overlaps = flag + plan not execute; the 29 existing ACFR nodes + 901 rows / 0 anomalies at v2.14 close)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (the read-only overlap-probe method + untouched-nodes-contract shape to reuse)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (and 117-BATCH2/3/4-SOURCES.md — each batch's per-state dispositions to consolidate)
  </read_first>
  <action>
Run **read-only** `SELECT` probes (no writes) via the Supabase read path against `treasury.municipalities` + `treasury.budgets` + `treasury.data_sources` for the 21 roster states, mirroring the 107-RECON dual-node check. For EACH roster state's state node: which `budgets` rows exist (dataset_type / fiscal_year / total_budget / data_source), and which `data_sources` metadata rows exist (dataset_id / api_type / dataset_type / fiscal_years). Write a `## Overlap Resolution (D-10)` section into `117-RECON.md`: (1) for any roster state carrying a **pre-existing custom-source node** (a `data_sources` row whose `api_type` is not the NASBO type, or a non-NASBO `data_source` string on its budgets rows), recommend the **in-place upgrade** (upgrade the single existing state node, no duplicate node — Phase 98 CA precedent) with the concrete load-phase steps; else record "none found — all 21 roster states are NASBO-only, clean in-place replace." (2) Enumerate the **29 existing ACFR nodes** from the DB and write the **untouched-nodes contract**: Phases 118–122 touch ONLY the 21 roster states + the 4 DEEP-05 nodes (CA/NY/FL/TX, extending their own windows — no new nodes); the 29 existing ACFR nodes + all un-upgraded states stay undisturbed; idempotent never-overwrite; a re-run is a no-op. (3) Confirm the probe was read-only (SELECT only).
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md -Pattern "Overlap Resolution|untouched-nodes|in-place upgrade|none found" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- `117-RECON.md` has an `## Overlap Resolution (D-10)` section: any roster state with a pre-existing custom-source node gets an in-place-upgrade recommendation (no duplicate node) with concrete steps, or "none found" is recorded — backed by the read-only DB probe
- The 29 existing ACFR nodes are enumerated from the DB and the untouched-nodes contract is written (only the 21 roster states + 4 DEEP-05 nodes touched; everything else undisturbed)
- Only read-only SELECT queries were run — no DB writes
  </acceptance_criteria>
  <done>Overlap resolution complete: any custom-source roster node flagged for in-place upgrade (or none found), and the untouched-nodes contract recorded — all from a read-only probe.</done>
</task>

<task id="117-06-02" type="auto">
  <name>Task 2: Lock roster + confirm batch split + NASBO-served list + consolidated handoff</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-01 fill policy + NASBO-served list, D-11 no-backfill, D-12 batch split, D-07 recency floor, D-09 scope divergence)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (AK/AR/DE/HI/ID findings + dispositions + gap logs)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (IA/KS/ME/MS/MT)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH3-SOURCES.md (NE/NV/NH/NM/ND)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (OK/RI/SD/VT/WV/WY)
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md (CA/NY/FL/TX deepening dispositions)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (the consolidated RECON handoff shape to mirror)
  </read_first>
  <action>
Consolidate the four batch SOURCES docs + the deepening doc into the decision-ready Phase 118–123 handoff, mirroring the `107-RECON.md` shape. Write these sections into `117-RECON.md`: (1) `## Roster Lock (D-11)` — for each of the 21 named states decide **RECON** (clean GAAP GF ACFR located, bookend-tied, clears the recency floor D-07) or **STAY-NASBO-exception** (no clean GAAP GF ACFR, or fails the recency floor unrecoverably, or only a broader/budgetary basis available — carrying the D-01 accept-relabel option and its honest basis label). Draw ONLY from the named 21; no backfill to a 22nd state; the count floats down honestly. Record as a table (state | RECON / STAY-NASBO-exception | clean window | basis | reason if exception). (2) `## Batch Split Confirmation (D-12)` — confirm 118 = AK/AR/DE/HI/ID; 119 = IA/KS/ME/MS/MT; 120 = NE/NV/NH/NM/ND; 121 = OK/RI/SD/VT/WV/WY; rebalance around any STAY-NASBO-exception survivors and record the final per-batch assignment. (3) `## Per-state summary + loader mapping` — a consolidated table (state | statement/GF column | units | FY-end | durable window | latest tie | old-end tie | scope-vs-NASBO magnitude + accept-relabel rec | closest loader template) pulled from the four batch docs. (4) `## DEEP-05 deepening summary` — pull the four targets' added-FY depth + URL patterns + pre-GASB-34 flags from 117-DEEPEN-SOURCES.md for Phase 122. (5) `## Nodes remaining NASBO-served after this milestone (D-01 → Phase 123)` — the explicit list of every STAY-NASBO-exception state with its basis label, so Phase 123 (NASBORT-01) demotes NASBO to fallback-only for served nodes while keeping these exception nodes rendering NASBO under an honest label; if the list is empty, state "all 50 states on ACFR — NASBO serves no live node." (6) `## NASBO-replace rule` — restate: ACFR rows replace NASBO operating per state-FY keyed on (muni,fy,dataset) idempotent never-overwrite; revenue is a pure insert (new dataset); the 29 existing ACFR nodes + un-upgraded states undisturbed. (7) `## Open Risks for 118–123` — roll up cross-batch: non-June FY-ends, scope-relabel confirmations (D-09), negative-category P2-clamp anticipations (D-08.2), units traps (NY-style ×millions), soft-404 cautions, pre-GASB-34 flags, and any recency-floor blockers. (8) `## Success-criteria coverage` — map back to Phase 117's 5 ROADMAP success criteria + RECON-11. Confirm $0 spend + no-DB-write at the top of the doc.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md -Pattern "Roster Lock|Batch Split Confirmation|NASBO-served|NASBO-replace|Open Risks|Success-criteria" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- `117-RECON.md` has a locked-roster table (each of the 21 marked RECON or STAY-NASBO-exception with clean window / basis / reason) honoring D-11 no-backfill + D-07 recency floor + D-01 fill policy
- The 118–121 batch split is confirmed per D-12 (118 AK/AR/DE/HI/ID; 119 IA/KS/ME/MS/MT; 120 NE/NV/NH/NM/ND; 121 OK/RI/SD/VT/WV/WY), rebalanced around exceptions
- A consolidated per-state summary + loader-template mapping table + a DEEP-05 deepening summary are present, pulled from the five recon docs
- The **NASBO-served list (D-01)** is written explicitly for Phase 123 (or "all 50 on ACFR" if empty); the NASBO-replace rule, open risks, and success-criteria coverage are recorded
- Top of doc confirms $0 spend + no DB writes; the doc mirrors the 107-RECON handoff shape
  </acceptance_criteria>
  <done>Roster + batch split locked, NASBO-served list produced for Phase 123, and the consolidated 117-RECON.md handoff complete and decision-ready for Phases 118–123; Phase 117's 5 success criteria + RECON-11 covered; documentation-only.</done>
</task>

</tasks>

<verification>
- `117-RECON.md` resolves D-10 overlaps (in-place upgrade for any custom-source roster node, or "none found") from a read-only DB probe, and records the untouched-nodes contract for the 29 existing ACFR nodes + un-upgraded states.
- The final roster is locked (RECON / STAY-NASBO-exception per state, D-11 no-backfill, D-07 recency floor, D-01 fill policy) and the 118–121 batch split is confirmed per D-12.
- The explicit NASBO-served list (D-01) is produced as the Phase 123 input contract (or "all 50 on ACFR" if no exceptions).
- The consolidated handoff mirrors the 107-RECON shape: per-state summary + loader mapping, DEEP-05 deepening summary, NASBO-replace rule, open risks (non-June FY-end / scope-relabel / P2-clamp / units / soft-404 / pre-GASB-34), success-criteria coverage.
- No DB writes (read-only SELECT only), no loader edits, no NASBO mutations, no frontend changes; $0 spend.
</verification>

<success_criteria>
Phase 117 is closed: overlaps resolved on paper, roster + batch split locked, the NASBO-served list produced for Phase 123, and a decision-ready 117-RECON.md handoff produced for Phases 118–123 — all documentation-only with a read-only DB probe, $0 spend.
</success_criteria>

<output>
Create `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-06-SUMMARY.md` when done.
</output>
