---
phase: 117-recon-source-location-roster-lock-overlap-scope-pre-flight-r
plan: "117-04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md
autonomous: true
requirements: [RECON-11]
user_setup: []

must_haves:
  truths:
    - "Each Batch-4 state (OK, RI, SD, VT, WV, WY) gets a D-03 triage first: does a GAAP Governmental Funds ACFR with a splittable GENERAL FUND column even exist? — recorded before any deep URL-spelunking"
    - "OK reuses its v2.14-preserved recon (git show v2.14:.../112-...) — its located statement, durable URL pattern, units, FY-end, and bookend ties are re-verified (URLs still resolve; latest FY still current) rather than re-spelunked from scratch"
    - "For each passing Batch-4 state, the ACFR Governmental Funds Statement of Rev/Exp/Changes GENERAL FUND column is located, with the durable per-year URL pattern and the cleanly pdftotext -table-extractable clean window recorded"
    - "Each passing Batch-4 state's clean window is bookend-tie-confirmed (oldest + latest FY) with actual dollar GF Total revenues figures tied to the printed page total (D-05)"
    - "Each Batch-4 state has its four risk facts pinned (units / negative-category years / exact GF column header + statement / FY-end month) per D-08 — non-June FY-ends explicitly flagged"
    - "Each passing Batch-4 state's clean window covers FY2023 + FY2024 (recency floor D-07) OR is flagged as a blocker/decision for the load phase"
    - "Each Batch-4 state's ACFR GF scope vs its NASBO GF is compared with magnitude; where materially broader/narrower a UT/AL/LA/TX-style accept-relabel is recommended (D-09)"
    - "Any Batch-4 state that fails triage is recorded as a stay-NASBO-exception candidate (D-01) with an accept-relabel option surfaced and fed to the 117-06 NASBO-served list — NOT silently dropped (D-11)"
    - "Each passing Batch-4 state is mapped to the closest existing loader template (extract_gf.py + gen_state.py, or the process{...}Acfr.js family) by GF-statement layout"
    - "A per-state gap log records every Batch-4 FY that does not cleanly extract or lacks a durable URL, with reason (D-04 effort-budget stops, D-06 durable-URL exclusions)"
  artifacts:
    - path: ".planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md"
      provides: "Per-state D-03 triage + ACFR source location + bookend ties + four risk facts + recency-floor verdict + scope-vs-NASBO + loader mapping + gap log for OK (reused/verified), RI, SD, VT, WV, WY"
      contains: "Oklahoma"
  key_links:
    - from: "117-BATCH4-SOURCES.md per-state block"
      to: "Phase 121 SOURCES-map + loader clone (extract_gf.py + gen_state.py)"
      via: "durable per-year URL pattern + GF column index + units + template mapping"
      pattern: "General Fund"
---

<objective>
Recon the Batch-4 states of the v2.15 final tail — **OK, RI, SD, VT, WV, WY** (the last 6, completing all 50) — for the State-ACFR upgrade. **OK reuses its v2.14-preserved recon** (from the Phase 112 docs at git tag `v2.14`); the other five are reconned fresh: D-03 triage first, then — for states that pass — locate the ACFR Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances* → **GENERAL FUND column** (GAAP), find its durable per-year URL pattern, establish the cleanly `pdftotext -table`-extractable clean window, bookend-tie-confirm it (oldest + latest FY), pin the four risk facts (D-08), check the recency floor (D-07), compare ACFR GF scope vs NASBO (D-09), map the closest loader template, and gap-log. States that fail triage are recorded as stay-NASBO-exception candidates (D-01) — never silently dropped (D-11).

Purpose: Input contract for Phase 121 (Batch 4 loads: ACFR-48..53 — the phase that brings all 50 states onto ACFR). **Documentation only — no DB writes, no NASBO mutations, no loader code, no frontend, $0 (`pdftotext` only, no AI).**
Output: `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md` — the per-state source doc mirroring the Phase 98/103/107/112 shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/milestones/v2.11-phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md
@.planning/followups/ca-acfr-reconciliation.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public state ACFR site → local disk | untrusted PDF/HTML fetched over the network into a local working dir |
| git tag v2.14 (preserved OK recon) → this doc | prior recon facts transcribed forward without re-verification if not re-checked |

Documentation-only, read-only recon: fetch public ACFR PDFs, run `pdftotext`, read a preserved recon doc via `git show`, write local markdown. No DB, no secrets, no user input, no network writes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-01 | Spoofing/Tampering | a fetched "ACFR" PDF that is actually a soft-404 HTML page mistaken for a real document | mitigate | Filter every download by Content-Type `application/pdf` or size (>~1MB), never by HTTP status alone. Confirm statement title + "General Fund" column header + units before trusting any figure. |
| T-117-02 | Tampering | extracting the wrong statement (Statement of Activities or a budgetary schedule) instead of the Governmental Funds GENERAL FUND column | mitigate | Confirm the statement title + General Fund column header; tie extracted GF Total revenues to the printed total (D-05/D-08.3). Budgetary-only → D-01/D-09 accept-relabel candidate. |
| T-117-04 | Repudiation/Staleness | OK's preserved v2.14 recon URLs have rotted or its "latest FY" is now stale, so a reused fact is wrong at Phase-121 load | mitigate | Re-verify OK's durable URL pattern actually resolves today (Content-Type/size filter) and re-confirm its latest-FY / recency-floor status; do not trust the preserved doc blind (CONTEXT canonical-ref instruction). |

No package installs → no supply-chain checkpoint needed.
</threat_model>

<tasks>

<task id="117-04-00" type="auto">
  <name>Task 0: Workspace + doc skeleton + retrieve OK's preserved recon + D-03 triage for the five fresh states</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-01..D-12 + canonical refs; OK-reuse instruction; D-03 triage-first + D-01 fill policy)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (the per-state SOURCES doc shape + table columns to mirror)
- .gitignore (confirm `_acfr-work/` is gitignored — it is, lines 108/133)
  </read_first>
  <action>
Create/reuse the gitignored `_acfr-work/{ok,ri,sd,vt,wv,wy}/` working directory. Confirm `pdftotext -v` runs (poppler); if absent, halt and note the install requirement. **Retrieve OK's preserved v2.14 recon:** run `git show v2.14:.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md` (and, if OK's block is elsewhere, `git show v2.14:.../112-RECON.md` / `112-BATCH1-SOURCES.md`) to recover the OK located statement, durable per-year URL pattern, units, FY-end, bookend ties, scope-vs-NASBO, and loader mapping — those files are NOT in the working tree, only at the tag. Scaffold `117-BATCH4-SOURCES.md` with the same 8-section skeleton used in Batch 1 (D-03 triage table; per-state source table; bookend tie-confirmations; four-risk-facts D-08; scope-vs-NASBO D-09; recency-floor D-07; consolidated gap log; loader-template mapping + Phase-121 load notes), and pre-fill OK's rows from the preserved recon (marked "reused v2.14 — pending re-verify in Task 1"). Then perform the **D-03 triage** for the five fresh states (RI, SD, VT, WV, WY): confirm whether each publishes a GAAP ACFR with a Governmental Funds *Statement of Rev/Exp/Changes* with a distinct General Fund column, vs a biennial/budgetary-only document or no clean GF split. Record each verdict (RECON / STAY-NASBO-candidate). Mark OK as RECON (reuse) pending Task-1 re-verify.
  </action>
  <verify>
    <automated>pdftotext -v 2>&1 | Select-String -Pattern "pdftotext" ; git show v2.14:.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md 2>&1 | Select-String -Pattern "Oklahoma|OK " | Measure-Object | Select-Object -ExpandProperty Count ; Test-Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md</automated>
  </verify>
  <acceptance_criteria>
- `pdftotext -v` runs (poppler available); if not, the plan halts with the install requirement recorded
- `_acfr-work/` exists and is gitignored (no PDFs committed)
- OK's preserved v2.14 recon is retrieved via `git show v2.14:...` and pre-filled into 117-BATCH4-SOURCES.md marked "reused — pending re-verify"
- `117-BATCH4-SOURCES.md` exists with the 8-section skeleton
- The D-03 triage table has a verdict (RECON or STAY-NASBO-candidate) for all six of OK, RI, SD, VT, WV, WY
  </acceptance_criteria>
  <done>Workspace ready, poppler confirmed, OK's v2.14 recon recovered from the tag and pre-filled, doc skeleton with all 8 sections in place, and every Batch-4 state has a D-03 triage verdict.</done>
</task>

<task id="117-04-01" type="auto">
  <name>Task 1: Re-verify OK (reuse) + recon RI + SD — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-04 effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-01 stay-NASBO fill policy, OK-reuse instruction)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- .planning/followups/ca-acfr-reconciliation.md (browser-download fallback for a 403/soft-404 — not a hard blocker)
- _acfr-work/extract_gf.py (the v2.14 loader-template GF extractor — confirms the located statement layout matches the clone target)
  </read_first>
  <action>
**Oklahoma (reuse-verify, NOT re-spelunk):** confirm OK's preserved v2.14 durable per-year URL pattern still resolves today (fetch the latest FY and the oldest FY of its recorded window, filtering by Content-Type `application/pdf`/size), re-confirm the four risk facts (units / negatives / GF column header + Gov-Funds-statement / FY-end month) still hold, and re-check the recency floor (D-07) — has a newer FY published since v2.14 that should extend the window? Update OK's rows from "reused — pending re-verify" to verified (or flag any rotted URL / stale latest-FY in the gap log). **Rhode Island** and **South Dakota** (fresh, D-03 verdict RECON) — each within the ~15-20 min/state D-04 budget: locate the Gov-Funds *Statement of Rev/Exp/Changes* → GENERAL FUND column (GAAP); discover the durable per-year URL pattern; download + `pdftotext -table` the latest + oldest cleanly-reachable FY (filtered by Content-Type/size); bookend-tie GF Total revenues to the printed total with dollar figures (D-05); pin the four risk facts (D-08) incl. a non-June FY-end flag; check the recency floor (D-07); compare scope vs NASBO (D-09); map the loader template; gap-log every non-extracting/non-durable/non-tying FY. If RI or SD has no clean GAAP GF ACFR, disposition STAY-NASBO per D-01. Transcribe all findings into `117-BATCH4-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md -Pattern "Oklahoma|Rhode Island|South Dakota" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- OK's reused v2.14 recon is re-verified: durable URL pattern resolves today (Content-Type/size confirmed), four risk facts re-confirmed, recency floor re-checked (any newer FY noted), any rotted URL / stale latest-FY flagged in the gap log
- RI and SD each have a per-state block (statement/page, GF column header, units, FY-end month with non-June flagged, durable URL pattern, clean window) — OR a recorded STAY-NASBO-exception disposition per D-01
- Each RECON-verdict state (OK/RI/SD) has its oldest + latest FY bookend GF Total revenues recorded with a dollar figure + printed-total tie check (D-05)
- Each has all four risk facts pinned (D-08); recency-floor verdict recorded (D-07); scope-vs-NASBO magnitude + accept-relabel rec (D-09); loader-template mapping
- Every non-extracting / non-durable / non-tying FY is in the gap log with a reason; no soft-404 HTML mistaken for a PDF
  </acceptance_criteria>
  <done>OK re-verified (or gap-flagged), RI and SD fully reconned (or dispositioned STAY-NASBO per D-01) and transcribed into 117-BATCH4-SOURCES.md.</done>
</task>

<task id="117-04-02" type="auto">
  <name>Task 2: Recon VT + WV + WY — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-04 effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-01 stay-NASBO fill policy)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- _acfr-work/gen_state.py (the v2.14 per-state loader generator — confirms the located statement maps onto the clone target)
  </read_first>
  <action>
For each of **Vermont**, **West Virginia**, and **Wyoming** whose D-03 verdict is RECON — each within the ~15-20 min/state D-04 budget, same procedure as Task 1's fresh states (locate Gov-Funds *Statement of Rev/Exp/Changes* → GENERAL FUND column; durable per-year URL pattern; download + `pdftotext -table` latest + oldest cleanly-reachable FY filtered by Content-Type/size; bookend-tie GF Total revenues to the printed total with dollar figures per D-05; pin four risk facts per D-08 incl. non-June FY-end flag; recency floor per D-07; scope vs NASBO per D-09; loader-template mapping; gap-log). **Wyoming note:** WY runs a biennial budget and is one of the smallest GFs — a leading STAY-NASBO / accept-relabel candidate; confirm an annual GAAP ACFR with an annual Governmental Funds statement exists before recording, else disposition per D-01/D-09. If VT, WV, or WY has no clean GAAP GF ACFR, disposition STAY-NASBO per D-01 with basis + accept-relabel option, noted for 117-06. Transcribe all findings into the VT, WV, WY rows of every table in `117-BATCH4-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md -Pattern "Vermont|West Virginia|Wyoming" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- VT, WV, WY each have a per-state block (statement/page, GF column header, units, FY-end month with non-June flagged, durable URL pattern, clean window) — OR a recorded STAY-NASBO-exception disposition per D-01
- Each RECON-verdict state has its oldest + latest FY bookend GF Total revenues recorded with a dollar figure + printed-total tie check (D-05)
- Each has all four risk facts pinned (D-08); recency-floor verdict recorded (D-07); scope-vs-NASBO magnitude + accept-relabel rec (D-09); loader-template mapping
- WY's annual-vs-biennial GF statement is confirmed (annual GAAP ACFR) or flagged as a D-01/D-09 accept-relabel / stay-NASBO candidate
- Every non-extracting / non-durable / non-tying FY for VT/WV/WY is in the gap log with a reason
- Batch-4 SOURCES doc complete for all 6 states across all 8 sections
  </acceptance_criteria>
  <done>VT, WV, WY fully reconned (or dispositioned STAY-NASBO per D-01); Batch-4 SOURCES doc complete for all 6 states across all 8 sections.</done>
</task>

</tasks>

<verification>
- `117-BATCH4-SOURCES.md` has a D-03 triage verdict + a complete per-state block for all 6 Batch-4 states (OK, RI, SD, VT, WV, WY) across all 8 sections.
- OK's block reuses its v2.14-preserved recon AND re-verifies that its durable URLs resolve today and its latest FY is current (not trusted blind).
- Every RECON-verdict state's oldest + latest bookend FY GF Total revenues is recorded with a dollar figure and tied to its printed page total via `pdftotext -table`.
- Every state's four risk facts (units / negatives / exact GF column+statement / FY-end month with non-June flagged) are pinned; recency-floor (FY2023+FY2024) verdict recorded per state.
- Any state failing triage (WY is a leading candidate) is recorded as a STAY-NASBO-exception candidate per D-01, never silently dropped (D-11); the gap log records every deferred/non-durable/non-tying FY with a reason; durable-URL-mandatory honored (D-06); no soft-404 HTML mistaken for a PDF.
- No DB writes, no loader edits, no NASBO mutations, no frontend changes; $0 spend.
</verification>

<success_criteria>
All 6 Batch-4 states triaged (D-03) and, where a clean GAAP GF ACFR exists, located + bookend-tie-confirmed (OK reused + re-verified) with dollar figures, four risk facts pinned, recency floor checked, scope-vs-NASBO compared, loader template mapped, and gap log written; any no-clean-ACFR state surfaced as a stay-NASBO-exception candidate (D-01) — the Phase 121 input contract for OK/RI/SD/VT/WV/WY (the last 6, all-50 completion) is complete and documentation-only.
</success_criteria>

<output>
Create `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-04-SUMMARY.md` when done.
</output>
