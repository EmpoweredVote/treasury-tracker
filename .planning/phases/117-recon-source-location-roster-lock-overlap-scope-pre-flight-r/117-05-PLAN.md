---
phase: 117-recon-source-location-roster-lock-overlap-scope-pre-flight-r
plan: "117-05"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md
autonomous: true
requirements: [RECON-11]
user_setup: []

must_haves:
  truths:
    - "For each DEEP-05 target below its current window — CA pre-FY2020, NY pre-FY2015, FL pre-FY2022, TX FY2016 — the additional cleanly pdftotext -table-extractable FY depth + durable per-year URL is located and bookend-tie-confirmed (D-02); go as deep as durable URLs allow with no hard FY floor"
    - "Per D-02: each target's dig is durable-URL bound with the ~15-20 min D-04 per-target effort budget; if no durable URL surfaces in budget, gap-log and stop; the recency floor (D-07) does NOT apply (these are pre-existing older windows)"
    - "Per D-06: a durable, re-fetchable URL is a hard requirement — a deeper year reachable only via a non-durable source (Wayback / one-off link) is EXCLUDED and gap-logged, not loaded, even if its GF total ties"
    - "Per D-05: bookend method — tie-confirm the OLDEST + LATEST newly-reachable FY of each deepened window; in-between FYs are extracted by Phase 122 at load time, not here"
    - "TX FY2016 is treated as a within-window file-id gap (its alternate ACFR file-id on the same archive page is located + tie-confirmed), not a below-window extension"
    - "Pre-GASB-34 years (before the FY2002 boundary) reached by the NY / FL digs are explicitly flagged for scripts/pre34Extract.mjs at load; CA pre-FY2020 and TX FY2016 are post-GASB-34 (no pre34 flag) unless a dig crosses FY2002"
    - "CA SCO soft-404 caution applied — downloads filtered by Content-Type application/pdf or size, never HTTP status alone; existing CA/NY/FL/TX ACFR rows are noted as untouched by this recon; $0 spend (pdftotext only, no AI)"
  artifacts:
    - path: ".planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md"
      provides: "Per-target deepening recon: current window, new durable old-end FY, added-year URL pattern, bookend tie evidence, units, pre-GASB-34 flags, deepening gap log for CA/NY/FL/TX"
      contains: "pre-GASB-34"
  key_links:
    - from: "117-DEEPEN-SOURCES.md per-target deepening block"
      to: "Phase 122 (DEEP-05) SOURCES-map extension + pre34Extract.mjs load"
      via: "added-year durable URL pattern + bookend tie + pre-GASB-34 flag"
      pattern: "General Fund"
---

<objective>
Deepening recon for the four existing ACFR nodes — **CA pre-FY2020, NY pre-FY2015, FL pre-FY2022, TX FY2016** — locating the durable per-year ACFR URLs *below* each node's current window (and TX's within-window FY2016 file-id gap), bookend-tie-confirming the newly-reachable FY depth via `pdftotext -table`, flagging any pre-GASB-34 years for `pre34Extract.mjs`, and writing a per-target deepening gap log for years that don't cleanly extract or lack a durable URL. **Go as deep as durable URLs allow (D-02) — no hard FY floor; the recency floor (D-07) does NOT apply here.**

Purpose: The DEEP-05-recon half of RECON-11 — the input contract for Phase 122 (recover the pre-window holes). **Documentation only — no DB writes, no NASBO mutations, no loader code, no frontend, $0 (`pdftotext` only, no AI).**
Output: `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md` — mirroring the Phase 103 `103-DEEPEN-SOURCES.md` shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md
@.planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-01-PLAN.md
@.planning/milestones/v2.11-phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md
@.planning/followups/ca-acfr-reconciliation.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public state ACFR site → local disk | untrusted older-year PDF/HTML fetched over the network into a local working dir |

Documentation-only, read-only recon: navigate SCO / OSC / myfloridacfo / TX Comptroller archive pages, download older public ACFR PDFs, run `pdftotext`, write local markdown. No DB, no secrets, no user input, no network writes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-05 | Spoofing/Tampering | the CA SCO soft-404 (HTTP 200 + HTML body) saved as ".pdf" and mistaken for a real older ACFR | mitigate | Filter every download by Content-Type `application/pdf` or size (>~1MB), never by HTTP status (D-06 + 98 soft-404 caution). |
| T-117-06 | Tampering | extracting the government-wide Statement of Activities or a budgetary schedule instead of the Governmental Funds GENERAL FUND column in an older ACFR whose layout drifted (pre-GASB-34 layout differs) | mitigate | Verify the exact statement title + "General Fund"/"General" column header + units before extracting; tie the extracted GF total to the printed total; flag pre-GASB-34 years for `pre34Extract.mjs` rather than forcing `-table`. |
| T-117-07 | Tampering | recording a Wayback/one-off deeper-year link that will rot, breaking the always-sourced standard | mitigate | Reject non-durable-only years to the gap log (D-06); a tie-confirmed total never overrides durability. |

No package installs → no supply-chain checkpoint needed.
</threat_model>

<tasks>

<task id="117-05-00" type="auto">
  <name>Task 0: Workspace + load the four targets' current windows + known durable URL patterns</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-02 deepening scope, D-05 bookend, D-06 durable-or-exclude, pre-GASB-34 flagging)
- .planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-01-PLAN.md (the deepening recon method + the DEEPEN-SOURCES doc shape to mirror)
- scripts/processCA.js (CA current window + General column + thousands + acfr{NN}web URL pattern)
- scripts/processNYAcfr.js (NY current window + General column + ×millions handling + finance/reports URL pattern)
- scripts/processFLAcfr.js (FL current window + General Fund column + fye-{YYYY} URL pattern)
- scripts/processTX.js (TX current window + General Revenue Fund column + thousands + 96-471.pdf URL pattern)
- .gitignore (confirm `_acfr-work/` is gitignored — it is, lines 108/133)
  </read_first>
  <action>
Reuse the gitignored `_acfr-work/{ca,ny,fl,tx}/` working dirs (they already exist). Confirm `pdftotext -v` runs (poppler). From the four loaders read above, record each node's **current** clean window (old-end FY) and its known durable per-year URL pattern, so this recon extends from known-good patterns rather than rediscovering them. Scaffold `117-DEEPEN-SOURCES.md` mirroring `103-DEEPEN-SOURCES.md`: (1) a per-target table — target | current window (old-end FY) | new durable old-end FY reached (or "no durable extension") | added-year per-year URL pattern | bookend tie evidence (new old-end + a current-end re-confirm) | units | pre-GASB-34 flag (years < FY2002 → `pre34Extract.mjs`); (2) a consolidated **deepening gap log** — target | FY | issue | disposition (light cleanup / excluded-non-durable / unavailable hole); (3) a one-line-per-target statement of how many additional clean FYs Phase 122 can load and from which URL pattern. Records fill in over Tasks 1–2.
  </action>
  <verify>
    <automated>pdftotext -v 2>&1 | Select-String -Pattern "pdftotext" ; Test-Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md</automated>
  </verify>
  <acceptance_criteria>
- `pdftotext -v` runs (poppler available); `_acfr-work/{ca,ny,fl,tx}/` reused and gitignored
- `117-DEEPEN-SOURCES.md` exists with the per-target table + deepening gap log skeleton mirroring 103-DEEPEN-SOURCES.md
- Each target's current old-end FY + known durable URL pattern is loaded from its loader as the starting point
  </acceptance_criteria>
  <done>Workspace ready, poppler confirmed, the four current windows + URL patterns loaded, and the DEEPEN-SOURCES skeleton in place.</done>
</task>

<task id="117-05-01" type="auto">
  <name>Task 1: Deepen NY (pre-FY2015) + resolve TX FY2016 file-id gap</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-02 no-floor, D-05 bookend, D-06 durable-or-exclude, pre-GASB-34 flag, recency floor N/A here)
- .planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-01-PLAN.md (the NY pre-FY2015 + TX FY2016 tasks — the direct precedent method)
- scripts/processNYAcfr.js (NY = ×millions; finance/reports ACFR not the NYSLRS pension ACFR; FY≤2021 naming)
- scripts/processTX.js (TX General Revenue Fund, thousands, 96-471.pdf pattern that 404'd for FY2016)
  </read_first>
  <action>
**New York — pre-FY2015.** NY uses the predictable `…/files/reports/finance/pdf/comprehensive-annual-financial-report-{YYYY}.pdf` naming for older years (the finance/reports ACFR, NOT the NYSLRS pension ACFR). Walk that pattern backward from the current old-end to find how deep durable, real PDFs extend (within the D-04 budget, no hard floor). In the OLDEST cleanly-reachable FY and the current old-end FY, confirm the Governmental Funds *Statement of Rev/Exp/Changes in Fund Balances* → **General** column (units = millions — confirm it still holds in the older layout — watch the ×1,000-millions trap), run `pdftotext -table`, and confirm the GF "Total revenues" ties to the printed total (bookend, D-05). **Flag any reached FY < FY2002 for `pre34Extract.mjs`** (pre-GASB-34 layout). Record the new old-end FY, the per-year URL pattern, and any FY that 404s/soft-404s/won't tie in the gap log; save text under `_acfr-work/ny/`. **Texas — FY2016 (within-window file-id gap).** On the Texas Comptroller comprehensive-annual-financial archive page for FY2016, locate the actual ACFR PDF's file-id / URL (the standard `…/2016/96-471.pdf` 404'd — the file-id was not used that year). Confirm the Governmental Funds *Statement of Rev/Exp/Changes* → **General Revenue Fund** column (thousands, post-GASB-34 — no pre34 flag), run `pdftotext -table`, and confirm the GF Total revenues ties to the printed total. Record FY2016's durable URL so the TX window becomes contiguous; if genuinely unavailable within the D-04 budget, gap-log FY2016 as a documented hole. Transcribe into `117-DEEPEN-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md -Pattern "New York|Texas|FY2016" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- NY's deeper clean window (old-end FY below its current old-end) is recorded with the durable per-year URL pattern, OR the gap log states where durable depth stopped and why; the old-end bookend GF Total revenues ties to the printed total via `-table`; units (millions) re-confirmed for the older layout; any FY < FY2002 flagged for `pre34Extract.mjs`
- TX FY2016's actual durable ACFR URL is located + FY2016 General Revenue Fund Total revenues tied via `-table`, OR the gap log documents FY2016 as an unavailable hole with the search attempted
- Every 404 / soft-404 / non-tying / non-durable FY is in the gap log with a reason
  </acceptance_criteria>
  <done>NY deepened (with pre-GASB-34 flags where it crosses FY2002) and TX FY2016 resolved or gap-logged — transcribed into 117-DEEPEN-SOURCES.md.</done>
</task>

<task id="117-05-02" type="auto">
  <name>Task 2: Deepen CA (pre-FY2020) + FL (pre-FY2022), then finalize DEEPEN-SOURCES</name>
  <files>.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md</files>
  <read_first>
- .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-CONTEXT.md (D-02 no-floor, D-06 durable-or-exclude, CA soft-404 caution, pre-GASB-34 flag)
- .planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-01-PLAN.md (the CA pre-FY2020 + FL pre-FY2022 tasks — the direct precedent method + the DEEPEN-SOURCES finalize step)
- .planning/followups/ca-acfr-reconciliation.md (CA ACFR CDN/403 access + browser-download fallback)
- scripts/processCA.js (CA General column, thousands, acfr{NN}web / cafr{NN}web pattern + soft-404 for FY≤2019)
- scripts/processFLAcfr.js (FL General Fund column, thousands, fye-{YYYY} naming starts FY2022)
  </read_first>
  <action>
**California — pre-FY2020.** Within the D-04 budget, navigate the SCO ARD / Public Documents archive to find whether older CA ACFRs (below the current old-end) have a durable, predictable, re-fetchable per-year URL (the standard `acfr{NN}web.pdf`/`cafr{NN}web.pdf` path returns an ~11.5KB HTML **soft-404** for older years). **Filter every candidate by Content-Type `application/pdf` or size > ~1MB — never by HTTP status (soft-404 returns 200).** If a durable URL is found, bookend-confirm: the Governmental Funds GF (General column, thousands) Total revenues ties via `pdftotext -table` (CA pre-FY2020 is post-GASB-34 — no pre34 flag unless the dig crosses FY2002). If the only older copies are non-durable (Wayback / one-off), record CA as "current old-end is the durable floor; deeper history located-but-not-durably-sourceable" in the gap log (D-06 — excluded, not loaded). Save any real PDFs under `_acfr-work/ca/`. **Florida — pre-FY2022.** The durable `…/cafr/fye-{YYYY}-…annual-comprehensive-financial-report.pdf` naming starts FY2022; older years use a different scheme. Within the D-04 budget, navigate the myfloridacfo.com transparency / CAFR archive to find whether older FL ACFRs have a durable, re-fetchable per-year URL. If found, bookend-confirm the Governmental Funds GF (General Fund column, thousands) Total revenues ties via `-table`, record the older URL pattern, and **flag any reached FY < FY2002 for `pre34Extract.mjs`**. If older copies are non-durable only, gap-log FL as "FY2022 is the durable floor; deeper located-but-not-durably-sourceable" per D-06. Save real PDFs under `_acfr-work/fl/`. **Then finalize `117-DEEPEN-SOURCES.md`:** complete the per-target table for all four targets + the consolidated deepening gap log + the one-line-per-target "how many additional clean FYs Phase 122 can load and from which URL pattern" statement; note that existing CA/NY/FL/TX ACFR rows are untouched by this recon; confirm $0 spend.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md -Pattern "California|Florida|pre-GASB-34" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- CA's deeper-history disposition is recorded: a durable per-year URL below its current old-end (bookend GF total tie-confirmed via `-table`) OR a gap-log entry "deeper CA history located-but-not-durably-sourceable — current old-end remains the durable floor" per D-06; no soft-404 HTML mistaken for a PDF (Content-Type/size validated)
- FL's deeper-history disposition is recorded: a durable per-year URL pattern below FY2022 (bookend GF total tie-confirmed via `-table`, pre-GASB-34 years flagged for `pre34Extract.mjs`) OR a gap-log entry per D-06; the older FL URL naming scheme recorded if found
- `117-DEEPEN-SOURCES.md` has a complete per-target table for all four targets (CA/NY/FL/TX) + consolidated deepening gap log + per-target added-FY count/URL statement; existing ACFR rows noted untouched; $0 spend confirmed
  </acceptance_criteria>
  <done>CA and FL deepened (or durably-floored + gap-logged), pre-GASB-34 years flagged; 117-DEEPEN-SOURCES.md finalized for all four DEEP-05 targets — the Phase 122 input contract is complete and documentation-only.</done>
</task>

</tasks>

<verification>
- `117-DEEPEN-SOURCES.md` has a per-target deepening disposition for all four DEEP-05 targets (CA/NY/FL/TX): a durable old-end FY + per-year URL pattern (bookend tie-confirmed via `-table`), or an explicit gap-log entry that deeper history isn't durably sourceable.
- TX FY2016's alternate file-id is located + tie-confirmed (or documented as an unavailable hole); every newly-reachable bookend FY is tie-confirmed to its ACFR's printed GF total.
- Pre-GASB-34 years (< FY2002) reached by any dig are flagged for `pre34Extract.mjs`; CA pre-FY2020 and TX FY2016 are treated as post-GASB-34 unless a dig crosses FY2002.
- Durable-URL-mandatory honored (D-06, no non-durable years loaded); CA soft-404 not mistaken for a PDF; recency floor (D-07) correctly NOT applied; existing CA/NY/FL/TX ACFR rows noted untouched; no DB writes; $0 spend.
</verification>

<success_criteria>
All four DEEP-05 targets have their pre-window durable URLs located + bookend-tie-confirmed as deep as durable URLs allow (D-02), TX FY2016 resolved or documented, pre-GASB-34 years flagged for `pre34Extract.mjs`, and a deepening gap log written — the Phase 122 input contract for CA/NY/FL/TX deepening is complete and documentation-only.
</success_criteria>

<output>
Create `.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-05-SUMMARY.md` when done.
</output>
