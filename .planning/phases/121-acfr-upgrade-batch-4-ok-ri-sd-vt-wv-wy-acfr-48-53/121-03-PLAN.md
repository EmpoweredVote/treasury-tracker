---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-03"
type: execute
wave: 3
depends_on: ["121-02"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processSDAcfr.js
  - scripts/processSDRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-03-SD-LOADLOG.md
autonomous: true
requirements: [ACFR-50]
user_setup: []

must_haves:
  truths:
    - "South Dakota state node (e7273079-b392-449d-af38-d2e4d0df73e0) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2002–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "SD units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL FUND column, Total Revenue line): FY2025 = $2,423,413K, FY2002 = $697,589K (store ×1,000 raw dollars)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General Fund | Transportation | Social Services Federal | COVID-19 Federal | Dakota Cement Trust | Education Enhancement Trust | Nonmajor | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are CLEANLY DERIVABLE (the best in Batch 4): `https://bfm.sd.gov/acfr/SD_ACFR_{YYYY}.PDF` (FY2021–FY2025) / `SD_CAFR_{YYYY}.PDF` (FY1998–FY2020) — the naming-era boundary at 2021 is fixed, not a per-year lookup. Landing: bfm.sd.gov/ACFR/"
    - "Pre-existing NASBO operating rows (SD FY2023 $2,231,000K, FY2024 $2,362,000K) are REPLACED in place at the same (e7273079, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (SD ACFR GF ~1.03× NASBO — the SMALLEST divergence in the entire v2.15 milestone; SD's federal-passthrough revenue routes to the Transportation/Federal columns, not GF, keeping GF near-parity) recorded at load; GAAP basis label verified"
    - "No negative GF line items observed at either bookend (Use of Money and Property positive both years); interior years checked at load; P2 clamp wired but likely unexercised"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on SD once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processSDAcfr.js"
      provides: "SD GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processSDRevenueAcfr.js"
      provides: "SD GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-03-SD-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~1.03× near-parity record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processSDAcfr.js / processSDRevenueAcfr.js"
      to: "treasury.budgets (SD state node e7273079)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['SD']"
      to: "scripts/processSDAcfr.js + scripts/processSDRevenueAcfr.js"
      via: "gen_state.py code generation from sd/sd_all.json + derivable per-FY SOURCES map"
      pattern: "General Fund"
---

<objective>
Upgrade the **South Dakota** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processSDAcfr.js` (GF spending-by-function, `operating`) + `scripts/processSDRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading SD ACFR PDFs FY2002–FY2025 into gitignored `_acfr-work/sd/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**SD is the easiest load in Batch 4** — a cleanly derivable `SD_ACFR_{YYYY}.PDF` / `SD_CAFR_{YYYY}.PDF` URL pattern (fixed 2021 era boundary). Units = THOUSANDS. Bookends: **FY2025 $2,423,413K, FY2002 $697,589K**. ~1.03× near-parity (the smallest scope divergence in the whole milestone) — accept-and-relabel honestly, but note the near-parity.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-03-SD-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['SD']` entry (mirror a recent clean-derivable thousands entry like 'NE'): name='South Dakota', abbr='SD', node='e7273079-b392-449d-af38-d2e4d0df73e0', years=[cleanly-tied FYs], tolerance small, data='sd/sd_all.json', out='SD', base_url the bfm.sd.gov/ACFR/ landing page, sources={ derivable per-FY URL map — SD_ACFR_{YYYY}.PDF / SD_CAFR_{YYYY}.PDF }, UNITS=1000. Use `head_note` for the ~1.03× near-parity note. Then `py -3 gen_state.py SD` emits the loaders.
- `_acfr-work/extract_gf.py` — GF-column extractor; capture revenues + expenditures sections for the GENERAL FUND (1st) column into sd/sd_all.json. Document hand-patches with printed-row evidence.
- Generated loaders inherit the v2.15 shape (see 121-01 interfaces). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'sd-acfr-gf-operating' (name 'South Dakota General Fund Operating Budget'), 'sd-acfr-gf-revenue' (name 'South Dakota General Fund Revenue'), api_type='pdf_download', base_url = bfm.sd.gov/ACFR/.
- NASBO baseline (live-confirmed): SD has 2 NASBO operating rows only — FY2023 $2,231,000K, FY2024 $2,362,000K, NO revenue rows, NO data_sources rows.
- DB access: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit.
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md SD Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = "General Fund", 1st of 7 (General Fund | Transportation | Social Services Federal | COVID-19 Federal | Dakota Cement Trust | Education Enhancement Trust | Nonmajor | Total). Note FY2002 had 5 columns (fewer funds). Units = THOUSANDS. FY-end June 30.
- Clean window FY2002–FY2025 (24 yr, both bookends tie-confirmed $0; full archive to FY1998, pre-FY2002 out of scope).
- URL — DERIVABLE: `https://bfm.sd.gov/acfr/SD_ACFR_{YYYY}.PDF` (FY2021–FY2025) / `SD_CAFR_{YYYY}.PDF` (FY1998–FY2020). Landing: `https://bfm.sd.gov/ACFR/`.
- Bookend ties (GF Total Revenue): FY2025 $2,423,413K (7-col sum $6,937,344K); FY2002 $697,589K (5-col sum $2,191,006K). Both diff $0.
- Investment income positive both bookends (Use of Money and Property FY2025 +$127,799K, FY2002 +$23,060K) — no P2 clamp at bookends; check interior years.
- Scope vs NASBO ~1.03× (FY2025 ACFR $2.42B vs NASBO $2.36B) — SMALLEST divergence in the milestone. Federal-passthrough revenue routes to Transportation/Federal columns, not GF. Accept-and-relabel honestly (near-parity).
</source_facts>
</context>

<tasks>

<task id="121-03-01" type="auto">
  <name>Task 1: Generate both SD loaders (UNITS=1000) via gen_state.py + download/extract FY2002–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/sd/ (gitignored PDFs + pdftotext + sd_all.json), scripts/processSDAcfr.js, scripts/processSDRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent clean-derivable thousands entry)
    - _acfr-work/extract_gf.py (GF-column extractor)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (SD Detail Block: derivable URL pattern, bookends, FY2002 5-column note)
    - scripts/processOKAcfr.js or processRIAcfr.js (a just-completed 121 loader — the exact v2.15 generated shape)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. For each FY across FY2002–FY2025, derive the URL (`SD_ACFR_{YYYY}.PDF` for FY2021+, `SD_CAFR_{YYYY}.PDF` for pre-2021) and download to `_acfr-work/sd/SD{YYYY}.pdf`. Soft-404 guard: `%PDF` magic + size > ~500KB. Run `pdftotext -table`, locate the Governmental Funds statement, run `extract_gf.py` to isolate the GENERAL FUND column (1st) for BOTH revenues and expenditures. Note the FY2002-era column count differs (5 not 7) — the tie still gates on the printed GENERAL FUND Total. Assemble `_acfr-work/sd/sd_all.json` (thousands, unscaled); document hand-patches with printed-row evidence. Add `CONFIGS['SD']` to gen_state.py (node e7273079…, years=[cleanly-tied FYs], UNITS=1000, derivable sources map, head_note ~1.03× near-parity). Run `py -3 gen_state.py SD`. Cross-check FY2025 revenue = 2,423,413×1000 and FY2002 = 697,589×1000 — mismatch → STOP and re-inspect column/statement. OMIT (honest hole) any interior FY that 404s/won't tie. Run both loaders `--dry-run`: every present FY PASS, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processSDAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processSDAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processSDRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '2,423,413,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/sd/` holds a real PDF per FY; soft-404/HTML rejected.
    - `node scripts/processSDAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints 2,423,413,000 and `--fy 2002` prints 697,589,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['SD']`; sd_all.json exists with per-FY operating + revenue trees.
    - Any interior negative GF line routes through clampForRender.
    - dataSource labels GAAP-basis-labelled; revenue RPC uses p_dataset_type:'revenue', dataset_id='sd-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both SD loaders exist with UNITS=1000, derivable SOURCES map, sd_all.json holds every tied FY, both bookends dry-run-tie, P2 clamp wired, no live write.</done>
</task>

<task id="121-03-02" type="auto">
  <name>Task 2: Live-load SD (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processSDAcfr.js, scripts/processSDRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processSDAcfr.js + processSDRevenueAcfr.js (--fy path + source-stamp + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; SD node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query the SD node (e7273079) and RECORD pre-load NASBO operating rows (FY2023 $2,231,000K, FY2024 $2,362,000K). For each present FY, run operating then revenue loaders live (--fy). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row where a NASBO year overlaps; revenue net-new), stamp GAAP source, self-clean its data_sources row. Abort+note any year failing the tie. Record loaded SD GF totals vs pre-load NASBO to confirm the ~1.03× near-parity (federal routed to non-GF columns) — expected, not a transcription error.
  </action>
  <verify>
    <automated>echo "SD verified in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, an operating row "South Dakota State ACFR — General Fund … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") + a revenue row, both non-null sources.
    - SD FY2025 revenue = 2,423,413,000 and FY2002 = 697,589,000.
    - SD FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (SD, fy); no NASBO label remains.
    - Pre-load NASBO + loaded ACFR totals recorded with the ~1.03× near-parity note.
  </acceptance_criteria>
  <done>Each transcribed SD FY has operating + revenue rows, thousands-scaled, GAAP-labelled + sourced; FY2023/FY2024 NASBO replaced in place; near-parity recorded.</done>
</task>

<task id="121-03-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-03-SD-LOADLOG.md</files>
  <read_first>
    - scripts/processSDAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative FY live (e.g. SD --fy 2025 op + rev), confirm 0 net change + 0 residue. Query treasury.budgets + data_sources to confirm: (a) every loaded SD FY has op + rev rows, GAAP-labelled, non-null sources; (b) 0 remaining NASBO operating rows (one op row per (SD, fy)); (c) honest-hole FYs ABSENT; (d) SD ≥1 revenue row (Money In); (e) 0 data_sources residue (LOAD-01); (f) cohort untouched. Append "Load Disposition" to 121-03-SD-LOADLOG.md (FYs loaded/skipped+reason, pre-load-NASBO-vs-loaded-ACFR with ~1.03× note, NASBO-replacement confirmation, idempotency result). Do NOT load beyond the tied window; touch no state other than SD.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-03-SD-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of SD --fy 2025 → 0 net change.
    - Every loaded SD FY has op + rev rows, GAAP-labelled, non-null sources; ZERO NASBO operating rows; one op row per (SD, fy).
    - SD ≥1 revenue row (Money In).
    - 0 data_sources residue for SD (LOAD-01).
    - Honest-hole FYs ABSENT.
    - Existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-03-SD-LOADLOG.md finalized.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change + 0 residue; DB query confirms SD fully ACFR-sourced, NASBO replaced, Money In enabled, honest holes absent, cohort unchanged; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bfm.sd.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the SD node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03-A | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (2,423,413K / 697,589K) asserted dry-run + live |
| T-121-03-B | Tampering | wrong statement/column (FY2002 has fewer fund columns) | mitigate | extract_gf.py isolates 1st column; printed GENERAL FUND Total tie gates before transcription |
| T-121-03-C | Tampering | wrong-era URL (ACFR vs CAFR) → wrong-year or 404 | mitigate | %PDF magic + size guard; honest hole if a year 404s |
| T-121-03-D | Tampering | NASBO row left alongside ACFR row | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one op row per (SD, fy), zero NASBO |
| T-121-03-E | Tampering | data_sources residue | mitigate | ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-03-F | Info Disclosure | recording ~1.03× as a transcription error rather than near-parity | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP label; near-parity noted |
| T-121-03-G | Tampering | write to a state other than SD | mitigate | loader resolves only the SD node id; Task 3 cohort spot-check |
| T-121-03-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both SD loaders `--dry-run`: every present FY PASS, no "sum ≠ total"; UNITS=1000.
- `processSDRevenueAcfr.js --dry-run --fy 2025` → 2,423,413,000; `--fy 2002` → 697,589,000.
- Live: SD node op + rev rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run SD --fy 2025 → 0 net change; 0 data_sources residue (LOAD-01).
- SD ≥1 revenue row → Money In auto-enabled.
- ~1.03× near-parity recorded; existing ACFR nodes + a sample NASBO state unchanged.
- 121-03-SD-LOADLOG.md finalized.
</verification>

<success_criteria>
The South Dakota state node is live on full State-ACFR GAAP across its clean durable window (target FY2002–FY2025, honest holes logged), thousands-scaled, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.03× near-parity recorded, idempotent never-overwrite proven, Money In auto-enabled, cohort untouched. Hands SD to Phase 124.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-03-SUMMARY.md` when done — record the SD FY window transcribed, any honest holes, the two bookend ties, the NASBO-replacement confirmation, the ~1.03× near-parity note, the idempotency re-run + 0-residue result, and Money-In auto-enable.
</output>
