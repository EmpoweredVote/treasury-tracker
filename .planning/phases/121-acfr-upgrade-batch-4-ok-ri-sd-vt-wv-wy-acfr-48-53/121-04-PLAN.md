---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-04"
type: execute
wave: 4
depends_on: ["121-03"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processVTAcfr.js
  - scripts/processVTRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-04-VT-LOADLOG.md
autonomous: true
requirements: [ACFR-51]
user_setup: []

must_haves:
  truths:
    - "Vermont state node (563d6f1c-ce2b-4071-938f-01725d283504) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2015–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "VT units are DOLLARS: UNITS=1 (NOT thousands — reuse the gen_state.py per-state units config added for NV/ND). Bookend ties (GENERAL FUND column, Total revenues line): FY2025 = $2,543,030,123, FY2015 = $1,392,033,404 (store as-is, no ×1,000)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (General Fund | Transportation Fund | Education Fund | Special Fund | Federal Revenue Fund | Global Commitment Fund | Non-major Governmental Funds | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "finance.vermont.gov requires a BROWSER User-Agent (bare curl gets HTTP 403) — all fetches must send `Mozilla/5.0 ... Chrome/122.0.0.0 Safari/537.36` (tn.gov precedent)"
    - "Source URLs mostly-derivable with naming exceptions: FY2021–FY2025 `VERMONT_{YYYY}_ACFR_FINAL.pdf`; FY2015–FY2018 `.../Rpts_Pubs/CAFR/FIN-{YYYY}_CAFR_FINAL.pdf`; FY2020 `VERMONT_2020_CAFR_FINAL.pdf`; FY2019 `2019_CAFR_FINAL.pdf` — special-case FY2019/FY2020 in the SOURCES map"
    - "Pre-existing NASBO operating rows (VT FY2023 $2,055,000K, FY2024 $2,510,000K — stored ×1,000) are REPLACED in place at the same (563d6f1c, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (VT ACFR GF ~1.01× NASBO — the SMALLEST divergence in Batch 4; VT federal grants routed mostly to the Transportation Fund, not GF) recorded at load; GAAP basis label verified"
    - "Investment income column header includes '(loss)' possibility — both bookends positive (FY2025 +$60,960,064, FY2015 +$304,938) but check intervening years at load; P2 clamp wired"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on VT once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processVTAcfr.js"
      provides: "VT GF spending-by-function loader (dataset_type='operating'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processVTRevenueAcfr.js"
      provides: "VT GF revenue-by-source loader (dataset_type='revenue'), UNITS=1 (dollars), tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-04-VT-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~1.01× near-parity record, browser-UA note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processVTAcfr.js / processVTRevenueAcfr.js"
      to: "treasury.budgets (VT state node 563d6f1c)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['VT']"
      to: "scripts/processVTAcfr.js + scripts/processVTRevenueAcfr.js"
      via: "gen_state.py code generation from vt/vt_all.json + per-FY SOURCES map, UNITS=1"
      pattern: "General Fund"
---

<objective>
Upgrade the **Vermont** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processVTAcfr.js` (GF spending-by-function, `operating`) + `scripts/processVTRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading VT ACFR PDFs FY2015–FY2025 into gitignored `_acfr-work/vt/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**VT is a DOLLARS state (UNITS=1)** — reuse the gen_state.py per-state units config from NV/ND; do NOT apply ×1,000. Bookends: **FY2025 $2,543,030,123, FY2015 $1,392,033,404**. ~1.01× near-parity (smallest in Batch 4) — accept-and-relabel honestly. **finance.vermont.gov 403s a bare curl** → send a browser User-Agent on every fetch. Naming exceptions: FY2019/FY2020 break the pattern.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-04-VT-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['VT']` entry (mirror a recent DOLLARS entry like 'ND' or 'NV' — UNITS=1): name='Vermont', abbr='VT', node='563d6f1c-ce2b-4071-938f-01725d283504', years=[cleanly-tied FYs], tolerance small, data='vt/vt_all.json', out='VT', base_url the finance.vermont.gov ACFR page, sources={ per-FY URL map with FY2019/FY2020 special-cased }, UNITS=1. Use `head_note` for the ~1.01× near-parity note. Then `py -3 gen_state.py VT` emits the loaders.
- `_acfr-work/extract_gf.py` — GF-column extractor; capture revenues + expenditures for the GENERAL FUND (1st) column into vt/vt_all.json. Document hand-patches with printed-row evidence.
- Fetching: send a browser UA header on curl (`-A 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36'`) — bare curl 403s on finance.vermont.gov.
- Generated loaders inherit the v2.15 shape (see 121-01 interfaces) with UNITS=1. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'vt-acfr-gf-operating' (name 'Vermont General Fund Operating Budget'), 'vt-acfr-gf-revenue' (name 'Vermont General Fund Revenue'), api_type='pdf_download', base_url = the finance.vermont.gov ACFR page.
- NASBO baseline (live-confirmed): VT has 2 NASBO operating rows only — FY2023 $2,055,000K, FY2024 $2,510,000K (stored ×1,000), NO revenue rows, NO data_sources rows.
- DB access: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit.
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md VT Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = "General Fund", 1st of 7 (General Fund | Transportation Fund | Education Fund | Special Fund | Federal Revenue Fund | Global Commitment Fund | Non-major | Total). Units = DOLLARS (whole-dollar figures). FY-end June 30.
- Clean window FY2015–FY2025 (11 yr, both bookends tie-confirmed $0). Pre-FY2015 not discoverable — out of scope.
- URL map (browser UA required): FY2021–FY2025 `https://finance.vermont.gov/sites/finance/files/documents/VERMONT_{YYYY}_ACFR_FINAL.pdf` (FY2021 under `.../documents/Rpts_Pubs/CAFR/`); FY2020 `.../Rpts_Pubs/CAFR/VERMONT_2020_CAFR_FINAL.pdf`; FY2019 `.../Rpts_Pubs/CAFR/2019_CAFR_FINAL.pdf`; FY2015–FY2018 `.../Rpts_Pubs/CAFR/FIN-{YYYY}_CAFR_FINAL.pdf`. Landing: `https://finance.vermont.gov/reports-and-publications/annual-comprehensive-financial-report`.
- Bookend ties (GF Total revenues): FY2025 $2,543,030,123 (7-col sum $9,281,271,214); FY2015 $1,392,033,404 (7-col sum $5,532,771,197). Both diff $0.
- Investment income positive both bookends (FY2025 +$60,960,064, FY2015 +$304,938); header includes "(loss)" — check interior years.
- Scope vs NASBO ~1.01× (FY2025 ACFR $2.54B vs NASBO $2.51B) — SMALLEST in Batch 4. Federal grants routed mostly to Transportation Fund, not GF. Accept-and-relabel honestly (near-parity).
</source_facts>
</context>

<tasks>

<task id="121-04-01" type="auto">
  <name>Task 1: Generate both VT loaders (UNITS=1 dollars) via gen_state.py + download/extract FY2015–FY2025 (browser UA) + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/vt/ (gitignored PDFs + pdftotext + vt_all.json), scripts/processVTAcfr.js, scripts/processVTRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent DOLLARS entry like 'ND'; confirm the per-state UNITS config wiring)
    - _acfr-work/extract_gf.py (GF-column extractor)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (VT Detail Block: dollars units, browser-UA requirement, FY2019/FY2020 naming exceptions, bookends)
    - scripts/processNDAcfr.js (the closest v2.15 DOLLARS/UNITS=1 loader analog)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. For each FY across FY2015–FY2025, resolve the URL from the SOURCES map (special-case FY2019 `2019_CAFR_FINAL.pdf` and FY2020 `VERMONT_2020_CAFR_FINAL.pdf`) and download to `_acfr-work/vt/VT{YYYY}.pdf` **sending a browser User-Agent** (bare curl 403s). Soft-404 guard: `%PDF` magic + size > ~500KB; an HTML/403 page must abort that FY. Run `pdftotext -table`, locate the Governmental Funds statement, run `extract_gf.py` to isolate the GENERAL FUND column (1st) for BOTH revenues and expenditures. Assemble `_acfr-work/vt/vt_all.json` with verbatim whole-dollar figures (DOLLARS — no ×1,000); document hand-patches with printed-row evidence. Add `CONFIGS['VT']` to gen_state.py (node 563d6f1c…, years=[cleanly-tied FYs], **UNITS=1**, per-FY sources map with FY2019/FY2020 special-cased, head_note ~1.01× near-parity). Run `py -3 gen_state.py VT`. Cross-check FY2025 revenue stored = 2,543,030,123 (NOT ×1,000) and FY2015 = 1,392,033,404 — mismatch or a 1000× inflation → STOP and re-inspect UNITS/column. OMIT (honest hole) any interior FY that 403s/won't tie. Run both loaders `--dry-run`: every present FY PASS, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processVTAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processVTAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processVTRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '2,543,030,123'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/vt/` holds a real PDF per FY (fetched with a browser UA); soft-404/HTML/403 rejected.
    - `node scripts/processVTAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints 2,543,030,123 and `--fy 2015` prints 1,392,033,404 (NOT ×1,000).
    - Both loaders carry `UNITS = 1`; gen_state.py has `CONFIGS['VT']` with UNITS=1; vt_all.json exists with per-FY operating + revenue trees.
    - Any interior negative GF line routes through clampForRender.
    - dataSource labels GAAP-basis-labelled; revenue RPC uses p_dataset_type:'revenue', dataset_id='vt-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both VT loaders exist with UNITS=1 (dollars), per-FY SOURCES map (FY2019/FY2020 special-cased), vt_all.json holds every tied FY, both bookends dry-run-tie at whole-dollar scale, P2 clamp wired, no live write.</done>
</task>

<task id="121-04-02" type="auto">
  <name>Task 2: Live-load VT (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processVTAcfr.js, scripts/processVTRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processVTAcfr.js + processVTRevenueAcfr.js (--fy path + source-stamp + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; VT node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query the VT node (563d6f1c) and RECORD pre-load NASBO operating rows (FY2023 $2,055,000K, FY2024 $2,510,000K — stored ×1,000). For each present FY, run operating then revenue loaders live (--fy). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row where a NASBO year overlaps; revenue net-new), stamp GAAP source, self-clean its data_sources row. Abort+note any year failing the tie. Note the NASBO rows are stored in ×1,000-dollars while VT ACFR is whole-dollars — the comparison is total-vs-total (both raw dollars in the DB), so the ~1.01× near-parity should hold; confirm no accidental 1000× skew.
  </action>
  <verify>
    <automated>echo "VT verified in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, an operating row "Vermont State ACFR — General Fund … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") + a revenue row, both non-null sources.
    - VT FY2025 revenue = 2,543,030,123 and FY2015 = 1,392,033,404 (whole dollars, no 1000× skew).
    - VT FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (VT, fy); no NASBO label remains.
    - Pre-load NASBO + loaded ACFR totals recorded with the ~1.01× near-parity note.
  </acceptance_criteria>
  <done>Each transcribed VT FY has operating + revenue rows, whole-dollar (UNITS=1), GAAP-labelled + sourced; FY2023/FY2024 NASBO replaced in place; near-parity recorded with no 1000× skew.</done>
</task>

<task id="121-04-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-04-VT-LOADLOG.md</files>
  <read_first>
    - scripts/processVTAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative FY live (e.g. VT --fy 2025 op + rev), confirm 0 net change + 0 residue. Query treasury.budgets + data_sources to confirm: (a) every loaded VT FY has op + rev rows, GAAP-labelled, non-null sources, whole-dollar scale; (b) 0 remaining NASBO operating rows (one op row per (VT, fy)); (c) honest-hole FYs ABSENT; (d) VT ≥1 revenue row (Money In); (e) 0 data_sources residue (LOAD-01); (f) cohort untouched. Append "Load Disposition" to 121-04-VT-LOADLOG.md (FYs loaded/skipped+reason, browser-UA note, pre-load-NASBO-vs-loaded-ACFR with ~1.01× note, NASBO-replacement confirmation, idempotency result). Do NOT load beyond the tied window; touch no state other than VT.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-04-VT-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of VT --fy 2025 → 0 net change.
    - Every loaded VT FY has op + rev rows, GAAP-labelled, non-null sources, whole-dollar scale; ZERO NASBO operating rows; one op row per (VT, fy).
    - VT ≥1 revenue row (Money In).
    - 0 data_sources residue for VT (LOAD-01).
    - Honest-hole FYs ABSENT.
    - Existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-04-VT-LOADLOG.md finalized.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change + 0 residue; DB query confirms VT fully ACFR-sourced (whole-dollar), NASBO replaced, Money In enabled, honest holes absent, cohort unchanged; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finance.vermont.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the VT node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-04-A | Tampering | units mis-scale — applying ×1,000 to a DOLLARS state inflates VT 1000× | mitigate | UNITS=1 hard-set; bookends (2,543,030,123 / 1,392,033,404) asserted whole-dollar in dry-run + live; explicit no-1000×-skew check in Task 2 |
| T-121-04-B | Tampering | wrong statement/column | mitigate | extract_gf.py isolates 1st column; bookend ties gate before transcription |
| T-121-04-C | Availability/Tampering | 403 (missing browser UA) or wrong naming-exception URL → no/wrong PDF | mitigate | browser UA on every fetch; FY2019/FY2020 special-cased; %PDF magic + size guard; honest hole if a year fails |
| T-121-04-D | Tampering | NASBO row left alongside ACFR row | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one op row per (VT, fy), zero NASBO |
| T-121-04-E | Tampering | data_sources residue | mitigate | ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-04-F | Info Disclosure | recording ~1.01× incorrectly / hiding a 1000× skew | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP label; near-parity + no-skew noted |
| T-121-04-G | Tampering | write to a state other than VT | mitigate | loader resolves only the VT node id; Task 3 cohort spot-check |
| T-121-04-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both VT loaders `--dry-run`: every present FY PASS, no "sum ≠ total"; UNITS=1.
- `processVTRevenueAcfr.js --dry-run --fy 2025` → 2,543,030,123; `--fy 2015` → 1,392,033,404 (whole dollars).
- Live: VT node op + rev rows per loaded FY, GAAP-labelled, non-null sources, whole-dollar; NASBO FY2023/FY2024 replaced in place.
- Re-run VT --fy 2025 → 0 net change; 0 data_sources residue (LOAD-01).
- VT ≥1 revenue row → Money In auto-enabled.
- ~1.01× near-parity recorded (no 1000× skew); existing ACFR nodes + a sample NASBO state unchanged.
- 121-04-VT-LOADLOG.md finalized.
</verification>

<success_criteria>
The Vermont state node is live on full State-ACFR GAAP across its clean durable window (target FY2015–FY2025, honest holes logged), whole-dollar (UNITS=1), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.01× near-parity recorded, idempotent never-overwrite proven, Money In auto-enabled, cohort untouched. Hands VT to Phase 124.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-04-SUMMARY.md` when done — record the VT FY window transcribed, any honest holes, the browser-UA note, the two bookend ties (whole-dollar), the NASBO-replacement confirmation, the ~1.01× near-parity note, the idempotency re-run + 0-residue result, and Money-In auto-enable.
</output>
