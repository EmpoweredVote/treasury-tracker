---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-05"
type: execute
wave: 5
depends_on: ["121-04"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processWVAcfr.js
  - scripts/processWVRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-05-WV-LOADLOG.md
autonomous: true
requirements: [ACFR-52]
user_setup: []

must_haves:
  truths:
    - "West Virginia state node (e21923d7-ad99-4711-b765-255b9807c059) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2020–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "WV units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL column, Total Revenues line): FY2025 = $14,639,897K, FY2020 = $10,760,376K (store ×1,000 raw dollars)"
    - "GENERAL column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General | Transportation | Tobacco Settlement Finance Authority | State Road | Other Governmental Funds | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are OPAQUE Drupal media IDs (no derivable pattern) — the SOURCES map enumerates all 6 years explicitly from the landing page: FY2025 media/37441, FY2024 media/10261, FY2023 media/10251, FY2022 media/10236, FY2021 media/10521, FY2020 media/10646 (all `https://finance.wv.gov/media/{ID}/download?inline`). Re-check the landing page at load for any newly-added older years"
    - "Pre-existing NASBO operating rows (WV FY2023 $3,943,000K, FY2024 $4,164,000K) are REPLACED in place at the same (e21923d7, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (WV ACFR GF ~3.52× NASBO — 2nd-largest in Batch 4; a very large Intergovernmental federal-passthrough line ~$6.92B of $14.64B GF, ~47%, plus nearly all state tax revenue consolidated into GF) recorded at load; GAAP basis label verified"
    - "No negative GF line items observed at either bookend (Investment Earnings positive both years); interior years checked at load; P2 clamp wired but likely unexercised"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on WV once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processWVAcfr.js"
      provides: "WV GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processWVRevenueAcfr.js"
      provides: "WV GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-05-WV-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~3.52× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processWVAcfr.js / processWVRevenueAcfr.js"
      to: "treasury.budgets (WV state node e21923d7)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['WV']"
      to: "scripts/processWVAcfr.js + scripts/processWVRevenueAcfr.js"
      via: "gen_state.py code generation from wv/wv_all.json + explicit per-FY SOURCES map"
      pattern: "General"
---

<objective>
Upgrade the **West Virginia** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processWVAcfr.js` (GF spending-by-function, `operating`) + `scripts/processWVRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading WV ACFR PDFs FY2020–FY2025 into gitignored `_acfr-work/wv/`, extracting the **GENERAL column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

Units = THOUSANDS (UNITS=1000). Bookends: **FY2025 $14,639,897K, FY2020 $10,760,376K**. ~3.52× scope divergence (2nd-largest in Batch 4 — Intergovernmental federal-passthrough ~47% of GF), accept-and-relabel honestly, document prominently. **URLs are opaque Drupal media IDs** — enumerate all 6 years explicitly. Shallow 6-year window (FY2020–FY2025) — re-check the landing page at load for newly-added older years.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-05-WV-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['WV']` entry (mirror a recent thousands entry with an EXPLICIT per-FY sources map): name='West Virginia', abbr='WV', node='e21923d7-ad99-4711-b765-255b9807c059', years=[cleanly-tied FYs], tolerance small, data='wv/wv_all.json', out='WV', base_url the finance.wv.gov ACFR page, sources={ explicit per-FY media-ID URL map }, UNITS=1000. Use `head_note` for the ~3.52× accept-relabel scope note (Intergovernmental federal-passthrough ~47% of GF). Then `py -3 gen_state.py WV` emits the loaders.
- `_acfr-work/extract_gf.py` — GF-column extractor; capture revenues + expenditures for the GENERAL (1st) column into wv/wv_all.json. Document hand-patches with printed-row evidence.
- Generated loaders inherit the v2.15 shape (see 121-01 interfaces). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'wv-acfr-gf-operating' (name 'West Virginia General Fund Operating Budget'), 'wv-acfr-gf-revenue' (name 'West Virginia General Fund Revenue'), api_type='pdf_download', base_url = the finance.wv.gov ACFR page.
- NASBO baseline (live-confirmed): WV has 2 NASBO operating rows only — FY2023 $3,943,000K, FY2024 $4,164,000K, NO revenue rows, NO data_sources rows.
- DB access: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit.
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md WV Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = "General", 1st of 5 (General | Transportation | Tobacco Settlement Finance Authority | State Road | Other Governmental Funds | Total). Units = THOUSANDS. FY-end June 30.
- Clean window FY2020–FY2025 (6 yr, both bookends tie-confirmed $0; pre-FY2020 not linked from the current page — shallow window, MD/GA precedent, no min depth beyond the recency floor).
- URL map (OPAQUE Drupal media IDs, enumerate from landing page): FY2025 `https://finance.wv.gov/media/37441/download?inline`; FY2024 `.../media/10261/...`; FY2023 `.../media/10251/...`; FY2022 `.../media/10236/...`; FY2021 `.../media/10521/...`; FY2020 `.../media/10646/...`. Landing: `https://finance.wv.gov/annual-comprehensive-financial-report-acfr`.
- Bookend ties (GF Total Revenues): FY2025 $14,639,897K (5-col sum $17,027,191K); FY2020 $10,760,376K (5-col sum $12,357,751K). Both diff $0.
- Investment income positive both bookends (Investment Earnings FY2025 +$352,526K, FY2020 +$96,028K) — no P2 clamp at bookends; check interior years.
- Scope vs NASBO ~3.52× (FY2025 ACFR $14.64B vs NASBO $4.16B) — 2nd-largest in Batch 4 (comparable to MI's ~3.56×). Intergovernmental federal-passthrough ~47% of GF + nearly all state tax revenue consolidated in. Accept-and-relabel honestly, document prominently.
</source_facts>
</context>

<tasks>

<task id="121-05-01" type="auto">
  <name>Task 1: Generate both WV loaders (UNITS=1000) via gen_state.py + download/extract FY2020–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/wv/ (gitignored PDFs + pdftotext + wv_all.json), scripts/processWVAcfr.js, scripts/processWVRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent thousands entry with an explicit per-FY sources map)
    - _acfr-work/extract_gf.py (GF-column extractor)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (WV Detail Block: opaque media-ID URL map, bookends)
    - scripts/processOKAcfr.js or processRIAcfr.js (a just-completed 121 loader — the exact v2.15 generated shape)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. Re-check the landing page (`finance.wv.gov/annual-comprehensive-financial-report-acfr`) for any newly-added older years beyond the 6 listed. For each FY across FY2020–FY2025, use the EXPLICIT per-FY media-ID URL and download to `_acfr-work/wv/WV{YYYY}.pdf`. Soft-404 guard: `%PDF` magic + size > ~500KB; an HTML soft-404 must abort that FY. Run `pdftotext -table`, locate the Governmental Funds statement, run `extract_gf.py` to isolate the GENERAL column (1st) for BOTH revenues and expenditures. Assemble `_acfr-work/wv/wv_all.json` (thousands, unscaled); document hand-patches with printed-row evidence. Add `CONFIGS['WV']` to gen_state.py (node e21923d7…, years=[cleanly-tied FYs], UNITS=1000, explicit media-ID sources map, head_note ~3.52×). Run `py -3 gen_state.py WV`. Cross-check FY2025 revenue = 14,639,897×1000 and FY2020 = 10,760,376×1000 — mismatch → STOP and re-inspect column/statement. OMIT (honest hole) any interior FY that 404s/won't tie. Run both loaders `--dry-run`: every present FY PASS, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processWVAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processWVAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processWVRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '14,639,897,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/wv/` holds a real PDF per FY; soft-404/HTML rejected.
    - `node scripts/processWVAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints 14,639,897,000 and `--fy 2020` prints 10,760,376,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['WV']`; wv_all.json exists with per-FY operating + revenue trees.
    - Any interior negative GF line routes through clampForRender.
    - dataSource labels GAAP-basis-labelled; revenue RPC uses p_dataset_type:'revenue', dataset_id='wv-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both WV loaders exist with UNITS=1000, explicit per-FY media-ID SOURCES map, wv_all.json holds every tied FY, both bookends dry-run-tie, P2 clamp wired, no live write.</done>
</task>

<task id="121-05-02" type="auto">
  <name>Task 2: Live-load WV (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processWVAcfr.js, scripts/processWVRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processWVAcfr.js + processWVRevenueAcfr.js (--fy path + source-stamp + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; WV node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query the WV node (e21923d7) and RECORD pre-load NASBO operating rows (FY2023 $3,943,000K, FY2024 $4,164,000K). For each present FY, run operating then revenue loaders live (--fy). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row where a NASBO year overlaps; revenue net-new), stamp GAAP source, self-clean its data_sources row. Abort+note any year failing the tie. Record loaded WV GF totals vs pre-load NASBO to confirm the ~3.52× accept-relabel divergence is the expected federal-passthrough-consolidation mechanism.
  </action>
  <verify>
    <automated>echo "WV verified in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, an operating row "West Virginia State ACFR — General … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") + a revenue row, both non-null sources.
    - WV FY2025 revenue = 14,639,897,000 and FY2020 = 10,760,376,000.
    - WV FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (WV, fy); no NASBO label remains.
    - Pre-load NASBO + loaded ACFR totals recorded with the ~3.52× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed WV FY has operating + revenue rows, thousands-scaled, GAAP-labelled + sourced; FY2023/FY2024 NASBO replaced in place; accept-relabel divergence recorded.</done>
</task>

<task id="121-05-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-05-WV-LOADLOG.md</files>
  <read_first>
    - scripts/processWVAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative FY live (e.g. WV --fy 2025 op + rev), confirm 0 net change + 0 residue. Query treasury.budgets + data_sources to confirm: (a) every loaded WV FY has op + rev rows, GAAP-labelled, non-null sources; (b) 0 remaining NASBO operating rows (one op row per (WV, fy)); (c) honest-hole FYs ABSENT; (d) WV ≥1 revenue row (Money In); (e) 0 data_sources residue (LOAD-01); (f) cohort untouched. Append "Load Disposition" to 121-05-WV-LOADLOG.md (FYs loaded/skipped+reason, pre-load-NASBO-vs-loaded-ACFR with ~3.52× note, NASBO-replacement confirmation, idempotency result). Do NOT load beyond the tied window; touch no state other than WV.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-05-WV-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of WV --fy 2025 → 0 net change.
    - Every loaded WV FY has op + rev rows, GAAP-labelled, non-null sources; ZERO NASBO operating rows; one op row per (WV, fy).
    - WV ≥1 revenue row (Money In).
    - 0 data_sources residue for WV (LOAD-01).
    - Honest-hole FYs ABSENT.
    - Existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-05-WV-LOADLOG.md finalized.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change + 0 residue; DB query confirms WV fully ACFR-sourced, NASBO replaced, Money In enabled, honest holes absent, cohort unchanged; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finance.wv.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the WV node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05-A | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (14,639,897K / 10,760,376K) asserted dry-run + live |
| T-121-05-B | Tampering | wrong statement/column | mitigate | extract_gf.py isolates 1st column; bookend ties gate before transcription |
| T-121-05-C | Tampering | wrong opaque media-ID URL → wrong-year or 404 PDF | mitigate | explicit per-FY media-ID map; %PDF magic + size guard; honest hole if a year 404s; landing-page re-check |
| T-121-05-D | Tampering | NASBO row left alongside ACFR row | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one op row per (WV, fy), zero NASBO |
| T-121-05-E | Tampering | data_sources residue | mitigate | ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-05-F | Info Disclosure | silently inflating WV (~3.52×) without recording | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP label |
| T-121-05-G | Tampering | write to a state other than WV | mitigate | loader resolves only the WV node id; Task 3 cohort spot-check |
| T-121-05-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both WV loaders `--dry-run`: every present FY PASS, no "sum ≠ total"; UNITS=1000.
- `processWVRevenueAcfr.js --dry-run --fy 2025` → 14,639,897,000; `--fy 2020` → 10,760,376,000.
- Live: WV node op + rev rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run WV --fy 2025 → 0 net change; 0 data_sources residue (LOAD-01).
- WV ≥1 revenue row → Money In auto-enabled.
- ~3.52× accept-relabel recorded; existing ACFR nodes + a sample NASBO state unchanged.
- 121-05-WV-LOADLOG.md finalized.
</verification>

<success_criteria>
The West Virginia state node is live on full State-ACFR GAAP across its clean durable window (target FY2020–FY2025, honest holes logged), thousands-scaled, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~3.52× accept-relabel divergence recorded, idempotent never-overwrite proven, Money In auto-enabled, cohort untouched. Hands WV to Phase 124.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-05-SUMMARY.md` when done — record the WV FY window transcribed, any honest holes, the two bookend ties, the NASBO-replacement confirmation, the ~3.52× divergence, the idempotency re-run + 0-residue result, and Money-In auto-enable.
</output>
