---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-06"
type: execute
wave: 6
depends_on: ["121-05"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processWYAcfr.js
  - scripts/processWYRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-06-WY-LOADLOG.md
autonomous: true
requirements: [ACFR-53]
user_setup: []

must_haves:
  truths:
    - "Wyoming STATE node (4009951b-8a23-457e-9591-1597356dfe34, entity_type='state') shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2005–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "CRITICAL node disambiguation: there are THREE municipalities named 'Wyoming' — the STATE node 4009951b (the ONLY target) plus two unrelated CITY nodes (Wyoming, MN = 1604b5eb-283d-4f65-91a4-e9de651a4241 and Wyoming, OH = dacac0e3-13ca-46b1-bcad-50b1099032d0). The loader MUST resolve/write the STATE node id 4009951b ONLY — never the city nodes"
    - "WY units are DOLLARS: UNITS=1 (NOT thousands — reuse the gen_state.py per-state units config from NV/ND/VT). Bookend ties (GENERAL FUND column, Total Revenues line): FY2025 = $4,027,001,270, FY2005 = $1,590,602,744 (store as-is, no ×1,000)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General Fund | Foundation Program Fund | Common School Land Fund | Permanent Mineral Trust Fund | Pandemic Relief Fund | Nonmajor Governmental Funds | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs span multiple naming eras (no single derivable pattern) — the SOURCES map enumerates every year explicitly from sao.wyo.gov/publications: FY2025 .../2026/01/2025-ACFR-12.22.25.pdf; FY2024 .../2025/01/2024-ACFR-State-of-Wyoming.pdf; FY2023 .../2024/02/2023-ACFR-State-of-Wyoming.pdf; FY2022 .../2023/02/ACFR-FY2022-1.31.23.pdf; FY2021 .../2022/06/ACFR-FY2021-5.31.22.pdf; FY2019 .../2020/04/CAFR_2019.pdf; FY2018 .../2019/10/2018-CAFR.pdf; FY2005–FY2017 .../2020/01/{YYYY}-CAFR.pdf"
    - "Pre-existing NASBO operating rows (WY FY2023 $1,525,000K, FY2024 $1,654,000K on the STATE node 4009951b) are REPLACED in place at the same (4009951b, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (WY ACFR GF ~2.43× NASBO — driver is UNUSUAL: a large Federal line $1.11B AND an even larger Investment Income line $1.41B, driven by Permanent Mineral Trust Fund earnings routed partly through the GF; investment income, not federal-passthrough or tax consolidation, is the largest driver) recorded at load; GAAP basis label verified"
    - "P2 clamp monitoring at EVERY loaded FY (not just bookends): WY's GF carries a mark-to-market 'Net Increase/(Decrease) in Fair Market Value of Investments' sub-line + very large Investment Income lines — a down-market year could plausibly go negative; both bookends are positive but interior years must be checked and clamped where negative"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on WY once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processWYAcfr.js"
      provides: "WY GF spending-by-function loader (dataset_type='operating'), UNITS=1 (dollars), STATE node 4009951b, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processWYRevenueAcfr.js"
      provides: "WY GF revenue-by-source loader (dataset_type='revenue'), UNITS=1 (dollars), STATE node 4009951b, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-06-WY-LOADLOG.md"
      provides: "Per-FY load disposition, node-disambiguation confirmation, NASBO-replacement confirmation, ~2.43× investment-income-driven accept-relabel record, per-year P2 clamp results, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processWYAcfr.js / processWYRevenueAcfr.js"
      to: "treasury.budgets (WY STATE node 4009951b — NOT the city nodes)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['WY']"
      to: "scripts/processWYAcfr.js + scripts/processWYRevenueAcfr.js"
      via: "gen_state.py code generation from wy/wy_all.json + per-FY SOURCES map, UNITS=1, node=4009951b"
      pattern: "General Fund"
---

<objective>
Upgrade the **Wyoming** STATE node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processWYAcfr.js` (GF spending-by-function, `operating`) + `scripts/processWYRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading WY ACFR PDFs FY2005–FY2025 into gitignored `_acfr-work/wy/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**This is the final state — Batch 4 close puts all 50 states on ACFR.** WY is a DOLLARS state (UNITS=1). Bookends: **FY2025 $4,027,001,270, FY2005 $1,590,602,744**. ~2.43× scope divergence with an UNUSUAL driver (investment income, not federal-passthrough). **Three critical flags:** (1) **node disambiguation** — target the STATE node 4009951b ONLY, never the two 'Wyoming' city nodes; (2) **per-year P2 clamp monitoring** — WY's GF investment-income exposure means interior years can go negative; (3) multiple URL naming eras (enumerate). FY1980–FY2004 are poor-OCR scans → stretch goal only, not required.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-06-WY-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['WY']` entry (mirror a recent DOLLARS entry like 'ND' — UNITS=1): name='Wyoming', abbr='WY', node='4009951b-8a23-457e-9591-1597356dfe34' (the STATE node — NOT a city), years=[cleanly-tied FYs FY2005+], tolerance small, data='wy/wy_all.json', out='WY', base_url the sao.wyo.gov/publications page, sources={ per-FY URL map across the naming eras }, UNITS=1. Use `head_note` for the ~2.43× accept-relabel note (investment-income-driven, distinct from the usual federal-passthrough mechanism); `neg_note` recording the per-year P2 clamp monitoring for the investment-income line. Then `py -3 gen_state.py WY` emits the loaders.
- `_acfr-work/extract_gf.py` — GF-column extractor; capture revenues + expenditures for the GENERAL FUND (1st) column into wy/wy_all.json. Document hand-patches with printed-row evidence. Watch older-format years (FY2005-era) where the statement layout differs from recent years.
- Generated loaders inherit the v2.15 shape (see 121-01 interfaces) with UNITS=1 and node=4009951b. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'wy-acfr-gf-operating' (name 'Wyoming General Fund Operating Budget'), 'wy-acfr-gf-revenue' (name 'Wyoming General Fund Revenue'), api_type='pdf_download', base_url = the sao.wyo.gov/publications page.
- NASBO baseline (live-confirmed on the STATE node): WY state node 4009951b has 2 NASBO operating rows only — FY2023 $1,525,000K, FY2024 $1,654,000K, NO revenue rows, NO data_sources rows. (The two 'Wyoming' CITY nodes carry unrelated MN-OSA / Ohio-AOS municipal rows — DO NOT touch them.)
- DB access: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit.
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md WY Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = "General Fund", 1st of 6 (General Fund | Foundation Program Fund | Common School Land Fund | Permanent Mineral Trust Fund | Pandemic Relief Fund | Nonmajor | Total). Units = DOLLARS (whole-dollar figures). FY-end June 30.
- Clean-text window FY2005–FY2025 (21 yr, both bookends tie-confirmed $0). FY1980–FY2004 archive exists but pre-2005 PDFs (at least FY2002) are poor-OCR scans — stretch goal only, NOT required for the recency floor.
- URL map (multiple naming eras, enumerate from publications page): FY2025 `https://sao.wyo.gov/wp-content/uploads/2026/01/2025-ACFR-12.22.25.pdf`; FY2024 `.../2025/01/2024-ACFR-State-of-Wyoming.pdf`; FY2023 `.../2024/02/2023-ACFR-State-of-Wyoming.pdf`; FY2022 `.../2023/02/ACFR-FY2022-1.31.23.pdf`; FY2021 `.../2022/06/ACFR-FY2021-5.31.22.pdf`; FY2019 `.../2020/04/CAFR_2019.pdf`; FY2018 `.../2019/10/2018-CAFR.pdf`; FY2005–FY2017 `.../2020/01/{YYYY}-CAFR.pdf`. Landing: `https://sao.wyo.gov/publications/`.
- Bookend ties (GF Total Revenues): FY2025 $4,027,001,270 (6-col sum $7,222,723,950); FY2005 $1,590,602,744 (6-col sum $3,647,136,539). Both diff $0.
- Investment income (CAUTION): GF carries "Net Increase/(Decrease) in Fair Market Value of Investments" (FY2005 = +$26,698,597) + large "Investment Income" lines (FY2025 = +$1,414,203,323, the largest single GF revenue line, from Permanent Mineral Trust Fund earnings). Both bookends positive but mark-to-market → recommend P2 clamp monitoring at EVERY load year.
- Scope vs NASBO ~2.43× (FY2025 ACFR $4.03B vs NASBO $1.65B) — driver is investment income + a $1.11B Federal line, both consolidated into GF. Accept-and-relabel honestly, flag the investment-income driver as distinct from the OK/WV/RI federal-passthrough mechanism.
</source_facts>
</context>

<tasks>

<task id="121-06-01" type="auto">
  <name>Task 1: Generate both WY loaders (UNITS=1 dollars, STATE node) via gen_state.py + download/extract FY2005–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/wy/ (gitignored PDFs + pdftotext + wy_all.json), scripts/processWYAcfr.js, scripts/processWYRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent DOLLARS entry like 'ND'; confirm UNITS + neg_note wiring)
    - _acfr-work/extract_gf.py (GF-column extractor — watch the FY2005-era older layout)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (WY Detail Block: dollars units, multi-era URL map, investment-income P2 caution, node disambiguation, poor-OCR pre-2005)
    - scripts/processNDAcfr.js (the closest v2.15 DOLLARS/UNITS=1 loader analog)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. For each FY across FY2005–FY2025, resolve the URL from the SOURCES map (era-specific — see source_facts) and download to `_acfr-work/wy/WY{YYYY}.pdf`. Soft-404 guard: `%PDF` magic + size > ~500KB; an HTML soft-404 must abort that FY. Run `pdftotext -table`, locate the Governmental Funds statement, run `extract_gf.py` to isolate the GENERAL FUND column (1st) for BOTH revenues and expenditures — verify older FY2005-era layouts extract the correct column. Assemble `_acfr-work/wy/wy_all.json` with verbatim whole-dollar figures (DOLLARS — no ×1,000); document hand-patches with printed-row evidence. **Check EVERY year's GF revenue lines for negatives** (investment-income mark-to-market) and confirm they route through the clamp path. Add `CONFIGS['WY']` to gen_state.py (**node 4009951b — the STATE node**, years=[cleanly-tied FYs], **UNITS=1**, multi-era sources map, head_note ~2.43× investment-income-driven, neg_note per-year P2 monitoring). Run `py -3 gen_state.py WY`. Cross-check FY2025 revenue stored = 4,027,001,270 (NOT ×1,000) and FY2005 = 1,590,602,744 — mismatch or 1000× inflation → STOP and re-inspect UNITS/column. OMIT (honest hole) any interior FY that 404s/won't tie; do NOT attempt FY1980–FY2004 (poor-OCR, out of scope). Run both loaders `--dry-run`: every present FY PASS, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processWYAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processWYAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processWYRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '4,027,001,270'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/wy/` holds a real PDF per FY2005–FY2025 FY; soft-404/HTML rejected; no FY1980–FY2004 attempted.
    - `node scripts/processWYAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints 4,027,001,270 and `--fy 2005` prints 1,590,602,744 (NOT ×1,000).
    - Both loaders carry `UNITS = 1` and resolve node `4009951b` (the STATE node); gen_state.py has `CONFIGS['WY']` with UNITS=1 + node 4009951b; wy_all.json exists with per-FY operating + revenue trees.
    - Any negative GF line (any year) routes through clampForRender (slice→0, signed magnitude in label, parent nets).
    - dataSource labels GAAP-basis-labelled; revenue RPC uses p_dataset_type:'revenue', dataset_id='wy-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both WY loaders exist with UNITS=1 (dollars) + node 4009951b, multi-era SOURCES map, wy_all.json holds every tied FY2005–FY2025 year, both bookends dry-run-tie at whole-dollar scale, per-year P2 clamp path wired, no live write.</done>
</task>

<task id="121-06-02" type="auto">
  <name>Task 2: Live-load WY STATE node (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processWYAcfr.js, scripts/processWYRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processWYAcfr.js + processWYRevenueAcfr.js (--fy path + node resolution + source-stamp + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; WY STATE node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets for the WY STATE node (4009951b) and RECORD pre-load NASBO operating rows (FY2023 $1,525,000K, FY2024 $1,654,000K). **Confirm the loader targets 4009951b and NOT the two 'Wyoming' city nodes (1604b5eb / dacac0e3)** before any write. For each present FY, run operating then revenue loaders live (--fy). Each must pass validate(), write via treasury_sync_budget_tree keyed to 4009951b (operating REPLACES the NASBO row where a NASBO year overlaps; revenue net-new), stamp GAAP source, self-clean its data_sources row. Abort+note any year failing the tie. Record loaded WY GF totals vs pre-load NASBO to confirm the ~2.43× accept-relabel divergence is the expected investment-income + federal mechanism, not a transcription error or a mis-scaled 1000× skew.
  </action>
  <verify>
    <automated>echo "WY STATE-node verification in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the WY STATE node (4009951b) has, per loaded FY, an operating row "Wyoming State ACFR — General Fund … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") + a revenue row, both non-null sources.
    - WY FY2025 revenue = 4,027,001,270 and FY2005 = 1,590,602,744 (whole dollars, no 1000× skew).
    - WY FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (4009951b, fy); no NASBO label remains on the state node.
    - The two 'Wyoming' city nodes (1604b5eb / dacac0e3) are UNCHANGED (row counts + labels identical to pre-load).
    - Pre-load NASBO + loaded ACFR totals recorded with the ~2.43× investment-income-driven divergence explained.
  </acceptance_criteria>
  <done>Each transcribed WY FY has operating + revenue rows on the STATE node 4009951b, whole-dollar (UNITS=1), GAAP-labelled + sourced; FY2023/FY2024 NASBO replaced in place; the two city nodes untouched; divergence recorded with no 1000× skew.</done>
</task>

<task id="121-06-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + node-disambiguation + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-06-WY-LOADLOG.md</files>
  <read_first>
    - scripts/processWYAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral lifecycle + node resolution)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative FY live (e.g. WY --fy 2025 op + rev), confirm 0 net change + 0 residue. Query treasury.budgets + data_sources to confirm: (a) every loaded WY FY has op + rev rows on node 4009951b, GAAP-labelled, non-null sources, whole-dollar scale; (b) 0 remaining NASBO operating rows on 4009951b (one op row per (4009951b, fy)); (c) honest-hole FYs ABSENT; (d) WY state node ≥1 revenue row (Money In); (e) 0 data_sources residue (LOAD-01); (f) **the two 'Wyoming' city nodes (1604b5eb / dacac0e3) unchanged** — explicit row-count + label check; (g) cohort untouched (sample existing ACFR nodes + one un-upgraded state). Append "Load Disposition" to 121-06-WY-LOADLOG.md (FYs loaded/skipped+reason, node-disambiguation confirmation, pre-load-NASBO-vs-loaded-ACFR with ~2.43× investment-income note, per-year P2 clamp results, NASBO-replacement confirmation, idempotency result). Note: this is the FINAL state — record the all-50-on-ACFR milestone status. Do NOT load beyond the tied window (no pre-FY2005); touch no state other than WY, and no 'Wyoming' city node.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-06-WY-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of WY --fy 2025 → 0 net change.
    - Every loaded WY FY has op + rev rows on node 4009951b, GAAP-labelled, non-null sources, whole-dollar scale; ZERO NASBO operating rows on the state node; one op row per (4009951b, fy).
    - WY state node ≥1 revenue row (Money In).
    - 0 data_sources residue for WY (LOAD-01).
    - The two 'Wyoming' city nodes (1604b5eb / dacac0e3) unchanged.
    - Honest-hole FYs ABSENT.
    - Existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-06-WY-LOADLOG.md finalized with the all-50-on-ACFR milestone note.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change + 0 residue; DB query confirms the WY STATE node fully ACFR-sourced (whole-dollar), NASBO replaced, Money In enabled, honest holes absent, the two city nodes + rest of the cohort unchanged; 121-06-WY-LOADLOG.md finalized noting all 50 states now on ACFR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sao.wyo.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the WY STATE node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-06-A | Tampering | writing to a 'Wyoming' CITY node instead of the STATE node | mitigate | loader hard-codes node 4009951b; Tasks 2–3 explicitly assert the two city nodes (1604b5eb / dacac0e3) unchanged before + after |
| T-121-06-B | Tampering | units mis-scale — applying ×1,000 to a DOLLARS state inflates WY 1000× | mitigate | UNITS=1 hard-set; bookends (4,027,001,270 / 1,590,602,744) asserted whole-dollar; explicit no-1000×-skew check |
| T-121-06-C | Tampering | negative investment-income line silently dropped or mis-signed (mark-to-market) | mitigate | per-year P2 clamp monitoring (not just bookends); clampForRender keeps signed magnitude in label, parent nets |
| T-121-06-D | Tampering | wrong-era URL / poor-OCR pre-2005 PDF parsed → garbled figures | mitigate | explicit multi-era per-FY map; FY2005 clean-text floor; FY1980–FY2004 excluded; %PDF + size guard; honest hole on failure |
| T-121-06-E | Tampering | wrong statement/column | mitigate | extract_gf.py isolates 1st column; bookend ties gate before transcription; older-layout check |
| T-121-06-F | Tampering | NASBO row left alongside ACFR row on the state node | mitigate | RPC keyed (4009951b, fy, 'operating') → UPDATE-in-place; Task 3 asserts one op row per (4009951b, fy), zero NASBO |
| T-121-06-G | Tampering | data_sources residue | mitigate | ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-06-H | Info Disclosure | silently inflating WY (~2.43×) without recording the investment-income driver | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP label; investment-income driver flagged distinctly |
| T-121-06-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both WY loaders `--dry-run`: every present FY PASS, no "sum ≠ total"; UNITS=1; node 4009951b.
- `processWYRevenueAcfr.js --dry-run --fy 2025` → 4,027,001,270; `--fy 2005` → 1,590,602,744 (whole dollars).
- Live: WY STATE node op + rev rows per loaded FY, GAAP-labelled, non-null sources, whole-dollar; NASBO FY2023/FY2024 replaced in place; the two 'Wyoming' city nodes unchanged.
- Re-run WY --fy 2025 → 0 net change; 0 data_sources residue (LOAD-01).
- WY state node ≥1 revenue row → Money In auto-enabled.
- Per-year P2 clamp results recorded; ~2.43× investment-income-driven accept-relabel recorded; existing ACFR nodes + a sample NASBO state unchanged.
- 121-06-WY-LOADLOG.md finalized noting all 50 states now on ACFR.
</verification>

<success_criteria>
The Wyoming STATE node (4009951b) is live on full State-ACFR GAAP across its clean-text durable window (target FY2005–FY2025, honest holes logged, FY1980–FY2004 poor-OCR excluded), whole-dollar (UNITS=1), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~2.43× investment-income-driven accept-relabel divergence recorded, per-year P2 clamp monitoring applied, the two 'Wyoming' city nodes demonstrably untouched, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Completing WY puts **all 50 states on ACFR**. Hands WY to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-06-SUMMARY.md` when done — record the WY FY window transcribed, any honest holes, the node-disambiguation confirmation (state node vs the two city nodes), the two bookend ties (whole-dollar), the NASBO-replacement confirmation, the ~2.43× investment-income-driven divergence, the per-year P2 clamp results, the idempotency re-run + 0-residue result, the Money-In auto-enable confirmation, and the all-50-states-on-ACFR milestone status.
</output>
