---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processOKAcfr.js
  - scripts/processOKRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-01-OK-LOADLOG.md
autonomous: true
requirements: [ACFR-48]
user_setup: []

must_haves:
  truths:
    - "Oklahoma state node (54233a91-919d-4a5f-9f24-2f9325250e64) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2002–FY2024), every loaded FY tying exactly ($0) to the printed GENERAL total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "OK units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL column, Total Revenues line): FY2024 = $30,604,464K, FY2002 = $9,568,595K (store ×1,000 raw dollars)"
    - "GENERAL column is the 1st of the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (General | Commissioners of the Land Office | Wildlife Lifetime Licenses | Tobacco Settlement Endowment | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are MOSTLY derivable: FY2000–FY2020 `https://oklahoma.gov/content/dam/ok/en/omes/documents/cafr{YYYY}.pdf`; FY2021–FY2023 `.../ACFR{YYYY}.pdf`; FY2024 breaks the pattern `.../acfr-2024.pdf`. Landing: oklahoma.gov/omes/divisions/central-accounting-reporting/financial-reporting/acfr-archives.html. FY2025 not yet published (2026-07-04) — honest hole, does not block recency floor"
    - "Pre-existing NASBO operating rows (OK FY2023 $7,752,000K, FY2024 $9,139,000K) are REPLACED in place at the same (54233a91, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (OK ACFR GF ~3.35× NASBO — the LARGEST in Batch 4; OK's GAAP GF consolidates nearly all state general-purpose taxes AND the full Federal Revenue line $13.78B into one fund) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows"
    - "No negative GF line items observed in FY2024 or FY2002 (Interest and Investment Revenue positive both years); interior years checked at load, P2 clamp wired but likely unexercised"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on OK once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processOKAcfr.js"
      provides: "OK GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processOKRevenueAcfr.js"
      provides: "OK GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-01-OK-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~3.35× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processOKAcfr.js / processOKRevenueAcfr.js"
      to: "treasury.budgets (OK state node 54233a91)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['OK']"
      to: "scripts/processOKAcfr.js + scripts/processOKRevenueAcfr.js"
      via: "gen_state.py code generation from ok/ok_all.json + per-FY SOURCES map"
      pattern: "General"
---

<objective>
Upgrade the **Oklahoma** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processOKAcfr.js` (GF spending-by-function, `operating`) + `scripts/processOKRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading OK ACFR PDFs FY2002–FY2024 into gitignored `_acfr-work/ok/`, extracting the **GENERAL column** (1st of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**OK recon was preserved + re-verified from v2.14** (byte-identical bookends, no rot). Units = THOUSANDS (UNITS=1000). Bookends: **FY2024 $30,604,464K, FY2002 $9,568,595K**. Widest scope divergence in Batch 4 (~3.35× NASBO — full Federal Revenue line consolidated into GF) — accept-and-relabel honestly, document prominently. FY2024 filename breaks the derivable pattern (`acfr-2024.pdf`); FY2025 not yet published (honest hole).

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-01-OK-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['OK']` entry (mirror the recent clean thousands entries like 'NE'): name='Oklahoma', abbr='OK', node='54233a91-919d-4a5f-9f24-2f9325250e64', years=[the FYs that cleanly tie], tolerance small (tied at $0), data='ok/ok_all.json', out='OK', base_url the OMES ACFR-archives landing page, sources={ per-FY URL map — cafr{YYYY}.pdf / ACFR{YYYY}.pdf / acfr-2024.pdf }, UNITS=1000. Use `head_note` for the ~3.35× accept-relabel scope note (full Federal Revenue line consolidated into GF). Then `py -3 gen_state.py OK` emits scripts/processOKAcfr.js + processOKRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor (anchors on the Total-revenues row's right edge). For OK, capture the revenue line items for the `revenue` tree AND the expenditure-by-function line items for the `operating` tree. Feeds ok/ok_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline with printed-row evidence.
- Generated loaders inherit the v2.15 shape: UNITS constant (1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle (delete-stale-by-dataset_id → insert → RPC/stamp → delete-by-id at end — leaves 0 residue). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ok-acfr-gf-operating' (name 'Oklahoma General Fund Operating Budget'), 'ok-acfr-gf-revenue' (name 'Oklahoma General Fund Revenue'), api_type='pdf_download', base_url = the OMES ACFR-archives landing page.
- NASBO baseline (live-confirmed): OK has 2 NASBO operating rows only — FY2023 $7,752,000K, FY2024 $9,139,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit (120-01..05 precedent — leave the tree clean).
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md OK Detail Block + Section tables — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = "General", 1st of 4 (General | Commissioners of the Land Office | Wildlife Lifetime Licenses | Tobacco Settlement Endowment | Total). Units = THOUSANDS. FY-end June 30.
- Clean window FY2002–FY2024 (22 yr, both bookends re-tie-confirmed $0). FY2025 not yet published (honest hole; recency floor already satisfied by FY2023+FY2024).
- URL map (confirmed application/pdf): FY2024 = `acfr-2024.pdf` (breaks pattern); FY2021–FY2023 = `ACFR{YYYY}.pdf`; FY2000–FY2020 = `cafr{YYYY}.pdf`. Base `https://oklahoma.gov/content/dam/ok/en/omes/documents/`. Landing: `https://oklahoma.gov/omes/divisions/central-accounting-reporting/financial-reporting/acfr-archives.html`.
- Bookend ties (GF Total Revenues): FY2024 $30,604,464K (4-col sum $31,259,646K); FY2002 $9,568,595K (5-col sum $9,609,878K incl. a small Nonmajor Capital Projects column). Both diff $0.
- Investment income positive both bookends (FY2024 +$459,743K, FY2002 +$96,796K) — no P2 clamp needed at bookends; check interior years.
- Scope vs NASBO ~3.35× (FY2024 ACFR $30.60B vs NASBO $9.14B) — LARGEST divergence in Batch 4. OK's GAAP GF consolidates nearly all state general-purpose taxes + the full Federal Revenue line ($13.78B) into one fund. Accept-and-relabel honestly, document prominently (TX/MI precedent).
</source_facts>
</context>

<tasks>

<task id="121-01-01" type="auto">
  <name>Task 1: Generate both OK loaders (UNITS=1000) via gen_state.py + download/extract FY2002–FY2024 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ok/ (gitignored PDFs + pdftotext + ok_all.json), scripts/processOKAcfr.js, scripts/processOKRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent thousands entry like 'NE'; confirm UNITS + sources + tolerance + head_note wiring)
    - _acfr-work/extract_gf.py (GF-column extractor — how it anchors on the Total-revenues row; how revenues + expenditures sections are each emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (OK Detail Block: URL map, units, bookends)
    - scripts/processNEAcfr.js (a recent gen_state.py clean thousands output — the exact generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). For each FY across the durable window (FY2002–FY2024), resolve the URL from the SOURCES map (era-specific: `cafr{YYYY}.pdf` / `ACFR{YYYY}.pdf` / `acfr-2024.pdf`) and download to `_acfr-work/ok/OK{YYYY}.pdf`. Re-check the landing page for a newly-posted FY2025 — include it only if it ties. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL column (1st) for BOTH the revenues section and the expenditures section. Assemble `_acfr-work/ok/ok_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names (thousands, unscaled at this stage); document any hand-patch inline with printed-row evidence. Add a `CONFIGS['OK']` entry to `_acfr-work/gen_state.py` (node 54233a91…, years=[the FYs that cleanly tie], UNITS=1000, tolerance small, data='ok/ok_all.json', out='OK', base_url the OMES landing page, sources=per-FY URL map, head_note recording the ~3.35× accept-relabel). Run `py -3 gen_state.py OK` to emit `scripts/processOKAcfr.js` + `scripts/processOKRevenueAcfr.js` (dataset_ids ok-acfr-gf-operating / ok-acfr-gf-revenue). Cross-check FY2024 revenue stored = 30,604,464×1000 and FY2002 = 9,568,595×1000 — if either bookend does not match, STOP and re-inspect column/statement selection before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processOKAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processOKAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processOKRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '30,604,464,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ok/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected.
    - `node scripts/processOKAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2024` prints 30,604,464,000 and `--fy 2002` prints 9,568,595,000 (×1,000).
    - Both loaders carry `UNITS = 1000`; `_acfr-work/gen_state.py` has a `CONFIGS['OK']` entry; `_acfr-work/ok/ok_all.json` exists with per-FY operating + revenue trees.
    - Any interior negative GF line routes through clampForRender (slice→0, signed magnitude in label) if present.
    - dataSource labels are GAAP-basis-labelled ("Oklahoma State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='ok-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both OK loaders exist (generated by gen_state.py) with UNITS=1000, per-FY SOURCES map, ok_all.json holds every tied FY, both bookends (30,604,464K / 9,568,595K) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="121-01-02" type="auto">
  <name>Task 2: Live-load OK (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processOKAcfr.js, scripts/processOKRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processOKAcfr.js + scripts/processOKRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; OK node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets for the OK node (54233a91) and RECORD the pre-load NASBO operating rows (FY2023 $7,752,000K, FY2024 $9,139,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processOKAcfr.js --fy <YYYY>` then `node scripts/processOKRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (54233a91, fy, 'operating') where a NASBO year overlaps; revenue inserts net-new at (54233a91, fy, 'revenue')), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded OK GF operating + revenue totals against the pre-load NASBO totals to confirm the ~3.35× accept-relabel divergence is the expected federal-consolidation mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "OK row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the OK node has, per loaded FY, an operating row "Oklahoma State ACFR — General … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both with non-null source_url/source_date.
    - OK FY2024 revenue row total = 30,604,464,000 and FY2002 = 9,568,595,000 (×1,000).
    - OK FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (OK, fy), GAAP-labelled; no "NASBO" label remains on the OK node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~3.35× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed OK FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="121-01-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-01-OK-LOADLOG.md</files>
  <read_first>
    - scripts/processOKAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. OK --fy 2024 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place) AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources to confirm: (a) every loaded OK FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) OK has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (OK, fy)); (c) honest-hole FYs (incl. FY2025 if still unpublished) ABSENT; (d) Money In auto-enable — OK has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for OK; (f) cohort untouched: spot-check a sample of existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK) + one un-upgraded state unchanged. Append a "Load Disposition" section to 121-01-OK-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the ~3.35× accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than OK.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-01-OK-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of OK --fy 2024 (operating + revenue) reports 0 net change (idempotent never-overwrite).
    - DB query: every loaded OK FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; OK has ZERO remaining "NASBO" operating rows; exactly one operating row per (OK, fy).
    - OK has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for OK (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-01-OK-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms OK fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 121-01-OK-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oklahoma.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the OK node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01-A | Tampering | units mis-scale (applying/omitting the ×1,000) | mitigate | UNITS=1000 hard-set; bookend totals (30,604,464K / 9,568,595K) asserted in dry-run + live |
| T-121-01-B | Tampering | wrong statement/column (Statement of Activities, or a non-General column) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-121-01-C | Tampering | wrong-era URL → wrong-year or 404 PDF (cafr/ACFR/acfr-2024 naming) | mitigate | %PDF magic + size guard; honest hole if a year 404s; landing-page cross-check |
| T-121-01-D | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (OK, fy), zero NASBO labels |
| T-121-01-E | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.15 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-01-F | Info Disclosure | silently inflating OK (~3.35×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-121-01-G | Tampering | write to a state other than OK | mitigate | loader resolves only the OK node id; Task 3 spot-checks cohort unchanged |
| T-121-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + existing python tooling |
</threat_model>

<verification>
- Both OK loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processOKRevenueAcfr.js --dry-run --fy 2024` → 30,604,464,000; `--fy 2002` → 9,568,595,000.
- Live: OK node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (OK, fy)).
- Re-run OK --fy 2024 → 0 net change; 0 data_sources residue for OK (LOAD-01).
- OK ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~3.35×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 121-01-OK-LOADLOG.md finalized.
</verification>

<success_criteria>
The Oklahoma state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2002–FY2024, FY2025 honest hole), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~3.35× accept-relabel divergence recorded at load, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands OK to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-01-SUMMARY.md` when done — record the OK FY window actually transcribed, any FY omitted as an honest hole (incl. FY2025 if unpublished), the two bookend tie confirmations, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence (~3.35×), the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
