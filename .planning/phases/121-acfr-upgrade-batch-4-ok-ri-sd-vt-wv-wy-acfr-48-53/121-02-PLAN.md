---
phase: 121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53
plan: "121-02"
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processRIAcfr.js
  - scripts/processRIRevenueAcfr.js
  - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-02-RI-LOADLOG.md
autonomous: true
requirements: [ACFR-49]
user_setup: []

must_haves:
  truths:
    - "Rhode Island state node (483f02b4-2167-4e3d-9f5c-0f3ed83be2e6) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2006–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "RI units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL column, Total revenues line): FY2025 = $10,095,792K, FY2006 = $4,585,920K (store ×1,000 raw dollars)"
    - "GENERAL column is the 1st of the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances (General | Intermodal Surface Transportation | Rhode Island Capital Plan | Other Governmental Funds | Total) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are OPAQUE per-year (date-stamped Drupal directory) — no derivable pattern; the SOURCES map enumerates every year explicitly from the financial-reports page (NC/GA/WV precedent). FY2022 filename has a literal trailing space before .pdf ('ACFR%206-30-2022%20.pdf') — verify the exact string at load"
    - "Pre-existing NASBO operating rows (RI FY2023 $5,075,000K, FY2024 $5,236,000K) are REPLACED in place at the same (483f02b4, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (RI ACFR GF ~1.93× NASBO — a large Federal grants line ~$4.55B of $10.1B GF, ~45%, consolidated into GF; MD/GA mechanism) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows"
    - "Investment income column header includes '(loss)' possibility — both bookends positive (FY2025 +$47,546K, FY2006 +$2,000K) but check intervening years at load; P2 clamp wired"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on RI once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processRIAcfr.js"
      provides: "RI GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processRIRevenueAcfr.js"
      provides: "RI GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-02-RI-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, ~1.93× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processRIAcfr.js / processRIRevenueAcfr.js"
      to: "treasury.budgets (RI state node 483f02b4)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['RI']"
      to: "scripts/processRIAcfr.js + scripts/processRIRevenueAcfr.js"
      via: "gen_state.py code generation from ri/ri_all.json + explicit per-FY SOURCES map"
      pattern: "General"
---

<objective>
Upgrade the **Rhode Island** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processRIAcfr.js` (GF spending-by-function, `operating`) + `scripts/processRIRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading RI ACFR PDFs FY2006–FY2025 into gitignored `_acfr-work/ri/`, extracting the **GENERAL column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

Units = THOUSANDS (UNITS=1000). Bookends: **FY2025 $10,095,792K, FY2006 $4,585,920K**. ~1.93× scope divergence (federal grants ~45% of GF consolidated in — MD/GA mechanism), accept-and-relabel honestly. **URLs are opaque per-year** (date-stamped Drupal dir) — enumerate every year explicitly; FY2022 filename has a literal trailing space (`%20`) before `.pdf`.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `121-02-RI-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md

<interfaces>
<!-- The v2.15 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['RI']` entry (mirror a recent thousands entry, with an EXPLICIT per-FY sources map like the opaque-URL states): name='Rhode Island', abbr='RI', node='483f02b4-2167-4e3d-9f5c-0f3ed83be2e6', years=[the FYs that cleanly tie], tolerance small, data='ri/ri_all.json', out='RI', base_url the controller.admin.ri.gov financial-reports page, sources={ explicit per-FY URL map }, UNITS=1000. Use `head_note` for the ~1.93× accept-relabel scope note. Then `py -3 gen_state.py RI` emits scripts/processRIAcfr.js + processRIRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For RI, capture revenue line items for the `revenue` tree AND expenditure-by-function line items for the `operating` tree. Feeds ri/ri_all.json. Document any hand-patch inline with printed-row evidence.
- Generated loaders inherit the v2.15 shape (see 121-01 interfaces): UNITS constant (1000), SOURCES map, validate(fy), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp UPDATE, clampForRender, ephemeral data_sources lifecycle. Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ri-acfr-gf-operating' (name 'Rhode Island General Fund Operating Budget'), 'ri-acfr-gf-revenue' (name 'Rhode Island General Fund Revenue'), api_type='pdf_download', base_url = the controller.admin.ri.gov financial-reports page.
- NASBO baseline (live-confirmed): RI has 2 NASBO operating rows only — FY2023 $5,075,000K, FY2024 $5,236,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows.
- DB access: mcp__supabase-local__execute_sql if present; else a small ad-hoc @supabase/supabase-js Node script deleted before the final commit (120-01..05 precedent).
</interfaces>

<source_facts>
<!-- From 117-BATCH4-SOURCES.md RI Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GF = "General", 1st of 4 (General | Intermodal Surface Transportation | Rhode Island Capital Plan | Other Governmental Funds | Total). Units = THOUSANDS. FY-end June 30.
- Clean window FY2006–FY2025 (20 yr, both bookends tie-confirmed $0). Pre-FY2006 files exist but not tie-confirmed within budget — honest holes / out of scope.
- URL map (OPAQUE, enumerate from financial-reports page): FY2025 `.../2026-06/State%20of%20Rhode%20Island%20ACFR%20FY2025%20-%20FINAL.pdf`; FY2024 `.../2025-03/2024%20State%20of%20Rhode%20Island%20ACFR%206.30.24%20-%20Final.pdf`; FY2023 `.../2024-02/ACFR%206-30-2023.pdf`; FY2022 `.../2023-01/ACFR%206-30-2022%20.pdf` (literal trailing %20); FY2021 `.../2022-04/ACFR%206-30-2021.pdf`; FY2017–FY2020 `.../2022-04/CAFR%2006-30-{YYYY}.pdf`; FY2006–FY2016 `.../2025-01/{YYYY}.pdf`. Base `https://controller.admin.ri.gov/sites/g/files/xkgbur621/files/`. Landing: `https://controller.admin.ri.gov/financial-reporting-and-accounting/financial-reports`.
- Bookend ties (GF Total revenues): FY2025 $10,095,792K (4-col sum $11,394,132K); FY2006 $4,585,920K (4-col sum $5,218,477K). Both diff $0.
- Investment income positive both bookends (FY2025 +$47,546K, FY2006 +$2,000K); header includes "(loss)" — check interior years.
- Scope vs NASBO ~1.93× (FY2025 ACFR $10.10B vs NASBO $5.24B) — federal grants ~45% of GF consolidated in (MD/GA mechanism). Accept-and-relabel honestly (TX precedent).
</source_facts>
</context>

<tasks>

<task id="121-02-01" type="auto">
  <name>Task 1: Generate both RI loaders (UNITS=1000) via gen_state.py + download/extract FY2006–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ri/ (gitignored PDFs + pdftotext + ri_all.json), scripts/processRIAcfr.js, scripts/processRIRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy a recent thousands entry with an explicit per-FY sources map)
    - _acfr-work/extract_gf.py (GF-column extractor)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH4-SOURCES.md (RI Detail Block: opaque URL map, bookends, trailing-space FY2022 gotcha)
    - scripts/processOKAcfr.js (the just-completed 121-01 loader — the exact v2.15 generated shape)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. For each FY across FY2006–FY2025, use the EXPLICIT per-FY URL from the SOURCES map (re-verify each still resolves — Drupal dirs are stable once published; watch the FY2022 literal trailing `%20`) and download to `_acfr-work/ri/RI{YYYY}.pdf`. Soft-404 guard: `%PDF` magic + size > ~500KB; an HTML soft-404 must abort that FY. Run `pdftotext -table`, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, and run `extract_gf.py` to isolate the GENERAL column (1st) for BOTH revenues and expenditures sections. Assemble `_acfr-work/ri/ri_all.json` (thousands, unscaled at this stage); document hand-patches with printed-row evidence. Add `CONFIGS['RI']` to gen_state.py (node 483f02b4…, years=[cleanly-tied FYs], UNITS=1000, explicit sources map, head_note ~1.93×). Run `py -3 gen_state.py RI`. Cross-check FY2025 revenue = 10,095,792×1000 and FY2006 = 4,585,920×1000 — mismatch on either bookend → STOP and re-inspect column/statement. OMIT (honest hole) any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`: every present FY PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processRIAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processRIAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processRIRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '10,095,792,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ri/` holds a real PDF per downloadable FY; soft-404/HTML rejected.
    - `node scripts/processRIAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints 10,095,792,000 and `--fy 2006` prints 4,585,920,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['RI']`; ri_all.json exists with per-FY operating + revenue trees.
    - Any interior negative GF line routes through clampForRender.
    - dataSource labels GAAP-basis-labelled; revenue RPC uses p_dataset_type:'revenue', dataset_id='ri-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both RI loaders exist with UNITS=1000, explicit per-FY SOURCES map, ri_all.json holds every tied FY, both bookends dry-run-tie, P2 clamp wired, no live write.</done>
</task>

<task id="121-02-02" type="auto">
  <name>Task 2: Live-load RI (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processRIAcfr.js, scripts/processRIRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processRIAcfr.js + processRIRevenueAcfr.js (--fy path + source-stamp + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; RI node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query the RI node (483f02b4) and RECORD pre-load NASBO operating rows (FY2023 $5,075,000K, FY2024 $5,236,000K). For each present FY, run operating then revenue loaders live (--fy). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row where a NASBO year overlaps; revenue net-new), stamp source_url/source_date/data_source (GAAP label), self-clean its data_sources row. Abort+note any year failing the tie — never force-write. Record loaded RI GF totals vs pre-load NASBO to confirm the ~1.93× accept-relabel divergence is the expected federal-grants-consolidation mechanism.
  </action>
  <verify>
    <automated>echo "RI verified in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, an operating row "Rhode Island State ACFR — General … (FY{fy} actual, GAAP basis)" (NOT "NASBO…") + a revenue row, both non-null source_url/source_date.
    - RI FY2025 revenue = 10,095,792,000 and FY2006 = 4,585,920,000.
    - RI FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (RI, fy), GAAP-labelled; no NASBO label remains.
    - Pre-load NASBO + loaded ACFR totals recorded with the ~1.93× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed RI FY has operating + revenue rows, thousands-scaled, GAAP-labelled + sourced; FY2023/FY2024 NASBO replaced in place; accept-relabel divergence recorded.</done>
</task>

<task id="121-02-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-02-RI-LOADLOG.md</files>
  <read_first>
    - scripts/processRIAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative FY live (e.g. RI --fy 2025 op + rev), confirm 0 net change + 0 data_sources residue. Query treasury.budgets + data_sources to confirm: (a) every loaded RI FY has op + rev rows, GAAP-labelled, non-null sources; (b) 0 remaining NASBO operating rows (one op row per (RI, fy)); (c) honest-hole FYs ABSENT; (d) RI ≥1 revenue row (Money In); (e) 0 data_sources residue (LOAD-01); (f) cohort untouched (sample existing ACFR nodes + one un-upgraded state). Append "Load Disposition" to 121-02-RI-LOADLOG.md (FYs loaded/skipped+reason, pre-load-NASBO-vs-loaded-ACFR with ~1.93× note, NASBO-replacement confirmation, idempotency result). Do NOT load beyond the tied window; touch no state other than RI.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 121-02-RI-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of RI --fy 2025 → 0 net change.
    - Every loaded RI FY has op + rev rows, GAAP-labelled, non-null sources; ZERO NASBO operating rows; one op row per (RI, fy).
    - RI ≥1 revenue row (Money In).
    - 0 data_sources residue for RI (LOAD-01).
    - Honest-hole FYs ABSENT.
    - Existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 121-02-RI-LOADLOG.md finalized.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change + 0 residue; DB query confirms RI fully ACFR-sourced, NASBO replaced, Money In enabled, honest holes absent, cohort unchanged; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller.admin.ri.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the RI node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-02-A | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (10,095,792K / 4,585,920K) asserted dry-run + live |
| T-121-02-B | Tampering | wrong statement/column | mitigate | extract_gf.py isolates 1st column; bookend ties gate before transcription |
| T-121-02-C | Tampering | wrong opaque URL / trailing-space FY2022 filename → wrong-year or 404 | mitigate | explicit per-FY URL map re-verified at load; %PDF magic + size guard; honest hole if a year 404s |
| T-121-02-D | Tampering | NASBO row left alongside ACFR row | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one op row per (RI, fy), zero NASBO |
| T-121-02-E | Tampering | data_sources residue | mitigate | ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-121-02-F | Info Disclosure | silently inflating RI (~1.93×) without recording | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP label |
| T-121-02-G | Tampering | write to a state other than RI | mitigate | loader resolves only the RI node id; Task 3 cohort spot-check |
| T-121-02-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both RI loaders `--dry-run`: every present FY PASS, no "sum ≠ total"; UNITS=1000.
- `processRIRevenueAcfr.js --dry-run --fy 2025` → 10,095,792,000; `--fy 2006` → 4,585,920,000.
- Live: RI node op + rev rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run RI --fy 2025 → 0 net change; 0 data_sources residue (LOAD-01).
- RI ≥1 revenue row → Money In auto-enabled.
- ~1.93× accept-relabel recorded; existing ACFR nodes + a sample NASBO state unchanged.
- 121-02-RI-LOADLOG.md finalized.
</verification>

<success_criteria>
The Rhode Island state node is live on full State-ACFR GAAP across its clean durable window (target FY2006–FY2025, honest holes logged), thousands-scaled, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.93× accept-relabel divergence recorded, idempotent never-overwrite proven, Money In auto-enabled, cohort untouched. Hands RI to Phase 124.
</success_criteria>

<output>
Create `.planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-02-SUMMARY.md` when done — record the RI FY window transcribed, any honest holes, the two bookend ties, the NASBO-replacement confirmation, the ~1.93× divergence, the idempotency re-run + 0-residue result, and Money-In auto-enable.
</output>
