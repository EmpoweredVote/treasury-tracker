---
phase: 21-gresham-or-revenue-load
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - .planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "treasury.budgets has 4 Gresham rows with dataset_type='revenue' (FY2023-FY2026), each total in the $400M-$525M range"
    - "The 4 existing Gresham operating data_source rows still exist and are unchanged after the revenue load (no collision/overwrite)"
    - "No Gresham revenue budget row's tree contains a 'Beginning Balance' or 'Total Resources' node"
    - "Citizens can select Gresham, OR in the app and see a 'Money In' tab with revenue categories alongside the existing Budget tab"
  artifacts:
    - path: ".planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md"
      provides: "Phase 21 verification report with DB evidence + human-verify result"
      contains: "dataset_type"
  key_links:
    - from: "scripts/processGresham.js loadFiscalYear()"
      to: "treasury.budgets (dataset_type='revenue')"
      via: "treasury_sync_budget_tree RPC p_dataset_type='revenue'"
      pattern: "revenue"
    - from: "treasury.budgets revenue rows"
      to: "App.tsx Money In tab"
      via: "available_datasets dataset_type='revenue' auto-discovery"
      pattern: "Money In"
---

<objective>
Run the live Gresham revenue load for all 4 fiscal years using the pipeline built in Plan 01, optionally enrich revenue categories (cost-gated), verify the data in the database and the live app, and write the phase verification report.

Purpose: Complete the phase goal — citizens can view Gresham's "Money In" data alongside its operating budget. The UI auto-discovers revenue data (App.tsx, verified in RESEARCH), so no frontend changes are needed; this plan just loads the data and confirms it renders.

Output: 4 revenue rows in treasury.budgets, optional enrichment, and 21-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md
@.planning/phases/21-gresham-or-revenue-load/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Live-load Gresham revenue for FY2023-FY2026 and verify in DB</name>
  <files>.planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md</files>
  <read_first>
    - scripts/processGresham.js (the --revenue pipeline produced by Plan 01)
    - .planning/phases/21-gresham-or-revenue-load/21-01-SUMMARY.md (Plan 01 dry-run confirmed totals to compare against)
    - .planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md (Schema and DB Context: Gresham municipality_id 5d4675f1-c207-4d7b-a346-85a799da0d4d; post-load verification SQL; expected totals)
    - .planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md (format/structure to mirror for the Phase 21 report)
  </read_first>
  <action>
    Run the live revenue load: `node scripts/processGresham.js --revenue` (no --dry-run). This writes 4 revenue budget rows via treasury_sync_budget_tree with p_dataset_type='revenue', creating 4 new data_source rows named `Gresham Revenue Budget FY2023..FY2026` (api_type pdf_download, dataset_id fyNNNN, dataset_type revenue).

    Then verify in the database using the mcp__supabase-local tools (apply via execute_sql; do NOT ask the user to paste into the Supabase Dashboard, per project convention). Run:
    - A SELECT on treasury.budgets WHERE municipality_id='5d4675f1-c207-4d7b-a346-85a799da0d4d' AND dataset_type='revenue' ORDER BY fiscal_year — expect 4 rows, totals in $400M-$525M band (NOT $731M-$896M).
    - A SELECT on treasury.data_sources WHERE municipality_id='5d4675f1-c207-4d7b-a346-85a799da0d4d' ORDER BY dataset_type, name — confirm BOTH the 4 pre-existing operating rows AND the 4 new revenue rows exist (no operating row was overwritten — collision-avoidance check).
    - Inspect one revenue row's tree JSON (e.g. FY2026) and confirm it contains no node named "Beginning Balance" or "Total Resources".

    Run idempotency check: execute `node scripts/processGresham.js --revenue` a second time and confirm the DB still has exactly 4 revenue rows (delete-then-insert idempotency, no duplicates).

    Write `.planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md` modeled on 20-VERIFICATION.md: include the Observable Truths table with live DB evidence, Required Artifacts, Key Link verification, and a Human Verification Required section for the app UI check (handed to Task 2). Mark the UI truth as HUMAN NEEDED until Task 2 confirms it.
  </action>
  <verify>
    <automated>node scripts/processGresham.js --revenue --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - After the live load, a DB query for municipality_id='5d4675f1-c207-4d7b-a346-85a799da0d4d' AND dataset_type='revenue' returns exactly 4 rows (FY2023, FY2024, FY2025, FY2026) (behavior assertion via mcp__supabase-local execute_sql).
    - Each of the 4 revenue total_budget values is between 400,000,000 and 525,000,000 (behavior assertion).
    - A DB query for the same municipality_id returns 4 data_source rows with dataset_type='operating' AND 4 with dataset_type='revenue' = 8 total; the 4 operating rows are still named `Gresham Operating Budget FY...` (no-regression / collision assertion).
    - The FY2026 revenue tree JSON contains no node with name "Beginning Balance" or "Total Resources" (behavior assertion).
    - Re-running `node scripts/processGresham.js --revenue` leaves exactly 4 revenue rows (idempotency assertion).
    - .planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md exists and contains an Observable Truths table referencing dataset_type='revenue' with the 4 FY totals (source assertion).
  </acceptance_criteria>
  <done>4 Gresham revenue rows live in treasury.budgets with totals in the $400M-$525M band, 8 total data_source rows (4 operating preserved + 4 revenue new), no Beginning Balance/Total Resources nodes, idempotent re-run; 21-VERIFICATION.md drafted with DB evidence and a pending human-UI check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Gresham Money In tab in the app, then finalize verification</name>
  <read_first>
    - .planning/phases/21-gresham-or-revenue-load/21-VERIFICATION.md (the report drafted in Task 1)
    - .planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md (UI verification section: expected revenue band, 10-category display, Money In tab auto-display)
  </read_first>
  <what-built>
    Loaded 4 fiscal years of Gresham revenue data (dataset_type='revenue') into treasury.budgets. The app (App.tsx) auto-discovers revenue datasets and shows a "Money In" tab — no frontend code was changed. Optional category enrichment was evaluated (see note below).
  </what-built>
  <how-to-verify>
    1. Start the app (`npm run dev`) or open treasurytracker.empowered.vote.
    2. Open the city/entity picker and select Gresham, OR (listed under "Oregon", alongside Portland).
    3. Confirm a "Money In" tab appears next to the existing "Budget" tab.
    4. Select the Money In tab. Confirm revenue categories render (Taxes, Intergovernmental, Charges for Services, Interfund Transfers, Utility License Fees, etc.) — about 10 categories, NOT a single "Total Resources" or "Beginning Balance" row.
    5. Confirm the displayed revenue total per fiscal year is in the ~$400M-$525M range (FY2026 ~$512M), NOT $731M-$896M (that would mean Beginning Balance leaked in).
    6. Switch the fiscal-year selector across FY2023-FY2026 and confirm each year shows revenue data.
  </how-to-verify>
  <action>
    Before presenting the checkpoint, evaluate enrichment per RESEARCH Pitfall 5 / Enrichment Decision (Claude's discretion): run `node scripts/enrichCategories.js --city Gresham --state OR --year 2026 --dry-run` to see what would be enriched. Revenue category names are plain English, so enrichment value is expected to be LOW. If the dry-run output adds genuine citizen value (e.g., for "Internal Svc Chrg", "Financing Proceeds", "Interfund Transfers"), run it live (`--year` per FY as needed) — cost at ~10 categories is well under $0.01, far below the $5 threshold; estimate before running per the API cost rule. If the dry-run output is trivial, skip enrichment and record the skip decision in the verification report.

    After the human confirms the UI, update 21-VERIFICATION.md: flip the app-UI truth to VERIFIED with the human result, record the enrichment decision (run or skipped, with rationale), and set the report status to complete/passed.
  </action>
  <resume-signal>Type "approved" if the Money In tab shows ~10 revenue categories with totals in the $400M-$525M range across FY2023-FY2026, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node → Supabase | Service-key writes via treasury_sync_budget_tree RPC |
| Node → Anthropic API (conditional) | enrichCategories.js call if enrichment is run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Tampering | revenue load overwriting operating data_source rows | mitigate | Post-load DB check confirms 4 operating + 4 revenue data_source rows coexist; parametric dataset_type lookup (from Plan 01) prevents overwrite. |
| T-21-03 | Information Disclosure | Beginning Balance inflating public-facing revenue total | mitigate | REVENUE_SKIP excludes Beginning Balance + Total Resources; verified by $400M-$525M band check in DB and UI. |
| T-21-04 | Denial (cost) | enrichCategories.js Anthropic API spend | mitigate | Dry-run first; ~10 plain-English categories cost <$0.01; $5 threshold enforced; skip if low value. |
</threat_model>

<verification>
- DB: 4 revenue rows (FY2023-FY2026) in $400M-$525M band; 8 total data_source rows (4 operating preserved + 4 revenue).
- No Beginning Balance / Total Resources node in any revenue tree.
- Idempotent re-run leaves exactly 4 revenue rows.
- Human-confirmed: Money In tab shows ~10 categories per FY in the correct band.
</verification>

<success_criteria>
- 4 Gresham revenue budget rows live with correct totals and no inflation rows.
- Operating data_source rows preserved (no collision).
- Money In tab verified in the app across all 4 fiscal years.
- 21-VERIFICATION.md complete with DB evidence, human-verify result, and enrichment decision recorded.
</success_criteria>

<output>
Create `.planning/phases/21-gresham-or-revenue-load/21-02-SUMMARY.md` when done.
</output>
