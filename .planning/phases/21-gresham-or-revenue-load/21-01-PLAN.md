---
phase: 21-gresham-or-revenue-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extractGresham.py
  - scripts/processGresham.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "python scripts/extractGresham.py <pdf> --mode revenue prints Resources-section rows (category + adopted_amount) for the given PDF"
    - "node scripts/processGresham.js --revenue --dry-run prints 4 fiscal years, each with 10 revenue categories and a total in the $400M-$525M range"
    - "Beginning Balance and Total Resources never appear as nodes in revenue dry-run output"
    - "Operating dry-run (node scripts/processGresham.js --dry-run) still produces unchanged FY2023-FY2026 operating totals (no regression)"
  artifacts:
    - path: "scripts/extractGresham.py"
      provides: "extract_revenue() function + argparse --mode operating|revenue dispatch"
      contains: "def extract_revenue"
    - path: "scripts/processGresham.js"
      provides: "--revenue flag, buildRevenueTree(), parametric upsertDataSource/loadFiscalYear/processPDF/extractPDF datasetType+mode"
      contains: "buildRevenueTree"
  key_links:
    - from: "scripts/processGresham.js extractPDF()"
      to: "scripts/extractGresham.py --mode revenue"
      via: "spawnSync args array push('--mode','revenue')"
      pattern: "--mode.*revenue"
    - from: "scripts/processGresham.js loadFiscalYear()"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type: datasetType"
      pattern: "p_dataset_type"
---

<objective>
Extend the existing Gresham budget pipeline with a revenue ("Money In") extraction mode, following the proven Phase 19 Portland pattern. Add `extract_revenue()` + `--mode` argparse to `scripts/extractGresham.py`, and add a `--revenue` flag plus parametric revenue path to `scripts/processGresham.js`. Validate end-to-end via dry-run against all 4 on-disk Gresham PDFs.

Purpose: Gresham currently shows only operating budget. The Resources section of the same 4 adopted-budget PDFs (already on disk from Phase 20) holds revenue categories. This plan builds the extraction/load capability; Plan 02 runs the live load.

Output: Modified `extractGresham.py` and `processGresham.js`. No new files, no new packages, no DB writes (dry-run only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md
@.planning/phases/21-gresham-or-revenue-load/21-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add extract_revenue() and --mode argparse to extractGresham.py</name>
  <files>scripts/extractGresham.py</files>
  <read_first>
    - scripts/extractGresham.py (current state: extract_budget() at lines 69-166, parse_money() lines 34-45, parse_fy_from_header() lines 48-66, SKIP_ROWS lines 20-31, __main__ lines 169-174)
    - scripts/extractPortland.py (lines 318-327: argparse --mode dispatch pattern to copy; extract_revenue() structure)
    - .planning/phases/21-gresham-or-revenue-load/21-PATTERNS.md (exact loop/token-splitting/OCR-fix/page-guard snippets to reuse)
    - .planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md (Resources section structure, REVENUE_SKIP set, NORMALIZE dict, expected totals)
  </read_first>
  <action>
    Add a new `extract_revenue(pdf_path)` function to scripts/extractGresham.py. It MUST reuse the existing helpers `parse_money()`, `parse_fy_from_header()`, and the same `pdfplumber.open` + `page.extract_text()` + line-splitting loop structure as `extract_budget()` (NOT extract_tables() — that returns empty on this page).

    Structural differences from extract_budget():
    - Section gating: set `in_resources = True` when a stripped, whitespace-collapsed line equals `'Resources'`; set `in_resources = False` when it equals `'Requirements'` (the inverse of extract_budget's `in_requirements` logic). Only parse rows while `in_resources` is True.
    - Page guard: keep the existing `'Resources and Requirements' not in text or 'All Funds' not in text` continue-guard, AND add a TOC-skip guard `if 'Taxes' not in text: continue` immediately after it.
    - Fiscal year parsing: copy the lines[:8] + parse_fy_from_header loop verbatim from extract_budget().
    - Row name field is `category` (not `department`).
    - Define two local constants: `REVENUE_SKIP = {'Total Resources', 'Beginning Balance'}` and `NORMALIZE = {'Internal Service Charges': 'Internal Svc Chrg'}`.
    - Reuse the exact token-splitting logic (name_tokens vs num_tokens, `in_nums` flag, `len(num_tokens) < 6` guard), the OCR split-number fix (num_tokens[-2] + num_tokens[-1] concat when second-to-last is 1-3 digits and last starts with 3+ digits and contains a comma), and `parse_money(adopted_raw)` with the `adopted <= 0: continue` guard.
    - After computing `category = re.sub(r'\s+', ' ', ' '.join(name_tokens)).strip()`, apply `if category in REVENUE_SKIP: continue`, then `category = NORMALIZE.get(category, category)` before appending.
    - Append dicts with keys `category`, `adopted_amount`, `fiscal_year`, `page_num`.
    - Preserve the `if results: break` single-page stop after the inner line loop, and the trailing None-fiscal_year warning block.

    Replace the simple `__main__` block (lines 169-174) with an argparse block mirroring scripts/extractPortland.py lines 318-327: positional `pdf_path`, optional `--mode` with `choices=['operating', 'revenue']` default `'operating'`. Description string should read 'Gresham budget PDF extractor'. Dispatch: `data = extract_revenue(args.pdf_path) if args.mode == 'revenue' else extract_budget(args.pdf_path)`, then `print(json.dumps(data, indent=2))`.

    Do NOT modify extract_budget(), SKIP_ROWS, parse_money(), or parse_fy_from_header() — they are verified working in Phase 20.
  </action>
  <verify>
    <automated>python scripts/extractGresham.py "docs/Gresham/fy2025-26.pdf" --mode revenue</automated>
  </verify>
  <acceptance_criteria>
    - scripts/extractGresham.py contains `def extract_revenue(` (source assertion).
    - scripts/extractGresham.py __main__ block contains `argparse` and `choices=['operating', 'revenue']` (source assertion).
    - Running `python scripts/extractGresham.py "docs/Gresham/fy2025-26.pdf" --mode revenue` exits 0 and prints a JSON array of 10 objects, each with keys `category`, `adopted_amount`, `fiscal_year`, `page_num` (behavior assertion).
    - The printed JSON contains NO object whose `category` equals `"Beginning Balance"` or `"Total Resources"` (behavior assertion).
    - The printed JSON `category` values include `"Taxes"`, `"Intergovernmental"`, `"Charges for Services"`, and `"Internal Svc Chrg"` (FY2026 uses the short form) (behavior assertion).
    - Running the same command with `--mode operating` (or no --mode) still prints department rows with a `department` key and FY2026 total of 330652078 when summed (no-regression assertion).
  </acceptance_criteria>
  <done>extract_revenue() and argparse --mode dispatch added; revenue extraction of FY2026 PDF prints 10 plain-English revenue categories with no Beginning Balance / Total Resources rows; operating extraction unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add --revenue mode pipeline to processGresham.js and validate all 4 FYs via dry-run</name>
  <files>scripts/processGresham.js</files>
  <read_first>
    - scripts/processGresham.js (current state: parseArgs lines 274-280, extractPDF lines 70-81, buildOperatingTree lines 99-122, upsertDataSource lines 143-173, loadFiscalYear lines 176-204, processPDF lines 207-270, main lines 273-314, SANITY_MAX line 94)
    - scripts/processPortland.js (lines 103-114 buildRevenueTree, 163-198 parametric upsertDataSource, 201-229 parametric loadFiscalYear, 232-294 processPDF mode dispatch, 297-308 parseArgs+mode)
    - .planning/phases/21-gresham-or-revenue-load/21-PATTERNS.md (sections 1-8 with exact Gresham adaptations)
    - .planning/phases/21-gresham-or-revenue-load/21-RESEARCH.md (dataset_id collision avoidance, expected per-FY revenue totals)
  </read_first>
  <action>
    Modify scripts/processGresham.js to support a revenue mode, keeping all operating behavior unchanged. Apply these exact changes:

    1. parseArgs options (lines 274-280): add `revenue: { type: 'boolean', default: false }`.

    2. main() (after `const dryRun = opts['dry-run'];`): add `const mode = opts.revenue ? 'revenue' : 'operating';`. Change the loader log line from the hardcoded `[operating]` to `[${mode}]`. Pass `mode` through to `processPDF(p, muniId, dryRun, mode)`.

    3. extractPDF (lines 70-81): change signature to `extractPDF(pdfPath, mode = 'operating')`. Build args as `const args = [pyScript, pdfPath];` then `if (mode === 'revenue') args.push('--mode', 'revenue');` and pass `args` to spawnSync. KEEP spawnSync with the array form (do NOT switch to execSync template string — spawnSync is the shell-injection-safe pattern introduced in Phase 20 / threat T-20-03).

    4. Add a new `buildRevenueTree(rows)` function modeled on processPortland.js lines 103-114 but using Gresham field names: filter `r => r.adopted_amount > 0`, map to `{ n: r.category, a: r.adopted_amount, i: [{ d: r.category, a: r.adopted_amount, aa: null, f: null, e: null }] }`, sort by `a` descending, return `{ tree: nodes, total }` where total is the sum of node amounts.

    5. upsertDataSource (lines 143-173): add a third parameter `datasetType`. Set `name` to `Gresham ${datasetType === 'revenue' ? 'Revenue Budget' : 'Operating Budget'} FY${fiscalYear}`. Set `dataset_type: datasetType` in the `src` object. CRITICAL: change the lookup `.eq('dataset_type', 'operating')` to `.eq('dataset_type', datasetType)` — this is what prevents the fy2026 revenue row from colliding with / overwriting the existing fy2026 operating data_source row.

    6. loadFiscalYear (lines 176-204): add a third parameter `datasetType` (placed after fiscalYear, before tree). Pass it to `upsertDataSource(muniId, fiscalYear, datasetType)`. Change the RPC argument `p_dataset_type: 'operating'` to `p_dataset_type: datasetType`. The delete-then-RPC idempotency block stays unchanged.

    7. processPDF (lines 207-270): change signature to `processPDF(pdfAbsPath, muniId, dryRun, mode = 'operating')`. Pass `mode` into `extractPDF(pdfAbsPath, mode)`. Inside the per-FY loop derive `const isRevenue = mode === 'revenue';`, build the tree via `isRevenue ? buildRevenueTree(fyRows) : buildOperatingTree(fyRows)`, and use unit label `categories` vs `departments` and type label `Revenue` vs `Operating` in the console lines. Gate the SANITY_MAX check on `mode === 'operating'` (revenue FY2026 ~$512M legitimately exceeds the $500M operating cap, so the operating sanity check must not fire in revenue mode). Update the "No department rows extracted" warning to say `category` when in revenue mode. Call `loadFiscalYear(muniId, fy, isRevenue ? 'revenue' : 'operating', tree, total, rowCount)`.

    Then run the revenue dry-run across all 4 PDFs and confirm the expected totals.
  </action>
  <verify>
    <automated>node scripts/processGresham.js --revenue --dry-run; node scripts/processGresham.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/processGresham.js contains `buildRevenueTree` and `revenue: { type: 'boolean'` and `.eq('dataset_type', datasetType)` (source assertions).
    - scripts/processGresham.js no longer contains a hardcoded `.eq('dataset_type', 'operating')` lookup nor `p_dataset_type: 'operating'` literal (source assertion — both must be the `datasetType` parameter).
    - extractPDF uses `spawnSync` with an args array and pushes `'--mode', 'revenue'` when mode is revenue; it does NOT use execSync for the Python call (source/security assertion, threat T-20-03).
    - `node scripts/processGresham.js --revenue --dry-run` exits 0 and prints four fiscal years FY2023, FY2024, FY2025, FY2026, each labeled `Revenue`, each with 10 categories (behavior assertion).
    - Revenue dry-run totals fall in the $400M-$525M band per FY (approx FY2023 ~$411M, FY2024 ~$460M, FY2025 ~$521M, FY2026 ~$512M) and are NOT in the $731M-$896M band (that band means Beginning Balance leaked in) (behavior assertion).
    - No revenue dry-run line lists a node named "Beginning Balance" or "Total Resources" (behavior assertion).
    - `node scripts/processGresham.js --dry-run` (operating, no flag) still prints FY2023=$269,306,991, FY2024=$275,500,631, FY2025=$306,839,832, FY2026=$330,652,078 with department counts unchanged (no-regression assertion).
  </acceptance_criteria>
  <done>processGresham.js supports `--revenue` end-to-end; revenue dry-run shows 4 FYs of 10 categories each with totals in the $400M-$525M band and no Beginning Balance/Total Resources nodes; operating dry-run unchanged; dataset_type is fully parametric so no operating/revenue data_source collision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → Python subprocess | PDF path passed from Node to extractGresham.py via spawnSync |
| Node → Supabase (Plan 02 only) | Service-key writes via treasury_sync_budget_tree RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Injection | extractPDF() Python subprocess call | mitigate | Keep spawnSync with args array (no shell); PDF path comes from controlled docs/Gresham/ readdir, not user input. Carries forward the T-20-03 fix — do NOT regress to execSync template string. |
| T-21-02 | Tampering | dataset_type collision overwriting operating data_source | mitigate | upsertDataSource lookup filters on `.eq('dataset_type', datasetType)`; revenue rows (fy2026/revenue) cannot match or overwrite operating rows (fy2026/operating). Verified by no-regression operating dry-run. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: none). No install task exists. |
</threat_model>

<verification>
- `python scripts/extractGresham.py "docs/Gresham/fy2025-26.pdf" --mode revenue` → 10 revenue categories, no Beginning Balance / Total Resources.
- `node scripts/processGresham.js --revenue --dry-run` → 4 FYs, 10 categories each, totals $400M-$525M.
- `node scripts/processGresham.js --dry-run` → unchanged operating totals (no regression).
</verification>

<success_criteria>
- extract_revenue() + argparse --mode added to extractGresham.py; extract_budget() untouched.
- processGresham.js has --revenue flag and fully parametric datasetType/mode plumbing.
- Revenue dry-run produces 4 FYs × 10 categories in the $400M-$525M band, excluding Beginning Balance and Total Resources.
- Operating dry-run output is byte-identical in totals to Phase 20.
</success_criteria>

<output>
Create `.planning/phases/21-gresham-or-revenue-load/21-01-SUMMARY.md` when done.
</output>
