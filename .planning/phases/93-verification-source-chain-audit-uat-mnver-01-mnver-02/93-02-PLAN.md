---
phase: 93-verification-source-chain-audit-uat-mnver-01-mnver-02
plan: 02
type: execute
wave: 2
depends_on: ["93-01"]
autonomous: false
files_modified:
  - scripts/processMN.js
  - scripts/processMNRevenue.js
  - .planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md
requirements: [MNVER-01]
must_haves:
  truths:
    - "Full-cohort source-chain audit across ALL loaded MN rows (20,414 city + 1,380 county budget rows + 136 universal enrichment rows + the 10 state-node rows) confirms: 0 NULL source_url / source_date / data_source (after the state-node fix), 0 fragile links (every OSA source_url is in scripts/mnOsaDatasets.json and resolves to a live osa.state.mn.us file; enrichment rows carry source='ai' + evidence), and 0 residue (no orphan budgets, no orphan budget_categories, no stale/duplicate (muni,fy,dataset) rows, no phantom municipalities beyond the documented mnCityResidual/mnCountyResidual gaps)"
    - "Independent re-derivation (CONTEXT D-93-06, the Phase 86 lesson): for the sample entities + ≥3 random others incl. ≥1 county and ≥1 CASH-basis entity, a handful of category amounts AND the dataset totals are re-computed straight from the OSA workbooks (via the loadMNOSA.js parse/tree-build helpers) and MATCH the stored DB values; 0 numeric-garbage category labels cohort-wide"
    - "Icicle drill-down confirmed structurally (CONTEXT D-93-07): the sample entities have real depth-1 AND depth-2 budget_categories for operating + revenue (Minneapolis FY2023 baseline: op 8/17/3, rev 8/16/14), and a cohort-wide check confirms multi-depth trees are the norm (not flat) — the MN source resolves the Ohio flat-source limitation"
    - "In-phase fix (D-93-05): the 10 'Minnesota' state-node General Fund NULL source_url + source_date rows are stamped with the MN MMB state-budget source (confirm the exact URL via processMN.js/processMNRevenue.js); after the fix the FULL MN cohort is literally 0-NULL source_url; the loader change is reproducible + idempotent"
    - "A written 93-02-AUDIT.md records the audit queries + results, the re-derivation samples, the icicle-depth confirmation, the state-node fix (before/after), and the MNVER-01 part B verdict; re-running the fix is idempotent (0 further changes)"
  artifacts:
    - path: ".planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md"
      provides: "The full-cohort source-chain audit + independent re-derivation + icicle confirmation + in-phase-fix record (MNVER-01 part B)"
      contains: "source-chain"
    - path: "scripts/processMN.js"
      provides: "Updated to stamp the MN state-node General Fund rows with the MMB source_url + source_date (fix), idempotent"
      contains: "source_url"
  key_links:
    - from: "All loaded Minnesota rows (budgets + enrichment + state node)"
      to: "a clean, durable, residue-free source chain + accurate, independently re-derived stored figures + confirmed multi-level icicle trees"
      via: "audit queries + independent workbook re-derivation + icicle-depth check + the approved state-node source-stamp fix"
      pattern: "source-chain"
---

<objective>
MNVER-01 part B: prove the full Minnesota cohort's source chain is durable, complete, and residue-free; that the stored figures are accurate (independently re-derived from the workbooks, not loader-trusted — the Phase 86 county near-miss lesson); and that the 2-level icicle drill-down trees are present (the MN source resolves the Ohio flat-source limitation). Then apply the one Chris-approved in-phase fix and re-verify.

Production writes are limited to the one approved fix (state-node source stamp); everything else is read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-CONTEXT.md
@.planning/milestones/v2.8-phases/88-verification-source-chain-audit-uat/88-02-SUMMARY.md
@scripts/processMN.js
@scripts/loadMNOSA.js

<interfaces>
<!-- Production target kxsdzaojfaibhuzmclfq (.env). Only 1 write (state-node stamp); rest read-only. mcp__supabase-local preferred. -->
- Cohort (re-verify counts): 858 cities / 20,414 rows; 87 counties / 1,380 rows; 136 universal enrichment rows; 10 state-node rows. Use total_budget/hierarchy column names ([[reference_treasury_budgets_probe_columns]]).
- Source-chain audit queries (read-only): NULL source_url/source_date/data_source by entity_type (expect 0 cities+counties, 10 state-node pre-fix); duplicate (municipality_id, fiscal_year, dataset_type); orphan budgets (no municipality) / orphan budget_categories (no budget); numeric-garbage depth-0 labels cohort-wide (regex ^-?[0-9]+$ → must be 0); enrichment rows with NULL plain_name/description (must be 0). Cross-check phantom-municipality gaps against scripts/mnCityResidual.json + mnCountyResidual.json (documented source gaps, not findings).
- Fragile-link check: every distinct MN OSA source_url should match scripts/mnOsaDatasets.json (the per-FY cired_YY_data.xlsx osa.state.mn.us URLs) and resolve HTTP 200 (spot-re-probe ~5). Durable-human-page bar (CONTEXT D-93-04).
- Independent re-derivation (D-93-06): use the loadMNOSA.js parsing / tree-building helpers (the MN analog of Ohio's detectLayout/buildRevenueTree/buildExpenditureTree) on the source workbooks to recompute totals + a few category amounts for Minneapolis, Hennepin County, + ≥3 random entities (incl. ≥1 county + ≥1 CASH-basis entity from mnCityBasis.json), compare to stored DB values.
- Icicle-depth confirmation (D-93-07): query budget_categories depth distribution for the sample + a cohort-wide aggregate — confirm depth-1 AND depth-2 nodes exist for operating + revenue (Minneapolis FY2023 baseline op 8/17/3, rev 8/16/14). This is the structural proof that drill-down trees exist; the live render is 93-03.
- Fix (state node): the 10 rows (data_source 'Minnesota General Fund Operating Budget' / 'Minnesota General Fund Revenue', FY2022–2026) have NULL source_url + source_date. processMN.js / processMNRevenue.js loaded them — read those for the intended MMB source; use the canonical Minnesota Management & Budget budget page (mn.gov/mmb). Stamp source_url + source_date (the budget publication/retrieval date). Update the loader so it's reproducible + idempotent, then apply. NOTE: treasury_sync_city_budget is NOT source-safe ([[project_sync_city_budget_not_source_safe]]) — apply this as a targeted UPDATE on the 10 state-node rows, not a full re-sync.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-cohort source-chain audit (read-only)</name>
  <files>.planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v2.8-phases/88-verification-source-chain-audit-uat/88-02-SUMMARY.md (the OH audit checks + format)
    - 93-CONTEXT.md (D-93-04 durability bar; the known state-node NULL gap)
    - scripts/mnCityResidual.json + scripts/mnCountyResidual.json (documented no-phantom source gaps)
  </read_first>
  <action>
    Run the read-only audit queries (mcp__supabase-local) over the full MN cohort and record results in 93-02-AUDIT.md: (a) NULL source_url/source_date/data_source by entity_type (expect 0 cities+counties, 10 state-node pre-fix); (b) duplicate (muni,fy,dataset); (c) orphan budgets / orphan budget_categories; (d) numeric-garbage depth-0 labels cohort-wide (must be 0); (e) enrichment rows with NULL plain_name/description (must be 0); (f) fragile links — every distinct OSA source_url is in scripts/mnOsaDatasets.json + spot-re-probe ~5 URLs for HTTP 200; (g) phantom check — any city/county with budgets absent from the source cross-referenced against mnCityResidual/mnCountyResidual (documented gaps are not findings). Flag every finding; the only expected one is the 10 state-node NULLs (fixed in Task 4).
  </action>
  <acceptance_criteria>
    - 93-02-AUDIT.md records every query + result; cities+counties show 0 NULL/fragile/residue; the 10 state-node NULLs are the only NULL finding
    - 0 duplicate (muni,fy,dataset) rows; 0 orphan budgets/categories; 0 numeric-garbage labels cohort-wide; 0 enrichment rows missing text
    - A sample of ~5 OSA source_urls re-probed HTTP 200 (durable); any phantom-municipality entries reconcile to the documented residual files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Independent re-derivation of stored figures (the Phase 86 lesson)</name>
  <files>.planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md</files>
  <read_first>
    - project_ohio_aos_county_vs_city_layout (why totals-only / loader-self-report is insufficient)
    - scripts/loadMNOSA.js (the parse/tree-build helpers for re-derivation)
    - scripts/mnCityBasis.json (pick ≥1 CASH-basis entity)
  </read_first>
  <action>
    For Minneapolis, Hennepin County, and ≥3 random MN entities (incl. ≥1 county and ≥1 CASH-basis entity), independently recompute from the OSA source workbooks (via the loadMNOSA.js helpers): the operating + revenue totals AND 2-3 named category amounts each, then compare to the stored DB values. Record matches/deltas in 93-02-AUDIT.md. Any mismatch is a finding (the Phase 86 county defect was exactly this class — wrong header row/columns). Confirm category labels are real text everywhere sampled.
  </action>
  <acceptance_criteria>
    - ≥5 entities (incl. ≥1 county + ≥1 CASH-basis) have independently re-derived totals + sample category amounts MATCHING the stored DB values
    - All sampled category labels are real text (no numeric garbage)
    - Any mismatch is recorded as a finding with root-cause (none expected)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Icicle drill-down structural confirmation</name>
  <files>.planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md</files>
  <read_first>
    - project_flat_source_icicle_limitation (the Ohio limitation this resolves)
    - 93-CONTEXT.md (D-93-07; Minneapolis FY2023 baseline op 8/17/3, rev 8/16/14)
  </read_first>
  <action>
    Query budget_categories depth distribution (mcp__supabase-local) for the sample entities (Minneapolis FY2023, Hennepin County FY2021) for operating + revenue, confirming real depth-1 AND depth-2 nodes exist. Then run a cohort-wide aggregate confirming multi-depth trees are the norm (e.g. the count / share of MN budgets that have any depth>=1 and depth>=2 categories). Record in 93-02-AUDIT.md as the structural proof that the 2-level icicle drill-down trees exist (resolving the Ohio flat-source limitation). NO writes.
  </action>
  <acceptance_criteria>
    - Sample entities confirmed to have depth-1 AND depth-2 budget_categories for op + rev (Minneapolis matches the 8/17/3 + 8/16/14 baseline)
    - A cohort-wide aggregate shows multi-depth trees are the norm, not the exception (drill-down is real cohort-wide)
    - 93-02-AUDIT.md records the depth evidence with an explicit "flat-source limitation resolved" note
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Apply the approved state-node source-stamp fix + re-verify (live)</name>
  <files>scripts/processMN.js, scripts/processMNRevenue.js, .planning/phases/93-verification-source-chain-audit-uat-mnver-01-mnver-02/93-02-AUDIT.md</files>
  <read_first>
    - scripts/processMN.js / scripts/processMNRevenue.js (the intended MMB state-budget source)
    - 93-CONTEXT.md (D-93-05 state-node stamp)
    - project_sync_city_budget_not_source_safe (apply a targeted UPDATE, not a re-sync)
  </read_first>
  <action>
    With .env sourced (production): stamp the 10 'Minnesota' state-node General Fund rows' source_url + source_date with the canonical MN MMB budget source (confirm/record the exact URL intended by processMN.js/processMNRevenue.js; use the MMB budget page mn.gov/mmb). Update processMN.js / processMNRevenue.js so the stamp is reproducible + idempotent, then apply as a targeted UPDATE on the 10 rows (NOT a full re-sync — treasury_sync_city_budget is not source-safe). Re-verify (mcp__supabase-local): full MN cohort now 0 NULL source_url/source_date across ALL entity_types; re-run is idempotent (0 further changes). Record before/after in 93-02-AUDIT.md with the MNVER-01 part B verdict.
  </action>
  <acceptance_criteria>
    - After the fix: 0 MN budget rows (any entity_type) have NULL source_url or source_date; the 10 state-node rows carry the MMB source_url + a source_date
    - The loader change is idempotent (re-run = 0 changes)
    - 93-02-AUDIT.md ends with the MNVER-01 part B PASS verdict + before/after evidence
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full-cohort source chain is durable + complete + residue-free: after the state-node stamp, 0 NULL source_url/source_date/data_source across ALL MN rows; 0 duplicate/orphan/residue; 0 numeric-garbage labels; enrichment intact; sampled source_urls resolve.
- Independent re-derivation (not loader self-report) confirms stored totals + sample category amounts match the workbooks for ≥5 entities incl. a county + a CASH-basis entity (the Phase 86 lesson applied).
- Icicle drill-down trees confirmed structurally (depth-1 + depth-2 present, cohort-wide norm) — the MN source resolves the Ohio flat-source limitation.
- The state-node source-stamp fix is idempotent; 93-02-AUDIT.md records everything with the MNVER-01 part B verdict.
- Live-app UAT + sign-off is plan 93-03.
</verification>
</output>
