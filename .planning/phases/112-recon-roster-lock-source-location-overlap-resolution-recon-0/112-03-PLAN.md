---
phase: 112-recon-roster-lock-source-location-overlap-resolution-recon-0
plan: "112-03"
type: execute
wave: 2
depends_on: ["112-01", "112-02"]
files_modified:
  - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
autonomous: true
requirements: [RECON-09, RECON-10]
user_setup: []

must_haves:
  truths:
    - "A read-only DB probe explicitly checks the UT state node's provenance (v2.5 loaded Utah MUNICIPAL data from Transparent Utah BigQuery — unaffected — but the state node itself is inventoried: row count, FYs, dataset types, data_source labels) and probes every roster state for pre-existing custom-source state-node rows (RECON-10)"
    - "Any roster state with a pre-existing custom-source node gets an in-place upgrade plan per the MA v1.8-DLS / CA v1.7 precedent — no duplicate node, ACFR replaces prior state-node GF rows per state-FY, provenance + row inventory documented before any write (D-03); anything that does not fit the in-place mold is flagged as a load-phase decision, not a new pattern"
    - "The recon doc confirms each upgraded state's ACFR rows will REPLACE its NASBO operating rows per state-FY idempotently + never-overwriting, un-upgraded NASBO states stay on scripts/loadStateGF.mjs, and the existing 19 ACFR nodes (MN/OH/VA/CA/TX/NY/FL/PA/IL/NJ/MA/NC/GA/MD/TN/CT/WI/WA/MI) are not disturbed — noted as the contract Phases 113/114 inherit, not executed (D-10)"
    - "The substitution round runs ONCE (D-01): each candidate that failed clean extraction, failed the recency floor, or was rank-corrected is substituted with the next-largest un-upgraded NASBO state from the 112-RECON.md ranking table, the substitute gets the full recon procedure (locate + bookend-tie + four risk facts + gap log), every substitution is documented with its reason, failed candidates land in ACFRX-03; if a substitute also fails, the count floats down — no second reach-down"
    - "The final ~10-state roster is LOCKED and the 113/114 batch split is LOCKED largest-GF-first from the re-ranked roster (Batch 1 / Phase 113 = ACFR-21..25; Batch 2 / Phase 114 = ACFR-26..30), rebalanced ~5/~5 around survivors if the roster changed, with the final ACFR-2x ↔ state mapping recorded for the traceability table (D-02)"
    - "112-RECON.md consolidates both batch SOURCES docs into the decision-ready Phase 113/114 handoff mirroring the 107-RECON.md shape (roster lock + rankings + substitutions + overlap plans + consolidated risk-fact table + gap-log rollup), notes the D-13 caveat that structure/URLs get re-verified at load with the GF total-tie as safety net, and confirms $0 spend + no DB writes (D-04)"
  artifacts:
    - path: ".planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md"
      provides: "Consolidated recon handoff: locked roster, locked batch split with ACFR-2x mapping, substitution record, UT provenance + overlap-resolution plans, 19-ACFR-nodes-untouched contract, per-state summary + loader mapping, open risks for Phases 113/114"
      contains: "Roster Lock"
  key_links:
    - from: "112-RECON.md locked roster + batch split"
      to: "Phase 113 + Phase 114 loads"
      via: "per-state window + template mapping + overlap-resolution plan"
      pattern: "Batch 1|Batch 2"
    - from: "112-RECON.md 19-nodes-untouched contract"
      to: "the existing 19 ACFR nodes"
      via: "read-only DB confirmation that only roster states are touched"
      pattern: "MN.*OH.*VA.*CA.*TX.*NY.*FL.*PA.*IL"
---

<objective>
Close Phase 112 by resolving prior-load overlaps (RECON-10), running the bounded substitution round, and locking the final roster + 113/114 batch split (RECON-09), consolidating both batch SOURCES docs into the decision-ready Phase 113/114 handoff. Uses a **read-only** DB probe to check the UT state node's provenance explicitly, identify any roster state carrying a pre-existing custom-source node, and confirm the existing 19 ACFR nodes will be left undisturbed.

Purpose: The overlap-resolution plans + locked roster + locked batch split are the final input contract Phases 113/114 inherit. **Documentation + read-only DB queries only — no DB writes, no NASBO mutations, no loader code, no frontend, $0.**
Output: `112-RECON.md` — the consolidated handoff mirroring the 107-RECON.md shape (scaffolded with the ranking table by plan 112-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md
@.planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-VERIFICATION.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public state ACFR site → local disk | untrusted PDF/HTML fetched during the substitution round (if any) |
| orchestrator → production Supabase | read-only SELECT probes against treasury.budgets / data_sources using the service key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-04 | Tampering | accidental DB mutation during the "read-only" probe | mitigate | Probe uses SELECT-only queries (mcp__supabase-local__execute_sql or a probe script issuing SELECTs); no INSERT/UPDATE/DELETE/RPC-with-side-effects anywhere in this plan; the plan's contract line "no DB writes" is verified in the summary |
| T-112-05 | Information Disclosure | probe output pasted into a committed doc leaking secrets | mitigate | Probe results recorded as row counts / FY ranges / source labels only — no keys, no connection strings; .env never quoted in the recon doc |
| T-112-01..03 | (carried from plans 112-01/02) | substitution-round fetches inherit the soft-404 / wrong-statement / non-durable-URL mitigations | mitigate | Same procedure as the batch plans (Content-Type/size filter, statement-title + GF-column confirmation, durable-URL-or-gap-log) |
</threat_model>

<tasks>

<task id="112-03-01" type="auto">
  <name>Task 1: Read-only overlap probe — UT state-node provenance + roster custom-source inventory + 19-nodes-untouched contract</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-03 in-place default, D-10 flag-not-execute, RECON-10 text)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (the overlap-resolution + untouched-nodes section shape to mirror)
- .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-VERIFICATION.md (the LOAD-01 ephemeral data_sources lifecycle every 113/114 loader inherits)
- scripts/loadStateGF.mjs (the NASBO loader whose rows get replaced — its idempotency/never-overwrite pattern and data_source labels)
  </read_first>
  <action>
Run **read-only** probes (mcp__supabase-local__execute_sql, SELECT only — column caution: `treasury.budgets` uses `total_budget` and `hierarchy`, there is no `row_count` column) to build the overlap inventory: (1) **UT state node explicitly (RECON-10):** inventory the Utah STATE node's rows — count, fiscal years, dataset types, and `data_sources` labels — to determine whether it holds only Phase-96 NASBO rows (`loadStateGF.mjs`) or any v2.5-era custom-source residue (watch for phantom-city-row / display-name quirks from the v2.5 Transparent-Utah era). Record that UT's *municipal* city/county data (Transparent Utah BigQuery) is a different set of municipality rows and is explicitly untouched. (2) **All 10 roster states (plus any substitution candidates flagged by plans 112-01/02):** probe each state node for non-NASBO/custom-source rows — any hit gets named with its row inventory (source label, FYs, dataset types). (3) **The 19 ACFR nodes** (MN, OH, VA, CA, TX, NY, FL, PA, IL, NJ, MA, NC, GA, MD, TN, CT, WI, WA, MI): confirm none is on the roster and record the untouched-nodes contract Phases 113/114 inherit — noted, not executed (D-10). Write the results into 112-RECON.md's Overlap Resolution section: for each state with pre-existing custom-source rows, an **in-place upgrade plan** per the MA v1.8-DLS / CA v1.7 precedent (no duplicate node; ACFR rows replace prior state-node GF rows per state-FY; idempotent + never-overwriting per the Phase-111 LOAD-01 contract); for clean NASBO-only states, confirm the standard ACFR-replaces-NASBO plan; anything not fitting the in-place mold is flagged as a load-phase decision rather than a new pattern (D-03). No INSERT/UPDATE/DELETE anywhere.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Utah" -Quiet ; Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Overlap" -Quiet</automated>
  </verify>
  <acceptance_criteria>
- 112-RECON.md's Overlap Resolution section records the UT state node's row inventory (count, FYs, dataset types, data_sources labels) and its provenance verdict, with the municipal-data-untouched note
- Every roster state (and any flagged substitution candidate) has a probe result: clean-NASBO-only or named custom-source rows with inventory
- Each custom-source state has an in-place upgrade plan (MA/CA precedent, no duplicate node, per-state-FY replace, idempotent + never-overwrite per LOAD-01); non-fitting cases flagged as load-phase decisions
- The 19-ACFR-nodes-untouched contract is recorded with the full node list; all probes were SELECT-only (no DB writes)
  </acceptance_criteria>
  <done>Overlap inventory complete: UT provenance explicitly checked, every roster state probed, in-place upgrade plans written where needed, 19-nodes-untouched contract recorded — all on paper, no DB writes (D-10).</done>
</task>

<task id="112-03-02" type="auto">
  <name>Task 2: Substitution round (one round only) — replace failed/rank-corrected candidates from the ranking ladder</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-01 substitution bounds — ONE round, documented reasons, failed candidates → ACFRX-03; D-05..D-09, D-11, D-12 recon procedure)
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (SUBSTITUTION CANDIDATE marks + gap logs from plan 112-01)
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md (SUBSTITUTION CANDIDATE marks + gap logs from plan 112-02)
  </read_first>
  <action>
Collect every candidate marked **SUBSTITUTION CANDIDATE** by plans 112-01/112-02 (failed clean `pdftotext -table` extraction or failed the D-07 recency floor) plus any **rank-correction flag** from the 112-RECON.md ranking table (a non-candidate state ranking above a candidate). If there are NONE, record "No substitutions — roster confirmed as the candidate 10" in 112-RECON.md's Substitutions section and finish this task. Otherwise run exactly **one substitution round (D-01)**: for each outgoing candidate, take the next-largest un-upgraded NASBO state from the ranking table and run the FULL recon procedure on it within the D-11 budget (locate the Governmental Funds Statement → GENERAL FUND column; durable per-year URL pattern; download + `pdftotext -table` bookends into `_acfr-work/{state}/`; bookend-tie with actual dollar figures per D-05; four risk facts per D-08; recency floor per D-07; scope-vs-NASBO per D-09; loader-template mapping; gap log per D-06), appending the substitute's per-state block to the appropriate BATCH SOURCES doc is NOT required — record the substitute's full recon block directly in 112-RECON.md's Substitutions section (the consolidated handoff is what 113/114 read). Document every substitution with its reason (rank correction | non-extractable | recency-floor fail); outgoing candidates land in ACFRX-03 (final tranche). If a substitute itself fails its recon check, the tranche count floats down — NO second reach-down. Also probe any incoming substitute for custom-source state-node rows (same read-only probe as Task 1) so its overlap plan is on paper too.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Substitution" -Quiet</automated>
  </verify>
  <acceptance_criteria>
- 112-RECON.md's Substitutions section exists and records either "No substitutions" or one documented substitution round
- Every substitution names the outgoing state, the reason (rank correction | non-extractable | recency-floor fail), and the incoming state with its full recon block (statement, URL pattern, bookend ties with dollar figures, four risk facts, recency verdict, template mapping, gap log)
- Every outgoing candidate is recorded as landing in ACFRX-03; no second reach-down occurred (count floats down if a substitute failed)
- Any incoming substitute has a read-only overlap-probe result recorded
  </acceptance_criteria>
  <done>Substitution round complete (or explicitly empty): every roster change documented with reason, substitutes fully reconned + overlap-probed, ACFRX-03 deferrals recorded — bounded to one round per D-01.</done>
</task>

<task id="112-03-03" type="auto">
  <name>Task 3: Lock the roster + 113/114 batch split and consolidate the decision-ready handoff</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-02 batch split, D-04 deliverable shape, D-13 structure-reverify-at-load caveat)
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (per-state findings to consolidate)
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md (per-state findings to consolidate)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (the consolidated-handoff shape: roster lock, batch split, per-state summary table, open risks)
- .planning/REQUIREMENTS.md (ACFR-21..30 state assignments — the traceability mapping the lock must record)
  </read_first>
  <action>
Finalize `112-RECON.md` as the decision-ready Phase 113/114 handoff mirroring 107-RECON.md: (1) **Roster Lock (RECON-09):** lock the final ~10-state roster from the surviving candidates + substitutes, each with its clean window, and record the lock rationale against the ranking table. (2) **Batch Split (D-02):** lock the split largest-GF-first from the re-ranked roster — Batch 1 / Phase 113 = ACFR-21..25, Batch 2 / Phase 114 = ACFR-26..30 (nominal AZ/IN/CO/MO/KY vs OR/SC/LA/OK/UT; if the roster changed, surviving states keep size-order assignment and batches rebalance ~5/~5) — and record the final **ACFR-2x ↔ state mapping** for the traceability table. (3) **Consolidated per-state summary table** across both batches: state | ACFR-2x | batch | clean window | units | FY-end | template | scope-vs-NASBO verdict | open risks — drawn from the two SOURCES docs. (4) **Consolidated risk-fact table + gap-log rollup** (D-04 shape). (5) **Contract notes for 113/114:** ACFR replaces NASBO per state-FY idempotently + never-overwriting (LOAD-01 lifecycle inherited by all loader clones); un-upgraded NASBO states remain on `scripts/loadStateGF.mjs`; the 19 ACFR nodes untouched; the D-13 caveat — recon totals are trusted but statement structure + URLs get re-verified at load time with the GF total-tie as the safety net (do not over-promise structural detail). (6) **Closeout confirmations:** $0 spend (pdftotext only, no AI), no DB writes anywhere in the phase, no NASBO mutations, no loader code, no frontend. Remove any leftover scaffold placeholders — every section filled or explicitly N/A.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Roster Lock" -Quiet ; Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Batch 1|Batch 2" -Quiet ; Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "ACFR-2[1-9]" -Quiet</automated>
  </verify>
  <acceptance_criteria>
- 112-RECON.md contains a locked roster (each state with clean window + lock rationale) and a locked Batch 1 / Batch 2 split with the ACFR-2x ↔ state traceability mapping (D-02)
- The consolidated per-state summary table covers every locked state with window, units, FY-end, template mapping, and scope verdict
- The consolidated risk-fact table + gap-log rollup are present (D-04); the D-13 re-verify-at-load caveat is stated
- Contract notes record: ACFR-replaces-NASBO idempotent + never-overwrite (LOAD-01), un-upgraded states stay on loadStateGF.mjs, 19 ACFR nodes untouched
- Closeout confirms $0 spend + zero DB writes for the whole phase; no scaffold placeholders remain
  </acceptance_criteria>
  <done>112-RECON.md is the complete, decision-ready Phase 113/114 input contract: roster locked, batch split locked with ACFR-2x mapping, overlaps resolved on paper, risk facts + gaps consolidated, contracts + closeout confirmed.</done>
</task>

</tasks>

<verification>
- The UT state node's provenance is explicitly checked and recorded; every roster state has an overlap-probe verdict; custom-source states have in-place upgrade plans (MA/CA precedent, D-03); the 19 ACFR nodes untouched contract is recorded (RECON-10).
- The substitution round ran at most once with every change documented (reason + full substitute recon), or "No substitutions" is recorded (D-01).
- The final roster + 113/114 batch split are locked with the ACFR-2x ↔ state mapping (RECON-09, D-02).
- 112-RECON.md mirrors the 107-RECON.md handoff shape and consolidates both SOURCES docs (D-04); the D-13 load-time re-verify caveat is stated.
- All DB access was SELECT-only; no DB writes, no NASBO mutations, no loader code, no frontend changes; $0 spend.
</verification>

<success_criteria>
Phase 112's three deliverables are complete and decision-ready: the locked ~10-state roster with locked 113/114 batch split (RECON-09), every locked state's located + bookend-tied + trap-checked ACFR source, and every prior-load overlap resolved on paper with the UT provenance explicitly checked and the 19 ACFR nodes contractually untouched (RECON-10).
</success_criteria>

<output>
Create `.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-03-SUMMARY.md` when done.
</output>
