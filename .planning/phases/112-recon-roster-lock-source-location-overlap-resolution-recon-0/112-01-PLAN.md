---
phase: 112-recon-roster-lock-source-location-overlap-resolution-recon-0
plan: "112-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md
  - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
autonomous: true
requirements: [RECON-09]
user_setup: []

must_haves:
  truths:
    - "The remaining 31 NASBO states are ranked by GF size from the NASBO 2025 SER (derived from scripts/loadStateGF.mjs FY2024 checksum-verified totals, spot-checked against the SER PDF) and the ranking table is written into the scaffolded 112-RECON.md — the D-01 substitution ladder and D-02 batch-split input"
    - "For each Batch-1 candidate (AZ, IN, CO, MO, KY) the ACFR Governmental Funds Statement of Rev/Exp/Changes in Fund Balances GENERAL FUND column is located, with durable per-year URL pattern and the cleanly pdftotext -table-extractable clean window recorded (D-04 deliverable shape)"
    - "Each Batch-1 state's clean window is bookend-tie-confirmed (oldest + latest FY) with actual dollar GF Total revenues figures tied to the printed page total (D-05)"
    - "Each Batch-1 state has its four risk facts pinned (units / negative-category years / exact GF column header + statement confirmed / FY-end month) per D-08"
    - "Each Batch-1 state's clean window covers FY2023 + FY2024 (recency floor D-07) OR is flagged as a blocker/decision for the load phase; a shallow clean window still makes a state roster-eligible (D-12 — no minimum depth beyond the recency floor)"
    - "A per-state gap log records every Batch-1 FY that does not cleanly extract or lacks a durable URL, with reason (D-11 effort-budget stops, D-06 durable-URL exclusions); windows stop at FY2002 (pre-GASB-34 = Phase 115 territory, not a gap-log failure)"
    - "Each Batch-1 state's ACFR GF scope vs its NASBO GF is compared with magnitude, and where materially broader/narrower an accept-relabel is recommended (D-09)"
    - "Each Batch-1 state is mapped to the closest existing loader template (the v2.13 process{NJ,MA,NC,GA,MD,TN,CT,WI,WA,MI}Acfr.js family or earlier process{PA,IL,CA,TX,NY,FL,OH,VA}*.js) by GF-statement layout"
  artifacts:
    - path: ".planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md"
      provides: "Per-state ACFR source location + bookend ties + four risk facts + gap log + scope-vs-NASBO + template mapping for AZ, IN, CO, MO, KY"
      contains: "Arizona"
    - path: ".planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md"
      provides: "Scaffold with the NASBO-2025-SER GF-size ranking table of the remaining 31 NASBO states (filled sections land in plan 112-03)"
      contains: "Ranking"
  key_links:
    - from: "112-BATCH1-SOURCES.md per-state block"
      to: "Phase 113 SOURCES-map + loader clone"
      via: "durable per-year URL pattern + GF column index + units + template mapping"
      pattern: "General Fund"
    - from: "112-RECON.md ranking table"
      to: "plan 112-03 roster lock + substitution round"
      via: "GF-size order of the remaining 31 NASBO states"
      pattern: "Ranking"
---

<objective>
Rank the remaining 31 NASBO states by GF size from the NASBO 2025 SER, then recon the Batch-1 candidate states (AZ, IN, CO, MO, KY) for the v2.14 tranche-3 State-ACFR upgrade. For each state: locate the ACFR Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances* → **GENERAL FUND column** (GAAP), find its durable per-year URL pattern, establish the cleanly `pdftotext -table`-extractable clean window, bookend-tie-confirm that window (oldest + latest FY), pin the four risk facts, compare ACFR GF scope vs NASBO, map it to the closest existing loader template, and gap-log every year that doesn't cleanly extract or lacks a durable URL.

Purpose: This is the input contract for Phase 113 (Batch 1 loads). It de-risks the loads before any write. **Documentation only — no DB writes, no NASBO mutations, no loader code, no frontend, $0 (pdftotext only, no AI).**
Output: `112-BATCH1-SOURCES.md` (per-state source doc mirroring the Phase 107 shape) + the scaffolded `112-RECON.md` carrying the ranking table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md
@.planning/followups/ca-acfr-reconciliation.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public state ACFR site → local disk | untrusted PDF/HTML fetched over the network into a local working dir |

This is a documentation-only, read-only recon plan: it fetches public ACFR PDFs, runs `pdftotext`, and writes local markdown. No DB, no secrets, no user input, no network writes. The honest threat surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Spoofing/Tampering | fetched "ACFR" PDF that is actually a soft-404 HTML page (HTTP 200 + HTML body) mistaken for a real document | mitigate | Filter every download by Content-Type `application/pdf` or size (>~1MB), never by HTTP status alone (CA-SCO soft-404 caution, 98-RECON). Confirm the exact statement title + "General Fund" column header + units before trusting any extracted figure. |
| T-112-02 | Tampering | extracting the wrong statement (government-wide Statement of Activities, or a budgetary/forecast schedule) instead of the Governmental Funds GENERAL FUND column | mitigate | Confirm the statement title is "Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds" and the column header is the General Fund column; tie the extracted GF Total revenues to the printed page total before recording (D-05/D-08.3). |
| T-112-03 | Tampering | recording a non-durable (Wayback/one-off) URL that will rot, breaking the always-sourced standard | mitigate | Reject non-durable-only years to the gap log (D-06); only a stable re-fetchable per-year URL pattern is recorded as loadable. |

No package installs in this plan (poppler `pdftotext` is a pre-existing system tool) → no supply-chain checkpoint needed. Some state sites need a browser User-Agent (tn.gov precedent) — a UA header on a public GET is normal client behavior, not evasion.
</threat_model>

<tasks>

<task id="112-01-00" type="auto">
  <name>Task 0: Workspace setup + NASBO 2025 SER re-ranking of the remaining 31 NASBO states</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md, .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-01..D-13 + canonical refs — THE authoritative scope)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (the exact per-state SOURCES doc shape + table columns to mirror)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (the consolidated-handoff shape 112-RECON.md will mirror; scaffold its sections now)
- scripts/loadStateGF.mjs (the STATES map — checksum-verified NASBO 2025 SER FY2024 GF per-program-area values per state, and the pinned NASBO_SER source URL)
- .gitignore (confirm `_acfr-work/` is gitignored so no PDFs get committed — entry exists from prior recon phases)
  </read_first>
  <action>
(1) Create/reuse the gitignored `_acfr-work/{az,in,co,mo,ky}/` working directories for downloaded PDFs + extracted text (local recon copies only — the app links the public source_url; PDFs are NOT committed). Confirm `pdftotext -v` runs (poppler available); if absent, halt and note the install requirement. (2) **Rank the remaining 31 NASBO states by GF size (RECON-09):** the 31 = 50 states minus the 19 ACFR-upgraded (MN, OH, VA, CA, TX, NY, FL, PA, IL, NJ, MA, NC, GA, MD, TN, CT, WI, WA, MI). Derive each remaining state's FY2024 GF operating total by summing its per-program-area values in the `STATES` map of `scripts/loadStateGF.mjs` (these are the checksum-verified NASBO 2025 SER figures loaded in Phase 96), sort descending, and spot-check the top ~12 against the NASBO 2025 SER PDF's total-GF-expenditure table (URL pinned in the loader's `NASBO_SER` constant) to catch any transcription drift. Record whether the ranking CONFIRMS or CORRECTS the candidate roster order AZ > IN > CO > MO > KY > OR > SC > LA > OK > UT; any state outside the candidate 10 that ranks above a candidate is a **rank-correction flag** for the D-01 substitution round in plan 112-03 — flag it, do NOT recon it here. (3) Scaffold `112-RECON.md` mirroring the 107-RECON.md section shape (Ranking table filled NOW; Roster Lock / Batch Split / Substitutions / Overlap Resolution / consolidated risk-fact table / gap-log rollup left as placeholders for plan 112-03), writing the full 31-state ranking table (rank | state | FY2024 GF total | candidate? | notes). (4) Scaffold `112-BATCH1-SOURCES.md` with the section skeleton mirroring 107-BATCH1-SOURCES.md: (a) per-state source table (State | Statement (page) | GF column header | Units | FY-end | Durable clean window | Per-year URL pattern), (b) bookend tie-confirmations table (State | FY | GF Total revenues | Tie check), (c) four-risk-facts table per D-08, (d) scope-vs-NASBO table per D-09, (e) recency-floor verdict table per D-07, (f) consolidated gap log, (g) per-state loader-template mapping + Phase-113 load notes. Records for AZ/IN/CO/MO/KY fill in over Tasks 1-2.
  </action>
  <verify>
    <automated>pdftotext -v 2>&1 | Select-String -Pattern "pdftotext" ; Test-Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md ; Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md -Pattern "Ranking" -Quiet</automated>
  </verify>
  <acceptance_criteria>
- `pdftotext -v` runs (poppler available); if not, the plan halts with the install requirement recorded
- `_acfr-work/{az,in,co,mo,ky}/` exist under the gitignored `_acfr-work/` (no PDFs committed)
- `112-RECON.md` exists with the 107-RECON section skeleton and a filled 31-state ranking table sorted by FY2024 GF total, marking the 10 candidates and any rank-correction flags
- The ranking table row count is exactly 31 and contains none of the 19 ACFR states
- `112-BATCH1-SOURCES.md` exists with the 7-section skeleton mirroring 107-BATCH1-SOURCES.md
  </acceptance_criteria>
  <done>Workspace ready, poppler confirmed, 31-state GF ranking written into the scaffolded 112-RECON.md with candidate/rank-correction marks, and 112-BATCH1-SOURCES.md skeleton in place with per-state placeholders for AZ/IN/CO/MO/KY.</done>
</task>

<task id="112-01-01" type="auto">
  <name>Task 1: Recon AZ + IN + CO (three states) — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-11 ~15-20 min/state effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-12 no-minimum-depth)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- .planning/followups/ca-acfr-reconciliation.md (browser-download fallback when a 403/soft-404 appears — do not treat as a hard blocker; tn.gov needed a browser User-Agent in v2.13)
- scripts/processNJAcfr.js (freshest template generation with the Phase-111 ephemeral data_sources lifecycle — the default clone target for layout mapping)
  </read_first>
  <action>
Recon **Arizona**, **Indiana**, and **Colorado** — each within the ~15-20 min/state D-11 effort budget. For EACH state: (1) Locate its ACFR Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances* → **GENERAL FUND column** (GAAP — NOT the government-wide Statement of Activities, NOT any budgetary/forecast schedule). Expected publishers (verify, don't trust): AZ = General Accounting Office, Arizona Dept of Administration (gao.az.gov); IN = State Comptroller (in.gov/comptroller, formerly Auditor of State); CO = Office of the State Controller (Dept of Personnel & Administration). (2) Discover the durable per-year URL pattern on that state's ACFR archive page; download the latest FY and the oldest cleanly-reachable FY (windows stop at FY2002 — the pre-GASB-34 boundary is Phase 115 territory, not a gap) into `_acfr-work/{az,in,co}/`, filtering every download by Content-Type `application/pdf` / size (never HTTP status — soft-404 caution; browser-UA fallback before treating a 403 as a blocker). (3) Run `pdftotext -table` on both bookend PDFs; confirm the GF column "Total revenues" ties to the printed page total (D-05) — record the actual dollar figures. (4) Pin the four risk facts (D-08): units (thousands/millions/dollars — the NJ-dollars trap), any negative GF category in the bookend years (P2 clamp anticipation), the exact GF column header + confirmation it is the Governmental Funds statement General Fund column, and the FY-end month (the MI Sep-30 class). (5) Check the recency floor (D-07): confirm the clean window covers FY2023 + FY2024, or flag as a load-phase blocker; a shallow window that meets the floor is roster-eligible (D-12). (6) Compare ACFR GF scope vs the state's NASBO GF magnitude (D-09) and recommend accept-relabel if materially divergent (TX/PA/IL/MI precedent, 1.14x-3.56x seen in v2.13). (7) Map to the closest loader template (v2.13 family first — all carry the Phase-111 ephemeral data_sources lifecycle). Gap-log any FY that 404s/soft-404s/won't tie/lacks a durable URL, and stop at the effort budget. A state that won't cleanly `-table`-extract at all or fails the recency floor is marked **SUBSTITUTION CANDIDATE (D-01)** for plan 112-03 — do not reach for a substitute here. Transcribe all findings into the corresponding rows of every table in `112-BATCH1-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md -Pattern "Arizona|Indiana|Colorado" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- AZ, IN, CO each have a per-state block with the located statement (page), GF column header, units, FY-end month, durable per-year URL pattern, and clean window
- Each of the 3 states has its oldest + latest FY bookend GF Total revenues recorded with an actual dollar figure and a printed-total tie check (D-05)
- Each of the 3 states has all four risk facts pinned (D-08); recency-floor verdict (FY2023+FY2024 covered, or blocker flagged) recorded (D-07)
- Each of the 3 states has a scope-vs-NASBO magnitude + accept-relabel recommendation where applicable (D-09), and a loader-template mapping
- Every non-extracting / non-durable / non-tying FY for these 3 states is in the gap log with a reason; no soft-404 HTML mistaken for a PDF; any extraction/recency failure is marked SUBSTITUTION CANDIDATE for 112-03
  </acceptance_criteria>
  <done>AZ, IN, CO fully reconned: located, bookend-tied with dollar figures, four risk facts pinned, recency floor checked, scope compared, template mapped, gaps logged — all transcribed into 112-BATCH1-SOURCES.md.</done>
</task>

<task id="112-01-02" type="auto">
  <name>Task 2: Recon MO + KY (two states) — locate, bookend-tie, pin risk facts</name>
  <files>.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md</files>
  <read_first>
- .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-CONTEXT.md (D-11 effort budget, D-05 bookend, D-06 durable-or-exclude, D-07 recency floor, D-08 four risk facts, D-09 scope divergence, D-12 no-minimum-depth)
- .planning/milestones/v2.13-phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (target statement + GF column + units confirmation pattern)
- scripts/processILAcfr.js (simplest GF-statement layout: General Fund | Other Nonmajor | Total — a template-mapping candidate)
  </read_first>
  <action>
Recon **Missouri** and **Kentucky** — each within the ~15-20 min/state D-11 effort budget, using the same procedure as Task 1 (locate the Governmental Funds *Statement of Rev/Exp/Changes* → GENERAL FUND column; find the durable per-year URL pattern; download + `pdftotext -table` the latest + oldest cleanly-reachable FY bounded at FY2002, filtering by Content-Type/size with browser-UA fallback; bookend-tie-confirm the GF Total revenues to the printed total with actual dollar figures per D-05; pin the four risk facts per D-08; check the recency floor per D-07; compare scope vs NASBO per D-09; map the loader template; gap-log every non-extracting/non-durable/non-tying FY; mark extraction/recency failures SUBSTITUTION CANDIDATE (D-01) for 112-03). Expected publishers (verify, don't trust): MO = Office of Administration, Division of Accounting; KY = Finance and Administration Cabinet, Office of the Controller. Transcribe all findings into the MO and KY rows of every table in `112-BATCH1-SOURCES.md`.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md -Pattern "Missouri|Kentucky" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- MO and KY each have a per-state block with the located statement (page), GF column header, units, FY-end month, durable per-year URL pattern, and clean window
- Each has its oldest + latest FY bookend GF Total revenues recorded with an actual dollar figure and a printed-total tie check (D-05)
- Each has all four risk facts pinned (D-08); recency-floor verdict recorded (D-07); scope-vs-NASBO magnitude + accept-relabel recommendation (D-09); loader-template mapping
- Every non-extracting / non-durable / non-tying FY for MO/KY is in the gap log with a reason; extraction/recency failures marked SUBSTITUTION CANDIDATE for 112-03
  </acceptance_criteria>
  <done>MO and KY fully reconned and transcribed into 112-BATCH1-SOURCES.md; Batch-1 SOURCES doc complete for all 5 candidates across all 7 sections.</done>
</task>

</tasks>

<verification>
- `112-RECON.md` scaffold exists with the filled 31-state NASBO-2025-SER GF ranking table (31 rows, none of the 19 ACFR states) and candidate/rank-correction marks.
- `112-BATCH1-SOURCES.md` has a complete per-state block for all 5 Batch-1 candidates (AZ, IN, CO, MO, KY) across all 7 sections.
- Every state's oldest + latest bookend FY GF Total revenues is recorded with an actual dollar figure and tied to its printed page total via `pdftotext -table`.
- Every state's four risk facts (units / negatives / exact GF column+statement / FY-end month) are pinned; recency-floor (FY2023+FY2024) verdict recorded per state.
- The gap log records every deferred/non-durable/non-tying FY with a reason; durable-URL-mandatory honored (D-06); no soft-404 HTML mistaken for a PDF; extraction/recency failures marked SUBSTITUTION CANDIDATE rather than substituted in-plan (D-01 belongs to 112-03).
- No DB writes, no loader edits, no NASBO mutations, no frontend changes; $0 spend (pdftotext only, no AI).
</verification>

<success_criteria>
The 31-state ranking is written and the 5 Batch-1 candidates are located + bookend-tie-confirmed with dollar figures, four risk facts pinned, recency floor checked, scope-vs-NASBO compared, loader template mapped, and gap log written — the Phase 113 input contract for AZ/IN/CO/MO/KY is complete and documentation-only.
</success_criteria>

<output>
Create `.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-01-SUMMARY.md` when done.
</output>
