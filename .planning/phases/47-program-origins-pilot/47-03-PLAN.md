---
phase: 47-program-origins-pilot
plan: 03
type: execute
wave: 3
depends_on: [47-02]
autonomous: true
files_modified:
  - C:/EV-Accounts/backend/src/lib/treasuryService.ts
  - C:/EV-Accounts/backend/src/routes/treasury.ts
  - src/components/federal/ProgramOrigins.tsx
  - src/App.tsx
requirements:
  - ORIG-02
  - ORIG-03
must_haves:
  truths:
    - "Drilling into a category with a program_details row shows a 'Where this program comes from' section: enabling bill, public law, year, sponsor, cosponsors — each value a link to its official record"
    - "Foundational programs display the sponsor-boundary note instead of empty fields"
    - "Non-federal entities and federal categories without rows render exactly as before"
    - "47-VERIFICATION.md: ORIG-01/02/03 PASS lines with evidence"
  artifacts:
    - path: "src/components/federal/ProgramOrigins.tsx"
      provides: "Tier 2 details display"
      contains: "Public Law"
    - path: ".planning/phases/47-program-origins-pilot/47-VERIFICATION.md"
      provides: "Phase evidence"
      contains: "ORIG-03"
  key_links:
    - from: "src/components/federal/ProgramOrigins.tsx"
      to: "treasury.program_details"
      via: "backend join/endpoint by name_key"
      pattern: "program_details"
---

<objective>
Make the origins visible: serve program_details through the API and render the details section where citizens drill. Close the phase with verification.

Output: backend exposure (lightest touch — decide join-into-categories vs /federal/programs after reading getBudgetById), ProgramOrigins component, deploys, 47-VERIFICATION.md.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-program-origins-pilot/47-CONTEXT.md
@.planning/phases/47-program-origins-pilot/47-PROGRAMS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend exposure</name>
  <files>C:/EV-Accounts/backend/src/lib/treasuryService.ts, C:/EV-Accounts/backend/src/routes/treasury.ts</files>
  <action>
    Read getBudgetById's enrichment-attachment path. Choose the lighter of: (a) LEFT JOIN program_details by (name_key, municipality_id) into the category/enrichment mapping (additive 'programOrigins' field on NestedCategory), or (b) GET /federal/programs returning all rows keyed by name_key (frontend matches client-side). Either way: additive only; existing consumers unchanged; numbers Number()-cast; details jsonb passed through. Implement, curl-verify locally, push (Render auto-deploys — ONLY my files committed, same care as 45-01), curl-verify production.
  </action>
  <verify><automated>Production response carries origins for a known name_key (e.g. 'social security'); Plano responses byte-stable</automated></verify>
  <acceptance_criteria>
    - Additive, deployed, verified
  </acceptance_criteria>
  <done>Origins data reaches the frontend.</done>
</task>

<task type="auto">
  <name>Task 2: ProgramOrigins component + wiring</name>
  <files>src/components/federal/ProgramOrigins.tsx, src/App.tsx</files>
  <action>
    Component: "Where this program comes from" card rendered beneath the enrichment description in the drill view (the App.tsx block at ~line 850 where shortDesc/desc render) when the current category has origins data. Fields as labeled rows, every value an external link (SourceChip idiom or inline links with rel=noopener): Enabling bill → enabling_bill_url; Public law → public_law_url; Enacted → year; Sponsor → sponsor_url; Cosponsors (n) → cosponsors_url. Foundational rows: render the details-jsonb sponsor_note in place of sponsor/cosponsors. details jsonb extras render as additional rows (field/value/link). Plain language labels; zero editorial text. Dark mode + mobile per existing tokens. tsc + build; push (Netlify).
  </action>
  <verify><automated>Dev: drilling Social Security shows the card with linked P.L./year and the boundary note; a category without origins shows nothing; build green</automated></verify>
  <acceptance_criteria>
    - Renders only when data exists; every claim is a link; safety line held (official record fields only)
  </acceptance_criteria>
  <done>Citizens can see where programs come from, with receipts.</done>
</task>

<task type="auto">
  <name>Task 3: 47-VERIFICATION.md</name>
  <files>.planning/phases/47-program-origins-pilot/47-VERIFICATION.md</files>
  <action>
    - ORIG-01: key verified (Phase pre-work) — reference
    - ORIG-02: row count + per-field URL sweep (sample 8 URLs incl. sponsor + cosponsors pages → 200) + live display screenshot/description
    - ORIG-03: loader-code audit statement (no generation step; field-by-field response tracing) + safety-line check (no personal info beyond official records; member pages are official congress.gov records)
    - Regression: city pages + federal categories without origins unchanged
  </action>
  <verify><automated>PASS lines with evidence per requirement</automated></verify>
  <acceptance_criteria>
    - Phase goal proven
  </acceptance_criteria>
  <done>Phase 47 closes with evidence.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Safety | sponsor display | mitigate | Official congress.gov member pages only; name/party/state as recorded; nothing else |
| T-47-05 | Tampering | shared repo push | mitigate | Stage only my files; check unpushed-commit state BEFORE pulling (45-01 lesson) |
| T-47-SC | Tampering | installs | accept | None |
</threat_model>

<verification>
1. Origins render in production for ≥3 spot-checked programs
2. ORIG-01/02/03 PASS; regression clean
</verification>

<success_criteria>
- Phase 47 complete: the "details" standard exists end-to-end for the votes/amendments hub to build on
</success_criteria>

<output>
Create .planning/phases/47-program-origins-pilot/47-03-SUMMARY.md when done
</output>
