---
phase: 47-program-origins-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/47-program-origins-pilot/47-PROGRAMS.md
autonomous: true
requirements:
  - ORIG-02
  - ORIG-03
must_haves:
  truths:
    - "15–20 programs selected, each with a LIVE-PROBED authoritative record: Congress.gov bill identifiers (modern) or GovInfo STATUTE package (foundational), with the fetched official title recorded verbatim"
    - "Every program→act mapping is supported by the fetched title/record per the 47-CONTEXT mapping discipline; unsupportable candidates are skipped with rationale"
    - "Each program is mapped to an exact category name_key from a SQL dump (function root, agency root, or subfunction node)"
  artifacts:
    - path: ".planning/phases/47-program-origins-pilot/47-PROGRAMS.md"
      provides: "The pilot contract: program → API identifiers → category mapping"
      contains: "packageId"
  key_links:
    - from: "47-PROGRAMS.md"
      to: "data/federal-programs.json (47-02)"
      via: "curated identifiers consumed by the fetch pipeline"
      pattern: "congress"
---

<objective>
Pin the pilot list: ~18 programs, each with a verified API record and a defensible mapping to a visible category. The mapping discipline (47-CONTEXT) is the ORIG-03 risk control — this plan is where it bites.

Output: 47-PROGRAMS.md — per-program: display name, tier (modern/foundational), API identifiers, fetched official title (verbatim), target name_key, skip list with rationales.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-program-origins-pilot/47-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: probe + select</name>
  <files>.planning/phases/47-program-origins-pilot/47-PROGRAMS.md</files>
  <action>
    For each 47-CONTEXT candidate (DATA_GOV_API_KEY from .env, never logged):
    - MODERN: locate the bill via Congress.gov search/known congress+type+number; fetch /v3/bill/{...}; record verbatim title, sponsor fullName, laws[], cosponsors count. The mapping stands ONLY if the fetched title (or law short-title within the fetched record) names the program.
    - FOUNDATIONAL: GovInfo STATUTE search; record packageId/granule, dateIssued, verbatim official title; public-law citation from the granule metadata.
    - Resolve each program to an exact name_key: SQL-dump the US category roots AND relevant subfunction/agency-lens nodes (depth 0-1, both lenses); choose the node a citizen would actually drill to. Programs with no reasonable node → skip with rationale.
    - Target 15–20 final; record skips (mapping unsupportable / no category node / API record not found) explicitly.
  </action>
  <verify><automated>47-PROGRAMS.md: every kept program has identifiers + verbatim fetched title + name_key; ≥15 kept; skips have rationales</automated></verify>
  <acceptance_criteria>
    - Zero memory-asserted mappings; every row probe-backed
  </acceptance_criteria>
  <done>The pilot list is evidence-backed before any row is written.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Info integrity | program→act mapping from memory | mitigate | Fetched-title rule; skip-with-rationale; trace recorded per program |
| T-47-02 | Info disclosure | API key | mitigate | Read from env; never printed/committed |
| T-47-SC | Tampering | installs | accept | None |
</threat_model>

<verification>
1. 47-PROGRAMS.md complete per must_haves
</verification>

<success_criteria>
- 47-02 can fetch/store with zero judgment calls left
</success_criteria>

<output>
Create .planning/phases/47-program-origins-pilot/47-01-SUMMARY.md when done
</output>
