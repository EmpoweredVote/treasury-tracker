---
phase: 47-program-origins-pilot
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - data/federal-programs.json
  - scripts/loadProgramOrigins.js
autonomous: true
requirements:
  - ORIG-02
  - ORIG-03
must_haves:
  truths:
    - "program_details holds one row per kept program, every populated field traceable to an API response captured at run time; sponsor/cosponsors NULL (with details-jsonb boundary note) for foundational programs"
    - "Every *_url field resolves: enabling_bill_url/sponsor_url/cosponsors_url → congress.gov pages; public_law_url → the GovInfo/congress.gov record"
    - "Zero LLM involvement — the loader is a deterministic structured fetch (the origins pipeline needs no generation at all)"
    - "Idempotent: re-run upserts identical rows (fetched_at refreshes)"
  artifacts:
    - path: "data/federal-programs.json"
      provides: "Curated program identifiers (committed human-judgment artifact)"
      contains: "name_key"
    - path: "scripts/loadProgramOrigins.js"
      provides: "Deterministic Congress.gov/GovInfo fetcher → program_details"
      contains: "program_details"
  key_links:
    - from: "scripts/loadProgramOrigins.js"
      to: "treasury.program_details"
      via: "upsert on (municipality_id, name_key)"
      pattern: "source_api"
---

<objective>
Build the structured fetch pipeline and load the rows: data/federal-programs.json (the 47-PROGRAMS list as machine identifiers) → loadProgramOrigins.js fetches each program's record live from Congress.gov/GovInfo and stores the fields VERBATIM with per-claim URLs. No LLM anywhere.

Output: 15–20 program_details rows, fully linked; both artifacts committed.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-program-origins-pilot/47-CONTEXT.md
@.planning/phases/47-program-origins-pilot/47-PROGRAMS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: data/federal-programs.json</name>
  <files>data/federal-programs.json</files>
  <action>
    Transcribe 47-PROGRAMS.md into machine form: per program { name_key, program_name, tier: 'modern'|'foundational', congress, bill_type, bill_number } or { ..., govinfo_package_id, govinfo_granule_id? }, plus _skipped with rationales. _meta documents the mapping discipline + that all VALUES are fetched at load time (this file holds identifiers only — facts come from the APIs).
    Add the .gitignore re-include for the file (pattern from Phase 46: !/data/federal-programs.json).
  </action>
  <verify><automated>JSON validates; entries match 47-PROGRAMS.md 1:1; file not gitignored</automated></verify>
  <acceptance_criteria>
    - Identifiers only — zero fact fields in the file
  </acceptance_criteria>
  <done>The curated list is reviewable and re-runnable.</done>
</task>

<task type="auto">
  <name>Task 2: loadProgramOrigins.js + run + verify</name>
  <files>scripts/loadProgramOrigins.js</files>
  <read_first>
    - scripts/loadFederalEnrichment.js — loadEnv/upsert/dry-run conventions
    - Phase 43 migration 20260612100200 — program_details columns
  </read_first>
  <action>
    Deterministic loader, --dry-run flag, DATA_GOV_API_KEY from env (halt if absent; never log it):
    - MODERN: GET /v3/bill/{congress}/{type}/{number}?api_key= → store program_name (from json), enabling_bill = "{TYPE} {number} ({congress}th Congress): {fetched title}", enabling_bill_url = https://www.congress.gov/bill/{congress}th-congress/{type-slug}/{number}, public_law = "P.L. {laws[0].number}" (from response; skip field if absent), public_law_url = congress.gov law page, enacted_year from latestAction/laws date in response, sponsor = sponsors[0].fullName, sponsor_url = congress.gov member page (bioguideId from response), cosponsors_count + cosponsors_url (the bill's cosponsors page). source_api='congress.gov'.
    - FOUNDATIONAL: GET GovInfo package/granule summary → public_law citation + dateIssued year + official title; sponsor fields NULL; details jsonb: [{field:'official_title', value:<fetched>, source_url:<govinfo link>}, {field:'sponsor_note', value:'Sponsor records predate the Congress.gov API's structured coverage (93rd Congress, 1973).', source_url:<congress.gov coverage doc or about page>}]. source_api='govinfo'.
    - EVERY stored value either copied from the API response or a deterministic URL template around response identifiers — grep-auditable by reading the mapping function.
    - Rate courtesy: sequential with ~300ms delay (well under 1,000/hr).
    - Upsert on (municipality_id=US, name_key); --dry-run prints the full row set without writing.
    Run dry → live → re-run. Verify SQL: row count = kept programs; foundational rows have NULL sponsor + boundary note; zero NULL source_api; curl 5 sampled *_url fields → 200.
  </action>
  <verify><automated>SQL checks + URL sweep green; idempotent; dry-run row dump in summary</automated></verify>
  <acceptance_criteria>
    - ORIG-02 data complete; ORIG-03 honored by construction (no generation step exists)
  </acceptance_criteria>
  <done>Every origin fact in the DB came from an API response this run.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Info integrity | hand-typed facts sneaking into JSON | mitigate | File schema holds identifiers only; loader stores from responses |
| T-47-02 | Info disclosure | API key | mitigate | env only; URLs stored WITHOUT api_key param (public page URLs) |
| T-47-SC | Tampering | installs | accept | None |
</threat_model>

<verification>
1. Rows = kept list; per-claim URLs resolve; foundational boundary notes present
2. Loader code review: every field assignment traces to response or URL template
</verification>

<success_criteria>
- ORIG-02 satisfied at the data layer; pipeline re-runnable for future program additions
</success_criteria>

<output>
Create .planning/phases/47-program-origins-pilot/47-02-SUMMARY.md when done
</output>
