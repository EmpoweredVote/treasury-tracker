---
phase: 36-selective-city-retrofit
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - scripts/extractPortland.py
  - scripts/processPortland.js
autonomous: true
requirements: [RETROFIT-02]
must_haves:
  truths:
    - "extractPortland.py populates each operating row's service_area from the PDF pages 12-13 mapping table"
    - "processPortland.js buildOperatingTree emits a 3-level tree: service_area (depth-0) → bureau (depth-1) → line item"
    - "A dry-run for FY2026 prints ~8 service area nodes grouping the 34 bureaus with the city total reconciled"
    - "processPortland.js no longer contains a hardcoded SUPABASE_URL fallback (WR-04 fix)"
  artifacts:
    - path: "scripts/extractPortland.py"
      provides: "Service area extraction + populated service_area field per row"
      contains: "extract_service_area_map"
    - path: "scripts/processPortland.js"
      provides: "3-level operating tree builder (service_area → bureau → items)"
      contains: "buildOperatingTree"
  key_links:
    - from: "scripts/extractPortland.py extract_service_area_map"
      to: "row['service_area']"
      via: "service_area_map.get(bureau_name, '')"
      pattern: "service_area_map"
    - from: "scripts/processPortland.js buildOperatingTree"
      to: "treasury_sync_budget_tree node shape { n, a, c:[{ n, a, i:[...] }] }"
      via: "service_area node has c[] children, bureau children have i[]"
      pattern: "buildOperatingTree"
---

<objective>
Retrofit the Portland operating loader to produce a genuine 3-level tree (Service Area → Bureau → line items) per the confirmed audit (RETROFIT-02, D-05/D-06/D-07). Code + dry-run validation only — no live DB writes (those happen in Wave 3 / plan 36-04 after the dry-run is verified).

Purpose: Portland's source genuinely supports a Service Area grouping (8 citizen-recognizable areas in the official Vol 1 user's guide table, pages 12-13). The current loader emits 34 flat bureau leaves. This adds the service-area parent level.

Output:
- `scripts/extractPortland.py` with `extract_service_area_map()` and populated `service_area` per row
- `scripts/processPortland.js` with a 3-level `buildOperatingTree()` and the WR-04 SUPABASE_URL fix
- Verified FY2026 dry-run output (no DB writes)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-selective-city-retrofit/36-CONTEXT.md
@.planning/phases/36-selective-city-retrofit/36-RESEARCH.md
@.planning/phases/36-selective-city-retrofit/36-PATTERNS.md
@scripts/processCA.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extract_service_area_map() to extractPortland.py and populate service_area per row</name>
  <files>scripts/extractPortland.py</files>
  <behavior>
    - extract_service_area_map(pdf) returns a dict { bureau_name: service_area_name } with ≥5 service areas for FY2025-26 (research confirms 8: Public Safety, Public Works, City Operations, Community & Economic Development, City Administrator, City Council, Office of the City Auditor, Office of the Mayor)
    - The table is located by keyword search ('Managing Agency' + 'Service Area' in page text) across pages, NOT by hardcoded page index (Pitfall 1 / Pitfall 5 guard — page range shifts across fiscal years)
    - Each output operating row gets service_area = service_area_map.get(bureau_name, '') (empty string when unmapped — D-06 collapse handled downstream in the loader, NOT a synthetic node here)
    - A WARNING is printed to stderr listing any bureaus with no service_area mapping (analogous to the existing none_fy warning pattern)
    - "Office of Vibrant Communities" ($0 budget) is already filtered by the zero-amount filter — no special handling
  </behavior>
  <read_first>
    - scripts/extractPortland.py (CURRENT full file — the existing service_area:'' placeholder around lines 203-208, extract_budget() signature ~line 105, the none_fy warning pattern, the existing pdfplumber open/iterate/extract_tables idiom; imports at lines 19-21)
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§scripts/extractPortland.py lines 23-112 — exact extract_service_area_map() body, modified extract_budget() signature, service_area assignment, warning guard)
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (Pattern 1 lines 295-329; Pitfall 1 lines 440-445; Pitfall 5 lines 468-473; §City 1 Portland lines 119-156)
  </read_first>
  <action>
    Insert a new `extract_service_area_map(pdf)` function before `extract_budget()`. It searches pages (0-indexed range ~9-20, bounded by len(pdf.pages)) for a page whose extract_text() contains BOTH 'Managing Agency' AND 'Service Area', then reads `page.extract_tables()[0]` rows shaped `[Managing Agency, Fund, Service Area, Fund Type]`. Build `{ bureau_name: service_area }`: when row[0] (agency) and row[2] (service area, excluding header values 'Service Area'/'SERVICE AREA'/'') are both present, set current_bureau and map it; for continuation rows (blank agency, has service area) preserve the existing mapping for current_bureau. No new imports needed (pdfplumber, sys, re, json already imported).

    Modify `extract_budget(pdf_path, service_area_map=None)`: inside the `with pdfplumber.open(...)` block, if service_area_map is None, build it via extract_service_area_map(pdf) and print `Service area map: {len} bureaus` to stderr. Change the row append from `'service_area': ''` to `'service_area': service_area_map.get(bureau_name, '')`. After the results loop, add a stderr WARNING listing bureaus where service_area is empty (mirror the none_fy warning format). Do NOT synthesize a service area for unmapped bureaus — leave empty; the loader collapses them per D-06.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync('py scripts/extractPortland.py --pdf \"docs/Portland/fy2025-26-vol1.pdf\" 2>&1 || python scripts/extractPortland.py --pdf \"docs/Portland/fy2025-26-vol1.pdf\" 2>&1',{encoding:'utf8',maxBuffer:16*1024*1024});const j=JSON.parse(out.slice(out.indexOf('[')));const withSA=j.filter(r=>r.service_area&&r.service_area.length>0);if(withSA.length<20){console.error('too few rows with service_area: '+withSA.length);process.exit(1)}const sas=new Set(withSA.map(r=>r.service_area));if(sas.size<5){console.error('too few distinct service areas: '+sas.size);process.exit(1)}console.log('service_area populated: '+withSA.length+' rows, '+sas.size+' areas')" </automated>
  </verify>
  <acceptance_criteria>
    - `scripts/extractPortland.py` contains `def extract_service_area_map(`
    - Running the extractor on `docs/Portland/fy2025-26-vol1.pdf` produces JSON rows where ≥20 rows have a non-empty `service_area` and ≥5 distinct service area values appear
    - The function locates the table by keyword search ('Managing Agency' + 'Service Area' in page text), not a hardcoded page index — source contains `'Managing Agency'` and `'Service Area'` text guards
    - Unmapped bureaus produce a stderr WARNING, not a synthetic service area node
    - No new top-of-file imports added (verify imports unchanged from current lines 19-21)
  </acceptance_criteria>
  <done>extractPortland.py emits service_area per operating row from the PDF mapping table, located by keyword search, with empty fallback for unmapped bureaus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite buildOperatingTree() in processPortland.js as a 3-level builder + apply WR-04 SUPABASE_URL fix</name>
  <files>scripts/processPortland.js</files>
  <behavior>
    - buildOperatingTree(rows) returns { tree, total } where tree is an array of service-area branch nodes shaped { n: service_area, a: sum, c: [{ n: bureau, a: amt, i: [{ d: bureau, a: amt, aa:null, f:null, e:null }] }] }
    - Bureaus with no service_area (empty string) are NOT merged into a shared bucket — each becomes a standalone depth-0 leaf node { n: bureau, a, i:[...] } and logs a [D-06] warning (matches processCA.js CR-01 collapse-and-log pattern)
    - Nodes are sorted by amount descending at every level
    - total equals the sum of all bureau amounts (reconciles to the prior flat-tree total — no rows dropped, no double-counting)
    - The FY2026 dry-run prints service-area count and bureau count
    - SUPABASE_URL has NO hardcoded fallback — process exits with a clear error if SUPABASE_URL is unset
  </behavior>
  <read_first>
    - scripts/processPortland.js (CURRENT full file — buildOperatingTree() ~lines 138-161 to replace; SUPABASE_URL line 58 hardcoded fallback to fix; the treasury_sync_budget_tree RPC call ~lines 235-244 which is already correct; the dry-run output block ~lines 315-319)
    - scripts/processCA.js (buildNLevelTree lines 159-220 — reference recursive N-level builder, Map-accumulate → sort-descending → emit; SUPABASE_URL guard lines 61-62 — the WR-04 target pattern; RPC call lines 256-269)
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§scripts/processPortland.js lines 116-228 — exact target buildOperatingTree, SUPABASE_URL fix lines 199-207, dry-run output update lines 222-228)
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (Pattern 2 lines 331-367; §N-Level Tree Node Shape; §D-06 Null Collapse)
  </read_first>
  <action>
    Replace `buildOperatingTree(rows)` entirely with the 3-level implementation: group rows into a Map keyed by service_area; for empty service_area use a unique key `__no_sa__${bureau}` (prevents merging unmapped bureaus) and console.warn a `[D-06]` message naming the bureau and amount. Sum amounts per (service_area, bureau). Emit: for normal service areas, `{ n: service_area, a: saTotal, c: bureauNodes }` where each bureau node is `{ n: bureau, a: amt, i: [{ d: bureau, a: amt, aa: null, f: null, e: null }] }`; for `__no_sa__` keys, emit a standalone depth-0 leaf `{ n: bureau, a, i: [...] }` (D-06 — no synthetic "Unknown Service Area" parent). Sort bureau children descending by `a`, sort top-level nodes descending by `a`. Return `{ tree: nodes, total }`.

    Apply the WR-04 fix at line 58: replace `const SUPABASE_URL = process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co';` with `const SUPABASE_URL = process.env.SUPABASE_URL;` followed by `if (!SUPABASE_URL) { console.error('Missing SUPABASE_URL'); process.exit(2); }` (copy the guard from processCA.js lines 61-62). Update the dry-run output to print service-area count (`tree.filter(n => n.c).length`) and bureau count (`tree.reduce((s,n)=>s+(n.c?n.c.length:1),0)`). Leave the treasury_sync_budget_tree RPC call unchanged — it already passes the N-level `tree` array. Do NOT run a live load in this task; only `--dry-run`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/processPortland.js','utf8');if(/process\.env\.SUPABASE_URL\s*\|\|\s*'https/.test(s)){console.error('hardcoded SUPABASE_URL fallback still present');process.exit(1)}if(!/buildOperatingTree/.test(s)){console.error('buildOperatingTree missing');process.exit(1)}const {execSync}=require('child_process');const out=execSync('node scripts/processPortland.js --dry-run --pdf \"docs/Portland/fy2025-26-vol1.pdf\" 2>&1',{encoding:'utf8',maxBuffer:16*1024*1024});if(!/service area/i.test(out)){console.error('dry-run did not report service areas:\\n'+out.slice(-800));process.exit(1)}console.log('processPortland.js 3-level dry-run OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' scripts/processPortland.js` contains NO `process.env.SUPABASE_URL || 'https` (WR-04 fix applied)
    - `scripts/processPortland.js` contains `buildOperatingTree` and emits service-area nodes with a `c` array of bureau children that each carry an `i` line-items array
    - `node scripts/processPortland.js --dry-run --pdf "docs/Portland/fy2025-26-vol1.pdf"` exits 0 and prints a service-area count and bureau count for FY2026
    - The dry-run reports ~8 service areas grouping ~34 bureaus, and the printed total reconciles to the prior flat Portland FY2026 operating total (record both in SUMMARY)
    - Bureaus with empty service_area appear as standalone depth-0 leaves with a logged [D-06] warning, NOT under a synthetic parent
  </acceptance_criteria>
  <done>processPortland.js builds a 3-level operating tree, dry-run validated for FY2026, with the WR-04 fix applied and no live DB write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file → extractor | PDF path comes from controlled docs/Portland/ readdir (T-17-03/T-23-02), not user input |
| extractor stdout → loader | JSON parsed by processPortland.js; amounts validated by existing parse logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Tampering | PDF path injection | mitigate | Maintain T-23-02: PDF path from controlled docs/Portland/ readdir, quoted in execSync |
| T-36-04 | Information disclosure | hardcoded SUPABASE_URL fallback | mitigate | WR-04 fix — remove `|| 'https://...'`; fail closed if env unset |
| T-36-05 | Tampering | malformed PDF table rows | mitigate | None-guard on table rows (len < 3 skip); empty service_area falls back to D-06 collapse, not crash |
| T-36-SC | Tampering | npm/pip installs | mitigate | No installs — pdfplumber + @supabase/supabase-js already present (36-RESEARCH §Package Legitimacy Audit — none added) |
</threat_model>

<verification>
- extractPortland.py FY2025-26 run: ≥20 rows with service_area, ≥5 distinct areas.
- processPortland.js: no hardcoded SUPABASE_URL fallback; dry-run prints service-area + bureau counts; total reconciles.
- No live DB writes performed in this plan.
</verification>

<success_criteria>
- Portland 3-level loader is code-complete and dry-run validated for FY2026 (RETROFIT-02 code half).
- WR-04 security fix applied to processPortland.js.
</success_criteria>

<output>
Create `.planning/phases/36-selective-city-retrofit/36-02-SUMMARY.md` when done. MUST record: distinct service-area count, bureau count, and FY2026 total reconciliation (new 3-level total vs prior flat total).
</output>
