---
phase: 36-selective-city-retrofit
plan: 04
type: execute
wave: 3
depends_on: ["36-02", "36-03"]
files_modified:
  - .planning/phases/36-selective-city-retrofit/36-VERIFICATION.md
autonomous: false
requirements: [RETROFIT-02, RETROFIT-03]
must_haves:
  truths:
    - "Portland FY2026 operating loads live as a 3-level tree (service_area depth-0, bureau depth-1, items depth-2)"
    - "Dallas FY2026 operating loads live as a 3-level tree (department depth-0, service depth-1, objectgroup depth-2)"
    - "Existing enrichment description rows for Portland and Dallas remain in the DB (none deleted)"
    - "New depth-0 nodes (Portland service areas, Dallas departments) are enriched within the $5 cost gate"
    - "Both retrofitted cities show a 3-level icicle drill-down in the live app; non-retrofitted cities show no regression"
  artifacts:
    - path: ".planning/phases/36-selective-city-retrofit/36-VERIFICATION.md"
      provides: "Phase 36 verification record (depth distributions, enrichment counts, human spot-check results)"
      contains: "RETROFIT-03"
  key_links:
    - from: "processPortland.js / bulkLoadBudget.js live load"
      to: "treasury.budget_categories depth 0/1/2 rows"
      via: "treasury_sync_budget_tree RPC"
      pattern: "treasury_sync_budget_tree"
    - from: "enrichCategories.js --depth"
      to: "new depth-0 service-area / department nodes"
      via: "name_key upsert (existing format unchanged)"
      pattern: "enrichCategories"
---

<objective>
Live-load the Portland and Dallas 3-level retrofits, preserve existing enrichment (D-10/D-11), enrich new nodes within the $5 gate (D-12), and human-verify the 3-level icicle in the live app with no regression to other cities (RETROFIT-02, RETROFIT-03).

Purpose: Wave 2 produced dry-run-validated 3-level loaders. This plan performs the actual reload, handles enrichment preservation, and closes the phase with end-to-end live verification.

Output:
- Portland + Dallas FY2026 operating reloaded as live 3-level trees
- Enrichment new-node run (cost-gated) with existing rows intact
- `.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-selective-city-retrofit/36-CONTEXT.md
@.planning/phases/36-selective-city-retrofit/36-RESEARCH.md
@.planning/phases/36-selective-city-retrofit/36-PATTERNS.md
@.planning/phases/36-selective-city-retrofit/36-01-SUMMARY.md
@.planning/phases/36-selective-city-retrofit/36-02-SUMMARY.md
@.planning/phases/36-selective-city-retrofit/36-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture enrichment baselines, live-load Portland + Dallas FY2026 3-level trees, verify depth + total</name>
  <files>.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/36-selective-city-retrofit/36-01-SUMMARY.md (the RPC depth-change verdict — whether a pre-reload DELETE of budget_categories by data_source_id+fiscal_year is required; the Portland baseline depth distribution)
    - .planning/phases/36-selective-city-retrofit/36-02-SUMMARY.md (Portland dry-run total reconciliation, service-area + bureau counts)
    - .planning/phases/36-selective-city-retrofit/36-03-SUMMARY.md (Dallas dry-run total reconciliation, department count, backward-compat baseline)
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (§Enrichment impact for Portland lines 152-156 and Dallas lines 194-199; §Validation Architecture Phase Requirements → Test Map lines 658-666)
    - scripts/processPortland.js, scripts/bulkLoadBudget.js (the live-load invocation — confirm the non-dry-run path)
  </read_first>
  <action>
    Step 1 — Baselines: via `mcp__supabase-local__execute_sql`, capture the pre-reload enrichment row counts for Portland (OR) and Dallas (TX). Use the enrichment table referenced in 36-RESEARCH (`SELECT count(*) FROM treasury.category_enrichment WHERE municipality_id = (SELECT id FROM treasury.municipalities WHERE name='Portland' AND state='OR')` — adjust table/column names to the actual schema; STATE.md says Portland has ~41 rows, Dallas TBD). Record both counts in 36-VERIFICATION.md as the "before" baseline.

    Step 2 — Pre-reload DELETE (conditional): if 36-01-SUMMARY concluded the RPC accumulates rather than replaces, run the documented DELETE of budget_categories for the Portland operating data_source_id + FY2026 and the Dallas operating data_source_id + FY2026 BEFORE reloading. If 36-01 concluded the RPC replaces, skip this step.

    Step 3 — Live load: run `node scripts/processPortland.js --fy 2026` (operating, live) and `node scripts/bulkLoadBudget.js --source "Dallas Operating" --fy 2026` (live). Limit to FY2026 only per research recommendation (older Portland years left flat — Open Question 1 / Pitfall 5; record this decision in 36-VERIFICATION.md).

    Step 4 — Verify depth + total: via `mcp__supabase-local__execute_sql`, query the depth distribution for each city's FY2026 operating budget. Portland must show depth 0 (service areas), 1 (bureaus), 2 (line items). Dallas must show depth 0 (departments), 1 (services), 2 (objectgroups). Confirm totals reconcile to the dry-run totals from Wave 2 SUMMARYs. Record before/after depth distributions and totals in 36-VERIFICATION.md.

    Step 5 — Enrichment intact: re-query the enrichment counts. The count MUST be >= the baseline (D-11: orphaned rows kept, never deleted). Record after-count in 36-VERIFICATION.md.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md','utf8');for(const t of ['Portland','Dallas','depth','enrichment']){if(!s.toLowerCase().includes(t.toLowerCase())){console.error('VERIFICATION missing: '+t);process.exit(1)}}console.log('VERIFICATION.md depth/enrichment record present')"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__supabase-local__execute_sql` depth query for Portland FY2026 operating returns rows at depth 0, 1, AND 2 (was `{"0":34}` before)
    - `mcp__supabase-local__execute_sql` depth query for Dallas FY2026 operating returns rows at depth 0, 1, AND 2 (was `{"0":188,"1":680}` before — now has a department layer above)
    - Post-reload enrichment row count for Portland >= pre-reload baseline; same for Dallas (D-11: no deletions)
    - Portland and Dallas FY2026 live totals match the Wave 2 dry-run totals recorded in 36-02/36-03 SUMMARYs
    - 36-VERIFICATION.md records before/after depth distributions, before/after enrichment counts, and the FY2026-only scope decision
  </acceptance_criteria>
  <done>Both cities are live 3-level trees for FY2026; depth distributions confirmed; existing enrichment rows intact (count not reduced).</done>
</task>

<task type="auto">
  <name>Task 2: Enrich new depth-0 nodes for Portland + Dallas behind the $5 cost gate</name>
  <files>.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (§Enrichment impact Portland lines 152-156 — 8 new service-area depth-0 nodes ~$0.002; Dallas lines 194-199 — ~50 new department depth-0 nodes ~$0.01; Pitfall 3 Portland name_key shift lines 454-459; §Code Examples enrichment --depth usage lines 511-519)
    - .planning/phases/36-selective-city-retrofit/36-CONTEXT.md (D-10/D-11/D-12 — enrichment preservation, orphaned-row policy, $5 gate)
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§Enrichment name_key Format lines 485-493 — do NOT change normalize(parent)|normalize(name) format)
    - scripts/enrichCategories.js (the --city/--state/--year/--depth flags, --dry-run, getExistingEnrichments, saveEnrichment name_key construction, cost estimation output)
  </read_first>
  <action>
    Run enrichment in DRY-RUN first to count nodes and estimate cost (D-12 $5 gate; per project memory feedback_api_cost_threshold — estimate before running, halt if >$5). For Portland new depth-0 service-area nodes: `node scripts/enrichCategories.js --city "Portland" --state OR --year 2026 --depth 0 --dry-run`. For Dallas new depth-0 department nodes: `node scripts/enrichCategories.js --city "Dallas" --state TX --year 2026 --depth 0 --dry-run`. Sum the estimated cost across both. Per research the combined estimate is ~$0.012 (well under $5). If the estimate exceeds $5, STOP and surface to the user before any live enrichment.

    If under $5, run both live (drop --dry-run). Do NOT alter the enrichCategories.js name_key format (Pitfall 3 / D-11): the script will re-enrich nodes whose key format shifted (Portland bureaus moving from root to parent|child) — that is expected; old keys remain in the DB as orphans (D-11, not deleted). Record in 36-VERIFICATION.md: dry-run node counts, estimated cost, actual cost, and confirmation that new depth-0 nodes now have descriptions. Do NOT enrich depth-1/depth-2 nodes in this phase unless the dry-run shows they are missing descriptions AND total stays under $5 (focus is new depth-0 nodes per D-12).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md','utf8');if(!/cost|estimat/i.test(s)){console.error('cost gate not recorded');process.exit(1)}if(!/\$/.test(s)){console.error('no cost figure recorded');process.exit(1)}console.log('cost gate recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - 36-VERIFICATION.md records the dry-run node counts and estimated cost for both Portland and Dallas depth-0 enrichment, and confirms estimate < $5 before any live run
    - `mcp__supabase-local__execute_sql` confirms new depth-0 nodes (Portland service areas; Dallas departments) have non-NULL/non-empty descriptions after the run
    - enrichCategories.js was run with the unchanged name_key format (no source edit to the normalize(parent)|normalize(name) logic)
    - 36-VERIFICATION.md records the actual enrichment cost
  </acceptance_criteria>
  <done>New depth-0 nodes are enriched within the $5 gate; existing descriptions preserved; name_key format unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — 3-level icicle live + no regression</name>
  <files>.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md</files>
  <action>
    Pause for human verification. The executor has completed the automated live load (Task 1) and cost-gated enrichment (Task 2); this task is a manual spot-check at treasurytracker.empowered.vote following the steps in how-to-verify. Do NOT auto-approve — wait for the human resume signal, then record the result in 36-VERIFICATION.md.
  </action>
  <what-built>
    Portland and Dallas FY2026 operating budgets reloaded as live 3-level trees, with new depth-0 nodes enriched. BudgetIcicle.tsx renders arbitrary depth (no frontend changes). This checkpoint confirms RETROFIT-03 end-to-end and guards against regression on non-retrofitted cities.
  </what-built>
  <how-to-verify>
    1. Visit https://treasurytracker.empowered.vote and open the Portland OR page. Confirm the Money Out / Budget icicle now shows a top level of Service Areas (Public Safety, Public Works, etc.). Click into a service area — confirm it drills to bureaus (depth-1). Click a bureau — confirm it drills to line items (depth-2 LineItemsTable).
    2. Confirm Portland bureau-level enrichment descriptions still appear (e.g., "Portland Parks & Recreation" still has its description) — D-10 preservation.
    3. Open the Dallas TX page. Confirm the icicle now shows a top level of Departments (Police Department, Dallas Fire Rescue, Water Utilities). Drill: department → service → object group line items.
    4. Regression spot-check: open at least 3 non-retrofitted cities/entities (e.g., San Jose, Los Angeles, and California state) — confirm they render correctly with their existing depth (no breakage from the bulkLoadBudget.js shared-code change).
    5. Confirm totals and per-capita on Portland and Dallas look correct (no doubled or halved totals).
  </how-to-verify>
  <verify>
    <human-check>All 5 spot-check steps pass: Portland 3-level icicle drill-down, Portland enrichment intact, Dallas 3-level icicle drill-down, 3 non-retrofitted entities render with no regression, totals/per-capita correct.</human-check>
  </verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe the issues observed.</resume-signal>
  <done>Human confirms RETROFIT-03 end-to-end: both retrofitted cities show 3-level icicle drill-down, existing enrichment intact, and no regression on non-retrofitted cities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → Supabase (RPC, service role) | Live tree writes via treasury_sync_budget_tree; idempotent per data_source_id+fiscal_year |
| enrichCategories.js → Anthropic API | AI cost-bearing; gated by $5 threshold and --dry-run-first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10 | Denial of service (financial) | enrichment API cost overrun | mitigate | $5 gate: --dry-run first, estimate, halt if >$5 (D-12, feedback_api_cost_threshold) |
| T-36-11 | Tampering | reload orphans prior-depth rows | mitigate | Wave 0 (36-01) determined replace-vs-accumulate; conditional pre-DELETE applied; depth distribution verified after |
| T-36-12 | Tampering | enrichment row loss | mitigate | D-11: baseline count captured before; post-count must be >= baseline; orphaned rows kept not deleted |
| T-36-13 | Tampering | shared bulkLoadBudget.js change regresses other cities | mitigate | Human regression spot-check of 3 non-retrofitted entities (San Jose / LA / CA state) |
| T-36-SC | Tampering | npm/pip installs | mitigate | No installs — all libraries already present (36-RESEARCH §Package Legitimacy Audit — none added) |
</threat_model>

<verification>
- Portland + Dallas FY2026 operating are live 3-level trees (depth 0/1/2 confirmed by DB query).
- Enrichment counts not reduced; new depth-0 nodes enriched under $5.
- Human confirms 3-level icicle drill-down on both cities and no regression on 3 other entities.
</verification>

<success_criteria>
- RETROFIT-02 complete: 2 cities (Portland, Dallas) reloaded as live 3-level trees.
- RETROFIT-03 complete: both display 3-level icicle drill-down; existing enrichment intact; non-retrofitted cities show no regression.
</success_criteria>

<output>
Create `.planning/phases/36-selective-city-retrofit/36-04-SUMMARY.md` when done, and finalize `.planning/phases/36-selective-city-retrofit/36-VERIFICATION.md` with the human spot-check result. Mark RETROFIT-02 and RETROFIT-03 satisfied.
</output>
