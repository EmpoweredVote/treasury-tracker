---
phase: 36-selective-city-retrofit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/AUDIT-FRAMEWORK.md
  - supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql
autonomous: true
requirements: [RETROFIT-01]
must_haves:
  truths:
    - "An engineer can read .planning/AUDIT-FRAMEWORK.md and apply the genuineness tests to any city"
    - "Each of the 3 pilot cities (Portland, Dallas, SF) has a recorded audit verdict in treasury.data_sources.audit_verdict"
    - "A reload that increases tree depth replaces prior depth rows rather than orphaning them (verified before any retrofit)"
  artifacts:
    - path: ".planning/AUDIT-FRAMEWORK.md"
      provides: "Reusable per-city audit framework (Socrata + PDF paths, genuineness tests, depth decision rule, Phase 36 verdicts)"
      contains: "Genuineness"
      min_lines: 60
    - path: "supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql"
      provides: "audit_verdict JSONB column on treasury.data_sources"
      contains: "audit_verdict"
  key_links:
    - from: "treasury.data_sources"
      to: "audit_verdict JSONB"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "audit_verdict JSONB"
    - from: ".planning/AUDIT-FRAMEWORK.md"
      to: "treasury.data_sources.audit_verdict"
      via: "doc names the DB column as machine-readable source of truth"
      pattern: "audit_verdict"
---

<objective>
Establish the durable audit framework (D-02/D-04), record the per-city verdict for all 3 pilot cities (D-01/D-03/D-09), and verify the highest-risk technical assumption (RPC depth-change replacement) BEFORE any retrofit code runs. This is the foundation Wave 2 retrofits depend on.

Purpose: D-04 requires the audit framework to live in TWO places — a human-readable markdown guide and machine-readable DB verdicts. RETROFIT-01 requires a completed source data audit identifying which cities have a genuine extractable 3rd level. The audit itself is already done in 36-RESEARCH.md; this plan persists it durably and de-risks the reload mechanics.

Output:
- `.planning/AUDIT-FRAMEWORK.md` (durable engineer guide + Phase 36 verdicts)
- `treasury.data_sources.audit_verdict` JSONB column (migration) populated for Portland, Dallas, SF
- Confirmed answer to "does the RPC replace vs accumulate when depth changes" (Open Question 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-selective-city-retrofit/36-CONTEXT.md
@.planning/phases/36-selective-city-retrofit/36-RESEARCH.md
@.planning/phases/36-selective-city-retrofit/36-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit_verdict JSONB column to treasury.data_sources and verify the RPC depth-change behavior</name>
  <files>supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql</files>
  <read_first>
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§DB migration: add_audit_verdict_to_data_sources — exact SQL pattern, lines 390-411; §DB Migration — Safe ALTER TABLE lines 516-523)
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (§Open Questions #3 RPC replace-vs-accumulate; #4 DB storage recommendation; §Runtime State Inventory)
    - supabase/migrations/20260606000000_add_state_entity_type.sql (analog migration — safe ALTER pattern, if present)
  </read_first>
  <action>
    Create migration file `supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql` that runs `ALTER TABLE treasury.data_sources ADD COLUMN IF NOT EXISTS audit_verdict JSONB DEFAULT NULL;` plus a `COMMENT ON COLUMN treasury.data_sources.audit_verdict` documenting the verdict shape: keys `recommended_depth` (integer), `evidence` (string), `last_audited` (ISO8601 string), `auditor` (string), `status` (one of `retrofit_recommended` | `depth_confirmed_current` | `audit_deferred`). Follow the idempotent `IF NOT EXISTS` pattern from the analog migration. Apply the migration with `mcp__supabase-local__apply_migration` (per project memory feedback_supabase_migration_mcp — do NOT ask the user to paste into the Dashboard). Then verify the column exists with `mcp__supabase-local__execute_sql` querying `information_schema.columns WHERE table_schema='treasury' AND table_name='data_sources' AND column_name='audit_verdict'`.

    Then resolve Open Question 3 (RPC replace vs accumulate on depth change) WITHOUT mutating production data: run `mcp__supabase-local__execute_sql` to capture the current Portland operating depth distribution baseline — `SELECT depth, count(*) FROM treasury.budget_categories bc JOIN treasury.budgets b ON bc.budget_id=b.id JOIN treasury.data_sources ds ON b.data_source_id=ds.id WHERE ds.municipality_id = (SELECT id FROM treasury.municipalities WHERE name='Portland' AND state='OR') GROUP BY depth ORDER BY depth` (adjust join columns to the actual schema discovered while reading). Record whether the RPC's contract (from 34-01-SUMMARY.md / treasury_sync_budget_tree behavior) replaces all categories for a given data_source_id + fiscal_year on each sync, or accumulates. If the RPC does NOT fully replace prior-depth rows, document in the SUMMARY the explicit pre-reload DELETE step (by data_source_id + fiscal_year) that Wave 2 plans 36-02/36-03 must run before reload. Do not perform the DELETE here — only determine and document the requirement.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql','utf8');if(!/audit_verdict\s+JSONB/i.test(s))process.exit(1);if(!/IF NOT EXISTS/i.test(s))process.exit(1);console.log('migration SQL OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file `supabase/migrations/20260609120000_add_audit_verdict_to_data_sources.sql` exists and contains the literal `audit_verdict JSONB` and `IF NOT EXISTS`
    - `mcp__supabase-local__execute_sql` query on `information_schema.columns` returns one row for column `audit_verdict` with data_type `jsonb`
    - SUMMARY records the RPC depth-change behavior verdict: either "RPC replaces all categories per data_source_id+fiscal_year on sync — no pre-DELETE needed" OR "RPC accumulates — Wave 2 must DELETE budget_categories for the data_source_id+fiscal_year before reload" with the exact query
    - The Portland baseline depth distribution is captured in the SUMMARY (expected `{"0": 34}` per research)
  </acceptance_criteria>
  <done>audit_verdict column exists and is queryable; RPC depth-change behavior is documented as a directive for Wave 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/AUDIT-FRAMEWORK.md and record the 3 pilot-city verdicts in the DB</name>
  <files>.planning/AUDIT-FRAMEWORK.md</files>
  <read_first>
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (§Audit Framework lines 538-570 — copy verbatim as the framework body; §Source Data Audit Findings lines 117-241 — the 3 city verdicts; §City Verdicts referenced by RETROFIT-02)
    - .planning/phases/36-selective-city-retrofit/36-CONTEXT.md (D-02, D-04, D-05, D-09 — framework requirements and verdict-recording rule)
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§.planning/AUDIT-FRAMEWORK.md lines 415-425 — content structure)
  </read_first>
  <action>
    Create `.planning/AUDIT-FRAMEWORK.md` with these sections (per D-02/D-04 and 36-PATTERNS.md content spec):
    (1) Preamble — state this is the durable audit asset from Phase 36 D-02/D-04, applicable to all 30+ cities, and that `treasury.data_sources.audit_verdict` JSONB is the machine-readable source of truth.
    (2) "For Socrata cities" — the 4-step column-inspection procedure copied verbatim from 36-RESEARCH.md §Audit Framework (fetch columns.json, test columns above category_column, test columns below subcategory_column, record recommended_depth + level_N_column + extraction_blocker).
    (3) "For PDF cities (pdfplumber)" — the 4-step procedure (ToC section groupings, "Summary by [Group]"/"Requirements by [Group]" tables, fund/bureau mapping table in user's guide, verify grouping used in official organization).
    (4) "Genuineness tests (D-05)" — the two-test table: citizen-recognizable + official document test, with pass criterion and fail examples.
    (5) "Depth decision rule" — both pass → retrofit_recommended; one/both fail → depth_confirmed_current with reason; partial coverage → D-06 null-collapse, record coverage %.
    (6) "Phase 36 Verdicts" — record the 3 pilot outcomes: Portland = retrofit_recommended, depth 3 (Service Area → Bureau → line items, PDF pages 12-13); Dallas = retrofit_recommended, depth 3 (Department/appropriation → Service → Object Group, Socrata e2fs-y4nb, config + shared code change); SF = audit_deferred, organization_group → department passes genuineness but leaf column unresolved and program column FAILS both tests (do NOT use program). Cite the per-decision IDs (D-05/D-06/D-07).

    Then write the same verdicts into the DB via `mcp__supabase-local__execute_sql` UPDATE statements on `treasury.data_sources.audit_verdict` for each pilot city's operating data_source row. First SELECT the data_source rows to find the correct ids (filter by municipality name/state and dataset_type='operating'). For each, set `audit_verdict` to a JSON object `{recommended_depth, evidence, last_audited:'2026-06-09', auditor:'phase-36-audit', status}` matching the markdown verdict. SF status = `audit_deferred` (D-09 — record the verdict, do not skip silently).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/AUDIT-FRAMEWORK.md','utf8').replace(/^#.*$/gm,'');const need=['Socrata','pdfplumber','Genuineness','retrofit_recommended','audit_deferred','Portland','Dallas'];for(const t of need){if(!s.includes(t)){console.error('missing: '+t);process.exit(1)}}if(s.split('\n').length<60){console.error('too short');process.exit(1)}console.log('AUDIT-FRAMEWORK.md OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/AUDIT-FRAMEWORK.md` exists, ≥60 lines, and contains all six sections (preamble, Socrata procedure, PDF procedure, genuineness tests, depth decision rule, Phase 36 verdicts)
    - File contains the strings `Socrata`, `pdfplumber`, `Genuineness`, `retrofit_recommended`, `audit_deferred`, `Portland`, `Dallas`
    - File explicitly states the SF `program` column FAILS genuineness and must NOT be used as a tree level
    - `mcp__supabase-local__execute_sql` query `SELECT name, audit_verdict FROM treasury.data_sources ds JOIN treasury.municipalities m ON ds.municipality_id=m.id WHERE m.name IN ('Portland','Dallas','San Francisco') AND ds.dataset_type='operating'` returns non-NULL audit_verdict for all 3, with status values retrofit_recommended (Portland, Dallas) and audit_deferred (SF)
  </acceptance_criteria>
  <done>The audit framework is durable in both .planning/ and the DB; all 3 pilot verdicts are recorded; RETROFIT-01 is satisfied (audit completed and identifies genuine 3rd-level cities).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase (service role) | DDL + verdict writes use the service role key; no untrusted input |
| Socrata/PDF source → audit | Audit is read-only inspection; no data ingested in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | audit_verdict migration | mitigate | `ADD COLUMN IF NOT EXISTS` idempotent; verify via information_schema query after apply |
| T-36-02 | Information disclosure | mcp__supabase-local writes | accept | Service-role DDL is the established project pattern (feedback_supabase_migration_mcp); no PII in verdicts |
| T-36-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; all libraries already present (36-RESEARCH §Package Legitimacy Audit — none added) |
</threat_model>

<verification>
- Migration applied and `audit_verdict` column present (information_schema query returns jsonb).
- All 3 pilot verdicts non-NULL in DB with correct status values.
- `.planning/AUDIT-FRAMEWORK.md` passes the structure check.
- RPC depth-change behavior documented for Wave 2 consumption.
</verification>

<success_criteria>
- RETROFIT-01 satisfied: audit completed, framework durable in two places, 3 verdicts recorded.
- Wave 2 has a definitive answer on whether a pre-reload DELETE is required.
</success_criteria>

<output>
Create `.planning/phases/36-selective-city-retrofit/36-01-SUMMARY.md` when done. MUST include: RPC depth-change verdict (replace vs accumulate + the DELETE directive if needed), Portland baseline depth distribution, and the 3 recorded DB verdicts.
</output>
