---
phase: 36-selective-city-retrofit
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - scripts/bulkLoadBudget.js
autonomous: true
requirements: [RETROFIT-02]
must_haves:
  truths:
    - "buildBudgetTree builds a 3-level tree (dept → category → subcategory) when column_mapping.department_column is set"
    - "buildBudgetTree is byte-for-byte backward compatible when department_column is absent (existing 2-level Socrata sources unchanged)"
    - "Dallas Operating data_sources.column_mapping has department_column='appropriation' set"
    - "bulkLoadBudget.js no longer contains a hardcoded SUPABASE_URL fallback (WR-04 fix)"
  artifacts:
    - path: "scripts/bulkLoadBudget.js"
      provides: "Optional department_column 3-level path in buildBudgetTree, gated behind if(deptCol)"
      contains: "department_column"
  key_links:
    - from: "data_sources.column_mapping.department_column='appropriation'"
      to: "buildBudgetTree 3-level path"
      via: "cm.department_column gate"
      pattern: "department_column"
    - from: "buildBudgetTree 3-level output"
      to: "treasury_sync_budget_tree node shape { n, a, c:[{ n, a, c:[{ n, a, i:[...] }] }] }"
      via: "dept branch → category branch → subcategory leaf"
      pattern: "jsonTree"
---

<objective>
Retrofit the generic Socrata loader to support a 3rd (department) level and reconfigure Dallas Operating to use it (RETROFIT-02, D-05/D-06/D-07). The Dallas `appropriation` column is the citizen-recognizable department level above `service`. Code + config + dry-run + backward-compat regression check only — no live DB writes (Wave 3 / plan 36-04 does the live load).

Purpose: Dallas's Socrata dataset already carries the department grouping (`appropriation`, labeled "DEPARTMENT") above the current `service` (depth-0) and `objectgroup` (depth-1). Adding it yields Department → Service → Object Group. The change must NOT regress the ~12 existing 2-level Socrata sources that share buildBudgetTree.

Output:
- `scripts/bulkLoadBudget.js` with a `department_column`-gated 3-level path + WR-04 fix
- Dallas Operating `data_sources.column_mapping` updated with `department_column: 'appropriation'`
- Verified Dallas FY2026 3-level dry-run AND a verified backward-compat dry-run on an existing 2-level source
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-selective-city-retrofit/36-CONTEXT.md
@.planning/phases/36-selective-city-retrofit/36-RESEARCH.md
@.planning/phases/36-selective-city-retrofit/36-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add department_column 3-level path to buildBudgetTree() + WR-04 fix in bulkLoadBudget.js</name>
  <files>scripts/bulkLoadBudget.js</files>
  <behavior>
    - When cm.department_column is set: buildBudgetTree returns a jsonTree of dept branch nodes shaped { n: dept, a, c: [{ n: category, a, c: [{ n: subcategory, a, i: [items] }] }] } (3-level: dept → category → subcategory)
    - When cm.department_column is absent: buildBudgetTree returns EXACTLY the prior 2-level shape { n: category, a, c: [{ n: subcategory, a, i: [items] }] } — no behavioral change for existing sources
    - Rows where department/category/subcategory are null/empty use the existing fallbacks ('Unknown'/'General') — D-06: collapse to parent, no synthetic invented groupings beyond the existing 'Unknown'/'General' fallback already in the 2-level path
    - Dallas NONE-service rows (service='NONE') still group under their appropriation department (kept under their literal service value or collapsed per existing fallback) — tree total reconciles
    - Nodes sorted by amount descending at every level
    - SUPABASE_URL has NO hardcoded fallback — exits with a clear error if unset
  </behavior>
  <read_first>
    - scripts/bulkLoadBudget.js (CURRENT full file — buildBudgetTree() lines 67-127 to extend; SUPABASE_URL line 28 hardcoded fallback to fix; parseAmount lines 60-64; syncBudgetSource + dry-run output block; the treasury_sync_budget_tree RPC call site)
    - .planning/phases/36-selective-city-retrofit/36-PATTERNS.md (§scripts/bulkLoadBudget.js lines 232-386 — exact deptCol extraction, 3-level accumulation loop gated by if(deptCol), 3-level conversion block, SUPABASE_URL fix lines 365-373, dry-run output update lines 375-386)
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (Pattern 3 lines 369-411; §City 2 Dallas lines 159-199; Pitfall 2 NONE rows lines 447-452; Pitfall 4 backward-compat lines 461-466)
    - scripts/processCA.js (SUPABASE_URL guard lines 61-62 — WR-04 target pattern)
  </read_first>
  <action>
    Add `const deptCol = cm.department_column || null;` as the first line inside buildBudgetTree (above catCol). In the row accumulation loop, gate a NEW 3-level branch behind `if (deptCol)`: build a nested Map dept → cat → sub, where dept = `row[deptCol] || 'Unknown'`, cat = `row[catCol] || 'Unknown'`, sub = `subCol ? (row[subCol] || 'General') : 'General'`, pushing the same line-item shape `{ d: sub, a: approved, aa: actual, f: fundCol?(row[fundCol]||null):null, e: null }`. The `else` branch MUST be the existing 2-level logic UNCHANGED. In the Map→JSON conversion, gate a NEW 3-level block behind `if (deptCol)` that emits dept nodes `{ n: dept, a: deptTotal, c: catNodes }` where catNodes are `{ n: cat, a: catTotal, c: subNodes }` and subNodes are `{ n: sub, a: subTotal, i: items }`, sorting descending at every level and returning `{ jsonTree, total, kept, droppedZero }`. Keep the existing 2-level conversion block intact as the non-deptCol path.

    Apply WR-04 at line 28: replace `const SUPABASE_URL = process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co';` with `const SUPABASE_URL = process.env.SUPABASE_URL;` plus `if (!SUPABASE_URL) { console.error('Missing SUPABASE_URL env var'); process.exit(1); }`. Update the dry-run preview to label children as 'services' when deptCol is set vs 'subcategories' otherwise. Do NOT run a live load — only `--dry-run`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('scripts/bulkLoadBudget.js','utf8');if(/process\.env\.SUPABASE_URL\s*\|\|\s*'https/.test(s)){console.error('hardcoded SUPABASE_URL fallback still present');process.exit(1)}if(!/department_column/.test(s)){console.error('department_column support missing');process.exit(1)}console.log('bulkLoadBudget.js source OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' scripts/bulkLoadBudget.js` contains NO `process.env.SUPABASE_URL || 'https` (WR-04 fix applied)
    - `scripts/bulkLoadBudget.js` contains `department_column` and a `deptCol`-gated 3-level path; the 2-level conversion block remains present for the `else` path
    - `node scripts/bulkLoadBudget.js --source "Dallas Revenue" --dry-run --fy 2026` (an existing 2-level source) produces the SAME category structure/counts as before the change — record the count in SUMMARY and confirm it matches a pre-change baseline (Pitfall 4 regression guard)
    - Source review confirms the non-deptCol path is byte-for-byte the original 2-level logic (no shared mutation of the 2-level branch)
  </acceptance_criteria>
  <done>buildBudgetTree supports an optional 3rd level gated by department_column, backward-compatible for 2-level sources, with WR-04 fix; no live DB write.</done>
</task>

<task type="auto">
  <name>Task 2: Set Dallas Operating column_mapping.department_column='appropriation' and dry-run validate the 3-level tree</name>
  <files>scripts/bulkLoadBudget.js</files>
  <read_first>
    - .planning/phases/36-selective-city-retrofit/36-RESEARCH.md (§City 2 Dallas lines 159-199 — appropriation column, current column_mapping {category_column:'service', subcategory_column:'objectgroup'}; §Code Examples Dallas appropriation structure lines 490-499)
    - .planning/phases/36-selective-city-retrofit/36-CONTEXT.md (D-05/D-06/D-07 — genuineness + null collapse; canonical_refs Dallas DB state lines 64-68)
    - scripts/bulkLoadBudget.js (--list and --source resolution logic, syncBudgetSource column_mapping read at line 131, dry-run output)
  </read_first>
  <action>
    Update the Dallas Operating data_source's `column_mapping` JSONB to add `department_column: 'appropriation'` while preserving existing keys (`category_column: 'service'`, `subcategory_column: 'objectgroup'`, and all amount/fund/fiscal-year keys). First SELECT the row via `mcp__supabase-local__execute_sql`: find the data_sources row for Dallas operating (filter by municipality Dallas/TX and dataset_type='operating', source name like 'Dallas Operating'). Apply the update with `mcp__supabase-local__execute_sql` using `jsonb_set(column_mapping, '{department_column}', '"appropriation"')` (do NOT overwrite the whole JSONB — merge the single key). Verify the updated column_mapping still contains category_column='service' and subcategory_column='objectgroup' plus the new department_column.

    Then run `node scripts/bulkLoadBudget.js --source "Dallas Operating" --dry-run --fy 2026` and confirm the dry-run now reports department-level top nodes (e.g., "Police Department GF", "Dallas Fire Rescue GF", "Water Utilities DWU") with service children. Confirm the FY2026 total reconciles against the prior 2-level Dallas operating total (research notes ~741 non-NONE rows, 67 distinct appropriations). Handle NONE-service rows per D-06: they remain grouped under their appropriation department via the existing 'Unknown'/'General' fallback — confirm no rows are dropped and the total matches. No live load in this task — dry-run only.
  </action>
  <verify>
    <automated>node -e "const {execSync}=require('child_process');const out=execSync('node scripts/bulkLoadBudget.js --source \"Dallas Operating\" --dry-run --fy 2026 2>&1',{encoding:'utf8',maxBuffer:16*1024*1024});if(!/Department|Police|Fire|Water/i.test(out)){console.error('dry-run did not show department-level nodes:\\n'+out.slice(-1000));process.exit(1)}if(!/dry.?run/i.test(out)){console.error('not a dry run');process.exit(1)}console.log('Dallas 3-level dry-run OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `mcp__supabase-local__execute_sql` SELECT of the Dallas Operating data_source returns column_mapping containing `"department_column": "appropriation"` AND retains `"category_column": "service"` and `"subcategory_column": "objectgroup"`
    - `node scripts/bulkLoadBudget.js --source "Dallas Operating" --dry-run --fy 2026` exits 0 and prints department-level top nodes (recognizable Dallas department names like Police/Fire/Water)
    - The FY2026 dry-run total reconciles to the prior 2-level Dallas operating total (record both in SUMMARY; difference must be 0 or explained by D-06 NONE-row handling)
    - No live RPC write occurred (output shows dry-run skip message)
  </acceptance_criteria>
  <done>Dallas Operating is configured for a 3-level dept→service→objectgroup tree and dry-run validated; live load deferred to plan 36-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socrata API → loader | API JSON parsed by buildBudgetTree; amounts validated by parseAmount; unexpected columns fall back to 'Unknown'/'General' |
| script → Supabase (service role) | column_mapping UPDATE via mcp__supabase-local; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-06 | Tampering | Socrata returns unexpected/null columns | mitigate | `row[col] || 'Unknown'/'General'` fallbacks; parseAmount handles null/undefined |
| T-36-07 | Tampering | 3-level change regresses shared 2-level sources | mitigate | Pitfall 4: gate strictly behind if(deptCol); dry-run an existing 2-level source (Dallas Revenue) and compare counts to baseline |
| T-36-08 | Information disclosure | hardcoded SUPABASE_URL fallback | mitigate | WR-04 fix — remove fallback; fail closed if unset |
| T-36-09 | Tampering | jsonb overwrite wiping existing column_mapping keys | mitigate | Use jsonb_set single-key merge, not full-object replace; verify existing keys retained after update |
| T-36-SC | Tampering | npm/pip installs | mitigate | No installs — @supabase/supabase-js already present (36-RESEARCH §Package Legitimacy Audit — none added) |
</threat_model>

<verification>
- buildBudgetTree 3-level path gated behind department_column; 2-level path unchanged.
- Backward-compat dry-run on Dallas Revenue matches pre-change counts.
- Dallas Operating column_mapping has department_column='appropriation' with existing keys retained.
- Dallas Operating FY2026 3-level dry-run shows department nodes; total reconciles.
- No live DB writes.
</verification>

<success_criteria>
- Dallas 3-level loader code + config complete, dry-run validated, no 2-level regression (RETROFIT-02 code half).
- WR-04 security fix applied to bulkLoadBudget.js.
</success_criteria>

<output>
Create `.planning/phases/36-selective-city-retrofit/36-03-SUMMARY.md` when done. MUST record: backward-compat baseline (Dallas Revenue category count before vs after), Dallas Operating FY2026 3-level total reconciliation, and distinct department count.
</output>
