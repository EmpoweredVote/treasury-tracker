---
phase: 32-state-entity-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 32-02
files_modified:
  - src/components/EntitySwitcher.tsx
autonomous: true
requirements:
  - INFRA-03
must_haves:
  truths:
    - "When municipalities includes a state entity, the dropdown shows a 'STATE GOVERNMENTS' sticky header above all state/city groups"
    - "State entities appear as flat clickable rows under the STATE GOVERNMENTS header — not nested under any state group (e.g., not under 'CALIFORNIA')"
    - "Selecting a state entity shows just the entity name in the button (e.g., 'California' not 'California, CA')"
    - "Existing city and county rows continue to render identically — the CALIFORNIA, TEXAS, OREGON groups are unaffected"
  artifacts:
    - path: "src/components/EntitySwitcher.tsx"
      provides: "EntitySwitcher with pre-filtered state entities, STATE GOVERNMENTS section, and conditional displayName"
      contains: "STATE GOVERNMENTS"
  key_links:
    - from: "stateEntities array (filtered before byState build)"
      to: "STATE GOVERNMENTS section JSX (rendered above grouped.entries())"
      via: "useMemo pre-filter + JSX conditional render"
      pattern: "entity_type !== 'state'"
    - from: "displayName constant"
      to: "selectedEntity.entity_type === 'state' branch"
      via: "conditional expression"
      pattern: "entity_type === 'state'"
---

<objective>
Update EntitySwitcher.tsx with three coordinated changes (D-05, D-06, D-07, D-08) that surface state entities in a dedicated top section without breaking the existing grouped city/county rendering.

Purpose: Without these changes, a CA state entity would appear inside the "CALIFORNIA" city group, creating a confusing "California > Cities > California" nesting. The fixes prevent that and give state governments their own visual section.

Output: EntitySwitcher renders correctly with and without state entities in the municipalities prop; TypeScript compiles; existing city/county layout is unchanged.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-state-entity-infrastructure/32-CONTEXT.md
@.planning/phases/32-state-entity-infrastructure/32-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-filter state entities and add ENTITY_TYPE_LABELS entry</name>
  <files>src/components/EntitySwitcher.tsx</files>
  <read_first>
    - src/components/EntitySwitcher.tsx — read the full file; focus on lines 11–19 (ENTITY_TYPE_LABELS), lines 61–88 (grouped useMemo), line 91–93 (displayName)
    - src/types/budget.ts lines 106–120 — confirm Municipality.entity_type now includes 'state' (from Plan 02)
    - .planning/phases/32-state-entity-infrastructure/32-CONTEXT.md — re-read D-05, D-06, D-07, D-08 for exact implementation details
  </read_first>
  <action>
    Make three coordinated changes to src/components/EntitySwitcher.tsx:

    CHANGE 1 — ENTITY_TYPE_LABELS (per D-07):
    In the ENTITY_TYPE_LABELS Record (currently lines 11–19), add:
      state: 'State Governments',
    Place it as the first entry so it reads: state, city, county, township, etc.

    CHANGE 2 — grouped useMemo pre-filter (per D-05):
    In the grouped useMemo (currently lines 61–88), split the filtering step into two:
    - Before building the byState Map, separate state entities into their own array:
        const stateEntities = filtered.filter(m => m.entity_type === 'state');
        const cityEntities = filtered.filter(m => m.entity_type !== 'state');
    - Build the byState Map only from cityEntities (not from filtered as currently written)
    - Sort stateEntities alphabetically by name within the useMemo
    - Return both from the useMemo as { byState, stateEntities }

    Update the grouped variable destructuring downstream accordingly wherever `grouped` is used:
    - Replace `grouped.size === 0` with `grouped.byState.size === 0 && grouped.stateEntities.length === 0`
    - Replace `[...grouped.entries()]` with `[...grouped.byState.entries()]`

    CHANGE 3 — displayName conditional (per D-08):
    Change line 91–93 from:
      const displayName = selectedEntity
        ? `${selectedEntity.name}, ${selectedEntity.state}`
        : 'Select jurisdiction';
    to:
      const displayName = selectedEntity
        ? selectedEntity.entity_type === 'state'
          ? selectedEntity.name
          : `${selectedEntity.name}, ${selectedEntity.state}`
        : 'Select jurisdiction';

    After all three changes, verify TypeScript: npx tsc --noEmit
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ENTITY_TYPE_LABELS contains entry state: 'State Governments'
    - The grouped useMemo returns an object with byState and stateEntities properties, not a raw Map
    - cityEntities (not municipalities or filtered) is iterated when building byState — confirmed by reading the useMemo body
    - The displayName expression has a branch for entity_type === 'state' returning only entity name (no ', CA' suffix)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Pre-filter and displayName changes applied; TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Render the STATE GOVERNMENTS section in the dropdown JSX</name>
  <files>src/components/EntitySwitcher.tsx</files>
  <read_first>
    - src/components/EntitySwitcher.tsx — read lines 132–180 (the grouped list JSX) after Task 1's changes are applied; the sticky header class string is at what was line 143 (now may be slightly shifted)
  </read_first>
  <action>
    In the dropdown JSX (the div.max-h-80.overflow-y-auto block), add a STATE GOVERNMENTS section immediately ABOVE the existing `{[...grouped.byState.entries()].sort(...)...}` render block.

    The section renders only when stateEntities.length > 0. Structure:

    1. A sticky header div using the exact same class string as the existing state group headers (from what was line 143):
       "sticky top-0 bg-[#F7F7F8] dark:bg-ev-gray-900 px-4 py-1.5 text-xs font-bold uppercase tracking-wider text-ev-gray-500 border-b border-[#E2EBEF] dark:border-ev-gray-700"
       Text content: STATE GOVERNMENTS

    2. Immediately below the header: a flat list of stateEntities.map(entity => ...) using the same button markup as what was line 155 (the entity row buttons inside the existing grouped list).
       Each button: same className pattern with border-l-2 selected/unselected states, same onClick handler (onEntityChange(entity); setIsOpen(false); setFilter(''))
       Button text: {entity.name}  (name only — no state suffix)

    No entity_type subheader (no "State Governments (1)" subheader) — the main sticky header IS the section label.

    For the empty-state check at the top of the list (currently `grouped.size === 0`), ensure it now checks both:
    grouped.byState.size === 0 && grouped.stateEntities.length === 0

    After adding the JSX, run: npx tsc --noEmit
    Then run: npm run build (or the equivalent build script from package.json)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - EntitySwitcher.tsx contains the text "STATE GOVERNMENTS" in a JSX element with the sticky-header class string
    - The STATE GOVERNMENTS section is rendered before the byState.entries() mapped sections (appears at the top of the scrollable list)
    - stateEntities.map(...) renders button elements with the same class structure as city entity buttons
    - The section is conditionally rendered (stateEntities.length > 0)
    - npm run build completes with exit code 0 (no TypeScript or build errors)
    - The existing state group sections (CALIFORNIA, TEXAS, OREGON) still render from byState — no city or county entity is missing from their respective groups
  </acceptance_criteria>
  <done>STATE GOVERNMENTS section renders in dropdown above all state/city groups; full build passes without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| municipalities prop → dropdown render | Prop data from API; entity_type is used for branching but never injected into HTML as raw string (used in comparisons and JSX text only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Tampering | EntitySwitcher entity_type branching | accept | entity_type values are compared against string literals ('state'); no dynamic key lookups that could be injected |
| T-32-04 | Elevation of Privilege | STATE GOVERNMENTS section visibility | accept | Section appears only when stateEntities.length > 0; no auth or access control change — all entities are already public read |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After both tasks complete:
1. grep src/components/EntitySwitcher.tsx for "STATE GOVERNMENTS" — returns 1 match inside JSX
2. grep src/components/EntitySwitcher.tsx for "entity_type !== 'state'" — returns 1 match in useMemo filter
3. grep src/components/EntitySwitcher.tsx for "entity_type === 'state'" — returns 1 match in displayName expression
4. npm run build exits 0
5. npx tsc --noEmit exits 0
</verification>

<success_criteria>
- INFRA-03 satisfied: Entity picker renders STATE GOVERNMENTS section above all state/city groups
- D-05: State entities pre-filtered before byState Map build — no circular nesting possible
- D-06: Sticky "STATE GOVERNMENTS" header + flat entity list, using same visual style as state group headers
- D-07: ENTITY_TYPE_LABELS includes state: 'State Governments'
- D-08: Selecting a state entity shows just the name (e.g., "California") — no state-code suffix
- All existing city and county pages render identically — no regression
</success_criteria>

<output>
Create .planning/phases/32-state-entity-infrastructure/32-03-SUMMARY.md when done
</output>
