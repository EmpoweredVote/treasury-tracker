---
phase: 32-state-entity-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/budget.ts
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "TypeScript compiles cleanly with entity_type: 'state' as a valid Municipality.entity_type value"
    - "Assigning entity_type = 'state' to a Municipality object produces no TypeScript error"
  artifacts:
    - path: "src/types/budget.ts"
      provides: "Municipality interface with entity_type union including 'state'"
      contains: "'city' | 'county' | 'township' | 'nonprofit' | 'state'"
  key_links:
    - from: "src/types/budget.ts"
      to: "src/components/EntitySwitcher.tsx"
      via: "Municipality type import"
      pattern: "import type.*Municipality.*from.*types/budget"
---

<objective>
Extend the `Municipality.entity_type` TypeScript union type to include `'state'` — a one-line change in `src/types/budget.ts` at line 111.

Purpose: The EntitySwitcher (Plan 03, Wave 2) and all future state-entity consumers need TypeScript to accept entity_type='state' on Municipality objects. Without this change, TypeScript would reject the CA state municipality row inserted in Phase 33.

Output: `src/types/budget.ts` updated; `npm run build` (or `tsc --noEmit`) passes cleanly.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-state-entity-infrastructure/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'state' to Municipality.entity_type union</name>
  <files>src/types/budget.ts</files>
  <read_first>
    - src/types/budget.ts — read lines 106–120 to see the exact current Municipality interface definition before editing
    - src/components/EntitySwitcher.tsx — confirm the import path (import type { Municipality } from '../types/budget') so you know this file consumes the type
  </read_first>
  <action>
    In src/types/budget.ts at line 111, change the entity_type field of the Municipality interface from:

      entity_type: 'city' | 'county' | 'township' | 'nonprofit';

    to (per D-04):

      entity_type: 'city' | 'county' | 'township' | 'nonprofit' | 'state';

    This is the only change to make in this file. Do not alter any other field, interface, or comment.

    After editing, run the TypeScript type-check to confirm zero errors:
      npx tsc --noEmit

    If the project uses a different build/type-check command (check package.json scripts), use that instead — but tsc --noEmit is the standard fallback.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/budget.ts line 111 reads exactly: entity_type: 'city' | 'county' | 'township' | 'nonprofit' | 'state';
    - npx tsc --noEmit exits with code 0 (no TypeScript errors)
    - No other lines in src/types/budget.ts are modified
  </acceptance_criteria>
  <done>Municipality.entity_type union includes 'state'; TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type definition → compiled output | Purely compile-time change; no runtime trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-02 | Information Disclosure | Municipality type | accept | Adding 'state' to a union widens the type; no security implication — entity_type is display/routing data only |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completion:
1. grep src/types/budget.ts for "'city' | 'county' | 'township' | 'nonprofit' | 'state'" — returns line 111
2. npx tsc --noEmit exits 0
</verification>

<success_criteria>
- INFRA-02 satisfied: TypeScript Municipality.entity_type union includes 'state'
- No TypeScript compilation errors introduced
</success_criteria>

<output>
Create .planning/phases/32-state-entity-infrastructure/32-02-SUMMARY.md when done
</output>
