---
phase: 32-state-entity-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606000000_add_state_entity_type.sql
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "The treasury.municipalities table accepts INSERT with entity_type = 'state' without error"
    - "INSERT with entity_type = 'invalid_value' is rejected by the CHECK constraint"
  artifacts:
    - path: "supabase/migrations/20260606000000_add_state_entity_type.sql"
      provides: "CHECK constraint migration adding 'state' to allowed entity_type values"
      contains: "municipalities_entity_type_check"
  key_links:
    - from: "supabase/migrations/20260606000000_add_state_entity_type.sql"
      to: "treasury.municipalities.entity_type"
      via: "ALTER TABLE ADD CONSTRAINT"
      pattern: "ADD CONSTRAINT municipalities_entity_type_check"
---

<objective>
Apply a database migration that adds 'state' to the allowed values of `entity_type` in `treasury.municipalities` via a CHECK constraint.

Purpose: Phase 33 will INSERT a California row with entity_type='state'. Without this constraint in place, the value is unconstrained (any string accepted) — adding the constraint now enforces data integrity and makes the allowed set explicit for all future entity types.

Output: One migration file applied to the local Supabase instance; the CHECK constraint is live and verified.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-state-entity-infrastructure/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and apply the entity_type CHECK constraint migration</name>
  <files>supabase/migrations/20260606000000_add_state_entity_type.sql</files>
  <read_first>
    - supabase/migrations/20260602235505_add_county_id_to_municipalities.sql — naming convention and file structure reference
    - supabase/migrations/20260602031258_add_all_funds_requirements_dataset_type.sql — pattern for adding to an enum/constraint
  </read_first>
  <action>
    Create the migration file at supabase/migrations/20260606000000_add_state_entity_type.sql (per D-03) containing exactly this statement and nothing else:

      ALTER TABLE treasury.municipalities
        ADD CONSTRAINT municipalities_entity_type_check
        CHECK (entity_type IN ('city', 'county', 'township', 'nonprofit', 'state'));

    Use a timestamp of 20260606000000 (as specified in D-03 naming pattern).

    Apply the migration using mcp__supabase-local__apply_migration with the migration_name "add_state_entity_type" and the SQL above as the query.

    After applying, verify using mcp__supabase-local__execute_sql:
      SELECT conname, pg_get_constraintdef(oid)
      FROM pg_constraint
      WHERE conrelid = 'treasury.municipalities'::regclass
        AND contype = 'c';

    Confirm the output shows a row where conname = 'municipalities_entity_type_check' and the definition includes all five values: 'city', 'county', 'township', 'nonprofit', 'state'.

    Then run a functional test via execute_sql:
      INSERT INTO treasury.municipalities (name, state, entity_type, population)
      VALUES ('_test_state', 'CA', 'state', 0)
      RETURNING id, entity_type;

    Confirm it returns a row with entity_type = 'state', then clean up:
      DELETE FROM treasury.municipalities WHERE name = '_test_state';
  </action>
  <verify>
    <automated>
      mcp__supabase-local__execute_sql:
        SELECT conname FROM pg_constraint
        WHERE conrelid = 'treasury.municipalities'::regclass
          AND contype = 'c'
          AND conname = 'municipalities_entity_type_check'
      -- returns 1 row
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260606000000_add_state_entity_type.sql exists and contains ADD CONSTRAINT municipalities_entity_type_check with all 5 values: 'city', 'county', 'township', 'nonprofit', 'state'
    - execute_sql query on pg_constraint returns exactly 1 row with conname = 'municipalities_entity_type_check'
    - The functional INSERT test with entity_type='state' succeeds (row returned, then deleted)
    - The migration appears in the supabase migration history (no error on apply)
  </acceptance_criteria>
  <done>CHECK constraint municipalities_entity_type_check is live in the local DB, the migration file exists, and INSERT with entity_type='state' succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → Supabase local DB | DDL applied via MCP tool; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | migration SQL | accept | Migration is authored inline — no external input or dynamic SQL |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After task completion:
1. pg_constraint query returns 'municipalities_entity_type_check' row
2. Migration file exists at supabase/migrations/20260606000000_add_state_entity_type.sql
3. No existing municipality rows are affected (constraint applies only on INSERT/UPDATE going forward — all existing values are already in the allowed set)
</verification>

<success_criteria>
- INFRA-01 satisfied: treasury.municipalities accepts entity_type = 'state' and rejects out-of-set values
- Zero impact on existing city, county, township, nonprofit rows
</success_criteria>

<output>
Create .planning/phases/32-state-entity-infrastructure/32-01-SUMMARY.md when done
</output>
