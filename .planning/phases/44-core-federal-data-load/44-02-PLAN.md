---
phase: 44-core-federal-data-load
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - scripts/extractOMBHistorical.py
  - scripts/loadFederalAnnualSummary.js
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "federal_annual_summary holds one row per fiscal year 1962–2025, actuals only, every row with source_name/source_url/source_date"
    - "FY2025 row matches recon anchors: receipts 5,236,421M / outlays 7,011,105M / deficit −1,774,684M (in dollars: ×1e6)"
    - "For every year: mandatory + discretionary_defense + discretionary_nondefense + net_interest ≈ outlays within 0.5%"
    - "Re-running the loader is idempotent (upsert by fiscal_year)"
  artifacts:
    - path: "scripts/loadFederalAnnualSummary.js"
      provides: "OMB Hist 1.1 + 8.1 → federal_annual_summary"
      contains: "federal_annual_summary"
  key_links:
    - from: "scripts/extractOMBHistorical.py"
      to: "scripts/loadFederalAnnualSummary.js"
      via: "JSON on stdout (processCA.js pattern)"
      pattern: "execSync"
---

<objective>
Load the multi-decade fiscal context: receipts/outlays/deficit (OMB Hist 1.1) and the Mandatory/Discretionary/Net Interest split (OMB Hist 8.1) for FY1962–FY2025 actuals into federal_annual_summary. This feeds the landing bands, the deficit strip, and historical context — DATA-03 and DATA-04.

Output: extractor + loader scripts; 64 sourced rows; validations green.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-core-federal-data-load/44-CONTEXT.md
@.planning/v2.0-recon/RECON.md
@.planning/v2.0-recon/samples/hist01z1_fy2027.xlsx
@.planning/v2.0-recon/samples/hist08z1_fy2027.xlsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: extractOMBHistorical.py</name>
  <files>scripts/extractOMBHistorical.py</files>
  <read_first>
    - scripts/extractCA.py — openpyxl extraction + JSON-to-stdout pattern
    - .planning/v2.0-recon/samples/hist01z1_fy2027.xlsx and hist08z1_fy2027.xlsx — REAL layouts (recon samples; reuse for development, but final run downloads fresh)
  </read_first>
  <action>
    Python script: takes paths to hist01z1 and hist08z1 xlsx, emits JSON array [{fiscal_year, receipts, outlays, surplus_or_deficit, mandatory, discretionary_defense, discretionary_nondefense, net_interest}].
    - Amounts in 1.1/8.1 are MILLIONS — multiply ×1,000,000 to dollars (table comment in 44-01 migration).
    - 8.1 has merged multi-row headers — locate columns by header TEXT (search rows 1–6 for 'Mandatory', 'National\nDefense', 'Non-\ndefense', 'Net Interest'), NEVER by position. The recon parse saw col layout [Year, Total, Disc-Total, Defense, Nondefense, Mand-Total, Mandatory, …] — treat as hint only.
    - Year rows: rows whose first cell is a 4-digit year. STOP at 2025 — rows beyond are OMB estimates; actuals only (the file may mark estimates; regardless, hard-stop at 2025). Skip 'TQ' (transition quarter 1976) row.
    - Emit only years present in BOTH tables (1962+; 1.1 goes back to 1789 but 8.1 starts 1962).
    - Validate before emit: every year, BEA components sum to outlays within 0.5%; surplus_or_deficit == receipts − outlays within rounding. On violation: print offending year to stderr and exit 1.
  </action>
  <verify><automated>Run against recon sample files: emits 64 objects (1962–2025); FY2025 values match anchors; validation passes</automated></verify>
  <acceptance_criteria>
    - Header-text-driven column mapping; estimates excluded; TQ skipped; dollars normalized
  </acceptance_criteria>
  <done>Deterministic extractor proven against recon samples.</done>
</task>

<task type="auto">
  <name>Task 2: loadFederalAnnualSummary.js — download, extract, upsert</name>
  <files>scripts/loadFederalAnnualSummary.js</files>
  <read_first>
    - scripts/processCA.js — execSync(python) + loadEnv + dry-run pattern; sanity-band precedent (T-33-04/05/06)
  </read_first>
  <action>
    Node script, --dry-run flag:
    1. Scrape https://www.whitehouse.gov/omb/information-resources/budget/historical-tables/ (browser User-Agent REQUIRED) for the current hist01z1*.xlsx and hist08z1*.xlsx URLs (do not hardcode the FY2027 paths — next year's edition moves them). Download to a temp/cache dir.
    2. execSync extractOMBHistorical.py → JSON.
    3. Upsert each row into federal_annual_summary by fiscal_year with: source_name='omb-historical-tables' (registry key), source_url = the historical-tables landing page URL (each row blends Table 1.1 and Table 8.1 figures; the landing page links both files and is the stable human-checkable entry point — the two exact xlsx URLs are recorded in the script header and in 44-VERIFICATION.md), source_date = run date.
    4. Validations (halt on failure): row count 64; FY2025 anchors exact (5,236,421M / 7,011,105M / −1,774,684M ×1e6); FY2024 outlays 6,735,261M ×1e6 (second anchor from recon parse); per-year BEA sum check.
    5. Re-run immediately; confirm 64 rows still (idempotent), updated_at/created_at sane.
  </action>
  <verify>
    <automated>
      execute_sql: SELECT count(*), min(fiscal_year), max(fiscal_year) FROM treasury.federal_annual_summary -- 64, 1962, 2025;
      SELECT receipts, outlays, surplus_or_deficit FROM treasury.federal_annual_summary WHERE fiscal_year=2025 -- matches anchors
    </automated>
  </verify>
  <acceptance_criteria>
    - 64 actual-year rows, every source column populated, anchors exact, idempotent
    - No treasury.budgets rows created (this data is context, not a dataset — US entity stays invisible)
  </acceptance_criteria>
  <done>DATA-03 + DATA-04 satisfied with per-row sourcing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitehouse.gov xlsx → parser | External file; openpyxl parse + hard numeric validations before any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03 | Tampering | downloaded xlsx | mitigate | Anchor checks (FY2025/FY2024 exact) + per-year sum checks; halt on any mismatch |
| T-44-04 | Spoofing | scraped xlsx URL | mitigate | Only accept https://www.whitehouse.gov/ URLs from the landing page |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs (openpyxl already present) |
</threat_model>

<verification>
1. 64 rows, 1962–2025, FY2025/FY2024 anchors exact
2. Every row's BEA components sum to outlays within 0.5%
3. All source columns populated; loader idempotent; zero treasury.budgets rows
</verification>

<success_criteria>
- DATA-03, DATA-04 complete: first split + multi-decade history queryable with sourcing
</success_criteria>

<output>
Create .planning/phases/44-core-federal-data-load/44-02-SUMMARY.md when done
</output>
