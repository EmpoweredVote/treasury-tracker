---
phase: 44-core-federal-data-load
plan: 05
type: execute
wave: 3
depends_on: [44-03]
files_modified:
  - scripts/loadFederalAgencies.js
autonomous: false
requirements:
  - DATA-06
must_haves:
  truths:
    - "The agency lens (dataset_type='federal_agency', FY2025) is a 3+ level tree: Department → Bureau → Account from MTS Table 5, built by parent_id walking — NEVER by summing 'Total--' rows"
    - "Department-level sum reconciles to total FY2025 net outlays within 0.5% (excluded negatives itemized)"
    - "Phase verification (44-VERIFICATION.md) confirms every DATA requirement with queries and source-URL spot checks"
  artifacts:
    - path: "scripts/loadFederalAgencies.js"
      provides: "MTS T5 parent-walked agency tree"
      contains: "parent_id"
    - path: ".planning/phases/44-core-federal-data-load/44-VERIFICATION.md"
      provides: "Phase-level evidence: DATA-01..07 each verified"
      contains: "DATA-07"
  key_links:
    - from: "scripts/loadFederalAgencies.js"
      to: "treasury.budget_categories"
      via: "data_sources (dataset_type='federal_agency') + RPC"
      pattern: "federal_agency"
---

<objective>
Load the toggle lens — who spends it — as a Department → Bureau → Account tree from MTS Table 5 (FY2025 = record_date 2025-09-30), then run phase-level verification across all of Phase 44.

Output: loadFederalAgencies.js; federal_agency FY2025 dataset (under 44-03 checkpoint rules); 44-VERIFICATION.md.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-core-federal-data-load/44-CONTEXT.md
@.planning/v2.0-recon/samples/mts5.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: loadFederalAgencies.js — parent-walked T5 tree</name>
  <files>scripts/loadFederalAgencies.js</files>
  <read_first>
    - .planning/v2.0-recon/samples/mts5.json — REAL T5 shape: 797 rows, sequence levels 1–5, parent_id, null-amount parent rows, 'Total--X' rows at MIXED levels
    - scripts/loadMACountyBudget.js — data_sources + RPC pattern
  </read_first>
  <action>
    Node script, --dry-run flag. Fetch T5 at record_date 2025-09-30 (page[size]=900, URL-encoded; verify total-count fits one page or paginate).
    Tree construction rules (the recon sample is the contract):
    - Build nodes from parent_id/classification_id linkage. ROOT nodes = L1 rows (departments/branches). Children via parent_id chains, depth from sequence_level_nbr.
    - EXCLUDE all 'Total--*' rows (they are printed subtotals at mixed levels — summing them double-counts; 43-CONTEXT gotcha).
    - Amount fields: current_fytd_net_outly_amt for leaves. Parent rows with null amounts take the sum of their children; where a parent has BOTH an amount and children, prefer children-sum and log discrepancies > 0.1%.
    - 'Memorandum' / 'Total On-Budget' / 'Total Off-Budget' style rows: exclude from tree, store the on/off-budget totals as context metrics (sourced) for Phase 45 disclosure.
    - Negative nodes (Proprietary Receipts from the Public, Intrabudgetary Transactions, Offsetting Governmental Receipts appear throughout): exclude from tree, AGGREGATE per department into a single context metric (agency_lens_offsets_fy2025 total + per-dept JSON in the metric's label or a details note) — itemized in dry-run output.
    - VALIDATION: sum of department roots + |excluded negatives| reconciles to T9 FY2025 total net outlays within 0.5%; also cross-check 2-3 known departments vs recon FYTD26 ordering sanity (DoD, HHS largest).
    - data_sources: api_type='fiscal-data-api', dataset_type='federal_agency', dataset_id='fy2025', base_url = exact query URL, fiscal_year_start_month=10. RPC + idempotent pre-delete.
    - Dry-run prints: root count, depth distribution, top-10 departments, exclusion ledger, reconciliation delta. Live run governed by the 44-03 checkpoint decision (GO/HOLD recorded there).
  </action>
  <verify>
    <automated>
      Dry-run: ~18-25 roots, max depth >= 2 (3 levels), reconciliation < 0.5%;
      if live: budgets row (US, 2025, federal_agency); depth distribution via SELECT depth, count(*) FROM budget_categories
    </automated>
  </verify>
  <acceptance_criteria>
    - No 'Total--' row in any tree node; exclusion ledger complete; reconciliation documented; idempotent
  </acceptance_criteria>
  <done>DATA-06 satisfied at bureau/account depth.</done>
</task>

<task type="auto">
  <name>Task 2: phase verification — 44-VERIFICATION.md</name>
  <files>.planning/phases/44-core-federal-data-load/44-VERIFICATION.md</files>
  <action>
    Run and record evidence for each requirement:
    - DATA-01: operating tree exists, depth + reconciliation (from 44-04)
    - DATA-02: revenue tree total vs anchor
    - DATA-03: 8.1 fields populated 1962–2025; FY2025 split sums
    - DATA-04: 64 annual_summary rows
    - DATA-05: fytd metrics as_of = latest MTS month
    - DATA-06: federal_agency tree depth distribution + reconciliation
    - DATA-07: debt + interest metrics present
    - Sourcing sweep: SELECT every federal data_sources row → base_url non-empty; every federal budgets row → data_source_id + generated_at; every annual_summary/context_metrics row → source columns non-null. curl 3 sampled source URLs → 200.
    - Regression: getCities count = pre-phase count + (1 if US visible); spot-check Plano + California state pages unchanged.
    - Record GO/HOLD state of each dataset (from 44-03 checkpoint) and what remains for Phase 45.
  </action>
  <verify><automated>44-VERIFICATION.md contains a PASS/FAIL line per DATA requirement with query evidence</automated></verify>
  <acceptance_criteria>
    - All seven DATA requirements PASS (or HOLD-deferred by checkpoint with metrics/annual data loaded); zero unsourced federal rows
  </acceptance_criteria>
  <done>Phase 44 goal proven with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MTS T5 hierarchy → tree | parent_id walk validated by reconciliation; Total-- exclusion is a hard rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-08 | Tampering | double-count via Total-- rows | mitigate | Hard exclusion + 0.5% reconciliation to T9 |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
1. Agency tree: no Total-- nodes, 3+ levels, reconciled
2. 44-VERIFICATION.md: per-requirement PASS lines, sourcing sweep clean, sampled URLs return 200
3. No regression on existing entities
</verification>

<success_criteria>
- DATA-06 complete; Phase 44 verified end-to-end with documented evidence
</success_criteria>

<output>
Create .planning/phases/44-core-federal-data-load/44-05-SUMMARY.md when done
</output>
