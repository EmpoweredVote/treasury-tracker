---
phase: 44-core-federal-data-load
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - scripts/loadFederalMTS.js
autonomous: false
requirements:
  - DATA-02
  - DATA-05
  - DATA-07
must_haves:
  truths:
    - "FY2025 receipts-by-source tree (dataset_type='revenue') is loaded and served; total within 0.5% of OMB 5,236,421M"
    - "federal_context_metrics holds fytd_outlays, fytd_receipts (latest MTS month), total_public_debt, fytd_interest_expense — all sourced"
    - "Chris explicitly approved the first public write before it happened (US entity becomes visible in production EntitySwitcher)"
    - "Negative receipt lines are excluded from the tree, logged, and stored as context metrics for disclosure"
  artifacts:
    - path: "scripts/loadFederalMTS.js"
      provides: "Fiscal Data API → revenue tree + context metrics"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/loadFederalMTS.js"
      to: "treasury.budgets"
      via: "data_sources upsert + RPC"
      pattern: "fiscal_year_start_month"
---

<objective>
Load Money In (FY2025 receipts by source, 2-level tree from MTS Table 9) and the live context metrics (FY2026 FYTD outlays/receipts, debt, interest). Contains the phase's go/no-go human checkpoint: the first treasury.budgets write makes "United States" publicly visible.

Output: loadFederalMTS.js; revenue dataset live; 4+ context metric rows; checkpoint record.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-core-federal-data-load/44-CONTEXT.md
@.planning/v2.0-recon/RECON.md
@.planning/v2.0-recon/samples/mts9.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: loadFederalMTS.js — build + dry-run</name>
  <files>scripts/loadFederalMTS.js</files>
  <read_first>
    - scripts/loadMACountyBudget.js — data_sources upsert + RPC call + idempotent delete pattern
    - scripts/processCA.js — tree building + dry-run + sanity-band patterns
    - .planning/v2.0-recon/samples/mts9.json — real T9 shape (sequence_level_nbr, classification_desc, FYTD fields)
  </read_first>
  <action>
    Node script (pure fetch, no python), flags: --dry-run, --dataset revenue|metrics|all.

    REVENUE TREE (FY2025 actuals):
    - Fetch T9 at record_date 2025-09-30 (September FYTD = full-year actuals). URL-encode page[size]. VERIFY the record exists; if not, list available record_dates and STOP.
    - Receipts section: rows between 'Receipts' (L1) and 'Net Outlays' (L1). Build 2-level tree: L2 rows are roots; 'Social Insurance and Retirement Receipts:' takes its L3 rows as children {n, a, c|i}. Strip trailing ':' from parent labels. Exclude 'Total' rows (tree total computed from nodes).
    - Negative or zero lines: exclude from tree, log loudly, write each as federal_context_metrics row (key e.g. excluded_receipt_<slug>_fy2025) for Phase 45 disclosure.
    - data_sources upsert: api_type='fiscal-data-api', dataset_type='revenue', dataset_id='fy2025', base_url = the EXACT API query URL, municipality_id = US id, fiscal_year_start_month=10 — confirm the RPC propagates start month (43-03 contract; data_sources must carry it or set on budgets after RPC: read RPC source first — it copies v_ds.fiscal_year_start_month from data_sources. Check data_sources has that column; if the column lives only on budgets, UPDATE budgets after RPC and document).
    - RPC treasury_sync_budget_tree with p_total = sum of positive nodes; idempotency via pre-delete on (data_source_id, fiscal_year) per loadMACountyBudget.js.
    - Sanity band: total within 0.5% of 5,236,421M×1e6 AFTER adding back excluded negatives; otherwise within 2% and halt for review if outside.

    CONTEXT METRICS (run before or after checkpoint — metrics tables don't create visibility):
    - fytd_outlays + fytd_receipts: latest T9 record_date (current_fytd_rcpt_outly_amt of the two 'Total' rows); as_of_date = record_date; source_url = exact query URL; source_name='treasury-fiscal-data'.
    - total_public_debt: v2/accounting/od/debt_to_penny latest (tot_pub_debt_out_amt).
    - fytd_interest_expense: v2/accounting/od/interest_expense latest month — inspect expense_catg_desc rows and select the total/appropriate aggregation; record the chosen filter in the summary (recon verified fields but not category semantics).
    - Upsert by metric_key.

    DRY-RUN against live API: print tree (names, amounts, depth), totals, deltas vs anchors, metric values. NO writes.
  </action>
  <verify><automated>--dry-run output: receipts tree ≈19 nodes/2 levels, total delta vs anchor < 0.5%, 4+ metrics printed with as_of dates</automated></verify>
  <acceptance_criteria>
    - Dry-run green with anchors; negative handling demonstrated; exact source URLs captured
  </acceptance_criteria>
  <done>Loader proven against live API without any DB write.</done>
</task>

<task type="checkpoint">
  <name>Task 2: HUMAN GO/NO-GO — first public federal write</name>
  <files>none</files>
  <action>
    Present to Chris before any live --dataset revenue run:
    - The dry-run tree + totals (what the public will see)
    - The consequence: the moment the revenue dataset lands, "United States" appears in the production EntitySwitcher under FEDERAL GOVERNMENT, rendering through the DEFAULT city UI (Money In donut/icicle of FY2025 receipts — correct and sourced, but not yet the Phase 45 federal presentation)
    - Options: (a) proceed now — interim default UI is acceptable; (b) hold all treasury.budgets writes until Phase 45 ships (context metrics + annual summary can still load — they create no visibility)
    Record the decision verbatim in the summary.
  </action>
  <verify><automated>Summary contains the decision and timestamp</automated></verify>
  <acceptance_criteria>
    - No treasury.budgets write for the US entity happens before recorded approval
  </acceptance_criteria>
  <done>Visibility decision is Chris's, on the record.</done>
</task>

<task type="auto">
  <name>Task 3: live load + verify (gated by Task 2)</name>
  <files>none</files>
  <action>
    If GO: run --dataset all live; if HOLD: run --dataset metrics only and mark the revenue step deferred-pending-45 in the summary + STATE.md.
    Verify via execute_sql: budgets row (US, 2025, revenue) with fiscal_year_start_month=10 and hierarchy set; budget_categories count matches tree; context metrics present with today's source_date. Re-run loader; confirm idempotent (same counts).
  </action>
  <verify>
    <automated>
      execute_sql checks above; getCities shape returns US row IFF the GO path was taken
    </automated>
  </verify>
  <acceptance_criteria>
    - DATA-02 + DATA-05 + DATA-07 satisfied (or revenue explicitly deferred by checkpoint decision with metrics still loaded)
  </acceptance_criteria>
  <done>Money In + live context metrics loaded with full sourcing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fiscal Data API → loader | External JSON; anchor validation + sanity bands before write |
| Loader → production visibility | Human checkpoint gates the first public write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Tampering | API response | mitigate | Cross-source anchor (OMB) within 0.5%; halt outside 2% |
| T-44-06 | Repudiation | unapproved public exposure | mitigate | checkpoint task blocks treasury.budgets writes |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
1. Revenue tree total reconciles to OMB anchor (delta documented incl. excluded negatives)
2. 4+ context metrics with exact source URLs and as_of dates
3. Checkpoint decision recorded before any visibility-creating write
4. Idempotency demonstrated
</verification>

<success_criteria>
- DATA-02, DATA-05, DATA-07 complete (revenue possibly deferred by explicit human decision, never silently)
</success_criteria>

<output>
Create .planning/phases/44-core-federal-data-load/44-03-SUMMARY.md when done
</output>
