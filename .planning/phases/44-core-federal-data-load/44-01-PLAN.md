---
phase: 44-core-federal-data-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612110000_add_federal_agency_dataset_type.sql
  - supabase/migrations/20260612110100_create_federal_context_tables.sql
  - scripts/seedUSFederal.js
  - src/types/budget.ts
autonomous: true
requirements:
  - DATA-05
  - DATA-07
must_haves:
  truths:
    - "data_sources accepts dataset_type='federal_agency'; treasury.budgets has no CHECK to update (verified 43/44 planning)"
    - "treasury.federal_annual_summary and treasury.federal_context_metrics exist with NOT NULL source_name/source_url/source_date"
    - "A 'United States' municipality row exists (entity_type='federal', state='US') with Census-sourced population — and is NOT yet visible in the app (no budgets rows)"
    - "Frontend dataset_type union includes 'federal_agency' and unknown-type rendering is verified non-breaking"
  artifacts:
    - path: "supabase/migrations/20260612110100_create_federal_context_tables.sql"
      provides: "federal_annual_summary + federal_context_metrics"
      contains: "CREATE TABLE treasury.federal_annual_summary"
    - path: "scripts/seedUSFederal.js"
      provides: "Idempotent US entity seed with Census-fetched population"
      contains: "entity_type"
  key_links:
    - from: "scripts/seedUSFederal.js"
      to: "treasury.municipalities"
      via: "upsert by (name='United States', entity_type='federal')"
      pattern: "United States"
---

<objective>
Create the federal-specific schema (dataset_type extension + two context tables) and seed the United States entity with Census-sourced population. After this plan, every later loader has its target schema and municipality_id.

Output: 2 migrations applied; seed script run; US row present but invisible (no budget data yet); frontend type extended.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-core-federal-data-load/44-CONTEXT.md
@.planning/v2.0-recon/RECON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dataset_type 'federal_agency' migration</name>
  <files>supabase/migrations/20260612110000_add_federal_agency_dataset_type.sql</files>
  <read_first>
    - supabase/migrations/20260602031258_add_all_funds_requirements_dataset_type.sql — the precedent
  </read_first>
  <action>
    DROP + re-ADD data_sources_dataset_type_check with values: operating, revenue, transactions, salaries, all_funds_requirements, federal_agency. Apply via apply_migration. Verify via pg_get_constraintdef. (treasury.budgets.dataset_type has no CHECK — confirmed during planning; do not invent one.)
  </action>
  <verify><automated>pg_constraint def contains 'federal_agency' + all 5 prior values</automated></verify>
  <acceptance_criteria>
    - Constraint has 6 values; existing rows unaffected
  </acceptance_criteria>
  <done>The agency lens has a legal dataset_type.</done>
</task>

<task type="auto">
  <name>Task 2: federal context tables migration</name>
  <files>supabase/migrations/20260612110100_create_federal_context_tables.sql</files>
  <action>
    CREATE TABLE treasury.federal_annual_summary (
      id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      fiscal_year smallint NOT NULL UNIQUE,
      receipts numeric NOT NULL,
      outlays numeric NOT NULL,
      surplus_or_deficit numeric NOT NULL,   -- raw OMB sign convention (negative = deficit); never re-derive or re-sign
      mandatory numeric,
      discretionary_defense numeric,
      discretionary_nondefense numeric,
      net_interest numeric,
      source_name text NOT NULL,
      source_url text NOT NULL,
      source_date date NOT NULL,
      created_at timestamptz NOT NULL DEFAULT now()
    );
    CREATE TABLE treasury.federal_context_metrics (
      id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
      metric_key text NOT NULL UNIQUE,
      value numeric NOT NULL,
      as_of_date date NOT NULL,
      label text NOT NULL,
      source_name text NOT NULL,
      source_url text NOT NULL,
      source_date date NOT NULL,
      updated_at timestamptz NOT NULL DEFAULT now()
    );
    Add COMMENTs: amounts in DOLLARS (not thousands/millions) — loaders normalize; annual_summary holds ACTUALS ONLY (no OMB estimate years); metrics upsert by metric_key. RLS disabled (matches category_enrichment read path). Apply via apply_migration; verify column counts (13 and 9).
  </action>
  <verify><automated>information_schema.columns counts: federal_annual_summary=13, federal_context_metrics=9</automated></verify>
  <acceptance_criteria>
    - Both tables exist; source columns NOT NULL; UNIQUE keys in place
  </acceptance_criteria>
  <done>First split, history, FYTD strip, and debt/interest have a sourced home.</done>
</task>

<task type="auto">
  <name>Task 3: seedUSFederal.js with Census-fetched population</name>
  <files>scripts/seedUSFederal.js</files>
  <read_first>
    - scripts/seedCAState.js — state entity seed pattern (idempotent upsert, loadEnv style)
    - scripts/loadMAPopulation.js — Census API fetch pattern (note: use the inline-comment-stripping loadEnv from seedMACountyLinks.js per WR-03)
  </read_first>
  <action>
    Node script, --dry-run flag, following seedCAState.js structure:
    1. FETCH US population from the Census PEP API (free, no key needed for this query) — e.g. https://api.census.gov/data/2024/pep/population (or the current vintage endpoint pattern used by loadMAPopulation.js, adapted to us:1 geography). DO NOT hardcode a population from memory (v2.0 ground rule). Log the fetched value + the exact URL.
    2. Upsert municipality: name='United States', state='US', entity_type='federal', population=fetched, population_year=2024 (or the vintage the endpoint returns). Idempotent: SELECT by name+entity_type first; UPDATE if exists, INSERT otherwise.
    3. Print the municipality id for later loaders.
    4. Verify via SELECT that exactly one federal row exists.
    If the Census endpoint shape differs from expectations, STOP and report rather than guessing — population feeds per-capita displays (VIZ-05).
  </action>
  <verify>
    <automated>
      execute_sql: SELECT count(*) FROM treasury.municipalities WHERE entity_type='federal' -- returns 1, population > 300000000;
      getCities shape returns 0 rows for it (no budgets yet — invisible, correct)
    </automated>
  </verify>
  <acceptance_criteria>
    - Exactly one federal municipality; population Census-fetched (logged URL), not hardcoded
    - Re-running the script changes nothing (idempotent)
    - Entity NOT visible via getCities shape (no budget data yet)
  </acceptance_criteria>
  <done>US entity exists with sourced population; loaders have their municipality_id.</done>
</task>

<task type="auto">
  <name>Task 4: frontend dataset_type union + unknown-type safety check</name>
  <files>src/types/budget.ts</files>
  <action>
    1. Add 'federal_agency' to the available_datasets dataset_type union in src/types/budget.ts.
    2. Grep the frontend for every consumer of dataset_type (tab rendering, labels, switches): `grep -rn "dataset_type" src/ | grep -v types/budget.ts`. For each consumer, confirm an unrecognized/extra type cannot crash rendering (fallback label or filtered out). If a consumer would break, apply the MINIMAL guard (e.g., skip unknown types in the tab list) and document it in the summary. Do NOT build federal_agency UI — that is Phase 45.
    3. npx tsc --noEmit && npm run build must pass.
  </action>
  <verify><automated>tsc + build green; summary lists every dataset_type consumer with disposition</automated></verify>
  <acceptance_criteria>
    - Union extended; all consumers audited; zero rendering risk from the new type
  </acceptance_criteria>
  <done>Frontend is safe for federal_agency data landing in available_datasets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Census API → seed script | External JSON; validate numeric population in sane band (300M–400M) before writing |
| Migrations → DB | Inline-authored DDL via MCP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Census response | mitigate | Sanity band 300M–400M; halt outside it |
| T-44-02 | Info disclosure | service key in seed script | mitigate | loadEnv pattern; never logged (T-33-08 precedent) |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. Constraint shows 6 dataset types; both context tables present with NOT NULL source columns
2. One federal municipality, Census-sourced population, invisible to getCities
3. tsc + build green; dataset_type consumer audit complete
</verification>

<success_criteria>
- All Phase 44 loaders have their schema + municipality_id; nothing public-facing changed yet
</success_criteria>

<output>
Create .planning/phases/44-core-federal-data-load/44-01-SUMMARY.md when done
</output>
