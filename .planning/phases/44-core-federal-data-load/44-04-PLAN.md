---
phase: 44-core-federal-data-load
plan: 04
type: execute
wave: 3
depends_on: [44-02, 44-03]
files_modified:
  - scripts/extractOMBPublicBudgetDB.py
  - scripts/loadFederalFunctions.js
autonomous: false
requirements:
  - DATA-01
must_haves:
  truths:
    - "The function lens (dataset_type='operating', FY2025) is a DEEP tree: Function → Subfunction → Account (3 levels) if the OMB Public Budget Database outlays file is usable; Function → Subfunction (2 levels) via the verified fallback otherwise"
    - "Tree total reconciles to OMB FY2025 outlays 7,011,105M within 0.5% after accounting for excluded negative nodes (each stored as a disclosed context metric)"
    - "Which path was taken (deep vs fallback) and why is recorded in the summary"
  artifacts:
    - path: "scripts/loadFederalFunctions.js"
      provides: "Function-lens deep tree loader"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/extractOMBPublicBudgetDB.py"
      to: "scripts/loadFederalFunctions.js"
      via: "JSON rows with function/subfunction codes"
      pattern: "subfunction"
---

<objective>
Load the DEFAULT lens — Money Out by budget function — as deep as sourced outlays allow (Chris 2026-06-12: federal needs more than 3-deep; clarity is the goal). Primary path: OMB Public Budget Database outlays file (account-level, function-coded) → Function → Subfunction → Account. Fallback (verified sources): OMB Hist 3.2 → Function → Subfunction.

Output: extractor + loader; operating FY2025 dataset live (post-checkpoint rules from 44-03 apply); reconciliation documented.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-core-federal-data-load/44-CONTEXT.md
@.planning/v2.0-recon/RECON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-first — OMB Public Budget Database outlays file</name>
  <files>none (recon)</files>
  <action>
    The outlays DB file was NOT verified during recon. Locate it from the OMB budget materials pages (whitehouse.gov, browser UA; historically "Public Budget Database" outlays.xlsx alongside the Budget/supplemental materials; also mirrored in GovInfo BUDGET collection — the api.data.gov key in .env works for the GovInfo API if needed). Confirm: downloads, parses with openpyxl, contains account-level rows with agency/bureau/account names, function+subfunction codes, and an FY2025 ACTUAL outlays column (column-by-header-text rule). Spot-total: sum of FY2025 outlays across all accounts within 1% of 7,011,105M×1e6.
    DECISION GATE: if any of these fail, record exactly what failed and proceed with the FALLBACK path (Task 2 variant B). Do not force the deep path on shaky parsing — wrong numbers are worse than fewer levels.
  </action>
  <verify><automated>Summary records: file URL, parse result, FY2025 spot-total delta, GO-deep/GO-fallback decision</automated></verify>
  <acceptance_criteria>
    - Evidence-based path decision; no unverified source used for load
  </acceptance_criteria>
  <done>Deep-tree feasibility settled by evidence.</done>
</task>

<task type="auto">
  <name>Task 2: extractor + loader for the chosen path</name>
  <files>scripts/extractOMBPublicBudgetDB.py, scripts/loadFederalFunctions.js</files>
  <read_first>
    - scripts/processCA.js buildNLevelTree — N-level data-driven builder to replicate (levelCols pattern)
    - scripts/loadMACountyBudget.js — data_sources + RPC + idempotency
  </read_first>
  <action>
    VARIANT A (deep, preferred): extractOMBPublicBudgetDB.py emits flat rows {function_title, subfunction_title, account_title, agency_title, fy2025_outlays_dollars}. loadFederalFunctions.js builds the tree with levelCols=['function_title','subfunction_title','account_title'] via a buildNLevelTree port; account nodes carry line items i=[{d: account_title + ' — ' + agency_title, a, aa:null, f:null, e:null}] so the agency attribution survives at the leaf.
    VARIANT B (fallback): extract OMB Hist 3.2 (hist03z2 xlsx, Function → Subfunction outlays) the same header-text way; 2-level tree.
    Both variants:
    - Negative/zero nodes excluded + logged + stored as context metrics (44-CONTEXT rule) — Undistributed Offsetting Receipts WILL be negative; Commerce and Housing Credit may be.
    - data_sources: api_type='xlsx_download', dataset_type='operating', dataset_id='fy2025', base_url = exact xlsx URL, fiscal_year_start_month=10.
    - p_total = sum of positive nodes; reconciliation: positive-total minus |excluded negatives| vs 7,011,105M×1e6 within 0.5%.
    - --dry-run prints level counts (expect ~20 functions / ~75 subfunctions / ~4-5k accounts in variant A), top-10 nodes per level, exclusions, reconciliation delta.
    - Live run only under the 44-03 checkpoint decision (GO → load; HOLD → stop after dry-run, mark deferred-pending-45).
  </action>
  <verify>
    <automated>
      Dry-run reconciliation within 0.5%; if live: execute_sql — budgets row (US, 2025, operating); budget_categories depth distribution shows max depth 2 (variant A: 3 levels = depths 0,1,2) or 1 (variant B); category count matches dry-run
    </automated>
  </verify>
  <acceptance_criteria>
    - Function lens loaded at maximum sourced depth; reconciliation documented; idempotent re-run
    - Default city UI renders it without error (icicle drill works at loaded depth — manual click-through)
  </acceptance_criteria>
  <done>DATA-01 satisfied at evidence-backed depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OMB xlsx (unverified file) → parser | Verify-first task + spot-total before any use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Tampering | Public Budget DB parse errors | mitigate | 1% spot-total gate; fallback path; 0.5% reconciliation |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | openpyxl present; no installs |
</threat_model>

<verification>
1. Path decision evidence-recorded
2. Reconciliation to OMB anchor within 0.5% (exclusions itemized as metrics)
3. Depth distribution matches variant; icicle click-through at full depth
</verification>

<success_criteria>
- DATA-01 complete at the deepest sourced level; depth boundary + exclusions disclosed for Phase 45/46
</success_criteria>

<output>
Create .planning/phases/44-core-federal-data-load/44-04-SUMMARY.md when done
</output>
