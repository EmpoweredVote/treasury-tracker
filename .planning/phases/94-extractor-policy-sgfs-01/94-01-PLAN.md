---
phase: 94-extractor-policy-sgfs-01
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
files_modified:
  - .planning/phases/94-extractor-policy-sgfs-01/94-01-SPIKE.md
  - .planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md
  - scripts/loadStateGF.mjs
  - scripts/loadStateGF.test.mjs
requirements: [SGFS-01]
must_haves:
  truths:
    - "A sourcing spike (94-01-SPIKE.md) evaluates a uniform multi-state General Fund source (NASBO State Expenditure Report + Census Annual Survey of State Government Finances) against per-state ACFR on: 50-state coverage, basis (is it General Fund?), category granularity (can it build a 1-level revenue-by-source + spending-by-function tree?), free + machine-readable, multi-year depth, and consistency with the MN ACFR node — ending in a clear recommendation; the source is LOCKED at a Chris checkpoint (no source committed before the checkpoint)"
    - "Cross-cutting policy (94-01-POLICY.md) is locked for the whole milestone: FY depth/window per state, how a negative-revenue category (e.g. MN FY2022 investment losses) is represented in the icicle, the node concept + label + basis under the chosen source, and the MN-consistency decision (re-do MN under the uniform source vs keep MN as ACFR) — every choice applies uniformly to all 50 states"
    - "A reusable loader/extractor for the CHOSEN source builds sourced operating (spending-by-function) + revenue (revenue-by-source) trees, source-stamps every budget row (source_url + source_date + accurate data_source — no estimates, no false provenance), uses a targeted write (NOT treasury_sync_city_budget), and is idempotent; pure helpers are unit-tested offline"
    - "Proven end-to-end on ≥1 NEW state (not MN): loaded to production, spot-verified against the chosen source (totals + a couple categories tie out), 0-NULL source on the loaded rows, re-run = no change"
  artifacts:
    - path: ".planning/phases/94-extractor-policy-sgfs-01/94-01-SPIKE.md"
      provides: "The sourcing evaluation matrix + recommendation + the locked source decision (SGFS-01)"
      contains: "NASBO"
    - path: ".planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md"
      provides: "The locked cross-cutting policy applied to all 50 states"
      contains: "negative"
    - path: "scripts/loadStateGF.mjs"
      provides: "The reusable state General Fund loader/extractor for the chosen source (filename may differ if ACFR-per-state is chosen)"
      contains: "source_url"
  key_links:
    - from: "The chosen state General Fund source (uniform dataset or per-state ACFR)"
      to: "sourced, real, 0-estimate state-node budget rows for ≥1 proven state"
      via: "the reusable loader/extractor + locked policy + post-write source stamp"
      pattern: "loadStateGF"
---

<objective>
SGFS-01: decide HOW to source the 50-state General Fund rollout (uniform multi-state dataset vs per-state ACFR), lock the cross-cutting policy, build the loader for the chosen source, and prove it on ≥1 state beyond MN. The source mechanism is an output of the spike — gated by a Chris checkpoint — per his "spike a uniform source first" call (CONTEXT D-94-01).

The whole milestone's effort + consistency hinges on this phase: a good uniform source replaces 50 ACFR extractions; if none is faithful/granular enough, fall back to a generalized per-state ACFR extractor (MN template). Everything stays source-honest: real source_url + source_date, no estimates, no false provenance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/94-extractor-policy-sgfs-01/94-CONTEXT.md
@scripts/processMN.js
@scripts/processMNRevenue.js

<interfaces>
<!-- Production target kxsdzaojfaibhuzmclfq (.env). Reads via mcp__supabase-local; the one proof-load via the loader + service key. -->
- MN template: scripts/processMN.js / processMNRevenue.js — per-FY SOURCES map, treasury_sync_budget_tree RPC (builds budget + depth-1 category tree from jsonTree), post-RPC UPDATE to set source_url/source_date/data_source (the RPC does NOT set them). reference_treasury_budgets_probe_columns (total_budget/hierarchy). project_sync_city_budget_not_source_safe (use a targeted UPDATE, never a full re-sync).
- Uniform-source candidates to probe (Task 1): NASBO State Expenditure Report (General Fund by omnibus area, all states, free PDF/data) + NASBO Fiscal Survey (GF revenue); US Census ASSGF (api.census.gov / downloadable, standardized categories, all states). Check: GF-basis? per-category granularity for a tree? free + machine-readable? multi-year? matches MN's ACFR figures closely enough to sit beside it (or does MN need re-doing under the uniform source)?
- State node shape (existing): one municipality per state (entity_type='state'); operating + revenue budgets per FY; jsonTree root = "<State> General Fund Budget"/"Revenue" with depth-1 category children. ~50 state nodes already exist (mostly estimate data to be replaced in Phases 95/96).
- Cohort facts (discovery): 47 states unsourced estimates, OH+VA falsely-sourced, MN real (FY2023-2025 ACFR). See project_state_node_unsourced_estimates.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sourcing spike — evaluate uniform sources vs per-state ACFR</name>
  <files>.planning/phases/94-extractor-policy-sgfs-01/94-01-SPIKE.md</files>
  <read_first>
    - 94-CONTEXT.md (D-94-01 evaluation criteria; the candidates)
    - project_state_node_unsourced_estimates (what's wrong today; the MN template)
  </read_first>
  <action>
    Probe each candidate enough to judge it (WebSearch/WebFetch + a sample download where feasible): (1) NASBO State Expenditure Report — does it give General Fund spending by category for all 50 states, free, multi-year, machine-readable or cleanly extractable? Revenue side via NASBO Fiscal Survey? (2) US Census Annual Survey of State Government Finances — coverage, categories, API/CSV, but note it's NOT budgetary General Fund (basis mismatch). (3) Per-state ACFR (fallback) — confirm the MN-proven approach + its effort. Pull a concrete sample for the top candidate (e.g. one state's GF figures) and compare to that state's reality / MN's ACFR magnitude. Write 94-01-SPIKE.md: an evaluation matrix across the criteria (coverage / GF-basis / granularity / free+machine-readable / multi-year / MN-consistency) + a clear RECOMMENDATION (which source, and whether MN must be re-done to match).
  </action>
  <acceptance_criteria>
    - 94-01-SPIKE.md scores NASBO + Census + per-state-ACFR on all CONTEXT D-94-01 criteria with concrete evidence (a real sample for the lead candidate)
    - States a clear recommended source + the MN-consistency implication (re-do MN vs keep as ACFR)
    - Honest about gaps (e.g. a uniform source that isn't strictly GF, or lacks a revenue breakdown)
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 2: Chris locks the source + MN-consistency decision</name>
  <files>.planning/phases/94-extractor-policy-sgfs-01/94-01-SPIKE.md</files>
  <action>
    Present the spike recommendation to Chris at a BLOCKING checkpoint. Chris locks: (a) the source for the 50-state rollout (uniform NASBO/Census vs per-state ACFR), and (b) the MN-consistency call (re-do MN under the uniform source for one basis across all 50, vs keep MN as the ACFR gold-standard outlier). Record the locked decision in 94-01-SPIKE.md. Do not build the loader before this is locked.
  </action>
  <acceptance_criteria>
    - The chosen source + MN-consistency decision are recorded with Chris's explicit confirmation
    - Any basis-consistency trade-off (mixed-basis risk) is surfaced before locking
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Lock cross-cutting policy</name>
  <files>.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md</files>
  <read_first>
    - 94-01-SPIKE.md (the locked source — policy is written against it)
    - 94-CONTEXT.md (D-94-03 policy questions)
  </read_first>
  <action>
    Write 94-01-POLICY.md locking, for the chosen source, the rules that apply uniformly to all 50 states: (1) FY depth/window (match what the source covers uniformly); (2) negative-revenue-category representation in the icicle (decide one: clamp-to-0-with-footnote / net-into-"Other" / show-flagged — pick what's honest + renders); (3) node concept + display label + basis label under the chosen source; (4) the source-stamp contract (source_url + source_date + data_source per row); (5) how a state with no clean source is handled (remove node vs leave absent). These become the spec Phases 95/96 follow.
  </action>
  <acceptance_criteria>
    - 94-01-POLICY.md locks FY-depth, negative-revenue handling, node label/basis, source-stamp contract, and no-source handling — each applicable uniformly to all 50 states
    - The negative-revenue rule is concrete enough to implement (not "decide later")
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build the loader + prove on ≥1 new state</name>
  <files>scripts/loadStateGF.mjs, scripts/loadStateGF.test.mjs</files>
  <read_first>
    - scripts/processMN.js / processMNRevenue.js (the template: SOURCES map, RPC, post-RPC source stamp, idempotency)
    - 94-01-SPIKE.md + 94-01-POLICY.md (chosen source + rules)
    - project_sync_city_budget_not_source_safe (targeted write, not re-sync)
  </read_first>
  <action>
    Build the reusable loader/extractor for the chosen source (a uniform-source loader if NASBO/Census; a generalized per-state-ACFR extractor adapted from processMN.js if ACFR). It must: parse/ingest the source into operating (spending-by-function) + revenue (revenue-by-source) trees per FY; apply the locked policy (FY window, negative-revenue rule); write via the budget-tree RPC + a targeted post-write UPDATE that stamps source_url + source_date + data_source (no estimates, no false provenance); be idempotent; export pure helpers with offline unit tests. Then PROVE end-to-end on ≥1 NEW state (not MN): with .env sourced, load that state to production, then verify (mcp__supabase-local) the totals + a couple categories tie to the source, 0-NULL source on the loaded rows, and a re-run makes no change.
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadStateGF.test.mjs` passes (pure-helper coverage)
    - ≥1 new state loaded to production from the chosen source, source-stamped, spot-verified against the source (totals + sample categories), 0-NULL source on its rows
    - Re-run is idempotent; the loader is the reusable tool Phases 95/96 will drive
  </acceptance_criteria>
</task>

</tasks>

<verification>
- A spike chose the 50-state General Fund source on evidence (uniform vs ACFR), Chris locked it + the MN-consistency call, and the cross-cutting policy (FY depth, negative-revenue handling, label/basis, source-stamp, no-source rule) is written down for Phases 95/96.
- A reusable, idempotent, source-stamping loader for the chosen source is built + unit-tested + proven on ≥1 new state (real, sourced, 0-estimate, spot-verified).
- $0 spend; every loaded row carries real provenance (no estimates, no false source). SGFS-01 met.
- Out of scope: extending MN history + OH/VA re-do (Phase 95); remaining states (Phase 96); cohort verification + UAT (Phase 97).
</verification>
