---
phase: 24-los-angeles-data-refresh
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/dashboard/PlainLanguageSummary.tsx
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "PlainLanguageSummary surfaces the 2-3 sentence enrichment.description for the top category, not only the 1-sentence shortDescription"
    - "The description paragraph is hidden when description is absent or equals shortDescription (no duplicate prose)"
    - "No new AI API calls and no DB writes — the description field already exists on the categories API response"
  artifacts:
    - path: "src/components/dashboard/PlainLanguageSummary.tsx"
      provides: "Rendered enrichment.description prose block for top operating category"
      contains: "enrichment?.description"
  key_links:
    - from: "categories API response (BudgetCategory.enrichment.description)"
      to: "PlainLanguageSummary topCategories[0].enrichment.description render block"
      via: "optional-chaining guard + description !== shortDescription"
      pattern: "topCategories\\[0\\].*enrichment.*description"
---

<objective>
Improve the compelling plain-language section summaries using ONLY existing enrichment data —
zero AI API calls, zero DB writes. The 70 LA enrichment rows already store a richer 2-3 sentence
`description` field that the categories API returns as `enrichment.description` on each
BudgetCategory, but PlainLanguageSummary currently shows only the 1-sentence
`enrichment.shortDescription`. Surface `description` for the top operating category as an
additional prose paragraph, following the exact two-field display pattern already used in
App.tsx (lines 771-790).

Purpose: Citizens get fuller, more compelling context about where the biggest dollars go, with
data the project already paid to generate — no new spend.
Output: One edited React component with a guarded description paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-los-angeles-data-refresh/24-RESEARCH.md
@.planning/phases/24-los-angeles-data-refresh/24-PATTERNS.md
@src/components/dashboard/PlainLanguageSummary.tsx
@src/App.tsx
@src/types/budget.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm description is in the API response, then add guarded description paragraph</name>
  <files>src/components/dashboard/PlainLanguageSummary.tsx</files>
  <read_first>
    - src/components/dashboard/PlainLanguageSummary.tsx (read the topCategories.length > 0 block ~lines 215-251 — the existing shortDescription inline usage and where the paragraph closes)
    - src/App.tsx lines 771-790 (the shortDesc + desc two-field display precedent, including the desc !== shortDesc guard)
    - src/types/budget.ts lines 63-66 (CategoryEnrichment interface — confirms description field exists)
    - .planning/phases/24-los-angeles-data-refresh/24-PATTERNS.md (PlainLanguageSummary section — exact insertion point and JSX block)
  </read_first>
  <action>
    First confirm the description field is actually returned by the categories API (RESEARCH.md
    Open Question #3). Fetch the LA FY2025 categories endpoint
    https://ev-accounts-api.onrender.com/api/treasury/budgets/5a85c4a6-456f-49ba-af63-771dd0dde3a5/categories
    and check that at least one category's enrichment object contains a non-empty `description`
    distinct from `shortDescription`. If description is absent from the API response, STOP and
    surface this to the user before editing (the UI change would render nothing) — do not invent
    a fallback. If present, proceed.

    In src/components/dashboard/PlainLanguageSummary.tsx, immediately after the closing </p> of
    the `topCategories.length > 0 && (<p>...</p>)` block (~after line 250), insert a new guarded
    paragraph that renders topCategories[0]?.enrichment?.description. Guard it with both an
    optional-chain check and `description !== shortDescription` (per the App.tsx precedent at
    lines 771-790) so it never duplicates the inline shortDescription already shown on line 222-224.
    Style it as muted secondary prose consistent with this component (e.g. text-[14px],
    leading-relaxed, ev-gray muted tone, dark-mode variant). Do NOT modify the shortDescription
    inline spans, the revenueData block, or any other section. No DB writes. No enrichCategories.js.
    No new AI API calls.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/components/dashboard/PlainLanguageSummary.tsx','utf8'); const hasDesc=/topCategories\[0\][\s\S]{0,80}enrichment[\s\S]{0,20}description/.test(s); const hasGuard=/description\s*!==\s*[\s\S]{0,40}shortDescription/.test(s); if(!hasDesc){console.error('description render block missing');process.exit(1)} if(!hasGuard){console.error('description !== shortDescription guard missing');process.exit(1)} console.log('OK: guarded description block present')"</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: categories API for budget 5a85c4a6 returns at least one enrichment.description non-empty and distinct from shortDescription (confirmed before editing)
    - Source: PlainLanguageSummary.tsx renders `topCategories[0]?.enrichment?.description` in a paragraph after the topCategories block
    - Source: the render is guarded by both optional chaining AND `description !== shortDescription` (no duplicate prose)
    - Source: the existing shortDescription inline spans (lines ~222-224, 232-234, 244-246) are unchanged
    - Behavior: project typecheck/build passes (e.g. `npm run build` or `tsc --noEmit` exits 0)
    - No DB writes and no AI API calls were made
  </acceptance_criteria>
  <done>API response confirmed to include description; PlainLanguageSummary renders a guarded enrichment.description paragraph for the top category; build passes; no DB writes; no AI spend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the description prose renders for LA in the running app</name>
  <what-built>
    A guarded enrichment.description paragraph was added to PlainLanguageSummary, surfacing the
    2-3 sentence description for the top operating category (data that already existed in the
    enrichment rows — no new AI spend).
  </what-built>
  <how-to-verify>
    1. Run the dev server (e.g. `npm run dev`) and open the app.
    2. Select Los Angeles, fiscal year FY2025.
    3. In the plain-language summary, confirm the top department line still shows its short
       one-line description (e.g. "Police — ..."), AND below the spending paragraph there is now
       a fuller 2-3 sentence description paragraph.
    4. Confirm the description text is NOT a duplicate of the short one-liner.
    5. Switch to a city/year whose top category has no description (or where description equals
       shortDescription) and confirm the extra paragraph is hidden (no empty block, no duplicate).
    6. Confirm the Money In / revenue summary still reads sensibly (this plan changed only the
       operating top-category prose).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (missing text, duplicate prose, layout break).</resume-signal>
</task>

</tasks>

<verification>
- Behavior: categories API includes enrichment.description distinct from shortDescription
- Source: PlainLanguageSummary renders guarded topCategories[0].enrichment.description
- Build: typecheck/build passes
- App: LA FY2025 summary shows the fuller description paragraph; hidden when absent/duplicate
- Zero AI API calls; zero DB writes
</verification>

<success_criteria>
Plain-language summaries are more compelling — the existing 2-3 sentence enrichment.description
is surfaced for the top operating category, guarded against duplication, using only data the
project already generated. No AI API calls, no DB writes.
</success_criteria>

<output>
Create `.planning/phases/24-los-angeles-data-refresh/24-03-SUMMARY.md` when done
</output>
