---
phase: 123-nasbo-retirement-nasbort-01
plan: "123-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadStateGF.mjs
  - scripts/loadStateGF.test.mjs
  - docs/state-acfr-5050.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [NASBORT-01]
user_setup: []

must_haves:
  truths:
    - "NASBORT-01: scripts/loadStateGF.mjs is demoted to FALLBACK-ONLY — its header docstring + main() console banner say FALLBACK-ONLY (retired 2026-07-05, all 50 states on ACFR), and it is NOT deleted (REQUIREMENTS non-goal: retire, don't delete)"
    - "Guard: loadStateFY skips any state-FY whose existing treasury.budgets operating row carries a non-NASBO (ACFR/other) data_source — it writes ONLY where the node is absent or itself NASBO; the skip decision is a pure exported helper (isAcfrOccupied) unit-tested offline"
    - "No-regression: an unfiltered live re-run `node scripts/loadStateGF.mjs` overwrites 0 ACFR nodes — every ACFR-occupied state-FY is SKIPPED before the ephemeral data_sources insert (0 residue on skipped state-FYs)"
    - "Idempotent: re-running the loader for the two remaining fallback nodes (NV FY2024, KY FY2023) is a safe no-op refresh; re-running for any ACFR-occupied state-FY is a skip"
    - "Criterion #2 verified: exactly two live state nodes carry a NASBO data_source (NV FY2024 operating, KY FY2023 operating), and both are years without same-year ACFR — no live node shows NASBO where same-year ACFR exists"
    - "Criterion #3 documented: docs/state-acfr-5050.md records the 50/50-ACFR end state, the two accepted honest fallback nodes with their reasons, and that the NASBO path remains available but dormant (fallback-only)"
  artifacts:
    - path: "scripts/loadStateGF.mjs"
      provides: "NASBO operating loader demoted to fallback-only — never-overwrite-ACFR guard + FALLBACK-ONLY relabel"
      contains: "FALLBACK-ONLY"
    - path: "scripts/loadStateGF.test.mjs"
      provides: "Offline unit test proving the isAcfrOccupied guard helper protects ACFR nodes and allows absent/NASBO nodes"
      contains: "isAcfrOccupied"
    - path: "docs/state-acfr-5050.md"
      provides: "Durable 50/50-ACFR end-state record: all 50 states on ACFR, the two honest NASBO fallback nodes, NASBO retired-to-fallback-only decision"
      contains: "50/50"
  key_links:
    - from: "scripts/loadStateGF.mjs (loadStateFY guard)"
      to: "treasury.budgets (existing operating row data_source)"
      via: "read-only maybeSingle() lookup before write; skip when isAcfrOccupied(existing.data_source)"
      pattern: "NASBORT-01"
---

<objective>
Retire the NASBO operating loader (`scripts/loadStateGF.mjs`) to **fallback-only** now that
all 50 states are on ACFR. Two mechanisms: (1) a behavioral **never-overwrite-ACFR guard**
(the loader skips any state-FY already served by a non-NASBO/ACFR source, writing only where
the node is absent or itself NASBO — making an unfiltered re-run a safe no-op), extracted as a
pure unit-tested helper; and (2) a **FALLBACK-ONLY relabel** of the loader header + banner
(retire, do NOT delete). Then read-only-verify that exactly two live nodes (NV FY2024, KY
FY2023) still carry NASBO — both years without same-year ACFR — and **document the 50/50-ACFR
end state** in `docs/state-acfr-5050.md`.

No new ACFR data load. No delete of the loader.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/123-nasbo-retirement-nasbort-01/123-CONTEXT.md

<interfaces>
- `scripts/loadStateGF.mjs` — NASBO operating loader. `loadStateFY(supabase, st, fy, dryRun)`
  resolves the municipality (`muni`), then inserts an ephemeral `treasury.data_sources` row,
  calls `treasury_sync_budget_tree`, stamps text provenance, and deletes the ephemeral row
  (WR-05/LOAD-01: 0 residue). `main()` loops `STATES` (all NASBO states) × their operating FYs;
  `--state`/`--fy` filter; `--dry-run` validates without DB writes.
- `treasury.budgets`: keyed (municipality_id, fiscal_year, dataset_type). The ACFR loaders
  stamp `data_source` like `"<State> State ACFR — General Fund (FY#### actual, GAAP basis)"`;
  the NASBO loader stamps `dataSourceLabel(fy)` = `"NASBO State Expenditure Report — General Fund (FY#### actual, budgetary basis)"`.
- `scripts/loadStateGF.test.mjs` — offline `node --test` harness; imports pure helpers from
  `loadStateGF.mjs` (no DB, no network). New guard helper test goes here.
- DB verification: use `mcp__supabase-local__execute_sql` (read-only).
</interfaces>

<source_facts>
<!-- Live-DB ground truth captured 2026-07-05 during planning — USE these. -->
- 50/50 states have ACFR operating data (count(distinct state)=50).
- Exactly two live state nodes carry a NASBO data_source: NV FY2024 operating, KY FY2023 operating.
  - NV: ACFR operating FY2019–2023; FY2024 = NASBO (ACFR not yet available — latest-year tail).
  - KY: ACFR operating FY2002–2025 EXCEPT FY2023 = NASBO (ACFR not obtained for FY2023; KY FY2023 revenue absent).
- Both NASBO nodes are years WITHOUT same-year ACFR → criterion #2 already satisfied.
- REQUIREMENTS.md non-goal: "Deleting the NASBO loader code → Retire to fallback-only, not delete."
- ACFR-41 (MS) / ACFR-42 (MT) are unchecked in REQUIREMENTS.md but were completed in Ph119
  (both are among the 50 ACFR states) — correct the checkboxes if verification confirms.
</source_facts>
</context>

<tasks>

<task id="123-01-01" type="auto">
  <name>Task 1: Add never-overwrite-ACFR guard + FALLBACK-ONLY relabel to loadStateGF.mjs</name>
  <files>scripts/loadStateGF.mjs</files>
  <read_first>
    - scripts/loadStateGF.mjs (full file — guard placement inside loadStateFY after muni resolution and BEFORE the ephemeral data_sources insert; header docstring + main() banner for the relabel)
    - .planning/phases/123-nasbo-retirement-nasbort-01/123-CONTEXT.md (locked decisions)
  </read_first>
  <action>
    (a) Add a pure exported helper `isAcfrOccupied(existingDataSource)`: returns `false` when
    `existingDataSource` is null/empty (node absent → NASBO may fill), `false` when it matches
    `/NASBO/i` (node is itself NASBO → allow idempotent refresh), and `true` otherwise (a
    non-NASBO/ACFR source occupies the node → protect it). Add it near `dataSourceLabel` and
    export it (mirror the existing `export function` style) so `loadStateGF.test.mjs` can import it.
    (b) In `loadStateFY`, in LIVE mode only (when `supabase` is truthy / not dry-run), AFTER the
    municipality (`muni`) is resolved and BEFORE the ephemeral `data_sources` insert, read the
    existing operating row: `supabase.schema('treasury').from('budgets').select('data_source')
    .eq('municipality_id', muni.id).eq('fiscal_year', fy).eq('dataset_type','operating').maybeSingle()`.
    If `isAcfrOccupied(existing?.data_source)` is true, log `  SKIP ${st.abbr} FY${fy}: ACFR node
    present — NASBO retired to fallback-only` and `return false` (no data_sources insert, no RPC,
    0 residue). Dry-run behavior is unchanged (validate-only, no DB read/write).
    (c) Relabel as FALLBACK-ONLY (do NOT delete anything): update the top-of-file header
    docstring title/intro to state the loader is FALLBACK-ONLY — retired 2026-07-05, all 50
    states on ACFR, kept as a dormant fallback for years without ACFR (currently NV FY2024,
    KY FY2023); and change the `main()` console banner string to include `[FALLBACK-ONLY]`
    (e.g. `State GF Loader — NASBO (operating) [FALLBACK-ONLY]${dryRun ? ' (dry-run)' : ''}`).
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node --check scripts/loadStateGF.mjs && grep -q "FALLBACK-ONLY" scripts/loadStateGF.mjs && grep -q "export function isAcfrOccupied" scripts/loadStateGF.mjs && grep -q "ACFR node present" scripts/loadStateGF.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/loadStateGF.mjs` exports `isAcfrOccupied` returning: false for null/empty, false for a NASBO-matching string, true for a non-NASBO string.
    - `loadStateFY` (live mode) reads the existing operating row and `return false` (skips) before the ephemeral data_sources insert when `isAcfrOccupied` is true, logging the SKIP line.
    - Header docstring and `main()` console banner both contain `FALLBACK-ONLY`.
    - Loader is NOT deleted; dry-run path unchanged; `node --check scripts/loadStateGF.mjs` exits 0.
  </acceptance_criteria>
  <done>The NASBO loader is demoted to fallback-only: a never-overwrite-ACFR guard skips every ACFR-occupied state-FY before any write, and the header + banner are relabelled FALLBACK-ONLY. Nothing deleted.</done>
</task>

<task id="123-01-02" type="auto">
  <name>Task 2: Unit-test the isAcfrOccupied guard helper (offline, no DB)</name>
  <files>scripts/loadStateGF.test.mjs</files>
  <read_first>
    - scripts/loadStateGF.test.mjs (existing offline test style — import from ./loadStateGF.mjs, node:test + node:assert/strict)
    - scripts/loadStateGF.mjs (isAcfrOccupied signature/behavior from Task 1)
  </read_first>
  <action>
    Add `isAcfrOccupied` to the import from `./loadStateGF.mjs`, then add a `test(...)` block
    asserting the guard's three branches with realistic labels:
    - `isAcfrOccupied(null)` === false and `isAcfrOccupied('')` === false (absent node → allow write).
    - `isAcfrOccupied('NASBO State Expenditure Report — General Fund (FY2024 actual, budgetary basis)')` === false (own fallback → allow idempotent refresh).
    - `isAcfrOccupied('Nevada State ACFR — General Fund (FY2023 actual, GAAP basis)')` === true (ACFR node → protect).
    - `isAcfrOccupied('Kentucky State ACFR — General Fund (FY2024 actual, GAAP basis)')` === true.
    Keep it in the same offline no-DB style as the surrounding tests.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node --test scripts/loadStateGF.test.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/loadStateGF.test.mjs` imports and tests `isAcfrOccupied` covering all three branches (absent, NASBO-self, ACFR-occupied).
    - `node --test scripts/loadStateGF.test.mjs` passes (0 failing) — new + all pre-existing tests green.
  </acceptance_criteria>
  <done>The guard helper has offline unit coverage proving it protects ACFR nodes while allowing absent and NASBO-self nodes; the full test file passes.</done>
</task>

<task id="123-01-03" type="auto">
  <name>Task 3: Read-only DB verification + document the 50/50-ACFR end state</name>
  <files>docs/state-acfr-5050.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/123-nasbo-retirement-nasbort-01/123-CONTEXT.md (source_facts, decisions)
    - .planning/REQUIREMENTS.md (NASBORT-01 line, ACFR-41/ACFR-42 checkboxes, non-goal)
  </read_first>
  <action>
    (a) Read-only verify via `mcp__supabase-local__execute_sql` (no writes):
      - `count(distinct state)=50` for `entity_type='state' AND dataset_type='operating' AND data_source ILIKE '%ACFR%'`.
      - Exactly two operating rows with `data_source ILIKE '%NASBO%'`: NV FY2024, KY FY2023.
      - For NV and KY, confirm those specific years have NO same-year ACFR operating row (NV FY2024, KY FY2023 are the only NASBO years and are the ACFR gap years).
    (b) Write `docs/state-acfr-5050.md` recording: the 50/50-ACFR end state (all 50 states on
    ACFR GAAP GF operating; CA/TX/NY/FL/etc. also revenue-by-source); a table of the **two
    accepted honest NASBO fallback nodes** (NV FY2024 — latest-year tail; KY FY2023 — one-year
    ACFR gap, revenue absent) with reasons; the **NASBORT-01 retirement decision** — the NASBO
    loader (`scripts/loadStateGF.mjs`) is demoted to fallback-only via a never-overwrite-ACFR
    guard + FALLBACK-ONLY relabel, kept available but dormant, NOT deleted; and how the two
    fallbacks will be superseded naturally when their ACFR years become available. Include the
    verification query results.
    (c) In `.planning/REQUIREMENTS.md`, mark `NASBORT-01` complete `[x]`, and — only if step (a)
    confirms MS and MT have ACFR operating — correct `ACFR-41` (MS) and `ACFR-42` (MT) to `[x]`.
    Update the milestone status table row for NASBORT-01 to Complete.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && test -f docs/state-acfr-5050.md && grep -q "50/50" docs/state-acfr-5050.md && grep -qi "NV FY2024" docs/state-acfr-5050.md && grep -qi "KY FY2023" docs/state-acfr-5050.md && grep -q "NASBORT-01" docs/state-acfr-5050.md</automated>
  </verify>
  <acceptance_criteria>
    - DB verification (read-only) confirms: 50 states with ACFR operating; exactly two NASBO operating nodes (NV FY2024, KY FY2023); neither year has a same-year ACFR operating row.
    - `docs/state-acfr-5050.md` exists and records the 50/50-ACFR end state, the two honest fallback nodes with reasons, and the NASBORT-01 retire-to-fallback-only decision (loader kept, not deleted).
    - `.planning/REQUIREMENTS.md` has NASBORT-01 marked `[x]`; ACFR-41/ACFR-42 corrected to `[x]` iff MS/MT ACFR operating confirmed; milestone status row updated to Complete.
    - No DB writes performed (verification is read-only).
  </acceptance_criteria>
  <done>DB verification confirms criterion #2 (only NV FY2024 + KY FY2023 on NASBO, both ACFR-gap years); `docs/state-acfr-5050.md` documents the 50/50-ACFR end state + honest fallbacks + retirement; REQUIREMENTS.md checkboxes corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loadStateFY guard → treasury.budgets | read-only `maybeSingle()` lookup of the existing operating row before any write; decides skip-vs-write |
| verification (Task 3) → treasury.budgets | READ-ONLY queries only; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-A | Tampering | an unfiltered loader re-run overwrites live ACFR nodes (data regression) | mitigate | never-overwrite-ACFR guard skips every ACFR-occupied state-FY before any write (Task 1); unit-tested (Task 2) |
| T-123-B | Tampering | guard placed AFTER the ephemeral data_sources insert → residue on skipped state-FYs | mitigate | Task 1 places the guard BEFORE the data_sources insert; skip = 0 residue |
| T-123-C | Info Disclosure | end-state doc silently omits an honest NASBO fallback (looks fully-ACFR when it isn't) | mitigate | Task 3 acceptance requires both NV FY2024 + KY FY2023 named with reasons |
| T-123-D | Tampering | guard over-broad and skips the two legitimate fallback refreshes (NV/KY) | mitigate | `isAcfrOccupied` returns false for NASBO-self labels; unit-tested (Task 2) |
| T-123-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- `node --check scripts/loadStateGF.mjs` exits 0; header + `main()` banner contain FALLBACK-ONLY; loader not deleted.
- `isAcfrOccupied` exported and correct: false for null/empty and NASBO-self; true for ACFR/other.
- `node --test scripts/loadStateGF.test.mjs` passes (new guard test + all pre-existing).
- Guard skips ACFR-occupied state-FYs before the ephemeral data_sources insert (0 residue on skips); NV FY2024 + KY FY2023 still refresh.
- Read-only DB verification: 50 ACFR states; exactly two NASBO nodes (NV FY2024, KY FY2023), both ACFR-gap years.
- `docs/state-acfr-5050.md` documents the 50/50 end state + two honest fallbacks + retirement; REQUIREMENTS.md NASBORT-01 `[x]` (+ ACFR-41/42 if confirmed).
</verification>

<success_criteria>
NASBORT-01 is satisfied: `scripts/loadStateGF.mjs` is demoted to fallback-only — a never-overwrite-ACFR guard (pure, unit-tested `isAcfrOccupied`) makes it serve only nodes without ACFR, and its header + banner are relabelled FALLBACK-ONLY (retired, not deleted). No live state node shows NASBO where same-year ACFR exists (only NV FY2024 + KY FY2023 remain, both ACFR-gap years). An unfiltered re-run overwrites 0 ACFR nodes and is idempotent. The 50/50-ACFR end state + the two honest fallbacks + the retirement are documented in `docs/state-acfr-5050.md`.
</success_criteria>

<output>
Create `.planning/phases/123-nasbo-retirement-nasbort-01/123-01-SUMMARY.md` when done — record the guard + relabel diff, the guard unit-test result, the read-only DB verification results (50 ACFR states; NV FY2024 + KY FY2023 the only NASBO nodes), the `docs/state-acfr-5050.md` end-state doc, and the REQUIREMENTS.md checkbox corrections. Note the Phase 124 hand-off (VER-10 UAT should confirm no node shows NASBO where ACFR exists).
</output>
