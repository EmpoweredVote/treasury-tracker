---
phase: 86-county-loads-data-model-linking
plan: 03
type: execute
wave: 3
depends_on: ["86-02"]
files_modified:
  - src/App.tsx
  - .planning/phases/86-county-loads-data-model-linking/86-HUMAN-UAT.md
autonomous: false
requirements: [OHLINK-01]
must_haves:
  truths:
    - "The Ohio hub is reachable from the top-level picker and Ohio cities + counties are selectable; selecting the Ohio state node lists its counties (and cities), mirroring the existing VA cohort behavior — verified in the running app, not just asserted"
    - "An Ohio city page shows the US → Ohio → <County> County → <City> breadcrumb (driven by the county_id set in 86-02 + the existing App.tsx jurisdictionParents resolver); an Ohio county page shows US → Ohio → <County> County and lists its cities in the Cities-in-County panel"
    - "Any Ohio-specific gap found during verification is fixed with a MINIMAL touch to the existing components (no rebuild — the nav kit already renders VA identically); if no gap is found, no component code changes and the plan is a verification + UAT-doc plan"
    - "A build + the existing test suite pass after any change (post-change integration gate); a 86-HUMAN-UAT.md documents the exact click-paths a human should confirm (Ohio selectable, a county page, a city breadcrumb, the Cities-in-County panel)"
  artifacts:
    - path: ".planning/phases/86-county-loads-data-model-linking/86-HUMAN-UAT.md"
      provides: "The human UAT script for Ohio navigation — concrete click-paths + expected results for OHLINK-01"
      contains: "Cities-in-County"
  key_links:
    - from: "county_id on OH cities + the Ohio state node + loaded county rows (86-02)"
      to: "rendered US→Ohio→County→City navigation, county pages, and the Cities-in-County panel"
      via: "existing EntitySwitcher / App.tsx jurisdictionParents / Breadcrumb / CitiesInCountyPanel — verify (+ minimal touch if needed)"
      pattern: "jurisdictionParents"
---

<objective>
Verify Ohio is navigable end-to-end in the running app — the final, UI half of OHLINK-01. The navigation kit already exists and renders the structurally-identical VA cohort, and 86-02 set the data (counties loaded, `county_id` on cities, Ohio state node). So this plan is verify-first: confirm US→Ohio→county→city renders (Ohio selectable, county pages, city breadcrumb, Cities-in-County panel), apply only a minimal touch if a real Ohio-specific gap surfaces, run a build/test gate, and produce a HUMAN-UAT script.

This is deliberately lean (CONTEXT D-09): the expensive frontend work was done for MA Phase 25 + VA Phase 81. Do not rebuild components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md
@src/App.tsx
@src/components/EntitySwitcher.tsx
@src/components/CitiesInCountyPanel.tsx
@src/components/Breadcrumb.tsx

<interfaces>
<!-- Everything renders already. This plan VERIFIES Ohio and only touches code if a gap is real. -->

Existing primitives (verified present 2026-06-25 — do not rebuild):
- `src/components/EntitySwitcher.tsx` — already shows `entity_type='state'` nodes regardless of data (the `withData` filter at line ~76–79 includes `m.entity_type === 'state'`), and groups non-state/federal entities as selectable localities. Ohio (entity_type='state') will appear in the State Governments list.
- `src/App.tsx` `jurisdictionParents` (line ~553) — resolves `selectedEntity.county_id` → the county municipality (line ~559–560) and composes the US→state→county→locality breadcrumb; rendered via `<Breadcrumb>` (line ~891). With 86-02's county_id set, Ohio city breadcrumbs resolve automatically.
- `src/components/CitiesInCountyPanel.tsx` — lists the cities whose `county_id` = the viewed county. Ohio counties will populate it once cities are linked.
- VA is the reference cohort: whatever renders for VA (state hub → county → city, Cities-in-County panel, breadcrumb) is the expected Ohio behavior.

How to run the app: follow the project's run skill / CLAUDE.md (e.g. `npm run dev`), or use the `run`/`verify` skills to launch + observe. Verify with real Ohio entities: Ohio hub, a county (e.g. "Franklin County"), a city (e.g. Columbus — expect US→Ohio→Franklin County→Columbus).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Ohio navigation end-to-end in the running app</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (jurisdictionParents resolver, breadcrumb render, selection handling)
    - src/components/EntitySwitcher.tsx (state grouping + withData state-node visibility)
    - src/components/CitiesInCountyPanel.tsx (county_id → cities list)
  </read_first>
  <action>
    Run the app (project run/verify skill or `npm run dev`). With the production data from 86-02, confirm each path renders (compare to the VA cohort as the known-good reference):
    1. Ohio appears in the top-level picker (State Governments) and is selectable as a hub.
    2. Selecting Ohio lists its counties + cities; an Ohio county (e.g. Franklin County) is selectable and shows operating + revenue data + per-capita.
    3. An Ohio city (e.g. Columbus) shows the US → Ohio → Franklin County → Columbus breadcrumb.
    4. The Franklin County page lists Columbus (and other Franklin cities) in the Cities-in-County panel.
    If ALL render correctly, make NO code change — record the observations. If a real Ohio-specific gap appears (e.g. a filter that excludes OH, a breadcrumb that stops at the state), apply the MINIMAL fix to the existing component and re-verify. Capture screenshots/observations for the SUMMARY.
  </action>
  <acceptance_criteria>
    - Ohio is selectable from the top-level picker; an Ohio county page renders data + per-capita
    - An Ohio city shows the full US→Ohio→County→City breadcrumb
    - The county page's Cities-in-County panel lists that county's Ohio cities
    - Any change made is a minimal touch to an existing component (no new components, no rebuild); if everything rendered, no src change was made
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Post-change build + test gate</name>
  <files>src/App.tsx</files>
  <read_first>
    - CLAUDE.md (build + test commands)
    - package.json (scripts)
  </read_first>
  <action>
    Run the project build (e.g. `npm run build`) and the existing test suite (the project's test command + `node --test scripts/loadOhioAOS.test.mjs`). Confirm green. This catches any regression from a Task-1 touch and any cross-cutting type/import break. If Task 1 made no code change, still run the build + tests to certify the phase-end integration state.
  </action>
  <acceptance_criteria>
    - The build completes with no errors
    - The existing test suite + `node --test scripts/loadOhioAOS.test.mjs` pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write 86-HUMAN-UAT.md</name>
  <files>.planning/phases/86-county-loads-data-model-linking/86-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v2.7-phases/81-towns-virginia-data-model-linking/81-HUMAN-UAT.md (the VA UAT-doc shape to mirror)
  </read_first>
  <action>
    Write `.planning/phases/86-county-loads-data-model-linking/86-HUMAN-UAT.md` — a concise human UAT script mirroring VA's 81-HUMAN-UAT.md: concrete click-paths + expected results for OHLINK-01 (Ohio selectable from the picker; open Franklin County → see data + per-capita + Columbus in the Cities-in-County panel; open Columbus → see US→Ohio→Franklin County→Columbus breadcrumb). Include 1–2 spot-check figures from 86-02's verification so the human can confirm numbers, plus a note on the F-1 Ironton per-capita gap if relevant.
  </action>
  <acceptance_criteria>
    - `86-HUMAN-UAT.md` exists with explicit Ohio click-paths + expected results covering: Ohio hub selectable, a county page (data + per-capita + Cities-in-County panel), and a city breadcrumb
    - It references concrete spot-check values a human can verify
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Ohio renders end-to-end in the app: hub selectable, county pages with data + per-capita, city US→Ohio→County→City breadcrumb, Cities-in-County panel populated (OHLINK-01 UI).
- Any code change was a minimal touch to an existing component; build + tests pass after the change.
- `86-HUMAN-UAT.md` gives a human a concrete Ohio navigation checklist.
- Cross-phase source-chain audit + UAT are Phase 88.
</verification>
