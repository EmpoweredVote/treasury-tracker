---
phase: 22-troutdale-or-budget-load
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - scripts/loadORPopulation.js
autonomous: false
requirements: []
user_setup: []
must_haves:
  truths:
    - "Citizens can select Troutdale, OR in the app and see operating budget department rows (~$21M FY2026)"
    - "Citizens see a Money In tab for Troutdale showing 10 revenue categories (~$33.7M FY2026)"
    - "Troutdale shows a per-capita figure derived from population 15749"
    - "Operating and revenue data_source rows for Troutdale coexist without dataset_id collision"
    - "D-03: All Funds Requirements (dataset_type='all_funds_requirements') deferred to Phase 23 per researcher judgment — Troutdale All Funds Requirements lists expenditure categories, not departments, adding non-trivial complexity out of scope for this phase"
  artifacts:
    - path: "scripts/loadORPopulation.js"
      provides: "Census OR population loader extended to include Troutdale (15749)"
      contains: "Troutdale"
  key_links:
    - from: "treasury.budgets (Troutdale operating + revenue)"
      to: "treasury.data_sources → treasury.municipalities (Troutdale, OR)"
      via: "treasury_sync_budget_tree RPC writes joined to seeded municipality"
      pattern: "Troutdale"
    - from: "App.tsx available_datasets"
      to: "Money In tab for dataset_type='revenue'"
      via: "auto-discovery (no frontend change)"
      pattern: "revenue"
---

<objective>
Live-load Troutdale operating and revenue budgets into the database, extend `loadORPopulation.js` to populate Troutdale's Census population, conditionally enrich opaque category descriptions, and human-verify the result in the app.

Purpose: This is the phase payoff — Troutdale becomes visible to citizens with Budget, Money In, and per-capita context, completing Multnomah County's three major cities (Portland, Gresham, Troutdale).
Output: Troutdale operating + revenue rows in treasury.budgets, Troutdale population in treasury.municipalities, optional enrichment rows, and a 22-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md
@.planning/phases/22-troutdale-or-budget-load/22-01-SUMMARY.md
@.planning/phases/22-troutdale-or-budget-load/22-02-SUMMARY.md
@scripts/loadORPopulation.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load Troutdale operating + revenue, then DB-verify</name>
  <read_first>
    - scripts/processTroutdale.js (the loader — confirm operating vs --revenue invocation)
    - .planning/phases/22-troutdale-or-budget-load/22-02-SUMMARY.md (the final FY include-list and per-FY expected totals decided in Plan 02)
    - .planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md (Validation Architecture: post-load DB verification SQL; data_source Rows to Create tables)
  </read_first>
  <action>
    Confirm the Troutdale municipality exists (seeded in Plan 01). Then run the live loads (no --dry-run):
    - `node scripts/processTroutdale.js` — loads operating budget for every FY in docs/Troutdale/ that passed Plan 02's dry-run.
    - `node scripts/processTroutdale.js --revenue` — loads revenue for the same FYs.
    The loader is idempotent (pre-load delete by data_source_id + fiscal_year), so re-runs are safe.

    After loading, verify in the DB using the supabase-local MCP tools (per project memory: use mcp__supabase-local__execute_sql, not the Dashboard). Run the two verification queries from RESEARCH Validation Architecture:
    1. Operating + revenue rows exist for Troutdale across the loaded FYs (join treasury.budgets → data_sources → municipalities WHERE m.name='Troutdale' AND m.state='OR', order by fiscal_year, dataset_type). Confirm FY2026 operating total ≈ $21.1M and FY2026 revenue total ≈ $33.7M.
    2. No operating/revenue data_source collision: list data_sources for Troutdale and confirm each FY has exactly two rows (one operating, one revenue) sharing dataset_id `fyYYYY` but distinct dataset_type — proving the 4-column upsert worked.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async({createClient})=>{const c=createClient(process.env.SUPABASE_URL||'https://kxsdzaojfaibhuzmclfq.supabase.co',process.env.SUPABASE_SERVICE_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY);const{data,error}=await c.schema('treasury').from('municipalities').select('id').eq('name','Troutdale').eq('state','OR').maybeSingle();if(error||!data)throw new Error('Troutdale muni missing');const{data:ds}=await c.schema('treasury').from('data_sources').select('id,dataset_type,dataset_id').eq('municipality_id',data.id);const op=ds.filter(r=>r.dataset_type==='operating');const rev=ds.filter(r=>r.dataset_type==='revenue');if(op.length<4)throw new Error('expected >=4 operating data_sources, got '+op.length);if(rev.length<4)throw new Error('expected >=4 revenue data_sources, got '+rev.length);console.log('DB OK: '+op.length+' operating + '+rev.length+' revenue data_sources')})"</automated>
  </verify>
  <acceptance_criteria>
    - At least 4 operating and 4 revenue data_source rows exist for Troutdale, OR (one of each per loaded FY)
    - Each fiscal year has both an operating and a revenue data_source sharing dataset_id `fyYYYY` with distinct dataset_type (no collision)
    - FY2026 operating budget total in treasury.budgets is within $1M of $21.1M
    - FY2026 revenue total in treasury.budgets is within $2M of $33.7M
    - The verify command exits 0 reporting the operating + revenue data_source counts
  </acceptance_criteria>
  <done>Troutdale operating and revenue budgets are loaded for all qualifying FYs; DB verification confirms correct totals and no operating/revenue data_source collision.</done>
</task>

<task type="auto">
  <name>Task 2: Extend loadORPopulation.js for Troutdale and run it; conditional enrichment</name>
  <read_first>
    - scripts/loadORPopulation.js (lines 15-22 — EXPECTED_CITIES and KNOWN_VALUES; the two constants to edit)
    - .planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md (Pattern 3 loadORPopulation.js two-constant edit; Population 15749; Enrichment Decision section; API Cost Threshold)
    - scripts/enrichCategories.js (confirm --city / --state / --year / --dry-run flags before running)
  </read_first>
  <action>
    Edit `scripts/loadORPopulation.js` with the two-constant change (Pattern 3):
    - Add `'Troutdale'` to `EXPECTED_CITIES` (becomes `['Portland', 'Gresham', 'Troutdale']`).
    - Add `Troutdale: 15749` to `KNOWN_VALUES` with the comment: Census sub-est2024_41.csv, SUMLEV=162, "Troutdale city" (2024).
    No other changes — the CSV download, parse, and per-city update loop already iterate the arrays. Then run `node scripts/loadORPopulation.js` to write Troutdale's population (it downloads the same Census file and updates the municipality row in place).

    Then assess enrichment. Run `node scripts/enrichCategories.js --city Troutdale --state OR --year 2026 --dry-run` first. Per RESEARCH Enrichment Decision: the opaque operating departments worth enriching are GENERAL GOVERNMENT, EXECUTIVE (largest GF dept at $2.6M), COMMUNITY SERVICES, and PD BUILDING OPERATIONS; revenue categories are plain English and low-value (same conclusion as Gresham Phase 21). Estimated cost is ~$0.027 total — well under the $5 threshold (project memory: estimate before running AI; stop if > $5). If the dry-run shows the enrichment adds citizen-meaningful descriptions for the opaque departments, run it live (drop --dry-run) for operating; skip revenue enrichment unless the dry-run shows clear value. Record the enrichment decision (RUN / SKIP per dataset) and actual/estimated cost in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/loadORPopulation.js && node -e "import('@supabase/supabase-js').then(async({createClient})=>{const c=createClient(process.env.SUPABASE_URL||'https://kxsdzaojfaibhuzmclfq.supabase.co',process.env.SUPABASE_SERVICE_KEY||process.env.SUPABASE_SERVICE_ROLE_KEY);const{data}=await c.schema('treasury').from('municipalities').select('population,population_year').eq('name','Troutdale').eq('state','OR').maybeSingle();if(!data||data.population!==15749)throw new Error('Troutdale population not 15749: '+(data&&data.population));console.log('population OK: '+data.population+' ('+data.population_year+')')})"</automated>
  </verify>
  <acceptance_criteria>
    - loadORPopulation.js contains `'Troutdale'` in EXPECTED_CITIES and `Troutdale: 15749` in KNOWN_VALUES
    - `node scripts/loadORPopulation.js` exits 0 with no sanity-mismatch warning for Troutdale
    - treasury.municipalities Troutdale row has population=15749, population_year=2024
    - An enrichment decision (RUN or SKIP, per operating/revenue) is recorded in the SUMMARY with cost estimate under $5
    - If enrichment ran, it used `--city Troutdale --state OR` (scoped, not universal — per project memory on NULL municipality_id bug)
  </acceptance_criteria>
  <done>Troutdale population (15749) is loaded via the two-constant loadORPopulation.js edit, and the enrichment decision is made and documented (run only if dry-run shows citizen value, under the $5 cap).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Troutdale in the app and write 22-VERIFICATION.md</name>
  <what-built>
    Troutdale, OR is now in the database with operating budget (~17 departments, ~$21M FY2026), revenue / Money In (10 categories, ~$33.7M FY2026), population 15749 for per-capita display, and optional enriched category descriptions. No frontend changes were needed — App.tsx auto-discovers the Money In tab from the revenue dataset_type.
  </what-built>
  <how-to-verify>
    1. Open the app (treasurytracker.empowered.vote, or the local dev server if running).
    2. Open the entity / city switcher and confirm Troutdale, OR appears — it should sit in the OR (Oregon) state group alongside Portland and Gresham.
    3. Select Troutdale. On the Budget tab, confirm ~17 department rows are shown and the FY2026 total reads ~$21M (e.g. POLICE OPERATIONS ~$6.7M, FIRE PROTECTION SERVICES ~$3.2M, EXECUTIVE ~$2.6M). Confirm NO row labeled PUBLIC SAFETY, COMMUNITY DEVELOPMENT, or PARKS & FACILITIES appears (those are subtotals).
    4. Confirm a per-capita figure is displayed and is plausible for ~15,749 residents (~$1,300/person operating).
    5. Confirm a "Money In" tab appears for Troutdale; open it and confirm 10 revenue categories totaling ~$33.7M FY2026 (PROPERTY TAXES, CHARGES FOR CURRENT SERVICES, etc.), with no Beginning Fund Balance row.
    6. If enrichment was run, confirm the opaque departments (EXECUTIVE, GENERAL GOVERNMENT, COMMUNITY SERVICES) show plain-language descriptions.
    Then write `.planning/phases/22-troutdale-or-budget-load/22-VERIFICATION.md` recording the verified totals, FY coverage, enrichment decision, and any UI observations.
  </how-to-verify>
  <resume-signal>Type "approved" if Troutdale displays correctly in Budget, Money In, and per-capita; otherwise describe what is wrong (wrong totals, missing tab, subtotal rows showing, per-capita off).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processTroutdale.js → Supabase RPC | Live budget writes authenticated by SUPABASE_SERVICE_KEY |
| enrichCategories.js → Anthropic API | AI enrichment incurs metered cost; scoped to Troutdale |
| loadORPopulation.js → census.gov | Census CSV download over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering | operating/revenue data_source collision | mitigate | DB-verify confirms each FY has distinct operating+revenue rows on shared dataset_id |
| T-22-04 | Tampering | inflated operating total from subtotal double-count | mitigate | SANITY_MAX $30M in loader + human-verify checks for absent subtotal rows |
| T-22-05 | Information Disclosure | enrichment writes universal (NULL municipality_id) descriptions bleeding into other cities | mitigate | enrichCategories.js invoked with `--city Troutdale --state OR` (scoped); per project memory on prior NULL-scope bug |
| T-22-06 | Repudiation / cost overrun | uncontrolled AI enrichment spend | mitigate | Dry-run + cost estimate (~$0.027) confirmed under $5 threshold before any live enrichment |
| T-22-SC | Tampering | npm/pip installs | accept | No new packages installed |
</threat_model>

<verification>
- Troutdale operating + revenue loaded; ≥4 operating and ≥4 revenue data_sources, no collision.
- FY2026 operating ≈ $21.1M, revenue ≈ $33.7M.
- Troutdale population = 15749 in municipalities.
- Enrichment decision recorded (run scoped to Troutdale, or skip), under $5.
- Human confirms Budget / Money In / per-capita display in app; 22-VERIFICATION.md written.
</verification>

<success_criteria>
- Citizens can select Troutdale, OR and see operating budget (~$21M FY2026, ~17 departments, no subtotal rows), Money In (10 categories, ~$33.7M FY2026), and a per-capita figure from population 15749.
- 22-VERIFICATION.md documents verified totals, FY coverage, and the enrichment decision.
</success_criteria>

<output>
Create `.planning/phases/22-troutdale-or-budget-load/22-03-SUMMARY.md` and `.planning/phases/22-troutdale-or-budget-load/22-VERIFICATION.md` when done. Record: final FYs loaded, FY2026 operating/revenue totals, population value, enrichment decision + cost, and human-verify result.
</output>
