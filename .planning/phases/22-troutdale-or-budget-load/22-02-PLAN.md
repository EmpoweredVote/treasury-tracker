---
phase: 22-troutdale-or-budget-load
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - scripts/processTroutdale.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "processTroutdale.js --dry-run prints FY2026 operating: 17 departments, ~$21.1M, with no DB writes"
    - "processTroutdale.js --revenue --dry-run prints FY2026 revenue: 10 categories, ~$33.7M, with no DB writes"
    - "The dry-run probes every PDF in docs/Troutdale/ so older FYs that parse cleanly are identified for inclusion (D-02)"
    - "Operating SANITY_MAX is $30M (small-city cap), not Gresham's $500M, and revenue mode is exempt from the sanity cap"
  artifacts:
    - path: "scripts/processTroutdale.js"
      provides: "Troutdale PDF→treasury_sync_budget_tree loader with operating and --revenue modes, worktree-safe docs/Troutdale discovery, 4-column data_source upsert"
      contains: "extractTroutdale.py"
  key_links:
    - from: "scripts/processTroutdale.js extractPDF"
      to: "scripts/extractTroutdale.py"
      via: "spawnSync with args array"
      pattern: "extractTroutdale\\.py"
    - from: "scripts/processTroutdale.js upsertDataSource"
      to: "treasury.data_sources"
      via: "4-column lookup including dataset_type"
      pattern: "\\.eq\\('dataset_type'"
---

<objective>
Create `scripts/processTroutdale.js`, the Node.js loader that runs `extractTroutdale.py`, builds the operating/revenue budget trees, and (in live mode) writes them via the `treasury_sync_budget_tree` RPC. This plan validates extraction end-to-end in dry-run for all available fiscal years — no DB writes yet.

Purpose: The loader is the bridge between the extractor (Plan 01) and the live DB load (Plan 03). Dry-running every PDF here resolves D-02 (which older FYs to include) before any live write.
Output: `scripts/processTroutdale.js` with passing operating and revenue dry-runs across all PDFs in docs/Troutdale/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md
@.planning/phases/22-troutdale-or-budget-load/22-PATTERNS.md
@scripts/processGresham.js
@.planning/phases/22-troutdale-or-budget-load/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create processTroutdale.js (operating + revenue loader)</name>
  <read_first>
    - scripts/processGresham.js (full file — copy template: imports, resolvePdfDir, Supabase init, extractPDF, inferFiscalYearFromFilename, buildOperatingTree, buildRevenueTree, ensureMunicipality, upsertDataSource, loadFiscalYear, processPDF, main)
    - .planning/phases/22-troutdale-or-budget-load/22-PATTERNS.md (processTroutdale.js section — the five required changes: city strings, resolvePdfDir path, PDF_URLS, extractor script name, SANITY_MAX)
    - .planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md (data_source Rows to Create table; Validation Architecture dry-run commands; PDF Availability media IDs)
    - scripts/extractTroutdale.py (the extractor this loader spawns — confirm the --mode flag and JSON shape: department/category, adopted_amount, fiscal_year, page_num)
  </read_first>
  <action>
    Copy `scripts/processGresham.js` to `scripts/processTroutdale.js`. Keep verbatim: the imports/module setup, `inferFiscalYearFromFilename` (the `fy(\d{4})-(\d{2})` regex already matches Troutdale's filenames), `buildOperatingTree`, `buildRevenueTree`, `loadFiscalYear` (including the pre-load delete and the `treasury_sync_budget_tree` RPC call with all seven `p_*` params), `processPDF`, and `main` (including the `parseArgs` options `dry-run`, `revenue`, `pdf`, and the readdir-based PDF discovery). Make these Troutdale-specific changes:

    1. In `resolvePdfDir`, change both `path.join(ROOT, 'docs', 'Gresham')` calls (primary candidate and worktree `mainCandidate`) to `'Troutdale'`. Keep the `git rev-parse --git-common-dir` worktree fallback intact.

    2. Replace the `PDF_URLS` dict with Troutdale media URLs keyed by ending fiscal year: 2026→`https://www.troutdaleoregon.gov/media/31436`, 2025→`media/26636`, 2024→`media/15016`, 2023→`media/15021`, 2022→`media/15026`, 2021→`media/15031`, 2020→`media/15036`, 2019→`media/15041`. These become the `base_url` stored on each data_source row.

    3. In `extractPDF`, change `pyScript` to `path.join(ROOT, 'scripts', 'extractTroutdale.py')` and update the error-message string to reference `extractTroutdale.py`. Keep `spawnSync` with the args array (no shell), `maxBuffer: 8 * 1024 * 1024`, and the `--mode revenue` arg push.

    4. Set `const SANITY_MAX = { 2026: 30_000_000 };` — Troutdale FY2026 operating is ~$21.1M; the $30M cap catches subtotal double-counting regressions. Keep the existing guard that applies SANITY_MAX only in operating mode (revenue mode is intentionally exempt because All Funds Resources legitimately exceeds operating).

    5. In `ensureMunicipality`, change `.eq('name', 'Gresham')` to `'Troutdale'` and the not-found error to reference `seedTroutdaleOregon.js`. In `upsertDataSource`, change the `name` template to `` `Troutdale ${label} FY${fiscalYear}` ``. Change the loader header log line and any remaining `Gresham` log strings to `Troutdale`.

    Do not change the 4-column data_source lookup (`municipality_id` + `api_type` + `dataset_id` + `dataset_type`) — the `dataset_type` filter is what prevents operating/revenue collision on the shared `fyYYYY` dataset_id (T-22-02).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/processTroutdale.js','utf8');if(!s.includes('extractTroutdale.py'))throw new Error('wrong extractor');if(!/30_?000_?000/.test(s))throw new Error('SANITY_MAX not 30M');if(!s.includes(\"'Troutdale'\")&&!s.includes('Troutdale '))throw new Error('Gresham strings remain');if(!s.includes(\"docs', 'Troutdale\"))throw new Error('resolvePdfDir path not Troutdale');if(!s.includes('media/31436'))throw new Error('PDF_URLS not Troutdale');console.log('processTroutdale.js wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - processTroutdale.js spawns `extractTroutdale.py` (not extractGresham.py)
    - `SANITY_MAX` is `{ 2026: 30_000_000 }`
    - `resolvePdfDir` joins `docs/Troutdale` in both the primary and worktree-fallback paths
    - `PDF_URLS` contains `media/31436` (FY2026) and is keyed by ending fiscal year
    - `ensureMunicipality` selects `name='Troutdale'`; `upsertDataSource` name template is `Troutdale ${label} FY${fiscalYear}`
    - The verify command exits 0 with output "processTroutdale.js wiring OK"
  </acceptance_criteria>
  <done>processTroutdale.js is a Troutdale-specific copy of processGresham.js with the extractor name, PDF_URLS, resolvePdfDir path, SANITY_MAX ($30M), and municipality/data_source strings all switched to Troutdale, and the operating-only sanity guard preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Dry-run validate operating + revenue across all PDFs (resolve D-02 FY depth)</name>
  <read_first>
    - scripts/processTroutdale.js (the loader created in Task 1)
    - .planning/phases/22-troutdale-or-budget-load/22-RESEARCH.md (Data Validation Reference: expected operating and revenue totals per FY; D-02 Assessment; Open Question 1 on older-FY structure)
    - .planning/phases/22-troutdale-or-budget-load/22-01-SUMMARY.md (which PDFs downloaded successfully)
  </read_first>
  <action>
    Run `node scripts/processTroutdale.js --dry-run` (operating) and `node scripts/processTroutdale.js --revenue --dry-run` (revenue). Both iterate every PDF in docs/Troutdale/ with no DB writes. Inspect output for each fiscal year:
    - Operating expected totals: FY2023 ~$17.2M, FY2024 ~$18.5M, FY2025 ~$18.8M, FY2026 ~$21.1M; each should show 17 departments. The $30M SANITY_MAX must NOT trip for any operating year.
    - Revenue expected totals: FY2023 ~$28.2M, FY2024 ~$31.4M, FY2025 ~$30.3M, FY2026 ~$33.7M; each should show 10 categories with no Beginning Balance row.

    Resolve D-02 from the output: the 4 verified FYs (FY2023-FY2026) MUST parse with 17 operating departments / 10 revenue categories and plausible totals. For the older FYs (FY2019-FY2022), inspect their dry-run output — if any older FY parses with a comparable department/category count and a plausible total (operating well under the $30M cap, no zero-row or fiscal-year-None warnings), it has consistent structure and should be included in the live load. If an older FY produces 0 rows, wildly wrong totals, or `Could not parse fiscal year` warnings, it has a different (pre-format-change) structure — exclude it. Per D-02, do NOT build a multi-format extractor for older formats.

    Record the final list of fiscal years to load (the 4 verified plus any older FYs that passed) in the SUMMARY. Do not modify the extractor to force older years to parse — exclusion is the correct outcome for incompatible formats.
  </action>
  <verify>
    <automated>node scripts/processTroutdale.js --dry-run > %TEMP%/tr_dry_op.txt 2>&1 && node scripts/processTroutdale.js --revenue --dry-run > %TEMP%/tr_dry_rev.txt 2>&1 && node -e "const fs=require('fs');const op=fs.readFileSync(process.env.TEMP+'/tr_dry_op.txt','utf8');const rev=fs.readFileSync(process.env.TEMP+'/tr_dry_rev.txt','utf8');if(!/FY2026 Operating/.test(op))throw new Error('no FY2026 operating line');if(/SANITY FAIL/.test(op))throw new Error('sanity cap tripped in operating dry-run');if(!/FY2026 Revenue/.test(rev))throw new Error('no FY2026 revenue line');if(!/17 departments/.test(op))throw new Error('FY2026 not 17 departments');if(!/10 categories/.test(rev))throw new Error('FY2026 not 10 categories');console.log('operating + revenue dry-runs OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processTroutdale.js --dry-run` exits 0, prints FY2026 Operating with 17 departments and a total within $1M of $21.1M, and prints no `SANITY FAIL`
    - `node scripts/processTroutdale.js --revenue --dry-run` exits 0, prints FY2026 Revenue with 10 categories and a total within $2M of $33.7M
    - All 4 verified FYs (FY2023-FY2026) appear in both dry-runs with the expected row counts
    - The SUMMARY records the explicit list of fiscal years that will be live-loaded in Plan 03, with older FYs included only if they parsed cleanly
    - No `Could not parse fiscal year` warning appears for any FY that is on the include list
  </acceptance_criteria>
  <done>Both dry-runs pass for the 4 verified FYs with correct row counts and totals; the older FYs are classified as include (clean parse) or exclude (format mismatch), and the final FY include-list is recorded for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processTroutdale.js → extractTroutdale.py | Node spawns Python via spawnSync with an args array (no shell) over controlled docs/Troutdale paths |
| processTroutdale.js → Supabase RPC | Live writes (Plan 03) go through treasury_sync_budget_tree authenticated by SUPABASE_SERVICE_KEY |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Elevation of Privilege | spawnSync extractor invocation | mitigate | Args array (no shell string); pdfPath from docs/Troutdale readdir, never user input |
| T-22-02 | Tampering | operating/revenue data_source collision on shared fyYYYY dataset_id | mitigate | 4-column upsert includes `.eq('dataset_type', datasetType)` — preserved verbatim |
| T-22-03 | Denial of Service | extractor stdout buffer overflow | mitigate | spawnSync maxBuffer 8MB; extractor emits compact JSON rows only |
| T-22-04 | Tampering | regressed extractor double-counts subtotals → inflated total | mitigate | SANITY_MAX $30M aborts operating load if FY2026 exceeds cap |
| T-22-SC | Tampering | npm/pip installs | accept | No new packages — @supabase/supabase-js and pdfplumber already installed |
</threat_model>

<verification>
- processTroutdale.js wiring check passes (extractor name, SANITY_MAX, Troutdale strings, PDF_URLS, resolvePdfDir).
- Operating dry-run: FY2026 = 17 departments, ~$21.1M; no SANITY FAIL.
- Revenue dry-run: FY2026 = 10 categories, ~$33.7M; no Beginning Balance.
- D-02 resolved: final FY include-list recorded in SUMMARY.
</verification>

<success_criteria>
- processTroutdale.js exists and is fully Troutdale-specific (extractor, URLs, path, sanity cap, strings).
- Both dry-runs pass for all 4 verified FYs with correct counts/totals and no DB writes.
- The set of fiscal years to live-load in Plan 03 is decided and documented.
</success_criteria>

<output>
Create `.planning/phases/22-troutdale-or-budget-load/22-02-SUMMARY.md` when done. Record: the final FY include-list for operating and revenue (4 verified + any older FYs that passed), per-FY dry-run totals and row counts, and any older FY excluded with the reason (0 rows / wrong total / fiscal-year parse failure).
</output>
