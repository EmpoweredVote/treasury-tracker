---
phase: 25-la-county-data-completion-county-city-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/cleanLACountyBudget.js
  - scripts/loadLACountyOperating.js
  - scripts/loadLACountyRevenue.js
autonomous: false
requirements: []
must_haves:
  truths:
    - "LA County operating FY2021-2024 totals come from the county dataset (~$32B, $33B, $35B, $38B), not city-aggregate (~$31.9B inflated)"
    - "LA County revenue FY2021-2024 totals come from the county dataset (~$32B, $34B, $36B, $39B)"
    - "LA County population is 10014009 with population_year 2020"
    - "No operating or revenue budget row for LA County has a null or orphaned data_source_id"
    - "LA County salaries rows FY2021-2025 are untouched"
    - "The wrong-sourced FY2025 operating row (~$44.1B) is removed (pending user confirmation in Task 1)"
  artifacts:
    - path: "scripts/cleanLACountyBudget.js"
      provides: "Scoped delete of stale operating/revenue rows + stale data_source rows + population fix"
    - path: "scripts/loadLACountyOperating.js"
      provides: "County operating loader (uctr-c2j8) — already exists, runs FY2021-2024"
    - path: "scripts/loadLACountyRevenue.js"
      provides: "County revenue loader (emxv-k8xv) — already exists, runs FY2021-2024"
  key_links:
    - from: "scripts/loadLACountyOperating.js"
      to: "treasury_sync_city_budget RPC"
      via: "supabase.rpc with p_data_source_name='CA State Controller - County Expenditures'"
      pattern: "treasury_sync_city_budget"
    - from: "scripts/cleanLACountyBudget.js"
      to: "treasury.budgets"
      via: "scoped delete eq(municipality_id) in(dataset_type) in(fiscal_year)"
      pattern: "\\.from\\('budgets'\\)\\s*\\.delete\\(\\)"
---

<objective>
Replace LA County's wrong-sourced operating and revenue budget data with accurate county-government data from the CA State Controller county datasets, fix population, and repair orphaned data_source FKs.

Purpose: The current LA County operating/revenue data was loaded from city-aggregate datasets (all 88 cities summed), not the county government's own budget — it is mislabeled and misleading. This implements D-01, D-02, and D-03.

Output: LA County shows accurate FY2021-2024 operating and revenue totals sourced from county datasets, population = 10,014,009, and zero orphaned data_source_id FKs on operating/revenue rows. Salaries rows untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-CONTEXT.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm FY2025 operating disposition before destructive delete</name>
  <decision>The current FY2025 operating row for LA County (~$44.1B) was loaded from the wrong source (city-aggregate dataset). The county dataset (uctr-c2j8) has no FY2025 data (verified — returns 0 rows). Should this FY2025 operating row be deleted, leaving FY2025 with only salaries data, or left intact?</decision>
  <context>D-01 says "delete all existing LA County operating and revenue budget rows." D-02 resolution caps the county source at FY2024. There is no county FY2025 source to replace the row with. RESEARCH.md (Pitfall 2, Open Question 2) recommends DELETE because the ~$44B figure is misleading city-aggregate data. This is an irreversible destructive choice, so it requires explicit confirmation before Task 2 runs.</context>
  <options>
    <option id="delete-fy2025-operating">
      <name>Delete FY2025 operating (recommended)</name>
      <pros>Removes misleading ~$44B city-aggregate figure; LA County operating shows only accurate FY2021-2024 county data; FY2025 still has accurate salaries</pros>
      <cons>FY2025 has no operating total until county source publishes FY2025</cons>
    </option>
    <option id="keep-fy2025-operating">
      <name>Keep FY2025 operating intact</name>
      <pros>Preserves an FY2025 operating figure for display continuity</pros>
      <cons>The figure is wrong-sourced (~$44B city-aggregate, not county government); contradicts D-01's intent; still has a null/orphaned data_source_id</cons>
    </option>
  </options>
  <resume-signal>Select: delete-fy2025-operating or keep-fy2025-operating</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create scoped cleanup script (delete stale rows + data_sources, fix population)</name>
  <files>scripts/cleanLACountyBudget.js</files>
  <read_first>
    - scripts/cleanLACountyBudget.js (current state — likely does not exist yet; confirm before writing)
    - scripts/loadLACountyOperating.js (Supabase client init pattern, schema-qualified call pattern, lines 20-26 and 122-134)
    - scripts/seedCollinCountyMunicipalities.js (error-guard pattern, lines 52-108)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (Pattern 1 delete code, Verified DB State table, exact stale source IDs)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (pre-reload delete pattern, lines 76-97; Supabase client init shared pattern, lines 499-503)
  </read_first>
  <action>
    Create scripts/cleanLACountyBudget.js as a Node.js ESM script. Use the Supabase client init pattern from loadLACountyOperating.js lines 20-26 (SUPABASE_URL default 'https://kxsdzaojfaibhuzmclfq.supabase.co', SUPABASE_SERVICE_KEY env, exit 1 if missing). Hardcode LA_COUNTY_ID = 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'.

    The script performs four operations in order, each guarded with error checks that process.exit(1) on error, and console.log of affected counts:

    1. Delete stale data_source rows by id: STALE_SOURCE_IDS = ['c68cc1d2-0274-40c7-9953-aa6f9d41f33c', '1f2e2694-571d-445b-86f5-3b35d4b0efc3'] via supabase.schema('treasury').from('data_sources').delete().in('id', STALE_SOURCE_IDS).

    2. Delete operating + revenue budget rows scoped to FY2021-2024 only: supabase.schema('treasury').from('budgets').delete().eq('municipality_id', LA_COUNTY_ID).in('dataset_type', ['operating','revenue']).in('fiscal_year', [2021,2022,2023,2024]). NEVER include 'salaries' in dataset_type. NEVER do a blanket delete without all three filters.

    3. FY2025 operating disposition — gate on a --delete-fy2025-operating CLI flag (parseArgs, strict:false). When the flag is present (Task 1 selected delete-fy2025-operating), delete the FY2025 operating row: supabase.schema('treasury').from('budgets').delete().eq('municipality_id', LA_COUNTY_ID).eq('dataset_type','operating').eq('fiscal_year', 2025). When absent, log "FY2025 operating left intact per user decision" and skip. Set the flag value in the run per Task 1's resolution.

    4. Fix population: supabase.schema('treasury').from('municipalities').update({ population: 10014009, population_year: 2020 }).eq('id', LA_COUNTY_ID) — implements D-03.

    Support a --dry-run flag: when set, log each intended operation and its filter scope without executing the delete/update calls. Wrap main() with .catch(err => { console.error('Fatal:', err); process.exit(1); }).
  </action>
  <verify>
    <automated>node scripts/cleanLACountyBudget.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/cleanLACountyBudget.js contains the literal string 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'
    - scripts/cleanLACountyBudget.js contains both stale source IDs 'c68cc1d2-0274-40c7-9953-aa6f9d41f33c' and '1f2e2694-571d-445b-86f5-3b35d4b0efc3'
    - The budgets delete chain includes .in('dataset_type', ['operating', 'revenue']) and .in('fiscal_year', [2021, 2022, 2023, 2024]) — grep confirms 'salaries' does NOT appear in any delete chain
    - scripts/cleanLACountyBudget.js contains population: 10014009 and population_year: 2020
    - node scripts/cleanLACountyBudget.js --dry-run exits 0 and prints the four planned operations without writing
  </acceptance_criteria>
  <done>Cleanup script exists, scopes deletes correctly to operating/revenue FY2021-2024 (never salaries), gates FY2025 operating on a flag, fixes population, and runs clean in --dry-run.</done>
</task>

<task type="auto">
  <name>Task 3: Run cleanup + reload operating and revenue FY2021-2024, verify totals</name>
  <files>scripts/loadLACountyOperating.js, scripts/loadLACountyRevenue.js</files>
  <read_first>
    - scripts/loadLACountyOperating.js (full file — confirm DATASET_ID 'uctr-c2j8', p_dataset_type 'operating', p_data_source_name 'CA State Controller - County Expenditures', and that it accepts --fy multiple flags)
    - scripts/loadLACountyRevenue.js (full file — confirm DATASET_ID 'emxv-k8xv', p_dataset_type 'revenue', p_data_source_name 'CA State Controller - County Revenues')
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (D-02 Resolution: expected totals by FY; Validation Architecture verification queries)
    - scripts/cleanLACountyBudget.js (the script created in Task 2)
  </read_first>
  <action>
    Execute the data reload in this exact order:

    1. Run the cleanup script live (with --delete-fy2025-operating included only if Task 1 resolved to delete-fy2025-operating): node scripts/cleanLACountyBudget.js [--delete-fy2025-operating]. Confirm console output shows the stale data_sources deleted and the FY2021-2024 operating/revenue rows deleted.

    2. Run the operating loader for the four county years: node scripts/loadLACountyOperating.js --fy 2021 --fy 2022 --fy 2023 --fy 2024. Confirm each FY reports a non-zero row count and a synced total. If any FY reports 0 rows fetched, STOP and report — do not silently continue.

    3. Run the revenue loader for the four county years: node scripts/loadLACountyRevenue.js --fy 2021 --fy 2022 --fy 2023 --fy 2024. Same non-zero check.

    Do NOT pass --fy 2025 to either loader — the county datasets (uctr-c2j8, emxv-k8xv) have no FY2025 data (verified, returns 0 rows). If a loader does not currently default to FY2021-2024, pass the explicit --fy flags as shown; do not modify the loader's hardcoded defaults unless they prevent the four-year run.

    4. Verify final state via the Supabase MCP execute_sql tool (mcp__supabase-local__execute_sql), running the Validation Architecture queries:
       - SELECT fiscal_year, total_budget FROM treasury.budgets WHERE municipality_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1' AND dataset_type='operating' ORDER BY fiscal_year — expect FY2021-2024 totals near $32B, $33B, $35B, $38B.
       - Same query for dataset_type='revenue' — expect near $32B, $34B, $36B, $39B.
       - SELECT count(*) FROM treasury.budgets WHERE municipality_id='f3db6f9f-...' AND data_source_id IS NULL AND dataset_type IN ('operating','revenue') — expect 0.
       - SELECT count(*) FROM treasury.budgets WHERE municipality_id='f3db6f9f-...' AND dataset_type='salaries' — confirm salaries row count is unchanged (5).
       - SELECT population, population_year FROM treasury.municipalities WHERE id='f3db6f9f-...' — expect 10014009, 2020.
  </action>
  <verify>
    <automated>node scripts/loadLACountyOperating.js --fy 2024 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Operating reload: SELECT total_budget per FY returns four rows for FY2021-2024, each within +/-15% of $32B, $33B, $35B, $38B respectively
    - Revenue reload: four rows for FY2021-2024, each within +/-15% of $32B, $34B, $36B, $39B respectively
    - SELECT count(*) FROM treasury.budgets WHERE municipality_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1' AND data_source_id IS NULL AND dataset_type IN ('operating','revenue') returns 0
    - SELECT count(*) FROM treasury.budgets WHERE municipality_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1' AND dataset_type='salaries' returns 5 (unchanged)
    - SELECT population, population_year returns 10014009 and 2020
    - If Task 1 chose delete-fy2025-operating: SELECT count(*) WHERE dataset_type='operating' AND fiscal_year=2025 returns 0
  </acceptance_criteria>
  <done>LA County operating and revenue FY2021-2024 reloaded from county datasets with correct totals, zero orphaned data_source_ids, population fixed, salaries untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Socrata API → loader | External CA State Controller data ingested into DB |
| loader/script → treasury.budgets | Privileged service-role writes/deletes to budget data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | Scoped delete on treasury.budgets | mitigate | Delete is filtered by municipality_id AND dataset_type IN ('operating','revenue') AND fiscal_year IN (2021-2024); salaries never in scope; --dry-run validates scope before live run |
| T-25-02 | Denial of Service | FY2025 silent zero-row reload | mitigate | Loaders never receive --fy 2025; Task 3 stops if any FY fetches 0 rows rather than leaving a gap |
| T-25-03 | Information Disclosure | SUPABASE_SERVICE_KEY in scripts | accept | Key read from env only, never hardcoded; consistent with all existing loaders; no new exposure |
</threat_model>

<verification>
- Operating and revenue FY2021-2024 totals match expected ranges (Validation Architecture queries in RESEARCH.md)
- Zero orphaned/null data_source_id on operating/revenue rows
- Salaries row count unchanged at 5
- Population = 10014009, population_year = 2020
</verification>

<success_criteria>
- LA County operating FY2021-2024 sourced from uctr-c2j8 with totals ~$32B/$33B/$35B/$38B
- LA County revenue FY2021-2024 sourced from emxv-k8xv with totals ~$32B/$34B/$36B/$39B
- FY2025 operating disposition matches Task 1 decision
- No null/orphaned data_source_id on operating/revenue rows; salaries untouched
- Population correct
</success_criteria>

<output>
Create `.planning/phases/25-la-county-data-completion-county-city-linking/25-01-SUMMARY.md` when done
</output>
