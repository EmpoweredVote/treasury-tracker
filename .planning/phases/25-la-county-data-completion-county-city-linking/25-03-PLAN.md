---
phase: 25-la-county-data-completion-county-city-linking
plan: 03
type: execute
wave: 2
depends_on: [25-02]
files_modified:
  - src/components/CitiesInCountyPanel.tsx
  - src/App.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "The /api/treasury/cities response includes county_id for LA city (verified before frontend work)"
    - "A city with county_id set shows a clickable county breadcrumb chip above its name, even at top level (no drill-down)"
    - "Clicking the county chip navigates to the county entity"
    - "A county entity page shows a 'Cities in [County]' panel below the budget with 'Available now' (clickable) and 'Coming soon' (non-clickable) sections"
    - "The LA County municipality id is never hardcoded in App.tsx — county is looked up from the municipalities list by county_id"
  artifacts:
    - path: "src/components/CitiesInCountyPanel.tsx"
      provides: "County roster panel with Available now / Coming soon sections"
      min_lines: 40
    - path: "src/App.tsx"
      provides: "countyEntity lookup, county breadcrumb prepend, panel injection, fixed render condition"
  key_links:
    - from: "src/App.tsx"
      to: "selectedEntity.county_id"
      via: "municipalities.find(m => m.id === selectedEntity.county_id)"
      pattern: "county_id"
    - from: "src/App.tsx"
      to: "src/components/CitiesInCountyPanel.tsx"
      via: "rendered when selectedEntity.entity_type === 'county'"
      pattern: "CitiesInCountyPanel"
    - from: "src/components/CitiesInCountyPanel.tsx"
      to: "municipalities county roster"
      via: "filter m.county_id === county.id && m.entity_type === 'city'"
      pattern: "county_id === county"
---

<objective>
Add bidirectional county-city navigation in the UI: a county breadcrumb chip on city pages and a "Cities in [County]" roster panel on county pages.

Purpose: Implements D-07 (county page = budget first, then cities panel), D-08 (two-section roster: Available now / Coming soon), and D-09 (clickable county chip on city pages). This is the first time a county entity has both real budget data and UI prominence in the app.

Output: city pages show "Los Angeles County →" clickable chip; county pages show the incorporated-cities roster below the budget. Depends on Plan 02's county_id data and type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-CONTEXT.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md
@src/types/budget.ts
@src/components/Breadcrumb.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify ev-accounts-api returns county_id (highest-risk dependency)</name>
  <what-built>Plan 02 added the county_id column and populated it. The frontend reads municipalities from the external Render-hosted ev-accounts-api at /api/treasury/cities. That service is NOT in this repo. If its SELECT is explicit (not SELECT *), county_id will be absent from the response and the breadcrumb will never render (selectedEntity.county_id will be undefined). This must be confirmed BEFORE writing frontend code.</what-built>
  <how-to-verify>
    1. Run: curl -s https://ev-accounts-api.onrender.com/api/treasury/cities | jq '.[] | select(.name=="Los Angeles") | .county_id'
    2. Expected: a UUID string (e.g. "f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1"), OR null for cities without a county. Either is acceptable — the field is PRESENT.
    3. FAILURE case: jq returns nothing / the key is absent (the object has no county_id key at all). This means the Render service SELECT is explicit and must be updated to include county_id before frontend work can proceed.
    4. If the field is absent: the ev-accounts-api is an external repo. Update its municipalities SELECT to include county_id and redeploy, then re-run step 1. Report to the user that the external service required a change and what was done. Do NOT proceed to Task 2 until the field appears in the API response.
  </how-to-verify>
  <resume-signal>Type "county_id present" once the API response includes the county_id field, or describe the blocker if the external service cannot be updated.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create CitiesInCountyPanel component</name>
  <files>src/components/CitiesInCountyPanel.tsx</files>
  <read_first>
    - src/components/CitiesInCountyPanel.tsx (confirm does not exist yet)
    - src/components/LinkedTransactionsPanel.tsx (closest analog — props interface, section layout, Tailwind tokens)
    - src/types/budget.ts (Municipality type with county_id and available_datasets shape)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (Pattern 5 + Code Examples CitiesInCountyPanel skeleton lines 470-538)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (CitiesInCountyPanel section lines 249-339: props interface, data split logic, chip styling, design tokens)
  </read_first>
  <action>
    Create src/components/CitiesInCountyPanel.tsx exporting a default React function component. Props interface CitiesInCountyPanelProps: { county: Municipality; municipalities: Municipality[]; onCityClick: (city: Municipality) => void }.

    Logic: cities = municipalities.filter(m => m.county_id === county.id && m.entity_type === 'city'). withData = cities where available_datasets.length > 0, sorted by name.localeCompare. withoutData = cities where available_datasets.length === 0, sorted by name.localeCompare. If cities.length === 0, return null (implements D-08 split + Pitfall 6 correct check).

    Render a card wrapper with the established Tailwind tokens (bg-white dark:bg-ev-gray-800, border border-[#E2EBEF] dark:border-ev-gray-700, rounded-xl, p-6, mt-8). Heading h2: "Cities in {county.name}".

    Two labeled sections (D-08):
    - "Available now ({withData.length})" section header (text-xs font-semibold uppercase tracking-wider text-ev-gray-500). Render each withData city as a clickable button calling onCityClick(city), styled with the interactive accent token (text-ev-muted-blue hover:bg-ev-muted-blue/10, border, rounded-lg, transition-colors duration-150). Only render this section if withData.length > 0.
    - "Coming soon ({withoutData.length})" section header (text-ev-gray-400). Render each withoutData city as a non-clickable span (no onClick), muted styling (text-ev-gray-400). Only render this section if withoutData.length > 0.

    Use the exact class tokens and chip markup shown in the PATTERNS.md CitiesInCountyPanel section. Do not invent new design tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/CitiesInCountyPanel.tsx exports a default component and imports Municipality from ../types/budget
    - File contains the filter m.county_id === county.id && m.entity_type === 'city'
    - File contains both literal section labels 'Available now' and 'Coming soon'
    - withData cities render as <button> with onClick={() => onCityClick(city)}; withoutData render as <span> with no onClick
    - File returns null when cities.length === 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>CitiesInCountyPanel component exists, splits cities into clickable Available now and non-clickable Coming soon sections, and type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire county breadcrumb + panel into App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current breadcrumbItems useMemo lines ~446-468; breadcrumb render condition line ~677; contextual help block lines ~916-924; handleEntityChange and municipalities/selectedEntity state)
    - src/components/CitiesInCountyPanel.tsx (the component created in Task 2 — its props)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (src/App.tsx modify section lines 343-448: countyEntity useMemo, modified breadcrumbItems, modified render condition, panel injection point)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (Pattern 4 breadcrumb prepend, Pitfall 5 render condition fix, anti-pattern: never hardcode county id)
  </read_first>
  <action>
    Make three edits to src/App.tsx:

    1. Add a countyEntity derived value via useMemo: when selectedEntity?.county_id is set, look up the county from the municipalities list — municipalities.find(m => m.id === selectedEntity.county_id) — else null. NEVER hardcode the LA County municipality id; always resolve via the county_id lookup (anti-pattern in RESEARCH.md).

    2. Modify the breadcrumbItems useMemo to prepend a county item when countyEntity is set: push { label: countyEntity.name, onClick: () => handleEntityChange(countyEntity) } as the first item, before the existing city/dataset/navigationPath items. Update the handleBreadcrumbClick index arithmetic so the existing dataset and navigationPath items still map to correct indices when the county item shifts everything by one (follow the modified useMemo in PATTERNS.md lines 389-420 exactly — it rebuilds the items array with items.length-relative indices). Add countyEntity and municipalities to the useMemo dependency array.

    3. Fix the breadcrumb render condition at line ~677: change `{breadcrumbItems.length > 2 && <Breadcrumb items={breadcrumbItems} />}` to `{(countyEntity != null || breadcrumbItems.length > 2) && <Breadcrumb items={breadcrumbItems} />}` so a county-linked city shows the county chip even with no drill-down (D-09 + Pitfall 5).

    4. Inject the CitiesInCountyPanel below the contextual help block (D-07 — budget first, then panel). Import CitiesInCountyPanel from './components/CitiesInCountyPanel'. Render it after the "How to explore" block when navigationPath.length === 0 && selectedEntity?.entity_type === 'county': <CitiesInCountyPanel county={selectedEntity} municipalities={municipalities} onCityClick={handleEntityChange} />.

    Use the existing handleEntityChange handler for navigation (do not create a new one). Confirm the existing state variable names (selectedEntity, municipalities, navigationPath, handleEntityChange, handleBreadcrumbClick) match before editing; adjust to actual names if they differ.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains a countyEntity definition using municipalities.find(m => m.id === selectedEntity?.county_id) — and does NOT contain the literal 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'
    - breadcrumbItems useMemo prepends countyEntity.name with onClick navigating via handleEntityChange(countyEntity)
    - Render condition is (countyEntity != null || breadcrumbItems.length > 2)
    - src/App.tsx imports and renders CitiesInCountyPanel guarded by selectedEntity?.entity_type === 'county' && navigationPath.length === 0
    - npx tsc --noEmit exits 0 and npm run build succeeds
  </acceptance_criteria>
  <done>App.tsx resolves county from county_id (no hardcoded id), shows the county breadcrumb chip on city pages including top-level, and renders CitiesInCountyPanel on county pages below the budget.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify bidirectional county-city navigation in the running app</name>
  <what-built>City pages now show a clickable county breadcrumb chip; county pages show a "Cities in [County]" panel with Available now / Coming soon sections. Backed by Plan 01 data and Plan 02 county_id links.</what-built>
  <how-to-verify>
    1. Run the dev server (npm run dev) and open the app.
    2. Select the "Los Angeles" city entity. Confirm a "Los Angeles County →" chip appears above the city name (visible even before any drill-down). Click it.
    3. Confirm it navigates to the Los Angeles County entity, showing the county's budget (Money In / Money Out tabs with accurate FY2021-2024 totals from Plan 01) and, below the budget, a "Cities in Los Angeles County" panel.
    4. In that panel, confirm "Available now" lists cities with budget data (e.g. Los Angeles, Long Beach, San Diego is NOT here — it is SD County) as clickable chips, and "Coming soon" lists the remaining LA County cities as non-clickable text. Click an "Available now" city and confirm navigation works.
    5. Confirm LA County's PlainLanguageSummary renders population (~10M residents) and per-capita figures correctly.
    6. Select San Francisco — confirm NO county chip appears (county_id is null per D-06).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the breadcrumb chip, the cities panel sections, or per-capita display.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ev-accounts-api → frontend | External API response shape consumed by App.tsx (untrusted field presence) |
| user click → entity navigation | Client-side navigation triggered by roster/breadcrumb clicks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Information Disclosure | county_id absent from API response | mitigate | Task 1 blocking checkpoint verifies the field exists before frontend work; undefined county_id falls through to no-chip (no crash) |
| T-25-08 | Tampering | Hardcoded county id in frontend | mitigate | County resolved by municipalities.find on county_id; acceptance criterion greps to confirm no hardcoded UUID in App.tsx |
| T-25-09 | Denial of Service | Empty/missing county roster | accept | CitiesInCountyPanel returns null when no cities match; no error surface |
</threat_model>

<verification>
- ev-accounts-api returns county_id (Task 1 blocking)
- County breadcrumb chip renders and navigates on city pages including top level
- CitiesInCountyPanel renders below budget on county pages with correct Available now / Coming soon split
- No hardcoded county id in App.tsx; tsc + build clean
</verification>

<success_criteria>
- City → county breadcrumb works for county-linked cities (D-09)
- County page shows budget then Cities panel with two labeled sections (D-07, D-08)
- SF shows no county chip (D-06)
- LA County per-capita display correct with ~10M population
</success_criteria>

<output>
Create `.planning/phases/25-la-county-data-completion-county-city-linking/25-03-SUMMARY.md` when done
</output>
