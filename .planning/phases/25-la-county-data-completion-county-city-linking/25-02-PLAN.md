---
phase: 25-la-county-data-completion-county-city-linking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedLACountyLinks.js
  - src/types/budget.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "treasury.municipalities has a county_id UUID column that self-references municipalities(id) with ON DELETE SET NULL"
    - "All 88 LA County incorporated cities have county_id set to the LA County municipality id"
    - "San Diego County, Sacramento County, and Alameda County municipality rows exist with entity_type='county'"
    - "San Diego city → SD County, Sacramento city → Sacramento County, Berkeley and Fremont → Alameda County county_id links exist"
    - "San Francisco's county_id remains null (no SF County row created)"
    - "The Municipality TypeScript type includes county_id"
  artifacts:
    - path: "scripts/seedLACountyLinks.js"
      provides: "Inserts 3 county rows + sets county_id for all CA city-county links (idempotent)"
    - path: "src/types/budget.ts"
      provides: "Municipality.county_id field"
      contains: "county_id"
  key_links:
    - from: "treasury.municipalities.county_id"
      to: "treasury.municipalities.id"
      via: "self-referential FK ON DELETE SET NULL"
      pattern: "REFERENCES treasury.municipalities"
    - from: "scripts/seedLACountyLinks.js"
      to: "treasury.municipalities"
      via: "bulk update county_id by name (88 cities) and by id (4 cities)"
      pattern: "county_id"
---

<objective>
Add a self-referential county_id FK to the municipalities schema and populate it for all LA County cities plus four cities in other CA counties, establishing the app's first county-city relational model.

Purpose: Implements D-04 (all 88 LA County cities linked), D-05 (San Diego/Sacramento/Alameda county rows + city links), and D-06 (SF stays null). Provides the data foundation the UI in Plan 03 consumes.

Output: county_id column exists on municipalities; 88 LA County cities linked; 3 new county rows seeded; 4 other CA cities linked; SF county_id null; Municipality type updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-CONTEXT.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md
@.planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add county_id column via MCP migration</name>
  <files>(DDL via mcp__supabase-local__apply_migration — no repo file)</files>
  <read_first>
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (Pattern 2 migration SQL, Pitfall 3 ON DELETE SET NULL rationale)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (Schema migration shared pattern, lines 537-548)
  </read_first>
  <action>
    Apply the county_id column migration using the mcp__supabase-local__apply_migration tool (project-mandated per MEMORY.md — never paste into the Supabase Dashboard, never use manual SQL). Name the migration "add_county_id_to_municipalities".

    The migration runs two statements:
    1. ALTER TABLE treasury.municipalities ADD COLUMN IF NOT EXISTS county_id UUID REFERENCES treasury.municipalities(id) ON DELETE SET NULL. Use ON DELETE SET NULL (NOT CASCADE) — per Pitfall 3, deleting a county must not delete its cities.
    2. CREATE INDEX IF NOT EXISTS idx_municipalities_county_id ON treasury.municipalities(county_id) WHERE county_id IS NOT NULL.

    After applying, verify with mcp__supabase-local__execute_sql: SELECT column_name FROM information_schema.columns WHERE table_schema='treasury' AND table_name='municipalities' AND column_name='county_id'.
  </action>
  <verify>
    <automated>echo "Verified via mcp__supabase-local__execute_sql column existence query in action"</automated>
  </verify>
  <acceptance_criteria>
    - SELECT column_name FROM information_schema.columns WHERE table_schema='treasury' AND table_name='municipalities' AND column_name='county_id' returns one row
    - The column's FK constraint uses ON DELETE SET NULL (confirm via pg_constraint or information_schema referential_constraints query — confdeltype 'n')
    - Index idx_municipalities_county_id exists (SELECT indexname FROM pg_indexes WHERE indexname='idx_municipalities_county_id' returns one row)
  </acceptance_criteria>
  <done>county_id UUID self-referential FK column with ON DELETE SET NULL and a partial index exist on treasury.municipalities.</done>
</task>

<task type="auto">
  <name>Task 2: Create seedLACountyLinks.js — county rows + all county_id links</name>
  <files>scripts/seedLACountyLinks.js</files>
  <read_first>
    - scripts/seedLACountyLinks.js (confirm does not exist yet before writing)
    - scripts/seedCollinCountyMunicipalities.js (diff + batch insert pattern lines 52-108, error guard lines 57-60)
    - scripts/seedCaliforniaCities.js (idempotent select-before-insert upsertMunicipality pattern lines 47-80)
    - scripts/loadLACountyOperating.js (Supabase client init lines 20-26)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (County-City Linking Scope section: full 88-city name list, the 4 other-county city UUIDs, the 3 new county rows; county_id seeding bulk UPDATE code example)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (seedLACountyLinks.js section lines 143-245: bulk update by name pattern, idempotent insert)
  </read_first>
  <action>
    Create scripts/seedLACountyLinks.js as a Node.js ESM script. Use the Supabase client init pattern (SUPABASE_URL default 'https://kxsdzaojfaibhuzmclfq.supabase.co', SUPABASE_SERVICE_KEY env, exit 1 if missing). Hardcode LA_COUNTY_ID = 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'. Support a --dry-run flag (parseArgs strict:false) that logs intended operations without writing. Guard every Supabase call with an error check that process.exit(1) on error and console.log of affected counts.

    Perform three operations in order:

    1. Insert (idempotent) three county municipality rows with entity_type='county', state='CA', population=0, population_year=null: 'San Diego County', 'Sacramento County', 'Alameda County'. Use the diff-by-name pattern from seedCollinCountyMunicipalities.js (select existing CA municipalities by name lowercased, filter to only missing rows, insert the missing set, .select('id, name')). Capture the returned ids for these three counties into a name→id map (SD_COUNTY_ID, SAC_COUNTY_ID, ALAMEDA_COUNTY_ID). If all three already exist, re-select their ids by name so step 3 still has the UUIDs.

    2. Set county_id = LA_COUNTY_ID for all 88 LA County cities by name: supabase.schema('treasury').from('municipalities').update({ county_id: LA_COUNTY_ID }).eq('state', 'CA').in('name', LA_COUNTY_CITY_NAMES). Define LA_COUNTY_CITY_NAMES as the exact 88-name array copied from RESEARCH.md County-City Linking Scope (Agoura Hills … Whittier). This implements D-04 — note 'Los Angeles' city is in this list and gets county_id set to LA County.

    3. Set county_id for the four other-county cities by their known UUIDs (implements D-05):
       - San Diego city '1ee32637-1f2e-4a91-a7c1-1cd976dd4aa2' → SD_COUNTY_ID
       - Sacramento city '9722596e-1102-4aca-8758-c32fc0c1731d' → SAC_COUNTY_ID
       - Berkeley '61236aa6-7845-49a2-a49a-78ef0050b395' → ALAMEDA_COUNTY_ID
       - Fremont 'eb7e50b1-eab5-4a0d-a9ce-a345109a13f9' → ALAMEDA_COUNTY_ID
       Each via .update({ county_id: <county uuid> }).eq('id', <city uuid>).

    Do NOT set county_id for San Francisco and do NOT create an SF County row (D-06 — SF is a consolidated city-county). Wrap main() with .catch(err => { console.error('Fatal:', err); process.exit(1); }).
  </action>
  <verify>
    <automated>node scripts/seedLACountyLinks.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seedLACountyLinks.js contains the literal 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'
    - The LA_COUNTY_CITY_NAMES array contains exactly 88 entries including 'Los Angeles', 'Long Beach', 'Whittier', 'Agoura Hills'
    - The file contains all four other-county city UUIDs: '1ee32637-1f2e-4a91-a7c1-1cd976dd4aa2', '9722596e-1102-4aca-8758-c32fc0c1731d', '61236aa6-7845-49a2-a49a-78ef0050b395', 'eb7e50b1-eab5-4a0d-a9ce-a345109a13f9'
    - The file contains the three county names 'San Diego County', 'Sacramento County', 'Alameda County' with entity_type 'county'
    - grep confirms 'San Francisco' does NOT appear in any county_id update or county insert
    - node scripts/seedLACountyLinks.js --dry-run exits 0
  </acceptance_criteria>
  <done>Seeder script exists, idempotently inserts 3 county rows, links 88 LA cities + 4 other-county cities, leaves SF untouched, and runs clean in --dry-run.</done>
</task>

<task type="auto">
  <name>Task 3: Run seeder, add county_id to Municipality type, verify links</name>
  <files>src/types/budget.ts, scripts/seedLACountyLinks.js</files>
  <read_first>
    - src/types/budget.ts (lines 107-119 current Municipality interface)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-PATTERNS.md (src/types/budget.ts modify section lines 452-490 — exact field placement)
    - .planning/phases/25-la-county-data-completion-county-city-linking/25-RESEARCH.md (Validation Architecture queries for county_id verification)
  </read_first>
  <action>
    1. Run the seeder live: node scripts/seedLACountyLinks.js. Confirm console output reports 3 counties inserted (or already present), 88 LA cities updated, and 4 other-county cities updated.

    2. Add a county_id field to the Municipality interface in src/types/budget.ts. Insert the line `county_id?: string | null;` immediately after the `hero_image_url?: string | null;` line and before the `available_datasets` array field. Comment it as a UUID reference to the parent county municipality row. Do not change any other field.

    3. Verify links via mcp__supabase-local__execute_sql:
       - SELECT count(*) FROM treasury.municipalities WHERE county_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1' — expect 88.
       - SELECT name, county_id FROM treasury.municipalities WHERE id IN ('1ee32637-1f2e-4a91-a7c1-1cd976dd4aa2','9722596e-1102-4aca-8758-c32fc0c1731d','61236aa6-7845-49a2-a49a-78ef0050b395','eb7e50b1-eab5-4a0d-a9ce-a345109a13f9') — all four have non-null county_id.
       - SELECT county_id FROM treasury.municipalities WHERE name='San Francisco' AND state='CA' — expect null.
       - SELECT count(*) FROM treasury.municipalities WHERE state='CA' AND entity_type='county' AND name IN ('San Diego County','Sacramento County','Alameda County') — expect 3.

    4. Run the type check: npx tsc --noEmit (or the project's typecheck script) to confirm the type change compiles.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/budget.ts contains `county_id?: string | null;` within the Municipality interface, positioned after hero_image_url and before available_datasets
    - npx tsc --noEmit exits 0
    - SELECT count(*) FROM treasury.municipalities WHERE county_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1' returns 88
    - The four other-county cities all return non-null county_id matching their respective new county rows
    - SELECT county_id FROM treasury.municipalities WHERE name='San Francisco' AND state='CA' returns null
    - SELECT count(*) WHERE entity_type='county' AND name IN ('San Diego County','Sacramento County','Alameda County') returns 3
  </acceptance_criteria>
  <done>Seeder run; 88 LA cities + 4 other-county cities linked; 3 county rows present; SF null; Municipality type has county_id and compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration tool → schema | DDL altering municipalities table structure |
| seeder script → treasury.municipalities | Privileged service-role bulk updates of county_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | Self-referential FK delete behavior | mitigate | ON DELETE SET NULL ensures deleting a county nulls children rather than cascading deletes of 88 cities |
| T-25-05 | Tampering | Bulk county_id update by name | mitigate | Update scoped to .eq('state','CA').in('name', <explicit 88-name list>); --dry-run verifies scope; SF explicitly excluded |
| T-25-06 | Repudiation | Idempotent county inserts | accept | Diff-by-name prevents duplicate county rows on re-run; low risk, no audit requirement |
</threat_model>

<verification>
- county_id column exists with ON DELETE SET NULL and partial index
- 88 LA County cities linked; 4 other-county cities linked; SF null; 3 county rows present
- Municipality type includes county_id and project type-checks clean
</verification>

<success_criteria>
- Schema migration applied via MCP tool with correct FK delete behavior
- All county_id links match D-04, D-05, D-06
- TypeScript Municipality type updated and compiles
</success_criteria>

<output>
Create `.planning/phases/25-la-county-data-completion-county-city-linking/25-02-SUMMARY.md` when done
</output>
