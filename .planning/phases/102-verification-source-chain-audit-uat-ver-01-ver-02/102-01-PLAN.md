---
phase: 102-verification-source-chain-audit-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase102-rederive.mjs
autonomous: true
requirements: [VER-01]

must_haves:
  truths:
    - "Each of the 4 upgraded states (CA/TX/NY/FL) has its newest displayed FY AND its oldest window-bookend FY independently re-derived from its own ACFR PDF — a second read that does NOT import or trust the loader's extraction"
    - "For each re-derived statement, the ACFR printed 'Total revenues' (revenue dataset) and 'Total expenditures' (operating dataset), after the documented unit multiplier (NY ×1,000,000; CA/TX/FL ×1,000), tie to the value currently stored in treasury.budgets for that (municipality_id, fiscal_year, dataset) — exact, with a $10M rounding fallback band"
    - "Any tie miss beyond the $10M band is reported as a FAIL with the state/FY/dataset/expected/stored/delta, not silently passed (feeds 102-02 gap-closure or escalation)"
    - "The re-derivation is loader-independent: scripts/verify-phase102-rederive.mjs does NOT import scripts/process{CA,TX,NY,FL}*.js — it re-extracts the printed total via its own pdftotext -table pass over the ACFR PDF"
  artifacts:
    - path: "scripts/verify-phase102-rederive.mjs"
      provides: "Loader-independent re-derivation harness: extracts the printed GF Total revenues + Total expenditures for the 8 target (state,FY) statements straight from each ACFR PDF, applies the unit multiplier, queries the stored treasury.budgets total, and prints a comparison table with PASS/FAIL per check"
      contains: "verify-phase102-rederive"
  key_links:
    - from: "scripts/verify-phase102-rederive.mjs"
      to: "treasury.budgets stored totals"
      via: "query the stored node total for each (municipality_id, fiscal_year, dataset) and diff against the independently re-read ACFR printed total"
      pattern: "treasury.budgets"
---

<objective>
Deliver the independent-reconciliation half of VER-01. For each of the 4 upgraded states (CA/TX/NY/FL), independently re-derive — from the state's own ACFR PDF, with a code path that does NOT trust the loader's extraction or its stored self-report — the General-Fund printed totals for the newest displayed FY and the oldest window-bookend FY, and confirm they tie to what is currently stored in `treasury.budgets`.

Purpose: prove the loaded ACFR figures are real and correctly scaled, sourced from the audited published statement rather than the loader's own claim. This is the "reconciled independently from its own ACFR (not loader self-report)" clause of VER-01.
Output: `scripts/verify-phase102-rederive.mjs` + a PASS/FAIL comparison table captured in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-CONTEXT.md

<interfaces>
<!-- Exact targets + shapes — do not re-explore. -->
- **Re-derivation targets (8 statements = newest displayed FY + oldest window-bookend, per state):**
  - CA: FY2025 (newest) + FY2020 (bookend). Units ×1,000. Anchors: FY2020 Total revenues $155,923,876K; FY2025 $221,591,201K.
  - TX: FY2024 + FY2015. Units ×1,000. Fund column = "General Revenue Fund". Anchors: FY2015 $95,574,830K; FY2024 $161,416,562K.
  - NY: FY2024 + FY2015. **Units ×1,000,000 (millions)**. Anchors: FY2024 Total revenues $93,894M.
  - FL: FY2024 + FY2022. Units ×1,000. Anchors: FY2022 $57,241,428K; FY2024 $59,810,603K.
- **Per-year ACFR URLs + GF column + page numbers** are recorded in `.planning/phases/98-recon-…/98-ACFR-SOURCES.md` and mirrored in each loader's `SOURCES` map. Use those URLs/pages, but re-read the PDF in a fresh pdftotext -table pass — do NOT import the loader.
- **Stored-total query:** `treasury.budgets` holds one tree per (municipality_id, fiscal_year, dataset_type ∈ {operating, revenue}); the node total = the sum of top-level category amounts (the same total the app shows). The 4 ACFR municipality_ids: CA `e1007bf5-bac9-4b1c-878e-f6834885f850`, NY `1a7f871c-7f2e-4786-9c55-5ab3409716f4`, FL `adb19ea0-de7c-4cd5-9445-cbf2108a8a1a`; TX = the single `entity_type='state'` node named Texas (look up by name).
- **DB client + .env pattern:** existing `.mjs` scripts (e.g. `scripts/loadStateGF.mjs`, `scripts/verify-phase56.mjs`) source `.env` for the Supabase service credentials and run a query/RPC. Match whichever client they use; read one before writing.
- **Extraction:** `pdftotext -table` (NOT `-layout`). The printed control totals are "Total revenues" (revenue) and "Total expenditures" (operating) on each year's Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances*, GF column.
- **P2-clamp note:** clamped negative categories do NOT change the printed control Total — the re-derivation ties to the printed Total revenues/expenditures, so clamped years still tie.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the loader-independent re-derivation harness</name>
  <files>scripts/verify-phase102-rederive.mjs</files>
  <read_first>
    - scripts/verify-phase56.mjs (or another verify-phaseNN.mjs — the established phase-scoped DB-query script shape: .env load, client, report table)
    - scripts/loadStateGF.mjs (the Supabase client + .env pattern + how a stored node total is computed/keyed — for the query side ONLY; do NOT copy its extraction)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md (per-year ACFR URLs, GF column label, page numbers for the 8 targets)
    - .planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-CONTEXT.md (D-01 depth, D-02 loader-independence, D-03 exact/$10M tolerance, D-04 newest+bookend anchors; D-12 verify-phaseNN.mjs idiom + $0/no-AI)
  </read_first>
  <action>
    Create `scripts/verify-phase102-rederive.mjs`. Hardcode the 8 target rows as a config array: `{ state, municipality_id|name, fiscal_year, url, gfColLabel, units }` for CA FY2025+FY2020, TX FY2024+FY2015, NY FY2024+FY2015, FL FY2024+FY2022 (URLs/columns/pages from 98-ACFR-SOURCES.md; NY units=1_000_000, others=1_000). For each target: (1) ensure the ACFR PDF is present (reuse `_acfr-tmp/{state}/` if cached, else download from the durable URL with the CA soft-404 guard — verify Content-Type application/pdf or size > ~1MB, never trust HTTP 200 alone); (2) run a FRESH `pdftotext -table` extraction over the GF statement page range and parse out the printed "Total revenues" and "Total expenditures" figures from the GF column — this code path must NOT import or call any `scripts/process*.js` loader; (3) multiply by `units` to get dollars; (4) query `treasury.budgets` for the stored node total of that (municipality_id, fiscal_year, dataset) for dataset ∈ {revenue (←Total revenues), operating (←Total expenditures)}; (5) compute delta and mark PASS if `abs(delta) === 0` (exact) or `abs(delta) <= 10_000_000` ($10M rounding fallback), else FAIL. Print a comparison table (state | FY | dataset | acfr_printed | stored | delta | verdict) and `process.exit(failures > 0 ? 2 : 0)`. $0 spend — pdftotext + DB read only, no AI calls.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node scripts/verify-phase102-rederive.mjs 2>&1 | tail -40</automated>
  </verify>
  <done>scripts/verify-phase102-rederive.mjs exists, imports NO process*.js loader, re-reads each of the 8 ACFR statements via its own pdftotext -table pass, compares the printed Total revenues/Total expenditures (×units) to the stored treasury.budgets total, and prints a PASS/FAIL table. The script runs to completion.</done>
</task>

<task type="auto">
  <name>Task 2: Run the re-derivation and record the reconciliation evidence</name>
  <files>scripts/verify-phase102-rederive.mjs</files>
  <read_first>
    - scripts/verify-phase102-rederive.mjs (the harness from Task 1)
  </read_first>
  <action>
    Run `node scripts/verify-phase102-rederive.mjs` and capture the full comparison table. For every check, record acfr_printed / stored / delta / verdict. If all 16 checks (8 statements × {revenue, operating}) PASS within tolerance, the independent reconciliation half of VER-01 is satisfied. If any check FAILs beyond $10M: do NOT mutate data here — record the failing (state, FY, dataset, expected, stored, delta) and flag it for 102-02 (if it is an integrity/labelling/residue defect fixable inline) or for escalation (if it implies a wrong extraction needing a re-load). Note that exact ties are expected for the recon-confirmed bookends; clamped-negative years still tie on the printed control Total.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node scripts/verify-phase102-rederive.mjs > /tmp/rederive102.txt 2>&1; echo "exit=$?"; tail -40 /tmp/rederive102.txt</automated>
  </verify>
  <done>The re-derivation has been run; the SUMMARY contains the full comparison table with a per-check verdict and an overall reconciliation result. Any failure is documented with expected/stored/delta and routed (102-02 inline fix vs escalation), not silently passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ACFR PDF (remote download) → extraction | untrusted file fetch; soft-404 HTML can masquerade as a PDF (CA SCO server) |
| extraction/DB read → report | read-only; this plan makes NO writes to treasury data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01-01 | Tampering | soft-404 HTML returned as a "PDF" → garbage extraction read as a tie/mismatch | mitigate | verify Content-Type application/pdf or size > ~1MB before extracting; reuse the recon's cached PDFs where present |
| T-102-01-02 | Information disclosure | none — all ACFR data is public audited finance data | accept | public-record only |
| T-102-01-SC | Tampering | npm dependency changes | accept | no new deps; pdftotext + existing DB client only; $0/no-AI |
</threat_model>

<verification>
- `node scripts/verify-phase102-rederive.mjs` runs and prints the 16-check comparison table; exit 0 iff all checks tie within tolerance.
- The script imports no `process*.js` loader (loader-independence assertion — grep the file).
- Each PASS row's stored value equals the independently re-read ACFR printed Total (×units) exactly, or within the $10M rounding band.
- Any FAIL is captured with expected/stored/delta and routed to 102-02 or escalation.
</verification>

<success_criteria>
VER-01 (independent-reconciliation half): each of CA/TX/NY/FL has its newest displayed FY + oldest bookend FY independently re-derived from its own ACFR and tied to the stored treasury.budgets total for both revenue and operating, via a loader-independent harness, with all ties within tolerance (or failures explicitly routed).
</success_criteria>

<output>
Create `.planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-01-SUMMARY.md` — record the harness, the full 16-check comparison table (acfr_printed/stored/delta/verdict), the overall reconciliation result, and any routed failures.
</output>
