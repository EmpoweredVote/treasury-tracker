---
phase: 23-or-all-funds-consistency-requirements-extraction-portland-gr
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extractTroutdale.py
  - scripts/processTroutdale.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "extractTroutdale.py --mode requirements reads the All Funds Combined REQUIREMENTS section and returns the 7 expenditure categories, skipping the $0 reserve row and the TOTAL REQUIREMENTS sum row"
    - "processTroutdale.js --requirements --dry-run prints FY2019-FY2026 totals; FY2026 ~$81M"
    - "Troutdale all_funds_requirements rows are loaded for FY2019-FY2026"
    - "treasury.data_sources has dataset_type='all_funds_requirements' rows for each Troutdale fiscal year"
  artifacts:
    - path: "scripts/extractTroutdale.py"
      provides: "extract_requirements() with RESOURCES->REQUIREMENTS section gate flip"
      contains: "def extract_requirements"
    - path: "scripts/processTroutdale.js"
      provides: "--requirements flag with datasetType=all_funds_requirements"
      contains: "all_funds_requirements"
  key_links:
    - from: "scripts/processTroutdale.js"
      to: "scripts/extractTroutdale.py"
      via: "extractPDF passes --mode requirements"
      pattern: "requirements"
    - from: "scripts/processTroutdale.js"
      to: "treasury_sync_budget_tree"
      via: "RPC with p_dataset_type='all_funds_requirements'"
      pattern: "all_funds_requirements"
---

<objective>
Add All Funds Requirements extraction to the Troutdale pipeline. D-05 is resolved YES — research confirmed Troutdale's "All Funds Combined" page has a REQUIREMENTS section across all 8 PDFs (FY2019–FY2026). Extraction is a near-copy of Troutdale's existing extract_revenue() with the section gate flipped from RESOURCES to REQUIREMENTS. Store under `dataset_type='all_funds_requirements'` for FY2019–FY2026 (D-06).

Purpose: Align Troutdale's Budget tab scope (General Fund operating ~$17M) with the all-funds picture (~$81M FY2026), so the gap-explanation label and balanced totals apply to all three OR cities.
Output: extract_requirements() in extractTroutdale.py, --requirements mode in processTroutdale.js, and Troutdale all_funds_requirements DB rows for FY2019–FY2026.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md
@.planning/phases/22-troutdale-or-budget-load/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extract_requirements() to extractTroutdale.py</name>
  <files>scripts/extractTroutdale.py</files>
  <read_first>
    - scripts/extractTroutdale.py — read the full file: extract_revenue() (the sibling being mirrored), the page guard (`'ALL FUNDS COMBINED'`/`'FUND SUMMARY'`, skip `'ACCOUNT 01.00'`), the Troutdale-specific token split that handles standalone `$` tokens, and the __main__ argparse block
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "scripts/extractTroutdale.py — add extract_requirements()" section: exact page-guard, section-gate (RESOURCES->REQUIREMENTS), Troutdale token-split, skip set, and __main__ patterns
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "Troutdale Requirements Section Structure" (7 categories + the $0 RESERVE row to skip + TOTAL REQUIREMENTS sum row; FY2026 total $81,181,239; FY2019 total $44,892,732; structure identical FY2019-FY2026)
  </read_first>
  <action>
Add `extract_requirements(pdf_path)` mirroring `extract_revenue()`. Reuse the identical page guard verbatim: skip pages without `'ALL FUNDS COMBINED'` and `'FUND SUMMARY'`, and skip the General Fund page guarded by `'ACCOUNT 01.00' in text`. Reuse the Troutdale-specific token-split that treats a standalone `'$'` and `'-'` as a number-section start (`clean_t = re.sub(r'[\$,]', '', t)`; `in_nums` triggers on `re.match(r'^\d+$', clean_t) or t == '-' or t == '$'`).

Flip the section gate: use `in_requirements` (not `in_resources`). Set `in_requirements = True` on the line `s == 'REQUIREMENTS'`, set it `False` on `s == 'RESOURCES'`. Skip lines while `not in_requirements`.

Skip the non-category rows: `REQUIREMENTS_SKIP = {'TOTAL REQUIREMENTS', 'RESERVE FOR FUTURE EXPENDITURE'}` (the latter is a $0 row). The surviving rows are the 7 expenditure categories: PERSONNEL SERVICES, MATERIALS & SERVICES, CAPITAL OUTLAY, DEBT SERVICE, TRANSFERS TO OTHER FUNDS, CONTINGENCY, UNAPPROPRIATED. Additionally skip any row whose parsed adopted amount is `<= 0` (covers a $0 RESERVE row even if the name varies).

Return the same dict shape: `{ 'category', 'adopted_amount', 'fiscal_year', 'page_num' }`, with the Adopted column being the last numeric token (same column position extract_revenue uses).

Extend `__main__` argparse: add `'requirements'` to `--mode` choices and dispatch `args.mode == 'requirements'` to `extract_requirements(args.pdf_path)`.
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; python -c "import ast; ast.parse(open('scripts/extractTroutdale.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/extractTroutdale.py contains `def extract_requirements(`
    - scripts/extractTroutdale.py contains the skip members `'TOTAL REQUIREMENTS'` and `'RESERVE FOR FUTURE EXPENDITURE'`
    - scripts/extractTroutdale.py `--mode` argparse choices include `'requirements'`
    - `python -c "import ast; ast.parse(open('scripts/extractTroutdale.py').read())"` exits 0
    - `python scripts/extractTroutdale.py docs/Troutdale/fy2025-26.pdf --mode requirements` returns 7 category objects (PERSONNEL SERVICES, MATERIALS & SERVICES, CAPITAL OUTLAY, DEBT SERVICE, TRANSFERS TO OTHER FUNDS, CONTINGENCY, UNAPPROPRIATED), summing within $1M of $81,181,239, with no row named TOTAL REQUIREMENTS
  </acceptance_criteria>
  <done>extract_requirements() returns Troutdale's 7 expenditure categories for FY2026 summing to ~$81M, skipping the $0 reserve and the sum row.</done>
</task>

<task type="auto">
  <name>Task 2: Add --requirements mode to processTroutdale.js and live-load FY2019-FY2026</name>
  <files>scripts/processTroutdale.js</files>
  <read_first>
    - scripts/processTroutdale.js — read the full file: main() parseArgs, extractPDF() (spawnSync arg push), processPDF() mode-label/datasetType block, buildRevenueTree(), upsertDataSource() label, SANITY_MAX guard (gated to mode==='operating', $30M operating cap)
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "scripts/processTroutdale.js — add --mode requirements" section: CLI parse, extractPDF arg push, SANITY_MAX note (requirements ~$81M exceeds $30M cap but is excluded), buildRevenueTree reuse, upsertDataSource label
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "SANITY_MAX for Requirements Mode" table and "Idempotency"
  </read_first>
  <action>
Mirror the Gresham loader changes (same pattern). Extend main()'s parseArgs `options` to add `requirements: { type: 'boolean', default: false }` and derive `const mode = opts.requirements ? 'requirements' : opts.revenue ? 'revenue' : 'operating'`.

In `extractPDF()`, after the existing `if (mode === 'revenue') args.push('--mode', 'revenue');`, add `if (mode === 'requirements') args.push('--mode', 'requirements');`.

In `processPDF()`, add `const isRequirements = mode === 'requirements'`, set `datasetType = mode === 'requirements' ? 'all_funds_requirements' : mode === 'revenue' ? 'revenue' : 'operating'`, set unitLabel/typeLabel for requirements ('categories' / 'All Funds Requirements'), and use `buildRevenueTree(fyRows)` when `isRevenue || isRequirements` (Troutdale requirements rows have the same `{ category, adopted_amount }` shape).

Do NOT change SANITY_MAX — it is gated to `mode === 'operating'` so the ~$81M requirements total skips the $30M operating cap, matching the revenue precedent.

In `upsertDataSource()`, extend the label: `datasetType === 'all_funds_requirements' ? 'All Funds Requirements'`. The select filter already includes `.eq('dataset_type', datasetType)`, isolating all_funds_requirements rows.

Dry-run all 8 FYs (`node scripts/processTroutdale.js --requirements --dry-run`); confirm FY2026 ~$81,181,239 and FY2019 ~$44,892,732, each with the 7 categories. Then live-load (`node scripts/processTroutdale.js --requirements`) for FY2019–FY2026.
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; node scripts/processTroutdale.js --requirements --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/processTroutdale.js contains `requirements:` in parseArgs options
    - scripts/processTroutdale.js contains the string `all_funds_requirements`
    - scripts/processTroutdale.js contains the label `'All Funds Requirements'`
    - `node scripts/processTroutdale.js --requirements --dry-run` exits 0 and prints 8 fiscal years (2019–2026); FY2026 total within $1M of $81,181,239
    - After live load, `treasury.data_sources` has `dataset_type='all_funds_requirements'` rows for Troutdale fiscal_years 2019 through 2026
    - After live load, the Troutdale FY2026 all_funds_requirements total in treasury.budgets is within $1M of $81,181,239
    - Re-running the live load does not duplicate rows (idempotency)
  </acceptance_criteria>
  <done>processTroutdale.js --requirements loads Troutdale all_funds_requirements rows for FY2019–FY2026; dry-run totals match the PDF; operating SANITY_MAX cap does not fire.</done>
</task>

</tasks>

<verification>
- `python scripts/extractTroutdale.py docs/Troutdale/fy2025-26.pdf --mode requirements` returns 7 categories summing ~$81M, no sum/reserve rows
- `node scripts/processTroutdale.js --requirements --dry-run` prints FY2019–FY2026 totals
- Live load creates `dataset_type='all_funds_requirements'` rows for all 8 Troutdale FYs
- Operating/revenue Troutdale rows untouched
</verification>

<success_criteria>
Troutdale All Funds Requirements data is extracted and loaded for FY2019–FY2026 under `dataset_type='all_funds_requirements'`, FY2026 ~$81M, ready for the frontend in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-03-SUMMARY.md` when done.
</output>
