---
phase: 23-or-all-funds-consistency-requirements-extraction-portland-gr
plan: 04
type: execute
wave: 2
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - src/types/budget.ts
  - src/App.tsx
  - src/components/dashboard/PlainLanguageSummary.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "The Budget (Money Out) tab headline total for an OR city shows the all_funds_requirements total (~$897M Gresham) instead of the departmental operating total (~$330M) (D-02)"
    - "A gap-explanation label appears below the headline stating the all-funds total, the departmental subset, and the remainder (D-03)"
    - "The departmental icicle/category breakdown is unchanged for operating mode (D-02)"
    - "TX and CA cities (no all_funds_requirements rows) show unchanged behavior — no gap label, operating headline (D-04 backward-compatibility)"
    - "all_funds_requirements does NOT appear as a selectable dataset tab/card"
    - "Switching fiscal years keeps Budget and Money In totals on matching All Funds scope (D-06)"
  artifacts:
    - path: "src/types/budget.ts"
      provides: "dataset_type union including 'all_funds_requirements'"
      contains: "all_funds_requirements"
    - path: "src/App.tsx"
      provides: "hasAllFundsRequirements detection, allFundsRequirementsData state + load, prop pass, operatingTotal override, tab-filter exclusion"
      contains: "allFundsRequirementsData"
    - path: "src/components/dashboard/PlainLanguageSummary.tsx"
      provides: "headline override + gap-explanation label gated on allFundsRequirementsData"
      contains: "allFundsRequirementsData"
  key_links:
    - from: "src/App.tsx"
      to: "loadBudgetData(..., 'all_funds_requirements')"
      via: "Promise.all fourth promise gated on hasAllFundsRequirements"
      pattern: "all_funds_requirements"
    - from: "src/App.tsx"
      to: "PlainLanguageSummary"
      via: "allFundsRequirementsData prop"
      pattern: "allFundsRequirementsData"
    - from: "src/components/dashboard/PlainLanguageSummary.tsx"
      to: "headline total"
      via: "allFundsRequirementsData?.metadata.totalBudget ?? operatingData.metadata.totalBudget"
      pattern: "allFundsRequirementsData\\?\\.metadata"
---

<objective>
Wire the All Funds Requirements data (loaded in Plans 01–03) into the frontend so the Budget (Money Out) tab shows the all-funds total as the headline, with a gap-explanation label, while keeping the departmental icicle breakdown as a drill-down. The change is data-driven and generic (D-04) — any city/year with `all_funds_requirements` rows gets the new behavior; TX/CA cities are unchanged.

Purpose: Complete the end-to-end fix (D-01) — citizens no longer see an apparent $180M windfall caused by mismatched Budget/Money In scopes. Both tabs now tell a coherent all-funds story.
Output: Updated dataset_type union, App.tsx data load + prop wiring + operatingTotal override + tab-filter exclusion, PlainLanguageSummary headline override + gap label, verified live in the app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md
@src/types/budget.ts
@src/App.tsx
@src/components/dashboard/PlainLanguageSummary.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dataset_type union and add allFundsRequirementsData load + wiring in App.tsx</name>
  <files>src/types/budget.ts, src/App.tsx</files>
  <read_first>
    - src/types/budget.ts — read around line 117, the `available_datasets` array type with `dataset_type: 'operating' | 'revenue' | 'salaries'`
    - src/App.tsx — read: state declarations (lines ~108-111, the operatingBudgetData/revenueData/salariesData useState calls), availableDatasetTypes useMemo (lines ~146-153), the "info cards" useEffect with hasOperating/hasRevenue/hasSalaries + Promise.all (lines ~265-296), and the DatasetTabs render (lines ~726-733, operatingTotal prop) and the PlainLanguageSummary render (~line 711)
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "src/types/budget.ts" and "src/App.tsx" sections: exact union change, detection block, Promise.all fourth promise, availableDatasetTypes filter, operatingTotal override, PlainLanguageSummary prop pass
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "Frontend Change Architecture" items 1, 2, 3 and "Pitfall 4: all_funds_requirements Appearing as a Selectable Tab"
  </read_first>
  <action>
In `src/types/budget.ts`, extend the `dataset_type` union to `'operating' | 'revenue' | 'salaries' | 'all_funds_requirements'`.

In `src/App.tsx`:
1. Add a state hook `const [allFundsRequirementsData, setAllFundsRequirementsData] = useState<BudgetData | null>(null);` alongside the existing operatingBudgetData/revenueData/salariesData hooks.
2. In the info-cards useEffect, after `hasSalaries`, add `const hasAllFundsRequirements = entityDatasets.some(d => d.dataset_type === 'all_funds_requirements');`. Add a fourth entry to the `promises` array: `hasAllFundsRequirements ? loadBudgetData(yearNum, selectedEntity.name, selectedEntity.state, 'all_funds_requirements') : Promise.resolve(null)`. Update the `Promise.all(...).then(([operating, revenue, salaries, allFundsReqs]) => {...})` destructure to call `setAllFundsRequirementsData(allFundsReqs)`, and in the `.catch` add `setAllFundsRequirementsData(null)`.
3. In the `availableDatasetTypes` useMemo, add `.filter(t => t !== 'all_funds_requirements')` to the mapped dataset types so all_funds_requirements is never rendered as a selectable tab/card (Pitfall 4). Leave the no-entity default array as-is.
4. In the `DatasetTabs` render, change the `operatingTotal` prop to prefer the all-funds total: `operatingTotal={allFundsRequirementsData?.metadata.totalBudget ?? operatingBudgetData?.metadata.totalBudget}`. This makes the Money Out card show the all-funds figure when available, falling back to operating for TX/CA cities.
5. In the `PlainLanguageSummary` render, pass a new prop `allFundsRequirementsData={allFundsRequirementsData}`.

Do NOT change the main `budgetData` load effect or `setActiveDataset` logic — `all_funds_requirements` is not a selectable active tab. The local `DatasetType` union and the `datasetParam` allowlist (`['operating', 'revenue', 'salaries']`) stay unchanged because all_funds_requirements is never the active dataset.
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/budget.ts dataset_type union includes `'all_funds_requirements'`
    - src/App.tsx contains `const [allFundsRequirementsData, setAllFundsRequirementsData]`
    - src/App.tsx contains `hasAllFundsRequirements`
    - src/App.tsx Promise.all destructure includes a fourth value and calls `setAllFundsRequirementsData`
    - src/App.tsx availableDatasetTypes useMemo contains `t !== 'all_funds_requirements'`
    - src/App.tsx DatasetTabs operatingTotal prop reads `allFundsRequirementsData?.metadata.totalBudget ?? operatingBudgetData?.metadata.totalBudget`
    - src/App.tsx PlainLanguageSummary render passes `allFundsRequirementsData={allFundsRequirementsData}`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>App.tsx loads all_funds_requirements data when available, passes it to PlainLanguageSummary, overrides the Money Out card total, excludes it from selectable tabs, and the build type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add headline override + gap-explanation label to PlainLanguageSummary.tsx</name>
  <files>src/components/dashboard/PlainLanguageSummary.tsx</files>
  <read_first>
    - src/components/dashboard/PlainLanguageSummary.tsx — read: the PlainLanguageSummaryProps interface (lines ~4-20), the destructured props + `const budgetedTotal = operatingData.metadata.totalBudget` (line ~38), the formatAmount helper (handles billion-scale already), and the JSX where the main budget sentence renders (and the `{revenueData && (...)}` backward-compat gating pattern for the new block placement)
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "src/components/dashboard/PlainLanguageSummary.tsx" section: props interface extension, headline override, gap-explanation label JSX with the `> operatingData.metadata.totalBudget` guard, formatAmount reuse
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "Frontend Change Architecture" item 4 and "Anti-Patterns to Avoid" (do not replace icicle data; guard the gap label against negative remainder)
  </read_first>
  <action>
Add an optional prop `allFundsRequirementsData?: BudgetData | null;` to the `PlainLanguageSummaryProps` interface and to the destructured params (default `null`).

Change the headline total: replace `const budgetedTotal = operatingData.metadata.totalBudget;` with `const budgetedTotal = allFundsRequirementsData?.metadata.totalBudget ?? operatingData.metadata.totalBudget;`. This makes the headline show the all-funds figure when present, falling back to operating (D-02). Do NOT change `actualTotal`, the icicle/`drillLevel` data, or any `operatingData.categories` usage — the department breakdown must remain the operating data (D-02; anti-pattern: do not swap icicle data).

Add a gap-explanation paragraph (D-03) rendered after the main budget sentence, gated so it appears ONLY when all_funds data is present AND larger than operating (anti-pattern guard against negative remainder): render only when `allFundsRequirementsData && operatingData && allFundsRequirementsData.metadata.totalBudget > operatingData.metadata.totalBudget`. The label text uses the existing `formatAmount` helper for all three figures: X = `allFundsRequirementsData.metadata.totalBudget` (all-funds total), Y = `operatingData.metadata.totalBudget` (departmental subset), Z = X − Y (remainder). Wording: state that the X total covers all city funds, the department breakdown below accounts for Y in departmental operations, and the remaining Z covers debt service, capital projects, and other city-wide requirements. Style it as a small italic secondary paragraph consistent with the existing secondary text classes (e.g. `text-[13px] ... italic`).

The block must be backward-compatible: for TX/CA cities `allFundsRequirementsData` is null, so neither the headline override nor the gap label changes anything (D-04).
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - PlainLanguageSummaryProps interface contains `allFundsRequirementsData`
    - PlainLanguageSummary.tsx headline reads `allFundsRequirementsData?.metadata.totalBudget ?? operatingData.metadata.totalBudget`
    - PlainLanguageSummary.tsx gap-label JSX is guarded by `allFundsRequirementsData.metadata.totalBudget > operatingData.metadata.totalBudget`
    - The gap-label remainder uses `allFundsRequirementsData.metadata.totalBudget - operatingData.metadata.totalBudget`
    - `operatingData.categories` / icicle drill data is NOT replaced by allFundsRequirementsData (icicle still uses operating)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>PlainLanguageSummary shows the all-funds headline + gap label for OR cities, keeps the operating icicle, and is a no-op for cities without all_funds_requirements data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the corrected Budget tab in the app</name>
  <what-built>
    The Budget (Money Out) tab now shows the All Funds Requirements total as the headline for OR cities, with a gap-explanation label, while the department breakdown (icicle) is unchanged. TX/CA cities are unaffected. all_funds_requirements is not a selectable tab.
  </what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`) and open the app.
    2. Select Gresham, OR, FY2026. Confirm the Money Out / Budget headline shows ~$897M (the All Funds Requirements total), NOT ~$330M (the departmental operating total).
    3. Confirm a gap-explanation label appears below the headline reading approximately: "This $897 million total covers all city funds. The department breakdown below accounts for $330 million in departmental operations; the remaining $566 million covers debt service, capital projects, and other city-wide requirements." (figures approximate).
    4. Confirm the department icicle/category breakdown below still shows the operating departments (unchanged).
    5. Confirm there is NO fourth selectable tab/card labeled all funds requirements — only the existing Money Out / Money In (and salaries where applicable) tabs.
    6. Switch the year selector across Gresham's FY2023–FY2026 and confirm the headline All Funds total updates per year and remains on matching scope with the Money In tab.
    7. Select Portland, OR — confirm a (much larger, billions-scale) All Funds headline + gap label render, and the FY2026 figure reconciles with the Money In side scope.
    8. Select Troutdale, OR FY2026 — confirm ~$81M All Funds headline + gap label.
    9. Select a TX city (e.g., Dallas) and a CA city (e.g., Los Angeles) — confirm NO gap label appears and the Budget headline is the normal operating total (unchanged behavior).
  </how-to-verify>
  <read_first>
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "Phase Requirements → Test Map" manual rows
  </read_first>
  <acceptance_criteria>
    - User confirms Gresham FY2026 Budget headline ≈ $897M (not $330M)
    - User confirms gap-explanation label present for all three OR cities
    - User confirms department icicle unchanged (operating departments)
    - User confirms no extra selectable tab for all_funds_requirements
    - User confirms year selector updates All Funds totals and keeps Budget/Money In on matching scope
    - User confirms TX (Dallas) and CA (Los Angeles) cities show no gap label and unchanged operating headline
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any display issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- Gresham/Portland/Troutdale Budget tabs show All Funds headline + gap label
- TX/CA cities unchanged (backward-compatible per D-04)
- all_funds_requirements not a selectable tab (Pitfall 4)
- Year selector keeps Budget and Money In on matching All Funds scope (D-06)
</verification>

<success_criteria>
End-to-end (D-01) complete: OR cities' Budget tab shows the All Funds Requirements total (D-02) with a gap-explanation label (D-03), generically for any city with the data (D-04), across all loaded FYs (D-06), human-verified in the app.
</success_criteria>

<output>
Create `.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-04-SUMMARY.md` when done.
</output>
