---
phase: 23-or-all-funds-consistency-requirements-extraction-portland-gr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/extractGresham.py
  - scripts/processGresham.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "extractGresham.py --mode requirements returns the 6 non-operating categories (Capital Improvement, Debt Service, Transfers, Contingency, Other Requirements, Unappropriated) with no department rows and no sum rows"
    - "processGresham.js --requirements --dry-run prints FY2023-FY2026 totals near $897M for FY2026"
    - "Gresham all_funds_requirements rows are loaded in treasury.budgets for FY2023-FY2026"
    - "treasury.data_sources has a row with dataset_type='all_funds_requirements' for each Gresham fiscal year"
  artifacts:
    - path: "scripts/extractGresham.py"
      provides: "extract_requirements() function + --mode requirements CLI choice"
      contains: "def extract_requirements"
    - path: "scripts/processGresham.js"
      provides: "--requirements flag dispatching mode=requirements / datasetType=all_funds_requirements"
      contains: "all_funds_requirements"
  key_links:
    - from: "scripts/processGresham.js"
      to: "scripts/extractGresham.py"
      via: "spawnSync with --mode requirements arg"
      pattern: "requirements.*--mode|--mode.*requirements"
    - from: "scripts/processGresham.js"
      to: "treasury_sync_budget_tree"
      via: "RPC with p_dataset_type='all_funds_requirements'"
      pattern: "all_funds_requirements"
---

<objective>
Add All Funds Requirements extraction to the Gresham pipeline so the Budget tab can show the same all-funds scope (~$897M FY2026) that the Money In tab already shows. Extract the non-operating Requirements categories from the "Resources and Requirements — All Funds" page (the same page Phase 21 used for revenue), store them under `dataset_type='all_funds_requirements'`, and live-load FY2023–FY2026 (D-06).

Purpose: Resolve the Budget/Money In scope mismatch for Gresham — the apparent $180M windfall is an accounting artifact of mismatched scopes (departmental operating ~$330M vs All Funds Resources ~$512M+). Storing All Funds Requirements makes both sides balance.
Output: extract_requirements() in extractGresham.py, --requirements mode in processGresham.js, and Gresham all_funds_requirements rows in the DB for FY2023–FY2026.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md
@.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extract_requirements() to extractGresham.py</name>
  <files>scripts/extractGresham.py</files>
  <read_first>
    - scripts/extractGresham.py — read the full file, especially extract_revenue() (the sibling function being mirrored) and the __main__ argparse block at the bottom
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "scripts/extractGresham.py — add extract_requirements()" section has the exact page-guard, fiscal-year-parse, section-gate, token-split, and OCR-fix patterns to reuse verbatim
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "Gresham Requirements Section Structure" (expected rows + FY2026 total $897,266,615) and "Pitfall 3: Gresham Requirements Include Department Rows"
  </read_first>
  <action>
Add a new function `extract_requirements(pdf_path)` as a sibling of `extract_revenue()`. Reuse the identical page guard (`'Resources and Requirements' in text and 'All Funds' in text`, plus the `'Taxes' not in text` skip-TOC guard), the same `parse_fy_from_header` fiscal-year-from-header loop over `lines[:8]`, and the same token-split + OCR split-number fix (`num_tokens[-2]` short-fragment / `num_tokens[-1]` starts-with `\d{1,3},` reassembly).

Flip the section gate: use a variable `in_requirements` (not `in_resources`). Set `in_requirements = True` when the line matches the Requirements header (`s_norm == 'Requirements'` or `s.startswith('Requirements')`), and set it `False` when the line matches the Resources header (`s_norm == 'Resources'` or `s.startswith('Resources ')` but not `'Resources and'`). Skip lines while `not in_requirements`.

Use a WHITELIST, not a blacklist, to avoid capturing department rows (per Pitfall 3). Define `REQUIREMENTS_CATEGORIES = {'Capital Improvement', 'Debt Service', 'Transfers', 'Contingency', 'Other Requirements', 'Unappropriated'}`. After building the category name with the same `re.sub(r'\s+', ' ', ...)` normalization, `continue` if `category not in REQUIREMENTS_CATEGORIES`. This automatically skips the department rows, the `Operating Total` / `Non-Operating Total` sub-sums, and the `Total Requirements` grand-total row.

Return the same dict shape as extract_revenue: `{ 'category', 'adopted_amount', 'fiscal_year', 'page_num' }`. Keep the `if results: break` once a real data page is found, and the trailing None-fiscal-year warning block.

Extend the `__main__` argparse: add `'requirements'` to `--mode` `choices` so it reads `choices=['operating', 'revenue', 'requirements']`, and extend the dispatch so `args.mode == 'requirements'` calls `extract_requirements(args.pdf_path)`, `'revenue'` calls `extract_revenue`, else `extract_budget`.
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; python -c "import ast; ast.parse(open('scripts/extractGresham.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/extractGresham.py contains `def extract_requirements(`
    - scripts/extractGresham.py contains the literal set members `'Capital Improvement'`, `'Debt Service'`, `'Transfers'`, `'Contingency'`, `'Other Requirements'`, `'Unappropriated'`
    - scripts/extractGresham.py `--mode` argparse choices include `'requirements'`
    - `python -c "import ast; ast.parse(open('scripts/extractGresham.py').read())"` exits 0
    - Running `python scripts/extractGresham.py docs/Gresham/fy2025-26.pdf --mode requirements` prints a JSON array of 6 objects, none of which is a department name or a row named "Operating Total" / "Non-Operating Total" / "Total Requirements"
  </acceptance_criteria>
  <done>extract_requirements() exists, parses cleanly, and CLI `--mode requirements` returns exactly the 6 non-operating categories for Gresham FY2026.</done>
</task>

<task type="auto">
  <name>Task 2: Add --requirements mode to processGresham.js and live-load FY2023-FY2026</name>
  <files>scripts/processGresham.js</files>
  <read_first>
    - scripts/processGresham.js — read the full file: extractPDF() (spawnSync arg push), processPDF() mode-label block, upsertDataSource() label, the SANITY_MAX guard (already gated to mode==='operating'), and the main() parseArgs block
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-PATTERNS.md — "scripts/processGresham.js — add --mode requirements" section: exact CLI parse, extractPDF, processPDF label, upsertDataSource label, and SANITY_MAX notes
    - .planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-RESEARCH.md — "SANITY_MAX for Requirements Mode" table (Gresham requirements ~$897M must skip the $500M cap) and "Idempotency" section
  </read_first>
  <action>
Extend main()'s parseArgs `options` to add `requirements: { type: 'boolean', default: false }`. Replace the mode derivation `const mode = opts.revenue ? 'revenue' : 'operating'` with `const mode = opts.requirements ? 'requirements' : opts.revenue ? 'revenue' : 'operating'`.

In `extractPDF(pdfPath, mode)`, after the existing `if (mode === 'revenue') args.push('--mode', 'revenue');`, add `if (mode === 'requirements') args.push('--mode', 'requirements');`.

In `processPDF()`, replace the `isRevenue`-only label block with three-way logic: `const isRevenue = mode === 'revenue'`, `const isRequirements = mode === 'requirements'`, `const unitLabel = isRevenue || isRequirements ? 'categories' : 'departments'`, `const typeLabel = isRevenue ? 'Revenue' : isRequirements ? 'All Funds Requirements' : 'Operating'`, `const datasetType = mode === 'requirements' ? 'all_funds_requirements' : mode === 'revenue' ? 'revenue' : 'operating'`. Use `buildRevenueTree(fyRows)` for requirements (the category row shape `{ n, a, i[] }` is identical) — change the tree selection so it uses buildRevenueTree when `isRevenue || isRequirements`.

Do NOT change the SANITY_MAX guard — it already reads `if (mode === 'operating' && SANITY_MAX[fy] && ...)`, so requirements mode (~$897M) is already excluded, matching the revenue precedent.

In `upsertDataSource()`, change the label to: `const label = datasetType === 'revenue' ? 'Revenue Budget' : datasetType === 'all_funds_requirements' ? 'All Funds Requirements' : 'Operating Budget'`. The existing select filter already includes `.eq('dataset_type', datasetType)`, so `all_funds_requirements` rows are isolated from operating/revenue — no collision.

After implementing, dry-run to validate all 4 FYs (`node scripts/processGresham.js --requirements --dry-run`), confirm FY2026 total is approximately $897,266,615 and 6 categories, then run the live load (`node scripts/processGresham.js --requirements`) to insert FY2023–FY2026 rows.
  </action>
  <verify>
    <automated>cd C:/treasury-tracker; node scripts/processGresham.js --requirements --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/processGresham.js contains `requirements:` in the parseArgs options block
    - scripts/processGresham.js contains the string `all_funds_requirements`
    - scripts/processGresham.js contains the label `'All Funds Requirements'`
    - `node scripts/processGresham.js --requirements --dry-run` exits 0 and prints a FY2026 line with total within $1M of $897,266,615 and "6 categories"
    - After live load, querying `treasury.data_sources` for Gresham returns rows with `dataset_type='all_funds_requirements'` for fiscal_years 2023, 2024, 2025, 2026
    - After live load, querying `treasury.budgets` for the Gresham all_funds_requirements data_source FY2026 returns a total within $1M of $897,266,615
    - Re-running `node scripts/processGresham.js --requirements` a second time does not create duplicate budget rows (delete-before-insert idempotency)
  </acceptance_criteria>
  <done>processGresham.js --requirements loads Gresham all_funds_requirements rows for FY2023–FY2026, dry-run totals match the PDF, and the operating SANITY_MAX cap does not fire on requirements mode.</done>
</task>

</tasks>

<verification>
- `python scripts/extractGresham.py docs/Gresham/fy2025-26.pdf --mode requirements` returns 6 non-operating categories, no department/sum rows
- `node scripts/processGresham.js --requirements --dry-run` prints FY2023–FY2026 totals; FY2026 ~$897M
- Live load creates `dataset_type='all_funds_requirements'` data_source + budget rows for all 4 Gresham FYs
- Operating and revenue Gresham rows are untouched (separate dataset_type isolation)
</verification>

<success_criteria>
Gresham All Funds Requirements data is extractable and loaded for FY2023–FY2026 under `dataset_type='all_funds_requirements'`, with FY2026 ~$897M, ready for the frontend to consume in Plan 04.
</success_criteria>

<output>
Create `.planning/phases/23-or-all-funds-consistency-requirements-extraction-portland-gr/23-01-SUMMARY.md` when done.
</output>
