---
phase: 11-population-schema-census-data-load-and-per-capita-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/treasury-tracker/scripts/loadTXPopulation.js
  - C:/EV-Accounts/backend/src/lib/treasuryService.ts
autonomous: true
must_haves:
  truths:
    - "loadTXPopulation.js parses sub-est2024_48.csv and produces the correct 12 TX city -> population mapping"
    - "loadTXPopulation.js is idempotent: re-running does not corrupt existing population values"
    - "EV-Accounts /api/treasury/cities response includes population_year for each city"
  artifacts:
    - path: "C:/treasury-tracker/scripts/loadTXPopulation.js"
      provides: "Census CSV downloader/parser + Supabase UPDATE for 12 TX cities"
      contains: "POPESTIMATE2024"
      min_lines: 80
    - path: "C:/EV-Accounts/backend/src/lib/treasuryService.ts"
      provides: "Updated CityRow, TreasuryCity, mapCity, getCities, getCityById to include population_year"
      contains: "population_year"
  key_links:
    - from: "loadTXPopulation.js"
      to: "treasury.municipalities (Supabase)"
      via: "createClient with db.schema='treasury' + .update().eq('name', X).eq('state', 'TX')"
      pattern: "\\.update\\(\\s*\\{\\s*population"
    - from: "treasuryService.ts getCities() SQL"
      to: "treasury.municipalities.population_year column"
      via: "explicit column SELECT"
      pattern: "m\\.population_year"
    - from: "treasuryService.ts mapCity()"
      to: "TreasuryCity.population_year"
      via: "row.population_year !== null ? Number(...) : null"
      pattern: "population_year"
---

<objective>
Build the two code artifacts needed to load population data and surface it through the API:
1. `loadTXPopulation.js` — Node.js loader script in the treasury-tracker repo that parses the cached Census CSV (`C:/tmp/sub-est2024_48.csv`) and UPDATEs `population` + `population_year` on the 12 TX city rows.
2. EV-Accounts `treasuryService.ts` update — add `population_year` to `CityRow`, `TreasuryCity`, `mapCity()`, and both `getCities()` + `getCityById()` SQL SELECTs.

Purpose: Without (1), no data ever loads into the new column. Without (2), the API silently omits `population_year` from responses even after the data is loaded — the frontend would show no year label. Both must ship before 11-03 can verify in the browser.

This plan does NOT execute the loader against the live DB and does NOT push EV-Accounts to deploy. Live execution and deploy happen in 11-03 (after the 11-01 migration is applied).

Output:
- `C:/treasury-tracker/scripts/loadTXPopulation.js` committed to treasury-tracker repo
- `C:/EV-Accounts/backend/src/lib/treasuryService.ts` updated and committed to EV-Accounts repo
- Both repos have local commits ready to push
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/treasury-tracker/.planning/PROJECT.md
@C:/treasury-tracker/.planning/STATE.md
@C:/treasury-tracker/.planning/phases/11-population-schema-census-data-load-and-per-capita-display/11-RESEARCH.md

# Existing loader pattern to follow
@C:/treasury-tracker/scripts/insertLongviewMunicipality.js
@C:/treasury-tracker/scripts/processGarlandBudget.js

# EV-Accounts API layer to update
@C:/EV-Accounts/backend/src/lib/treasuryService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build loadTXPopulation.js loader script</name>
  <files>C:/treasury-tracker/scripts/loadTXPopulation.js</files>
  <action>
Create `C:/treasury-tracker/scripts/loadTXPopulation.js` following the established loader pattern from `insertLongviewMunicipality.js`. The script must:

**Shebang + imports:**
```javascript
#!/usr/bin/env node
import { createClient } from '@supabase/supabase-js';
import { readFileSync, existsSync, createWriteStream } from 'node:fs';
import { get as httpsGet } from 'node:https';
import { parseArgs } from 'node:util';
```

**Constants:**
- `SUPABASE_URL = process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co'`
- `SUPABASE_KEY = process.env.SUPABASE_SERVICE_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY`
- `CSV_URL = 'https://www2.census.gov/programs-surveys/popest/datasets/2020-2024/cities/totals/sub-est2024_48.csv'`
- `CSV_PATH = 'C:/tmp/sub-est2024_48.csv'`
- `POP_YEAR = 2024`
- `EXPECTED_CITIES` array — exactly these 12 strings (must match `municipalities.name` exactly): `['Allen','Celina','Frisco','Garland','McKinney','Murphy','Plano','Princeton','Prosper','Richardson','Sachse','Wylie']`

**Logic order:**
1. Parse `--dry-run` flag via `parseArgs({ options: { 'dry-run': { type: 'boolean' } } })`.
2. Exit with helpful error if `SUPABASE_KEY` is missing.
3. Download CSV if `CSV_PATH` does not exist (use `https.get` + `createWriteStream`; follow `processGarlandBudget.js` pattern). If it exists, log "Using cached CSV".
4. Read CSV via `readFileSync(CSV_PATH, 'utf8').split('\n')`.
5. Parse header; verify column indices: `SUMLEV` at 0, `NAME` at 8, `POPESTIMATE2024` at 15. If header does not match, fail loudly with message "Census CSV format changed — expected POPESTIMATE2024 at column 15".
6. Build a map `Map<dbName, population>` by iterating lines, filtering `cols[0] === '162'`, normalizing `cols[8]` via `normalizeCensusName()`, parsing `cols[15]` as int via `parseInt(..., 10)`.
7. `normalizeCensusName(name)` function: `name.replace(/ city$/, '').replace(/ town$/, '').replace(/ village$/, '').trim()`
8. After parsing, verify all 12 EXPECTED_CITIES are present in the map. If any missing, fail loudly listing the missing names.
9. Also verify the parsed values match the known-good values from research (sanity check — guards against silent CSV corruption):
   ```
   Allen=113746, Celina=51661, Frisco=235208, Garland=250431, McKinney=227526,
   Murphy=21109, Plano=293286, Princeton=37019, Prosper=44503, Richardson=118221,
   Sachse=33008, Wylie=62954
   ```
   If any value drifts more than 1% from these, log a warning but continue (Census may issue revisions; do not block on warnings).
10. Create Supabase client: `createClient(SUPABASE_URL, SUPABASE_KEY, { db: { schema: 'treasury' } })`.
11. For each of the 12 cities:
    - SELECT current `population, population_year` for that name+state=TX row.
    - **Idempotence guard:** if `population === expectedPop && population_year === 2024`, log "SKIP {name}: already set to {pop} (2024)" and continue.
    - If `--dry-run`, log "DRY: would UPDATE {name} to population={pop}, population_year=2024" and continue.
    - Otherwise: `.update({ population: expectedPop, population_year: POP_YEAR }).eq('name', name).eq('state', 'TX')`. If `error`, log and increment failure counter (do not throw — continue to next city so we get a complete report).
12. Print final summary: `Updated: N, Skipped: M, Failed: F` exit code 0 if F=0 else 1.

**Critical do-nots:**
- Do NOT use `.insert()` — these rows already exist; this is a batch UPDATE.
- Do NOT use `supabase.schema('treasury').from(...)` chaining — use the `createClient` config option per project convention.
- Do NOT call this `loadPopulation.js` — the roadmap names it `loadTXPopulation.js` (TX-scoped is intentional; future state loaders will be separate).
- Do NOT touch Longview, Portland, Bangor, or any non-TX municipality — Longview already has a hardcoded population set (83000) and is not in scope.
- Do NOT overwrite a non-TX city by accident — both `.eq('name', X)` AND `.eq('state', 'TX')` are required on every update.

After writing the file, commit ONLY this file to the treasury-tracker repo:

```bash
cd C:/treasury-tracker
git add scripts/loadTXPopulation.js
git commit -m "feat(11): add loadTXPopulation.js Census CSV loader

Downloads sub-est2024_48.csv from census.gov, parses POPESTIMATE2024 for
12 TX cities, and UPDATEs population + population_year columns via
Supabase service-role client. Idempotent (skips rows already set to 2024
vintage); supports --dry-run flag.

Refs: Phase 11 / POP-02"
```
  </action>
  <verify>
1. `node C:/treasury-tracker/scripts/loadTXPopulation.js --dry-run` runs to completion with exit code 0 (or 1 if SUPABASE_KEY missing — but the CSV parse + sanity check should still complete and print all 12 cities and values).
2. Dry-run output shows all 12 cities with correct populations matching the research table (Allen=113746, etc.).
3. Dry-run output shows "DRY: would UPDATE ..." for each of the 12 cities (assuming the DB rows currently have NULL population; if 11-01 migration not yet applied, the SELECT may fail — that is acceptable for this task since 11-01 and 11-02 run in parallel).
4. `cd C:/treasury-tracker && git log -1 --oneline` shows the commit.
  </verify>
  <done>
`loadTXPopulation.js` exists, dry-run mode parses the cached Census CSV correctly and produces the expected 12-city map with matching population values, and the script is committed to treasury-tracker main. (Live UPDATE execution happens in 11-03 after 11-01 migration is applied.)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update EV-Accounts treasuryService.ts to expose population_year</name>
  <files>C:/EV-Accounts/backend/src/lib/treasuryService.ts</files>
  <action>
Read the current `C:/EV-Accounts/backend/src/lib/treasuryService.ts` file in full, then make exactly these four edits. All edits are mechanical additions; do NOT refactor or restructure surrounding code.

**Edit 1: CityRow interface (raw DB row shape)**
Find the `interface CityRow` declaration. Add a new field directly after the existing `population` field:
```typescript
population_year: string | null; // INTEGER returned as string by node-postgres
```
Why `string | null`: the `pg` driver returns INTEGER values as strings by default (the same way it returns `population`). Inspect the existing `population` field's type to confirm — match its style exactly.

**Edit 2: TreasuryCity interface (public API shape)**
Find the `export interface TreasuryCity` declaration. Add a new field directly after the existing `population` field:
```typescript
population_year: number | null;
```

**Edit 3: mapCity() transform function**
Find the `mapCity(row: CityRow): TreasuryCity` function (or named equivalent). Inside the return object, directly after the `population: ...` line, add:
```typescript
population_year: row.population_year !== null ? Number(row.population_year) : null,
```
Match the surrounding indentation and trailing-comma style of the existing properties exactly.

**Edit 4: getCities() and getCityById() SQL SELECTs**
Find every SQL string in the file that selects from `treasury.municipalities` (there are at least two: in `getCities()` and `getCityById()`). In each SELECT column list, add `m.population_year` directly after `m.population`:
- Before: `m.population, m.hero_image_url`
- After:  `m.population, m.population_year, m.hero_image_url`

Do this for **every** occurrence — there must be no SELECT against `treasury.municipalities` that omits `m.population_year` after this task completes (otherwise that endpoint will silently strip the column from responses, exactly the pitfall called out in research).

**Critical do-nots:**
- Do NOT change the order of columns in SQL — only insert `m.population_year` after `m.population`.
- Do NOT use `SELECT *` to avoid the issue — the project's explicit-column convention is intentional.
- Do NOT add `population_year` with a default of `0` in `mapCity()` — `null` must remain `null` so the frontend can choose whether to display the year label.
- Do NOT modify any other function or interface in this file beyond the four edits above.

After editing, run TypeScript compile to confirm no type errors introduced:

```bash
cd C:/EV-Accounts
npm run build 2>&1 | tail -30
```

Then commit ONLY this file to the EV-Accounts repo:

```bash
cd C:/EV-Accounts
git add backend/src/lib/treasuryService.ts
git commit -m "feat(treasury): expose population_year in cities API

Adds population_year to CityRow, TreasuryCity, mapCity(), and both
getCities + getCityById SQL SELECTs. Without this, the new
population_year DB column is invisible to the treasury-tracker frontend
even after data is loaded.

Refs: treasury-tracker Phase 11 / POP-03"
```

Do NOT push yet — push happens in 11-03 alongside the live data load (so the deploy lands in the right order relative to the frontend changes).
  </action>
  <verify>
1. `grep -c 'population_year' C:/EV-Accounts/backend/src/lib/treasuryService.ts` returns at least 5 (one per: CityRow, TreasuryCity, mapCity, getCities SELECT, getCityById SELECT).
2. `cd C:/EV-Accounts && npm run build` exits 0 with no TypeScript errors.
3. There is no SELECT statement against `treasury.municipalities m` in this file that lacks `m.population_year` — confirm by grepping for `m.population` and checking each result is followed by `m.population_year`.
4. `cd C:/EV-Accounts && git log -1 --oneline` shows the commit.
  </verify>
  <done>
`treasuryService.ts` is updated in all four required locations, TypeScript compiles cleanly, and the file is committed to EV-Accounts main (not yet pushed).
  </done>
</task>

</tasks>

<verification>
1. `C:/treasury-tracker/scripts/loadTXPopulation.js` exists, dry-run parses 12 cities with correct values, committed.
2. `C:/EV-Accounts/backend/src/lib/treasuryService.ts` has `population_year` in 5+ places (CityRow, TreasuryCity, mapCity, 2x SQL SELECTs), compiles, committed.
3. Neither change has been pushed to remote yet — push happens in 11-03 after live load and migration apply (11-01) are both green.
</verification>

<success_criteria>
- Loader script can parse the cached Census CSV and produce the exact 12 TX city -> population mapping documented in research (Allen=113746, ..., Wylie=62954)
- Loader has idempotence guard (skips rows already set to 2024 vintage)
- Loader supports `--dry-run` for safe pre-flight
- EV-Accounts `treasuryService.ts` returns `population_year` from `/api/treasury/cities` and `/api/treasury/cities/:id` (verified by inspecting all SELECT statements)
- Both files committed locally to their respective repos
</success_criteria>

<output>
After completion, create `.planning/phases/11-population-schema-census-data-load-and-per-capita-display/11-02-SUMMARY.md` documenting:
- Loader script path + commit SHA in treasury-tracker
- treasuryService.ts commit SHA in EV-Accounts
- Dry-run output showing the 12 cities + populations
- Any deviations from the values documented in 11-RESEARCH.md (Census revisions, parse changes, etc.)
- Confirmation: both repos still have these commits unpushed (11-03 owns the push + deploy)
</output>
