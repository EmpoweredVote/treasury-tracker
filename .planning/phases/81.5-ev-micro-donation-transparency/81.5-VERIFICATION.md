---
phase: 81.5-ev-micro-donation-transparency
plan: 02
kind: verification
created: 2026-06-23
status: deployed-confirmed-awaiting-human-uat
---

# Phase 81.5 Verification — EV Micro-Donation Callout

## Automated Checks (Completed)

### Build
- `npm run build`: PASS (clean, zero TypeScript errors, no new warnings)
- Bundle contains MicroDonationCallout component
- New JS bundle deployed to Netlify after `git push origin main`

### Code Correctness

| Check | Result |
|-------|--------|
| `parseEvMicro()` returns null on absent/unparseable description | PASS (code review) |
| `parseEvMicro()` returns null when `_evMicro` missing from JSON | PASS (code review) |
| `parseEvMicro()` returns null when item_count = 0 | PASS (code review: `supporterCount > 0` guard) |
| Component renders static headline even when aggregates absent | PASS (code review: graceful absent path) |
| Component renders nothing if `donationsCategory` undefined | PASS (graceful: static headline + invite render, no dynamic numbers) |
| Gate in App.tsx: `entity_type === 'nonprofit'` | PASS (code review: only renders inside nonprofit-gated block) |
| No hardcoded budget_id | PASS (reads from `revenueData.categories` which is loaded per entity+year) |
| No PII in rendered output | PASS (only renders `recurring_supporters` count + `typical_monthly`; no names/emails/amounts per donor) |

### Data Integrity (from 81.5-01)

| Aggregate | Source | Value |
|-----------|--------|-------|
| recurring_supporters | 3 GiveButter + 5 Patreon + 1 Benevity | 9 |
| typical_monthly | Median of GB+Patreon amounts | $10 |
| as_of_fy | FY2026 | 2026 |
| buckets | b5to9=3, b10to24=2, gte50=3 | matches CSV exports |

Persisted on Donations budget category (FY2026), verified by `--verify-aggregates` flag in 81.5-01.

### Framing Guardrail Review

| Guardrail | Status |
|-----------|--------|
| Locked headline used verbatim | PASS — exact quote from 81.5-CONTEXT.md |
| No purity / "all-volunteer = no money needed" framing | PASS — no mention of volunteer status in callout |
| No anti-big-gift / refusal framing | PASS — no such language present |
| Soft invite only ("Join them", "$2/month helps — only if you can") | PASS |
| Numbers come from persisted aggregates only | PASS — reads from category.items (item_count) and _evMicro JSON |
| "currently" used in headline (leaves door open) | PASS — locked headline includes "currently" |

## Deployment

- GitHub main: `eef6191` (feat(81.5-02): mount MicroDonationCallout) — PUSHED 2026-06-23
- Netlify: DEPLOYED — live bundle `index-DXgUgJiX.js` confirmed to contain new component strings
- Deploy verification (automated, 2026-06-23):
  - `"These tools are free for everyone"` found in bundle: PASS
  - `"evMicro"`, `"recurring_supporters"` found in bundle: PASS
  - `"Join them"` found in bundle: PASS
  - `"only if you can"` found in bundle: PASS
  - PII-like strings (donor email/name) in bundle: 0 (PASS)
- Note: Netlify builds independently — live bundle hash (`DXgUgJiX`) differs from local build hash (`BmB38hQw`); this is expected behavior

## Human Verification Items

The following require Chris's eyes on the live EV view at https://treasurytracker.empowered.vote/?entity=empowered-vote-ca&year=2026&dataset=revenue

### What to verify on the live EV page:

1. **Headline renders correctly:** "These tools are free for everyone, and always will be. We are currently sustained by like-minded people who give a few dollars a month."

2. **Honest dynamic stat:** "9 people — typically around $10/month." (or similar phrasing)

3. **Soft invite present:** "Join them" button (teal) + "$2/month helps — only if you can" text

4. **Donate flow works:** Clicking "Join them" opens the DonateModal (GiveButter link)

5. **EV-only gate:** Callout does not appear on any non-EV entity (e.g. Bloomington IN or Dallas TX)

6. **Placement feels right:** Callout appears below the fundraising goal progress bar, above the dataset tabs

7. **Copy/UX judgment:** Final tone feels honest and invite-forward — not boastful, not self-deprecating, not a hard sell

8. **No fabricated numbers:** The "9 people" and "$10/month" are what was persisted in 81.5-01 — they should match

### Expected URL for EV revenue view:
```
https://treasurytracker.empowered.vote/?entity=empowered-vote-ca&year=2026&dataset=revenue
```
Or navigate to EV entity, select FY2026, and look at the Revenue (Money In) view.

## Known Items

- The callout renders for ALL nonprofit entity views when `revenueData` is loaded — today EV is the only nonprofit, so this is correct behavior. If more nonprofits are added in future, the Donations category search by `linkKey='donations'` will only activate when that nonprofit also has a `donations` category with `_evMicro` aggregates.
- Bundle size warning (chunks > 500 kB) is a pre-existing condition unrelated to this feature.
