---
id: 81.5-02
wave: 2
depends_on: ["81.5-01"]
autonomous: true
objective: >
  Render the "how we stay free" micro-donation callout on the EV nonprofit financials view —
  locked headline + a quiet recurring-supporter stat (+ optional simple size bar) + a soft
  recurring-donate invite, reading the anonymized aggregates from 81.5-01. Then verify honesty
  + UX live.
files_modified:
  - src/components/dashboard/MicroDonationCallout.tsx
  - src/App.tsx
task_count: 3
---

# Plan 81.5-02 — EV micro-donation callout (UI half) + verify

Read `81.5-CONTEXT.md` + memory `project_ev_funding_philosophy` first. Brand per `project_ev_financials_branding`. **Never frame EV as refusing larger/bridge support; never spin volunteer status as "no money needed"; soft invite only.**

## Task 1 — MicroDonationCallout component
New `src/components/dashboard/MicroDonationCallout.tsx`. Props: the EV revenue budget's Donations category (to read `item_count` + parse the `_evMicro` JSON from `description`). Renders:
- **Locked headline (static):** "These tools are free for everyone, and always will be. We are currently sustained by like-minded people who give a few dollars a month."
- **Quiet dynamic stat (from data):** e.g. "{recurring_supporters} people, typically about ${typical_monthly}/month." Word as *gifts/supporters* honestly; never imply a number we didn't persist.
- Optional **simple size bar** from `buckets` (only if it renders cleanly; else omit — text-first).
- **Soft invite:** a low-key "Join them" linking to the existing Donate flow (reuse `setDonateOpen`); copy stays gentle ("$2/month helps — only if you can"). No hard sell.
- **Graceful absence:** if `_evMicro` is missing/unparseable or `item_count` is 0, render nothing (or just the static free-for-everyone line, no fabricated numbers).

**Verify:** `npm run build` clean; component renders with mocked data and renders nothing when aggregates absent.

## Task 2 — Mount on the EV financials view
In `src/App.tsx`, render `<MicroDonationCallout>` on the nonprofit (EV) Money In / revenue view only (gate on `selectedEntity?.entity_type === 'nonprofit'`), positioned where it reads as "how we stay free" (near the income summary). Pull the Donations category from the already-loaded revenue budget data — no new fetch.

**Verify:** `npm run build` clean; shows only for EV; does not appear for municipal entities.

## Task 3 — Live verification + honesty UAT (write 81.5-VERIFICATION.md)
After deploy (push to main → Netlify), confirm on `treasurytracker.empowered.vote` (EV view): the headline + stat render; the displayed numbers match the source-export reconciliation from 81.5-01; no PII anywhere in the payload; the invite is soft and links to Donate; nothing claims EV refuses big gifts. Capture results in `81.5-VERIFICATION.md` for Chris's sign-off.

**Verify:** live EV view shows the honest callout; numbers reconcile; no PII in network payload; copy matches the locked wording + guardrails.

## Success criteria
- [ ] Callout renders the locked headline + honest dynamic stat + soft recurring-donate invite on the EV view only.
- [ ] Numbers come from the persisted anonymized aggregates and reconcile to source exports.
- [ ] No PII in the rendered payload; graceful when aggregates absent.
- [ ] No purity / anti-big-gift / volunteer-as-virtue framing; build clean; deployed; VERIFICATION written.
