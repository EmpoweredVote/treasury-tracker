---
phase: 48-source-chain-verification-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/auditFederalSources.mjs
  - .planning/phases/48-source-chain-verification-uat/48-AUDIT.md
autonomous: true
requirements:
  - VERIFY-01
must_haves:
  truths:
    - "Every URL-bearing federal claim row (48-CONTEXT inventory: budgets→registry, annual_summary, context_metrics, US enrichment, program_details incl. details jsonb) gets a verdict: PASS / HUMAN-CHECK / FAIL"
    - "govinfo URLs are verified via api.govinfo.gov (never page status); bot-walled domains get a real-browser pass before anything lands on the human list"
    - "FAILs are fixed at the data layer (correct the row, re-audit) or explicitly dispositioned — zero silent failures"
    - "48-AUDIT.md committed with per-surface counts, deduped per-URL results, and the exact HUMAN-CHECK list for 48-02"
  artifacts:
    - path: "scripts/auditFederalSources.mjs"
      provides: "Re-runnable source-chain auditor"
      contains: "program_details"
    - path: ".planning/phases/48-source-chain-verification-uat/48-AUDIT.md"
      provides: "Committed audit report"
      contains: "HUMAN-CHECK"
  key_links:
    - from: "scripts/auditFederalSources.mjs"
      to: "treasury.program_details"
      via: "URL inventory walk"
      pattern: "source_url"
---

<objective>
Automated source-chain audit: walk every federal figure/text row, confirm its source URL resolves (per-domain strategy), commit the report. VERIFY-01 closed except for the human-check residue, which is handed to 48-02 as a precise list.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-source-chain-verification-uat/48-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/auditFederalSources.mjs + run</name>
  <files>scripts/auditFederalSources.mjs</files>
  <read_first>
    - scripts/loadProgramOrigins.js — loadEnv, govinfo API existence checks, key hygiene
    - 48-CONTEXT.md — inventory + per-domain strategy + verdict rules
  </read_first>
  <action>
    Node script, no new deps (Supabase client + fetch). Pulls the full inventory via SQL (the five surfaces; program_details unions all 4 url columns + details jsonb source_urls), dedupes URLs, applies the per-domain strategy:
    - friendly domains: GET, browser UA, redirects, 15s timeout, 300ms courtesy delay, retry once on 5xx/timeout
    - govinfo.gov/app/details/{pkg}[/{granule}]: parse IDs → api.govinfo.gov summary (DATA_GOV_API_KEY from env, never logged)
    - congress.gov + bioguide.congress.gov + gao.gov: mark BROWSER (handled in Task 2)
    Outputs JSON results keyed by URL with the referencing rows (surface + name_key/label/fiscal_year). Also verify: all 3 US budgets rows join to a registry row AND production /api/treasury/budgets/:id responses carry data_source_info (curl the 3 datasets).
  </action>
  <verify><automated>Script runs clean; every inventory row has a verdict or BROWSER mark; counts match the 48-CONTEXT inventory</automated></verify>
  <done>Machine-checkable URLs all audited; bot-walled residue precisely enumerated.</done>
</task>

<task type="auto">
  <name>Task 2: browser pass + 48-AUDIT.md</name>
  <files>.planning/phases/48-source-chain-verification-uat/48-AUDIT.md</files>
  <action>
    Playwright pass (47-03 harness pattern: playwright-core in a temp dir + local chromium-1217 — NOT a project dep) over the BROWSER-marked URLs: load page, assert expected content (bill title fragment for congress.gov bill pages, member surname for bioguide, report/page title for gao.gov), screenshot 2–3 samples into the phase folder. Still-blocked or inconclusive → HUMAN-CHECK.
    Any FAIL anywhere: fix the data row (e.g., re-pin a moved URL) and re-run the auditor — document the fix in the report.
    Write 48-AUDIT.md: per-surface table (rows / unique URLs / PASS / HUMAN-CHECK / FAIL-fixed), full HUMAN-CHECK list with "what to look for" per URL, method notes (govinfo-via-API, bot-wall handling). Commit script + report.
  </action>
  <verify><automated>48-AUDIT.md committed; zero unresolved FAILs; HUMAN-CHECK list is exact URLs with expectations</automated></verify>
  <acceptance_criteria>
    - VERIFY-01 satisfied: every claim audited, report committed, failures fixed or dispositioned
  </acceptance_criteria>
  <done>The source chain is proven end-to-end by machine wherever a machine can reach.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Info disclosure | DATA_GOV_API_KEY | mitigate | env only; never in logs/report; report URLs are public pages |
| T-48-02 | Info integrity | false PASS via SPA 200s | mitigate | govinfo via API; browser pass requires content match, not status |
| T-48-SC | Tampering | installs | accept | playwright-core one-off in temp dir only; no new project deps |
</threat_model>

<verification>
1. Verdict counts in 48-AUDIT.md sum to the 48-CONTEXT inventory totals
2. Re-run of the auditor is idempotent (same verdicts; report regenerable)
</verification>

<success_criteria>
- VERIFY-01: automated audit complete, committed, zero silent failures
</success_criteria>

<output>
Create .planning/phases/48-source-chain-verification-uat/48-01-SUMMARY.md when done
</output>
