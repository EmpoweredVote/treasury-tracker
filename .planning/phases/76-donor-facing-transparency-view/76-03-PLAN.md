---
phase: 76-donor-facing-transparency-view
plan: 03
type: execute
wave: 2
depends_on: ["76-01", "76-02"]
files_modified:
  - src/components/dashboard/OrgTransparencyPanel.tsx
  - src/App.tsx
  - src/components/dashboard/PlainLanguageSummary.tsx
autonomous: false
requirements: [EVVIEW-01, EVVIEW-02, EVVIEW-03, EVVIEW-04]
must_haves:
  truths:
    - "On the EV (nonprofit) page, a 'Funds on Hand' figure shows the bank balance with its as-of date (EVVIEW-03)"
    - "A fundraising-goal progress indicator shows income_net vs goal_amount, capped at 100% with a 'Goal reached' celebration state when met (EVVIEW-04, D-03); it is hidden when goal_amount is null"
    - "The income story reads 'Donors gave $X; after $Y in platform fees, $Z reached EV' (totals from the reconciled summary) followed by a compact per-source GiveButter/Patreon/Benevity gave→fee→net breakdown (EVVIEW-01, D-07/D-08)"
    - "An honest burn-PACE line ('EV currently spends about $X/month, mostly on <top category>') replaces any runway countdown — runway is NOT displayed (EVVIEW-03 reframe, D-05/D-06)"
    - "The expense breakdown reads as an honest by-category breakdown with NEUTRAL framing; the $0-staff line is a plain year-specific fact with no celebratory all-volunteer branding (EVVIEW-02 reframe, D-10/D-11/D-12)"
    - "All EV figures still carry a source; non-nonprofit (city/county/federal/state) pages render identically to before (no regression)"
  artifacts:
    - path: "src/components/dashboard/OrgTransparencyPanel.tsx"
      provides: "Funds-on-Hand tile + goal-progress indicator for nonprofit orgs"
      contains: "Funds on Hand"
    - path: "src/components/dashboard/PlainLanguageSummary.tsx"
      provides: "fee gross→net sentence + per-source mini-list + burn-pace line + neutral volunteer framing"
      contains: "reached"
    - path: "src/App.tsx"
      provides: "nonprofit org-summary fetch + render of OrgTransparencyPanel"
      contains: "loadOrgFinancialSummary"
  key_links:
    - from: "loadOrgFinancialSummary (76-02)"
      to: "OrgTransparencyPanel + PlainLanguageSummary"
      via: "App.tsx useEffect fetches the summary for the nonprofit entity/FY and passes it down as props"
      pattern: "orgSummary"
---

<objective>
Render Empowered Vote's reconciled finances on its page so a donor understands the org at a glance — the visible deliverable of all four EVVIEW requirements, as reframed in CONTEXT. Add a small `OrgTransparencyPanel` (Funds on Hand + goal-progress bar) above the existing summary, and extend `PlainLanguageSummary` with the gross→fees→net income story, a per-source breakdown, an honest burn-pace line (no runway countdown), and neutral (non-celebratory) volunteer/expense framing. App.tsx fetches the summary for the nonprofit entity (mirroring the federal-context fetch) and passes it down.

Note: `QuickFactsRow` is currently dead code (never rendered) — do NOT revive it. The Funds-on-Hand tile + goal bar live in the new `OrgTransparencyPanel`, which reuses the existing `InsightCard` primitive.
Output: a working donor-facing transparency view for EV, with zero regression on city/county/state/federal pages, verified live by Chris.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md
@src/App.tsx
@src/components/dashboard/PlainLanguageSummary.tsx
@src/components/dashboard/InsightCard.tsx
@src/data/dataLoader.ts
@src/utils/brandColors.ts

<interfaces>
Data source: loadOrgFinancialSummary(orgId, fiscalYear) → OrgFinancialSummary (added in 76-02). EV org id = ee6f34f7-bd85-4387-8d71-4c2ed8cb8fdf.
Fetch precedent: App.tsx federal-context useEffect (~lines 407-411) — keyed on selectedEntity, sets state, .catch hides gracefully. Mirror it for entity_type === 'nonprofit', keyed on (selectedEntity, selectedYear), parsing the FY via parsePeriod(selectedYear).fiscalYear.
Render site: the nonprofit/city branch around App.tsx ~lines 909-924 (the `else` that renders PlainLanguageSummary). OrgTransparencyPanel goes just above PlainLanguageSummary, gated on entity_type === 'nonprofit' && orgSummary present.
InsightCard props: { label, value, subtext?, icon?, variant?: 'default'|'primary'|'highlight' }. Nonprofit money format = exact 2-decimal dollars.
PlainLanguageSummary current volunteer line (~lines 212-221): "All work is done by unpaid volunteers — …$0 in staff compensation." → soften per D-11.
Top expense category for burn-pace copy: PlainLanguageSummary already computes topCategories[0] (largest expense) — reuse for the "mostly on <category>" clause.
Progress metric (D-02): income_net / goal_amount, capped at 100% (D-03). Goal reached when income_net >= goal_amount → show celebratory "Goal reached — thank you!" state.

Decisions enforced:
- D-05/D-06: show burn pace, NOT runway. Do not render runway_months anywhere.
- D-07/D-08: totals sentence + per-source mini-list (income_by_source).
- D-09/D-12: fees are a reduction of income, never an expense line.
- D-10/D-11/D-12: neutral expense framing; $0-staff is a plain fact; no volunteer badge/celebration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx — fetch + hold the nonprofit org summary</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (federal-context useEffect ~407-411; federalContextData useState ~118; the import line 16 for dataLoader fns; parsePeriod usage; the nonprofit render branch ~909-924)
    - src/data/dataLoader.ts (loadOrgFinancialSummary signature from 76-02)
    - src/types/budget.ts (OrgFinancialSummary type from 76-02)
  </read_first>
  <action>
    Import `loadOrgFinancialSummary` (add to the existing dataLoader import) and the `OrgFinancialSummary` type. Add state `const [orgSummary, setOrgSummary] = useState<OrgFinancialSummary | null>(null)`. Add a useEffect keyed on `[selectedEntity, selectedYear]` that, when `selectedEntity?.entity_type === 'nonprofit'`, calls `loadOrgFinancialSummary(selectedEntity.id, parsePeriod(selectedYear).fiscalYear).then(setOrgSummary).catch(() => setOrgSummary(null))`; for any non-nonprofit entity, set `orgSummary` to null (so it never leaks across entities). Mirror the federal-context effect's graceful .catch (hide on failure — no fallback figure).
  </action>
  <acceptance_criteria>
    - src/App.tsx imports loadOrgFinancialSummary and declares an orgSummary state
    - A useEffect fetches the summary only for entity_type === 'nonprofit', keyed on entity + year, and resets to null otherwise
    - On fetch failure the effect sets orgSummary to null (no thrown error, no fallback value)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build OrgTransparencyPanel (Funds on Hand + goal progress)</name>
  <files>src/components/dashboard/OrgTransparencyPanel.tsx</files>
  <read_first>
    - src/components/dashboard/InsightCard.tsx (props + styling to reuse for the Funds-on-Hand tile)
    - src/components/dashboard/QuickFactsRow.tsx (nonprofit exact-2dp money formatting helper to copy — formatCompact for isNonprofit)
    - src/utils/brandColors.ts (brand greens for the progress fill / goal-reached state)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (D-03 cap-at-100% + celebrate; D-06 no runway)
  </read_first>
  <action>
    Create `src/components/dashboard/OrgTransparencyPanel.tsx`, a presentational component taking `{ summary: OrgFinancialSummary; orgName: string }`. Render:
    (a) A "Funds on Hand" InsightCard: value = balance formatted as exact 2-dp dollars, subtext = `As of <balance_as_of>` (format the date readably). Use icon + variant consistent with InsightCard usage.
    (b) A goal-progress indicator, shown ONLY when `summary.goal_amount` is non-null and > 0: a labeled progress bar where pct = min(100, round(income_net / goal_amount * 100)); show `<income_net> of <goal_amount>` and the goal_label. When `income_net >= goal_amount`, fill to 100% and switch the label/treatment to a celebratory "Goal reached — thank you!" state (green). Do NOT show overage beyond 100% (D-03). When goal_amount is null/0, render nothing for the goal (no broken/empty bar).
    Do NOT render runway anywhere. Match the app's existing card/border/spacing vocabulary (Tailwind classes consistent with InsightCard/QuickFactsRow).
  </action>
  <acceptance_criteria>
    - File src/components/dashboard/OrgTransparencyPanel.tsx exists and exports a component taking a `summary: OrgFinancialSummary` prop
    - Renders a "Funds on Hand" label with the balance and an "As of" date subtext
    - Goal bar pct is capped: `Math.min(100, ...)` appears; income_net >= goal_amount renders a "Goal reached" state
    - When goal_amount is null the goal element does not render (guard present)
    - No reference to runway_months in the file
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Render OrgTransparencyPanel for nonprofit in App.tsx</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (the nonprofit/city render branch ~909-924 where PlainLanguageSummary renders; the federal vs non-federal conditional ~901)
    - src/components/dashboard/OrgTransparencyPanel.tsx (the component contract from Task 2)
  </read_first>
  <action>
    Import OrgTransparencyPanel. In the non-federal render branch, just ABOVE the PlainLanguageSummary block, render `{selectedEntity?.entity_type === 'nonprofit' && orgSummary && (<div className="mb-6"><OrgTransparencyPanel summary={orgSummary} orgName={selectedEntity.name} /></div>)}`. Ensure it appears only for nonprofit and only when orgSummary is loaded (so city/county/state pages are untouched).
  </action>
  <acceptance_criteria>
    - src/App.tsx renders OrgTransparencyPanel gated on entity_type === 'nonprofit' && orgSummary
    - The panel renders above PlainLanguageSummary in the layout
    - City/county/state/federal branches are unchanged (panel never renders for them)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: PlainLanguageSummary — fee story, burn pace, neutral framing</name>
  <files>src/components/dashboard/PlainLanguageSummary.tsx</files>
  <read_first>
    - src/components/dashboard/PlainLanguageSummary.tsx (full component — the props interface, isNonprofit branches, the revenue paragraph ~270-305, the volunteer paragraph ~212-221, topCategories computation ~98-101, formatAmount ~123-129)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (D-05 burn pace, D-07/D-08 fee story, D-10/D-11/D-12 neutral framing)
    - src/types/budget.ts (OrgFinancialSummary type)
  </read_first>
  <action>
    Add an optional prop `orgSummary?: OrgFinancialSummary | null` to PlainLanguageSummaryProps (default null). Then, gated on `isNonprofit && orgSummary`:
    (a) INCOME / FEE STORY (D-07/D-08): render a paragraph "Donors gave <income_gross>; after <income_fees> in platform fees, <income_net> reached <entity.name>." (exact 2-dp dollars), immediately followed by a compact per-source list from `orgSummary.income_by_source` — one row per source: "<source>: <gross> → <fee> fee → <net> net" (skip sources with gross 0). This is the nonprofit income paragraph; do NOT also print a separate conflicting gross-income figure from revenueData (avoid double-telling). Keep the existing "primary source being <category>" flavor only if it does not contradict the totals.
    (b) BURN PACE (D-05): when `orgSummary.monthly_burn > 0`, render "<entity.name> currently spends about <monthly_burn>/month, mostly on <topCategories[0].name>." Do NOT render runway.
    (c) NEUTRAL VOLUNTEER FRAMING (D-10/D-11/D-12): change the $0-staff paragraph from the celebratory "All work is done by unpaid volunteers — …$0 in staff compensation." to a plain, year-specific fact: "So far in <fiscalYear>, <entity.name> has paid $0 in staff compensation." (past-year variant: "In <fiscalYear>, <entity.name> paid $0 in staff compensation."). Remove the "All work is done by unpaid volunteers" lead-in and any 100%-to-mission / overhead phrasing. Keep the salariesTotal > 0 branch as-is. The expense-by-category breakdown stays as-is (already neutral) — confirm no celebratory volunteer wording remains.
    Pass `orgSummary` from App.tsx into PlainLanguageSummary (update the call site ~912-922).
  </action>
  <acceptance_criteria>
    - PlainLanguageSummaryProps includes an optional orgSummary prop and App.tsx passes it
    - Rendered nonprofit output contains the fee framing: a "reached" sentence using income_gross/income_fees/income_net, plus a per-source gave→fee→net list driven by income_by_source
    - A burn-pace sentence renders when monthly_burn > 0 and contains no runway/months-remaining language
    - The volunteer paragraph no longer contains the string "All work is done by unpaid volunteers"; the $0-staff statement remains as a neutral year-specific fact
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Human live-app verification (all four EVVIEW, reframed)</name>
  <files>src/App.tsx</files>
  <read_first>
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (success criteria + the two reframes for EVVIEW-02 and EVVIEW-03)
  </read_first>
  <action>
    Run the app (dev server) and have Chris view the EV page for FY2026. Confirm visually: (1) Funds on Hand shows the balance + as-of date; (2) goal-progress bar shows income_net vs goal with cap-at-100% (or "Goal reached" if met); (3) the "Donors gave → fees → reached EV" sentence + per-source breakdown render with real numbers; (4) the burn-pace line shows ("spends about $X/month") and NO runway countdown appears; (5) the $0-staff line is neutral/factual with no all-volunteer celebration; (6) a city page (e.g. Dallas) and the federal page render identically to before (no regression). Capture Chris's sign-off. Write 76-VERIFICATION inputs accordingly.
  </action>
  <acceptance_criteria>
    - Chris confirms all four EVVIEW elements render correctly with real, sourced figures on the live EV page
    - No runway countdown appears anywhere; volunteer framing is neutral (the two reframes honored)
    - At least one city page + the federal page confirmed visually unchanged (no regression)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- tsc clean across App.tsx, OrgTransparencyPanel, PlainLanguageSummary.
- EVVIEW-01: gross→fees→net sentence + per-source breakdown visible.
- EVVIEW-02: honest expense-by-category breakdown, neutral framing, no volunteer celebration.
- EVVIEW-03: Funds on Hand + burn pace; no runway display.
- EVVIEW-04: goal-progress bar (income_net vs goal), cap-at-100% + celebrate.
- Regression: non-nonprofit pages unchanged; orgSummary never leaks across entities.
- Chris signs off in the live app.
</verification>

<must_haves_check>
All four EVVIEW requirements are visible on the EV page with real, sourced data, honoring the two CONTEXT reframes (no runway display; neutral non-celebratory volunteer/expense framing). The actual-spend graphic remains Phase 77.
</must_haves_check>
