---
phase: 76-donor-facing-transparency-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/budget.ts
  - src/data/dataLoader.ts
autonomous: true
requirements: [EVVIEW-01, EVVIEW-03]
must_haves:
  truths:
    - "A TypeScript interface OrgFinancialSummary mirrors the live ev-accounts API contract exactly (all org_financial_summary columns incl. income_by_source, recon_*, runway_months, goal_amount, goal_label, sourcing columns)"
    - "loadOrgFinancialSummary(orgId, fiscalYear) fetches GET /api/treasury/orgs/:id/financial-summary?fiscal_year=YYYY and returns the typed object"
    - "The loader throws on a non-OK response (no silent fallback figures — consistent with loadFederalContext / D-06)"
    - "The fetch is session-cached per (orgId, fiscalYear) like loadFederalContext"
  artifacts:
    - path: "src/types/budget.ts"
      provides: "OrgFinancialSummary + IncomeBySource + ReconBySource interfaces"
      contains: "OrgFinancialSummary"
    - path: "src/data/dataLoader.ts"
      provides: "loadOrgFinancialSummary fetch helper"
      contains: "loadOrgFinancialSummary"
  key_links:
    - from: "loadOrgFinancialSummary"
      to: "ev-accounts API GET /treasury/orgs/:id/financial-summary"
      via: "API_BASE fetch mirroring loadFederalContext"
      pattern: "orgs/"
---

<objective>
Add the frontend data-access layer for the EV reconciled summary: a typed `OrgFinancialSummary` interface and a `loadOrgFinancialSummary()` fetch helper that calls the already-built ev-accounts endpoint `GET /api/treasury/orgs/:id/financial-summary?fiscal_year=YYYY`. Pure additive plumbing — no UI, no App.tsx wiring (that is 76-03). This unblocks the rendering plan and can run in parallel with the data-layer plan (76-01) since it shares no files.

Purpose: mirror the established `loadFederalContext` precedent (cached, throws-on-failure, no fallback) so 76-03 just consumes a typed promise. The endpoint already returns goal_amount/goal_label as null today and real values once 76-01's migration + loader run — the type accounts for both.
Output: one new interface group + one fetch function, both compiling clean under tsc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md
@src/types/budget.ts
@src/data/dataLoader.ts

<interfaces>
Live API contract (built by EV-Accounts, see CONTEXT "API endpoint — BUILT"):
  GET /api/treasury/orgs/:id/financial-summary?fiscal_year=YYYY
  - optionalAuth (public); 422 on bad UUID/fiscal_year; fiscal_year omitted → latest FY; 404 if no row.
  - Response = the OrgFinancialSummary shape below (numbers coerced; jsonb passed straight through).
EV org id: ee6f34f7-bd85-4387-8d71-4c2ed8cb8fdf.
Precedent to mirror: loadFederalContext in src/data/dataLoader.ts (module-level cache var, API_BASE fetch, throw on !ok, throw if empty) + FederalContext interface group in src/types/budget.ts (lines ~206-232).

Target OrgFinancialSummary shape (from CONTEXT canonical refs):
  municipality_id: string; fiscal_year: number;
  balance: number; balance_as_of: string;
  monthly_burn: number; burn_window_months: number; runway_months: number | null;
  income_gross: number; income_fees: number; income_net: number;
  income_by_source: { source: string; gross: number; fee: number; net: number }[];
  recon_variance: number | null; recon_explanation: string | null;
  recon_by_source: { source: string; platform_net: number; bank_deposits: number; variance: number }[];
  unmatched_deposits: { date: string; amount: number; description: string }[];
  goal_amount: number | null; goal_label: string | null;
  source_name: string; source_url: string | null; source_date: string;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OrgFinancialSummary interfaces to budget.ts</name>
  <files>src/types/budget.ts</files>
  <read_first>
    - src/types/budget.ts (FederalAnnualSummaryRow / FederalContextMetric / FederalContext group ~lines 206-232 — match the export style + section-comment convention)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (the OrgFinancialSummary contract under canonical refs)
  </read_first>
  <action>
    Append an exported interface group to src/types/budget.ts with a section comment header (e.g. `// ── Org financial summary (Phase 76) — /api/treasury/orgs/:id/financial-summary ──`). Define `IncomeBySource` ({ source: string; gross: number; fee: number; net: number }), `ReconBySource` ({ source: string; platform_net: number; bank_deposits: number; variance: number }), `UnmatchedDeposit` ({ date: string; amount: number; description: string }), and `OrgFinancialSummary` with EXACTLY the fields listed in the interfaces block above (use the nullable types as written: runway_months, recon_variance, recon_explanation, goal_amount, goal_label, source_url are `| null`).
  </action>
  <acceptance_criteria>
    - src/types/budget.ts contains `export interface OrgFinancialSummary` with fields balance, monthly_burn, income_net, income_by_source, goal_amount, goal_label, source_name
    - goal_amount and goal_label are typed `number | null` and `string | null` respectively
    - income_by_source is typed `IncomeBySource[]`
    - `npx tsc --noEmit` (or the project's typecheck script) exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add loadOrgFinancialSummary to dataLoader.ts</name>
  <files>src/data/dataLoader.ts</files>
  <read_first>
    - src/data/dataLoader.ts (loadFederalContext ~lines 183-202 — cache var + fetch + throw pattern; API_BASE definition lines 12-14; the type import line 8)
    - src/types/budget.ts (the new OrgFinancialSummary type to import)
  </read_first>
  <action>
    Add `OrgFinancialSummary` to the type import from '../types/budget'. Add a module-level cache `Map<string, OrgFinancialSummary>` keyed by `${orgId}:${fiscalYear}`. Export `async function loadOrgFinancialSummary(orgId: string, fiscalYear: number): Promise<OrgFinancialSummary>` that returns the cached value if present, else fetches `${API_BASE}/treasury/orgs/${orgId}/financial-summary?fiscal_year=${fiscalYear}`, throws `Error(\`Org financial summary API returned ${response.status}\`)` on !ok, parses JSON, caches, and returns it. Mirror loadFederalContext's throw-on-failure discipline (no fallback object). Add it to the clearCache() reset if a cache map is module-scoped.
  </action>
  <acceptance_criteria>
    - src/data/dataLoader.ts exports `loadOrgFinancialSummary`
    - The function fetches the path `/treasury/orgs/${orgId}/financial-summary?fiscal_year=${fiscalYear}` and throws on a non-OK response
    - Result is cached per (orgId, fiscalYear) — a second call with the same args does not refetch
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- tsc clean with the new interface + loader.
- loadOrgFinancialSummary hits the correct path, caches, and throws on failure (loadFederalContext parity).
- No UI/App.tsx changes in this plan (those are 76-03).
</verification>

<must_haves_check>
EVVIEW-01/03 data access in place: the donor view (76-03) can now fetch a fully-typed reconciled summary (balance, burn, income gross/fees/net + per-source, goal) for the EV org. Rendering happens in 76-03.
</must_haves_check>
