---
phase: 106-verification-source-chain-audit-uat-ver-03-ver-04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase106-rederive.mjs
  - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-REDERIVATION.md
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "Every sampled FY's General-Fund printed total, blind-re-extracted from the source ACFR PDF, ties to the live treasury.budgets value at exactly 0 delta (or a documented-explained delta)"
    - "The re-derivation imports zero scripts/process*.js loaders — it is independent of the loader's hardcoded map"
    - "The risk-weighted sample covers both deepened bookends (CA, NY, FL), every negative-clamp year (FL FY2021, IL FY2022), PA + IL bookends, and 1-2 documented random middle years"
    - "Any non-zero delta is either explained-and-recorded or fixed in-phase — no silent tolerance band"
  artifacts:
    - path: "scripts/verify-phase106-rederive.mjs"
      provides: "Loader-independent blind ACFR re-derivation harness for the v2.12 risk-weighted sample"
      contains: "pdftotext"
    - path: ".planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-REDERIVATION.md"
      provides: "The independent re-derivation log: sample list, per-FY independent total, live DB total, delta, disposition"
  key_links:
    - from: "scripts/verify-phase106-rederive.mjs"
      to: "source ACFR PDFs"
      via: "pdftotext -table re-extraction (no loader import)"
      pattern: "pdftotext"
    - from: "scripts/verify-phase106-rederive.mjs"
      to: "treasury.budgets"
      via: "DB read of stored total_budget per (municipality_id, fiscal_year, dataset_type)"
      pattern: "budgets"
---

<objective>
Independently re-derive (VER-03) a risk-weighted sample of v2.12's added state-FY-datasets — the deepened CA / NY / FL history (Phase 104) and the new PA + IL nodes (Phase 105) — by BLIND re-extraction from each source ACFR PDF, then diff the independent General-Fund total against the live `treasury.budgets` value. This is the only method that catches a transposed-digit transcription error (the loader's own `validate()` checks the map against itself and cannot).

Purpose: Prove the v2.12 data is real and faithfully transcribed from its own source — not loader self-report (VER-03, the Phase 102 16/16-tie precedent scaled to v2.12's larger added surface).
Output: `scripts/verify-phase106-rederive.mjs` (loader-independent harness) + `106-REDERIVATION.md` (the per-FY tie log).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
@.planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-PA-IL-SOURCES.md
@scripts/verify-phase102-rederive.mjs

<interfaces>
<!-- The Phase 102 harness is the structural template. Reuse its env-loading, native-https dbGet,
     and pdftotext-via-execFileSync pattern. The two changes for Phase 106 are: (1) the sample set
     below, and (2) the tolerance rule — D-03 forbids the $10M rounding fallback; the bar is EXACT 0. -->

D-03 tolerance change (CRITICAL): verify-phase102-rederive.mjs PASSed on `abs(delta) === 0 OR abs(delta) <= 10_000_000`. Phase 106 D-03 forbids the band: PASS only on abs(delta) === 0. A non-zero delta is FAIL → either explained-and-recorded (published restatement / printed rounding line / units note) or a defect to fix in-phase.

Live DB municipality_ids (from 105-PA-IL-LOADLOG.md):
  PA = d4a4aadc-f91e-45e4-852f-2cf21e177de5
  IL = ac8b3dee-b431-48d0-9f59-deea46c85948
  (CA/NY/FL ids: resolve at runtime by entity name via the same DB lookup the 102 harness uses.)

Units: NY ×1,000,000 (millions); CA/FL/PA/IL ×1,000 (thousands).

Per-year source URL patterns (for blind re-fetch — DO NOT read the loader SOURCES maps):
  NY  FY2003-FY2014: https://www.osc.ny.gov/files/reports/finance/pdf/comprehensive-annual-financial-report-{YYYY}.pdf
  CA  FY2008-FY2019: https://www.sco.ca.gov/Files-ARD/CAFR/cafr{NN}web.pdf   (NN = 08..19)
  FL  FY2021:        https://www.myfloridacfo.com/docs-sf/default-source/transparency-docs/cafr/fye-2021-state-of-florida-annual-comprehensive-financial-report.pdf
  PA  FY2016-FY2023: https://www.pa.gov/content/dam/copapwp-pagov/en/budget/documents/publications-and-reports/annualfinancialreport/june-30-{YYYY}-acfr.pdf
  PA  FY2024-FY2025: same path but june-30-{YYYY}%20acfr.pdf (space, not hyphen)
  IL  per-year variant naming under https://illinoiscomptroller.gov/__media/sites/comptroller/assets/File/CAFR/ : "ACFR Final 2021.pdf", "ACFR Final 2023 - Bookmarked.pdf", etc. (FINAL audited only — never the Interim/unaudited file)

CA SCO soft-404 caution: HTTP 200 + HTML returned for missing files — filter by Content-Type: application/pdf and multi-MB payload size, NOT by status code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the loader-independent re-derivation harness for the v2.12 risk-weighted sample</name>
  <read_first>
    - scripts/verify-phase102-rederive.mjs (the structural template — env load, native-https dbGet, pdftotext execFileSync, GF "Total revenues"/"Total expenditures" line extraction)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (per-year URL patterns + units for CA/NY/FL)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-PA-IL-SOURCES.md (per-year URL patterns + units + IL "Final audited only" rule for PA/IL)
    - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md (D-01 sample definition, D-02 blind method, D-03 exact-0 tolerance)
  </read_first>
  <action>
    Create scripts/verify-phase106-rederive.mjs modeled on verify-phase102-rederive.mjs (reuse its env-loading from ../.env.local/../.env, its native-https dbGet, and its pdftotext `-table` extraction of the GENERAL-FUND column "Total revenues" and "Total expenditures" printed lines). Import ZERO scripts/process*.js modules — re-key the GF column independently (D-02 blind re-extract).

    Encode this risk-weighted sample (D-01, ~18 FY-dataset ties; each FY checks BOTH revenue and operating unless noted):
      - CA deepened bookends: FY2008 (oldest) + FY2019 (newest deepened); plus random middle FY2013 (documented choice).
      - NY deepened bookends: FY2003 (oldest, exercises ×millions scaling) + FY2014 (newest deepened); plus random middle FY2009 (documented choice).
      - FL deepened: FY2021 (only deepened FY AND a negative-clamp year — revenue side has "Investment earnings (losses)" −$398,287K).
      - PA bookends: FY2016 (oldest) + FY2025 (newest).
      - IL bookends: FY2021 (oldest) + FY2025 (newest); plus FY2022 (negative-clamp: "Interest and other investment income" −$197,857K).
    Document the random-middle-year choices (CA FY2013, NY FY2009) inline so the sample is reproducible (D-01 Claude's-discretion clause). No NY/CA market-loss negative years exist in the 104 window (104 gap log records none) — note that the only negative-clamp years in the v2.12 surface are FL FY2021 and IL FY2022.

    For each sample FY: re-fetch the source PDF via the per-year URL pattern from the SOURCES recon files (NOT the loader maps), filter CA fetches by Content-Type application/pdf + payload size (SCO soft-404 guard), run `pdftotext -table`, re-key the GF-column printed total, multiply by the state unit multiplier (NY ×1,000,000; others ×1,000), and diff against the stored treasury.budgets total_budget for that (municipality_id, fiscal_year, dataset_type).

    Set the PASS bar to abs(delta) === 0 EXACTLY (D-03) — do NOT carry over the 102 harness's $10M rounding fallback. Print PASS/FAIL per check; exit 0 only if every check is exact-0; exit 2 if any check is non-zero (so a real delta surfaces as a hard failure to be explained-or-fixed in Task 2). For the negative-clamp years (FL FY2021, IL FY2022) the bar is the printed root/control total (which already nets the negative), matching how the loader stored total_budget.
  </action>
  <verify>
    <automated>node scripts/verify-phase106-rederive.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase106-rederive.mjs exists and imports no scripts/process*.js module (grep -v '^//' scripts/verify-phase106-rederive.mjs | grep -c "process" against import lines = 0 loader imports).
    - The script runs all ~18 FY-dataset checks and prints a per-check delta.
    - PASS gate is abs(delta) === 0 exactly (no $10M band present in the source).
    - Expected exact-tie values to confirm against (from 104 gap log / 105 load log): NY FY2003 operating $40,910,000,000 + revenue $29,250,000,000; CA FY2008 operating $98,975,042,000 + revenue $97,774,378,000; CA FY2019 operating $129,113,153,000 + revenue $140,503,627,000; FL FY2021 operating $37,277,963,000 + revenue $46,989,188,000; PA FY2016 operating $56,135,869,000 + revenue $56,741,506,000; PA FY2025 operating $94,758,255,000 + revenue $92,414,817,000; IL FY2021 operating $59,523,406,000 + revenue $63,136,008,000; IL FY2022 revenue root $73,204,339,000 (clamp year); IL FY2025 operating $75,456,922,000 + revenue $78,342,927,000.
  </acceptance_criteria>
  <done>Harness runs every sampled FY blind from source, compares to live DB at exact-0, exits 0 (all tie) or 2 (a delta surfaced for Task 2 disposition).</done>
</task>

<task type="auto">
  <name>Task 2: Disposition every delta and write 106-REDERIVATION.md</name>
  <read_first>
    - .planning/milestones/v2.11-phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-VERIFICATION.md (the SC1 report-structure precedent: "16/16 checks PASS with delta = $0 (exact)")
    - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md (D-03 explain-or-fix; D-05 fix data/source defects in-phase, defer cosmetic)
    - The Task 1 harness output (the per-check delta table)
  </read_first>
  <action>
    Run scripts/verify-phase106-rederive.mjs and capture every check's result. Write 106-REDERIVATION.md as the independent re-derivation log: a table with columns State | FY | Dataset | Independent re-extracted total | Live DB total_budget | Delta | Disposition. Record the chosen random-middle years (CA FY2013, NY FY2009) and the negative-clamp dispositions (FL FY2021, IL FY2022 — note the root total nets the clamped-negative line, matching the loader).

    For any non-zero delta (D-03): either (a) EXPLAIN it with a documented cause — a published restatement between ACFR editions, a printed "rounding" line, or a units/scaling note — and record the explanation verbatim with its source location, OR (b) declare it a defect. If a defect is found, per D-05 it MUST be fixed in-phase before sign-off: re-run the relevant loader through its guarded never-overwrite path (NOT treasury_sync_city_budget, which is not source-safe — use the loader's own treasury_sync_budget_tree path), then re-run the harness to confirm the corrected exact-0 tie, and record the before/after in the log. Do NOT introduce or accept any numeric tolerance band.

    State the headline verdict at the top (e.g. "N/N exact ties, 0 deltas" or "N-1/N exact, 1 explained: <reason>"). This file is what the cohort audit (Plan 02) and the gsd-verifier read for VER-03 part (a).
  </action>
  <verify>
    <automated>node scripts/verify-phase106-rederive.mjs; test -f .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-REDERIVATION.md && grep -qiE "delta|tie" .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-REDERIVATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 106-REDERIVATION.md exists with the full per-FY sample table (State | FY | Dataset | independent total | DB total | delta | disposition).
    - Every row has either delta = $0 OR a documented explanation OR a recorded in-phase fix (with before/after + re-run confirmation).
    - No numeric tolerance band appears anywhere in the disposition logic.
    - The random-middle-year choices (CA FY2013, NY FY2009) are recorded so the sample is reproducible.
    - The harness exits 0 after any in-phase fixes (every retained check ties exact-0, or the only non-zero rows are explicitly explained).
  </acceptance_criteria>
  <done>106-REDERIVATION.md records the independent tie for every sampled FY; all deltas are 0, explained, or fixed-in-phase; harness exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → source ACFR PDFs (public web) | Untrusted external bytes (PDF) enter the harness; CA SCO can return HTTP 200 + HTML (soft-404) for a missing file. |
| local script → treasury.budgets (read) | Read-only DB access via service key (env-only). No write in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | source ACFR PDF fetch (CA SCO soft-404) | mitigate | Filter CA fetches by Content-Type: application/pdf + multi-MB payload size, not HTTP status — a soft-404 HTML page must FAIL the check, never silently tie to 0. |
| T-106-02 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Key is read from ../.env.local/../.env only; never inlined in the script or the log. Read-only DB use. Standing project practice. |
| T-106-03 | Denial of Service | external PDF host (SCO/OSC/PA/IL) flaky | accept | Read-only re-fetch; if a host is intermittently unavailable, re-run (no DB state at risk). Low value, no data integrity impact. |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — harness uses only Node built-ins + existing @supabase/supabase-js (already in package.json, used by the 102 harness). No install task; no [ASSUMED]/[SUS] packages. |

This phase's threat surface is minimal (read-only DB queries + local PDF re-extraction). The only data-integrity-relevant threat is T-106-01 (soft-404 silently passing) — mitigated by content-type/size filtering.
</threat_model>

<verification>
- scripts/verify-phase106-rederive.mjs runs all ~18 FY-dataset ties blind from source and exits 0 (every retained check ties exact-0).
- 106-REDERIVATION.md records each tie with delta + disposition; any non-zero delta is explained or fixed-in-phase.
- No loader import in the harness (independence proven); no $10M (or any) tolerance band.
</verification>

<success_criteria>
- VER-03 part (a): each deepened pilot (CA, NY, FL) + each new state (PA, IL) reconciled independently from its own ACFR, exact-0 (or explained), recorded in 106-REDERIVATION.md.
</success_criteria>

<output>
Create `.planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-01-SUMMARY.md` when done.
</output>
