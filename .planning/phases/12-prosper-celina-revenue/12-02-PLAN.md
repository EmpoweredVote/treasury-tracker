---
phase: 12-prosper-celina-revenue
plan: "02"
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Prosper revenue data is visible in the app for all FYs that passed validation in 12-01"
    - "Per-capita revenue figure is visible for Prosper in the app ($/resident shown)"
    - "Validation results from 12-01 are documented and reviewed"
  artifacts:
    - path: ".planning/phases/12-prosper-celina-revenue/12-01-SUMMARY.md"
      provides: "Documented validation console output from Prosper loader run"
  key_links:
    - from: "treasury.data_sources (Prosper, dataset_type=revenue)"
      to: "app revenue display"
      via: "last_synced_at non-null enables revenue rows to appear in treasury budget tree"
      pattern: "last_synced_at IS NOT NULL"
---

<objective>
Verify that the Prosper revenue data loaded in plan 12-01 is visible in the app and that
per-capita revenue figures are displaying correctly. This is a human-verify checkpoint: no
code is written in this plan. The executor confirms the app state matches the expected outcomes
and documents any issues for remediation.

Purpose: The 12-01 loader runs autonomously but only a human can confirm the app is showing
the data correctly. This gate exists so the SUMMARY documents what actually shipped.

Output: Human confirmation that Prosper revenue is live in the app (or documented issues)
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-prosper-celina-revenue/12-CONTEXT.md
@.planning/phases/12-prosper-celina-revenue/12-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 12-01 built and ran processProsperjRevenuePDF.js, which:
    - Extracted revenue line items from Prosper ACFR PDFs (FY2023, FY2024, FY2025)
    - Validated each FY's extracted total against a hardcoded expected value (±20%)
    - Loaded revenue rows to DB and set last_synced_at for FYs that passed validation
    - Corrected the FY2023/FY2024 data_source base_url values (were pointing to FY2025 PDF)

    The 12-01-SUMMARY.md documents which FYs passed/failed and the exact validation console output.
  </what-built>
  <how-to-verify>
    1. Open https://treasurytracker.empowered.vote in a browser
    2. Navigate to Prosper TX
    3. Check that revenue data is visible for each FY that passed validation in 12-01
       - Revenue amounts should show fund/source breakdowns (General Fund revenue line items)
       - Amounts should be plausible (FY2025 total near $108M, not $768M from the Haiku failure)
    4. Check that per-capita revenue is displayed ($/resident figure visible in the UI)
       - Population was loaded in Phase 11; revenue loaded in 12-01
       - Both must be present for per-capita to render
    5. If any FY failed validation in 12-01: confirm that FY shows NO revenue data in the app
       (last_synced_at was NOT set, so the app should not display that year's revenue)
    6. Confirm Celina is NOT showing any new revenue data yet (12-03 has not run)

    If data is NOT visible:
    - Check whether last_synced_at is set: run Supabase query
      `SELECT fiscal_year, last_synced_at FROM treasury.data_sources WHERE name ILIKE '%Prosper%Revenue%'`
    - If last_synced_at is null for all rows: the loader may have failed validation silently —
      re-run `node scripts/processProsperjRevenuePDF.js --verbose` and report the output
    - If last_synced_at is set but data is not visible: investigate whether the app gates on a
      different field or whether the budget tree RPC call completed correctly
  </how-to-verify>
  <resume-signal>
    Type "approved" if Prosper revenue data is visible in the app and per-capita is showing.
    Or describe what you see (e.g., "FY2025 visible, FY2023/2024 not loaded — validation failed
    with X% diff") so the SUMMARY can document the actual state.
  </resume-signal>
</task>

</tasks>

<verification>
Human has confirmed via the checkpoint whether Prosper revenue data is visible in the app.
Result (approved or issues) is documented in the SUMMARY.
</verification>

<success_criteria>
- Human has reviewed Prosper revenue display in the app
- Outcome documented in SUMMARY.md (visible FYs, any validation failures, per-capita status)
- No further action needed in this plan (issues handled in gap closure if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-prosper-celina-revenue/12-02-SUMMARY.md`

Include:
- What the human verified (which FYs are visible, per-capita status)
- Any issues found and whether they require gap closure
- Confirmation that 12-01 validation results match what's shown in the app
</output>
