---
phase: 45-federal-visualization
plan: 03
type: execute
wave: 3
depends_on: [45-02]
files_modified:
  - src/components/federal/LensToggle.tsx
  - src/components/federal/MethodologyPanel.tsx
  - src/components/federal/ScaleToggle.tsx
  - src/App.tsx
  - src/components/BudgetVisualization.tsx
  - scripts/loadFederalMTS.js
autonomous: false
requirements:
  - VIZ-03
  - VIZ-04
  - VIZ-05
  - VIZ-06
must_haves:
  truths:
    - "Money Out on the US page defaults to the function lens with a visible toggle to the agency lens; toggling swaps the tree without page reload; lens=agency survives in the URL"
    - "The visualization header shows the active dataset's SourceChip (from the budgets response source fields)"
    - "Scale toggle offers $ / per person / % of total; per-person divides by Census population with the formula disclosed; per-taxpayer included ONLY if an IRS-sourced return count was verified and loaded"
    - "MethodologyPanel discloses: outlays-vs-budget-authority choice, visual-vs-official totals (+$521.1B / +$1,895.5B), offsetting receipts handling, FYTD caveat — every claim chipped"
  artifacts:
    - path: "src/components/federal/MethodologyPanel.tsx"
      provides: "VIZ-06 + Phase-44 owed disclosures"
      contains: "budget authority"
    - path: "src/components/federal/LensToggle.tsx"
      provides: "function/agency lens switch"
      contains: "Who spends it"
  key_links:
    - from: "src/components/federal/LensToggle.tsx"
      to: "loadBudgetData(..., 'federal_agency')"
      via: "App.tsx federal lens state"
      pattern: "federal_agency"
---

<objective>
Complete the federal interaction layer: the two-lens drill (the IA's citizen's-choice decision), comparative-scale aids, per-dataset source chips, and the methodology disclosures Phase 44 left owed. After this plan every VIZ requirement is implemented.

Output: three new components + App/BudgetVisualization wiring; optional IRS taxpayer metric; build green.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-federal-visualization/45-CONTEXT.md
@.planning/phases/45-federal-visualization/45-02-SUMMARY.md
@.planning/phases/44-core-federal-data-load/44-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: LensToggle + dataset wiring</name>
  <files>src/components/federal/LensToggle.tsx, src/App.tsx</files>
  <action>
    LensToggle: segmented control, two options — "What it's for" (operating) / "Who spends it" (federal_agency). Props {lens, onChange}. Match DatasetTabs visual idiom; aria-pressed semantics.
    App.tsx: federal-only state `federalLens` ('operating' | 'federal_agency'), default 'operating'. Rendered between DatasetTabs and BudgetVisualization ONLY when entity_type==='federal' AND activeDataset==='operating' (Money In has one lens). When lens==='federal_agency', the visualization + its data load use 'federal_agency' instead of 'operating' (loadBudgetData already takes the dataset string; ensure the load effect keys on federalLens). Navigation path resets on lens switch. URL: `lens=agency` param read/written alongside the existing year/dataset syncURL logic. Lens resets to default when switching away from the US entity.
  </action>
  <verify><automated>tsc + build; dev: toggle swaps 18-function tree ↔ 29-department tree; URL param round-trips; non-federal pages show no toggle</automated></verify>
  <acceptance_criteria>
    - VIZ-03 drill behavior complete; deep-link with lens=agency lands on the agency tree
  </acceptance_criteria>
  <done>Citizens choose their lens; default answers "what's it for".</done>
</task>

<task type="auto">
  <name>Task 2: per-dataset SourceChip on the visualization header</name>
  <files>src/App.tsx, src/components/BudgetVisualization.tsx</files>
  <action>
    Thread the active budget's source fields (source_display_name, source_dataset_url, generated_at — from the 45-01 budgets response) into BudgetVisualization as an optional prop; render SourceChip beside the visualization title when present. Federal datasets always have it; municipal datasets render nothing until the sourcing backfill milestone (prop undefined → no chip; zero visual change for cities).
  </action>
  <verify><automated>tsc + build; US page: chip shows "OMB Public Budget Database · fetched 2026-06-12" on function lens, "Treasury Fiscal Data" on agency lens + Money In; Plano: no chip</automated></verify>
  <acceptance_criteria>
    - VIZ-04 satisfied for the drill area; additive prop, no municipal change
  </acceptance_criteria>
  <done>Every federal tree is visibly attributed at the point of use.</done>
</task>

<task type="auto">
  <name>Task 3: ScaleToggle (VIZ-05) — with IRS verify-first for per-taxpayer</name>
  <files>src/components/federal/ScaleToggle.tsx, src/App.tsx, scripts/loadFederalMTS.js</files>
  <action>
    VERIFY-FIRST (per-taxpayer denominator): attempt to fetch an official IRS individual-return count (IRS SOI / IRS Data Book at irs.gov — likely bot-blocked like CBO/GAO; try api.data.gov-hosted alternatives too). Requirements: official IRS publication, parseable, with a stable URL. If verified: add a `tax_returns_filed` metric load to scripts/loadFederalMTS.js metrics section (value, tax year, exact URL) and include "per taxpayer" in the toggle. If NOT verifiable: OMIT per-taxpayer, record the gap explicitly in the summary + 45-VERIFICATION (VIZ-05 partial: unsourced denominators are banned by ground rule 3).
    ScaleToggle: segmented control `$` / `per person` / `% of total` (+ `per taxpayer` if verified). Scaling applies to the federal visualization display values: per-person = value / population (selectedEntity.population, Census 2024); % of total = value / displayed dataset total. Tooltip on each mode states the formula and the denominator's source ("÷ 340,110,988 — US population, Census Vintage 2024" with chip). Implementation: a display-transform layer in the federal branch (transform amounts before they reach the visualization, or a formatter context — pick the lighter touch consistent with BudgetVisualization's props; do NOT mutate loaded data).
  </action>
  <verify><automated>tsc + build; dev: per-person mode shows e.g. Social Security ≈ $4,647/person (1,580.7e9/340,110,988); formulas in tooltips; IRS decision recorded</automated></verify>
  <acceptance_criteria>
    - Arithmetic on sourced numbers only; formula disclosed at point of use; per-taxpayer present IFF sourced
  </acceptance_criteria>
  <done>VIZ-05 satisfied with disclosed math.</done>
</task>

<task type="auto">
  <name>Task 4: MethodologyPanel (VIZ-06 + owed disclosures)</name>
  <files>src/components/federal/MethodologyPanel.tsx, src/App.tsx</files>
  <action>
    Collapsible panel ("How to read these numbers") at the bottom of the federal top-level page, expanded state in URL hash optional. Content sections, every figure chipped, numbers pulled from /federal/context metrics where they exist (excluded_* and offsets_* metric rows) rather than hardcoded:
    1. WHAT WE COUNT: outlays (money actually spent), consistently, from Treasury/OMB — not budget authority (what Congress authorized). One-line distinction.
    2. VISUAL vs OFFICIAL TOTALS: function view total ($7,532.2B) = official net outlays ($7,011.1B) before subtracting $521.1B of net-negative categories (largest: trust-fund interest receipts, federal employer retirement contributions); agency view ($8,905.0B) before $1,895.5B of offsetting receipts. Render these three numbers from the context payload (annual_summary FY2025 + summing excluded_*/offsets_* metrics) so they can never drift from the data.
    3. NEGATIVE MONEY: what offsetting receipts are (one plain sentence); where they live (line items marked "(offsetting)").
    4. PARTIAL YEAR: FY2026 strip caveat.
    5. DEPTH BOUNDARY: account-level is the deepest sourced outlay data; program-level detail exists only as obligations and is not mixed in.
    Tone: plain language, no editorializing, every paragraph ends with its SourceChip.
  </action>
  <verify><automated>tsc + build; panel renders with live-computed figures matching 44-VERIFICATION's numbers; all chips link</automated></verify>
  <acceptance_criteria>
    - VIZ-06 satisfied; both Phase-44 owed disclosures discharged; zero hardcoded disclosure figures
  </acceptance_criteria>
  <done>The honesty layer is in the product, not just the planning docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IRS download → metric | Verify-first; sanity band (100M–200M returns); official irs.gov/api.data.gov URLs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Tampering | IRS figure | mitigate | Sanity band + halt; omit feature if unverifiable |
| T-45-04 | Info integrity | scale math | mitigate | Display-transform only; loaded data never mutated; formulas disclosed |
| T-45-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
1. Lens toggle, source chips, scale toggle, methodology panel all live on the US page
2. Disclosure figures computed from data, matching 44-VERIFICATION
3. Non-federal entities pixel-identical
</verification>

<success_criteria>
- VIZ-03 through VIZ-06 complete (VIZ-05 per-taxpayer present or explicitly gap-documented)
</success_criteria>

<output>
Create .planning/phases/45-federal-visualization/45-03-SUMMARY.md when done
</output>
