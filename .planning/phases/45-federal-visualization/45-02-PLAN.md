---
phase: 45-federal-visualization
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - src/components/federal/FederalLanding.tsx
  - src/components/federal/DeficitStrip.tsx
  - src/components/federal/FirstSplitBands.tsx
  - src/components/federal/ThisYearStrip.tsx
  - src/components/federal/SourceChip.tsx
  - src/data/dataLoader.ts
  - src/types/budget.ts
  - src/App.tsx
autonomous: true
requirements:
  - VIZ-01
  - VIZ-02
  - VIZ-04
must_haves:
  truths:
    - "The US page's top section shows: deficit strip (receipts vs outlays bars, labeled gap, debt chip), proportional Mandatory/Discretionary/Net Interest bands, and the FY2026 'this year so far' strip — and is NOT an icicle"
    - "Every figure in the landing block has a SourceChip linking to its source URL with fetch date"
    - "PlainLanguageSummary is suppressed for federal; non-federal entities render exactly as before"
    - "All landing figures come from /federal/context (official numbers), never from tree totals"
  artifacts:
    - path: "src/components/federal/FederalLanding.tsx"
      provides: "Federal top-level landing composition"
      contains: "DeficitStrip"
    - path: "src/components/federal/SourceChip.tsx"
      provides: "Reusable source attribution pill"
      contains: "fetched"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/federal/FederalLanding.tsx"
      via: "entity_type === 'federal' branch at top-level dashboard"
      pattern: "FederalLanding"
---

<objective>
Build the federal landing experience: the two structural facts citizens most lack (where the money goes at the highest level; that spending exceeds revenue), rendered proportionally with official sourced figures. This is the IA decision from recon made real (VIZ-01, VIZ-02) plus the live strip's data plumbing.

Output: five new components + App.tsx integration; build green; visual check on the live US page.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-federal-visualization/45-CONTEXT.md
@.planning/phases/45-federal-visualization/45-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: data layer — loadFederalContext + types</name>
  <files>src/data/dataLoader.ts, src/types/budget.ts</files>
  <action>
    Types: FederalAnnualSummaryRow, FederalContextMetric, FederalContext (per the 45-01 response shape doc). dataLoader: loadFederalContext() fetching `${API_BASE}/treasury/federal/context`, cached like loadBudgetData, throws on failure (D-06: no fallback data).
  </action>
  <verify><automated>tsc passes; manual fetch in dev console returns 64 rows</automated></verify>
  <acceptance_criteria>
    - One cached fetch; no hardcoded figures anywhere in the data layer
  </acceptance_criteria>
  <done>Components have typed, sourced data.</done>
</task>

<task type="auto">
  <name>Task 2: SourceChip component</name>
  <files>src/components/federal/SourceChip.tsx</files>
  <action>
    Props: { sourceName: string; sourceUrl: string; fetchDate?: string | null; compact?: boolean }. Renders a small pill: "{sourceName}{fetchDate ? ` · fetched ${fetchDate}` : ''} ↗" as an <a target="_blank" rel="noopener noreferrer">. Styling: existing token classes (text-xs, ev-gray-500, border-[#E2EBEF], dark:* variants — match DatasetTabs pill idioms). compact drops the date. aria-label "Data source: {sourceName}".
    This component is the VIZ-04 workhorse — keep it generic; cities adopt it in a later milestone (sourcing backfill, REQUIREMENTS Future).
  </action>
  <verify><automated>tsc; renders in all four landing spots in Task 4</automated></verify>
  <acceptance_criteria>
    - Generic, reusable, dark-mode correct, external-link safe
  </acceptance_criteria>
  <done>One consistent chip for every sourced figure.</done>
</task>

<task type="auto">
  <name>Task 3: DeficitStrip + FirstSplitBands + ThisYearStrip</name>
  <files>src/components/federal/DeficitStrip.tsx, src/components/federal/FirstSplitBands.tsx, src/components/federal/ThisYearStrip.tsx</files>
  <action>
    All three consume FederalContext props (no fetching inside; FederalLanding fetches once).

    DeficitStrip (VIZ-02): FY2025 official figures. Two horizontal bars on a shared scale: Money In $5,236.4B vs Money Out $7,011.1B; the outlay bar's overhang segment visually distinct (hatched/600-weight tint) and labeled "Deficit: $1,774.7B — about 25¢ of every dollar spent was borrowed" (compute 25% from the two sourced numbers; formula in title attr). Right-aligned chip: "Total debt: $39.2T (as of {as_of_date})" from metrics.total_public_debt. SourceChips: one for OMB (annual summary), one for Debt to the Penny.

    FirstSplitBands (VIZ-01): one full-width proportional stacked bar of FY2025 outlays: Mandatory $4,165.9B | Discretionary $1,875.1B (on hover/expand: Defense $893.6B / Nondefense $981.5B) | Net Interest $970.1B. Percent + $ labels inside segments (≥10% width) or in a legend below (mobile/all under 640px: vertical stack). Each segment: popover (click/tap) with a 2-3 sentence plain definition — write from structure, not model trivia: Mandatory = spending set by ongoing laws (eligibility formulas), not annual votes; Discretionary = set by annual appropriations; Net Interest = owed on the debt regardless of votes. NOT drill targets (45-CONTEXT decision) — popover only. SourceChip → OMB Historical Tables.

    ThisYearStrip: compact band: "FY2026 so far (through {as_of}): in $3,655.6B · out $4,901.9B" + small caveat "partial year — proportions not comparable to FY2025" + SourceChip → Treasury Fiscal Data. All values from metrics, never hardcoded.

    All amounts formatted $X,XXX.XB consistently (shared util if one exists; else local formatter matching DatasetTabs's).
  </action>
  <verify><automated>tsc + build; render with real context data in dev; mobile breakpoint stacks correctly</automated></verify>
  <acceptance_criteria>
    - Zero hardcoded dollar values (everything from props); bands proportional to the pixel; popovers keyboard-accessible (the icicle's button/aria idioms)
  </acceptance_criteria>
  <done>The three structural-context components exist and render official sourced figures.</done>
</task>

<task type="auto">
  <name>Task 4: FederalLanding + App.tsx integration</name>
  <files>src/components/federal/FederalLanding.tsx, src/App.tsx</files>
  <action>
    FederalLanding: fetches loadFederalContext() (useEffect + loading/error states per App.tsx idiom), composes DeficitStrip → FirstSplitBands → ThisYearStrip with section headings ("The big picture — FY2025" / "This year so far"). Error state: compact "couldn't load federal context" note — NEVER fabricated numbers.
    App.tsx: at the top-level dashboard (navigationPath.length === 0), when selectedEntity?.entity_type === 'federal': render FederalLanding INSTEAD of PlainLanguageSummary (45-CONTEXT: suppression decision — its uncontextualized $7.5T total). All other entity types: untouched rendering (verify by diff inspection that the non-federal path is identical).
  </action>
  <verify>
    <automated>
      tsc + npm run build green; dev server: US page shows landing block above DatasetTabs; Plano + California pages unchanged (PlainLanguageSummary still present)
    </automated>
  </verify>
  <acceptance_criteria>
    - VIZ-01 + VIZ-02 visibly satisfied on the live US page; zero regression on other entities
  </acceptance_criteria>
  <done>A citizen opening United States sees the first split and the deficit before anything else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API JSON → render | Own backend; all values rendered as text nodes (JSX auto-escape); external links rel=noopener |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Spoofing | source links | mitigate | URLs come from DB rows we wrote from .gov sources; rel=noopener noreferrer |
| T-45-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
1. US page: deficit strip, bands, FYTD strip, all chipped, proportions correct vs the sourced numbers
2. No PlainLanguageSummary for federal; present for everyone else
3. Mobile + dark mode pass; keyboard popovers work
</verification>

<success_criteria>
- VIZ-01, VIZ-02 complete; VIZ-04 satisfied for every landing figure
</success_criteria>

<output>
Create .planning/phases/45-federal-visualization/45-02-SUMMARY.md when done
</output>
