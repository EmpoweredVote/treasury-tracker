---
phase: 45-federal-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/EV-Accounts/backend/src/routes/treasury.ts
  - C:/EV-Accounts/backend/src/lib/treasuryService.ts
autonomous: true
requirements:
  - VIZ-04
must_haves:
  truths:
    - "GET /api/treasury/federal/context returns annual_summary rows (64 years) + context metrics, each row carrying source_name/source_url/source_date"
    - "GET /api/treasury/cities/:id/budgets responses include source chip fields: registry display_name + url, dataset base_url, generated_at"
    - "Both endpoints follow the existing optionalAuth + error-shape conventions; non-federal responses unchanged except the additive source fields"
  artifacts:
    - path: "C:/EV-Accounts/backend/src/routes/treasury.ts"
      provides: "GET /federal/context route"
      contains: "federal/context"
  key_links:
    - from: "GET /api/treasury/federal/context"
      to: "treasury.federal_annual_summary + treasury.federal_context_metrics"
      via: "treasuryService query"
      pattern: "federal_annual_summary"
---

<objective>
Expose the Phase 44 data to the frontend: one federal context endpoint (annual summary + metrics) and additive source-chip fields on the existing budgets endpoint. Without this, no Phase 45 component has data.

Output: two backend changes in EV-Accounts, verified by curl against the running backend; deploy step identified and executed per that repo's process.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-federal-visualization/45-CONTEXT.md
@.planning/phases/44-core-federal-data-load/44-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/treasury/federal/context</name>
  <files>C:/EV-Accounts/backend/src/routes/treasury.ts, C:/EV-Accounts/backend/src/lib/treasuryService.ts</files>
  <read_first>
    - C:/EV-Accounts/backend/src/routes/treasury.ts lines 46-130 — route conventions (optionalAuth, error shape, validation)
    - C:/EV-Accounts/backend/src/lib/treasuryService.ts getCities — pool.query + mapper conventions
  </read_first>
  <action>
    treasuryService: add getFederalContext():
      - annual_summary: SELECT fiscal_year, receipts, outlays, surplus_or_deficit, mandatory, discretionary_defense, discretionary_nondefense, net_interest, source_name, source_url, source_date FROM treasury.federal_annual_summary ORDER BY fiscal_year — numbers via Number() (the bigint/numeric casting note at treasuryService.ts:16)
      - metrics: SELECT metric_key, value, as_of_date, label, source_name, source_url, source_date FROM treasury.federal_context_metrics — return as an object keyed by metric_key
      - Join source_registry display_name for each distinct source_name so the frontend never hardcodes registry labels
    Route: GET /federal/context (optionalAuth, no params). 200 with { annual_summary, metrics, source_display_names }. Standard 500 shape on error.
    Verify by curl: response has 64 annual_summary rows; metrics.total_public_debt.value > 39e12; every row has non-null source_url.
  </action>
  <verify><automated>curl localhost backend /api/treasury/federal/context — 64 rows, sourced, metrics keyed</automated></verify>
  <acceptance_criteria>
    - Endpoint live locally; response shape documented in the summary for 45-02's consumption
  </acceptance_criteria>
  <done>Frontend has one fetch for all landing-strip data.</done>
</task>

<task type="auto">
  <name>Task 2: source-chip fields on budgets responses</name>
  <files>C:/EV-Accounts/backend/src/lib/treasuryService.ts</files>
  <action>
    In getBudgetsByCityId/getBudgetById SELECTs, add LEFT JOIN treasury.source_registry sr ON sr.id = b.data_source_id and LEFT JOIN treasury.data_sources ds ON ds.name = b.data_source AND ds.municipality_id = b.municipality_id; expose additive fields: source_display_name (sr.display_name), source_registry_url (sr.url), source_dataset_url (ds.base_url), generated_at (already selected — confirm it reaches the JSON).
    ADDITIVE ONLY — existing consumers must see identical fields plus the new ones. Verify: curl a city budget (Plano) — new fields null-or-populated, old fields byte-identical; curl the US budgets — all three datasets carry display_name + dataset URL.
  </action>
  <verify><automated>curl comparisons: Plano response superset of before; US budgets have sr/ds fields populated</automated></verify>
  <acceptance_criteria>
    - No breaking change for any existing consumer; federal rows fully chipped
  </acceptance_criteria>
  <done>Every dataset can render its source chip from its own response.</done>
</task>

<task type="auto">
  <name>Task 3: deploy per EV-Accounts process</name>
  <files>none</files>
  <action>
    Determine how backend changes ship (read EV-Accounts repo root: README/render.yaml/deploy docs; check how 32-04's backend change shipped — see .planning/phases/32-state-entity-infrastructure/32-04-SUMMARY.md). If deploy is push-triggered: commit + push per that repo's conventions. If manual: STOP and hand Chris the exact action needed (human checkpoint).
    After deploy: curl the PRODUCTION /api/treasury/federal/context and verify 64 rows.
  </action>
  <verify><automated>Production endpoint returns the context payload</automated></verify>
  <acceptance_criteria>
    - Production serves the new endpoint + additive fields; deploy method recorded in summary
  </acceptance_criteria>
  <done>45-02/45-03 can build against production data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public endpoint → DB | Read-only SELECTs, no params on /federal/context; existing optionalAuth pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | DoS | unparameterized context endpoint | accept | Fixed-size result (64+~71 rows); same exposure class as /cities |
| T-45-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
1. Local + production curl evidence for both endpoints
2. Plano budgets response: additive-only diff
3. Response shapes documented for 45-02
</verification>

<success_criteria>
- All Phase 45 frontend work has production data behind it; zero breaking API changes
</success_criteria>

<output>
Create .planning/phases/45-federal-visualization/45-01-SUMMARY.md when done
</output>
