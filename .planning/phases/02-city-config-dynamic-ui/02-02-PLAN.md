---
phase: 02-city-config-dynamic-ui
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [CITY-01, CITY-02, CITY-03]

must_haves:
  truths:
    - "Changing heroTitle in bloomington.ts config changes the h1 on /bloomington"
    - "Setting availableDatasets to ['operating'] in bloomington.ts shows only the Money Out tab"
    - "The city picker at / renders a card for each entry in the city registry"
    - "Per-resident cost displays correctly using cityConfig.population"
    - "/los-angeles still redirects to / (no route generated for isComingSoon cities)"
  artifacts: []
  key_links: []
---

<objective>
Verify all 5 Phase 2 success criteria via automated grep checks and a human smoke test of the running application.

Purpose: Confirm that config-driven UI works end-to-end, not just at the TypeScript level.
Output: Human-verified confirmation that all Phase 2 success criteria pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-city-config-dynamic-ui/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of config wiring</name>
  <files></files>
  <read_first>
    src/config/types.ts
    src/config/cities/bloomington.ts
    src/config/cityRegistry.ts
    src/pages/CityPage.tsx
    src/pages/CityPickerPage.tsx
    src/AppRouter.tsx
    src/components/datasets/DatasetTabs.tsx
  </read_first>
  <action>
Run the following automated checks and report results:

1. **TypeScript compilation:** `npx tsc --noEmit` — must exit 0
2. **Vite build:** `npm run build` — must succeed
3. **No hardcoded Bloomington strings in UI files:**
   - `grep -rn "Monroe_County_Courthouse" src/pages/ src/components/` — must return zero results
   - `grep -rn "Bloomington, Indiana Finances" src/pages/ src/components/` — must return zero results
   - `grep -n "const years = \[" src/pages/CityPage.tsx` — must return zero results
   - `grep -n "path=\"/bloomington\"" src/AppRouter.tsx` — must return zero results
4. **Config files have correct structure:**
   - `grep "export interface CityConfig" src/config/types.ts` — must match
   - `grep "export const CITY_REGISTRY" src/config/cityRegistry.ts` — must match
   - `grep "export function getCityConfig" src/config/cityRegistry.ts` — must match
   - `grep "isComingSoon: true" src/config/cityRegistry.ts` — must match (LA placeholder)
5. **Wiring in place:**
   - `grep "getCityConfig(slug)" src/pages/CityPage.tsx` — must match
   - `grep "CITY_REGISTRY.map" src/pages/CityPickerPage.tsx` — must match
   - `grep "CITY_REGISTRY" src/AppRouter.tsx` — must match
   - `grep "availableDatasets" src/components/datasets/DatasetTabs.tsx` — must match
   - `grep "cityConfig.population" src/pages/CityPage.tsx` — must match
   - `grep "cityConfig.hasTransactions" src/pages/CityPage.tsx` — must match

Report pass/fail for each check. All must pass before proceeding to human verification.
  </action>
  <verify>
    <automated>cd "/mnt/c/Treasury Tracker" && npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` succeeds
    - grep for "Monroe_County_Courthouse" in src/pages/ and src/components/ returns zero matches
    - grep for "Bloomington, Indiana Finances" in src/pages/ and src/components/ returns zero matches
    - grep for `getCityConfig(slug)` in CityPage.tsx returns at least one match
    - grep for `CITY_REGISTRY.map` in CityPickerPage.tsx returns at least one match
    - grep for `cityConfig.population` in CityPage.tsx returns at least one match
  </acceptance_criteria>
  <done>All automated checks pass. Zero hardcoded Bloomington strings in UI files. Config wiring confirmed via grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test of all 5 success criteria</name>
  <what-built>
    City config system with Bloomington config, city registry, and all 7 UI locations wired to read from config instead of hardcoded values.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    **Criterion 1 — Config schema exists with Bloomington entry:**
    Already verified by automated checks (TypeScript compiles). No browser action needed.

    **Criterion 2 — Hero section reads from config:**
    1. Open http://localhost:5173/bloomington
    2. Confirm the hero section shows "Bloomington, Indiana Finances" title and the courthouse background image
    3. (Optional deep test) Edit `src/config/cities/bloomington.ts`, change `heroTitle` to `"Test City"`, save, confirm HMR updates the h1 to "Test City", then revert

    **Criterion 3 — Dataset tabs driven by config:**
    1. On `/bloomington`, confirm you see all three tabs: "Money In", "Money Out", "People"
    2. (Optional deep test) Edit `src/config/cities/bloomington.ts`, change `availableDatasets` to `['operating']`, save, confirm only "Money Out" tab is visible, then revert

    **Criterion 4 — Context card values from config:**
    1. On `/bloomington`, confirm the info card shows "Population ~79,168 residents" and a per-resident dollar amount
    2. The fiscal year and total budget should still come from the data file (not config)

    **Criterion 5 — City picker renders from registry:**
    1. Navigate to http://localhost:5173/
    2. Confirm you see a "Bloomington, IN" card (clickable) and a "Los Angeles, CA" card (disabled, says "Coming Soon")
    3. Confirm clicking Bloomington card navigates to `/bloomington`
    4. Navigate to http://localhost:5173/los-angeles — confirm it redirects to `/`
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe which criteria fail and what you see.</resume-signal>
</task>

</tasks>

<verification>
All 5 Phase 2 success criteria confirmed:
1. CityConfig schema exists and Bloomington satisfies it (TypeScript compilation proves this)
2. Hero section driven by config (human visual confirmation)
3. Dataset tabs driven by config (human visual confirmation)
4. Context card values from config (human visual confirmation)
5. City picker renders from registry (human visual confirmation + LA redirect confirmed)
</verification>

<success_criteria>
- All automated grep/build checks pass
- Human confirms all 5 success criteria visually in the running application
- /los-angeles still redirects to / (Phase 1 behavior preserved)
- Bloomington behavior identical to post-Phase-1 state
</success_criteria>

<output>
After completion, create `.planning/phases/02-city-config-dynamic-ui/02-02-SUMMARY.md`
</output>
