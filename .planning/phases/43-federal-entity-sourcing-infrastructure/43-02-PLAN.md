---
phase: 43-federal-entity-sourcing-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/budget.ts
  - src/components/EntitySwitcher.tsx
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "TypeScript accepts a Municipality object with entity_type='federal'"
    - "EntitySwitcher renders a FEDERAL GOVERNMENT section above STATE GOVERNMENTS when a federal entity is present, and renders identically to today when none is"
    - "A selected federal entity's pill shows its name only (no ', US' suffix)"
    - "A federal entity never appears inside a state's city/county group"
  artifacts:
    - path: "src/types/budget.ts"
      provides: "'federal' in the entity_type union"
      contains: "'federal'"
    - path: "src/components/EntitySwitcher.tsx"
      provides: "Federal section rendering + group-exclusion filters"
      contains: "federalEntities"
  key_links:
    - from: "src/components/EntitySwitcher.tsx"
      to: "src/types/budget.ts"
      via: "Municipality type import"
      pattern: "entity_type === 'federal'"
---

<objective>
Add 'federal' to the Municipality entity_type union and teach EntitySwitcher to render federal entities in their own top section — exactly the Phase 32 state pattern (32-02 + 32-03), one tier higher.

Purpose: when Phase 44 seeds the United States row with budget data, the frontend must render it correctly with zero further changes. Until then nothing visible changes (getCities filters out data-less entities — see 43-CONTEXT.md).

Output: both files updated; build passes; existing rendering unchanged.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/43-federal-entity-sourcing-infrastructure/43-CONTEXT.md
@.planning/phases/32-state-entity-infrastructure/32-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: entity_type union — add 'federal'</name>
  <files>src/types/budget.ts</files>
  <action>
    In the Municipality interface (entity_type union starting at src/types/budget.ts:111), add `| 'federal'` as the final union member after `| 'town'`.
    Run `npx tsc --noEmit` — must pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit exits 0; grep "'federal'" src/types/budget.ts returns the new union member</automated>
  </verify>
  <acceptance_criteria>
    - Union has 12 members ending with 'federal'
    - tsc passes with no new errors
  </acceptance_criteria>
  <done>Municipality objects may carry entity_type='federal'.</done>
</task>

<task type="auto">
  <name>Task 2: EntitySwitcher federal section</name>
  <files>src/components/EntitySwitcher.tsx</files>
  <read_first>
    - src/components/EntitySwitcher.tsx lines 12-23 (ENTITY_TYPE_LABELS), 57-100 (grouping memo), 100-110 (header pill), 144-180 (state section render) — the state pattern being mirrored
  </read_first>
  <action>
    Four coordinated changes, mirroring the Phase 32 state pattern:

    1. ENTITY_TYPE_LABELS: add `federal: 'Federal Government',` as the first entry.

    2. Grouping memo (~line 71): alongside the existing `stateEntities` pre-filter, add
         `const federalEntities = withData.filter(m => m.entity_type === 'federal');`
       and change the cityEntities filter from `m.entity_type !== 'state'` to
         `m.entity_type !== 'state' && m.entity_type !== 'federal'`
       (CRITICAL — without this the US row nests inside a "US" state group, the exact bug D-05 fixed for states). Include `federalEntities` in the memo's return object. Apply the same name/state filter matching to federal entities as states get.

    3. Empty-state check (~line 146): include `grouped.federalEntities.length === 0` in the "No jurisdictions match" condition.

    4. Render a FEDERAL GOVERNMENT section ABOVE the STATE GOVERNMENTS section (~line 152), copying the state section's JSX structure verbatim (sticky header div + option buttons showing `entity.name`), with header text "FEDERAL GOVERNMENT" and `grouped.federalEntities` as the list.

    5. Header pill (~line 102): change the condition so both 'state' and 'federal' show name-only:
         `['state','federal'].includes(selectedEntity.entity_type) ? selectedEntity.name : \`${selectedEntity.name}, ${selectedEntity.state}\``

    Run `npx tsc --noEmit` and `npm run build` — both must pass.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit exits 0; npm run build exits 0;
      grep -c "federalEntities" src/components/EntitySwitcher.tsx returns >= 4
    </automated>
  </verify>
  <acceptance_criteria>
    - All five changes present; build green
    - With zero federal entities in props, rendering is byte-identical behavior to today (sections conditional on length > 0)
    - cityEntities filter excludes both 'state' and 'federal'
  </acceptance_criteria>
  <done>EntitySwitcher is federal-ready; invisible until federal data exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| municipalities prop → render | Data from own backend; rendered via JSX text nodes (auto-escaped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Spoofing | entity_type discrimination | accept | Values constrained by DB CHECK (43-01); unknown values fall through to existing grouped rendering |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. tsc --noEmit and npm run build pass
2. grep confirms federalEntities filter, section render, label entry, pill condition, and city-filter exclusion
3. No changes to any other component
</verification>

<success_criteria>
- INFRA-01 (frontend layer): TypeScript + EntitySwitcher accept and render federal entities
- Zero visible change until a federal entity with data exists
</success_criteria>

<output>
Create .planning/phases/43-federal-entity-sourcing-infrastructure/43-02-SUMMARY.md when done
</output>
