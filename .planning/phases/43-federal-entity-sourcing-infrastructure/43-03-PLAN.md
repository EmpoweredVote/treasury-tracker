---
phase: 43-federal-entity-sourcing-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [43-01, 43-02]
files_modified: []
autonomous: true
requirements:
  - INFRA-01
must_haves:
  truths:
    - "A federal municipality row WITH a budget row is returned by the getCities query shape (i.e., ev-accounts-api would serve it)"
    - "A federal municipality row WITHOUT budget data is excluded by the getCities query shape (hidden until Phase 44)"
    - "The backend MTFCC/geofence map does NOT include 'federal' (no geofence lookup attempted)"
    - "Existing municipality counts and a spot-check entity are unchanged after the test cycle"
  artifacts:
    - path: ".planning/phases/43-federal-entity-sourcing-infrastructure/43-03-SUMMARY.md"
      provides: "E2E + regression evidence"
      contains: "getCities"
  key_links:
    - from: "treasury.municipalities (entity_type='federal')"
      to: "C:/EV-Accounts/backend/src/lib/treasuryService.ts getCities()"
      via: "SQL query shape replication"
      pattern: "entity_type"
---

<objective>
Prove the full chain end-to-end WITHOUT writing code: a federal entity with budget data flows through the backend's getCities query shape and would render in EntitySwitcher; without data it stays hidden; nothing regresses. Expected outcome: zero backend changes needed (entity_type is `string | null` there) — this plan VERIFIES that expectation and files the evidence, or surfaces the gap if wrong.

Purpose: Phase 43's success criterion 1 says "created AND served". 43-01/43-02 prove created; this proves served — before Phase 44 builds loaders on the assumption.

Output: 43-03-SUMMARY.md with query evidence; any discovered backend gap documented (and fixed only if a one-liner; otherwise escalated as a checkpoint).
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-federal-entity-sourcing-infrastructure/43-CONTEXT.md
@.planning/phases/32-state-entity-infrastructure/32-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend audit — confirm federal needs no code change</name>
  <files>none (read-only)</files>
  <action>
    Read C:/EV-Accounts/backend/src/lib/treasuryService.ts:
    1. The MTFCC/geofence map (~line 39): confirm 'federal' is absent and that absence means "no geofence lookup" (not an error path). Quote the relevant lines in the summary.
    2. getCities() (~lines 380-420): extract the EXACT SQL it runs (including the budget-data HAVING/JOIN filter from 32-04). Record it for Task 2.
    3. Search the whole backend for entity_type comparisons/allowlists (grep entity_type, grep 'state', grep switch/case on type) that would mishandle 'federal'. List every hit and its disposition in the summary.
    If any code path REQUIRES a change for federal to be served: if it is a one-line addition matching an existing pattern, make it, note it in the summary, and update files_modified; otherwise STOP and report the gap as a checkpoint for Chris.
  </action>
  <verify>
    <automated>Summary lists every entity_type touchpoint in the backend with disposition</automated>
  </verify>
  <acceptance_criteria>
    - MTFCC map confirmed federal-free with safe fallthrough
    - getCities SQL captured verbatim
    - Zero unexplained entity_type touchpoints
  </acceptance_criteria>
  <done>Backend behavior for federal entities is known, not assumed.</done>
</task>

<task type="auto">
  <name>Task 2: E2E simulation with temp rows, then full cleanup</name>
  <files>none (DB temp rows only)</files>
  <action>
    Record BEFORE counts: total municipalities; municipalities returned by the getCities query shape; operating_budgets count.

    Via execute_sql, in order:
    1. INSERT temp federal municipality: name='_test United States', state='US', entity_type='federal', population=340110988 RETURNING id.
    2. Run the EXACT getCities SQL from Task 1 → temp row must be ABSENT (no budget data → hidden; this is the Phase-45 flag behavior working for free).
    3. INSERT one operating_budgets row for it: fiscal_year=2025, department='_test National Defense', approved_amount=893600000000, source_name='omb-historical-tables', source_url='https://www.whitehouse.gov/wp-content/uploads/2026/04/hist08z1_fy2027.xlsx', source_date='2026-06-12' — exercising BOTH new sourcing columns.
    4. Re-run the getCities SQL → temp row must now be PRESENT with entity_type='federal' and its available dataset metadata.
    5. DELETE the operating_budgets temp row, then the municipality temp row (or rely on ON DELETE CASCADE where applicable — verify both gone).

    Record AFTER counts — must equal BEFORE counts exactly. Spot-check one existing entity (e.g. Plano TX) returns identical fields before/after.
  </action>
  <verify>
    <automated>
      execute_sql: SELECT count(*) FROM treasury.municipalities WHERE name LIKE '_test%' -- returns 0 after cleanup;
      BEFORE/AFTER count pairs identical in summary
    </automated>
  </verify>
  <acceptance_criteria>
    - Hidden-without-data and served-with-data both demonstrated against the real query shape
    - source_url + source_date round-trip through an INSERT
    - Zero residue: all counts restored, no _test rows remain
  </acceptance_criteria>
  <done>Phase 43 success criterion 1 proven; Phase 44 can build loaders on verified behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Temp test rows → production tables | Local Supabase only; prefixed '_test'; deleted in-plan with count verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering | temp rows left behind | mitigate | LIKE '_test%' count = 0 is a hard acceptance criterion |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. Backend touchpoint audit complete with dispositions
2. getCities excludes data-less federal row, includes data-bearing one
3. All temp rows removed; before/after counts identical; spot-check entity unchanged
</verification>

<success_criteria>
- Phase 43 ROADMAP criteria 1 and 5 proven with evidence, not assumption
- Any backend gap either fixed (one-liner) or escalated — never silently deferred
</success_criteria>

<output>
Create .planning/phases/43-federal-entity-sourcing-infrastructure/43-03-SUMMARY.md when done
</output>
