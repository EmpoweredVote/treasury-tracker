---
phase: 43-federal-entity-sourcing-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260612100000_add_federal_entity_type.sql
  - supabase/migrations/20260612100100_add_budget_sourcing_columns.sql
  - supabase/migrations/20260612100200_create_program_details.sql
  - supabase/migrations/20260612100300_seed_federal_source_registry.sql
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "INSERT into treasury.municipalities with entity_type='federal' succeeds; an out-of-set value is rejected"
    - "operating_budgets and revenue_budgets each have nullable source_url (text) and source_date (date) columns"
    - "treasury.program_details exists with UNIQUE(municipality_id, name_key) and paired *_url columns for every claim field"
    - "source_registry contains the 5 federal source rows exactly once (re-running the seed adds nothing)"
  artifacts:
    - path: "supabase/migrations/20260612100000_add_federal_entity_type.sql"
      provides: "entity_type CHECK constraint including 'federal' (12 values)"
      contains: "municipalities_entity_type_check"
    - path: "supabase/migrations/20260612100200_create_program_details.sql"
      provides: "Tier 2 program origins table"
      contains: "CREATE TABLE treasury.program_details"
  key_links:
    - from: "supabase/migrations/20260612100200_create_program_details.sql"
      to: "treasury.municipalities.id"
      via: "FK municipality_id"
      pattern: "REFERENCES treasury.municipalities"
---

<objective>
Apply four DB migrations: (1) add 'federal' to the entity_type CHECK constraint, (2) add source_url/source_date provenance columns to both budget tables, (3) create the program_details table for Tier 2 origins, (4) seed federal sources into source_registry.

Purpose: Phase 44 loaders INSERT a federal municipality and sourced budget rows; Phase 47 writes program_details rows. All schema must exist first. This is the always-sourced standard's foundation (v2.0 ground rule 3).

Output: Four migration files applied to local Supabase, all verified by query.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-federal-entity-sourcing-infrastructure/43-CONTEXT.md
@.planning/v2.0-recon/RECON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: entity_type CHECK constraint — add 'federal'</name>
  <files>supabase/migrations/20260612100000_add_federal_entity_type.sql</files>
  <read_first>
    - supabase/migrations/20260606000000_add_state_entity_type.sql — Phase 32 precedent
  </read_first>
  <action>
    The constraint EXISTS with 11 values (verified 2026-06-12; see 43-CONTEXT.md) — it must be dropped and re-added. Create the migration file containing exactly:

      ALTER TABLE treasury.municipalities
        DROP CONSTRAINT municipalities_entity_type_check;
      ALTER TABLE treasury.municipalities
        ADD CONSTRAINT municipalities_entity_type_check
        CHECK (entity_type IN ('city', 'county', 'township', 'nonprofit', 'state', 'municipality', 'special_district', 'school_district', 'conservancy', 'library', 'town', 'federal'));

    Apply via mcp__supabase-local__apply_migration (name: "add_federal_entity_type").

    Functional test via execute_sql: INSERT a row with name='_test_federal', state='US', entity_type='federal', population=0 RETURNING id; then INSERT with entity_type='_bogus' (expect CHECK violation error); then DELETE the test row.
  </action>
  <verify>
    <automated>
      execute_sql: SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='municipalities_entity_type_check'
      -- definition contains 'federal' and all 11 prior values
    </automated>
  </verify>
  <acceptance_criteria>
    - Constraint definition contains all 12 values
    - entity_type='federal' INSERT succeeds; bogus value rejected; test row cleaned up
  </acceptance_criteria>
  <done>'federal' is an accepted entity_type; out-of-set values still rejected.</done>
</task>

<task type="auto">
  <name>Task 2: source_url + source_date on budget tables</name>
  <files>supabase/migrations/20260612100100_add_budget_sourcing_columns.sql</files>
  <action>
    Migration containing exactly:

      ALTER TABLE treasury.operating_budgets
        ADD COLUMN source_url text,
        ADD COLUMN source_date date;
      ALTER TABLE treasury.revenue_budgets
        ADD COLUMN source_url text,
        ADD COLUMN source_date date;

      COMMENT ON COLUMN treasury.operating_budgets.source_url IS 'Direct URL to the source dataset/record. REQUIRED on federal rows (v2.0 always-sourced standard); nullable for legacy municipal rows.';
      COMMENT ON COLUMN treasury.operating_budgets.source_date IS 'Date the source data was fetched. REQUIRED on federal rows.';
      COMMENT ON COLUMN treasury.revenue_budgets.source_url IS 'Direct URL to the source dataset/record. REQUIRED on federal rows (v2.0 always-sourced standard); nullable for legacy municipal rows.';
      COMMENT ON COLUMN treasury.revenue_budgets.source_date IS 'Date the source data was fetched. REQUIRED on federal rows.';

    Apply via apply_migration (name: "add_budget_sourcing_columns").
    Columns are nullable BY DESIGN — 38k+ legacy rows cannot backfill; the federal requirement is enforced by Phase 44 loader code and the Phase 48 source-chain audit (D-43 decision in 43-CONTEXT.md).
  </action>
  <verify>
    <automated>
      execute_sql: SELECT table_name, column_name FROM information_schema.columns WHERE table_schema='treasury' AND column_name IN ('source_url','source_date') AND table_name IN ('operating_budgets','revenue_budgets')
      -- returns 4 rows
    </automated>
  </verify>
  <acceptance_criteria>
    - 4 new columns exist (2 tables × 2 columns), all nullable
    - Existing row counts unchanged (no data mutation)
  </acceptance_criteria>
  <done>Both budget tables can carry per-row direct source URL + fetch date.</done>
</task>

<task type="auto">
  <name>Task 3: program_details table</name>
  <files>supabase/migrations/20260612100200_create_program_details.sql</files>
  <action>
    Migration containing:

      CREATE TABLE treasury.program_details (
        id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
        municipality_id uuid NOT NULL REFERENCES treasury.municipalities(id) ON DELETE CASCADE,
        name_key text NOT NULL,
        program_name text NOT NULL,
        enabling_bill text,
        enabling_bill_url text,
        public_law text,
        public_law_url text,
        enacted_year smallint,
        sponsor text,
        sponsor_url text,
        cosponsors_count integer,
        cosponsors_url text,
        details jsonb,
        source_api text NOT NULL,
        fetched_at timestamptz NOT NULL DEFAULT now(),
        created_at timestamptz NOT NULL DEFAULT now(),
        UNIQUE (municipality_id, name_key)
      );

      COMMENT ON TABLE treasury.program_details IS 'Tier 2 program origins (v2.0): enabling statute, public law, sponsor — structured from Congress.gov/GovInfo. Every claim field has a paired _url; nothing here may come from model memory.';
      COMMENT ON COLUMN treasury.program_details.details IS 'Extensible claims beyond the structured columns. Convention: array of {field, value, source_url} objects — a claim without source_url must not be stored.';
      COMMENT ON COLUMN treasury.program_details.source_api IS 'Which official API produced this row: congress.gov | govinfo';

    RLS stays DISABLED — matches category_enrichment (same backend-service-role read path; see 43-CONTEXT.md decision table).

    Apply via apply_migration (name: "create_program_details"). Functional test: INSERT one row referencing any existing municipality id with name_key='_test', source_api='congress.gov'; confirm a duplicate (municipality_id, name_key) INSERT fails; DELETE the test row.
  </action>
  <verify>
    <automated>
      execute_sql: SELECT count(*) FROM information_schema.columns WHERE table_schema='treasury' AND table_name='program_details'
      -- returns 17
    </automated>
  </verify>
  <acceptance_criteria>
    - Table exists with all 17 columns, FK to municipalities, UNIQUE(municipality_id, name_key)
    - Duplicate-key INSERT rejected; test rows cleaned up
  </acceptance_criteria>
  <done>program_details is ready for Phase 47 writes.</done>
</task>

<task type="auto">
  <name>Task 4: seed federal sources into source_registry</name>
  <files>supabase/migrations/20260612100300_seed_federal_source_registry.sql</files>
  <action>
    Migration inserting these 5 rows (name | display_name | url), each wrapped in idempotent INSERT ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM treasury.source_registry WHERE name = '<name>') — do NOT assume a unique constraint on name:

      treasury-fiscal-data   | Treasury Fiscal Data       | https://fiscaldata.treasury.gov
      omb-historical-tables  | OMB Historical Tables      | https://www.whitehouse.gov/omb/information-resources/budget/historical-tables/
      usaspending            | USAspending.gov            | https://www.usaspending.gov
      congress-gov           | Congress.gov               | https://www.congress.gov
      govinfo                | GovInfo                    | https://www.govinfo.gov

    Apply via apply_migration (name: "seed_federal_source_registry"). Then apply the same SQL a second time via execute_sql and confirm count is still 16 (11 existing + 5) — proves idempotency.
  </action>
  <verify>
    <automated>
      execute_sql: SELECT count(*) FROM treasury.source_registry WHERE name IN ('treasury-fiscal-data','omb-historical-tables','usaspending','congress-gov','govinfo')
      -- returns 5
    </automated>
  </verify>
  <acceptance_criteria>
    - 5 federal rows present exactly once; total registry count 16
    - Re-running the seed SQL adds zero rows
  </acceptance_criteria>
  <done>Federal source registry entries exist for Phase 44 loaders to reference via source_name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration files → Supabase local DB | DDL/seed applied via MCP tool; all SQL authored inline, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | DROP CONSTRAINT window | accept | Single-transaction migration; constraint re-added in same statement batch |
| T-43-02 | Tampering | seed SQL | accept | Static INSERT values, official .gov URLs only |
| T-43-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. Constraint definition includes 'federal' (12 values)
2. 4 sourcing columns exist across the two budget tables
3. program_details exists with FK + unique key; functional insert/duplicate/delete cycle passed
4. source_registry count = 16, federal names present exactly once
5. Zero changes to existing rows in any table
</verification>

<success_criteria>
- INFRA-01 (DB layer), INFRA-02, INFRA-03 satisfied at the schema level
- Phase 44 can INSERT a federal municipality and sourced budget rows with no further DDL
</success_criteria>

<output>
Create .planning/phases/43-federal-entity-sourcing-infrastructure/43-01-SUMMARY.md when done
</output>
