---
phase: 02-data-layer-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-data-layer-audit/02-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true

must_haves:
  truths:
    - "DATA-01 answered: frontend reads pre-aggregated budget_categories.amount, never sums line items at runtime"
    - "DATA-02 answered: Edge Function must atomically update 3 rows per donation (line item INSERT + leaf/parent category UPDATE + budget total UPDATE)"
    - "Phase 3 has a concrete, unambiguous technical contract (which columns to add, which rows to update, which dedup key to use)"
    - "REQUIREMENTS.md shows DATA-01 and DATA-02 as complete with inline findings"
    - "STATE.md reflects the strategy decisions as locked constraints for Phase 3"
    - "CSV re-import deduplication risk is documented with a concrete mitigation strategy"
  artifacts:
    - path: ".planning/phases/02-data-layer-audit/02-01-SUMMARY.md"
      provides: "Durable phase summary: audit findings, strategy, Phase 3 contract"
      contains: "DATA-01"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-01 and DATA-02 marked complete with inline answers"
      contains: "DATA-01"
    - path: ".planning/STATE.md"
      provides: "Updated current position + new decisions from audit"
      contains: "Phase 2"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "02-01-SUMMARY.md"
      via: "traceability table references phase summary"
      pattern: "DATA-01.*Phase 2.*(Complete|Done|✓)"
    - from: ".planning/STATE.md"
      to: "audit findings"
      via: "Decisions section captures 3-row atomic update strategy"
      pattern: "atomic|3.?row|budget_categories\\.amount"
    - from: "02-01-SUMMARY.md"
      to: "02-RESEARCH.md"
      via: "summary cites research as source of findings"
      pattern: "02-RESEARCH"
---

<objective>
Record the Phase 2 data layer audit findings as durable planning artifacts so Phase 3 has an unambiguous technical contract.

Purpose: The 02-RESEARCH.md already answers DATA-01 and DATA-02 definitively via direct codebase inspection. But research is an ephemeral artifact — it gets big and is hard to reference. This phase distills the findings into three places Phase 3 will actually consume: a short phase SUMMARY.md (the canonical answer), REQUIREMENTS.md (the requirement tracker), and STATE.md (the project's current constraints).

Output: Three updated files. No application code changes — Phase 3 owns all code changes (schema migration, Edge Function, loader script updates).

Phase 2 is a documentation/audit phase, not a coding phase.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-data-layer-audit/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 02-01-SUMMARY.md capturing audit findings and Phase 3 contract</name>
  <files>.planning/phases/02-data-layer-audit/02-01-SUMMARY.md</files>
  <action>
Create `.planning/phases/02-data-layer-audit/02-01-SUMMARY.md` as the canonical, concise summary of the Phase 2 audit. This is the file Phase 3 plans will @reference via context. Do NOT duplicate the full RESEARCH.md — distill it.

Required frontmatter:

```yaml
---
phase: 02-data-layer-audit
plan: 01
completed: 2026-04-21
subsystem: data-layer
affects: [phase-03-webhook-backend]
tech-stack:
  added: []
  modified: []
key-decisions:
  - "Frontend reads pre-aggregated budget_categories.amount — no runtime aggregation"
  - "Webhook must update 3 rows atomically per donation (leaf category + parent category + budget total)"
  - "Use Postgres function (supabase.rpc) to encapsulate dedup check + atomic multi-row update"
  - "Add external_id TEXT + source TEXT columns to budget_line_items via Supabase SQL editor (skip EV-Backend GORM)"
  - "loadEVFinances.js must preserve source='webhook' rows during clearExistingBudget"
---
```

Required body sections (use these exact H2 headings):

1. `## DATA-01 Answer` — One paragraph: frontend reads `budget_categories.amount` directly. Cite the 3 components (CategoryList.tsx, DatasetTabs.tsx, PlainLanguageSummary.tsx) and the fact that `budget.total_budget` is also pre-aggregated. No SQL SUM() or JS aggregation at runtime. Conclusion: webhook cannot rely on "the UI will sum line items for us" — it must update the pre-aggregated columns.

2. `## DATA-02 Answer` — The atomic update contract. Phase 3 Edge Function MUST update 3 rows per successful donation in a single Postgres transaction:
   - INSERT into `treasury.budget_line_items` (with external_id, source='webhook')
   - UPDATE `treasury.budget_categories SET amount = amount + $delta` for BOTH the leaf category (Give Butter) AND its parent (Donations)
   - UPDATE `treasury.budgets SET total_budget = total_budget + $delta`
   Note the hierarchy depth is 2 (Donations -> Give Butter) for EV revenue, so 2 category rows + 1 budget row = 3 updates total.

3. `## Phase 3 Technical Contract` — An explicit list Phase 3 must implement. Write each as a checkbox so Phase 3 plans can verify:
   - Schema: add `external_id TEXT` and `source TEXT DEFAULT 'csv'` to `treasury.budget_line_items`
   - Schema: add unique partial index `idx_line_items_external_id_source` on `(external_id, source) WHERE external_id IS NOT NULL`
   - Schema changes applied via Supabase SQL editor (NOT via EV-Backend GORM AutoMigrate — the Go API never writes webhook rows, so it doesn't need to know)
   - Postgres function `treasury.record_givebutter_donation(p_external_id, p_leaf_category_id, p_parent_category_id, p_budget_id, p_description, p_amount, p_date, p_vendor)` encapsulating dedup + 3-row update
   - Edge Function calls `supabase.rpc('record_givebutter_donation', {...})` — no direct table writes from TS
   - `loadEVFinances.js` sets `source: 'csv'` on every line item upsert
   - `clearExistingBudget()` modified to preserve rows where `source = 'webhook'` (do NOT delete them on CSV re-import)
   - Category ID resolution: lookup by `(budget_id, name)` — find "Donations" (depth=0) and "Give Butter" (depth=1) for the current-year EV revenue budget

4. `## Deduplication Strategy` — Concise restatement of the two-layer dedup:
   - Layer 1 (webhook retries): Postgres function checks `external_id + source='webhook'` before insert — idempotent no-op on conflict
   - Layer 2 (CSV re-import destruction): `clearExistingBudget()` skips webhook-sourced rows; CSV rows get `source='csv'` and `external_id=NULL`, so they never collide with webhook rows on the unique index

5. `## Open Questions Deferred to Phase 3` — List the 3 open questions from RESEARCH.md that Phase 3 owns:
   - GiveButter webhook payload shape (is the payment ID stable enough for external_id?)
   - Exact GiveButter signature header name and verification algorithm
   - Amount unit confirmation (cents vs dollars) via a real $1 test donation

6. `## Source References` — Bullet list pointing to `.planning/phases/02-data-layer-audit/02-RESEARCH.md` as the authoritative source, and cite the 3 primary source files inspected: `src/data/dataLoader.ts`, `src/components/CategoryList.tsx`, `scripts/loadEVFinances.js`.

Target: 80-150 lines total. This is a distillation, not a copy. Use plain markdown, no fluff. No emojis.
  </action>
  <verify>
1. File exists at `.planning/phases/02-data-layer-audit/02-01-SUMMARY.md`
2. File contains frontmatter with `key-decisions` listing all 5 decisions above
3. All 6 required H2 sections present: `## DATA-01 Answer`, `## DATA-02 Answer`, `## Phase 3 Technical Contract`, `## Deduplication Strategy`, `## Open Questions Deferred to Phase 3`, `## Source References`
4. Line count between 60 and 200 (`wc -l` on the file)
5. Grep confirms key technical strings: `external_id`, `source`, `record_givebutter_donation`, `clearExistingBudget`
  </verify>
  <done>Phase 3 can @reference this single summary file to get every piece of audit context it needs without reading the full 359-line RESEARCH.md. All five key-decisions are captured in frontmatter for tooling. The Phase 3 contract section is a copyable checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md — mark DATA-01 and DATA-02 complete with inline findings</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit `.planning/REQUIREMENTS.md` to reflect that Phase 2 requirements are now answered.

Changes to make:

1. In the `### Phase 2 — Data Layer Audit` section, change both checkboxes from `- [ ]` to `- [x]` and append a one-sentence finding to each:

   - `- [x] **DATA-01**: Confirmed — frontend reads pre-aggregated `budget_categories.amount` directly; no runtime aggregation. See `.planning/phases/02-data-layer-audit/02-01-SUMMARY.md`.`
   - `- [x] **DATA-02**: Strategy defined — Edge Function calls Postgres RPC `treasury.record_givebutter_donation` that atomically updates 3 rows (line item INSERT + leaf/parent category UPDATE + budget total UPDATE). See `.planning/phases/02-data-layer-audit/02-01-SUMMARY.md`.`

2. In the `## Traceability` table, change DATA-01 and DATA-02 status from `Pending` to `✓ Complete`.

3. Update the coverage line at the bottom from `17 requirements · 3 complete · 14 pending` to `17 requirements · 5 complete · 12 pending`.

4. Update the `*Last updated:*` footer line at the very end from `*Last updated: 2026-04-21 — Phase 1 shipped (donate button)*` to `*Last updated: 2026-04-21 — Phase 2 audit complete (DATA-01, DATA-02 answered)*`.

Do NOT modify any other requirement text, Phase 3/4 sections, or Out of Scope content. Leave those untouched.
  </action>
  <verify>
1. `grep -c "- \[x\] \*\*DATA-0[12]\*\*" .planning/REQUIREMENTS.md` returns 2
2. `grep "DATA-01 | Phase 2" .planning/REQUIREMENTS.md` contains `✓ Complete`
3. `grep "DATA-02 | Phase 2" .planning/REQUIREMENTS.md` contains `✓ Complete`
4. `grep "5 complete · 12 pending" .planning/REQUIREMENTS.md` returns 1 match
5. Footer line mentions "Phase 2 audit complete"
6. Phase 3 and Phase 4 requirement sections are byte-identical to before (only Phase 2 and the 2 summary lines changed)
  </verify>
  <done>REQUIREMENTS.md honestly reflects that 5/17 requirements are now complete. Traceability table is in sync with the Phase 2 section. The inline findings let anyone reading REQUIREMENTS.md in isolation understand what was decided without digging through research files.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — record Phase 2 decisions and advance current position</name>
  <files>.planning/STATE.md</files>
  <action>
Edit `.planning/STATE.md` to reflect that Phase 2 is now complete and Phase 3 is next, capturing the new locked decisions so they constrain Phase 3 planning.

Changes:

1. Update `## Current Position` block:
   - `Phase:` line → `Phase 2 complete — Phase 3 (webhook backend) is next`
   - `Status:` line → `Phase 2 audit complete; Phase 3 has unambiguous technical contract (see .planning/phases/02-data-layer-audit/02-01-SUMMARY.md)`
   - `Last activity:` line → `2026-04-21 — Phase 2 data layer audit complete (DATA-01, DATA-02 answered)`

2. In the `### Decisions` list, APPEND (do not replace existing entries) the following 5 new decisions from the audit:
   - `- Frontend reads pre-aggregated budget_categories.amount — webhook MUST update pre-aggregated columns (no SUM-at-query alternative)`
   - `- Atomic 3-row update per donation: INSERT budget_line_items + UPDATE leaf category amount + UPDATE parent category amount + UPDATE budgets.total_budget`
   - `- Use Postgres function treasury.record_givebutter_donation via supabase.rpc() — encapsulates dedup check and atomic multi-row update in one DB transaction`
   - `- Schema changes applied via Supabase SQL editor (NOT via EV-Backend GORM) — Go API never writes webhook rows`
   - `- Dedup via unique partial index on (external_id, source) WHERE external_id IS NOT NULL; loadEVFinances.js preserves source='webhook' rows on clearExistingBudget`

3. In the `### Known Constraints` list, APPEND (do not replace existing entries):
   - `- Category hierarchy for EV revenue is 2 levels deep: Donations (depth=0) → Give Butter (depth=1); webhook must update BOTH category amounts + budget total`
   - `- Category UUIDs are generated at import time — Edge Function must look up (budget_id, name='Give Butter') and (budget_id, name='Donations') dynamically, cannot hardcode IDs`

Preserve all existing content in Decisions and Known Constraints — these are additive.
  </action>
  <verify>
1. `grep -c "Phase 2 complete" .planning/STATE.md` returns at least 1
2. `grep "record_givebutter_donation" .planning/STATE.md` returns 1 match
3. `grep "clearExistingBudget" .planning/STATE.md` returns 1 match
4. All 3 pre-existing decisions are still present (`grep -c "^- " .planning/STATE.md` returns at least 10 — 3 old decisions + 5 new decisions + 2 old constraints + 2 new constraints)
5. Last activity date is `2026-04-21` and mentions Phase 2 audit
  </verify>
  <done>STATE.md is the authoritative "where are we and what's locked" snapshot. After this edit, any future /gsd:plan-phase or /gsd:discuss-phase run for Phase 3 will automatically load these new decisions as constraints, preventing re-litigation of the atomic-update strategy or the Postgres-function approach.</done>
</task>

</tasks>

<verification>
Run these checks after all 3 tasks complete:

1. New summary file exists and is non-empty: `test -s .planning/phases/02-data-layer-audit/02-01-SUMMARY.md && echo OK`
2. REQUIREMENTS.md coverage math is updated: `grep "5 complete · 12 pending" .planning/REQUIREMENTS.md`
3. STATE.md references the summary: `grep "02-01-SUMMARY" .planning/STATE.md`
4. No application code was modified: `git status --short | grep -v "^.. \.planning/" | wc -l` returns 0 (all changes under .planning/)
5. RESEARCH.md is untouched: `git diff .planning/phases/02-data-layer-audit/02-RESEARCH.md` is empty

If any check fails, the phase is not complete.
</verification>

<success_criteria>
- Phase 3 planners (human or Claude) can read `02-01-SUMMARY.md` alone and know exactly what schema to migrate, what Postgres function to write, and what dedup logic to implement — without reading the 359-line RESEARCH.md.
- REQUIREMENTS.md traceability table accurately shows 5/17 complete.
- STATE.md surfaces all 5 new decisions and 2 new constraints so they flow into Phase 3 planning automatically.
- No code files touched; no schema migrations run; no Edge Functions deployed. All of that is Phase 3.
</success_criteria>

<output>
After completion, the 02-01-SUMMARY.md created in Task 1 IS the phase summary — no additional summary file needs to be created by the execute-plan workflow. The filename already matches the expected `{phase}-{plan}-SUMMARY.md` convention.

If the execute-plan workflow would normally create a separate summary on completion, it should skip that step for this plan since Task 1 already produced the artifact.
</output>
