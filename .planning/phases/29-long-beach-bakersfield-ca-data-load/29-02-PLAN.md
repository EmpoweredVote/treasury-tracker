---
phase: 29-long-beach-bakersfield-ca-data-load
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - scripts/extractLongBeach.py
  - scripts/processLongBeach.js
  - docs/Long Beach/
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "Long Beach General Fund summary PDFs (FY2022–FY2026, as many as have consistent structure, minimum FY2025+FY2026) are downloaded into docs/Long Beach/"
    - "extractLongBeach.py emits JSON department rows with ending-year fiscal_year (FY 2024-25 → 2025) and never emits Port/Harbor rows"
    - "processLongBeach.js --dry-run produces a General Fund total inside the $1.3B–$1.7B sanity band for each FY"
    - "Live load writes Long Beach operating (and best-effort revenue) rows via treasury_sync_budget_tree"
  artifacts:
    - path: "scripts/extractLongBeach.py"
      provides: "pdfplumber General Fund department extractor with filename FY detection"
      contains: "detect_fy_from_filename"
    - path: "scripts/processLongBeach.js"
      provides: "Node processor: execSync Python → tree → treasury_sync_budget_tree RPC with sanity band"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/processLongBeach.js"
      to: "scripts/extractLongBeach.py"
      via: "execSync python invocation"
      pattern: "extractLongBeach\\.py"
    - from: "scripts/processLongBeach.js"
      to: "treasury_sync_budget_tree"
      via: "supabase.rpc on live load"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processLongBeach.js"
      to: "treasury.data_sources"
      via: "looks up Long Beach data_source by municipality+dataset (seeded in Plan 01)"
      pattern: "ensureMunicipality"
---

<objective>
Build the Long Beach pipeline: download the General Fund summary PDFs, write `extractLongBeach.py` (pdfplumber department extractor with ending-year FY detection from filename), write `processLongBeach.js` (Node orchestrator with worktree-safe PDF discovery, $1.3B–$1.7B sanity band, and treasury_sync_budget_tree RPC), validate via dry-run, then live-load operating and best-effort revenue.

Purpose: DATA-04 — Long Beach operating budget (~$1.5B General Fund) visible in the app, Port of Long Beach (~$760M) excluded, enterprise funds (Gas/Refuse/Water/Airport/Harbor) excluded, non-standard Oct–Sep FY stored as ending year per D-01.
Output: `scripts/extractLongBeach.py`, `scripts/processLongBeach.js`, `docs/Long Beach/*.pdf`, plus Long Beach budget rows in DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md
@scripts/extractOakland.py
@scripts/processOakland.js
@scripts/processSanJose.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Long Beach GF summary PDFs and write extractLongBeach.py</name>
  <files>scripts/extractLongBeach.py, docs/Long Beach/</files>
  <read_first>
    - scripts/extractOakland.py (template — copy parse_money() verbatim from lines 28-43; model extract_budget() structure on lines 132-311; copy dedup-by-(department,fiscal_year) from lines 295-309; copy argparse main entrypoint from lines 351-358)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md (§Long Beach PDF Download Reference for the verified per-FY URLs and section-number-changes-per-FY pitfall; §Common Pitfalls 1, 2, 4, 6; Open Question 3 on revenue location)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md (extractLongBeach.py section — docstring, detect_fy_from_filename, extract_budget skeleton)
  </read_first>
  <action>
    First create the `docs/Long Beach/` directory and download the General Fund summary PDFs using the verified per-FY URLs in RESEARCH §Long Beach PDF Download Reference. Base domain: https://www.longbeach.gov/globalassets/finance/media-library/documents/city-budget-and-finances/budget/budget-documents/ . NOTE the section number changes per FY (FY22/FY23 = section 34, FY24/FY25 = section 35, FY26 = section 33) — use the exact verified suffixes: FY22 `fy-22-adopted-budget/34-fund-summary-gp_22a-v2-gs`, FY23 `fy-23-adopted-budget/34-fund-summary-gp_23a_v1`, FY24 `fy-24-adopted-budget/35-fund-summary-gp`, FY25 `fy-25-adopted-budget/35-fund-summary-gp`, FY26 `fy-26-adopted-budget/33-fund-summary-gp`. Save each as `docs/Long Beach/fy{NN}-fund-summary-gp.pdf` (two-digit FY ending year, e.g. fy25, fy26) so the filename FY detector works. If any URL returns HTTP 404, record it and continue — the minimum acceptable set is FY2025 + FY2026 (D-05: load as many years as have reliable format, prefer back to FY2022). Target ONLY fund-summary-gp PDFs — these contain General Fund only; Enterprise/Port funds live in a separate fund-summary-ef section, so Port exclusion is automatic (Pitfall 2).

    Write scripts/extractLongBeach.py. Copy parse_money() verbatim from extractOakland.py. Add detect_fy_from_filename(pdf_path) that applies the ending-year convention (D-01): match `fy(\d{2})-` → 2000 + NN (fy25 → 2025), and a 4-digit fallback `fy(\d{4})-`. Write extract_budget(pdf_path) that opens the PDF with pdfplumber, derives fiscal_year via detect_fy_from_filename, iterates pages, and for each text line parses department-name + adopted-amount columns into dicts { department, fund: 'General Fund', adopted_amount, fiscal_year, page_num }. Skip rows where amount <= 0 or department length <= 2. Reject/skip any line whose department text contains 'Port' or 'Harbor' (case-insensitive) — these must never appear in output (Pitfall 2). Dedup by (department, fiscal_year) printing skipped duplicates to stderr. main: argparse with positional pdf_path; print json.dumps(extract_budget(path), indent=2) to stdout.

    Before finalizing the line-parsing regex, inspect the actual FY25 PDF text: run `python -c "import pdfplumber; p=pdfplumber.open('docs/Long Beach/fy25-fund-summary-gp.pdf'); print(p.pages[0].extract_text())"` and adapt the regex to the real column layout (Open Question 1). Do NOT assume the placeholder regex in PATTERNS is final.
  </action>
  <verify>
    <automated>python scripts/extractLongBeach.py "docs/Long Beach/fy25-fund-summary-gp.pdf"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/Long Beach/` contains at least `fy25-fund-summary-gp.pdf` and `fy26-fund-summary-gp.pdf` (minimum set); ideally fy22–fy26.
    - `python scripts/extractLongBeach.py "docs/Long Beach/fy25-fund-summary-gp.pdf"` prints a JSON array of objects, each with keys department, fund, adopted_amount, fiscal_year, page_num.
    - Every emitted object has `fiscal_year` equal to 2025 for the fy25 PDF (ending-year convention, NOT 2024).
    - No emitted object's `department` contains the substring "Port" or "Harbor" (case-insensitive).
    - Source contains `def detect_fy_from_filename` and `def parse_money`.
  </acceptance_criteria>
  <done>PDFs downloaded (≥ FY25, FY26); extractor emits clean General Fund department rows with ending-year FY and no Port/Harbor rows.</done>
</task>

<task type="auto">
  <name>Task 2: Write processLongBeach.js and dry-run validate the sanity band + scale</name>
  <files>scripts/processLongBeach.js</files>
  <read_first>
    - scripts/processOakland.js (template — copy loadEnv lines 45-56, Supabase init lines 59-62, resolvePdfDir lines 74-88, extractPDF lines 93-101, buildOperatingTree lines 106-129, ensureMunicipality lines 132-147, upsertDataSource lines 165-195, loadFiscalYear lines 198-218, processPDF sanity band lines 263-270, main parseArgs lines 281-325, fatal handler line 325)
    - scripts/processSanJose.js (lines 64-82 multi-variant resolvePdfDir reference; not needed for spaces but shows worktree pattern)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md (processLongBeach.js section — sanity band $1.3B–$1.7B, resolvePdfDir with ['Long Beach'], extractPDF script name, fund label 'General Fund', ensureMunicipality 'Long Beach'/'CA')
    - scripts/extractLongBeach.py (the extractor created in Task 1, so the processor's extractPDF parses its JSON shape)
  </read_first>
  <action>
    Write scripts/processLongBeach.js modeled exactly on processOakland.js. Reuse: loadEnv(), Supabase client init (key never logged — T-29-06), the fatal handler `main().catch(e => { console.error('Fatal:', e); process.exit(2); })`.

    Define sanity band constants: `const GF_BAND_MIN = 1_300_000_000;` and `const GF_BAND_MAX = 1_700_000_000;` (target ~$1.5B GF per DATA-04). Add comment `// Long Beach FY runs Oct 1 – Sep 30; stored as ending year (D-01)`.

    resolvePdfDir(): check `path.join(ROOT, 'docs', 'Long Beach')` (single variant — directory name has a space, no no-space variant needed), with the git-common-dir worktree fallback from processOakland.js.

    extractPDF(pdfPath): execSync `${pythonBin} "${pyScript}" "${pdfPath}"` where pyScript = scripts/extractLongBeach.py and pythonBin = 'python' on win32 else 'python3'; maxBuffer: 8 * 1024 * 1024 (T-29-04); return JSON.parse(raw). PDF path comes from controlled docs/ readdir and is double-quoted (T-29-05).

    buildOperatingTree(rows): build nodes { n: department, a: amount, i: [{ d: department, a: amount, aa: null, f: 'General Fund', e: null }] }, sum total, sort desc by a; return { tree, total }.

    ensureMunicipality(): SELECT id,name from municipalities WHERE name='Long Beach' AND state='CA' .maybeSingle(); if found return id; else console.error("Long Beach, CA municipality not found — run seedLongBeachBakersfieldCA.js first") and process.exit(2).

    upsertDataSource(muniId, fiscalYear, datasetType, pdfAbsPath): name = `Long Beach General Fund ${datasetType==='revenue'?'Revenue':'Operating'} Budget FY${fiscalYear}`; api_type 'pdf_download'; dataset_type datasetType; dataset_id `fy${fiscalYear}`; base_url 'file://'+pdfAbsPath; fiscal_years [fiscalYear]; municipality_id muniId. SELECT existing by (municipality_id, api_type='pdf_download', dataset_id, dataset_type) then UPDATE or INSERT.

    loadFiscalYear(...): call treasury_sync_budget_tree with p_data_source_id, p_fiscal_year, p_dataset_type, p_total, p_tree, p_row_count, p_triggered_by:'bulk_load'; handle rpcErr and rpc?.error.

    processPDF(pdfPath, muniId, dryRun): extract rows, group by fiscal_year, build tree+total per FY, log per-FY total and row count. Apply the sanity band halt: if total < GF_BAND_MIN || total > GF_BAND_MAX, console.error a SCALE MISMATCH message naming possible causes (wrong units/thousands, Port bleed, wrong section) and process.exit(3) — BEFORE any live write (T-29-07). If not dryRun, call loadFiscalYear for the operating dataset.

    main(): parseArgs with options 'dry-run' (boolean), pdf (string), and 'revenue' (boolean) — strict:false. If --revenue, set datasetType to 'revenue' for the load path (best-effort: see Task 3). PDF discovery: if --pdf use that single path, else readdirSync(pdfDir) filter .pdf, sort, map to full paths; exit(1) if none. If not dryRun, muniId = await ensureMunicipality().

    SCALE VERIFICATION during dry-run: run `node scripts/processLongBeach.js --dry-run` and compare each FY total to ~$1.5B. If totals come out ~$1,500 or ~$1.5M (off by 1000x), the PDF amounts are in thousands — add a `* 1000` scale in extractLongBeach.py (or a toFullDollars helper) and re-run until totals land in the $1.3B–$1.7B band (Pitfall A5 / RESEARCH §Amounts Scale).
  </action>
  <verify>
    <automated>node scripts/processLongBeach.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processLongBeach.js --dry-run` exits 0 and prints a per-FY operating total for each downloaded PDF.
    - Each printed FY operating total is within $1,300,000,000 and $1,700,000,000 (no SCALE MISMATCH exit(3)).
    - Source contains `GF_BAND_MIN = 1_300_000_000` and `GF_BAND_MAX = 1_700_000_000`.
    - Source contains `treasury_sync_budget_tree` and `extractLongBeach.py` and `maxBuffer: 8 * 1024 * 1024`.
    - Source contains `ensureMunicipality` selecting name='Long Beach' and state='CA'.
    - No SUPABASE_SERVICE_KEY value is console.logged.
  </acceptance_criteria>
  <done>processLongBeach.js dry-run produces GF totals inside the $1.3B–$1.7B band for every downloaded FY; scale confirmed correct (full dollars).</done>
</task>

<task type="auto">
  <name>Task 3: Live-load Long Beach operating + best-effort revenue</name>
  <files>scripts/processLongBeach.js, scripts/extractLongBeach.py</files>
  <read_first>
    - scripts/processLongBeach.js (created in Task 2)
    - scripts/extractLongBeach.py (created in Task 1)
    - scripts/extractFremont.py (reference for operating + revenue in same PDF — best-effort revenue approach)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md (Open Question 3 — Long Beach revenue likely in a Budget Summaries section, not the GF expenditure summary; best-effort pattern is acceptable for CA cities)
  </read_first>
  <action>
    Run the live operating load: `node scripts/processLongBeach.js` (no --dry-run). This calls ensureMunicipality (requires Plan 01 seeder to have run), upserts per-FY operating data_sources, and writes operating rows via treasury_sync_budget_tree for every downloaded FY. The sanity band halt protects against bad data.

    Then attempt best-effort revenue (DATA-04 specifies "operating + revenue"). Inspect whether the GF fund-summary-gp PDF contains a revenue/resources table; if it does, add an extract_revenue() path in extractLongBeach.py and a --revenue branch in processLongBeach.js that emits/loads dataset_type='revenue' (model on extractFremont.py operating+revenue handling). If the GF summary PDF has no usable revenue table, download the Budget Summaries section PDF (RESEARCH Open Question 3 names `10-budget-summaries` / `09-budget-summaries`) into docs/Long Beach/ and extract revenue from it. Best-effort means: load at least one FY of revenue so the Money In tab is populated; if no clean revenue table exists in any available PDF, document the gap in the SUMMARY and proceed (consistent with CA best-effort revenue pattern). Apply a revenue-appropriate sanity check (do not reuse the operating $1.3B–$1.7B band for revenue).

    Verify in DB after load: operating rows present for each loaded FY; revenue rows present for at least one FY (or gap documented).
  </action>
  <verify>
    <automated>node scripts/processLongBeach.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Live operating load completed: a DB query of treasury budget rows for Long Beach returns operating data for at least FY2025 and FY2026 with totals in the $1.3B–$1.7B range.
    - At least one fiscal year of Long Beach revenue (dataset_type='revenue') rows exists in DB; OR the SUMMARY documents why no clean revenue table was available and which PDFs were inspected.
    - `node scripts/processLongBeach.js --dry-run` still exits 0 (regression check — operating path unchanged).
    - If extract_revenue() was added, extractLongBeach.py contains `def extract_revenue` and processLongBeach.js handles the `--revenue` flag.
  </acceptance_criteria>
  <done>Long Beach operating rows live in DB within sanity band; revenue loaded best-effort (≥1 FY) or gap documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file→Python | PDF content parsed; paths come from controlled docs/Long Beach/ readdir, not user input |
| Python stdout→Node | JSON piped via execSync with bounded maxBuffer |
| Node→Supabase | Service-role RPC writes; sanity band gates the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Denial of service | execSync large PDF | mitigate | maxBuffer 8MB cap; target 400-558KB section PDFs not 16-29MB full books |
| T-29-05 | Tampering | PDF path injection in execSync | mitigate | path from controlled docs/Long Beach/ readdirSync, double-quoted in shell command |
| T-29-07 | Tampering/integrity | wrong-scale or Port-bleed data to DB | mitigate | sanity band halt (exit 3) before RPC; extractor rejects Port/Harbor rows |
| T-29-06 | Information disclosure | service key in logs | mitigate | loadEnv pattern; key never logged |
| T-29-SC | Tampering | package installs | mitigate | no new packages (pdfplumber pre-installed) |
</threat_model>

<verification>
- extractLongBeach.py emits ending-year FY and no Port/Harbor rows.
- processLongBeach.js --dry-run totals inside $1.3B–$1.7B band.
- Live operating load present in DB; revenue best-effort.
</verification>

<success_criteria>
- DATA-04 (operating): Long Beach General Fund operating ~$1.5B loaded for ≥ FY2025, FY2026 (more if available).
- Port of Long Beach excluded (no Port/Harbor rows; total within band).
- Revenue loaded best-effort (≥1 FY) or documented gap.
</success_criteria>

<output>
Create `.planning/phases/29-long-beach-bakersfield-ca-data-load/29-02-SUMMARY.md` when done.
</output>
