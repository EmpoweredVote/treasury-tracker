---
phase: 29-long-beach-bakersfield-ca-data-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedLongBeachBakersfieldCA.js
autonomous: true
requirements: [POPUL-01]
must_haves:
  truths:
    - "Long Beach municipality row exists in DB with state='CA', population=451000, population_year=2024, county_id=LA_COUNTY_ID"
    - "Bakersfield municipality row exists in DB with state='CA', population=417000, population_year=2024, county_id NULL"
    - "Four data_source rows exist (LB operating, LB revenue, BF operating, BF revenue), each visible via treasury_list_source_ids"
    - "Re-running the seeder produces no duplicate rows (idempotent)"
  artifacts:
    - path: "scripts/seedLongBeachBakersfieldCA.js"
      provides: "Two-city idempotent seeder for municipality + data_source rows"
      min_lines: 200
      contains: "treasury_list_source_ids"
  key_links:
    - from: "scripts/seedLongBeachBakersfieldCA.js"
      to: "treasury.municipalities"
      via: "upsertMunicipality() SELECT then INSERT/UPDATE"
      pattern: "from\\('municipalities'\\)"
    - from: "scripts/seedLongBeachBakersfieldCA.js"
      to: "treasury_list_source_ids"
      via: "verification RPC call"
      pattern: "treasury_list_source_ids"
---

<objective>
Create `scripts/seedLongBeachBakersfieldCA.js`, an idempotent two-city seeder that inserts the Long Beach and Bakersfield municipality rows (with 2024 Census population for per-capita display) and the four data_source rows (operating + revenue per city) that the downstream processors look up by name. This is the foundation plan — both the Long Beach processor (Plan 02) and Bakersfield processor (Plan 03) require these rows to exist before they can run.

Purpose: POPUL-01 — Long Beach (~451K) and Bakersfield (~417K) seeded with 2024 population so per-capita ($/resident) displays correctly. Also establishes the data_source rows that anchor all budget loads for both cities.
Output: `scripts/seedLongBeachBakersfieldCA.js` plus DB rows (2 municipalities, 4 data_sources).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-CONTEXT.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md
@scripts/seedOaklandSanJoseCA.js
@scripts/seedLACountyLinks.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write seedLongBeachBakersfieldCA.js (two-city seeder)</name>
  <files>scripts/seedLongBeachBakersfieldCA.js</files>
  <read_first>
    - scripts/seedOaklandSanJoseCA.js (exact template — copy loadEnv, Supabase init, upsertMunicipality, upsertDataSourceByName, treasury_list_source_ids verification, fatal error handler)
    - scripts/seedLACountyLinks.js (verify LA_COUNTY_ID value at line 36 and Long Beach membership in LA_COUNTY_CITY_NAMES at line 63)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md (seedLongBeachBakersfieldCA.js section — full file structure with exact payloads)
  </read_first>
  <action>
    Create scripts/seedLongBeachBakersfieldCA.js modeled exactly on scripts/seedOaklandSanJoseCA.js. Reuse verbatim: loadEnv() (tries ../.env.local then ../.env), Supabase client init (SUPABASE_URL default 'https://kxsdzaojfaibhuzmclfq.supabase.co', key from SUPABASE_SERVICE_KEY || SUPABASE_SERVICE_ROLE_KEY, exit(1) if missing), upsertMunicipality(m) (SELECT by name+state via .maybeSingle() then INSERT or UPDATE, returns row.id), upsertDataSourceByName(src) (SELECT by name then INSERT or UPDATE), and the fatal handler `main().catch(e => { console.error('Fatal:', e); process.exit(2); })`.

    Define `const LA_COUNTY_ID = 'f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1';` (verified from seedLACountyLinks.js line 36).

    MUNICIPALITIES array (two entries):
    - Long Beach: { name: 'Long Beach', state: 'CA', entity_type: 'city', population: 451000, population_year: 2024, county_id: LA_COUNTY_ID } — set county_id directly because Long Beach IS in the Phase 25 LA County 88-city list (D per RESEARCH §Long Beach county_id Integration).
    - Bakersfield: { name: 'Bakersfield', state: 'CA', entity_type: 'city', population: 417000, population_year: 2024 } — NO county_id key (Kern County not loaded; deferred per CONTEXT.md).

    Add a comment above the array: `// Long Beach FY runs Oct 1 – Sep 30; stored as ending year (D-01)`. This documents the non-standard FY period in the seeder only (D-02) — no DB schema change, no UI change.

    Capture returned ids: `const longBeachId = await upsertMunicipality(MUNICIPALITIES[0]);` and `const bakersfieldId = await upsertMunicipality(MUNICIPALITIES[1]);`.

    dataSources array (four entries, exact names — these names are the contract that processors and enrichment look up):
    - { name: 'Long Beach General Fund Operating Budget', api_type: 'pdf_download', dataset_type: 'operating', dataset_id: 'longbeach-gf-operating', base_url: 'https://www.longbeach.gov/globalassets/finance/media-library/documents/city-budget-and-finances/budget/budget-documents/', municipality_id: longBeachId }
    - { name: 'Long Beach General Fund Revenue Budget', api_type: 'pdf_download', dataset_type: 'revenue', dataset_id: 'longbeach-gf-revenue', base_url: same longbeach.gov URL, municipality_id: longBeachId }
    - { name: 'Bakersfield Operating Budget', api_type: 'pdf_download', dataset_type: 'operating', dataset_id: 'bakersfield-operating', base_url: 'https://docs.bakersfieldcity.us/', municipality_id: bakersfieldId }
    - { name: 'Bakersfield Revenue Budget', api_type: 'pdf_download', dataset_type: 'revenue', dataset_id: 'bakersfield-revenue', base_url: 'https://docs.bakersfieldcity.us/', municipality_id: bakersfieldId }

    Loop the array calling upsertDataSourceByName(src) for each.

    Verification block: call `supabase.rpc('treasury_list_source_ids')`, then for the expectedNames array (the four data_source names above) assert each appears in the listing; log `OK: {name}` for found, `MISSING: {name}` for absent. If any missing, console.error and process.exit(1).

    Do NOT log SUPABASE_SERVICE_KEY anywhere (Security T-28-06 — never log the service key).
  </action>
  <verify>
    <automated>node scripts/seedLongBeachBakersfieldCA.js</automated>
  </verify>
  <acceptance_criteria>
    - Running `node scripts/seedLongBeachBakersfieldCA.js` exits 0 and prints `OK:` for all four data_source names: 'Long Beach General Fund Operating Budget', 'Long Beach General Fund Revenue Budget', 'Bakersfield Operating Budget', 'Bakersfield Revenue Budget'.
    - DB query confirms Long Beach municipality row: state='CA', population=451000, population_year=2024, county_id='f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1'.
    - DB query confirms Bakersfield municipality row: state='CA', population=417000, population_year=2024, county_id IS NULL.
    - Source contains the literal comment `Long Beach FY runs Oct 1` and the literal `D-01`.
    - Source contains the literal `D-02` is documented via the seeder comment (FY period note); no schema/UI change present in this file.
    - No `console.log` line includes `SUPABASE_SERVICE_KEY` or the key value.
  </acceptance_criteria>
  <done>Seeder runs clean, all four data_sources verified present via RPC, both municipality rows correct (Long Beach with LA county_id, Bakersfield with NULL county_id).</done>
</task>

<task type="auto">
  <name>Task 2: Re-run idempotency check</name>
  <files>scripts/seedLongBeachBakersfieldCA.js</files>
  <read_first>
    - scripts/seedLongBeachBakersfieldCA.js (the file created in Task 1)
  </read_first>
  <action>
    Run the seeder a second time without modification to confirm idempotency. The upsertMunicipality() and upsertDataSourceByName() patterns SELECT-then-UPDATE on existing rows, so a second run must update-in-place (printing `(updated existing ...)`) rather than insert duplicates. No code change is expected in this task — it is a verification gate. If the second run produces duplicate rows or errors, fix the upsert dedup logic in scripts/seedLongBeachBakersfieldCA.js to match the SELECT-by-unique-key pattern in seedOaklandSanJoseCA.js.
  </action>
  <verify>
    <automated>node scripts/seedLongBeachBakersfieldCA.js</automated>
  </verify>
  <acceptance_criteria>
    - Second run exits 0 and prints `OK:` for all four data_source names.
    - Second-run output shows `(updated existing ...)` log lines (not `(inserted new ...)`) for the municipality and data_source rows.
    - DB query: COUNT of municipalities WHERE name IN ('Long Beach','Bakersfield') AND state='CA' equals exactly 2 (no duplicates).
    - DB query: COUNT of data_sources WHERE name IN (the four names) equals exactly 4 (no duplicates).
  </acceptance_criteria>
  <done>Second seeder run updates in place; municipality count = 2 and data_source count = 4 with no duplicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→Supabase | Service-role key writes to treasury schema; no user input crosses this boundary |
| .env→process | Service key loaded from local .env files into process env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | Information disclosure | SUPABASE_SERVICE_KEY in logs | mitigate | loadEnv() pattern; key never console.logged (copied from seedOaklandSanJoseCA.js) |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit confirms zero installs) |
</threat_model>

<verification>
- Seeder exits 0 on first and second run.
- All four data_source names returned by treasury_list_source_ids.
- Municipality rows present with correct population and county_id.
</verification>

<success_criteria>
- POPUL-01 (seeding portion): Long Beach (451000) and Bakersfield (417000) municipality rows present with population_year=2024.
- Four data_source rows present and verified by RPC.
- Seeder is idempotent (re-run safe).
</success_criteria>

<output>
Create `.planning/phases/29-long-beach-bakersfield-ca-data-load/29-01-SUMMARY.md` when done.
</output>
