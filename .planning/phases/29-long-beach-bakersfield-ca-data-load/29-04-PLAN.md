---
phase: 29-long-beach-bakersfield-ca-data-load
plan: 04
type: execute
wave: 3
depends_on: ["29-02", "29-03"]
files_modified:
  - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md
autonomous: false
requirements: [ENRICH-01, POPUL-01, DATA-04, DATA-07]
must_haves:
  truths:
    - "Long Beach and Bakersfield top operating (and any revenue) categories have AI enrichment descriptions in DB"
    - "Combined enrichment cost stayed under the $0.10 gate (D-08); estimated before running"
    - "Both cities appear under California in the app city picker with correct operating totals and per-capita display"
  artifacts:
    - path: ".planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md"
      provides: "Phase verification record covering all 6 ROADMAP success criteria"
      contains: "DATA-04"
  key_links:
    - from: "scripts/enrichCategories.js"
      to: "treasury enrichment rows (name_key upsert)"
      via: "--city/--state/--year invocation per city"
      pattern: "enrichCategories"
---

<objective>
Enrich the Long Beach and Bakersfield budget categories with plain-language AI descriptions (behind the $0.10 combined cost gate per D-08), then spot-check the app to confirm both cities satisfy all six ROADMAP Phase 29 success criteria, and write 29-VERIFICATION.md.

Purpose: ENRICH-01 — both new CA cities have AI category enrichment; final confirmation that DATA-04, DATA-07, and POPUL-01 are visible end-to-end in the app.
Output: enrichment rows in DB, `29-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-CONTEXT.md
@scripts/enrichCategories.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estimate enrichment cost (dry-run) for both cities</name>
  <files>.planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md</files>
  <read_first>
    - scripts/enrichCategories.js (confirm --city/--state/--year/--dry-run flags and how it reports a cost/token estimate)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-CONTEXT.md (D-08 — combined $0.10 gate, tighter than project-wide $5)
  </read_first>
  <action>
    Run cost-estimate dry-runs for every loaded fiscal year of both cities (use the FYs actually loaded by Plans 02 and 03 — at minimum 2025 and 2026):
    `node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2025 --dry-run`
    `node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2026 --dry-run`
    `node scripts/enrichCategories.js --city Bakersfield --state CA --year 2025 --dry-run`
    `node scripts/enrichCategories.js --city Bakersfield --state CA --year 2026 --dry-run`
    Sum the reported cost/token estimates across all dry-runs into a single combined figure. Record the per-run and combined estimate in 29-VERIFICATION.md (create the file if absent). Do NOT run live enrichment in this task — that is gated by Task 2.
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2025 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Each dry-run command exits 0 and reports a cost or token estimate.
    - 29-VERIFICATION.md records the per-run estimates and the combined total in dollars.
    - The combined estimate value is explicitly stated so Task 2's gate can be evaluated against $0.10.
  </acceptance_criteria>
  <done>Combined enrichment cost estimate computed and recorded in 29-VERIFICATION.md.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Cost gate — approve enrichment only if combined estimate < $0.10</name>
  <decision>Proceed with live AI enrichment for Long Beach + Bakersfield?</decision>
  <context>
    Per D-08, the combined enrichment cost gate for this phase is $0.10 (tighter than the project-wide $5 threshold). The combined dry-run estimate from Task 1 is recorded in 29-VERIFICATION.md. Empowered Vote is an unfunded nonprofit; do not exceed the gate without explicit approval.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with live enrichment</name>
      <pros>Combined estimate is under $0.10; satisfies ENRICH-01; categories get plain-language descriptions</pros>
      <cons>Incurs the (sub-$0.10) AI API cost</cons>
    </option>
    <option id="hold">
      <name>Hold — estimate at/over $0.10 or unexpected</name>
      <pros>Avoids overspend; lets the user review scope (e.g. unexpectedly many categories/FYs)</pros>
      <cons>ENRICH-01 not yet satisfied; phase blocked pending decision</cons>
    </option>
  </options>
  <resume-signal>Select: proceed (combined estimate confirmed &lt; $0.10) or hold (with reason)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run live enrichment for both cities</name>
  <files>.planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md</files>
  <read_first>
    - scripts/enrichCategories.js (idempotent via name_key upsert — safe to re-run)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md (the recorded combined estimate from Task 1)
  </read_first>
  <action>
    Only after Task 2 approval (combined estimate confirmed < $0.10), run live enrichment for every loaded FY of both cities (omit --dry-run):
    `node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2025`
    `node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2026`
    `node scripts/enrichCategories.js --city Bakersfield --state CA --year 2025`
    `node scripts/enrichCategories.js --city Bakersfield --state CA --year 2026`
    Include any revenue FY years that Plans 02/03 loaded, if enrichCategories.js enriches revenue categories. The script is idempotent (name_key upsert) so re-running is safe. Record the actual completion (cities, FYs enriched, row counts) in 29-VERIFICATION.md.
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city "Long Beach" --state CA --year 2025 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Live enrichment runs exit 0 for both cities across all loaded FYs.
    - DB query: enrichment rows exist for Long Beach top operating categories (description non-empty) for ≥1 FY.
    - DB query: enrichment rows exist for Bakersfield top operating categories (description non-empty) for ≥1 FY.
    - Re-running the dry-run after live enrichment reports near-zero remaining cost (categories already enriched — idempotent).
    - 29-VERIFICATION.md records the cities and FYs enriched.
  </acceptance_criteria>
  <done>Both cities enriched (descriptions non-empty for top categories); idempotency confirmed; recorded in VERIFICATION.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: App spot-check — verify all 6 Phase 29 success criteria, write VERIFICATION</name>
  <what-built>
    Long Beach and Bakersfield seeded (Plan 01), operating + best-effort revenue loaded (Plans 02, 03), and categories enriched (Task 3). Both cities should now render fully in the app.
  </what-built>
  <how-to-verify>
    Open https://treasurytracker.empowered.vote and verify each ROADMAP Phase 29 success criterion:
    1. "Long Beach" and "Bakersfield" appear in the city picker under "California".
    2. Long Beach operating budget total is in the ~$1.5B General Fund range (Port of LB excluded; no Harbor/Port categories).
    3. Bakersfield operating budget total is in the ~$765M range (not ~$287M GF-only).
    4. Both cities show a Revenue / Money In tab with at least one fiscal year populated (or note the documented revenue gap from Plan 02/03 SUMMARY).
    5. Per-capita ($/resident) displays for Long Beach (~$3,300/capita at $1.5B / 451K) and Bakersfield (~$1,835/capita at $765M / 417K).
    6. Enrichment descriptions are visible (not empty) for top categories in both cities.
    Record pass/fail for each of the 6 criteria in 29-VERIFICATION.md, with the observed operating totals and per-capita values. If any criterion fails, note the gap (it becomes a gap-closure candidate).
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 criteria pass (or describe which failed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→AI API | enrichCategories.js calls the AI provider; cost is the controlled risk |
| script→Supabase | enrichment rows written via service role; name_key upsert idempotent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Denial of service (cost) | AI enrichment spend | mitigate | $0.10 combined cost gate (D-08); blocking decision checkpoint before live run; dry-run estimate first |
| T-29-06 | Information disclosure | service/API keys in logs | mitigate | loadEnv pattern; keys never logged |
| T-29-SC | Tampering | package installs | mitigate | no new packages; enrichCategories.js reused unchanged |
</threat_model>

<verification>
- Combined enrichment cost estimated < $0.10 and approved before live run.
- Enrichment rows present for both cities.
- All 6 ROADMAP success criteria confirmed in-app and recorded in 29-VERIFICATION.md.
</verification>

<success_criteria>
- ENRICH-01: Long Beach and Bakersfield categories enriched within the $0.10 gate.
- POPUL-01: per-capita displays correctly for both cities.
- DATA-04 / DATA-07: both cities visible in-app with correct operating totals.
- 29-VERIFICATION.md written with pass/fail for all 6 criteria.
</success_criteria>

<output>
Create `.planning/phases/29-long-beach-bakersfield-ca-data-load/29-04-SUMMARY.md` and `.planning/phases/29-long-beach-bakersfield-ca-data-load/29-VERIFICATION.md` when done.
</output>
