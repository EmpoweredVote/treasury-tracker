---
phase: 29-long-beach-bakersfield-ca-data-load
plan: 03
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - scripts/extractBakersfield.py
  - scripts/processBakersfield.js
  - docs/Bakersfield/
autonomous: true
requirements: [DATA-07]
must_haves:
  truths:
    - "Bakersfield adopted budget PDFs (FY2024-25 + FY2025-26 minimum) are downloaded into docs/Bakersfield/"
    - "extractBakersfield.py emits all-operating-funds department rows (~$765M scope, not General-Fund-only ~$287M) with ending-year fiscal_year"
    - "processBakersfield.js --dry-run produces an operating total inside the $600M–$900M sanity band for each FY"
    - "Live load writes Bakersfield operating (and best-effort revenue) rows via treasury_sync_budget_tree"
  artifacts:
    - path: "scripts/extractBakersfield.py"
      provides: "pdfplumber all-funds operating extractor with filename FY detection"
      contains: "detect_fy_from_filename"
    - path: "scripts/processBakersfield.js"
      provides: "Node processor: execSync Python → tree → treasury_sync_budget_tree RPC with sanity band"
      contains: "treasury_sync_budget_tree"
  key_links:
    - from: "scripts/processBakersfield.js"
      to: "scripts/extractBakersfield.py"
      via: "execSync python invocation"
      pattern: "extractBakersfield\\.py"
    - from: "scripts/processBakersfield.js"
      to: "treasury_sync_budget_tree"
      via: "supabase.rpc on live load"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processBakersfield.js"
      to: "treasury.data_sources"
      via: "looks up Bakersfield data_source (seeded in Plan 01)"
      pattern: "ensureMunicipality"
---

<objective>
Build the Bakersfield pipeline: download the adopted budget PDFs, write `extractBakersfield.py` (pdfplumber all-operating-funds department extractor with ending-year FY detection), write `processBakersfield.js` (Node orchestrator with worktree-safe PDF discovery, $600M–$900M sanity band, treasury_sync_budget_tree RPC), validate via dry-run, then live-load operating and best-effort revenue.

Purpose: DATA-07 — Bakersfield operating budget (~$765M across ALL operating funds, NOT the General-Fund-only ~$287M) visible in the app. The CSV path was rejected by research (D-03: CSV covers only FY2019–2024 and yields wrong scope) — PDF extraction is required per D-04.
Output: `scripts/extractBakersfield.py`, `scripts/processBakersfield.js`, `docs/Bakersfield/*.pdf`, plus Bakersfield budget rows in DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md
@.planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md
@scripts/extractOakland.py
@scripts/processOakland.js
@scripts/extractFremont.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Bakersfield adopted budget PDFs and write extractBakersfield.py</name>
  <files>scripts/extractBakersfield.py, docs/Bakersfield/</files>
  <read_first>
    - scripts/extractOakland.py (template — copy parse_money() verbatim lines 28-43; model extract_budget() on lines 132-311; copy dedup-by-(department,fiscal_year) lines 295-309; copy argparse main lines 351-358)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md (§Bakersfield PDF Download Reference for WebLink URLs and FY availability; §D-03 Decision explaining all-funds vs General-Fund scope; §Common Pitfalls 3 and 5; Open Question 2 on all-funds summary location; Assumption A1/A4 on PDF structure)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md (extractBakersfield.py section — docstring, detect_fy_from_filename for fy2024-25 form, extract_budget skeleton with all-funds marker)
  </read_first>
  <action>
    Create `docs/Bakersfield/` and download the adopted budget PDFs. Sources (RESEARCH §Bakersfield PDF Download Reference): FY2024-25 at https://docs.bakersfieldcity.us/WebLink/DocView.aspx?id=1957514&dbid=0&repo=CITYRECORDS and FY2025-26 at https://docs.bakersfieldcity.us/WebLink/DocView.aspx?id=1957516&dbid=0&repo=CITYRECORDS . These are WebLink JavaScript viewer pages, NOT direct PDF URLs (Pitfall 5) — the DocView URL will not return a PDF via plain download. Obtain the direct PDF download URL from the WebLink viewer (the WebLink "Download"/"DownloadAsPDF" endpoint, typically of the form `.../WebLink/DownloadAsset.aspx?id=...` or `.../WebLink/0/doc/{docid}/Content`); if it cannot be resolved programmatically, this becomes a blocking human-download step — surface the WebLink page URLs to the user and ask them to place the PDFs in docs/Bakersfield/. Save as `docs/Bakersfield/fy2024-25-adopted-budget.pdf` and `docs/Bakersfield/fy2025-26-adopted-budget.pdf` so the FY detector works. Minimum acceptable set is these two FYs (D-06); FY2022-23/FY2023-24 are optional if accessible.

    Inspect the PDF before finalizing the extractor (Open Question 2 / A1): scan for the all-funds operating summary table — search the table of contents and page text for "ALL FUNDS", "DEPARTMENT SUMMARY", "OPERATING SUMMARY", or an expenditures-by-department-across-funds table. The $765M target spans General Fund (~$287M) + PUBSAF 1% Sales Tax (~$130M) + Equipment Management + Refuse + others, so a General-Fund-only filter is WRONG (Pitfall 3). Determine the correct section/marker from the actual PDF.

    Write scripts/extractBakersfield.py. Copy parse_money() verbatim from extractOakland.py. Add detect_fy_from_filename: match `fy(\d{4})-(\d{2})` and return century + ending-two-digits (fy2024-25 → 2025, fy2025-26 → 2026). Write extract_budget(pdf_path): open with pdfplumber, derive fiscal_year, locate the all-funds operating summary section using the marker confirmed during inspection, parse department + amount rows into dicts { department, fund: 'All Operating Funds', adopted_amount, fiscal_year, page_num }; filter amount > 100_000 and department length > 2. Dedup by (department, fiscal_year). main: argparse positional pdf_path; print json.dumps(..., indent=2). Replace the PATTERNS placeholder marker logic with the real section boundary found during inspection — do not ship the placeholder 'ALL FUNDS'+'OPERATING' heuristic unverified.
  </action>
  <verify>
    <automated>python scripts/extractBakersfield.py "docs/Bakersfield/fy2024-25-adopted-budget.pdf"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/Bakersfield/` contains at least `fy2024-25-adopted-budget.pdf` and `fy2025-26-adopted-budget.pdf`.
    - `python scripts/extractBakersfield.py "docs/Bakersfield/fy2024-25-adopted-budget.pdf"` prints a JSON array of objects with keys department, fund, adopted_amount, fiscal_year, page_num.
    - Every emitted object has `fiscal_year` equal to 2025 for the fy2024-25 PDF (ending-year convention).
    - Sum of adopted_amount across the FY2025 rows is in the $600M–$900M range (all-operating-funds scope, NOT ~$287M General-Fund-only).
    - Source contains `def detect_fy_from_filename` and `def parse_money`.
  </acceptance_criteria>
  <done>Bakersfield PDFs downloaded (≥ FY2024-25, FY2025-26); extractor emits all-funds department rows summing ~$765M with ending-year FY.</done>
</task>

<task type="auto">
  <name>Task 2: Write processBakersfield.js and dry-run validate the sanity band + scale</name>
  <files>scripts/processBakersfield.js</files>
  <read_first>
    - scripts/processOakland.js (template — copy loadEnv, Supabase init, resolvePdfDir, extractPDF, buildOperatingTree, ensureMunicipality, upsertDataSource, loadFiscalYear, processPDF sanity band, main parseArgs, fatal handler)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-PATTERNS.md (processBakersfield.js section — sanity band $600M–$900M, resolvePdfDir 'Bakersfield', extractBakersfield.py script name, fund label 'All Operating Funds', ensureMunicipality 'Bakersfield'/'CA')
    - scripts/extractBakersfield.py (created in Task 1 — processor's extractPDF parses its JSON shape)
  </read_first>
  <action>
    Write scripts/processBakersfield.js modeled exactly on processOakland.js (and structurally identical to processLongBeach.js with Bakersfield substitutions). Reuse loadEnv(), Supabase init (key never logged — T-29-06), fatal handler.

    Sanity band: `const OP_BAND_MIN = 600_000_000;` and `const OP_BAND_MAX = 900_000_000;` (target ~$765M per DATA-07).

    resolvePdfDir(): check `path.join(ROOT, 'docs', 'Bakersfield')` with git-common-dir worktree fallback.

    extractPDF(pdfPath): execSync `${pythonBin} "${pyScript}" "${pdfPath}"` with pyScript = scripts/extractBakersfield.py, pythonBin 'python' on win32 else 'python3', maxBuffer 8 * 1024 * 1024 (T-29-04), JSON.parse result. Path from controlled readdir, double-quoted (T-29-05).

    buildOperatingTree(rows): nodes { n: department, a: amount, i: [{ d, a, aa: null, f: 'All Operating Funds', e: null }] }, total, sort desc; return { tree, total }.

    ensureMunicipality(): SELECT WHERE name='Bakersfield' AND state='CA'; if missing, console.error("Bakersfield, CA municipality not found — run seedLongBeachBakersfieldCA.js first") and exit(2).

    upsertDataSource(muniId, fiscalYear, datasetType, pdfAbsPath): name `Bakersfield ${datasetType==='revenue'?'Revenue':'Operating'} Budget FY${fiscalYear}`; api_type 'pdf_download'; dataset_type datasetType; dataset_id `fy${fiscalYear}`; base_url 'file://'+pdfAbsPath; fiscal_years [fiscalYear]; municipality_id muniId. SELECT-then-UPDATE/INSERT by (municipality_id, api_type, dataset_id, dataset_type).

    loadFiscalYear(...): treasury_sync_budget_tree with p_data_source_id/p_fiscal_year/p_dataset_type/p_total/p_tree/p_row_count/p_triggered_by:'bulk_load'; handle errors.

    processPDF: group by fiscal_year, build tree+total, sanity band halt — if total < OP_BAND_MIN || total > OP_BAND_MAX, console.error SCALE MISMATCH (causes: General-Fund-only extraction yielding ~$287M, wrong units, wrong section) and process.exit(3) BEFORE any live write (T-29-07). If not dryRun, loadFiscalYear operating.

    main(): parseArgs options 'dry-run' (boolean), pdf (string), 'revenue' (boolean), strict:false. PDF discovery via readdirSync or --pdf. ensureMunicipality only on live run.

    SCALE VERIFICATION: run `node scripts/processBakersfield.js --dry-run`; each FY total must land in $600M–$900M. If total ≈ $287M, the extractor captured General-Fund-only — return to extractBakersfield.py and widen to the all-funds summary section (Pitfall 3). If total is off by 1000x, amounts are in thousands — apply a scale fix. Iterate until totals land in band.
  </action>
  <verify>
    <automated>node scripts/processBakersfield.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processBakersfield.js --dry-run` exits 0 and prints a per-FY operating total for each downloaded PDF.
    - Each printed FY operating total is within $600,000,000 and $900,000,000 (no SCALE MISMATCH exit(3); not ~$287M).
    - Source contains `OP_BAND_MIN = 600_000_000` and `OP_BAND_MAX = 900_000_000`.
    - Source contains `treasury_sync_budget_tree`, `extractBakersfield.py`, and `maxBuffer: 8 * 1024 * 1024`.
    - Source contains `ensureMunicipality` selecting name='Bakersfield' and state='CA'.
    - No SUPABASE_SERVICE_KEY value is console.logged.
  </acceptance_criteria>
  <done>processBakersfield.js dry-run produces operating totals inside the $600M–$900M band for every downloaded FY; all-funds scope confirmed (not GF-only).</done>
</task>

<task type="auto">
  <name>Task 3: Live-load Bakersfield operating + best-effort revenue</name>
  <files>scripts/processBakersfield.js, scripts/extractBakersfield.py</files>
  <read_first>
    - scripts/processBakersfield.js (created in Task 2)
    - scripts/extractBakersfield.py (created in Task 1)
    - scripts/extractFremont.py (reference for operating + revenue in same PDF — best-effort revenue)
    - .planning/phases/29-long-beach-bakersfield-ca-data-load/29-RESEARCH.md (§D-03 — revenue CSV ~$298M GF for FY2024 confirms a revenue table exists in source; best-effort CA revenue pattern)
  </read_first>
  <action>
    Run the live operating load: `node scripts/processBakersfield.js` (no --dry-run). Requires Plan 01 seeder to have run (ensureMunicipality). Upserts per-FY operating data_sources and writes operating rows via treasury_sync_budget_tree for every downloaded FY; sanity band halt protects against bad data.

    Then attempt best-effort revenue (DATA-07 specifies "operating + revenue"). Inspect the adopted budget PDF for a revenue/resources summary table (the CSV evidence in RESEARCH §D-03 shows a revenue side exists). If found, add extract_revenue() to extractBakersfield.py and a --revenue branch in processBakersfield.js loading dataset_type='revenue' (model on extractFremont.py). Apply a revenue-appropriate sanity check (NOT the operating $600M–$900M band). Best-effort: load at least one FY of revenue so the Money In tab populates; if no clean revenue table exists in any available PDF, document the gap and which PDFs/pages were inspected in the SUMMARY and proceed.

    Verify in DB: operating rows for each loaded FY; revenue rows for ≥1 FY or documented gap.
  </action>
  <verify>
    <automated>node scripts/processBakersfield.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Live operating load completed: DB query of Bakersfield budget rows returns operating data for FY2025 and FY2026 with totals in the $600M–$900M range.
    - At least one fiscal year of Bakersfield revenue (dataset_type='revenue') rows exists in DB; OR the SUMMARY documents why no clean revenue table was available and which PDFs were inspected.
    - `node scripts/processBakersfield.js --dry-run` still exits 0 (regression check — operating path unchanged).
    - If extract_revenue() was added, extractBakersfield.py contains `def extract_revenue` and processBakersfield.js handles `--revenue`.
  </acceptance_criteria>
  <done>Bakersfield operating rows live in DB within sanity band; revenue loaded best-effort (≥1 FY) or gap documented in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF file→Python | PDF content parsed; paths from controlled docs/Bakersfield/ readdir |
| Python stdout→Node | JSON via execSync with bounded maxBuffer |
| Node→Supabase | Service-role RPC writes; sanity band gates the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Denial of service | execSync large PDF | mitigate | maxBuffer 8MB cap on execSync |
| T-29-05 | Tampering | PDF path injection in execSync | mitigate | path from controlled docs/Bakersfield/ readdirSync, double-quoted |
| T-29-07 | Tampering/integrity | wrong-scope (GF-only) or wrong-scale data to DB | mitigate | sanity band halt (exit 3) before RPC; band excludes the ~$287M GF-only figure |
| T-29-06 | Information disclosure | service key in logs | mitigate | loadEnv pattern; key never logged |
| T-29-SC | Tampering | package installs | mitigate | no new packages (pdfplumber pre-installed) |
</threat_model>

<verification>
- extractBakersfield.py emits all-funds rows (~$765M) with ending-year FY.
- processBakersfield.js --dry-run totals inside $600M–$900M band.
- Live operating load present in DB; revenue best-effort.
</verification>

<success_criteria>
- DATA-07 (operating): Bakersfield all-operating-funds ~$765M loaded for ≥ FY2025, FY2026.
- All-funds scope confirmed (not GF-only ~$287M).
- Revenue loaded best-effort (≥1 FY) or documented gap.
</success_criteria>

<output>
Create `.planning/phases/29-long-beach-bakersfield-ca-data-load/29-03-SUMMARY.md` when done.
</output>
