---
phase: 10-collin-county
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/processGarlandBudget.js
  - scripts/processRichardsonBudget.js
autonomous: false

must_haves:
  truths:
    - "Garland operating budget data appears in the app with named departments and a plausible grand total ($200M–$500M)"
    - "Richardson operating budget data appears in the app with named departments and a plausible grand total ($100M–$250M)"
    - "Re-running the loader scripts does not duplicate budget rows (idempotent via treasury_sync_budget_tree)"
    - "Department names are recognizable (not 'Unknown') for ≥80% of loaded rows"
  artifacts:
    - path: "scripts/processGarlandBudget.js"
      provides: "Garland-specific budget loader OR Haiku-pipeline invocation wrapper, per routing decision in 10-01-DRYRUN-NOTES.md"
      contains: "Garland"
    - path: "scripts/processRichardsonBudget.js"
      provides: "Richardson-specific budget loader OR Haiku-pipeline invocation wrapper, per routing decision in 10-01-DRYRUN-NOTES.md"
      contains: "Richardson"
  key_links:
    - from: "scripts/processGarlandBudget.js"
      to: "treasury.budgets (municipality_id=Garland, dataset_type='operating')"
      via: "treasury_sync_budget_tree RPC with p_triggered_by='bulk_load'"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processRichardsonBudget.js"
      to: "treasury.budgets (municipality_id=Richardson, dataset_type='operating')"
      via: "treasury_sync_budget_tree RPC with p_triggered_by='bulk_load'"
      pattern: "treasury_sync_budget_tree"
    - from: "treasury.budgets (Garland/Richardson)"
      to: "App operating budget view"
      via: "DatasetTabs.tsx availableDatasets check"
      pattern: "dataset_type.*operating"
---

<objective>
Live-load operating budget data for Garland and Richardson into `treasury.budgets`, using the routing decisions recorded in `10-01-DRYRUN-NOTES.md`. Each city follows its assigned path: pdftotext-first (write a city-specific parser modeled on `processLongviewBudget.js`), or Haiku-fallback (use `bulkLoadPDF.js --source "<name>"`). The plan ends with a human checkpoint confirming both cities appear in the app with named departments and plausible totals.

Purpose: Garland (~240k pop) and Richardson (~115k pop) are the two largest cities remaining in Collin County. They satisfy Success Criterion 1 of Phase 10. They run in parallel with 10-03 because they touch disjoint files and disjoint data_sources rows.

Output:
1. `treasury.budgets` rows for Garland and Richardson (FY2025 or FY2026, per dry-run preferred FY).
2. Operating budget tab visible for both cities in the app.
3. Per-city loader script (`processGarlandBudget.js`, `processRichardsonBudget.js`) checked into `scripts/`, OR documented invocation of `bulkLoadPDF.js` if Haiku fallback was the routing decision.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-collin-county/10-CONTEXT.md
@.planning/phases/10-collin-county/10-RESEARCH.md
@.planning/phases/10-collin-county/10-01-DRYRUN-NOTES.md
@.planning/phases/10-collin-county/10-01-SUMMARY.md
@scripts/processLongviewBudget.js
@scripts/bulkLoadPDF.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load Garland operating budget</name>
  <files>scripts/processGarlandBudget.js</files>
  <action>
**Step A — Read the routing decision for Garland from `.planning/phases/10-collin-county/10-01-DRYRUN-NOTES.md`.** The Garland section's "Routing decision" line dictates which branch below to follow.

---

**BRANCH 1: Routing decision = `pdftotext-parser`**

Create `scripts/processGarlandBudget.js` modeled on `scripts/processLongviewBudget.js`. Key elements to copy:
- Module-level `PDF_URL` constant (use FY2025 from RESEARCH.md unless 10-01 notes say otherwise)
- `CACHE_PATH = 'C:/tmp/collin-budgets/garland_fy<year>.pdf'`
- `FISCAL_YEAR = 2025` (or 2026 per dry-run preferred FY)
- `MUNICIPALITY_NAME = 'Garland'`
- `DATA_SOURCE_NAME = 'Garland Operating Budget FY2025'` (must match the row seeded in 10-01)
- Supabase client setup using `SUPABASE_SERVICE_KEY`
- `parseGarlandBudget(lines)` function — adapt the column-position detection and money parser from `parseLongviewFormat` in `processLongviewBudget.js`. Use the layout you observed in 10-01 dry-run to drive column positions.
- INCLUDED/EXCLUDED department sets — derive from the dry-run header sample. Include only General Fund operating departments; exclude utility/internal-service/grant funds.
- `treasury_sync_budget_tree` RPC call with `p_triggered_by: 'bulk_load'` (REQUIRED — STATE.md confirmed)
- CLI flags: `--dry-run`, `--verbose`, `--no-cache`

Run dry-run first:
```powershell
node --env-file=.env scripts/processGarlandBudget.js --dry-run --verbose
```

Verify:
- Grand total is in $200M–$500M range.
- Department names are recognizable (City Manager, Police, Fire, Parks, etc.) — not "Unknown".
- No more than 20% of expected departments are missing/zero.

If dry-run passes, run live:
```powershell
node --env-file=.env scripts/processGarlandBudget.js
```

Verify with SQL:
```sql
SELECT b.fiscal_year, b.total_budget, COUNT(*) as row_count
FROM treasury.budgets b
JOIN treasury.data_sources ds ON ds.id = b.data_source_id
JOIN treasury.municipalities m ON m.id = ds.municipality_id
WHERE m.name = 'Garland' AND ds.dataset_type = 'operating'
GROUP BY b.fiscal_year, b.total_budget
ORDER BY b.fiscal_year;
```

Run the loader a SECOND time to confirm idempotency — row counts and totals must be unchanged.

---

**BRANCH 2: Routing decision = `Haiku-fallback`**

No new script needed — use the existing `bulkLoadPDF.js` pipeline (already calls Haiku vision with the Phase 8 `section_heading` prompt fix).

Run dry-run:
```powershell
node --env-file=.env scripts/bulkLoadPDF.js --source "Garland Operating Budget FY2025" --dry-run
```

Verify:
- Exit code 0 or 1 (NOT 2 — exit 2 = JSON truncation; Phase 8 fix should prevent this).
- Grand total in $200M–$500M.
- Section headings show named departments (not "Unknown" for >20%).

If dry-run passes, run live (no `--dry-run` flag):
```powershell
node --env-file=.env scripts/bulkLoadPDF.js --source "Garland Operating Budget FY2025"
```

Create `scripts/processGarlandBudget.js` as a thin wrapper that documents the invocation:
```js
#!/usr/bin/env node
/**
 * Garland Operating Budget Loader (Haiku fallback)
 *
 * pdftotext dry-run in Phase 10-01 showed the PDF layout was not tractable
 * for text parsing. This wrapper documents the production invocation:
 *
 *   node --env-file=.env scripts/bulkLoadPDF.js --source "Garland Operating Budget FY2025"
 *
 * See .planning/phases/10-collin-county/10-01-DRYRUN-NOTES.md for rationale.
 */
console.log('Use: node scripts/bulkLoadPDF.js --source "Garland Operating Budget FY2025"');
process.exit(0);
```

---

**BRANCH 3: Routing decision = `skip`**

If 10-01 notes documented `skip` for Garland (grand total wildly off, or summary-only PDF, or image-only PDF), document the skip in the SUMMARY file and proceed to Task 2 (Richardson). Phase 10 success criteria allow partial coverage — see CONTEXT.md Decision 2.

Create a stub `scripts/processGarlandBudget.js` with comment explaining why Garland was skipped, referencing the dry-run notes file.

**Anti-patterns to avoid:**
- Do NOT use ACFRs (RESEARCH.md Pitfall 6 — 5x inflation risk).
- Do NOT use FY2026 if the dry-run notes indicate the FY2026 PDF is summary-only (the 211KB document — see RESEARCH.md Critical Notes).
- Do NOT mix pdftotext and Haiku approaches in the same script. Pick one branch.
- Do NOT skip the SECOND idempotency run — re-running must not duplicate rows.
- Do NOT forget `p_triggered_by: 'bulk_load'` in the RPC call.
  </action>
  <verify>
1. `scripts/processGarlandBudget.js` exists in the working tree.
2. SQL query: `SELECT total_budget, COUNT(*) FROM treasury.budgets b JOIN treasury.data_sources ds ON ds.id = b.data_source_id JOIN treasury.municipalities m ON m.id = ds.municipality_id WHERE m.name = 'Garland' AND ds.dataset_type = 'operating' GROUP BY total_budget;` returns either:
   - At least one row with `total_budget` in $200M–$500M, OR
   - Zero rows (only if routing decision was `skip` and skip is documented in SUMMARY)
3. Department name audit: `SELECT category, COUNT(*) FROM treasury.budgets b JOIN treasury.data_sources ds ON ds.id = b.data_source_id JOIN treasury.municipalities m ON m.id = ds.municipality_id WHERE m.name = 'Garland' GROUP BY category ORDER BY COUNT(*) DESC LIMIT 20;` — top departments are recognizable city department names (not "Unknown").
4. Re-running the loader does not change row counts or grand total.
  </verify>
  <done>
Garland operating budget is loaded into `treasury.budgets` with a plausible grand total ($200M–$500M) and named departments, OR Garland is documented as skipped with a written rationale in the SUMMARY referencing 10-01 dry-run notes. Loader script is idempotent. `scripts/processGarlandBudget.js` exists either as parser, Haiku-wrapper, or skip-stub.
  </done>
</task>

<task type="auto">
  <name>Task 2: Load Richardson operating budget</name>
  <files>scripts/processRichardsonBudget.js</files>
  <action>
**Step A — Read the routing decision for Richardson from `.planning/phases/10-collin-county/10-01-DRYRUN-NOTES.md`.**

Follow the same three-branch logic as Task 1 (pdftotext-parser / Haiku-fallback / skip), substituting these Richardson-specific values:

- `PDF_URL` = `RICHARDSON_BUDGET_FY2026` (https://cdnsm5-hosted.civiclive.com/UserFiles/Servers/Server_7964838/File/Government/Departments/Finance/Budget/2026%20Adopted%20Budget%20Book.8-5-25%20compressed.final%20with%20TOC.pdf)
- `CACHE_PATH` = `C:/tmp/collin-budgets/richardson_fy2026.pdf`
- `FISCAL_YEAR` = 2026 (preferred per RESEARCH.md and CONTEXT.md "most recent first" rule)
- `MUNICIPALITY_NAME` = `'Richardson'`
- `DATA_SOURCE_NAME` = `'Richardson Operating Budget FY2026'`
- Sanity range: **$100M–$250M** for General Fund

**BRANCH 1 (pdftotext-parser):** Create `scripts/processRichardsonBudget.js` modeled on `processLongviewBudget.js`. Use the column layout observed in the 10-01 dry-run. Richardson's PDF is "compressed" — text extraction may have spacing artifacts; the column-position detection in `parseLongviewFormat` is robust to this.

Run dry-run, verify total in $100M–$250M, run live, verify idempotency by re-running.

**BRANCH 2 (Haiku-fallback):** Create a thin wrapper script documenting:
```bash
node --env-file=.env scripts/bulkLoadPDF.js --source "Richardson Operating Budget FY2026"
```
Dry-run first; live load second; verify.

**BRANCH 3 (skip):** Document rationale in SUMMARY referencing 10-01 notes.

**Richardson-specific notes from RESEARCH.md:**
- The `cor.net` domain blocks WebFetch but the CDN URL works directly. Use the `cdnsm5-hosted.civiclive.com` URL as-is.
- The PDF is described as "compressed" in the filename — text extraction may have unusual spacing. If pdftotext output looks corrupted (mojibake or missing characters), route to Haiku fallback.
- Richardson FY2025 actual total city budget was $406M; General Fund portion typically 40–60% of that, so expect $160M–$240M for GF operating. If grand total is $406M+, that's the *total* budget across all funds — needs General-Fund filtering.

**SQL verification after live load:**
```sql
SELECT b.fiscal_year, b.total_budget, COUNT(*) as row_count
FROM treasury.budgets b
JOIN treasury.data_sources ds ON ds.id = b.data_source_id
JOIN treasury.municipalities m ON m.id = ds.municipality_id
WHERE m.name = 'Richardson' AND ds.dataset_type = 'operating'
GROUP BY b.fiscal_year, b.total_budget;
```

**Anti-patterns to avoid:**
- Do NOT try the `cor.net` URL — use the CDN URL only (Pitfall 2).
- Do NOT load the "total city budget" if it includes all funds — filter to General Fund only.
- Do NOT modify `processGarlandBudget.js` — Richardson is a separate file.
- Do NOT skip the idempotency re-run.
  </action>
  <verify>
1. `scripts/processRichardsonBudget.js` exists.
2. SQL: `SELECT total_budget, COUNT(*) FROM treasury.budgets b JOIN treasury.data_sources ds ON ds.id = b.data_source_id JOIN treasury.municipalities m ON m.id = ds.municipality_id WHERE m.name = 'Richardson' AND ds.dataset_type = 'operating' GROUP BY total_budget;` returns at least one row in $100M–$250M, OR zero rows with skip documented.
3. Department-name audit: top categories are named (not "Unknown") for the majority of rows.
4. Idempotency: re-running the loader does not change row counts.
  </verify>
  <done>
Richardson operating budget is loaded into `treasury.budgets` (total in $100M–$250M, named departments) OR documented as skipped. Loader is idempotent. `scripts/processRichardsonBudget.js` exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Garland and Richardson in app</name>
  <what-built>
- Garland operating budget loaded (or documented as skipped) per Task 1.
- Richardson operating budget loaded (or documented as skipped) per Task 2.
- Idempotency verified for both loaders.
  </what-built>
  <how-to-verify>
1. Open the app at https://treasurytracker.empowered.vote (or local dev server if running locally).
2. Navigate to Garland's page:
   - Search/select Garland, TX from the city picker.
   - Confirm an "Operating Budget" tab/dataset appears.
   - Click into it. Expected: a tree/icicle view of departments with dollar amounts.
   - Sanity check: grand total displayed in $200M–$500M range.
   - Department names recognizable (Police, Fire, Parks, City Manager, etc.) — not "Unknown" dominating the tree.
3. Navigate to Richardson's page:
   - Confirm "Operating Budget" tab appears.
   - Click into it. Tree/icicle should render with departments.
   - Sanity check: grand total in $100M–$250M.
   - Department names recognizable.
4. If either city shows wildly inflated totals, missing departments, or "Unknown" dominance, document the issue and stop — do not approve.
5. Note: If a city was deliberately skipped per Task 1/Task 2 (routing decision = `skip`), it is acceptable for that city to NOT appear in the app. Verify the skip rationale is documented in the SUMMARY file.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Plan 10-02 complete, or describe any visible issues (wrong totals, "Unknown" department dominance, missing tab) so they can be addressed before continuing.</resume-signal>
</task>

</tasks>

<verification>
1. `treasury.budgets` contains rows for Garland (`dataset_type='operating'`) with grand total in $200M–$500M (unless skipped).
2. `treasury.budgets` contains rows for Richardson (`dataset_type='operating'`) with grand total in $100M–$250M (unless skipped).
3. Top-20 department audit shows recognizable names (not "Unknown" dominant) for both cities.
4. Re-running both loaders produces no changes to row counts.
5. App shows operating budget tab for both Garland and Richardson with rendered tree/icicle.
6. Human verification checkpoint approved.
</verification>

<success_criteria>
- Satisfies Phase 10 Success Criterion 1: "Garland and Richardson each have at least one fiscal year of operating budget data visible in the app with named departments" (or both are documented as skipped with rationale).
- Satisfies COL-01 and COL-02 from Phase 10 requirements.
- Honors CONTEXT.md Decision 3 (≥80% departments populated to load; >3x sanity total → skip).
- Honors CONTEXT.md Decision 1 (pdftotext-first; Haiku fallback only if pdftotext fails to converge).
- Both loader scripts are idempotent.
</success_criteria>

<output>
After completion, create `.planning/phases/10-collin-county/10-02-SUMMARY.md` summarizing:
- Garland: FY loaded, grand total, row count, routing branch used (pdftotext / Haiku / skip), idempotency confirmed
- Richardson: same fields
- Any skipped cities with rationale
- Any issues surfaced during human verification
- Link to the loader scripts created
</output>
