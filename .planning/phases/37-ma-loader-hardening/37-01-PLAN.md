---
phase: 37-ma-loader-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/scrapeMaDLS.js
autonomous: false
requirements: [LOAD-01]
must_haves:
  truths:
    - "Running `--explore --report gf-expenditures` returns a real data table (not a 404 / 'Definition does not exist' error)"
    - "The confirmed rdreport produces headers matching MA DLS GF expenditure category names (e.g. General Government, Public Safety, Education, Public Works)"
    - "REPORTS[].gf-expenditures has the confirmed rdreport and tableID with the 'best guess' comment removed"
  artifacts:
    - path: "scripts/scrapeMaDLS.js"
      provides: "Confirmed gf-expenditures REPORTS[] entry"
      contains: "gf-expenditures"
    - path: "scripts/output/explore_gf-expenditures.html"
      provides: "Fresh explore HTML proving the rdreport resolves to a real table"
  key_links:
    - from: "scripts/scrapeMaDLS.js REPORTS[gf-expenditures].rdreport"
      to: "MA DLS Gateway report definition"
      via: "exploreReport() GET request"
      pattern: "rdreport:\\s*'[^']+'"
---

<objective>
Confirm the correct `rdreport` (and `tableID`) for the MA DLS "General Fund Expenditures by Function" report, and update the `gf-expenditures` entry in `REPORTS[]` so the bulk load in Phase 38 produces correct operating data.

Purpose: The current rdreport (`ScheduleA.GF.ExpendituresByFunctionMain`) is CONFIRMED WRONG — the on-disk `explore_gf-expenditures.html` shows a "Definition ... does not exist" 404 error. Running the 351-city bulk load against a wrong tableID would label a non-operating dataset as "operating" across all cities. This is the highest-stakes item in the milestone and MUST be resolved before any MA city data is written (STATE.md sequencing constraint). Per D-01 + D-02 this is a discovery step requiring human portal navigation, not a rubber-stamp.

Output: Updated `REPORTS[]` `gf-expenditures` entry in `scripts/scrapeMaDLS.js` with a confirmed `rdreport`/`tableID` and the `// best guess` comment removed; a fresh `explore_gf-expenditures.html` proving the report resolves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ma-loader-hardening/37-CONTEXT.md
@.planning/phases/37-ma-loader-hardening/37-RESEARCH.md
@.planning/phases/37-ma-loader-hardening/37-PATTERNS.md
@scripts/scrapeMaDLS.js
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Human discovers the correct GF Expenditures rdreport from the MA DLS portal</name>
  <decision>What are the correct `rdreport` and `tableID` values for "General Fund Expenditures by Function" on the MA DLS Gateway?</decision>
  <context>
The current `REPORTS[1]` (gf-expenditures) value `rdreport: 'ScheduleA.GF.ExpendituresByFunctionMain'` with `tableID: 'xtGFExp'` is confirmed wrong — `scripts/output/explore_gf-expenditures.html` contains the server error "The Definition E:\wwwroot\gateway_test\_Definitions\_Reports\ScheduleA.GF.ExpendituresByFunctionMain.lgx does not exist." The portal is interactive (Logi Analytics framework) and the correct rdreport cannot be obtained via CLI — a human must read it from the portal URL bar. This is why this is a human-action discovery step per D-01/D-02. Known-working sibling values for format reference: special-revenue uses `ScheduleA.Special_Rev_Funds.SpecialRevFunds`; revenue-by-source uses `RevenueBySource.RBS.RevbySource2`.

Steps for the human:
1. Open https://dls-gw.dor.state.ma.us/reports/rdpage.aspx in a browser.
2. Navigate to the Schedule A "General Fund Expenditures by Function" report.
3. Read the `rdreport=` parameter from the resulting URL — this is the correct rdreport string.
4. Note the table/export ID if visible (the `rdExportTableID` / table `id="xt..."`); if not visible from the URL, leave tableID as-is and let Task 2's `--explore` run reveal the actual `id="xt..."` table IDs in the HTML.

RESEARCH candidate values are `[ASSUMED]` (training knowledge only, LOW confidence): `ScheduleA.GF.ExpendituresByFunction`, `ScheduleA.GF.GFExpenditures`, `ScheduleA.GeneralFund.ExpendituresByFunction`. The portal URL bar is the authoritative source — do NOT commit any candidate without portal confirmation.
  </context>
  <options>
    <option id="portal-confirmed">
      <name>Use the rdreport read from the live portal URL</name>
      <pros>Authoritative — same method that confirmed revenue-by-source and special-revenue</pros>
      <cons>Requires the human to be online and navigate the portal</cons>
    </option>
    <option id="candidate-then-verify">
      <name>Try a RESEARCH candidate, then confirm via Task 2 --explore output</name>
      <pros>Faster if a candidate happens to be correct</pros>
      <cons>Candidates are LOW-confidence; wrong values produce the same 404 — still need the portal as fallback</cons>
    </option>
  </options>
  <resume-signal>Provide the confirmed `rdreport` string (and `tableID` if known), or reply "portal" with the URL parameters you read.</resume-signal>
  <read_first>
    - scripts/scrapeMaDLS.js (REPORTS[] array lines ~51-89; exploreReport() lines ~445-505)
    - scripts/output/explore_gf-expenditures.html (the existing 404 error page — confirms current value is wrong; NOT a source of the correct value)
    - .planning/phases/37-ma-loader-hardening/37-RESEARCH.md (Pattern 3: Finding the Correct rdreport; Open Question 1)
  </read_first>
  <action>
This is a blocking human-decision checkpoint, not autonomous work. The executor pauses and presents the decision above. The human navigates the live MA DLS Gateway portal and supplies the confirmed `rdreport` (and `tableID` if read from the URL). The executor records the supplied value and passes it to Task 2 — it does NOT guess or auto-commit a candidate value. No code is written in this task.
  </action>
  <acceptance_criteria>
    - A concrete `rdreport` string is provided by the human (or portal URL pasted) that is NOT `ScheduleA.GF.ExpendituresByFunctionMain`
    - The provided value is captured for use in Task 2
  </acceptance_criteria>
  <done>The human has supplied a confirmed rdreport (and tableID if available) for GF Expenditures, captured for Task 2 to write into REPORTS[].</done>
</task>

<task type="auto">
  <name>Task 2: Run --explore with the confirmed rdreport and update REPORTS[gf-expenditures]</name>
  <files>scripts/scrapeMaDLS.js</files>
  <read_first>
    - scripts/scrapeMaDLS.js (REPORTS[1] gf-expenditures entry lines ~61-69; exploreReport() lines ~445-505; parseTable() lines ~170-190)
    - .planning/phases/37-ma-loader-hardening/37-PATTERNS.md (LOAD-01 section: shape to copy, known-working rdreport formats)
    - .planning/phases/37-ma-loader-hardening/37-RESEARCH.md (Pitfall 4 "explore succeeds but table is still wrong"; Pitfall 5 "existing HTML is stale")
  </read_first>
  <action>
Edit `REPORTS[1]` (the `gf-expenditures` entry) in `scripts/scrapeMaDLS.js`: set `rdreport` to the value confirmed in Task 1, and remove the trailing `// best guess — verify with --explore` comment on that line. Keep the entry's existing shape (`name: 'gf-expenditures'`, `label: 'General Fund Expenditures by Function'`, `exportFilename: 'gfexp'`, `datasetType: 'operating'`, `supportsType: false`) unchanged. Then run `node scripts/scrapeMaDLS.js --explore --report gf-expenditures`, which overwrites `scripts/output/explore_gf-expenditures.html` with the live response and prints the table IDs, headers, and row 1 sample. Read the console output: if the configured `tableID` (`xtGFExp`) is NOT listed among the discovered `id="xt..."` table IDs, update `tableID` in `REPORTS[1]` to the correct discovered table ID and re-run `--explore`. Confirm via the printed Headers line that they match MA DLS GF expenditure category names (e.g. General Government, Public Safety, Education, Public Works) and NOT a totals-only summary table (per Pitfall 4). Do NOT scrape or load any city data in this plan — discovery + REPORTS[] update only.
  </action>
  <verify>
    <automated>node scripts/scrapeMaDLS.js --explore --report gf-expenditures</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/scrapeMaDLS.js --explore --report gf-expenditures` console output contains `Table "..." found!` (NOT `Table "..." NOT found`)
    - Console output does NOT contain "does not exist" or "Definition" error text
    - The printed `Headers:` line includes recognizable GF expenditure category names (e.g. "General Government", "Public Safety", "Education", or "Public Works") — not totals-only columns
    - scripts/scrapeMaDLS.js REPORTS[1] no longer contains the substring `best guess`
    - scripts/scrapeMaDLS.js REPORTS[1] `rdreport` value is not `ScheduleA.GF.ExpendituresByFunctionMain`
    - scripts/output/explore_gf-expenditures.html was regenerated (no longer contains "does not exist")
  </acceptance_criteria>
  <done>The gf-expenditures REPORTS[] entry has a confirmed rdreport/tableID, the best-guess comment is gone, and a fresh --explore run shows a real data table with GF category headers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → MA DLS Gateway portal | Outbound HTTP GET; untrusted HTML response parsed by regex |
| developer → REPORTS[] config | Human-supplied rdreport string written into source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | gf-expenditures rdreport/tableID value | mitigate | LOAD-01 discovery confirms the value against the live portal and Task 2 `--explore` proves it resolves to a real GF-category table before any city write — blocking human checkpoint per D-02 |
| T-37-02 | Information Disclosure | explore_gf-expenditures.html on disk | accept | Contains only public MA DLS report HTML and DOR-code/municipality data (public records); no secrets written |
| T-37-03 | Denial of Service | MA DLS portal rate-limiting | mitigate | Existing `DELAY_MS = 1500` and `sleep(DELAY_MS * 2)` between explores preserved; this plan issues only a single explore call |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all changes use packages already imported in scrapeMaDLS.js (node:fs, @supabase/supabase-js, exceljs). Package Legitimacy Gate not applicable (RESEARCH.md confirms "No new packages") |
</threat_model>

<verification>
- `node scripts/scrapeMaDLS.js --explore --report gf-expenditures` exits without a fatal error and prints `Table "..." found!` with GF-category headers.
- `scripts/scrapeMaDLS.js` REPORTS[1] no longer contains `best guess` and its rdreport differs from the original wrong value.
- This plan performs NO DB writes and NO scrape of city data (discovery only).
</verification>

<success_criteria>
- Phase 37 Success Criterion 1 satisfied: `--explore --report gf-expenditures` returns the rdreport/tableID for General Fund Expenditures, confirmed correct before any city data is written.
- LOAD-01 complete: the rdreport is confirmed and `REPORTS[]` updated.
</success_criteria>

<output>
Create `.planning/phases/37-ma-loader-hardening/37-01-SUMMARY.md` when done. Record the confirmed rdreport and tableID values so Plan 02 and Phase 38 can rely on them.
</output>
