---
phase: 20-gresham-or-budget-load
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/loadORPopulation.js
autonomous: true
must_haves:
  truths:
    - "Gresham, OR municipality row has population 111507 and population_year 2024"
    - "Population is derived from the Census sub-est2024_41.csv file (SUMLEV=162, 'Gresham city'), not hardcoded into the DB write"
    - "Portland's existing population (635749) is unchanged by the edit"
    - "The edit to loadORPopulation.js is a two-constant change only (EXPECTED_CITIES, KNOWN_VALUES) — no other logic changes"
  artifacts:
    - path: "scripts/loadORPopulation.js"
      provides: "Census Oregon subcounty population loader extended to Portland + Gresham"
      contains: "Gresham"
  key_links:
    - from: "scripts/loadORPopulation.js"
      to: "treasury.municipalities"
      via: "update population where state=OR (covers both Portland and Gresham)"
      pattern: "\\.eq\\('state', 'OR'\\)"
---

<objective>
Load Gresham's 2024 Census population into the municipalities row by adding Gresham to the two config constants in the existing loadORPopulation.js, then running it.

Purpose: Per-capita display (matching all prior cities, including Portland) requires a population value sourced from the Census file rather than hardcoded (RESEARCH anti-pattern "Hardcoding population"). loadORPopulation.js already exists for Oregon (Portland only) — adding Gresham is a two-line config edit (RESEARCH Pattern 3; PATTERNS loadORPopulation.js section).

Output: scripts/loadORPopulation.js with Gresham added to EXPECTED_CITIES and KNOWN_VALUES, and Gresham's municipalities row updated to population 111507, population_year 2024.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-gresham-or-budget-load/20-RESEARCH.md
@.planning/phases/20-gresham-or-budget-load/20-PATTERNS.md
@scripts/loadORPopulation.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gresham to loadORPopulation.js config and load its population</name>
  <read_first>
    - scripts/loadORPopulation.js (the existing loader — read in full to see the current EXPECTED_CITIES = ['Portland'] and KNOWN_VALUES = { Portland: 635749 } constants, the normalizeCensusName function, the SUMLEV='162' filter, the POPESTIMATE2024 column validation, and the .eq('state','OR') DB update which already covers all OR cities)
    - .planning/phases/20-gresham-or-budget-load/20-PATTERNS.md ("scripts/loadORPopulation.js" section — the exact current-state vs required-change diff, and the verification that no other code changes are needed: SUMLEV filter, normalizeCensusName " city" strip, column header validation all already correct for Gresham)
    - .planning/phases/20-gresham-or-budget-load/20-RESEARCH.md ("Population Data" section + Pitfall 6 — Census row confirmation and the EXPECTED_CITIES guard behavior)
  </read_first>
  <action>
    Edit scripts/loadORPopulation.js with a TWO-CONSTANT change only — do NOT rewrite the file or touch any other logic:

    - Change `const EXPECTED_CITIES = ['Portland'];` to `const EXPECTED_CITIES = ['Portland', 'Gresham'];`
    - Change `const KNOWN_VALUES = { Portland: 635749 };` to add `Gresham: 111507` (Census sub-est2024_41.csv, SUMLEV=162, "Gresham city" -> "Gresham" after normalizeCensusName, 2024 vintage).

    Leave everything else verbatim. RESEARCH and PATTERNS confirm: normalizeCensusName already strips the " city" suffix ("Gresham city" -> "Gresham"); the `cols[0] !== '162'` SUMLEV filter is already correct; the POPESTIMATE2024 column header validation is unchanged; the `.eq('state', 'OR')` DB filter already covers both Portland and Gresham. The CSV is already cached at C:/tmp/sub-est2024_41.csv from Phase 17 (the loader will re-download if absent).

    Run `node scripts/loadORPopulation.js`. The EXPECTED_CITIES guard requires BOTH Portland and Gresham to be found in the CSV (RESEARCH Pitfall 6) — confirm the run finds the SUMLEV=162 Gresham row, validates 111507 against KNOWN_VALUES, and updates the Gresham municipalities row. Confirm Portland's existing 635749 is left intact (the loader updates both OR cities). If the analog supports --dry-run, run that first to confirm the parsed Gresham value is 111507 before writing.
  </action>
  <verify>
    <automated>node scripts/loadORPopulation.js && grep -c "Gresham" scripts/loadORPopulation.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/loadORPopulation.js EXPECTED_CITIES contains 'Gresham' and KNOWN_VALUES contains `Gresham: 111507`
    - scripts/loadORPopulation.js still contains the SUMLEV `'162'` filter and the `.eq('state', 'OR')` filter (unchanged logic)
    - Running `node scripts/loadORPopulation.js` exits 0 and reports Gresham population 111507 (and does not error on the EXPECTED_CITIES guard)
    - `SELECT population, population_year FROM treasury.municipalities WHERE name='Gresham' AND state='OR'` returns population=111507, population_year=2024
    - `SELECT population FROM treasury.municipalities WHERE name='Portland' AND state='OR'` still returns 635749 (Portland unaffected)
  </acceptance_criteria>
  <done>Gresham's municipalities row has Census-derived population 111507 for 2024 (Portland unchanged), enabling per-capita display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov -> local disk | Public HTTPS CSV download, no auth (cached from Phase 17) |
| script -> Supabase | population UPDATE via service key, scoped to OR cities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Tampering | Census CSV layout drift | mitigate | Existing header validation at columns 0/8/15 aborts if format changed (kept verbatim) |
| T-20-07 | Integrity | Summing county partition rows or wrong place | mitigate | Existing SUMLEV=162 filter selects only the incorporated-place row; KNOWN_VALUES 111507 sanity check |
</threat_model>

<verification>
- `node scripts/loadORPopulation.js` exits 0 and reports Gresham 111507
- Gresham municipalities row: population=111507, population_year=2024; Portland still 635749
- loadORPopulation.js retains SUMLEV=162 filter, header validation, and .eq('state','OR') (two-constant edit only)
</verification>

<success_criteria>
Gresham's population is loaded from the Census Oregon subcounty file (111507, vintage 2024) via a two-constant edit to the existing loadORPopulation.js, with Portland's value unaffected.
</success_criteria>

<output>
Create `.planning/phases/20-gresham-or-budget-load/20-03-SUMMARY.md` when done.
</output>
