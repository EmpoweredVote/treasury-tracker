---
phase: 20-gresham-or-budget-load
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - scripts/extractGresham.py
  - scripts/processGresham.js
autonomous: true
must_haves:
  truths:
    - "extractGresham.py uses page.extract_text() text-line parsing (NOT extract_tables()) on the 'Resources and Requirements — All Funds' page (RESEARCH Pitfall 1)"
    - "extractGresham.py emits JSON of department-level operating rows (department, adopted_amount, fiscal_year, page_num) for a single Gresham PDF"
    - "The Adopted amount is the LAST numeric column (column 6) on each department line"
    - "Totals, non-operating, and revenue (Resources) rows are excluded via the SKIP_ROWS set so only operating departments are emitted"
    - "fiscal_year is parsed from the column header as the ENDING year (2025/26 -> 2026, 2022/2023 -> 2023) handling both YYYY/YY and YYYY/YYYY formats"
    - "processGresham.js builds an operating budget tree and loads it via treasury_sync_budget_tree for FY2023-FY2026, full dollars, idempotent"
    - "Gresham amounts are stored in full dollars (no thousands multiplication)"
  artifacts:
    - path: "scripts/extractGresham.py"
      provides: "pdfplumber text-line operating-budget extractor outputting JSON to stdout"
      min_lines: 60
    - path: "scripts/processGresham.js"
      provides: "Node loader: runs Python extractor, builds tree, calls treasury_sync_budget_tree, infers FY from filename"
      min_lines: 100
  key_links:
    - from: "scripts/processGresham.js"
      to: "scripts/extractGresham.py"
      via: "execSync python invocation + JSON.parse"
      pattern: "execSync.*extractGresham"
    - from: "scripts/processGresham.js"
      to: "treasury_sync_budget_tree"
      via: "supabase.rpc"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Build the Gresham PDF extraction + load pipeline: a pdfplumber text-line Python extractor (extractGresham.py) and a Node.js loader (processGresham.js) that calls treasury_sync_budget_tree, following the Portland pattern but with Gresham-specific extraction. Validate end-to-end with --dry-run across all four PDFs; do NOT perform the live DB load here (Plan 04 owns the live load + verification).

Purpose: This is the core data pipeline. The extractor must use text-line parsing (not extract_tables(), which returns empty on Gresham's All Funds page per RESEARCH Pitfall 1), must read the Adopted figure from the last column, and must exclude totals/non-operating/revenue rows. It must be written against the FY2023-24 structure documented in 20-01-SUMMARY plus the FY2025-26/FY2022-23 structures verified in RESEARCH.

Output: scripts/extractGresham.py and scripts/processGresham.js, validated against all four downloaded PDFs via dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-gresham-or-budget-load/20-RESEARCH.md
@.planning/phases/20-gresham-or-budget-load/20-PATTERNS.md
@.planning/phases/20-gresham-or-budget-load/20-01-SUMMARY.md
@scripts/extractPortland.py
@scripts/processPortland.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write extractGresham.py (pdfplumber text-line operating extractor)</name>
  <read_first>
    - scripts/extractPortland.py (the boilerplate to reuse: shebang/docstring, imports, the json.dumps(data, indent=2) stdout convention, and the usage-error-to-stderr + sys.exit(1) argv handling. NOTE: do NOT copy its extract_tables() logic or its parse_fy — Gresham differs on both)
    - .planning/phases/20-gresham-or-budget-load/20-RESEARCH.md ("Code Examples" -> "extractGresham.py — Core Extraction Logic" — the verified parse_money, parse_fy_from_header, SKIP_ROWS, and extract_budget functions)
    - .planning/phases/20-gresham-or-budget-load/20-PATTERNS.md ("scripts/extractGresham.py" section — confirms text-line strategy, the SKIP_ROWS set, and the none_fy post-validation warning)
    - .planning/phases/20-gresham-or-budget-load/20-01-SUMMARY.md ("FY2023-24 PDF Structure Findings" — the actual All Funds page number, header line, department list, and OCR artifacts to parse correctly)
  </read_first>
  <action>
    Create scripts/extractGresham.py implementing the text-line extractor from RESEARCH "Code Examples". Take the PDF path as sys.argv[1] (simple argv, NOT argparse — operating only, no --mode flag). Required components:

    - parse_money(s): strip all of `[\$\(\)\s,]` via `re.sub(r'[\$\(\)\s,]', '', s.strip())`, treat leading '(' as negative, return int; this removes OCR spaces inside numbers ("3 5,569,000" -> 35569000) per RESEARCH Pitfall 2. Return 0 for empty or '-'.
    - parse_fy_from_header(header_line): find all matches of `\d{4}/(?:\d{4}|\d{2})(?!\d)`, take the LAST match (= the Adopted column), and return the ending year as int — handle both YYYY/YYYY (return the 4-digit second group) and YYYY/YY (return century-of-first + 2-digit second). "2022/23 2023/24 2024/25 2025/26 2025/26 2025/26" -> 2026; "2019/20 ... 2022/2023 2022/2023 2022/2023" -> 2023. Per RESEARCH Pitfall 5.
    - SKIP_ROWS: the exact set from RESEARCH (totals: 'Operating Total', 'Non-Operating Total', 'Total Requirements', 'Total Resources'; non-operating: 'Capital Improvement', 'Debt Service', 'Transfers', 'Contingency', 'Other Requirements', 'Unappropriated'; revenue/Resources rows: 'Taxes', 'Licenses & Permits', 'Intergovernmental', 'Charges for Services', 'Utility License Fees', 'Miscellaneous Income', 'Internal Payments', 'Interfund Transfers', 'Internal Svc Chrg', 'Internal Service Charges', 'Financing Proceeds', 'Beginning Balance').
    - extract_budget(pdf_path): iterate pdf.pages; select the page whose text contains BOTH 'Resources and Requirements' AND 'All Funds'; parse fiscal_year from the first 8 header lines via parse_fy_from_header; only collect lines AFTER the line equal to 'Requirements' (the in_requirements flag) so Resources-section lines are excluded; for each candidate line split on whitespace, partition into name tokens vs number tokens (a token is numeric if it matches `^[\d,]+$` or equals '-'); join name tokens, normalize internal whitespace via `re.sub(r'\s+', ' ', dept).strip()`, skip if the normalized name is in SKIP_ROWS; the Adopted amount is parse_money(num_tokens[-1]); skip rows where adopted <= 0 (emit skipped lines to stderr). Append { 'department', 'adopted_amount', 'fiscal_year', 'page_num' }. `break` after the single All Funds page.

    After the loop, add the none_fy post-validation: warn to stderr if any result has fiscal_year is None.

    Output json.dumps(data, indent=2) to stdout. Usage error to stderr + sys.exit(1) when no PDF arg given.

    Do NOT multiply amounts by 1000 — Gresham amounts are full dollars (RESEARCH State of the Art; PATTERNS Key Differences). Do NOT use extract_tables() — it returns empty (RESEARCH Pitfall 1).
  </action>
  <verify>
    <automated>python scripts/extractGresham.py "docs/Gresham/fy2025-26.pdf" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);if(!Array.isArray(a)||a.length===0)process.exit(1);const t=a.reduce((x,r)=>x+r.adopted_amount,0);const r=a[0];if(typeof r.department!=='string'||typeof r.adopted_amount!=='number'||r.fiscal_year!==2026)process.exit(1);if(t>500000000){console.error('total too large: '+t);process.exit(1);}console.log('rows='+a.length+' total='+t+' sample='+JSON.stringify(r))})"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/extractGresham.py "docs/Gresham/fy2025-26.pdf"` prints a JSON array with length > 0 (expected ~15 departments)
    - Each element has string `department`, integer `adopted_amount`, integer `fiscal_year`, integer `page_num`
    - fiscal_year equals 2026 for the FY2025-26 PDF (NOT 202526)
    - The sum of adopted_amount across FY2025-26 departments is ~$330,652,078 and is under $500M (NOT the $897M Total Requirements figure — RESEARCH Pitfall 3)
    - No element has adopted_amount <= 0; no element's department is in SKIP_ROWS (skipped lines go to stderr)
    - extractGresham.py contains "extract_text" and does NOT contain "extract_tables" or any multiply-by-1000 conversion
  </acceptance_criteria>
  <done>The Python extractor produces clean department-level operating JSON in full dollars with correct integer fiscal years, excluding totals/non-operating/revenue rows.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write processGresham.js (Node loader → treasury_sync_budget_tree) and dry-run validate all four PDFs</name>
  <read_first>
    - scripts/processPortland.js (the full loader to adapt: imports/Supabase setup with chain `.schema('treasury')`, extractPDF execSync invocation, inferFiscalYearFromFilename, buildOperatingTree, ensureMunicipality, upsertDataSource, loadFiscalYear with the treasury_sync_budget_tree RPC, parseArgs CLI, docs/<City>/ readdir discovery, and the resolvePdfDir worktree helper)
    - scripts/extractGresham.py (the JSON schema this loader consumes: department, adopted_amount, fiscal_year, page_num)
    - .planning/phases/20-gresham-or-budget-load/20-PATTERNS.md ("scripts/processGresham.js" section — every adaptation: PDF_URLS map, ensureMunicipality name='Gresham', buildOperatingTree using row.department, upsertDataSource naming, docs/Gresham/ discovery with NO volSuffix filter, and the treasury_sync_budget_tree parameter shape)
    - .planning/phases/20-gresham-or-budget-load/20-01-SUMMARY.md (confirmed working PDF URLs)
  </read_first>
  <action>
    Create scripts/processGresham.js adapting scripts/processPortland.js. Set up the Supabase client from process.env (SUPABASE_URL with the project fallback, SUPABASE_SERVICE_KEY/SUPABASE_SERVICE_ROLE_KEY), exit(2) if key missing. Use the chain-schema form: `createClient(SUPABASE_URL, SUPABASE_KEY)` then `.schema('treasury')` on every non-RPC query (matches processPortland.js; RPC needs no schema).

    PDF_URLS: the four-entry map keyed by DB fiscal year (2026/2025/2024/2023) -> Gresham URLs from PATTERNS "processGresham.js" section / RESEARCH. Single PDF per FY — no vol1/vol2 distinction.

    extractPDF(pdfPath): execSync `<python> "<ROOT>/scripts/extractGresham.py" "<pdfPath>"` with maxBuffer 8*1024*1024, encoding utf8, JSON.parse the result. Use `process.platform === 'win32' ? 'python' : 'python3'` for the python binary.

    inferFiscalYearFromFilename(filename): copy the Portland regex `fy(\d{4})-(\d{2})` -> century-of-group1 + group2 (fy2025-26 -> 2026, fy2022-23 -> 2023).

    buildOperatingTree(rows): for each row push a node `{ n: row.department, a: row.adopted_amount, i: [{ d: row.department, a: row.adopted_amount, aa: null, f: null, e: null }] }`; sum amounts into total; sort nodes descending by `a`; return { tree, total }. Use row.department (NOT row.bureau — that is the Portland field name; PATTERNS Key Differences).

    ensureMunicipality(): look up name='Gresham', state='OR'; if missing, error "Gresham, OR municipality not found — run seedGreshamOregon.js first" and exit(2).

    upsertDataSource(muniId, fiscalYear): name `Gresham Operating Budget FY${fiscalYear}`, api_type 'pdf_download', dataset_type 'operating', dataset_id `fy${fiscalYear}`, base_url PDF_URLS[fiscalYear] ?? '', fiscal_years [fiscalYear], municipality_id muniId — select-then-insert/update keyed on municipality_id + api_type + dataset_id + dataset_type for idempotency. No revenue branch (operating only for Phase 20 — RESEARCH Scope Decision).

    loadFiscalYear: delete existing budgets for that data_source_id + fiscal_year, then call `supabase.rpc('treasury_sync_budget_tree', { p_data_source_id, p_fiscal_year, p_dataset_type: 'operating', p_total: total, p_tree: tree, p_row_count: tree.length, p_triggered_by: 'bulk_load' })`. Check both rpcErr and rpc?.error.

    CLI: parseArgs with 'dry-run' boolean (default false) and optional 'pdf' string override, strict:false. Discover PDFs from docs/Gresham/ via readdirSync filtering `.pdf` (NO volSuffix filter — all are operating). Carry forward the resolvePdfDir worktree helper from processPortland.js pointed at docs/Gresham/. Map each PDF to its fiscal year via inferFiscalYearFromFilename. When --dry-run, print the assembled tree node count, per-FY total, and fiscal_year to stdout and perform NO DB writes; ensureMunicipality is only called when NOT dry-run. Do NOT run a live load in this plan — Plan 04 owns that.
  </action>
  <verify>
    <automated>node scripts/processGresham.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processGresham.js --dry-run` exits 0 and prints a per-fiscal-year total and node/row count for all four PDFs (FY2023, FY2024, FY2025, FY2026) without writing to the DB
    - scripts/processGresham.js contains "treasury_sync_budget_tree", "pdf_download", and "execSync" invoking extractGresham.py
    - scripts/processGresham.js builds tree nodes from `row.department` (not `row.bureau`)
    - The dry-run shows fiscal_year values 2023/2024/2025/2026 (never 202223/202526) and the FY2026 total ~$330,652,078 (a sub-$500M, full-dollar figure — RESEARCH Pitfall 3)
    - The loader errors with a clear "run seedGreshamOregon.js first" message if the Gresham municipality is absent (non-dry-run path)
  </acceptance_criteria>
  <done>The Node loader assembles correct full-dollar operating budget trees from the Python extractor for all four fiscal years and is ready for a live load via Plan 04, validated by a clean dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF content -> extractor | Untrusted file content parsed by pdfplumber; malformed/zero/OCR-artifact rows handled defensively and skipped to stderr |
| Node loader -> Supabase | Writes via service key (existing trusted pattern); dry-run path performs no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | execSync python invocation with file path arg | mitigate | PDF path comes from controlled docs/Gresham/ readdir, not user input; argument is quoted |
| T-20-04 | Denial of Service | execSync maxBuffer overflow on large PDF JSON | mitigate | maxBuffer 8MB matches Portland; extractor emits compact JSON (~15 department rows only, not full text) |
| T-20-05 | Integrity | Wrong total summed (Total Requirements $897M vs Operating $330M) | mitigate | SKIP_ROWS excludes Total Requirements/non-operating; dry-run + verify assert FY2026 total under $500M (RESEARCH Pitfall 3) |
</threat_model>

<verification>
- `python scripts/extractGresham.py docs/Gresham/fy2025-26.pdf` emits valid department JSON with fiscal_year=2026 and total ~$330M
- `node scripts/processGresham.js --dry-run` prints totals and node counts for all four fiscal years with no DB writes
- No thousands-multiplication in either script (full-dollar amounts); no extract_tables() in the extractor
</verification>

<success_criteria>
A working text-line extractor + loader pipeline validated by dry-run against all four downloaded PDFs, producing full-dollar department-level operating budget trees keyed to fiscal_year 2023–2026 (FY2026 total ~$330M), with no live DB writes performed yet.
</success_criteria>

<output>
Create `.planning/phases/20-gresham-or-budget-load/20-02-SUMMARY.md` when done. Record the dry-run per-FY totals and department counts so Plan 04 can sanity-check the live load against them.
</output>
