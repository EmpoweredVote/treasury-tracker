---
phase: 20-gresham-or-budget-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedGreshamOregon.js
  - docs/Gresham/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Gresham is NOT on Socrata — the PDF pipeline is used (RESEARCH Data Source Finding), one PDF per fiscal year"
    - "Python 3 and pdfplumber 0.11.9 are confirmed available in the environment"
    - "All four Gresham Adopted Budget PDFs (FY2023-FY2026) are downloaded to docs/Gresham/"
    - "The FY2023-24 All Funds page structure is inspected and documented so Plan 02's text-line extractor can be written deterministically (RESEARCH Open Question 1)"
    - "Gresham, OR exists as a municipality row with population 111507, population_year 2024"
    - "seedGreshamOregon.js does NOT create data_source rows (those are owned by processGresham.js)"
    - "City picker already shows 'Oregon' for OR — no EntitySwitcher change required (verified at line 25)"
  artifacts:
    - path: "scripts/seedGreshamOregon.js"
      provides: "Idempotent Gresham municipality seeder (municipality only, no data_source rows)"
      min_lines: 60
    - path: "docs/Gresham/"
      provides: "Downloaded FY2023, FY2024, FY2025, FY2026 adopted budget PDFs"
  key_links:
    - from: "scripts/seedGreshamOregon.js"
      to: "treasury.municipalities"
      via: "supabase.from('municipalities') with init-option schema 'treasury'"
      pattern: "from\\('municipalities'\\)"
---

<objective>
Establish the Gresham, OR foundation: verify the pdfplumber toolchain, create docs/Gresham/ and download the four Adopted Budget PDFs (FY2023-FY2026), inspect the FY2023-24 All Funds page to confirm its structure for the text-line extractor, and seed the Gresham municipality row.

Purpose: Plan 02 (extractGresham.py) cannot be finalized until the FY2023-24 page structure is confirmed (RESEARCH Open Question 1 — only FY2025-26, FY2024-25, and FY2022-23 were inspected during research). Plan 02 and Plan 03 both require the Gresham municipality row to exist first (FK dependency / population update target). This plan removes those blockers in one wave.

Output: docs/Gresham/ with four PDFs, captured pdfplumber text-line findings for FY2023-24, and an idempotent Gresham municipality row (population 111507, year 2024).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-gresham-or-budget-load/20-RESEARCH.md
@.planning/phases/20-gresham-or-budget-load/20-PATTERNS.md
@scripts/seedPortlandOregon.js
@src/components/EntitySwitcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify toolchain, download the four Gresham PDFs, and inspect the FY2023-24 All Funds page</name>
  <read_first>
    - scripts/extractPortland.py (the pdfplumber usage pattern extractGresham.py will adapt — how page.extract_text() and page iteration are used)
    - .planning/phases/20-gresham-or-budget-load/20-RESEARCH.md ("Data Source Details" section — the four PDF URLs, the All Funds page numbers per FY, the column structure, and Pitfall 2 on OCR artifacts)
    - .planning/phases/20-gresham-or-budget-load/20-PATTERNS.md ("processGresham.js" PDF directory + filename-inference section — docs/Gresham/ discovery convention and the fy2025-26.pdf -> 2026 naming)
  </read_first>
  <action>
    Confirm Python 3 and pdfplumber are present by running `python --version` and `python -c "import pdfplumber; print(pdfplumber.__version__)"` (expect 0.11.9). If pdfplumber is missing, run `pip install pdfplumber` and record this in the SUMMARY.

    Create the directory docs/Gresham/. Download the four Adopted Budget PDFs from the URLs in RESEARCH "Data Source Details", saving each with a filename whose fiscal year is inferable by the `fy(\d{4})-(\d{2})` pattern (processGresham.js infers FY from filename: fy2025-26 -> 2026):
    - FY 2025-26 (DB 2026): https://www.greshamoregon.gov/globalassets/city-departments/budget-and-finance/budget-documents/fy-2025-26-adopted-budget.pdf -> save as docs/Gresham/fy2025-26.pdf
    - FY 2024-25 (DB 2025): https://www.greshamoregon.gov/globalassets/city-departments/budget-and-finance/budget-documents/adopted-budget-fy24-25.pdf -> save as docs/Gresham/fy2024-25.pdf
    - FY 2023-24 (DB 2024): https://www.greshamoregon.gov/globalassets/city-departments/budget-and-finance/budget-documents/adopted-budget-fy-2023-24.pdf -> save as docs/Gresham/fy2023-24.pdf
    - FY 2022-23 (DB 2023): https://www.greshamoregon.gov/globalassets/city-departments/budget-and-finance/adopted-budget-for-fiscal-year-2022-23.pdf -> save as docs/Gresham/fy2022-23.pdf

    Verify each download is a real PDF (content starts with %PDF; file sizes per RESEARCH are ~7.3 MB, ~7.9 MB, ~6.8 MB, ~7.9 MB respectively). RESEARCH notes the URL patterns are inconsistent across years; if any returns 404 or non-PDF content-type, locate the current URL from greshamoregon.gov/budget-and-finance/budget-and-financial-documents/ and record the working URL in the SUMMARY.

    Write a throwaway inspection script (e.g. scripts/_inspect-gresham-temp.py — delete or clearly mark temporary) that opens docs/Gresham/fy2023-24.pdf with pdfplumber and, for the page containing "Resources and Requirements" AND "All Funds", prints page.extract_text() and confirms page.extract_tables() returns empty (RESEARCH Pitfall 1). Capture: the All Funds page number, the column-header line (to confirm parse_fy_from_header will read "2023/24" as the last/adopted column -> 2024), the list of Requirements-section department lines, and any OCR spacing artifacts (mid-word spaces in names like "Li censes", spaces inside numbers like "3 5,569,000" per Pitfall 2). Record this in the SUMMARY under a "FY2023-24 PDF Structure Findings" heading, including the expected Operating Total for FY2024 (RESEARCH estimates ~$239M) so Plan 02/04 can sanity-check.

    Note for downstream: Gresham amounts are FULL DOLLARS (RESEARCH State of the Art) — do NOT plan a multiply-by-1000 step. The Adopted budget is ALWAYS the last numeric column (column 6) on each department line. Total Requirements (~$897M) and Operating Total are NOT departments — they are excluded via the SKIP_ROWS set in Plan 02.
  </action>
  <verify>
    <automated>python -c "import pdfplumber; print('ok')" && node -e "const fs=require('fs');const files=['fy2025-26','fy2024-25','fy2023-24','fy2022-23'].map(n=>'docs/Gresham/'+n+'.pdf');for(const f of files){if(!fs.existsSync(f)){console.error('missing '+f);process.exit(1);}if(fs.readFileSync(f).subarray(0,4).toString()!=='%PDF'){console.error('not a PDF: '+f);process.exit(1);}}console.log('4 PDFs present and valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pdfplumber"` exits 0
    - docs/Gresham/fy2025-26.pdf, fy2024-25.pdf, fy2023-24.pdf, and fy2022-23.pdf all exist and begin with the bytes %PDF
    - SUMMARY contains a "FY2023-24 PDF Structure Findings" section naming the All Funds page number, the column-header line, the department line list, and any OCR artifacts observed
    - SUMMARY confirms extract_tables() returns empty on the FY2023-24 All Funds page (text-line parsing required)
    - SUMMARY records the expected FY2024 Operating Total for downstream sanity-checking
  </acceptance_criteria>
  <done>All four PDFs are on disk and valid, pdfplumber is confirmed importable, and the FY2023-24 All Funds structure is documented for Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Write seedGreshamOregon.js (municipality only) and confirm EntitySwitcher needs no change</name>
  <read_first>
    - scripts/seedPortlandOregon.js (the exact seeder pattern to adapt: init-option schema `{ db: { schema: 'treasury' } }`, upsertMunicipality select-on-name+state via .maybeSingle() then insert/update, fatal-exit pattern. NOTE: seedPortlandOregon.js does create a data_source row but Gresham's seeder must NOT — drop that block)
    - .planning/phases/20-gresham-or-budget-load/20-PATTERNS.md ("scripts/seedGreshamOregon.js" section — the GRESHAM payload, the upsertMunicipality body to copy, and the note that data_source rows are owned by processGresham.js)
    - src/components/EntitySwitcher.tsx (confirm `OR: 'Oregon'` is already present at ~line 25 — no change needed)
  </read_first>
  <action>
    Create scripts/seedGreshamOregon.js adapting scripts/seedPortlandOregon.js. Initialize the Supabase client with the init-option schema form: `createClient(SUPABASE_URL, SUPABASE_KEY, { db: { schema: 'treasury' } })` (matches seedPortlandOregon.js), reading SUPABASE_URL with the project fallback and SUPABASE_SERVICE_KEY/SUPABASE_SERVICE_ROLE_KEY; exit(1) if the key is missing. With the init-option schema set, `.from()` calls omit `.schema('treasury')`.

    Single municipality payload: { name: 'Gresham', state: 'OR', entity_type: 'city', population: 111507, population_year: 2024 }.

    Copy upsertMunicipality verbatim from the analog (PATTERNS lines for the function body): select id on name+state via .maybeSingle(); if existing.id update by id else insert; .select() the result; fatal-exit on error or missing returned row; return row.id.

    IMPORTANT — do NOT create any data_source rows in this seeder. RESEARCH Pattern 2 and PATTERNS both state data_source rows are owned by processGresham.js (one per fiscal year). The seeder's main() should: log a startup banner noting "municipality only — data_source rows created by processGresham.js", call upsertMunicipality(GRESHAM), log the returned id, and end with `main().catch(err => { console.error('Fatal:', err); process.exit(1); })`.

    Run `node scripts/seedGreshamOregon.js`, then run it a SECOND time to prove idempotency (the second run must take the update branch, not insert a duplicate).

    Verify src/components/EntitySwitcher.tsx already contains `OR: 'Oregon'` (PATTERNS confirms it is present at line 25 from Phase 17). If — and only if — it is absent, add `OR: 'Oregon',` to the STATE_LABELS map immediately after the `TX: 'Texas',` entry. Record in the SUMMARY whether a change was needed (expected: no change).
  </action>
  <verify>
    <automated>node scripts/seedGreshamOregon.js && node scripts/seedGreshamOregon.js && grep -c "OR: 'Oregon'" src/components/EntitySwitcher.tsx</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seedGreshamOregon.js contains the strings "Gresham", "111507", and "population_year"
    - scripts/seedGreshamOregon.js does NOT contain `from('data_sources')` (data_source rows are owned by processGresham.js)
    - Running `node scripts/seedGreshamOregon.js` exits 0 and prints the Gresham municipality id
    - Re-running it a second time exits 0 and takes the update branch (no duplicate municipality row)
    - `SELECT id, population, population_year FROM treasury.municipalities WHERE name='Gresham' AND state='OR'` returns exactly one row with population=111507, population_year=2024
    - src/components/EntitySwitcher.tsx contains `OR: 'Oregon'` (grep count >= 1)
  </acceptance_criteria>
  <done>The Gresham municipality row exists, is idempotent, and the city picker labels OR as Oregon (no EntitySwitcher change expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| greshamoregon.gov -> local disk | Public HTTPS PDF download, no auth; untrusted file content later parsed by pdfplumber |
| script -> Supabase (service key) | Municipality upsert via SUPABASE_SERVICE_KEY, existing trusted pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | Downloaded PDF (wrong/404 content served by CDN; inconsistent URL patterns) | mitigate | Verify %PDF magic bytes and file size before use (Task 1 verify); record working URLs in SUMMARY |
| T-20-02 | Information Disclosure | SUPABASE_SERVICE_KEY in scripts | accept | Read from process.env only, never hardcoded — matches existing seeder pattern |
| T-20-SC | Tampering | pip install pdfplumber | accept | pdfplumber 0.11.9 already required by extractPortland.py and confirmed installed; no new package introduced |
</threat_model>

<verification>
- `python -c "import pdfplumber; print('ok')"` succeeds
- All four PDFs present in docs/Gresham/ and begin with %PDF
- `node scripts/seedGreshamOregon.js` is idempotent (run twice, single Gresham municipality row)
- EntitySwitcher.tsx contains `OR: 'Oregon'` (no change expected)
- SUMMARY documents the FY2023-24 All Funds structure for Plan 02
</verification>

<success_criteria>
Toolchain verified, four Gresham PDFs downloaded and the FY2023-24 structure documented, and the Gresham municipality row seeded idempotently (population 111507, year 2024).
</success_criteria>

<output>
Create `.planning/phases/20-gresham-or-budget-load/20-01-SUMMARY.md` when done. MUST include a "FY2023-24 PDF Structure Findings" section (All Funds page number, column-header line, department lines, OCR artifacts, expected FY2024 Operating Total, confirmation extract_tables() is empty) and the confirmed working PDF URLs.
</output>
