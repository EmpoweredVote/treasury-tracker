---
phase: 20-gresham-or-budget-load
plan: 04
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified: []
autonomous: false
must_haves:
  truths:
    - "Gresham operating budget rows exist in treasury.budgets for FY2023, FY2024, FY2025, and FY2026 (dataset_type='operating')"
    - "Each fiscal year's total is a full-dollar figure in the ~$239M–$330M range (FY2026 ~$330,652,078); none exceeds $500M (RESEARCH Pitfall 3)"
    - "enrichCategories.js is run for Gresham with a cost estimate produced before the live enrich (well under the $5/run threshold)"
    - "Gresham category_enrichment rows have non-null plain_name and a non-null Gresham municipality_id (no cross-city bleed)"
    - "A user can select Gresham, OR in the app and see operating budget data with per-capita context and enriched category descriptions across the four fiscal years"
  artifacts:
    - path: ".planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md"
      provides: "Verification record confirming Gresham data is live and correct"
  key_links:
    - from: "treasury.budgets"
      to: "Gresham municipality_id"
      via: "data_source_id -> municipality_id join"
      pattern: "municipality_id"
---

<objective>
Perform the live data load: run the Gresham operating budget loader against the real DB for all four fiscal years, run category enrichment (cost-gated), and verify Gresham appears correctly in the app with per-capita context and enriched descriptions. Produce 20-VERIFICATION.md following the Phase 17 pattern.

Purpose: Plans 01–03 built and dry-ran the pipeline and loaded population; this plan commits the budget data and confirms the Phase 20 goal end to end. The enrichment run incurs API cost (RESEARCH estimates ~$0.12, worst case $0.50 — well under the $5 threshold) but a human checkpoint confirms the cost estimate before the AI run.

Output: Live Gresham budget + enrichment data, plus 20-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-gresham-or-budget-load/20-RESEARCH.md
@.planning/phases/17-portland-or-budget-load/17-VERIFICATION.md
@.planning/phases/20-gresham-or-budget-load/20-01-SUMMARY.md
@.planning/phases/20-gresham-or-budget-load/20-02-SUMMARY.md
@.planning/phases/20-gresham-or-budget-load/20-03-SUMMARY.md
@scripts/enrichCategories.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load Gresham operating budget for FY2023–FY2026</name>
  <read_first>
    - scripts/processGresham.js (the loader being run live — confirm it writes to treasury.budgets via treasury_sync_budget_tree, deletes-then-inserts per data_source_id + fiscal_year for idempotency)
    - .planning/phases/20-gresham-or-budget-load/20-02-SUMMARY.md (the dry-run per-FY totals and department counts to validate the live load against)
    - .planning/phases/17-portland-or-budget-load/17-VERIFICATION.md (the verification query pattern to replicate for the join across budgets/data_sources/municipalities)
  </read_first>
  <action>
    Run a final dry-run to confirm expected totals: `node scripts/processGresham.js --dry-run`. Compare per-FY totals and department counts against those recorded in 20-02-SUMMARY.

    Run the live load: `node scripts/processGresham.js` (no --dry-run). This calls treasury_sync_budget_tree for FY2023, FY2024, FY2025, and FY2026 operating data. The loader deletes-then-inserts per data_source_id + fiscal_year, so it is idempotent — run it a second time and confirm row counts are unchanged.

    Verify in the DB:
    - `SELECT b.fiscal_year, b.dataset_type, b.total_budget FROM treasury.budgets b JOIN treasury.data_sources d ON b.data_source_id=d.id JOIN treasury.municipalities m ON d.municipality_id=m.id WHERE m.name='Gresham' AND m.state='OR' ORDER BY b.fiscal_year;` returns operating rows for 2023, 2024, 2025, and 2026.
    - Each total_budget is a full-dollar figure in the plausible Gresham operating range (~$239M–$330M; FY2026 ~$330,652,078) and NONE exceeds $500M (a >$500M value indicates Total Requirements was summed instead of operating departments — RESEARCH Pitfall 3).

    Record actual loaded totals and department counts per FY for the VERIFICATION doc.
  </action>
  <verify>
    <automated>node scripts/processGresham.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processGresham.js` exits 0
    - `treasury.budgets` contains operating rows for Gresham (state OR) with fiscal_year 2023, 2024, 2025, AND 2026
    - Re-running the loader leaves the budget row count unchanged (idempotent)
    - Every Gresham fiscal-year total is a full-dollar figure under $500M (FY2026 ~$330,652,078; not off by 1000x and not the $897M Total Requirements)
  </acceptance_criteria>
  <done>Gresham FY2023–FY2026 operating budgets are loaded and idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Run category enrichment for Gresham (cost estimate first)</name>
  <read_first>
    - scripts/enrichCategories.js (the --city/--state/--year flags and --dry-run support; how it counts categories to enrich and the name_key upsert scoped to municipality_id)
    - .planning/phases/20-gresham-or-budget-load/20-RESEARCH.md ("Enrichment Cost Estimate" — ~15 departments x 4 years = up to 60 category rows, idempotent via name_key, cost ~$0.12 worst case $0.50; "Common Pitfalls" Pitfall 4 on department renames across fiscal years)
  </read_first>
  <action>
    First, dry-run to count categories and estimate cost for each loaded fiscal year: `node scripts/enrichCategories.js --city Gresham --state OR --year 2026 --dry-run` and repeat for --year 2025, 2024, 2023. Compute the cost estimate from the category count (RESEARCH: ~$2/1M tokens, ~1000 tokens/call). Per the $5/run threshold (feedback_api_cost_threshold), the estimate should be well under $1 — if it unexpectedly exceeds $5, STOP and surface to the user before running.

    Run enrichment live for all four fiscal years:
    - `node scripts/enrichCategories.js --city Gresham --state OR --year 2026`
    - `node scripts/enrichCategories.js --city Gresham --state OR --year 2025`
    - `node scripts/enrichCategories.js --city Gresham --state OR --year 2024`
    - `node scripts/enrichCategories.js --city Gresham --state OR --year 2023`
    Enrichment is idempotent via name_key upsert scoped to municipality_id, so re-runs are safe. Note RESEARCH Pitfall 4: Gresham renamed some departments across years (e.g. "City Manager's Office" -> "Office of Governance & Management"; "Fire & Emergency Services" -> "Fire") — these correctly produce separate enrichment entries; do NOT attempt to merge them.

    Verify: `SELECT COUNT(*), COUNT(plain_name) FROM treasury.category_enrichment ce JOIN treasury.municipalities m ON ce.municipality_id=m.id WHERE m.name='Gresham' AND m.state='OR';` shows enriched rows with non-null plain_name. Confirm no Gresham-related enrichment rows have a NULL municipality_id (per project memory enrichment-scoping fix — NULL municipality_id causes cross-city text bleed).
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city Gresham --state OR --year 2026 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run prints a category count and an estimated cost recorded in the SUMMARY (estimate under $5)
    - After the live runs, `category_enrichment` has rows with non-null `plain_name` scoped to Gresham's municipality_id
    - No category_enrichment rows for Gresham have a NULL municipality_id (avoids the cross-city bleed bug noted in project memory)
  </acceptance_criteria>
  <done>Gresham categories have AI-enriched plain-language descriptions across the four fiscal years, scoped correctly to Gresham.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gresham, OR operating budget (FY2023–FY2026) loaded into treasury.budgets, population 111507 set for per-capita display, categories AI-enriched, and the city picker already labels OR as "Oregon" (from Phase 17).
  </what-built>
  <how-to-verify>
    1. Start the app locally (e.g. `npm run dev`) or open treasurytracker.empowered.vote if the load targeted production.
    2. Open the city/entity picker. Confirm Gresham appears under "Oregon" (alongside Portland), not "OR".
    3. Select Gresham, OR. Confirm operating budget data renders with department categories (Police, Fire, Environmental Services, etc.).
    4. Confirm a per-capita figure displays (population 111,507 applied).
    5. Confirm category descriptions show enriched plain-language text (not raw department codes).
    6. Switch fiscal years and confirm FY2023, FY2024, FY2025, and FY2026 are each selectable with data; confirm the totals are in the hundreds-of-millions range (not billions, not sub-million).
  </how-to-verify>
  <resume-signal>Type "approved" if Gresham displays correctly, or describe what is wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 20-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/17-portland-or-budget-load/17-VERIFICATION.md (the verification doc structure to replicate)
  </read_first>
  <action>
    Create .planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md following the Phase 17 VERIFICATION.md structure. Include: the DB verification queries and their actual results (operating totals per FY 2023–2026, department counts, enrichment counts, population 111507), the enrichment cost actually incurred, the working PDF URLs used, the human-verify checkpoint outcome, and any follow-ups (revenue budget deferred per RESEARCH Scope Decision — Resources rows on the same All Funds page can be a future phase; any tech-debt notes). Confirm the goal statement from ROADMAP Phase 20 is met (Gresham operating budget viewable with per-capita and enriched descriptions; second Multnomah County city loaded).
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('.planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md exists
    - It records actual FY2023–FY2026 operating totals, department counts, enrichment counts, population value, and enrichment cost
    - It notes revenue budget (Resources rows) as a deferred follow-up
    - It states whether the Phase 20 ROADMAP goal is met
  </acceptance_criteria>
  <done>A verification record exists documenting the live Gresham data and confirming the phase goal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichCategories.js -> Anthropic API | AI calls cost money; cost gated by estimate + $5 threshold rule |
| loader -> Supabase | Live writes via service key (existing trusted pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Denial of Service (cost) | enrichCategories.js Anthropic spend | mitigate | Dry-run cost estimate before live run; stop-and-surface if > $5 (feedback_api_cost_threshold) |
| T-20-09 | Integrity | enrichment NULL municipality_id cross-city bleed | mitigate | Verify all Gresham enrichment rows have non-null municipality_id (per project memory enrichment-scoping fix) |
| T-20-10 | Integrity | Wrong operating total loaded (Total Requirements $897M) | mitigate | Verify every FY total under $500M against the dry-run; reject billions/sub-million figures (RESEARCH Pitfall 3) |
</threat_model>

<verification>
- Gresham operating budget rows exist for FY2023–FY2026 (hundreds-of-millions full-dollar totals, each under $500M)
- Gresham category_enrichment rows have non-null plain_name and correctly-scoped (non-null) Gresham municipality_id
- Gresham population 111507 applied; per-capita renders in app
- City picker shows "Oregon"; human-verify checkpoint approved
- 20-VERIFICATION.md created
</verification>

<success_criteria>
Citizens can select Gresham, OR in the app and view AI-enriched operating budget data with per-capita context for FY2023–FY2026 — the Phase 20 goal — with a verification record on file, completing Multnomah County's two major cities (Portland + Gresham).
</success_criteria>

<output>
Create `.planning/phases/20-gresham-or-budget-load/20-04-SUMMARY.md` and `.planning/phases/20-gresham-or-budget-load/20-VERIFICATION.md` when done.
</output>
