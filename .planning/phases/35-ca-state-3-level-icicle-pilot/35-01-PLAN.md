---
phase: 35-ca-state-3-level-icicle-pilot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ICICLE-01]
must_haves:
  truths:
    - "The distinct function-column value distribution for CA GF FY2026 is known (resolves A1)"
    - "Whether treasury_sync_budget_tree accepts a node with BOTH c (children) and i (items) is known (resolves A2)"
    - "The D-05 collapse strategy for Wave 1 is decided based on A2 outcome"
  artifacts:
    - path: ".planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md"
      provides: "A1 function distribution + A2 mixed-node RPC verdict + chosen D-05 strategy"
      contains: "A2 VERDICT"
  key_links:
    - from: "discovery findings"
      to: "35-02-PLAN buildNLevelTree collapseItems handling"
      via: "A2 VERDICT determines mixed-node vs fallback path"
      pattern: "A2 VERDICT"
---

<objective>
Resolve the two unverified assumptions (A1 and A2) from 35-RESEARCH.md BEFORE any code is changed, because the outcome of A2 directly determines how `buildNLevelTree()` must handle D-05 null-function collapse rows in Plan 02.

This is a pure discovery plan — no source files are modified. It produces `35-DISCOVERY.md` recording:
- **A1:** The distribution of distinct `function` (col 2) values for CA General Fund FY2026 — confirms whether a genuine 3rd level exists (not nearly-all-null, which would degenerate to 2 levels).
- **A2:** Whether `treasury_sync_budget_tree` accepts a single node carrying BOTH `c` (children) AND `i` (line items) — the mixed branch/leaf case that arises when a department has both null-function rows (D-05 collapse) and non-null-function rows (depth-2 children).
- **Decision:** The chosen D-05 implementation strategy for Plan 02 (mixed-node emit vs. fallback drop-items-keep-total).

Purpose: De-risk Plan 02. Without A2 resolved, the executor of Plan 02 would have to guess the collapse strategy and may produce a tree the RPC silently rejects.
Output: `.planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure A1 — distinct function-column distribution for CA GF FY2026</name>
  <files>.planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md (discovery output only — no source files modified)</files>
  <read_first>
    - scripts/extractCA.py (lines 40-49 COLS dict, lines 105-126 rows_out loop — confirm `function` = col 2, currently NOT emitted)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (Open Questions §1, Assumptions Log A1/A3)
  </read_first>
  <action>
    Do NOT modify extractCA.py. Instead, run a one-off read-only Python snippet (via `python -c` or a temp script under scripts/_tmp that you delete after) that opens `docs/California/Historical_Expenditures.xlsx`, sheet `Pivot Table Data`, filters `Fund == 'General Fund'` and non-null amount for fiscal year string ending `-26` (FY2026), and tallies:
      (1) total GF FY2026 rows,
      (2) count of rows where col 2 (`function`) is None/blank/whitespace,
      (3) count of rows where col 2 is non-null,
      (4) the top 20 distinct non-null function values with their row counts (use collections.Counter on col 2),
      (5) a sample of 5 distinct function values verbatim to confirm A3 (human-readable names like 'Local Assistance', 'State Operations', 'Capital Outlay' — NOT numeric budget-act codes).
    Run from the repo root C:\treasury-tracker so the relative XLSX_PATH resolves. Use `read_only=True, data_only=True` matching extractCA.py line 102.
    Record all five outputs verbatim into 35-DISCOVERY.md under a `## A1 — Function Distribution (FY2026)` heading.
    Interpretation rule: if non-null function rows are >= 20% of GF FY2026 rows, A1 is CONFIRMED (genuine 3rd level exists). If < 20%, flag in DISCOVERY.md as "A1 WEAK — 3rd level sparse" but proceed (D-05 collapse handles sparse functions; tree is still valid where data supports it).
    Delete any temp script you created.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md','utf8');if(!/## A1 — Function Distribution/.test(t))process.exit(1);if(!/non-?null/i.test(t))process.exit(1);console.log('A1 section present')"</automated>
  </verify>
  <acceptance_criteria>
    - 35-DISCOVERY.md contains a `## A1 — Function Distribution (FY2026)` heading
    - The section reports total GF FY2026 row count, null-function count, non-null-function count, and a top-20 distinct-value tally
    - The section records 5 verbatim sample function values and states whether they are human-readable names (A3) or numeric codes
    - The section states the A1 verdict: CONFIRMED (non-null >= 20%) or A1 WEAK (< 20%)
    - No changes were made to scripts/extractCA.py (`git diff --stat scripts/extractCA.py` shows no changes)
  </acceptance_criteria>
  <done>35-DISCOVERY.md A1 section reports the function distribution and states a CONFIRMED/WEAK verdict; extractCA.py is unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve A2 — test whether treasury_sync_budget_tree accepts a mixed c+i node, then decide D-05 strategy</name>
  <files>.planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md (discovery output only — no source files modified)</files>
  <read_first>
    - scripts/processCA.js (lines 196-211 loadFiscalYear — the exact RPC call shape: p_data_source_id, p_fiscal_year, p_dataset_type, p_total, p_tree, p_row_count, p_triggered_by)
    - .planning/phases/34-3-level-tree-infrastructure-ev-accounts-api/34-01-SUMMARY.md (how the Phase 34 3-level RPC test was constructed and cleaned up — reuse its sentinel-FY + cleanup pattern)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (Open Questions §2, Pitfall 1, Assumptions Log A2)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md (D-05 implementation note, lines 204-215)
  </read_first>
  <action>
    Write a throwaway test script (e.g. scripts/_verify-mixed-node-temp.mjs) that loads env via the same loadEnv idiom in processCA.js (read `.env` for SUPABASE_URL + SUPABASE_SERVICE_KEY), creates a Supabase client, looks up the CA data source via `treasury_list_source_ids` (name = 'California General Fund Operating Budget', id known to be `e47a4cb5-d69f-4cf5-be10-ada8505296e3`), and calls `treasury_sync_budget_tree` with a sentinel fiscal year of 9998 and dataset_type 'operating', passing a MIXED tree: one agency node with one department child that carries BOTH `c` (one function child with one line item) AND `i` (one collapsed line item). Concretely the department node must be `{ n, a, c: [{ n, a, i: [{d,a,aa:null,f:null,e:null}] }], i: [{d,a,aa:null,f:null,e:null}] }`. Use small amounts (e.g. 100 and 200) so they never collide with the $150B sanity band (you are calling the RPC directly, NOT processCA.js, so the sanity band does not apply).
    Inspect the RPC response: capture whether it errored (rpcErr or rpc.error) and the reported rows_inserted. Then query budget_categories + budget_line_items for that sentinel budget to confirm whether both the collapsed item AND the function child's item landed (mixed accepted) or whether one was dropped/the call failed (mixed rejected).
    CLEANUP (mandatory): after capturing results, delete the sentinel FY9998 budget and all its categories/line_items. If the RPC provides a delete path use it; otherwise delete the budget row for data_source_id `e47a4cb5...` + fiscal_year 9998 (cascade should remove children — verify the budgets row for FY9998 is gone with a follow-up SELECT). Delete the temp script.
    Record into 35-DISCOVERY.md under `## A2 — Mixed c+i Node RPC Test`:
      - The exact tree submitted,
      - The RPC response (error or rows_inserted),
      - The post-call DB row counts (how many line items landed and at what depth),
      - A line reading exactly `A2 VERDICT: ACCEPTED` or `A2 VERDICT: REJECTED`,
      - confirmation that the sentinel FY9998 budget was deleted.
    Then write a `## D-05 Strategy Decision` section stating the strategy Plan 02 must implement:
      - If `A2 VERDICT: ACCEPTED` → "Emit mixed nodes: a department with both null-function rows and non-null-function rows produces `{ n, a, c: [function children], i: [collapsed null-function line items] }`."
      - If `A2 VERDICT: REJECTED` → "Fallback per D-05 (accurate totals over granularity): a department with both kinds of rows produces `{ n, a, c: [function children] }` where `a` INCLUDES the null-function amounts, and the null-function rows are OMITTED as separate line items. A department with ONLY null-function rows produces a leaf `{ n, a, i: [line items] }`."
    Use the mcp__supabase-local execute_sql tool for the verification/cleanup queries per project convention (feedback_supabase_migration_mcp).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/35-ca-state-3-level-icicle-pilot/35-DISCOVERY.md','utf8');if(!/A2 VERDICT: (ACCEPTED|REJECTED)/.test(t))process.exit(1);if(!/## D-05 Strategy Decision/.test(t))process.exit(1);console.log('A2 verdict + D-05 strategy present')"</automated>
  </verify>
  <acceptance_criteria>
    - 35-DISCOVERY.md contains a `## A2 — Mixed c+i Node RPC Test` section with the submitted tree, RPC response, and post-call DB row counts
    - The section contains a line matching exactly `A2 VERDICT: ACCEPTED` or `A2 VERDICT: REJECTED`
    - 35-DISCOVERY.md contains a `## D-05 Strategy Decision` section whose text matches the ACCEPTED or REJECTED branch above (and matches the actual verdict)
    - The sentinel FY9998 budget is deleted: a SELECT on treasury.budgets for data_source_id `e47a4cb5-d69f-4cf5-be10-ada8505296e3` and fiscal_year 9998 returns 0 rows (record this confirmation in DISCOVERY.md)
    - No throwaway test script remains in scripts/ (`git status` shows no untracked _verify-mixed-node-temp.mjs)
    - scripts/processCA.js and scripts/extractCA.py are unmodified
  </acceptance_criteria>
  <done>35-DISCOVERY.md records an `A2 VERDICT:` line and a matching `## D-05 Strategy Decision` section; the FY9998 sentinel budget and temp script are removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase RPC | Service-role key from .env crosses into the DB; sentinel writes must be cleaned up so they never reach the live app |
| Excel file → Python reader | Local trusted file (docs/California), read-only, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | sentinel FY9998 budget left in DB | mitigate | Mandatory cleanup task (Task 2) deletes the sentinel budget; acceptance criterion asserts SELECT returns 0 rows for FY9998 |
| T-35-02 | Information Disclosure | service-role key in temp script | mitigate | Temp script reads key from process.env via .env loadEnv idiom (no hardcoded key); script deleted after run |
| T-35-03 | Tampering | sentinel write collides with real CA data | accept | FY9998 is far outside the real 2022-2026 range and the $150B sanity band; RPC called directly, not via processCA.js |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- 35-DISCOVERY.md exists with `## A1`, `## A2`, and `## D-05 Strategy Decision` sections
- `A2 VERDICT:` line is present and matches the D-05 strategy text
- No source scripts modified; no temp scripts or sentinel DB rows left behind
</verification>

<success_criteria>
- A1 distribution measured and verdict recorded (CONFIRMED or WEAK)
- A2 mixed-node behavior tested live against the RPC; verdict recorded
- D-05 implementation strategy for Plan 02 is decided and written down
- DB and working tree are clean (no sentinel rows, no temp scripts)
</success_criteria>

<output>
Create `.planning/phases/35-ca-state-3-level-icicle-pilot/35-01-SUMMARY.md` when done. The SUMMARY must restate the `A2 VERDICT` and the chosen D-05 strategy so Plan 02's executor reads it from the SUMMARY without re-running the test.
</output>
