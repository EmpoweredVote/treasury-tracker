---
phase: 35-ca-state-3-level-icicle-pilot
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified: []
autonomous: false
requirements: [ICICLE-01, ICICLE-02, ICICLE-03]
user_setup:
  - service: anthropic
    why: "enrichCategories.js requires ANTHROPIC_API_KEY for depth-2 enrichment (D-09); it is NOT present in .env or .env.local — must be in the shell environment"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (set in shell env before running enrichment)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key (enrichCategories.js requires this exact var name, distinct from SUPABASE_SERVICE_KEY used by processCA.js)"
must_haves:
  truths:
    - "CA General Fund is reloaded as a 3-level tree for all 5 FYs (2022-2026) via treasury_sync_budget_tree"
    - "treasury.budget_categories has rows at depth 0, 1, AND 2 for CA FY2026 after reload"
    - "Each reloaded FY total stays within the $150B-$300B sanity band and equals the pre-reload total"
    - "The 12 existing depth-0 CA enrichment rows survive the reload unchanged (name-keyed, not id-keyed)"
    - "New depth-2 function nodes are AI-enriched with state-level framing (D-09)"
    - "CA state icicle drills 3 clickable levels in the live app and Level 3 opens LineItemsTable"
  artifacts:
    - path: ".planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md"
      provides: "DB depth distribution + enrichment count + human spot-check results for ICICLE-01/02/03"
      contains: "ICICLE-02"
  key_links:
    - from: "scripts/processCA.js buildNLevelTree output"
      to: "treasury.budget_categories depth 0/1/2"
      via: "treasury_sync_budget_tree RPC reload"
      pattern: "depth"
    - from: "treasury.budget_categories depth-2 nodes"
      to: "treasury.category_enrichment name_key with pipe"
      via: "enrichCategories.js --depth 2"
      pattern: "\\|"
    - from: "depth-2 budget_categories + line_items"
      to: "live icicle Level 3 + LineItemsTable"
      via: "getBudgetById N-level response rendered by BudgetIcicle.tsx"
      pattern: "navigationPath"
---

<objective>
Take the modified loader from Plan 02 to production: reload all 5 CA fiscal years as 3-level trees, verify the DB now holds depth-2 categories, enrich the new function-level nodes, and human-verify the live icicle drills 3 levels end-to-end. This is the plan that satisfies ICICLE-02 and ICICLE-03 (live-app behavior) on top of ICICLE-01 (data shape).

Sequence: env preflight → live reload (5 FYs) → DB depth verification → enrichment dry-run + cost gate → live enrichment → human spot-check of the live app → write 35-VERIFICATION.md.

This plan is NOT autonomous: it contains a $5 cost-gate decision point and a human visual verification checkpoint.

Purpose: Make the 3-level CA icicle real and visible to citizens (ICICLE-01/02/03).
Output: Reloaded DB (depth 0/1/2), new depth-2 enrichments, and 35-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-CONTEXT.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Env preflight + live reload of all 5 FYs + DB depth verification (ICICLE-01)</name>
  <files>scripts/processCA.js</files>
  <read_first>
    - scripts/processCA.js (lines 44-62 env loading + guards, lines 196-211 loadFiscalYear RPC call, lines 213-285 main loop)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (D-07 RPC in-place replacement lines 374-387; Current Live DB State lines 491-505; Post-Reload DB Verification SQL lines 630-645)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-02-SUMMARY.md (per-FY dry-run totals and which D-05 branch shipped)
  </read_first>
  <action>
    Step 1 — Env preflight: confirm `SUPABASE_URL` and `SUPABASE_SERVICE_KEY` are available to processCA.js (they are in .env). Do NOT proceed to reload if either is missing — processCA.js now exits(2) on missing SUPABASE_URL (Plan 02 D-12 fix), which is the correct fail-loud behavior.
    Step 2 — Live reload: run `node scripts/processCA.js --fy 2022 --fy 2023 --fy 2024 --fy 2025 --fy 2026` from repo root (no --dry-run). The RPC replaces each FY's tree in place for data_source_id `e47a4cb5-d69f-4cf5-be10-ada8505296e3` (D-07 verified: old depth-1 leaf items are removed, new depth-2 nodes + items inserted; no explicit DELETE needed; no orphaned rows). Each FY should report rows_inserted greater than the old 2-level count (FY2026 was 169 categories at 2 levels; expect more with depth-2 nodes added). Confirm the script does NOT exit(3) on the sanity band for any FY.
    Step 3 — DB depth verification: query treasury.budget_categories joined to budgets/municipalities for California FY2026 operating, grouped by depth. Expected: rows at depth 0 (~12 agencies), depth 1 (~157 departments), AND depth 2 (> 0 function nodes). The presence of depth-2 rows is the ICICLE-01 proof. Also confirm the FY2026 budget total still reads $228,365,858,000 (D-07: in-place replacement preserves the total). Repeat the depth-distribution query for FY2022-2025 and record counts.
    Record the per-FY rows_inserted and the depth distributions in 35-VERIFICATION.md under `## ICICLE-01 — DB Depth Verification`.
    Use the mcp__supabase-local tools (execute_sql) for the verification SQL per project convention (feedback_supabase_migration_mcp) rather than asking the user to paste into the Dashboard.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md','utf8');if(!/## ICICLE-01 — DB Depth Verification/.test(t))process.exit(1);if(!/depth\D*2/i.test(t))process.exit(1);console.log('ICICLE-01 depth verification recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processCA.js --fy 2022 --fy 2023 --fy 2024 --fy 2025 --fy 2026` exits 0 (no exit 2 env error, no exit 3 sanity error)
    - Each FY reports rows_inserted; FY2026 rows_inserted exceeds the prior 2-level count (>169)
    - The depth-distribution query for CA FY2026 operating returns rows at depth 0, depth 1, AND depth 2 (depth-2 count > 0)
    - CA FY2026 budget total reads $228,365,858,000 after reload (unchanged from before)
    - 35-VERIFICATION.md `## ICICLE-01 — DB Depth Verification` records per-FY rows_inserted and the depth distribution showing depth-2 rows
  </acceptance_criteria>
  <done>All 5 FYs reloaded as 3-level trees; budget_categories has depth-0/1/2 rows for CA FY2026; the total is unchanged; depth distributions recorded in 35-VERIFICATION.md.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: $5 cost gate — approve depth-2 enrichment (D-10)</name>
  <decision>Approve running depth-2 enrichment after seeing the actual dry-run cost estimate</decision>
  <context>
    D-10 requires the $5 API cost gate. Research estimates ~100-200 unique depth-2 function name_keys at ~$0.0002/call via Claude Haiku = ~$0.02-$0.04, well under $5. But the actual count must be confirmed by a dry-run before spending. ALSO: enrichCategories.js requires `ANTHROPIC_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` (note: the ROLE key, distinct from the SUPABASE_SERVICE_KEY used by processCA.js). Neither var is present in .env or .env.local — they must be set in the shell environment. Phase 33 enrichment succeeded, so they exist in the operator's environment.

    Before this checkpoint, the executor MUST:
    1. Confirm `ANTHROPIC_API_KEY` and `SUPABASE_SERVICE_ROLE_KEY` are set in the shell env (without printing their values). If either is missing, STOP and report — enrichment cannot run.
    2. Run the dry-run: `node scripts/enrichCategories.js --city "California" --state CA --year 2026 --depth 2 --dry-run` and report the exact count of depth-2 nodes it would enrich and the implied cost (count x $0.0002).
  </context>
  <options>
    <option id="approve">
      <name>Approve live enrichment</name>
      <pros>Function-level (Level 3) nodes get human-readable descriptions; cost confirmed under $5</pros>
      <cons>Spends a few cents of Anthropic API budget</cons>
    </option>
    <option id="skip-over-budget">
      <name>Stop — cost exceeds $5 or unexpected count</name>
      <pros>Respects the unfunded-nonprofit API budget; avoids surprise spend</pros>
      <cons>Level 3 nodes ship without enrichment descriptions (can be enriched later)</cons>
    </option>
  </options>
  <resume-signal>Confirm both env vars are set and the dry-run count/cost is reported, then select: approve or skip-over-budget</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Live depth-2 enrichment + survival check of existing depth-0 enrichments (D-08/D-09)</name>
  <files>scripts/enrichCategories.js</files>
  <read_first>
    - scripts/enrichCategories.js (lines 35-54 loadEnv, lines 84-98 SUPABASE/ANTHROPIC key guards, line 75 DEPTH flag, lines 192-205 getBudgetCategories depth filter, lines 382-384 saveEnrichment name_key)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (D-08 enrichment survival lines 389-407; D-11 depth flag lines 409-435; Enrichment Cost Estimate lines 509-517; Pitfall 3 lines 543-549)
  </read_first>
  <action>
    Only run this task if the checkpoint selected `approve`. If `skip-over-budget` was selected, record the skip in 35-VERIFICATION.md and proceed to Task 3 (Level 3 nodes ship without descriptions).
    Step 1 — Survival baseline: query `SELECT count(*) FROM treasury.category_enrichment WHERE municipality_id = 'e1007bf5-bac9-4b1c-878e-f6834885f850' AND name_key NOT LIKE '%|%'` and confirm it returns 12 (the existing depth-0 DOF Agency enrichments — keyed by plain name, so they survive the reload unchanged per D-08). Record this.
    Step 2 — Live enrichment of depth-2 nodes for all reloaded FYs that have function data. Run `node scripts/enrichCategories.js --city "California" --state CA --year 2026 --depth 2` (and repeat for --year 2022..2025 if those FYs have depth-2 nodes per Task 1). The `--depth 2` flag targets only `depth = 2` rows (D-11 confirmed; default depth 0 would wrongly skip them — Pitfall 3). entity_type='state' is already set so state-level framing applies (carried from Phase 33). Do NOT edit enrichCategories.js — it is read-only reference per D-11.
    Step 3 — Verify new enrichments landed: `SELECT count(*) FROM treasury.category_enrichment WHERE municipality_id = 'e1007bf5-bac9-4b1c-878e-f6834885f850' AND name_key LIKE '%|%'` should return > 0 (depth-2 rows have `parent_dept|function` name_keys containing a pipe). Re-run the depth-0 count from Step 1 and confirm it is still 12 (existing enrichments not wiped).
    Record counts and the cost actually spent in 35-VERIFICATION.md under `## Enrichment (D-08/D-09)`.
    Use mcp__supabase-local execute_sql for the verification queries.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md','utf8');if(!/## Enrichment \(D-08\/D-09\)/.test(t))process.exit(1);console.log('Enrichment section recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - If approved: depth-2 enrichment count (name_key LIKE '%|%') for CA is > 0 after the run
    - The depth-0 enrichment count (name_key NOT LIKE '%|%') is still exactly 12 before AND after enrichment (D-08: existing enrichments survive unchanged)
    - 35-VERIFICATION.md `## Enrichment (D-08/D-09)` records the before/after depth-0 count (12/12), the new depth-2 count, and the actual cost spent (or the skip decision)
    - No code changes were made to scripts/enrichCategories.js (`git diff --stat` shows it unmodified — it is read-only reference per D-11)
  </acceptance_criteria>
  <done>Depth-2 function nodes enriched (or skip recorded), the 12 existing depth-0 enrichments confirmed intact, and counts/cost recorded in 35-VERIFICATION.md; enrichCategories.js unmodified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: human visual verification of the live 3-level CA icicle (ICICLE-02/03)</name>
  <what-built>
    The CA state budget reloaded as a 3-level tree (DOF Agency → Department → Function) and depth-2 function nodes enriched. No frontend code changed — BudgetIcicle.tsx already renders arbitrary depth via navigationPath.
  </what-built>
  <how-to-verify>
    1. Open https://treasurytracker.empowered.vote in a browser (hard-refresh / clear cache to avoid a stale bundle).
    2. Open the entity picker, find the "STATE GOVERNMENTS" section, and click "California".
    3. Confirm the Money Out tab shows the General Fund total in the ~$228B range for FY2025-26, per-capita ~$5,800, and the year selector shows FY2024-25 and FY2025-26.
    4. ICICLE-02: In the icicle chart, click a top-level DOF Agency block (Level 1) → confirm it drills into Departments (Level 2) → click a Department block → confirm it drills into Function nodes (Level 3). All three drill levels must be clickable and animate without layout breakage.
    5. ICICLE-03: At Level 3 (a Function node), confirm clicking it opens the LineItemsTable with leaf line items, identical in behavior to a 2-level city at its deepest level.
    6. Spot-check a Level-3 function node's description (e.g. "Local Assistance" under a department) — it should show enriched state-level framing text, not be blank (skip this check if the cost-gate was declined).
    7. Cross-check NO regression: open one existing 2-level city (e.g. Portland or Dallas) and confirm it still drills correctly to its deepest level (no extra empty level, no broken animation).
    Record pass/fail for ICICLE-02 and ICICLE-03 and any visual issues in 35-VERIFICATION.md under `## ICICLE-02 / ICICLE-03 — Live App Spot-Check`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 CA drill levels work and Level 3 opens LineItemsTable with no regression on existing cities, or describe the issues observed</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 35-VERIFICATION.md and mark ICICLE-01/02/03 complete</name>
  <files>.planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (ICICLE-01/02/03 lines 33-35 and Traceability table lines 80-82)
    - .planning/ROADMAP.md (Phase 35 section, success criteria lines 595-601)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md (the sections Tasks 1, 2, and the checkpoint wrote)
  </read_first>
  <action>
    Finalize 35-VERIFICATION.md so it contains, in order: `## ICICLE-01 — DB Depth Verification` (per-FY rows_inserted + depth distribution), `## Enrichment (D-08/D-09)` (counts + cost or skip), and `## ICICLE-02 / ICICLE-03 — Live App Spot-Check` (human pass/fail). Add a top summary line stating each of ICICLE-01, ICICLE-02, ICICLE-03 as PASS or FAIL with one-line evidence each.
    In .planning/REQUIREMENTS.md, change `- [ ] **ICICLE-01**` → `- [x] **ICICLE-01**`, same for ICICLE-02 and ICICLE-03 (only if the live spot-check passed). Update the Traceability table rows for ICICLE-01/02/03 from Pending → Complete.
    In .planning/ROADMAP.md Phase 35 section, update `**Plans:** TBD` to `**Plans:** 3/3 plans complete` and check the Phase 35 box in the Phase Summary list. Update the Progress table row for Phase 35 to Complete with today's date.
    If the live spot-check FAILED any criterion, do NOT mark the requirement complete — instead record the gap in 35-VERIFICATION.md so a `--gaps` plan can address it.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const v=fs.readFileSync('.planning/phases/35-ca-state-3-level-icicle-pilot/35-VERIFICATION.md','utf8');for(const k of ['ICICLE-01','ICICLE-02','ICICLE-03']){if(!v.includes(k))process.exit(1);}console.log('all three ICICLE reqs referenced in VERIFICATION.md')"</automated>
  </verify>
  <acceptance_criteria>
    - 35-VERIFICATION.md contains all three sections and a top summary marking ICICLE-01/02/03 PASS or FAIL with evidence
    - If all passed: REQUIREMENTS.md shows `- [x] **ICICLE-01**`, `- [x] **ICICLE-02**`, `- [x] **ICICLE-03**` and the Traceability table shows all three Complete
    - ROADMAP.md Phase 35 shows `**Plans:** 3/3 plans complete` and the Progress table row for Phase 35 is Complete
    - If any criterion failed, the requirement remains unchecked and the gap is documented in 35-VERIFICATION.md
  </acceptance_criteria>
  <done>35-VERIFICATION.md records ICICLE-01/02/03 results; on full pass, REQUIREMENTS.md and ROADMAP.md are updated to Complete; on any failure, the gap is documented and the requirement left open.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → live DB | Service-role write to production budget_categories/line_items via treasury_sync_budget_tree; in-place replacement of real CA data |
| enrichment → Anthropic API | Category names sent to Claude Haiku; descriptions written back to category_enrichment |
| shell env → enrichment script | ANTHROPIC_API_KEY + SUPABASE_SERVICE_ROLE_KEY read from env (not committed); preflight confirms presence without printing values |
| live app → citizen | Public read of the reloaded 3-level tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | reload corrupts/double-counts CA data | mitigate | Task 1 asserts FY2026 total unchanged ($228,365,858,000) and sanity band per FY; RPC in-place replacement verified in Phase 34 (D-07) leaves no orphans |
| T-35-08 | Tampering | reload wipes existing depth-0 enrichments | mitigate | Task 2 asserts the 12 depth-0 enrichments (name-keyed) are intact before AND after; enrichment survival verified by D-08 research |
| T-35-09 | Denial of Service / cost | runaway enrichment spend | mitigate | Blocking $5 cost-gate decision checkpoint with mandatory dry-run count before any live enrichment (D-10); checkpoint is never auto-approved |
| T-35-10 | Information Disclosure | API keys printed to logs | mitigate | Preflight confirms env vars are SET without echoing their values; keys are read from env, never hardcoded |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- All 5 FYs reloaded; CA FY2026 budget_categories has depth-0/1/2 rows; total unchanged
- 12 depth-0 enrichments intact; new depth-2 enrichments present (or skip recorded)
- Live app: 3 drill levels work, Level 3 opens LineItemsTable, no regression on a 2-level city
- 35-VERIFICATION.md written; REQUIREMENTS.md + ROADMAP.md updated on full pass
</verification>

<success_criteria>
- ICICLE-01: CA loaded as a genuine 3-level tree (depth-2 rows in DB)
- ICICLE-02: icicle renders 3 drill-down levels in the live app
- ICICLE-03: Level 3 opens LineItemsTable with leaf line items
- Existing enrichments preserved; new depth-2 nodes enriched within the $5 gate
- Phase 35 marked complete in REQUIREMENTS.md and ROADMAP.md
</success_criteria>

<output>
Create `.planning/phases/35-ca-state-3-level-icicle-pilot/35-03-SUMMARY.md` when done.
</output>
