---
phase: 35-ca-state-3-level-icicle-pilot
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified: [scripts/extractCA.py, scripts/processCA.js]
autonomous: true
requirements: [ICICLE-01]
must_haves:
  truths:
    - "extractCA.py emits the function column (col 2) in every row of its JSON output"
    - "processCA.js builds a data-driven N-level tree from a column list, not a hardcoded 2-level tree"
    - "Rows with null/blank function collapse to the department per the D-05 strategy decided in 35-DISCOVERY.md"
    - "processCA.js throws on a missing SUPABASE_URL instead of falling back to a hardcoded production URL"
    - "Dry-run for all 5 FYs produces a 3-level tree whose per-FY totals stay inside the $150B-$300B sanity band"
  artifacts:
    - path: "scripts/extractCA.py"
      provides: "function field added to rows_out (D-03)"
      contains: "row[COLS['function']]"
    - path: "scripts/processCA.js"
      provides: "buildNLevelTree N-level builder + SUPABASE_URL guard (D-02/D-04/D-05/D-12)"
      contains: "buildNLevelTree"
  key_links:
    - from: "scripts/extractCA.py rows_out"
      to: "scripts/processCA.js buildNLevelTree"
      via: "function field consumed as the 3rd level column"
      pattern: "function"
    - from: "scripts/processCA.js buildNLevelTree"
      to: "treasury_sync_budget_tree p_tree"
      via: "c->c->i tree shape passed to loadFiscalYear"
      pattern: "buildNLevelTree\\(fyRows"
---

<objective>
Implement the data reshape that turns the CA loader from a 2-level (DOF Agency → Department) tree into a data-driven N-level tree (DOF Agency → Department → Function) WITHOUT changing the data source, RPC, API, or frontend. All changes are in-place edits to two existing scripts per D-01.

Three edits:
1. **extractCA.py (D-03):** add `'function': row[COLS['function']]` to the `rows_out` dict so the 3rd-level column reaches processCA.js. Update the module/return docstrings to mention the Function third level.
2. **processCA.js (D-02/D-04/D-05):** replace `buildCATree()` with a recursive, data-driven `buildNLevelTree(rows, levelCols)` driven by a `LEVEL_COLS = ['dof_agency', 'department', 'function']` constant; handle null-function collapse per the **D-05 strategy recorded in 35-01-SUMMARY.md / 35-DISCOVERY.md** (mixed-node emit if A2 ACCEPTED, drop-items-keep-total fallback if A2 REJECTED).
3. **processCA.js (D-12):** remove the hardcoded `|| 'https://kxsdzaojfaibhuzmclfq.supabase.co'` SUPABASE_URL fallback and exit(2) on a missing env var, matching the existing SUPABASE_KEY guard.

This plan validates via dry-run only — NO live DB writes happen here. The live reload is Plan 03.

Purpose: Produce the tree shape that makes the icicle drill 3 levels (ICICLE-01).
Output: Modified scripts/extractCA.py and scripts/processCA.js; dry-run confirms 3-level output for all 5 FYs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-CONTEXT.md
@.planning/phases/35-ca-state-3-level-icicle-pilot/35-01-SUMMARY.md
@scripts/extractCA.py
@scripts/processCA.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: extractCA.py — emit the function column (D-03)</name>
  <files>scripts/extractCA.py</files>
  <read_first>
    - scripts/extractCA.py (lines 1-24 docstring, lines 40-49 COLS dict, lines 77-135 extract_budget rows_out loop and the dry-run summary block)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md (extractCA.py section, lines 21-77 — exact target dict and docstring edits)
  </read_first>
  <action>
    In `extract_budget()` (currently lines 121-126), add the key `'function': row[COLS['function']],` to the `rows_out.append({...})` dict, placed between the `'department'` line and the `'amount_thousands'` line. `COLS['function']` is already defined as 2 (line 43) — do not change the COLS dict. The function value is one of 'Local Assistance', 'State Operations', 'Capital Outlay', or None.
    Update the module docstring (line 7) from "groups by DOF Agency (top-level) -> Department (second-level)." to "groups by DOF Agency (top-level) -> Department (second-level) -> Function (third-level)."
    Update the return-shape comment (line 93) from "{ fiscal_year, dof_agency, department, amount_thousands }" to "{ fiscal_year, dof_agency, department, function, amount_thousands }".
    Do NOT change: XLSX_PATH, SHEET, the General Fund filter, the null-amount skip, fy_to_int, the requested_fys filter, the dry-run summary logic, or the __main__ block. The function value may legitimately be None — do NOT add a filter that skips null-function rows (D-05 requires those rows to survive as department-level line items).
  </action>
  <verify>
    <automated>python -c "import ast; src=open('scripts/extractCA.py').read(); assert \"row[COLS['function']]\" in src, 'function field not added'; ast.parse(src); print('extractCA.py parses and emits function')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "row\[COLS\['function'\]\]" scripts/extractCA.py` returns a line inside the rows_out.append dict (between 'department' and 'amount_thousands')
    - Running `python scripts/extractCA.py --fy 2026` from repo root emits JSON where the first object has a `function` key (value is a string or null)
    - `python scripts/extractCA.py --fy 2026` still reports ~219 GF rows / ~$228.4B (no rows lost — the null-amount skip and GF filter are unchanged)
    - The COLS dict is unchanged (still has `'function': 2`)
    - No filter was added that drops null-function rows
  </acceptance_criteria>
  <done>extractCA.py rows_out includes the function field; `python scripts/extractCA.py --fy 2026` emits objects with a `function` key and the FY2026 row count/total are unchanged from Phase 33 (~219 rows, ~$228.4B).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: processCA.js — replace buildCATree with buildNLevelTree + fix SUPABASE_URL (D-02/D-04/D-05/D-12)</name>
  <files>scripts/processCA.js</files>
  <read_first>
    - scripts/processCA.js (line 59 SUPABASE_URL fallback; lines 60-62 SUPABASE_KEY guard idiom to copy; lines 138-161 buildCATree to replace; lines 254-279 main() call site at line 261 and the total/sanity logic)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-01-SUMMARY.md (the chosen D-05 strategy: ACCEPTED=mixed-node emit, REJECTED=drop-items-keep-total)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-PATTERNS.md (lines 81-251 — exact target patterns for buildNLevelTree, LEVEL_COLS constant, SUPABASE_URL guard, header JSDoc; "Key patterns to preserve" list at lines 132-138)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (Pattern 2 two-pass builder lines 289-339; D-05 implementation note lines 342-345)
  </read_first>
  <action>
    Apply three edits to scripts/processCA.js. Do NOT touch loadEnv, resolveMainRoot, extractExcel, ensureMunicipality, getDataSource, loadFiscalYear, the sanity band constants, or the CLI arg parsing.

    Edit A (D-12, line 59): Replace `const SUPABASE_URL = process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co';` with `const SUPABASE_URL = process.env.SUPABASE_URL;` followed by `if (!SUPABASE_URL) { console.error('Missing SUPABASE_URL'); process.exit(2); }`. This mirrors the existing SUPABASE_KEY guard on the next line. The string `kxsdzaojfaibhuzmclfq` must not appear anywhere in the file after this edit.

    Edit B (D-02/D-04/D-05): Replace the entire `buildCATree(rows)` function (lines 138-161) with `buildNLevelTree(rows, levelCols)`, a recursive data-driven builder. Use the two-pass recursive approach from 35-RESEARCH.md Pattern 2 (lines 289-339). Preserve these exact invariants from the old function:
      - amount scaling `(row.amount_thousands || 0) * 1000` (the x1000 LAO-thousands rule),
      - sort by amount descending at EVERY level (`nodes.sort((a, b) => b.a - a.a)`),
      - leaf line-item shape exactly `{ d: name, a: amount, aa: null, f: null, e: null }`,
      - branch node shape `{ n: name, a: total, c: children }`, leaf node shape `{ n: name, a: total, i: [items] }`.
    Recursion contract: `recurse(rows, levelIdx)` groups rows by `levelCols[levelIdx]`. At the last level index, each group becomes a leaf node with `i` line items. At internal levels, each group recurses to build `c` children. The node amount `a` is the sum of all its rows' scaled amounts (so totals are preserved regardless of collapse).
    D-05 null/blank handling — implement EXACTLY the strategy recorded in 35-01-SUMMARY.md:
      - If 35-01-SUMMARY says `A2 VERDICT: ACCEPTED` → a row whose `function` is null/blank/whitespace at the function level collapses to a line item placed on its DEPARTMENT node alongside that department's `c` children, producing `{ n: dept, a: deptTotal, c: [function children], i: [collapsed line items] }`. A department with ONLY null-function rows becomes a leaf `{ n: dept, a, i: [...] }` with no `c`.
      - If 35-01-SUMMARY says `A2 VERDICT: REJECTED` → null-function rows are NOT emitted as separate line items, but their amounts ARE summed into the department node's `a` (and into all ancestor totals). A department with ONLY null-function rows still becomes a leaf `{ n: dept, a, i: [its line items] }`. Never invent a synthetic "General"/"Other" node (D-05).
    Add a `const LEVEL_COLS = ['dof_agency', 'department', 'function'];` constant near the top of the file (after the sanity band block, around line 70) with a comment that adding a 4th entry requires zero further code changes (D-02). Update the file-header JSDoc (lines ~13-15) from the 2-level shape to the 3-level shape shown in 35-PATTERNS.md lines 244-250.

    Edit C (D-04 call site, line 261): Replace `const tree = buildCATree(fyRows);` with `const tree = buildNLevelTree(fyRows, LEVEL_COLS);`. This is the only caller of buildCATree (confirmed in 35-PATTERNS.md note 3). The `const total = tree.reduce((sum, n) => sum + n.a, 0);` line directly below is unchanged and still correct (it sums top-level agency amounts).
  </action>
  <verify>
    <automated>node --check scripts/processCA.js && node -e "const s=require('fs').readFileSync('scripts/processCA.js','utf8'); if(s.includes('kxsdzaojfaibhuzmclfq'))process.exit(1); if(!/function buildNLevelTree\(/.test(s))process.exit(1); if(/buildCATree\(/.test(s))process.exit(1); if(!/LEVEL_COLS\s*=\s*\['dof_agency',\s*'department',\s*'function'\]/.test(s))process.exit(1); console.log('processCA.js edits present, no hardcoded URL, no buildCATree caller')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/processCA.js` exits 0
    - The string `kxsdzaojfaibhuzmclfq` does NOT appear anywhere in scripts/processCA.js
    - `function buildNLevelTree(` is defined; `buildCATree` is neither defined nor called anywhere in the file
    - `LEVEL_COLS = ['dof_agency', 'department', 'function']` constant exists
    - The line-item shape `{ d:`, `a:`, `aa: null, f: null, e: null }` is preserved in the new builder
    - `nodes.sort((a, b) => b.a - a.a)` (descending sort) appears in the recursive builder
    - The amount scaling `* 1000` appears in the new builder
    - The D-05 branch implemented matches the verdict in 35-01-SUMMARY.md (mixed-node emit for ACCEPTED, or amount-only-into-total for REJECTED); no synthetic "General"/"Other" node string is created
  </acceptance_criteria>
  <done>processCA.js defines buildNLevelTree, calls it from the FY loop, has no hardcoded SUPABASE_URL fallback and no buildCATree references, and implements the D-05 strategy from 35-01-SUMMARY.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Dry-run validation — confirm 3-level output and total preservation for all 5 FYs</name>
  <files>scripts/processCA.js, scripts/extractCA.py</files>
  <read_first>
    - scripts/processCA.js (lines 96-136 extractExcel dry-run path, lines 254-279 the dry-run early-return and sanity check)
    - .planning/phases/35-ca-state-3-level-icicle-pilot/35-RESEARCH.md (Dry-Run Verification Commands lines 607-626; Pitfall 4 sanity band lines 551-557)
  </read_first>
  <action>
    Validate the two code changes WITHOUT any DB writes. This task makes no further source edits unless a defect is found.
    Step 1: Run `python scripts/extractCA.py --fy 2026` from repo root — confirm each emitted object has a `function` key and the row count is ~219 / total ~$228.4B (unchanged from Phase 33).
    Step 2: Confirm the FY2026 tree built by buildNLevelTree is genuinely 3 levels deep. Use a temporary node snippet (e.g. scripts/_verify-tree-temp.mjs, deleted after) that pipes the extractor JSON for FY2026 through the same LEVEL_COLS=['dof_agency','department','function'] builder logic (import from processCA.js if it is exported, otherwise inline a copy of buildNLevelTree) and asserts: at least one agency node has a department child that has a non-empty `c` array, and at least one of those function children carries a non-empty `i` array. Keep processCA.js logging behavior unchanged — do not add permanent console output to processCA.js for this check.
    Step 3: Confirm total preservation. Compute the FY2026 tree total `tree.reduce((s,n)=>s+n.a,0)` and assert it is inside $150B-$300B AND within $1 of the Phase 33 2-level total $228,365,858,000 (Pitfall 4 — the reshape must not change the total). A nonzero delta means double-counting or dropped rows; if so, fix buildNLevelTree in Task 2 before completing.
    Step 4: Repeat the total-in-band check and the depth-2-node-present check for FY2022, FY2023, FY2024, FY2025. Each FY total must stay in the sanity band. Each FY must have at least one depth-2 node, OR be explicitly documented in the SUMMARY as legitimately 2-level-only if 35-DISCOVERY.md A1 showed that FY lacks function data.
    Delete the temp snippet. Record per-FY totals and depth-2 presence in 35-02-SUMMARY.md.
  </action>
  <verify>
    <automated>python scripts/extractCA.py --fy 2026 | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const rows=JSON.parse(d);if(!rows.length)process.exit(1);if(!('function' in rows[0]))process.exit(1);const t=rows.reduce((s,r)=>s+(r.amount_thousands||0)*1000,0);if(t<150e9||t>300e9)process.exit(1);console.log('FY2026 rows='+rows.length+' total=$'+t.toLocaleString())})"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/extractCA.py --fy 2026` emits objects each carrying a `function` key
    - The FY2026 tree total equals $228,365,858,000 (within $1 of the Phase 33 total) and is inside $150B-$300B
    - The FY2026 tree has at least one depth-2 node: an agency → department → function(`c`) → line-items(`i`) chain exists
    - FY2022-FY2025 each produce a total inside the $150B-$300B sanity band
    - Each of FY2022-FY2026 either has a depth-2 node or is documented in the SUMMARY as legitimately 2-level-only per A1
    - No temp verification script remains in scripts/ (`git status` shows no untracked _verify-*-temp file)
  </acceptance_criteria>
  <done>Dry-run confirms the extractor emits `function`, the FY2026 tree is 3 levels deep with an unchanged $228,365,858,000 total, and all 5 FYs pass the sanity band; per-FY findings recorded in the SUMMARY; no temp files left.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → script config | SUPABASE_URL/KEY read from .env; D-12 removes the hardcoded production URL fallback so a missing env var fails loudly instead of silently targeting prod |
| Excel file → Python reader | Local trusted file, read-only |
| extractor JSON → Node builder | Internal trust boundary; the only untrusted-ish content is function-name strings used as object keys (no eval, no SQL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information Disclosure | hardcoded SUPABASE_URL fallback (line 59) | mitigate | Edit A removes the `kxsdzaojfaibhuzmclfq` fallback and exits(2) on missing SUPABASE_URL; acceptance criterion asserts the string is absent from the file (D-12 / WR-04) |
| T-35-05 | Tampering | wrong totals from buildNLevelTree double-count or drop rows | mitigate | Task 3 asserts FY2026 total equals the Phase 33 total within $1 and all FYs stay in the $150B-$300B sanity band |
| T-35-06 | Tampering | function-name strings used as Map keys | accept | Strings are used only as JS object/Map keys and node names; no eval, no shell, no SQL string-building (RPC uses parameterized p_tree) |
| T-35-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH.md Package Legitimacy Audit: zero new packages) |
</threat_model>

<verification>
- `node --check scripts/processCA.js` passes; `python -c "import ast; ast.parse(open('scripts/extractCA.py').read())"` passes
- `kxsdzaojfaibhuzmclfq` absent from processCA.js; buildCATree fully removed; buildNLevelTree present
- FY2026 dry-run total = $228,365,858,000 (unchanged) with a depth-2 node present
- No temp scripts left in the working tree
</verification>

<success_criteria>
- extractCA.py emits the `function` field (D-03)
- processCA.js uses a data-driven N-level builder with LEVEL_COLS (D-02/D-04), implements the D-05 strategy from 35-01-SUMMARY, and has no hardcoded SUPABASE_URL (D-12)
- Dry-run proves a genuine 3-level tree with totals preserved for all 5 FYs
- No DB writes occurred in this plan (writes are Plan 03)
</success_criteria>

<output>
Create `.planning/phases/35-ca-state-3-level-icicle-pilot/35-02-SUMMARY.md` when done. Record per-FY totals, depth-2 presence per FY, and which D-05 branch was implemented.
</output>
