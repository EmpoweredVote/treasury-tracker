---
phase: 100-new-york-florida-acfr-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - scripts/processNY.js
  - scripts/processNYRevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: false
requirements: [ACFR-03, ACFR-05, RECON-03]

must_haves:
  truths:
    - "User sees New York GF revenue-by-source on the NY state node (ACFR-sourced, GAAP basis-labelled)"
    - "User sees New York GAAP spending-by-function on the NY state node, replacing the prior NASBO operating rows"
    - "Each loaded NY FY ties to its ACFR GF column printed total (millions x1,000,000)"
    - "Re-running the NY loaders writes 0 net-new rows (idempotent in-place update)"
    - "The 2 stale NY data_sources (ny-gf-operating, ny-gf-revenue) are deleted; fresh ny-acfr-* exist"
    - "Negative NY categories (investment income) render clamped to 0 with signed magnitude in the label"
  artifacts:
    - path: "scripts/processNY.js"
      provides: "Live NY operating load — treasury.budgets (muni 1a7f871c, dataset_type operating, FY2015-2024)"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processNYRevenueAcfr.js"
      provides: "Live NY revenue insert — treasury.budgets (dataset_type revenue, FY2015-2024)"
      contains: "dataset_type"
  key_links:
    - from: "scripts/processNY.js"
      to: "NY state node operating rows (municipality_id 1a7f871c, fy, 'operating')"
      via: "treasury_sync_budget_tree updates in place → NASBO replaced by ACFR GAAP"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processNYRevenueAcfr.js"
      to: "NY state node revenue rows (new dataset_type='revenue')"
      via: "pure insert (no prior NASBO revenue)"
      pattern: "p_dataset_type.*revenue|dataset_type.*revenue"
---

<objective>
Run the NY loaders from 100-01 against the LIVE production treasury table: load NY GF revenue-by-source + GAAP spending-by-function for FY2015-2024 (millions-scaled), replacing the existing NASBO operating rows on the NY state node idempotently (RECON-03), delete the 2 stale NY `data_sources`, and verify each FY ties to its ACFR printed total + a re-run is a no-op.

Purpose: Deliver ACFR-03 (NY upgraded to full ACFR GAAP node) on production, reusing the proven Phase-99 path.
Output: Live NY operating (replaced) + revenue (new) rows on node 1a7f871c; stale NY data_sources removed; fresh ny-acfr-* data_sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-CONTEXT.md
@.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-01-SUMMARY.md

<interfaces>
<!-- Data model the live write touches. -->
- NY state node: treasury.municipalities id = 1a7f871c-7f2e-4786-9c55-5ab3409716f4 (entity_type='state').
- budgets keyed (municipality_id, fiscal_year, dataset_type); columns total_budget, hierarchy (text[]), data_source, source_url, source_date. Probe with total_budget (NOT total) and hierarchy (NOT tree); there is no row_count column.
- Operating replace (D-11): treasury_sync_budget_tree with p_dataset_type='operating' on the SAME (muni,fy,'operating') key NASBO used → RPC UPDATES IN PLACE → NASBO row replaced by ACFR GAAP row. Then targeted post-RPC UPDATE of {source_url, source_date, data_source}. NEVER treasury_sync_city_budget (not source-safe).
- Revenue insert (D-12): dataset_type='revenue' is new on this node → pure insert.
- Pre-existing NY NASBO operating rows: FY2023 $84.474B, FY2024 $91.070B (both replaced by ACFR FY2023/FY2024). Window FY2015-2024 fully covers them → no orphan FY (D-13).
- UNITS: NY ACFR is in MILLIONS → stored dollars = printed x 1,000,000. FY2024 GF Total revenues $93,894M → total_budget $93,894,000,000.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve first live NY production write</name>
  <action>PAUSE for human approval before the first production write to treasury.budgets for NY. Present the dry-run results below; do not run the live NY load until the operator types "approved".</action>
  <what-built>Two dry-run-clean NY loaders (100-01) ready to write to the LIVE production treasury.budgets table on the NY state node (1a7f871c).</what-built>
  <how-to-verify>
    This is the first NY production DB write. Before approving:
    1. Run `node scripts/processNY.js --dry-run` and `node scripts/processNYRevenueAcfr.js --dry-run`; confirm every FY2015-2024 prints "validation: PASS" and the bookend ties (FY2024 rev $93,894,000,000 — i.e. the millions x1,000,000 scaling is correct, NOT $93,894,000).
    2. Confirm this writes ONLY to muni 1a7f871c (New York state node) — no other state touched.
    3. Confirm the operating load will REPLACE NASBO FY2023 ($84.474B) + FY2024 ($91.070B) with ACFR GAAP (expected — one basis per state-FY).
    4. Acknowledge the NY node total/basis will change to ACFR GAAP and Money In becomes a real revenue view; negative investment-income years will render clamped.
  </how-to-verify>
  <resume-signal>Type "approved" to run the live NY load, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Live-load NY operating + revenue (FY2015-2024) and replace NASBO operating</name>
  <files>scripts/processNY.js, scripts/processNYRevenueAcfr.js</files>
  <read_first>
    - scripts/processNY.js, scripts/processNYRevenueAcfr.js (built + dry-run-validated in 100-01)
    - .planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-01-SUMMARY.md (per-FY extracted totals to reconcile against)
  </read_first>
  <action>
    Source the env (SUPABASE_URL + SUPABASE_SERVICE_KEY) then run the live loads (no --dry-run): `node scripts/processNY.js` (operating, all FY2015-2024) and `node scripts/processNYRevenueAcfr.js` (revenue, all FY2015-2024). Each writes via treasury_sync_budget_tree keyed (1a7f871c, fy, dataset_type) then the targeted post-RPC UPDATE stamps {source_url=per-FY ACFR URL, source_date=FY-03-31, data_source=GAAP-labelled string}.
    The operating writes hit the SAME (muni,fy,'operating') key as the NASBO rows → the RPC updates in place, so NASBO FY2023/FY2024 are REPLACED by ACFR GAAP (D-11, RECON-03); the other FYs are new operating rows. Revenue rows are pure inserts (dataset_type='revenue' new on this node, D-12). The per-FY tie-check gate (built in 100-01) still runs and will exit 2 before writing any FY that does not tie to its printed total. If a download is flaky, drive per-`--fy` in a retry loop.
    Do NOT use treasury_sync_city_budget. Do NOT touch any non-NY node.
  </action>
  <verify>
    <automated>node scripts/processNY.js --fy 2024 --dry-run && node scripts/processNYRevenueAcfr.js --fy 2024 --dry-run</automated>
  </verify>
  <done>treasury.budgets has NY (1a7f871c) operating rows for FY2015-2024 carrying ACFR GAAP data_source strings (NASBO FY2023/FY2024 strings gone), and revenue rows for FY2015-2024; FY2024 revenue total_budget = $93,894,000,000 (millions x1,000,000); each row's source_url points at the per-year osc.ny.gov finance ACFR PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale NY data_sources, create fresh ny-acfr-*, verify idempotency + clamp</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs, scripts/processNY.js, scripts/processNYRevenueAcfr.js</files>
  <read_first>
    - scripts/cleanupStaleStateGFDataSources.mjs (extended + dry-run-validated for NY in 100-01)
  </read_first>
  <action>
    Run the live cleanup for NY: `node scripts/cleanupStaleStateGFDataSources.mjs --state NY` — deletes ny-gf-operating + ny-gf-revenue (D-04) after re-asserting each backs 0 budgets rows (the script exits 2 rather than delete a row-backed data_source). The fresh `ny-acfr-gf-operating` + `ny-acfr-gf-revenue` data_sources are created by the loaders' find-or-create in Task 1 — confirm they exist and the deleted stale ones are gone. Do NOT delete ny-gf-operating-nasbo.
    Verify IDEMPOTENCY (D-14, SC#2): re-run `node scripts/processNY.js` and `node scripts/processNYRevenueAcfr.js` once more and confirm 0 net-new budgets rows (RPC updates the same (muni,fy,dataset) rows in place); re-run the cleanup and confirm 0 deleted. Verify the P2 clamp (D-15, ACFR-05): for each NY FY with a negative GF category (investment income), confirm it renders area 0 with the signed magnitude in the hierarchy label and the FY's total_budget preserves the net (un-clamped) control total. Confirm no NASBO/other-state node changed — including CA/TX from Phase 99 (un-upgraded + already-upgraded states untouched, D-14).
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --state NY --dry-run</automated>
  </verify>
  <done>treasury.data_sources for NY: ny-gf-operating + ny-gf-revenue gone; ny-acfr-gf-operating + ny-acfr-gf-revenue present; ny-gf-operating-nasbo untouched. Second loader run = 0 net-new rows; second cleanup run = 0 deleted. Each negative NY category renders clamped-to-0 with signed label; FY total preserved. No other state node altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets (NY node 1a7f871c) | production finance table mutation crosses here |
| cleanup → treasury.data_sources | production metadata deletion crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-02-01 | Tampering | RPC writing wrong-keyed/over-broad rows (e.g. via treasury_sync_city_budget) | mitigate | use only treasury_sync_budget_tree keyed (1a7f871c, fy, dataset_type); explicit muni-id scope; never the city RPC |
| T-100-02-02 | Information disclosure | NY millions read as thousands (1000x understated) or a FY that does not tie | mitigate | per-FY tie-check gate (<=$10M) exits 2 before write; FY2024 bookend asserted at $93,894,000,000 |
| T-100-02-03 | Tampering | deleting a data_source that still backs live rows | mitigate | cleanup pre-asserts 0 budgets rows; exits 2 otherwise; only ny-gf-operating/ny-gf-revenue targeted; ny-gf-operating-nasbo never touched |
| T-100-02-04 | Repudiation | unsourced/mislabelled basis shown as real | mitigate | post-RPC stamp of data_source (GAAP) + source_url + source_date per FY |
| T-100-02-05 | Denial of service | flaky download aborts a multi-FY run mid-way | accept | per-FY retry loop; idempotent re-run is safe (0 net-new) |
| T-100-02-SC | Tampering | npm installs | accept | no new packages; uses vendored @supabase/supabase-js + system pdftotext |
</threat_model>

<verification>
- NY operating rows FY2015-2024 carry ACFR GAAP data_source; NASBO FY2023/FY2024 replaced (RECON-03).
- NY revenue rows FY2015-2024 inserted; Money In now has data (ACFR-03).
- Bookend ties: FY2024 $93,894,000,000 (millions confirmed).
- Re-run = 0 net-new rows (idempotent, SC#2/D-14).
- Stale ny-gf-operating + ny-gf-revenue deleted; ny-acfr-* present (D-04); ny-gf-operating-nasbo untouched.
- Negative investment-income categories clamped (ACFR-05/SC#3).
- No other state node touched (incl. CA/TX).
</verification>

<success_criteria>
NY state node (1a7f871c) shows ACFR-sourced GF revenue-by-source + GAAP spending-by-function, GAAP basis-labelled, each FY tying to the ACFR printed total (millions-scaled), NASBO operating replaced in place, idempotent re-run a no-op, stale NY data_sources cleaned up, negatives clamped, un-upgraded + already-upgraded states untouched. ACFR-03 + RECON-03 (NY) + ACFR-05 (NY) satisfied.
</success_criteria>

<output>
Create `.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-02-SUMMARY.md` when done — record NY per-FY loaded totals + tie results (with millions conversion shown), the NASBO→ACFR replace confirmation, idempotency proof, the data_sources before/after, and the clamped negative investment-income categories.
</output>
