---
phase: 100-new-york-florida-acfr-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - scripts/processFL.js
  - scripts/processFLRevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: false
requirements: [ACFR-04, ACFR-05, RECON-03]

must_haves:
  truths:
    - "User sees Florida GF revenue-by-source on the FL state node (ACFR-sourced, GAAP basis-labelled)"
    - "User sees Florida GAAP spending-by-function on the FL state node, replacing the prior NASBO operating rows"
    - "Each loaded FL FY ties to its ACFR GF column printed total"
    - "Re-running the FL loaders writes 0 net-new rows (idempotent in-place update)"
    - "The 2 stale FL data_sources (fl-gf-operating, fl-gf-revenue) are deleted; fresh fl-acfr-* exist"
    - "Any negative FL category renders clamped to 0 with signed magnitude in the label"
  artifacts:
    - path: "scripts/processFL.js"
      provides: "Live FL operating load — treasury.budgets (muni adb19ea0, dataset_type operating, FY2022-2024)"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processFLRevenueAcfr.js"
      provides: "Live FL revenue insert — treasury.budgets (dataset_type revenue, FY2022-2024)"
      contains: "dataset_type"
  key_links:
    - from: "scripts/processFL.js"
      to: "FL state node operating rows (municipality_id adb19ea0, fy, 'operating')"
      via: "treasury_sync_budget_tree updates in place → NASBO replaced by ACFR GAAP"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processFLRevenueAcfr.js"
      to: "FL state node revenue rows (new dataset_type='revenue')"
      via: "pure insert (no prior NASBO revenue)"
      pattern: "p_dataset_type.*revenue|dataset_type.*revenue"
---

<objective>
Run the FL loaders from 100-01 against the LIVE production treasury table: load FL GF revenue-by-source + GAAP spending-by-function for FY2022-2024, replacing the existing NASBO operating rows on the FL state node idempotently (RECON-03), delete the 2 stale FL `data_sources`, and verify each FY ties to its ACFR printed total + a re-run is a no-op.

Purpose: Deliver ACFR-04 (FL upgraded to full ACFR GAAP node) on production — the final state of the v2.11 4-state cohort load.
Output: Live FL operating (replaced) + revenue (new) rows on node adb19ea0; stale FL data_sources removed; fresh fl-acfr-* data_sources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-CONTEXT.md
@.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-01-SUMMARY.md

<interfaces>
<!-- Data model the live write touches. -->
- FL state node: treasury.municipalities id = adb19ea0-de7c-4cd5-9445-cbf2108a8a1a (entity_type='state').
- budgets keyed (municipality_id, fiscal_year, dataset_type); columns total_budget, hierarchy (text[]), data_source, source_url, source_date. Probe with total_budget (NOT total) and hierarchy (NOT tree); there is no row_count column.
- Operating replace (D-11): treasury_sync_budget_tree with p_dataset_type='operating' on the SAME (muni,fy,'operating') key NASBO used → RPC UPDATES IN PLACE → NASBO row replaced by ACFR GAAP row. Then targeted post-RPC UPDATE of {source_url, source_date, data_source}. NEVER treasury_sync_city_budget (not source-safe).
- Revenue insert (D-12): dataset_type='revenue' is new on this node → pure insert.
- Pre-existing FL NASBO operating rows: FY2023 $44.219B, FY2024 $51.649B (both replaced by ACFR FY2023/FY2024). Window FY2022-2024 fully covers them → no orphan FY (D-13).
- UNITS: FL ACFR is in thousands → stored dollars = printed x 1,000. FY2024 GF Total revenues $59,810,603K → total_budget $59,810,603,000. No TX-style scope mismatch — like-for-like GF.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Approve first live FL production write</name>
  <action>PAUSE for human approval before the first production write to treasury.budgets for FL. Present the dry-run results below; do not run the live FL load until the operator types "approved".</action>
  <what-built>Two dry-run-clean FL loaders (100-01) ready to write to the LIVE production treasury.budgets table on the FL state node (adb19ea0).</what-built>
  <how-to-verify>
    This is the first FL production DB write. Before approving:
    1. Run `node scripts/processFL.js --dry-run` and `node scripts/processFLRevenueAcfr.js --dry-run`; confirm every FY2022-2024 prints "validation: PASS" and the bookends tie (FY2022 rev $57,241,428,000, FY2024 $59,810,603,000).
    2. Confirm this writes ONLY to muni adb19ea0 (Florida state node) — no other state touched.
    3. Confirm the operating load will REPLACE NASBO FY2023 ($44.219B) + FY2024 ($51.649B) with ACFR GAAP (expected — one basis per state-FY; FY2024 jumps to ~$59.8B, the normal GAAP-vs-budgetary spread, NOT a TX-style scope change).
    4. Acknowledge the FL node total/basis will change to ACFR GAAP and Money In becomes a real revenue view.
  </how-to-verify>
  <resume-signal>Type "approved" to run the live FL load, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Live-load FL operating + revenue (FY2022-2024) and replace NASBO operating</name>
  <files>scripts/processFL.js, scripts/processFLRevenueAcfr.js</files>
  <read_first>
    - scripts/processFL.js, scripts/processFLRevenueAcfr.js (built + dry-run-validated in 100-01)
    - .planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-01-SUMMARY.md (per-FY extracted totals to reconcile against)
  </read_first>
  <action>
    Source the env (SUPABASE_URL + SUPABASE_SERVICE_KEY) then run the live loads (no --dry-run): `node scripts/processFL.js` (operating, all FY2022-2024) and `node scripts/processFLRevenueAcfr.js` (revenue, all FY2022-2024). Each writes via treasury_sync_budget_tree keyed (adb19ea0, fy, dataset_type) then the targeted post-RPC UPDATE stamps {source_url=per-FY ACFR URL, source_date=FY-06-30, data_source=GAAP-labelled string}.
    The operating writes hit the SAME (muni,fy,'operating') key as the NASBO rows → the RPC updates in place, so NASBO FY2023/FY2024 are REPLACED by ACFR GAAP (D-11, RECON-03); FY2022 is a new operating row. Revenue rows are pure inserts (dataset_type='revenue' new on this node, D-12). The per-FY tie-check gate (built in 100-01) still runs and will exit 2 before writing any FY that does not tie to its printed total. If a download is flaky, drive per-`--fy` in a retry loop.
    Do NOT use treasury_sync_city_budget. Do NOT touch any non-FL node.
  </action>
  <verify>
    <automated>node scripts/processFL.js --fy 2024 --dry-run && node scripts/processFLRevenueAcfr.js --fy 2024 --dry-run</automated>
  </verify>
  <done>treasury.budgets has FL (adb19ea0) operating rows for FY2022-2024 carrying ACFR GAAP data_source strings (NASBO FY2023/FY2024 strings gone), and revenue rows for FY2022-2024; FY2022 revenue total_budget = $57,241,428,000 and FY2024 = $59,810,603,000; each row's source_url points at the per-year myfloridacfo.com ACFR PDF.</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale FL data_sources, create fresh fl-acfr-*, verify idempotency + clamp</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs, scripts/processFL.js, scripts/processFLRevenueAcfr.js</files>
  <read_first>
    - scripts/cleanupStaleStateGFDataSources.mjs (extended + dry-run-validated for FL in 100-01)
  </read_first>
  <action>
    Run the live cleanup for FL: `node scripts/cleanupStaleStateGFDataSources.mjs --state FL` — deletes fl-gf-operating + fl-gf-revenue (D-07) after re-asserting each backs 0 budgets rows (the script exits 2 rather than delete a row-backed data_source). The fresh `fl-acfr-gf-operating` + `fl-acfr-gf-revenue` data_sources are created by the loaders' find-or-create in Task 1 — confirm they exist and the deleted stale ones are gone. Do NOT delete fl-gf-operating-nasbo.
    Verify IDEMPOTENCY (D-14, SC#2): re-run `node scripts/processFL.js` and `node scripts/processFLRevenueAcfr.js` once more and confirm 0 net-new budgets rows; re-run the cleanup and confirm 0 deleted. Verify the P2 clamp (D-15, ACFR-05): if any FL FY has a negative GF category, confirm it renders area 0 with the signed magnitude in the hierarchy label and the FY's total_budget preserves the net control total. Confirm no other state node changed — including CA/TX (Phase 99) and NY (100-02) (D-14).
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --state FL --dry-run</automated>
  </verify>
  <done>treasury.data_sources for FL: fl-gf-operating + fl-gf-revenue gone; fl-acfr-gf-operating + fl-acfr-gf-revenue present; fl-gf-operating-nasbo untouched. Second loader run = 0 net-new rows; second cleanup run = 0 deleted. Any negative FL category renders clamped-to-0 with signed label; FY total preserved. No other state node altered (CA/TX/NY intact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets (FL node adb19ea0) | production finance table mutation crosses here |
| cleanup → treasury.data_sources | production metadata deletion crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03-01 | Tampering | RPC writing wrong-keyed/over-broad rows (e.g. via treasury_sync_city_budget) | mitigate | use only treasury_sync_budget_tree keyed (adb19ea0, fy, dataset_type); explicit muni-id scope; never the city RPC |
| T-100-03-02 | Information disclosure | a FY loaded that does not tie to the ACFR printed total | mitigate | per-FY tie-check gate (<=$10M) exits 2 before write; bookends asserted |
| T-100-03-03 | Tampering | deleting a data_source that still backs live rows | mitigate | cleanup pre-asserts 0 budgets rows; exits 2 otherwise; only fl-gf-operating/fl-gf-revenue targeted; fl-gf-operating-nasbo never touched |
| T-100-03-04 | Repudiation | unsourced/mislabelled basis shown as real | mitigate | post-RPC stamp of data_source (GAAP) + source_url + source_date per FY |
| T-100-03-05 | Denial of service | flaky download aborts a multi-FY run mid-way | accept | per-FY retry loop; idempotent re-run is safe (0 net-new) |
| T-100-03-SC | Tampering | npm installs | accept | no new packages; uses vendored @supabase/supabase-js + system pdftotext |
</threat_model>

<verification>
- FL operating rows FY2022-2024 carry ACFR GAAP data_source; NASBO FY2023/FY2024 replaced (RECON-03).
- FL revenue rows FY2022-2024 inserted; Money In now has data (ACFR-04).
- Bookends tie: FY2022 $57,241,428,000; FY2024 $59,810,603,000.
- Re-run = 0 net-new rows (idempotent, SC#2/D-14).
- Stale fl-gf-operating + fl-gf-revenue deleted; fl-acfr-* present (D-07); fl-gf-operating-nasbo untouched.
- Negative category (if any) clamped (ACFR-05/SC#3).
- No other state node touched (CA/TX/NY intact).
</verification>

<success_criteria>
FL state node (adb19ea0) shows ACFR-sourced GF revenue-by-source + GAAP spending-by-function, GAAP basis-labelled, each FY tying to the ACFR printed total, NASBO operating replaced in place, idempotent re-run a no-op, stale FL data_sources cleaned up, negatives clamped, all other states untouched. ACFR-04 + RECON-03 (FL) + ACFR-05 (FL) satisfied — completing the v2.11 4-state cohort load.
</success_criteria>

<output>
Create `.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-03-SUMMARY.md` when done — record FL per-FY loaded totals + tie results, the NASBO→ACFR replace confirmation, idempotency proof, the data_sources before/after, and any clamped negative category.
</output>
