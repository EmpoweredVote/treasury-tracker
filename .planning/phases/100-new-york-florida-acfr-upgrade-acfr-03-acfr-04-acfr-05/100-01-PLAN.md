---
phase: 100-new-york-florida-acfr-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processNY.js
  - scripts/processNYRevenueAcfr.js
  - scripts/processFL.js
  - scripts/processFLRevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: true
requirements: [ACFR-03, ACFR-04, ACFR-05, RECON-03]

must_haves:
  truths:
    - "Four loader scripts exist (NY operating, NY revenue, FL operating, FL revenue), each --dry-run-validatable with no DB writes"
    - "Each script's per-FY GF column total ties to the ACFR printed Total before it would write (tie-check gate refuses to load on mismatch)"
    - "NY figures are scaled millions->dollars (x1,000,000); FL figures thousands->dollars (x1,000)"
    - "Negative GF categories (e.g. NY investment income) are clamped to 0 area with the signed magnitude preserved in the label (P2 / ACFR-05)"
    - "The cleanup script can delete ny-gf-operating + ny-gf-revenue (NY) and fl-gf-operating + fl-gf-revenue (FL) idempotently, gated on a 0-budgets-rows assertion"
  artifacts:
    - path: "scripts/processNY.js"
      provides: "NY GF operating (spend-by-function) loader, FY2015-2024, General column, MILLIONS (x1,000,000)"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processNYRevenueAcfr.js"
      provides: "NY GF revenue-by-source loader with P2 clamp, FY2015-2024, millions"
      contains: "clampForRender"
    - path: "scripts/processFL.js"
      provides: "FL GF operating loader, FY2022-2024, General Fund column, thousands"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processFLRevenueAcfr.js"
      provides: "FL GF revenue-by-source loader with P2 clamp, FY2022-2024"
      contains: "clampForRender"
    - path: "scripts/cleanupStaleStateGFDataSources.mjs"
      provides: "Extended idempotent delete of stale non-NASBO data_sources, now also NY + FL"
      contains: "ny-gf-operating"
  key_links:
    - from: "scripts/processNY.js"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type='operating', p_fiscal_year, keyed (municipality_id, fy, 'operating')"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processNYRevenueAcfr.js"
      to: "P2 clamp"
      via: "clampForRender + signed label on negative categories"
      pattern: "clampForRender"
---

<objective>
Build the four NY/FL ACFR loaders (operating + revenue per state) and extend the existing Phase-99 stale-`data_sources` cleanup script with NY+FL targets — all extraction + validation logic, NO production DB writes in this plan. The loaders are copied from the proven Phase-99 CA loaders (closest analog) / the upstream `processMN.js` + `processOHRevenueAcfr.js` templates, configured per-state per Phase 98 recon, and proven via `--dry-run` with a hard per-FY tie-to-printed-total gate. Wave 2 (100-02 NY, 100-03 FL) runs these against the live table.

Purpose: Produce executable, dry-run-clean loaders so the live-write plans (100-02, 100-03) only flip the switch — extraction risk is fully retired here at $0 (pdftotext only, D-10).
Output: `scripts/processNY.js`, `scripts/processNYRevenueAcfr.js`, `scripts/processFL.js`, `scripts/processFLRevenueAcfr.js`, and the extended `scripts/cleanupStaleStateGFDataSources.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-CONTEXT.md
@.planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md

<interfaces>
<!-- Template patterns to copy. Use these directly — do not re-explore the codebase. -->

From scripts/processCA.js + scripts/processCARevenueAcfr.js (Phase-99 state-ACFR loaders — the CLOSEST analog; copy these for NY/FL):
  - loadEnv() reads ../.env.local then ../.env into process.env (run from repo root).
  - const SOURCES = { <fy>: { url, date }, ... }   // per-FY ACFR URL + fiscal-year-end date
  - const UNITS_MULTIPLIER (CA/FL/TX = 1_000 thousands->dollars; NY = 1_000_000 millions->dollars)
  - const dataSource = (fy) => `<State> State ACFR — General Fund (FY${fy} actual, GAAP basis)` (operating) / `... General Fund Revenue ...` (revenue)
  - const EXPENDITURES / REVENUE = { <fy>: { total, confidence:'actual', categories:[{name,total}] } }
  - validate(fy): catSum must tie to printed total within $10M else return false -> process.exit(2)
  - buildTree(fy): operating children = categories.filter(total>0).map -> {n,a,i}; revenue children = categories.map(cat => { rendered=clampForRender(cat.total); label = cat.total<0 ? `${name} (net loss — shown at 0)` : name; return {n:label,a:rendered,i:[]} }); root = [{n:'<State> General Fund Budget', a:total, c:children}]
  - function clampForRender(amount) { return Math.max(amount, 0); }   // P2 / ACFR-05
  - main(): resolve muni by name+state+entity_type='state', find-or-create data_source, then per-FY: validate -> buildTree -> supabase.rpc('treasury_sync_budget_tree', {p_data_source_id, p_fiscal_year:fy, p_dataset_type, p_total:total, p_tree, p_row_count, p_triggered_by:'bulk_load'}) -> SELECT budgets WHERE muni+fy+dataset_type -> UPDATE {source_url, source_date, data_source}
  - --dry-run flag short-circuits before any RPC/UPDATE; --fy <YYYY> filter for per-FY runs.

From scripts/loadStateGF.mjs (the NASBO loader being replaced — the replace MUST match this key):
  - State node resolve: .from('municipalities').eq('name',name).eq('state',abbr).eq('entity_type','state').single()
  - Operating rows keyed (municipality_id, fiscal_year, 'operating') via treasury_sync_budget_tree -> RPC UPDATES IN PLACE (this is why ACFR operating replaces NASBO operating).

From scripts/cleanupStaleStateGFDataSources.mjs (Phase-99 cleanup — EXTEND, don't rewrite):
  - Already handles --state CA|TX with their stale dataset_ids; --dry-run flag; pre-delete 0-budgets-rows assertion that exits 2 if a target backs >0 rows; refuses *-nasbo ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build NY loaders (operating + revenue), millions-scaled, extract + tie-check FY2015-2024</name>
  <files>scripts/processNY.js, scripts/processNYRevenueAcfr.js</files>
  <read_first>
    - scripts/processCA.js (Phase-99 operating loader to copy -> processNY.js)
    - scripts/processCARevenueAcfr.js (Phase-99 revenue loader to copy, has P2 clamp -> processNYRevenueAcfr.js)
    - scripts/processMN.js, scripts/processOHRevenueAcfr.js (upstream templates, if the CA copies need clarification)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md (NY window, dual URL naming, "General" column, MILLIONS units, finance-not-pension caution, bookend ties)
  </read_first>
  <action>
    Copy scripts/processCA.js -> scripts/processNY.js and scripts/processCARevenueAcfr.js -> scripts/processNYRevenueAcfr.js. Set STATE_NAME='New York', STATE_ABBR='NY'. Resolve the node by name+state+entity_type='state' (id 1a7f871c-7f2e-4786-9c55-5ab3409716f4 per D-01). Window FY2015-2024 (D-02).
    SOURCES map under base `https://www.osc.ny.gov/files/reports/finance/pdf/`: for FY>=2022 use `annual-comprehensive-financial-report-{YYYY}.pdf`; for FY<=2021 use `comprehensive-financial-report-{YYYY}.pdf` (the naming flips at FY2022 — encode BOTH). Use the State (finance/reports) ACFR, NOT the NYSLRS pension ACFR. date = `{FY}-03-31` (NY FYE Mar 31).
    UNITS = MILLIONS: multiply every printed figure by 1,000,000 to store dollars (D-03) — this is the extra x1000 vs the CA/FL thousands template; set the units multiplier accordingly and confirm the bookend conversions below.
    GF column = "General", 1st numeric col. Extract each FY's GF column with `pdftotext -table` (NOT -layout — D-09); statement at ~PDF p.43-44; narrow with -f/-l as light cleanup. Operating loader parses the EXPENDITURES section into EXPENDITURES{fy}; revenue loader parses the REVENUES section into REVENUE{fy}. Transcribe verbatim ACFR category names. data_source strings: `New York State ACFR — General Fund (FY${fy} actual, GAAP basis)` (operating) and `New York State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)` (revenue).
    Hard tie-check gate: each FY's extracted GF-column category sum MUST tie to the ACFR printed Total (revenue -> printed "Total revenues"; operating -> printed "Total expenditures") within $10M; validate(fy) returns false and the loader process.exit(2) on mismatch. Anchor on the recon bookend: FY2024 Total revenues $93,894M -> $93,894,000,000 (after x1,000,000). NY has negative investment-income years (D-15/ACFR-05): use the revenue template's clampForRender + signed label so they render at 0 area with the true value in the label and the root total (which already nets the negative) is preserved.
    Do NOT write to the DB in this plan: run only `node scripts/processNY.js --dry-run` and `node scripts/processNYRevenueAcfr.js --dry-run`. $0 spend — pdftotext only, no AI (D-10).
  </action>
  <verify>
    <automated>node scripts/processNY.js --dry-run && node scripts/processNYRevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both NY loaders run --dry-run with FY2015-2024 each passing the tie-check (PASS printed per FY); FY2024 revenue ties to $93,894,000,000 (millions x1,000,000 confirmed); any negative category prints "(net loss — shown at 0)"; no DB write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Build FL loaders (operating + revenue), thousands, extract + tie-check FY2022-2024</name>
  <files>scripts/processFL.js, scripts/processFLRevenueAcfr.js</files>
  <read_first>
    - scripts/processCA.js (operating loader to copy -> processFL.js — FL is the closest copy of CA: both thousands, same statement)
    - scripts/processCARevenueAcfr.js (revenue loader to copy, has P2 clamp -> processFLRevenueAcfr.js)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md (FL window, URL pattern, "General Fund" column, thousands, bookend ties)
  </read_first>
  <action>
    Copy scripts/processCA.js -> scripts/processFL.js and scripts/processCARevenueAcfr.js -> scripts/processFLRevenueAcfr.js. Set STATE_NAME='Florida', STATE_ABBR='FL'. Resolve node by name+state+entity_type='state' (id adb19ea0-de7c-4cd5-9445-cbf2108a8a1a per D-05). Window FY2022-2024 (D-05).
    SOURCES map = `https://www.myfloridacfo.com/docs-sf/default-source/transparency-docs/cafr/fye-{YYYY}-state-of-florida-annual-comprehensive-financial-report.pdf` with date `{FY}-06-30` (FL FYE Jun 30). UNITS = thousands (x1,000 -> dollars, same as CA) per D-06. GF column = "General Fund", 1st numeric col.
    Download the FY2022-2024 PDFs into _acfr-tmp/fl/ (reuse any recon-cached FY2022+FY2024). Extract each FY's "General Fund" column with `pdftotext -table` (statement ~PDF p.33-35). Operating loader -> EXPENDITURES{fy}; revenue loader -> REVENUE{fy}. Transcribe verbatim category names. data_source strings: `Florida State ACFR — General Fund (FY${fy} actual, GAAP basis)` (operating) and `Florida State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)` (revenue). FL has NO TX-style scope mismatch — it is a like-for-like GF (FY2024 $59.8B vs NASBO $51.6B is the expected GAAP-vs-budgetary spread); no relabel beyond the GAAP basis string.
    Hard tie-check gate (within $10M, exit 2 on fail) tied to printed Totals; anchor on recon bookends: FY2022 Total revenues $57,241,428K -> $57,241,428,000 and FY2024 $59,810,603K -> $59,810,603,000. P2 clamp on any negative category (ACFR-05). NO DB writes — run `--dry-run` only. $0 spend.
  </action>
  <verify>
    <automated>node scripts/processFL.js --dry-run && node scripts/processFLRevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both FL loaders run --dry-run; FY2022-2024 each passes the tie-check; FY2022 rev ties to $57,241,428,000 and FY2024 to $59,810,603,000; any negative category prints "(net loss — shown at 0)"; no DB write occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the stale-data_sources cleanup script with NY + FL (dry-run only)</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs</files>
  <read_first>
    - scripts/cleanupStaleStateGFDataSources.mjs (the Phase-99 script — already handles CA + TX; extend it)
    - .planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-CONTEXT.md (D-04 NY: ny-gf-operating + ny-gf-revenue; D-07 FL: fl-gf-operating + fl-gf-revenue — both back 0 budgets rows)
  </read_first>
  <action>
    Extend scripts/cleanupStaleStateGFDataSources.mjs (do NOT rewrite the CA/TX logic): add NY and FL to its per-state stale-`data_sources` map per D-04/D-07: for NY delete dataset_id IN ('ny-gf-operating','ny-gf-revenue'); for FL delete dataset_id IN ('fl-gf-operating','fl-gf-revenue'). The existing `--state` filter must now accept NY and FL.
    Reuse the existing SAFETY gate unchanged: before deleting each data_source, assert it backs ZERO budgets rows (SELECT count budgets WHERE data_source_id = ds.id); if a target unexpectedly backs >0 rows, REFUSE and process.exit(2). NEVER touch the `*-gf-operating-nasbo` data_sources (ny-gf-operating-nasbo / fl-gf-operating-nasbo) or any other state's rows. Idempotent: a second run finds rows already gone and reports 0 deleted, exit 0.
    Run `--dry-run` only in this plan, for NY and FL (`--state NY --dry-run`, `--state FL --dry-run`): it must print exactly which dataset_ids it WOULD delete + their 0-row assertion result. The actual deletes happen in 100-02 (NY) and 100-03 (FL). Confirm CA/TX handling is unchanged.
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --state NY --dry-run && node scripts/cleanupStaleStateGFDataSources.mjs --state FL --dry-run</automated>
  </verify>
  <done>--dry-run for NY lists ny-gf-operating + ny-gf-revenue (each 0 budgets rows); for FL lists fl-gf-operating + fl-gf-revenue (each 0 rows); neither names a *-nasbo or other-state row; no deletes performed; CA/TX paths still work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded PDF → loader | untrusted/maybe-missing remote PDF crosses into the extractor |
| loader → treasury.budgets / data_sources | this plan does NOT cross it (dry-run only); 100-02/100-03 do |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | wrong units scaling (NY millions read as thousands -> 1000x understated) | mitigate | explicit MILLIONS multiplier on NY; bookend FY2024 must tie to $93,894,000,000; tie-check exits 2 otherwise |
| T-100-02 | Information disclosure | wrong fund column / wrong page / wrong NY (pension) ACFR extracted | mitigate | per-FY tie-to-printed-total gate (<=$10M); exit 2 on mismatch; finance-not-pension URL pinned in SOURCES |
| T-100-03 | Tampering | cleanup script deleting a data_source that backs live rows | mitigate | reuse Phase-99 pre-delete 0-budgets-rows assertion; exit 2 if >0; never touch *-nasbo or other states |
| T-100-04 | Repudiation | unsourced figure shown as real | mitigate | every FY stamps data_source + source_url + source_date (per-year ACFR) in 100-02/100-03 |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | no new packages — copies existing scripts using already-vendored @supabase/supabase-js; pdftotext is a pre-existing system tool |
</threat_model>

<verification>
- `node scripts/processNY.js --dry-run` + revenue: all FY2015-2024 PASS tie-check; FY2024 rev = $93,894,000,000 (millions confirmed).
- `node scripts/processFL.js --dry-run` + revenue: all FY2022-2024 PASS; FY2022 rev = $57,241,428,000, FY2024 = $59,810,603,000.
- `node scripts/cleanupStaleStateGFDataSources.mjs --state NY --dry-run` and `--state FL --dry-run`: list the 4 stale ids, 0-row asserted, no deletes; CA/TX unchanged.
- No row written to treasury.budgets or treasury.data_sources by this plan.
</verification>

<success_criteria>
Four NY/FL loaders exist and pass `--dry-run` with hard per-FY tie-checks (anchored on recon bookends), NY millions-scaled + dual-URL-named, FL thousands like-for-like, P2 clamp wired on negatives, and the cleanup script extended with NY+FL targets gated on a 0-row assertion. Zero production DB writes. $0 spend.
</success_criteria>

<output>
Create `.planning/phases/100-new-york-florida-acfr-upgrade-acfr-03-acfr-04-acfr-05/100-01-SUMMARY.md` when done — record per-FY extracted totals + tie results for NY (with the millions conversion shown) and FL, any negative category clamped, and confirm the cleanup dry-run output for NY + FL.
</output>
