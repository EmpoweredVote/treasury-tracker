---
phase: 111-loader-debt-atomic-data-sources-upsert-load-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/process*Acfr.js (all 34 state ACFR loaders)
  - scripts/loadStateGF.mjs
  - .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESIDUE-PROOF.md
autonomous: true
requirements: [LOAD-01]
must_haves:
  truths:
    - "Every state ACFR loader (34 process*Acfr.js) and loadStateGF.mjs uses the ephemeral data_sources lifecycle: delete stale by dataset_id → insert fresh → run RPC/stamp → delete by id at end; no check-then-insert .maybeSingle() on data_sources remains in any of them"
    - "A full successful loader run — including an idempotent re-run — leaves 0 state *-gf-* data_sources rows with no manual re-clean (the WR-05 class is closed, proven by the cohort-audit residue probe before and after a live NJ FY2025 op+rev re-run)"
    - "Existing loaded data is unchanged after the re-run: NJ row count, per-FY totals, and source stamps identical; never-overwrite intact; city data_sources rows (6 CITY_PREFIXES × 2) untouched"
    - "The audit oracle scripts/verify-phase110-cohort-audit.mjs is NOT modified — the fix is at the loader template level, not the measuring stick"
  artifacts:
    - path: "scripts/processNJAcfr.js"
      provides: "Canonical instance of the fixed ephemeral lifecycle (the template future states clone)"
      contains: "delete()"
    - path: ".planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESIDUE-PROOF.md"
      provides: "Before/after residue-probe evidence + NJ idempotency snapshot proving criterion 2 and 3"
  key_links:
    - from: "scripts/process*Acfr.js data_sources block"
      to: "public.treasury_sync_budget_tree RPC"
      via: "ephemeral row created before the FY loop, passed as p_data_source_id, deleted after the loop"
      pattern: "data_sources"
    - from: "111-RESIDUE-PROOF.md"
      to: "scripts/verify-phase110-cohort-audit.mjs"
      via: "INV-2 residue probe run before and after the live re-run (exit 0 both times, no manual clean)"
      pattern: "INV-2"
---

<objective>
Close the WR-05 loader debt at the template level (LOAD-01): convert the persistent check-then-insert `data_sources` write in all 34 `process*Acfr.js` state ACFR loaders + `loadStateGF.mjs` into an ephemeral create→use→delete lifecycle, so every loader run — including idempotent re-runs — leaves 0 residue rows with no manual re-clean. Prove it live with the NJ FY2025 op+rev re-run precedent bracketed by the existing cohort-audit residue probe.

Purpose: every load in v2.14 (batches 113/114, deepening 115) runs residue-free; the recurring per-milestone manual cleanup (106: 10 rows, 110: 20 rows) ends here.
Output: 35 fixed loader scripts + `111-RESIDUE-PROOF.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESEARCH.md
@scripts/processNJAcfr.js
@scripts/loadStateGF.mjs

<interfaces>
<!-- Root cause (verified in 111-RESEARCH.md — read it first, it is definitive):
     budgets.data_source_id FKs to source_registry NOT data_sources, so a loader-created
     data_sources row can NEVER be referenced; INV-2 flags every such row as residue.
     The RPC treasury_sync_budget_tree REQUIRES the row (reads municipality_id, name,
     column_mapping, fiscal_year_start_month from it) so it cannot be simply not-written.
     sync_logs FK is ON DELETE CASCADE → deleting the row at end of run is self-contained.
     There is NO unique constraint on data_sources.dataset_id → .maybeSingle() errors on
     duplicates; delete-then-insert also fixes that fragility without a migration. -->

The replacement lifecycle, per loader (all writes stay inside the existing `if (!dryRun)` guards):

  BEFORE (defective, e.g. processNJAcfr.js:181-183):
    select id by dataset_id .maybeSingle() → update if found else insert   // persistent row = residue

  AFTER:
    await sb.from('data_sources').delete().eq('dataset_id', srcPayload.dataset_id);  // self-heal crashed-run residue
    insert srcPayload → ds  (keep existing error handling / process.exit(2))
    ... existing per-FY RPC + budgets source-stamp code UNCHANGED ...
    await sb.from('data_sources').delete().eq('id', ds.id);   // after the FY loop — leaves 0 residue

  Also REMOVE the now-pointless `update({ last_synced_at })` calls on data_sources
  (processNJAcfr.js:208-style inside the FY loop; processMIAcfr.js:141-style after it).

loadStateGF.mjs variant (lines ~1586-1603, 1627): its find is by (municipality_id, api_type,
dataset_type) and its lifecycle is PER-STATE inside the state loop — replace the find-or-create
with delete-then-insert scoped to `.eq('dataset_id', srcPayload.dataset_id)` (dataset_id is
`{abbr}-gf-operating-nasbo`, unique per state), and delete by ds.id at the end of that state's
iteration (where the last_synced_at update at line 1627 currently sits).

Known per-loader drift (clones are near- but not byte-identical): NJ-style loaders update
last_synced_at INSIDE the FY loop; MI-style once after it. Read each file's block before editing —
do not blind-regex across files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all 35 loaders to the ephemeral data_sources lifecycle</name>
  <read_first>
    - .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESEARCH.md (root cause + chosen fix — definitive)
    - scripts/processNJAcfr.js (canonical defective block: lines 178-184 create, 208 last_synced_at)
    - scripts/processMIAcfr.js (custom Sep-30 variant: lines 119-121 create, 141 last_synced_at after loop)
    - scripts/loadStateGF.mjs (NASBO variant: lines 1586-1603 find-or-create, 1627 last_synced_at; per-state loop scope)
  </read_first>
  <action>
    In each of the 34 scripts/process*Acfr.js files AND scripts/loadStateGF.mjs, replace the
    data_sources check-then-insert block with delete-then-insert (delete scoped to
    .eq('dataset_id', srcPayload.dataset_id) — never a LIKE/broader match), keep the existing
    insert error handling, leave every RPC call and budgets source-stamp line byte-identical,
    remove the data_sources last_synced_at update call(s), and add the end-of-run
    .delete().eq('id', ds.id) after the FY loop (for loadStateGF.mjs: at the end of each state's
    loop iteration). Add a one-line comment at the ephemeral-insert site stating the row is a
    transient RPC parameter vehicle deleted at end of run (WR-05 / LOAD-01) — this is the contract
    line future state clones inherit. All new statements must sit inside the existing
    `if (!dryRun)` guards. Do NOT touch: city loaders, scripts/verify-phase110-cohort-audit.mjs,
    the RPC/migrations, or any budgets-table write.
  </action>
  <verify>
    <automated>for f in scripts/process*Acfr.js scripts/loadStateGF.mjs; do node --check "$f" || exit 1; done; grep -l "maybeSingle" scripts/process*Acfr.js | wc -l | grep -qx 0 && grep -c "from('data_sources').delete()" scripts/processNJAcfr.js && node scripts/processNJAcfr.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0 for all 35 modified files
    - `grep -l "maybeSingle" scripts/process*Acfr.js` returns 0 files, and loadStateGF.mjs's data_sources find-or-create is replaced (grep "existingDs" scripts/loadStateGF.mjs returns nothing)
    - Every modified file contains exactly one start-of-run data_sources delete-by-dataset_id and one end-of-run delete-by-id, both inside dry-run guards
    - `node scripts/processNJAcfr.js --dry-run` completes with the same validation PASS output as before the change (dry-run path untouched)
    - `git diff` shows no changes to budgets writes, RPC calls, source-stamp lines, city loaders, or verify-phase110-cohort-audit.mjs
  </acceptance_criteria>
  <done>All 35 loaders compile, contain the ephemeral lifecycle with scoped deletes, and no data_sources check-then-insert remains.</done>
</task>

<task type="auto">
  <name>Task 2: Live idempotency proof with before/after residue probe → 111-RESIDUE-PROOF.md</name>
  <read_first>
    - .planning/milestones/v2.13-phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md (NJ re-run precedent + the WR-05 incident record this phase closes)
    - scripts/verify-phase110-cohort-audit.mjs (the residue oracle — run it, never edit it)
  </read_first>
  <action>
    (a) BEFORE: run `node scripts/verify-phase110-cohort-audit.mjs` (source .env first) and record
    exit code + INV-2 line (expect PASS — baseline verified clean 2026-07-02). Snapshot NJ budgets
    via read-only query: row count, per-FY totals, and data_source/source_url/source_date for all
    (New Jersey, state) rows, both datasets. Also snapshot the full *-gf-* data_sources listing
    (expect exactly the 12 city rows).
    (b) RUN the fixed loaders live against prod (the 108/110 precedent, guarded never-overwrite
    path): `node scripts/processNJAcfr.js --fy 2025` then `node scripts/processNJRevenueAcfr.js
    --fy 2025`. Expect 0 net budgets change ("Loaded 0 rows"-class output / totals unchanged) and
    each run to end having deleted its own data_sources row.
    (c) AFTER: re-run the audit script and the two snapshots WITHOUT any manual data_sources
    cleanup. Expect: exit 0, INV-2 PASS, NJ snapshot byte-identical (12 rows, same totals, same
    stamps), *-gf-* listing still exactly the 12 city rows.
    (d) WRITE 111-RESIDUE-PROOF.md: root-cause one-paragraph summary (link 111-RESEARCH.md),
    before/after probe outputs, NJ snapshot diff (expect none), the explicit statement that no
    manual re-clean was performed (contrast: 106 deleted 10 rows, 110 deleted 20), and the $0
    spend line. If ANY probe fails or drift appears, stop and fix in-phase before writing the
    proof — do not record a failing state as done.
  </action>
  <verify>
    <automated>node scripts/verify-phase110-cohort-audit.mjs; test -f .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESIDUE-PROOF.md && grep -qiE "INV-2|no manual" .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01/111-RESIDUE-PROOF.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Audit script exits 0 both before AND after the live NJ FY2025 op+rev re-runs, with zero manual data_sources deletions in between
    - NJ budgets snapshot identical before/after: same row count, per-FY totals, and source stamps (never-overwrite + no drift, criterion 3)
    - data_sources *-gf-* listing after the re-run contains exactly the 12 pre-existing city rows (0 state rows — the loaders cleaned up after themselves)
    - 111-RESIDUE-PROOF.md exists with before/after evidence, the no-manual-re-clean statement, and $0 spend
  </acceptance_criteria>
  <done>Criterion 2 and 3 proven live: idempotent re-run leaves 0 residue with no manual re-clean and 0 data drift; evidence durably recorded.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Tampering / data loss | data_sources deletes | Mitigated | Deletes scoped to exact `.eq('dataset_id', ...)` / `.eq('id', ds.id)` — never LIKE or broad filters; city rows and all other sources unreachable; sync_logs cascade is intended bookkeeping cleanup |
| T-111-02 | Data integrity | budgets table | Mitigated | No budgets write is added or changed; RPC + stamp lines byte-identical; Task 2 snapshot-diff proves 0 drift on live re-run |
| T-111-03 | Elevation | SUPABASE_SERVICE_KEY | Existing control | Key read from .env as in every prior loader run; never committed; dry-run path requires no key |
| T-111-04 | Repudiation | sync history loss (cascade) | Accepted | sync_logs for state loads are transient bookkeeping; durable provenance lives in the text-stamped budgets columns (INV-1) and LOADLOG docs |
</threat_model>

<verification>
- Quick: `for f in scripts/process*Acfr.js scripts/loadStateGF.mjs; do node --check "$f" || exit 1; done`
- Full: `node scripts/verify-phase110-cohort-audit.mjs` → exit 0 with no manual cleanup performed this phase
- Success criteria mapping: criterion 1 = Task 1 (template-level fix inherited by every v2.14 loader); criterion 2 = Task 2(a-c); criterion 3 = Task 2 snapshot diff
</verification>
