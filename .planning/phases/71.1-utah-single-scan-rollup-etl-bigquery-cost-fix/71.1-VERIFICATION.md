---
phase: 71.1-utah-single-scan-rollup-etl-bigquery-cost-fix
verified: 2026-06-20T00:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 71.1: Utah Single-Scan Rollup ETL Verification Report

**Phase Goal:** Replace the loader's per-(entity,fiscal_year,type) LIVE BigQuery query pattern (~21 TiB / ~$132 on 2026-06-19) with a single-scan `--rollup` ETL into Supabase — manual refresh, cost-gated. Requirement: UETL-01.
**Verified:** 2026-06-20T00:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | BigQuery is touched by exactly ONE scan per refresh: the `--rollup` path runs a single GROUP BY query for all 15 mapped entities x FY2014–2025 x EX/RV/PY; the per-(entity,FY,type) live-query loop is never used for bulk loads (UETL-01) | VERIFIED | `ROLLUP_QUERY` at line 161–169 is a single parameterized GROUP BY. The `--rollup` branch (lines 570–733) calls `runGuardedQuery` exactly once with `ROLLUP_QUERY`. The per-entity path at line 736+ is only reached when `isRollup` is false. Live --confirm run: 539 rows imported from one scan. |
| 2 | The rollup query projects ONLY entity_name, fiscal_year, type, fund1, org1, cat1, SUM(amount) — no PII column is ever projected; a unit test FAILS if any PII token appears in the query string or in any emitted tree (D-71-01) | VERIFIED | `ROLLUP_QUERY` (lines 161–169) projects exactly those 6 columns + SUM(amount). PII grep on the loader finds PII tokens only in JSDoc comment blocklists (lines 155–156, 256–257), never in the SELECT. Test suite: `ROLLUP_QUERY PII-exclusion guard` group (11 tests) asserts each of 11 PII tokens absent from the query string AND from trees produced from a PII-laden fixture. 81/81 tests pass. |
| 3 | For type='PY', fund1 is dropped and re-summed in memory before buildSalaryTree, so the stored salaries tree stays the 2-level Department→Wages/Benefits shape (D-71-02); EX/RV keep fund1 for the 3-level fund tree (D-69-01) | VERIFIED | `groupRollupRows` lines 218–232: PY branch iterates group rows, collapses by `${org1}\|${cat1}` key (dropping fund1), then calls `buildSalaryTree(collapsedRows)`. EX/RV branch (line 235) calls `buildTree(group.rows, TREE_OPTS)`. Tests: `PY groups produce a 2-level dept->cat tree` and `PY fund1-collapse: two fund rows for the same dept/cat are summed into one leaf` (500 = 300+200) — both pass. |
| 4 | The rollup never scans without a cost gate: dry-runs first (free), prints estimated GiB + $ + quota-fit, and only performs the real scan+writes when --confirm is passed; the real scan uses a built-in ~64 GiB rollup cap (ROLLUP_LOADER_MAX_GIB, decoupled from per-entity LOADER_MAX_GIB); the per-(entity,FY,type) path keeps its 2 GiB default cap | VERIFIED | Lines 586–615: `rollupDryRun()` always called first; preview printed; without `--confirm`, exits at line 614. With `--confirm`, WR-04 fix (lines 627–631) aborts pre-scan if `gib > ROLLUP_MAX_GIB`. Real scan at line 637 passes `ROLLUP_MAX_BYTES_BILLED`. CR-01 fix confirmed: `ROLLUP_MAX_GIB = Number(process.env.ROLLUP_LOADER_MAX_GIB) \|\| 64` (line 68) is decoupled from `MAX_BILLED_GIB = Number(process.env.LOADER_MAX_GIB) \|\| 2` (line 60). Three source-assertion tests in `cost caps are independent env vars (CR-01)` group pass. Live dry-run: 47.17 GiB, $0.2879, FITS quota. |
| 5 | Re-running is an idempotent upsert of our own Transparent Utah rows; the never-overwrite guard still SKIPS any different-source row; an (entity,FY,type) absent from the scan leaves the prior row in place (never deleted) and is logged as a coverage note (D-71.1-04) | VERIFIED | Lines 669–683: `findConflictingBudget` called per group before write; conflict → `skipped++` + continue. Lines 679–683: zero-total group → `coverageGaps++` + continue (prior row left in place). No DELETE call anywhere in the rollup path. Live --confirm result: Imported 539, Skipped 0, Coverage gaps 0, Errors 0; Provo FY2024 salaries = $92,945,952.78 (exact match to pre-71.1); 0 different-source rows touched; nothing deleted. `neverOverwriteDecision` unit tests pass. |
| 6 | FY2026 and any non-mapped entity are excluded; operating/revenue/salaries rows already loaded in Phases 69–71 are reproduced identically (idempotent), not corrupted | VERIFIED | `ROLLUP_QUERY` line 167: `AND fiscal_year BETWEEN 2014 AND 2025` (server-side exclusion of FY2026). `groupRollupRows` line 196–197: defensive in-memory guard excludes non-mapped entities (`!mappedEntityTypes.has(row.entity_name)`) and `fiscal_year > 2025`. Tests: `FY2026 rows are excluded` and `non-mapped entity rows are excluded` both pass. Live result: 0 FY2026 rows in Supabase; 0 NULL source_url; populations intact (WR-01 verified benign in REVIEW.md). |

**Score: 6/6 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/loadUtahTransparency.js` | `--rollup` mode: ROLLUP_QUERY, groupRollupRows, dry-run, --confirm gate, rollup cap | VERIFIED | All exports confirmed: `ROLLUP_QUERY` (line 161), `ROLLUP_ENTITY_MAP` (line 125), `groupRollupRows` (line 188), `rollupDryRun` (line 429), `--rollup`/`--confirm` CLI (lines 557–558), `ROLLUP_MAX_GIB` (line 68). `node --check` exits 0. |
| `scripts/loadUtahTransparency.test.mjs` | Rollup PII-exclusion guard + groupRollupRows shape tests | VERIFIED | Two new describe groups at lines 347–615: `ROLLUP_QUERY PII-exclusion guard` (11 tests) and `groupRollupRows shape` (13 tests) + `cost caps are independent env vars` (3 tests). 81/81 tests pass, 0 failing. |
| `docs/utah-bigquery-access.md` | Manual rollup-refresh runbook + cost-incident note | VERIFIED | Section "Manual rollup refresh (the ONLY bulk BigQuery access — Phase 71.1 / UETL-01)" present (lines 42–104). Contains step-by-step runbook, cost estimate, quota discipline rules, and incident background. `--rollup` mentioned at lines 84 and 90. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `loadUtahTransparency.js --rollup --confirm` | `treasury_sync_city_budget` (operating/revenue/salaries) | single ROLLUP_QUERY → groupRollupRows → supabase.rpc per (entity,fy,type) group | VERIFIED | Lines 637–644: `runGuardedQuery(ROLLUP_QUERY, …, ROLLUP_MAX_BYTES_BILLED)` → line 654: `groupRollupRows(allRows)` → lines 664–719: loop calling `supabase.rpc('treasury_sync_city_budget', …)` per group. Live run: 539 imports. |
| rollup dry-run (no --confirm) | BigQuery dryRun job statistics (totalBytesProcessed) | `createQueryJob({ dryRun: true })` → estimated GiB/$/quota-fit, zero writes | VERIFIED | `rollupDryRun()` lines 429–441: `bq.createQueryJob({ …, dryRun: true })` → reads `job.metadata.statistics.totalBytesProcessed` → returns `{ bytes, gib, estimatedUSD, fitsUnderQuota }`. Live dry-run confirmed: 47.17 GiB, $0.2879, FITS. |

---

### Data-Flow Trace (Level 4)

The rollup ETL is not a UI component — it is a CLI ETL script. Data flows from BigQuery (source) through in-memory grouping to Supabase (sink). The live --confirm run (Task 3, operator-confirmed) is the production data-flow proof: 539 rows imported, Provo FY2024 salaries = $92,945,952.78, matching pre-71.1 state.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `loadUtahTransparency.js` rollup path | `allRows` | `ROLLUP_QUERY` via `runGuardedQuery` (live BigQuery) | Yes — one GROUP BY scan, 539 aggregate rows | FLOWING |
| `groupRollupRows(allRows)` | `groups` | `allRows` in-memory partition + `buildTree`/`buildSalaryTree` | Yes — 539 groups, verified totals | FLOWING |
| `supabase.rpc('treasury_sync_city_budget', …)` | Supabase `treasury.budgets` | `groups[i].tree` + `groups[i].total` | Yes — 539 imported, 0 errors | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `node --check` passes (no syntax errors) | `node --check scripts/loadUtahTransparency.js` | Exit 0 | PASS |
| All 81 tests pass (incl. PII guard + groupRollupRows + cap independence) | `node --test scripts/loadUtahTransparency.test.mjs` | 81 pass, 0 fail, 0 cancelled | PASS |
| `ROLLUP_QUERY` contains no PII token | grep for PII column names in `ROLLUP_QUERY` constant | Found only in JSDoc comment blocklists, not in SELECT/GROUP BY | PASS |
| `ROLLUP_MAX_GIB` reads `ROLLUP_LOADER_MAX_GIB`, not `LOADER_MAX_GIB` | source inspection line 68 | `Number(process.env.ROLLUP_LOADER_MAX_GIB) \|\| 64` | PASS |
| Live dry-run: one scan ~$0.29, FITS quota | operator-run `--rollup` (Task 2, this session) | 47.17 GiB, $0.2879, fits 1 TiB/day, zero writes | PASS |
| Live --confirm: idempotent, 539 imported, 0 errors, 0 FY2026 rows | operator-run `--rollup --confirm` (Task 3, this session) | Imported 539, Skipped 0, Coverage gaps 0, Errors 0; Provo FY2024 = $92,945,952.78 | PASS |

---

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes declared or conventional for this phase. The equivalent verification was the live `--rollup` (dry-run) and `--rollup --confirm` operator runs documented in Task 2 and Task 3 of the SUMMARY.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| UETL-01 | 71.1-01-PLAN.md | Utah data is loaded into Supabase via a single-scan rollup ETL (one BigQuery query per refresh, run manually), never queried live per-(entity,FY,type). BigQuery is treated as a periodic source. Remediates the 2026-06-19 cost incident; per-entity live-query path is guardrailed off. | SATISFIED | Single `ROLLUP_QUERY` GROUP BY scan confirmed in code (line 161). Per-entity path still capped at 2 GiB (line 60) making raw-table scans trip the guardrail. Live run: one scan, 539 imports, ~$0.29, idempotent. REQUIREMENTS.md traceability table maps UETL-01 → Phase 71.1 (line 74). SUMMARY.md `requirements-completed: [UETL-01]`. |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `scripts/loadUtahTransparency.js` | 197 | `if (Number(row.fiscal_year) > 2025) continue;` — in-memory lower-bound guard only checks upper bound, not `< 2014` (WR-05 from REVIEW.md) | Info | Non-blocking: SQL WHERE `BETWEEN 2014 AND 2025` enforces both bounds at the server. The in-memory guard is belt-and-suspenders. The REVIEW.md flagged this as a warning, acknowledged it as non-blocking. No debt marker (TBD/FIXME/XXX) present. |
| `scripts/loadUtahTransparency.js` | 703–707 | `p_row_count` re-scans `allRows` with `.filter()` per group instead of using `group.rows.length` (IN-01 from REVIEW.md) | Info | Performance-only; correctness is sound. Non-blocking advisory. |

No TBD, FIXME, or XXX markers found in any file modified by this phase.

---

### Human Verification Required

None. All observable truths were verifiable from code inspection and the operator-confirmed live run results documented in the SUMMARY.md (Task 2 dry-run: 47.17 GiB, $0.2879; Task 3 confirm: 539 imported, 0 errors, idempotency verified against pre-71.1 Supabase state).

The Task 3 checkpoint gate was resolved this session with operator sign-off ("approved" — documented in SUMMARY.md). No items remain pending human testing.

---

### Gaps Summary

No gaps. All 6 must-haves are VERIFIED in the codebase. UETL-01 is SATISFIED.

The two open items from the code review (WR-05 one-sided FY lower-bound guard; IN-01 row_count O(n) recount) are advisory-only warnings that do not affect correctness or the phase goal. Neither is a debt marker requiring a follow-up issue reference. Both were explicitly acknowledged as non-blocking in the REVIEW.md.

---

_Verified: 2026-06-20T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
