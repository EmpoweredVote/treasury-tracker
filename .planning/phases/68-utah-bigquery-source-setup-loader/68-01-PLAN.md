---
phase: 68-utah-bigquery-source-setup-loader
plan: "68-01"
type: execute
wave: 1
depends_on: []
files_modified: ["docs/utah-bigquery-access.md"]
autonomous: false
requirements: [UTSRC-01]
must_haves:
  truths:
    - "An access-request email was sent to alexnielson@utah.gov with EV's org + the Google account(s) to grant (chris@empowered.vote; personal Gmail noted as fallback per D-17) — recorded with date (D-15, D-16)"
    - "After Utah grants access, an authenticated query against ut-sao-transparency-prod.transaction.transaction returns rows (NOT Access Denied) from project empowered-vote-486302 at $0 (BQ free tier) (D-16, D-18)"
    - "The working access recipe (gcloud SDK PATH, project, ADC, test query) is documented for reuse by 68-02/68-03 and downstream Utah phases"
  artifacts:
    - path: ".planning/phases/68-utah-bigquery-source-setup-loader/68-01-SUMMARY.md"
      provides: "The access-establishment record: email sent, grant confirmation, working test query + recipe — the UTSRC-01 access evidence"
      contains: "email recipient + accounts + org + date, Utah grant confirmation + date, the verified test query output, the reusable access recipe, any account-type/billing notes"
  key_links:
    - from: "Utah State Auditor (alexnielson@utah.gov) access grant"
      to: "chris@empowered.vote query access to ut-sao-transparency-prod.transaction.transaction"
      via: "email request (org + Google accounts) → Utah IAM grant → verify with SELECT DISTINCT entity_name"
      pattern: "request → wait for human grant → verify live query returns rows → document recipe"
---

<objective>
Satisfy the access half of UTSRC-01: get EV granted read access to the Transparent Utah BigQuery table.
Verified live on 2026-06-17 that the table is access-by-request (Access Denied without a grant). This plan
sends the request to alexnielson@utah.gov, waits for the human grant, verifies an authenticated query
returns rows from project empowered-vote-486302 at $0, and documents the reusable access recipe. This plan
is GATED on Utah's response (lead time unknown) — it is the milestone's critical external dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-RESEARCH.md
@.planning/research/UTAH-RECON.md

<interfaces>
<!-- Environment verified 2026-06-17:
  gcloud SDK: C:\Users\Chris\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin  (NOT on PATH in pre-existing
    shells — prepend it: $env:PATH = "<bin>;$env:PATH"  before calling bq/gcloud)
  Authed: chris@empowered.vote | active project: empowered-vote-486302 | bigquery.googleapis.com enabled | ADC quota project set
  Table: ut-sao-transparency-prod.transaction.transaction  (currently Access Denied — awaiting grant)
  Verify query (run AFTER grant):
    bq query --project_id=empowered-vote-486302 --use_legacy_sql=false \
      'SELECT DISTINCT entity_name FROM `ut-sao-transparency-prod.transaction.transaction` WHERE LOWER(entity_name) LIKE "%provo%" LIMIT 20'
  Access process (old.transparent.utah.gov/how_to_become_superuser.php): email alexnielson@utah.gov with org + Google account(s). -->
</interfaces>
</context>

<tasks>

<task type="manual">
  <name>Task 1: Send the Transparent Utah BigQuery access request</name>
  <files>docs/utah-bigquery-access.md</files>
  <read_first>
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md (D-15..D-18 — access process, account + billing nuances)
    - .planning/research/UTAH-RECON.md (data source + license)
  </read_first>
  <action>
    Send an email (from chris@empowered.vote) to alexnielson@utah.gov requesting read access to the
    Transparent Utah BigQuery transaction dataset. Include: organization = Empowered Vote (nonprofit);
    purpose = free public financial-transparency tool sourcing Utah city/county budgets to Transparent Utah;
    Google account(s) to grant = chris@empowered.vote (note a personal Gmail is available as fallback if a
    Workspace account cannot be granted — D-17); GCP project already set up = empowered-vote-486302.
    Create docs/utah-bigquery-access.md recording: the recipient, the accounts + org submitted, the purpose,
    and the date sent. (This task is human-driven — Chris sends the email; the executor records it.)
  </action>
  <acceptance_criteria>
    - docs/utah-bigquery-access.md exists and records: recipient alexnielson@utah.gov, the Google account(s) submitted, org = Empowered Vote, project empowered-vote-486302, and the date the request was sent.
  </acceptance_criteria>
  <done>The access request is sent and recorded — the external dependency is in motion.</done>
</task>

<task type="manual">
  <name>Task 2: Verify access once Utah grants it</name>
  <files>docs/utah-bigquery-access.md</files>
  <read_first>
    - docs/utah-bigquery-access.md (the request record from Task 1)
  </read_first>
  <action>
    After Utah confirms the grant, verify it. In a terminal, prepend the SDK bin to PATH
    (`$env:PATH = "C:\Users\Chris\AppData\Local\Google\Cloud SDK\google-cloud-sdk\bin;$env:PATH"`), then run
    the verify query against project empowered-vote-486302:
      bq query --project_id=empowered-vote-486302 --use_legacy_sql=false 'SELECT DISTINCT entity_name FROM `ut-sao-transparency-prod.transaction.transaction` WHERE LOWER(entity_name) LIKE "%provo%" LIMIT 20'
    Confirm it returns rows (entity names) rather than "Access Denied". Run a `--dry_run` first to confirm the
    bytes scanned are small (well under the 1 TB/month free tier → $0). If Utah only granted a personal Gmail
    (D-17), re-auth as that account (`gcloud auth login`) and use its project. Record the grant date and the
    successful query output in docs/utah-bigquery-access.md.
  </action>
  <acceptance_criteria>
    - The verify query returns at least one entity_name row (no Access Denied) from project empowered-vote-486302.
    - A dry-run confirms bytes scanned are within the free tier ($0).
    - docs/utah-bigquery-access.md records the grant date, the granted account, and the working query output.
  </acceptance_criteria>
  <done>EV has verified read access to the Utah table at $0 — UTSRC-01 access portion satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Document the reusable access recipe + write 68-01-SUMMARY</name>
  <files>docs/utah-bigquery-access.md, .planning/phases/68-utah-bigquery-source-setup-loader/68-01-SUMMARY.md</files>
  <read_first>
    - docs/utah-bigquery-access.md (request + grant records)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Finalize docs/utah-bigquery-access.md as the reusable access runbook: the gcloud SDK path + PATH-prepend
    note, the active project (empowered-vote-486302), ADC setup, the granted account, the verified test query,
    and any account-type/billing notes that came up. Then write 68-01-SUMMARY.md recording the request, the
    grant, the verified query, and the recipe. State the UTSRC-01 access portion is satisfied (entity-name
    mapping itself is completed in 68-03).
  </action>
  <acceptance_criteria>
    - docs/utah-bigquery-access.md contains the full reusable recipe (PATH, project, ADC, granted account, verified query).
    - 68-01-SUMMARY.md documents email → grant → verified query → recipe.
  </acceptance_criteria>
  <done>The access path is established and documented for reuse by 68-02/68-03 and downstream Utah phases.</done>
</task>

</tasks>

<verification>
- Access request sent + recorded (Task 1).
- Authenticated query returns rows from empowered-vote-486302, $0 (Task 2).
- Reusable access recipe + summary documented (Task 3).
</verification>

<success_criteria>
- UTSRC-01 (access portion) satisfied: EV has verified read access to ut-sao-transparency-prod.transaction.transaction at $0.
- The access recipe is documented for reuse.
- GATED on Utah's human grant — this plan cannot complete until access is granted; 68-02 (loader build) proceeds in parallel meanwhile.
</success_criteria>

<output>
Create `.planning/phases/68-utah-bigquery-source-setup-loader/68-01-SUMMARY.md` when done.
</output>
