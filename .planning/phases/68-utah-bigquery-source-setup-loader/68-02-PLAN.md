---
phase: 68-utah-bigquery-source-setup-loader
plan: "68-02"
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/loadUtahTransparency.js", "scripts/loadUtahTransparency.test.mjs", "package.json"]
autonomous: true
requirements: [UTSRC-02]
must_haves:
  truths:
    - "scripts/loadUtahTransparency.js mirrors scripts/bulkLoadStateController.js — same tree shape ({n,a,c}/{n,a,i}), same treasury_sync_city_budget RPC params, same never-overwrite guard (findConflictingBudget) — swapping only the data-fetch layer for a parameterized BigQuery query (column projection + entity/FY/type filters) (D-07, D-08, never-overwrite carry-forward)"
    - "The category tree is function/purpose-first via a configurable source column (default function1, fallback cat1/org1) and the dataset_type is derived from BQ type (EX→operating, RV→revenue; PY out of scope this phase) (D-05, D-06)"
    - "Source attribution writes data_source_name='Transparent Utah', a per-entity source_url placeholder (confirmed in 68-03), source_date computed once per run; CC BY 4.0 (D-08, D-09)"
    - "scripts/loadUtahTransparency.test.mjs unit-tests the tree builder + amt parsing + never-overwrite decision against fixture rows and exits 0 with NO BigQuery access (offline-buildable per the access blocker D-15)"
  artifacts:
    - path: ".planning/phases/68-utah-bigquery-source-setup-loader/68-02-SUMMARY.md"
      provides: "The loader-build record: structure mirrored from the analog, the BQ query shape, the tree mapping, the test results — the UTSRC-02 build evidence"
      contains: "the analog parity checklist, BQ query + free-tier note, tree-column config, RPC param mapping, never-overwrite behavior, test command + pass output, what remains gated on access (live dry-run in 68-03)"
  key_links:
    - from: "BigQuery ut-sao-transparency-prod.transaction.transaction rows (fixture-shaped this phase)"
      to: "treasury_sync_city_budget RPC params (dry-run printed, not written)"
      via: "scripts/loadUtahTransparency.js tree builder (function/purpose-first) + never-overwrite guard"
      pattern: "mirror bulkLoadStateController.js → swap fetch for BQ query → unit-test tree builder on fixtures (offline) → live dry-run deferred to 68-03"
---

<objective>
Satisfy the build half of UTSRC-02: write scripts/loadUtahTransparency.js by mirroring the proven
scripts/bulkLoadStateController.js — same tree shape, same treasury_sync_city_budget RPC, same never-overwrite
guard — swapping only the Socrata HTTP fetch for a parameterized BigQuery query. This is buildable NOW with no
BigQuery access (gated parts are deferred to 68-03): build against the documented schema and unit-test the
tree builder against fixture rows. $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-RESEARCH.md
@.planning/research/UTAH-RECON.md
@scripts/bulkLoadStateController.js
@scripts/loadCASalaries.test.mjs

<interfaces>
<!-- ANALOG (copy structure): scripts/bulkLoadStateController.js
  - amt(v): parse "$1,234"/"(123)" → number
  - findCityMunicipality / treasury_ensure_municipality
  - findConflictingBudget(muniId, fy, datasetType, sourceName): pre-skip rows whose data_source != this run (NEVER overwrite)
  - tree: category -> subcategory_1 -> line items; compact JSON {n,a,c} parents / {n,a,i} items; children sorted desc; zero rows skipped
  - RPC treasury_sync_city_budget({p_municipality_id,p_fiscal_year,p_dataset_type,p_total,p_tree,p_row_count,p_data_source_name,p_source_url,p_source_date})
  - source_date computed ONCE per run (--source-date override), durable PAGE url (not API endpoint)
  - flags: --county/--city/--fy/--type/--source-date/--dry-run/--list-cities ; dry-run collision pre-pass classifies before any write

  UTAH BQ schema (documented, UTAH-RECON.md): columns entity_name, entity_id, amount, fiscal_year,
    fund1-4, org1-10, cat1-7, program1-7, function1-7, type ('EX'|'RV'|'PY'), govt_lvl
  Query shape (project only needed cols; filter to keep bytes tiny for the 1 TB/mo free tier):
    SELECT function1, cat1, org1, amount, fiscal_year FROM `ut-sao-transparency-prod.transaction.transaction`
    WHERE entity_name=@entity AND fiscal_year=@fy AND type=@type
  Type→dataset: EX→'operating', RV→'revenue' (PY→'salaries' deferred to Phase 71)
  Fetch lib: @google-cloud/bigquery (ADC) — add to package.json; OR shell `bq query --format=json` (planner's choice, D-04)
  THIS PHASE = build + offline unit tests only; live query + dry-run happen in 68-03 (gated on access). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold loadUtahTransparency.js mirroring the analog (fetch layer = BigQuery)</name>
  <files>scripts/loadUtahTransparency.js, package.json</files>
  <read_first>
    - scripts/bulkLoadStateController.js (the analog — copy structure wholesale)
    - .planning/research/UTAH-RECON.md (BQ schema + type codes)
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md (D-04..D-09)
  </read_first>
  <action>
    Create scripts/loadUtahTransparency.js by copying the shape of bulkLoadStateController.js: the Supabase
    client bootstrap (SUPABASE_URL/SUPABASE_SERVICE_KEY), amt() parser, treasury_ensure_municipality +
    findCityMunicipality, findConflictingBudget never-overwrite guard, and the treasury_sync_city_budget RPC
    call with identical params. Replace fetchAllPages() with a BigQuery fetch function that runs a parameterized
    query (`@google-cloud/bigquery` client using ADC; add the dependency to package.json) selecting ONLY the
    needed columns (function1, cat1, org1, amount, fiscal_year) filtered by entity_name/@fy/@type. Map type
    EX→'operating', RV→'revenue'. Add flags: --entity (exact BQ entity_name), --fy (multiple), --type,
    --source-date, --dry-run, --source-column (default 'function1'). source_date computed once per run.
    data_source_name='Transparent Utah'; source_url = a per-entity placeholder constant to be finalized in 68-03.
    Do NOT execute any live query in this plan (no access yet) — the query function exists but is exercised only
    by fixtures/dry-run in tests.
  </action>
  <acceptance_criteria>
    - scripts/loadUtahTransparency.js exists; `node -c scripts/loadUtahTransparency.js` (or node --check) parses with no syntax error.
    - It contains: a findConflictingBudget never-overwrite guard, a treasury_sync_city_budget RPC call with the same 9 params as bulkLoadStateController.js, a parameterized BigQuery query referencing `ut-sao-transparency-prod.transaction.transaction` with column projection, and a --dry-run path that prints (does not write).
    - package.json lists @google-cloud/bigquery in dependencies.
  </acceptance_criteria>
  <done>The Utah loader is scaffolded mirroring the analog, fetch layer swapped to BigQuery — buildable without access.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the function/purpose-first tree builder (configurable source column)</name>
  <files>scripts/loadUtahTransparency.js</files>
  <read_first>
    - scripts/bulkLoadStateController.js (tree-building block lines ~122-156)
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Implement the tree builder: top level = the configured functional column (default function1; fallback to
    cat1 or org1 via --source-column), second level = a sub-category column (e.g. cat1 when function1 is the
    top, else org1), items = line rows. Build the same compact JSON shape as the analog ({n, a, c} parents with
    {n, a, i} items), sort children by amount descending, skip zero-amount rows, sum totals bottom-up. Keep the
    chosen source column a single configurable constant so switching function1↔cat1 after the 68-03 data
    inspection is a one-line change. No reflexive deep icicle — cap at 2-3 levels.
  </action>
  <acceptance_criteria>
    - The tree builder groups by the configurable source column and produces {n,a,c}/{n,a,i} nodes with children sorted by amount desc and zero rows skipped.
    - Parent/category totals equal the sum of their children (verified by Task 3 tests).
    - Tree depth is 2-3 levels (no deeper).
  </acceptance_criteria>
  <done>The loader builds a function/purpose-first tree matching the CA/Federal approach, with a one-line column switch for post-access tuning.</done>
</task>

<task type="auto">
  <name>Task 3: Unit-test the tree builder + parsing + never-overwrite against fixtures (offline, exit 0)</name>
  <files>scripts/loadUtahTransparency.test.mjs</files>
  <read_first>
    - scripts/loadCASalaries.test.mjs (test style precedent)
    - scripts/loadUtahTransparency.js (export the pure functions to test)
  </read_first>
  <action>
    Refactor the pure logic (tree builder, amt parser, type→dataset mapper, never-overwrite decision) into
    exported functions so they can be imported without running the loader or touching BigQuery/Supabase. Write
    scripts/loadUtahTransparency.test.mjs with fixture BQ rows (a small array shaped like the documented schema:
    entity_name, function1, cat1, amount, fiscal_year, type) asserting: (a) tree totals equal sum of children;
    (b) zero/negative/offset amounts handled correctly; (c) EX→operating, RV→revenue mapping; (d) the
    never-overwrite decision returns "skip" when an existing row's data_source differs and "refresh" when it
    matches 'Transparent Utah'. The test MUST pass with NO network/BigQuery/Supabase access. Run it.
  </action>
  <verify>
    <automated>node scripts/loadUtahTransparency.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/loadUtahTransparency.test.mjs runs offline (no BigQuery/Supabase) and exits 0.
    - Tests cover: tree total = sum of children, amount parsing (incl. negatives/offsets), EX/RV→dataset mapping, and never-overwrite skip-vs-refresh decision.
  </acceptance_criteria>
  <done>The loader's pure logic is proven by offline unit tests — the build half of UTSRC-02 is verifiable without access.</done>
</task>

<task type="auto">
  <name>Task 4: Write 68-02-SUMMARY</name>
  <files>.planning/phases/68-utah-bigquery-source-setup-loader/68-02-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 68-02-SUMMARY.md: the analog-parity checklist (what was copied vs swapped), the BQ query shape +
    free-tier note, the tree-column config, the RPC param mapping, the never-overwrite behavior, the test
    command + pass output, and an explicit list of what remains GATED on access (the live pilot dry-run + the
    source_url finalization + the function1-vs-cat1 decision — all in 68-03). State the UTSRC-02 build portion
    is satisfied.
  </action>
  <acceptance_criteria>
    - 68-02-SUMMARY.md documents the build, the passing offline test, and the access-gated remainder handed to 68-03.
  </acceptance_criteria>
  <done>The loader build is documented; UTSRC-02 build portion satisfied; live proof handed to 68-03.</done>
</task>

</tasks>

<verification>
- loadUtahTransparency.js mirrors the analog (never-overwrite guard + treasury_sync_city_budget RPC + BQ fetch) (Task 1).
- Function/purpose-first tree builder with configurable source column (Task 2).
- Offline unit tests pass (exit 0) with no BigQuery access (Task 3).
- Build documented; access-gated remainder handed to 68-03 (Task 4).
</verification>

<success_criteria>
- UTSRC-02 (build portion) satisfied: a reusable BigQuery loader mirroring the proven analog, with a unit-tested function/purpose-first tree builder and never-overwrite guard, built and tested WITHOUT BigQuery access.
- $0; no DB writes (dry-run/fixtures only).
- Live pilot dry-run + source_url finalization deferred to 68-03 (gated on access).
</success_criteria>

<output>
Create `.planning/phases/68-utah-bigquery-source-setup-loader/68-02-SUMMARY.md` when done.
</output>
