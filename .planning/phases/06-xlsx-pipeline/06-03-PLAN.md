---
phase: 06-xlsx-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - .planning/phases/06-xlsx-pipeline/06-03-LOAD-LOG.md
autonomous: false

must_haves:
  truths:
    - "Every non-deferred XLSX data_sources row has been loaded at least once via bulkLoadXLSX.js, and treasury.transactions contains the expected rows for that (data_source_id, fiscal_year) pair"
    - "McKinney check register data is visible in the app for all loaded fiscal years (FY22-FY25) under the McKinney municipality view"
    - "McKinney payroll data is visible in the app under dataset_type='salaries' for all loaded fiscal years"
    - "Frisco check register data is visible in the app for all loaded fiscal years (FY18-FY26 minus any that fail to load)"
    - "Plano check register data is visible OR Plano is documented as deferred in 06-03-LOAD-LOG.md (matching the 06-02 Task 2 decision)"
    - "Re-running bulkLoadXLSX.js for any single source produces 0 inserted (all skipped) — proves SHA-256 row-hash idempotency end-to-end"
    - "Running bulkLoadXLSX.js with --force-reload for a single source clears and re-inserts that source's rows for that fiscal_year ONLY (other fiscal years untouched)"
  artifacts:
    - path: ".planning/phases/06-xlsx-pipeline/06-03-LOAD-LOG.md"
      provides: "Per-source load summary (inserted/skipped/errors counts per fiscal year), idempotency test results, force-reload test results, app visibility verification"
      min_lines: 60
    - path: "treasury.transactions table"
      provides: "Loaded XLSX rows with non-null source_row_id (SHA-256 hashes)"
      contains: "data_source_id IN (xlsx_download row ids)"
  key_links:
    - from: "bulkLoadXLSX.js execution"
      to: "treasury.transactions rows"
      via: "treasury_sync_transactions RPC with rid=hashRow(row)"
      pattern: "treasury_sync_transactions"
    - from: "frontend app at financials.empowered.vote (or local dev server)"
      to: "loaded data via Supabase client"
      via: "existing app data-fetch path; municipality+fiscal_year filter"
      pattern: "supabase.*transactions"
    - from: "second run of bulkLoadXLSX.js"
      to: "skip-on-conflict via source_row_id"
      via: "RPC returns rows_skipped > 0, rows_inserted = 0"
      pattern: "rows_skipped"
---

<objective>
Execute the live load: run `bulkLoadXLSX.js` against every seeded `xlsx_download` data source, verify the data appears correctly in the Treasury Tracker app, and prove idempotency end-to-end. This plan is the verification gate for Phase 6 — it is the moment the phase's success criteria become observable.

Purpose: Satisfy XLSX-02 (Plano transactions visible — if not deferred), XLSX-03 (McKinney check register visible), XLSX-04 (McKinney payroll visible under salaries dataset type), XLSX-05 (Frisco check register visible), and XLSX-07 (re-run does not duplicate rows). Produce a load log capturing per-source counts, idempotency results, and app verification.

Output:
- `.planning/phases/06-xlsx-pipeline/06-03-LOAD-LOG.md` — per-source load summary (inserted/skipped/errors), idempotency test, force-reload test, app verification screenshots/notes.
- A populated `treasury.transactions` table containing all XLSX-sourced rows.
- Visible data in the app for Plano (or deferred), McKinney (transactions + salaries), and Frisco.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-xlsx-pipeline/06-CONTEXT.md
@.planning/phases/06-xlsx-pipeline/06-01-SUMMARY.md
@.planning/phases/06-xlsx-pipeline/06-02-SUMMARY.md
@.planning/phases/06-xlsx-pipeline/06-02-NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live load all seeded XLSX sources and capture per-source counts</name>
  <files>.planning/phases/06-xlsx-pipeline/06-03-LOAD-LOG.md</files>
  <action>
1. **List sources first:**
   `node scripts/bulkLoadXLSX.js --list`
   Confirm count matches what 06-02-SUMMARY.md says was seeded. If mismatch, stop and reconcile (likely missing env vars or a seeder gap).

2. **Dry-run pass for sanity** (catches obvious column-mapping errors before any DB write):
   For each source, optionally run `node scripts/bulkLoadXLSX.js --dry-run --source "<name>"` and confirm the parsed-row count is non-zero and parse errors stay below the 5% threshold. If any source dry-run aborts due to >5% error rate, stop and fix the column_mapping in the seeder (re-run 06-02 Task 3 with corrected mapping). DO NOT proceed to live load until all sources dry-run cleanly.

3. **Live load each source:**
   - Plano (if not deferred): `node scripts/bulkLoadXLSX.js --source "Plano"`
   - McKinney check register (4 FYs): `node scripts/bulkLoadXLSX.js --source "McKinney Check"` (or per-FY if loader matches multiple)
   - McKinney payroll (4 FYs): `node scripts/bulkLoadXLSX.js --source "McKinney Payroll"`
   - Frisco check register (9 FYs): `node scripts/bulkLoadXLSX.js --source "Frisco"`

   Capture stdout for each. The final summary line per FY follows the CONTEXT format:
   `<source_name> FY<year>: <N> inserted | <N> skipped | <N> errors`

4. **Build `06-03-LOAD-LOG.md`** with this structure:
```markdown
# Phase 6 Plan 03 — Load Log

## Source Counts (First Run)

| Source | Fiscal Year | Inserted | Skipped | Errors | Status |
|--------|-------------|----------|---------|--------|--------|
| Plano Check Register FY2025 | 2025 | ... | ... | ... | OK / DEFERRED |
| McKinney Check Register FY2022 | 2022 | ... | ... | ... | OK |
| McKinney Check Register FY2023 | 2023 | ... | ... | ... | OK |
... (one row per fiscal-year load)

## Total Rows Loaded by City
- Plano: <total> rows
- McKinney check register: <total> rows
- McKinney payroll: <total> rows
- Frisco check register: <total> rows

## Failed Loads (if any)
- <source>: <reason and stderr excerpt>
```

If any source fails, capture stderr in the log and decide: (a) fix and re-run for transient errors (network), (b) document and defer for systemic errors (bad URL, malformed XLSX). Do NOT delete or rewrite history — just append a "Retry / Resolution" subsection.
  </action>
  <verify>
- 06-03-LOAD-LOG.md exists with the source-counts table populated for every seeded source.
- For every non-deferred source, "Inserted" column is > 0 and "Errors" is < 5% of total rows attempted.
- `treasury_list_source_ids` filtered to api_type='xlsx_download' shows row counts via a follow-up RPC or SQL query (e.g., `select data_source_id, count(*) from treasury.transactions where data_source_id in (...) group by 1`).
- Stdout matches the exact summary-line format from CONTEXT (`<name> FY<year>: <N> inserted | <N> skipped | <N> errors`).
  </verify>
  <done>Every non-deferred xlsx_download data_sources row has been loaded at least once; 06-03-LOAD-LOG.md captures per-(source, FY) counts; total rows loaded recorded per city.</done>
</task>

<task type="auto">
  <name>Task 2: Verify idempotency (re-run) and force-reload behavior</name>
  <files>.planning/phases/06-xlsx-pipeline/06-03-LOAD-LOG.md</files>
  <action>
Append two new sections to `06-03-LOAD-LOG.md`:

**Section A: Idempotency test (re-run with no flags)**
- Pick one source per city (e.g., Frisco FY2025, McKinney Check FY2025, McKinney Payroll FY2025, Plano if loaded).
- For each, re-run: `node scripts/bulkLoadXLSX.js --source "<exact name>"`
- Capture stdout. Expected: `<N> inserted | <M> skipped | 0 errors` where `N == 0` and `M == row count from first load`.
- If `N > 0`: idempotency is BROKEN. Investigate immediately — likely causes: (a) hashRow non-determinism (keys not sorted, Date objects not normalized), (b) RPC not honoring `rid` field on conflict. Fix at the source (likely 06-01 loader code), re-run from scratch, do NOT mark phase complete until clean.

Append to LOAD-LOG:
```markdown
## Idempotency Test (Re-run No Flags)

| Source | First-Run Inserted | Re-Run Inserted | Re-Run Skipped | Pass? |
|--------|-------------------|-----------------|----------------|-------|
| Frisco Check Register FY2025 | 12,048 | 0 | 12,048 | ✅ |
| McKinney Check Register FY2025 | ... | 0 | ... | ✅ |
... (all checked sources)
```

**Section B: --force-reload test (single source)**
- Pick ONE source (Frisco FY2025 is fine).
- Capture row count before: `select count(*) from treasury.transactions where data_source_id = '<frisco-fy25-id>' and fiscal_year = 2025;`
- Run: `node scripts/bulkLoadXLSX.js --source "Frisco Check Register FY2025" --force-reload`
- Capture stdout. Expected: log line `--force-reload: cleared existing rows for FY2025` followed by `<N> inserted | 0 skipped | 0 errors` where N == row count from before.
- Capture row count after: should equal pre-test count.
- Verify a DIFFERENT fiscal year was NOT touched: confirm row count for Frisco FY2024 is unchanged before/after the FY2025 force-reload.

Append to LOAD-LOG:
```markdown
## --force-reload Test (Single FY Scoped)

- Target: Frisco Check Register FY2025
- Pre-test rows (FY2025): <N>
- Post-force-reload rows (FY2025): <N> (should match pre-test)
- Frisco FY2024 rows pre-test: <X>
- Frisco FY2024 rows post-test: <X> (should be unchanged — proves scope is per-FY)
- Pass? ✅ / ❌
```

If any test fails, root-cause it in the loader code (likely 06-01) and re-test until passing. Do NOT proceed to Task 3 with broken idempotency.
  </action>
  <verify>
- 06-03-LOAD-LOG.md has "Idempotency Test" section with all checked sources reporting `Re-Run Inserted = 0`.
- 06-03-LOAD-LOG.md has "--force-reload Test" section showing the target FY's row count was preserved (cleared then re-inserted equally) AND a different FY of the same source was untouched.
- Both tests report PASS.
  </verify>
  <done>Idempotency proven on re-run (0 new inserts), force-reload proven scoped to single fiscal year (other FYs unaffected). Both results documented in LOAD-LOG.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies XLSX data is visible and correct in the app</name>
  <what-built>
Phase 6 has loaded check register and payroll data for McKinney, Frisco, and (if not deferred) Plano into treasury.transactions. The Treasury Tracker app at https://treasurytracker.empowered.vote should now display this data when a user navigates to each city's view. McKinney specifically should show both transaction (check register) and salary (payroll) datasets.
  </what-built>
  <how-to-verify>
1. **Open the app:** Visit https://treasurytracker.empowered.vote (production) OR run `npm run dev` locally and visit http://localhost:5173.

2. **Verify McKinney check register:**
   - Navigate to McKinney's municipal view (city selector or URL parameter — match existing app convention).
   - Open the transactions/check-register dataset.
   - Confirm: at least one fiscal year (FY22-FY25) shows non-empty data with payee names, dates, amounts, and totals.
   - Spot-check one fiscal year (e.g., FY2025): does the transaction count roughly match the LOAD-LOG count?

3. **Verify McKinney payroll (salaries dataset type):**
   - Same McKinney view, switch to the salaries/payroll dataset.
   - Confirm: at least one fiscal year shows employee names, gross-pay amounts, and totals.
   - Confirm the dataset displays distinctly from check-register (different totals, different rows).

4. **Verify Frisco check register:**
   - Navigate to Frisco's municipal view.
   - Confirm: multiple fiscal years (FY18-FY26 minus any failed loads) appear in the year selector with non-empty data.
   - Spot-check FY2025: payee names + amounts visible.

5. **Verify Plano (if not deferred):**
   - Navigate to Plano's view, confirm transactions visible. If Plano was deferred per the 06-02 Task 2 decision, confirm the LOAD-LOG documents this clearly and skip this step.

6. **Spot-check totals plausibility:** McKinney FY2025 check register total should be in the millions range (typical municipal check register). McKinney payroll FY2025 should also be in the millions. Frisco FY2025 similar. If any city's total looks wildly off (e.g., $0 or $50 trillion), the column_mapping is likely wrong — flag this as an issue.

7. **Append app-verification section to LOAD-LOG:**
   - Add a "## App Verification" section noting: cities verified visible, fiscal years verified, totals plausibility check, any issues observed.
   - Optionally include screenshots saved to `screenshots/06-03-<city>.png`.

Type "approved" if all four cities (or three + deferred Plano) display correctly with plausible data. Otherwise describe the specific issue (which city, which dataset, what's wrong) so it can be fixed before phase close.
  </how-to-verify>
  <resume-signal>Type "approved" if all data is visible and totals are plausible, OR describe the specific issue (city, dataset, observed problem) to trigger a fix.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **All seeded sources loaded:**
   - 06-03-LOAD-LOG.md "Source Counts" table covers every xlsx_download row from 06-02.
   - Total inserted rows per city are documented.

2. **Idempotency end-to-end:**
   - "Idempotency Test" section shows re-run inserts = 0 for sampled sources.
   - "--force-reload Test" section shows scoped behavior (target FY cleared+re-inserted, other FYs untouched).

3. **App displays the data:**
   - Human-verify checkpoint passed with explicit "approved" OR documented deferrals.
   - "App Verification" section appended to LOAD-LOG with cities + fiscal years confirmed visible.

4. **No silent failures:**
   - Any source that failed to load is documented with reason in LOAD-LOG, not silently omitted.
</verification>

<success_criteria>
- Every non-deferred xlsx_download data_sources row has been loaded; treasury.transactions contains rows with non-null source_row_id (SHA-256) for each (data_source_id, fiscal_year) pair.
- Re-running bulkLoadXLSX.js without --force-reload produces zero new inserts (idempotency proven).
- --force-reload is scoped to a single (data_source_id, fiscal_year) pair (other FYs of the same source are not touched).
- McKinney check register, McKinney payroll, and Frisco check register data are visible in the app for at least one fiscal year each (verified by user via human-verify checkpoint).
- Plano either visible or explicitly deferred per the 06-02 Task 2 decision.
- 06-03-LOAD-LOG.md captures: per-source counts, idempotency test, force-reload test, app verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xlsx-pipeline/06-03-SUMMARY.md` with: tech-stack delta (none), patterns confirmed (XLSX SHA-256 dedup, --force-reload single-FY scope), key files (06-03-LOAD-LOG.md), DB state (total rows loaded per city), any deferred items (Plano if applicable), and frontmatter linking subsystem='loaders' with `phase_complete: true`.

Then update `.planning/ROADMAP.md` Phase 6 progress: mark all three plans `[x]` complete and update Phase 6 status to "Complete — <date>".
</output>
