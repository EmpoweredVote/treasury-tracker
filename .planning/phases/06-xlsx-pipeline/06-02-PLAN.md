---
phase: 06-xlsx-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedXLSXDataSources.js
  - .planning/phases/06-xlsx-pipeline/06-02-NOTES.md
autonomous: false

must_haves:
  truths:
    - "treasury.data_sources contains rows for: Plano (1 row per available fiscal year, dataset_type='transactions'), McKinney check register (FY22-FY25, dataset_type='transactions'), McKinney payroll register (FY22-FY25, dataset_type='salaries'), Frisco check register (FY18-FY26, dataset_type='transactions') — OR a documented decision in 06-02-NOTES.md if any city's URLs cannot be confirmed"
    - "Every seeded XLSX row has api_type='xlsx_download', a populated download_url, a single fiscal_year integer (NOT an array), a populated column_mapping JSON, and a municipality_id resolved by querying treasury.municipalities by name"
    - "Plano: a static download URL has been investigated; either (a) a working static URL is seeded with column_mapping confirmed against a downloaded sample, or (b) Plano is documented as deferred in 06-02-NOTES.md with the reason"
    - "Each city's column_mapping JSON keys (date_column, amount_column, vendor_column, etc.) reference actual header names that were observed by downloading and inspecting at least one sample XLSX from that city"
    - "scripts/seedXLSXDataSources.js is idempotent: re-running produces the same DB state (uses select-by-name then insert-or-update pattern, NOT plain insert)"
    - "treasury_list_source_ids RPC, when filtered to api_type='xlsx_download', returns the expected count of rows (sum of FY rows across Plano/McKinney/Frisco minus any deferred city)"
  artifacts:
    - path: "scripts/seedXLSXDataSources.js"
      provides: "Idempotent Node.js seeder for all XLSX data_sources rows (Plano, McKinney transactions+payroll, Frisco)"
      min_lines: 150
      contains: "xlsx_download"
    - path: ".planning/phases/06-xlsx-pipeline/06-02-NOTES.md"
      provides: "Investigation notes: Plano URL probe results, observed column headers per city, any deferred cities/years"
      min_lines: 40
      contains: "Plano"
    - path: "treasury.data_sources (xlsx_download rows)"
      provides: "One row per city/dataset/fiscal-year combination"
      contains: "xlsx_download"
  key_links:
    - from: "scripts/seedXLSXDataSources.js"
      to: "treasury.data_sources table"
      via: "supabase.schema('treasury').from('data_sources') with select-by-name then update or insert"
      pattern: "select.*name.*update|insert"
    - from: "seeded data_sources rows"
      to: "treasury.municipalities"
      via: "municipality_id resolved at seed time by querying municipalities by name"
      pattern: "municipalities.*select.*name"
    - from: "each data_sources row"
      to: "actual XLSX file at download_url"
      via: "download_url field — verified by HEAD or sample-download during seeder development"
      pattern: "download_url"
    - from: "column_mapping JSON"
      to: "actual XLSX column headers"
      via: "header names observed by downloading sample files in Task 1"
      pattern: "column_mapping"
---

<objective>
Investigate the three cities' XLSX sources, then build and run an idempotent seeder that registers every (city, dataset, fiscal year) combination as a `treasury.data_sources` row with `api_type='xlsx_download'`. The seeder writes the download URL, fiscal year, municipality_id, and a column_mapping JSON whose keys reference actual XLSX column headers verified by downloading sample files.

Purpose: Satisfy XLSX-06 (data_sources rows exist with correct api_type, URL, column mapping) and resolve Open Questions 1, 2, and 4 from 06-RESEARCH.md (Plano static URL, city column headers, municipality UUIDs). Phase 6 cannot live-load anything until these rows exist.

Output:
- `scripts/seedXLSXDataSources.js` — new ~200-line idempotent seeder (mirrors `seedDallasDataSources.js` pattern)
- `.planning/phases/06-xlsx-pipeline/06-02-NOTES.md` — investigation notes documenting: Plano URL findings, sample-file column headers per city, any cities/years deferred
- A populated `treasury.data_sources` table with all confirmed XLSX rows
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-xlsx-pipeline/06-CONTEXT.md
@.planning/phases/06-xlsx-pipeline/06-RESEARCH.md

# Existing seeder pattern to mirror exactly (idempotent select-by-name → insert/update)
@scripts/seedDallasDataSources.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate sources — verify Plano URL, sample one XLSX per city, capture headers</name>
  <files>.planning/phases/06-xlsx-pipeline/06-02-NOTES.md</files>
  <action>
Goal: Resolve the three open questions from 06-RESEARCH.md before writing the seeder. Produce `06-02-NOTES.md` documenting findings.

**Step 1: Resolve Plano static URL**
- Visit `https://checkregister.plano.gov/` and identify how the Excel export works. Use WebFetch to load the page and look for any direct XLSX link in the HTML, or any documented data export endpoint.
- Try common patterns: a static `https://checkregister.plano.gov/export.xlsx` style URL, or an API endpoint returning XLSX content.
- If you find a working static URL, attempt a download with `curl -I -L "<url>"` (HEAD with redirect-follow) and confirm content-type indicates XLSX (`spreadsheet`, `excel`, or `octet-stream`).
- If no static URL is discoverable: document this in NOTES and mark Plano as DEFERRED for this seeder pass. Do NOT block the rest of the seeder.

**Step 2: Download sample XLSX from McKinney + Frisco**
For each, pick the most recent FY (FY25) and download to a temp location to inspect headers:
- McKinney check register FY25: `https://www.mckinneytexas.org/Archive.aspx?ADID=2752` — note this is a redirect; use `curl -L -o /tmp/mck_check_fy25.xlsx "<url>"` (or PowerShell `Invoke-WebRequest -MaximumRedirection 5`)
- McKinney payroll FY25: `https://www.mckinneytexas.org/Archive.aspx?ADID=2753`
- Frisco check register FY25: `https://www.friscotexas.gov/DocumentCenter/View/35341/Copy-of-City_of_Frisco_Check_Register_FY25_To_Date-XLSX`

For each downloaded file, write a tiny one-off Node script (or use the helpers from Plan 06-01 if 06-01 is complete) to print row 1 headers:
```js
import ExcelJS from 'exceljs';
const wb = new ExcelJS.Workbook();
await wb.xlsx.readFile('/tmp/mck_check_fy25.xlsx');
const ws = wb.worksheets[0];
const headers = ws.getRow(1).values.slice(1).map(v => String(v ?? '').trim());
console.log(JSON.stringify(headers, null, 2));
const sample = ws.getRow(2).values.slice(1);
console.log('Sample row 2:', JSON.stringify(sample, null, 2));
```

Run for all three. Capture exact header strings (case, punctuation, spaces) into NOTES.

**Step 3: Resolve municipality UUIDs**
Query `treasury.municipalities` for the three city names. Use a quick script or supabase CLI:
```js
const { data } = await supabase.schema('treasury').from('municipalities')
  .select('id, name').in('name', ['Plano', 'McKinney', 'Frisco']);
```
Capture the three UUIDs into NOTES. If any city is missing, add to NOTES as a blocker — the existing Phase 5 quick task 001 added Collin County municipalities, so all three should be present.

**Step 4: Write `06-02-NOTES.md`**
Structure:
```markdown
# Phase 6 Plan 02 — Investigation Notes

## Plano Check Register
- Result: [STATIC_URL_FOUND | DEFERRED]
- URL (if found): ...
- Reason (if deferred): ...
- Column headers (if found): [...]

## McKinney Check Register
- Sample file: <URL used>
- Column headers (raw): [...]
- Proposed column_mapping:
  - date_column: ...
  - amount_column: ...
  - vendor_column: ...
  - description_column: ...
  - department_column: ... (or null)
  - fund_column: ... (or null)
  - check_number_column / invoice_number_column: ...

## McKinney Payroll Register
- Sample file: <URL used>
- Column headers (raw): [...]
- Proposed column_mapping: (treats employee as "vendor", gross-pay as "amount")
  - date_column: ...
  - amount_column: ... (gross pay or net pay — choose the most authoritative)
  - vendor_column: ... (employee name)
  - description_column: ... (job title or position, if available)
  - department_column: ...

## Frisco Check Register
- Sample file: <URL used>
- Column headers (raw): [...]
- Proposed column_mapping: ...

## Municipality UUIDs
- Plano: <uuid>
- McKinney: <uuid>
- Frisco: <uuid>
```

Save NOTES to `.planning/phases/06-xlsx-pipeline/06-02-NOTES.md`.
  </action>
  <verify>
- `cat .planning/phases/06-xlsx-pipeline/06-02-NOTES.md` exists and contains all four sections (Plano, McKinney check, McKinney payroll, Frisco) plus Municipality UUIDs.
- For each non-deferred city: column_mapping has at minimum `date_column`, `amount_column`, `vendor_column` populated with non-empty header strings.
- Three valid UUIDs (or documented absence) for Plano/McKinney/Frisco municipalities.
- Plano section explicitly states either STATIC_URL_FOUND or DEFERRED with a reason.
  </verify>
  <done>06-02-NOTES.md exists with: Plano URL decision (found or deferred), real column headers from sample downloads for McKinney check + McKinney payroll + Frisco check, three municipality UUIDs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decision — Plano URL outcome and seeder scope</name>
  <decision>What is the seeder scope based on Task 1 investigation? Confirm or adjust the seed list before writing the seeder script.</decision>
  <context>Plano's check register portal is dynamic (per 06-RESEARCH.md Open Question 1). Task 1 either found a static URL or marked Plano as DEFERRED. The user must confirm whether to proceed with the discovered URL, defer Plano entirely, or take a different approach (e.g., manual export-then-load workflow).</context>
  <options>
    <option id="proceed-plano-found">
      <name>Proceed with Plano static URL found</name>
      <pros>All three cities seeded; full phase scope met in one pass; matches roadmap's success criteria literally.</pros>
      <cons>Plano URL may be fragile (not officially documented as static); could break in future when Plano updates portal.</cons>
    </option>
    <option id="defer-plano">
      <name>Defer Plano to a follow-up plan; seed McKinney + Frisco now</name>
      <pros>Unblocks 80% of phase scope (McKinney transactions+payroll, Frisco transactions); avoids fragile URL hack; clearly documents Plano gap for later.</pros>
      <cons>Phase 6 success criteria #1 ("Plano, McKinney, Frisco each have at least one loaded dataset") will not be fully satisfied without a follow-up.</cons>
    </option>
    <option id="manual-export-plano">
      <name>Manual export pattern for Plano (file:// or local path)</name>
      <pros>Works around dynamic-export limitation; user manually downloads XLSX once per fiscal year, places in known location, seeder uses file:// URL or local path.</pros>
      <cons>Loader currently only supports `https://` URLs (per CONTEXT decision: "static direct URL only"); would require a CONTEXT-decision revisit. Adds operational friction.</cons>
    </option>
  </options>
  <resume-signal>Select: proceed-plano-found, defer-plano, or manual-export-plano</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write idempotent seedXLSXDataSources.js and run it</name>
  <files>scripts/seedXLSXDataSources.js</files>
  <action>
Implement `scripts/seedXLSXDataSources.js` mirroring `scripts/seedDallasDataSources.js` exactly:

1. **Header + env setup** (copy from seedDallasDataSources.js):
   - `#!/usr/bin/env node` shebang
   - JSDoc explaining purpose, idempotency, usage
   - `import { createClient } from '@supabase/supabase-js'`
   - `SUPABASE_URL` + `SUPABASE_KEY` env vars (same fallbacks as existing seeder)
   - Exit 1 if `SUPABASE_KEY` missing

2. **Resolve municipality UUIDs at runtime** (do NOT hardcode):
   ```js
   const { data: munis, error } = await supabase
     .schema('treasury')
     .from('municipalities')
     .select('id, name')
     .in('name', ['Plano', 'McKinney', 'Frisco']);
   if (error) { console.error('Failed to fetch municipalities:', error.message); process.exit(1); }
   const muniId = (name) => {
     const m = munis.find(x => x.name === name);
     if (!m) { console.error('Municipality not found: ' + name); process.exit(1); }
     return m.id;
   };
   ```

3. **Define SOURCES array** based on the decision in Task 2 and the column_mapping observed in Task 1. Use this shape per row:
   ```js
   {
     name: 'Frisco Check Register FY2025',
     api_type: 'xlsx_download',
     dataset_type: 'transactions',  // or 'salaries' for payroll
     download_url: 'https://www.friscotexas.gov/DocumentCenter/View/35341/...',
     fiscal_year: 2025,  // INTEGER, not array
     municipality_id: muniId('Frisco'),
     column_mapping: { /* from Task 1 NOTES */ }
   }
   ```

   Generate one row per (city, dataset, fiscal year):
   - **McKinney check register**: FY22, FY23, FY24, FY25 — 4 rows (URLs from research §McKinney Check Register)
   - **McKinney payroll register**: FY22, FY23, FY24, FY25 — 4 rows (URLs from research §McKinney Payroll Register)
   - **Frisco check register**: FY18, FY19, FY20, FY21, FY22, FY23, FY24, FY25, FY26 — 9 rows (URLs from research §Frisco Check Register)
   - **Plano**: 0 or N rows depending on Task 2 decision

   Generate the URLs programmatically where possible (e.g., a helper map keyed by FY for McKinney/Frisco) so the SOURCES array is built from a compact data structure rather than 17+ hand-typed objects. Suggested helper:
   ```js
   const MCKINNEY_CHECK_URLS = { 2022: '...', 2023: '...', 2024: '...', 2025: '...' };
   const MCKINNEY_PAYROLL_URLS = { 2022: '...', 2023: '...', 2024: '...', 2025: '...' };
   const FRISCO_CHECK_URLS = { 2018: '...', /* ... */ 2026: '...' };
   ```

4. **Idempotent upsertByName function** — copy verbatim from seedDallasDataSources.js (select-by-name, then insert if missing or update if present). Critical: treasury.data_sources has NO unique constraint on `name`, so plain insert/upsert won't work.

5. **Main loop**:
   - Print `'Seeding XLSX data_sources rows...'`
   - For each src in SOURCES: call upsertByName(src), print id/api_type/dataset_type/fiscal_year/download_url
   - After loop: call `supabase.rpc('treasury_list_source_ids')`, filter to `api_type === 'xlsx_download'`, assert count matches `SOURCES.length`. Exit 1 if mismatch.
   - Print final per-row verification list

6. **Run the seeder**:
   - `SUPABASE_SERVICE_KEY=<from .env.local> node scripts/seedXLSXDataSources.js`
   - Capture stdout — must show every source upserted and the final verification list matches SOURCES.length
   - If any row fails to upsert, fix the issue and re-run (seeder is idempotent)
  </action>
  <verify>
- `node scripts/seedXLSXDataSources.js` runs successfully and prints all upserts + final verification count.
- Re-run `node scripts/seedXLSXDataSources.js` — output shows `(updated existing row ...)` for every row (proves idempotency).
- `node scripts/bulkLoadXLSX.js --list` (from Plan 06-01, if complete) shows all expected XLSX sources.
- Direct DB check via supabase JS or RPC: `treasury_list_source_ids` returns rows with `api_type='xlsx_download'` matching the SOURCES.length count.
- For each row, `download_url`, `fiscal_year` (integer), `column_mapping` (object), `municipality_id` (uuid) are all populated.
- `wc -l scripts/seedXLSXDataSources.js` >= 150 lines.
  </verify>
  <done>seedXLSXDataSources.js exists, ran successfully, is idempotent on re-run, populated treasury.data_sources with all rows from the Task 2 decision (4 McKinney check + 4 McKinney payroll + 9 Frisco + 0/N Plano = 17+ rows), and treasury_list_source_ids verifies the count.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Investigation notes captured:**
   - `.planning/phases/06-xlsx-pipeline/06-02-NOTES.md` exists with Plano result, real column headers per city, three municipality UUIDs.

2. **Seeder script correctness:**
   - `scripts/seedXLSXDataSources.js` exists, ≥150 lines, mirrors `seedDallasDataSources.js` structure (select-by-name then insert-or-update).
   - Municipality UUIDs are resolved at runtime via a `treasury.municipalities` query, NOT hardcoded.
   - Every row has `api_type='xlsx_download'`, single integer `fiscal_year`, populated `download_url`, populated `column_mapping`, valid `municipality_id`.

3. **DB state:**
   - `treasury_list_source_ids` filtered to `api_type='xlsx_download'` returns the expected count (17+ rows depending on Plano decision).
   - Re-running the seeder is a no-op (every row reports "updated existing row").

4. **Compatibility with loader (Plan 06-01):**
   - `node scripts/bulkLoadXLSX.js --list` lists all seeded XLSX sources (proves loader's filter `api_type='xlsx_download'` works against real rows).
</verification>

<success_criteria>
- 06-02-NOTES.md captures: Plano URL outcome (found or deferred with reason), real observed column headers for McKinney check + McKinney payroll + Frisco check, three municipality UUIDs.
- scripts/seedXLSXDataSources.js is a self-contained idempotent seeder; re-runs report only updates, no new inserts after first run.
- treasury.data_sources contains one xlsx_download row per (city, dataset, fiscal year) combination from the Task 2 decision.
- The XLSX loader from Plan 06-01 can `--list` the seeded sources.
- No city-specific code branches anywhere — all city differences live in the SOURCES array's column_mapping JSON.
</success_criteria>

<output>
After completion, create `.planning/phases/06-xlsx-pipeline/06-02-SUMMARY.md` with: tech-stack delta (none — exceljs already added in 06-01 or used here standalone for header inspection), key files (scripts/seedXLSXDataSources.js, 06-02-NOTES.md), decisions resolved (Plano URL outcome from Task 2 checkpoint, observed column headers per city, municipality UUIDs), DB state (count of xlsx_download rows), and frontmatter linking subsystem='loaders', affects=['06-03'].
</output>
