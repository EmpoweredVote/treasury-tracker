---
phase: 62-acfr-verification-source-chain-audit-uat
plan: "62-02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "Every backfilled budget/salary row across the full Phase 58/59/60 cohort carries source attribution: SCO-sourced rows have a non-NULL durable source_url; custom rows carry a data_source label (D-04a)"
    - "Zero SCO-sourced rows have a NULL source_url; the only NULL-source_url rows are the documented pre-existing non-SCO custom rows (the 37 LA/LB/WeHo rows from Phase 58) — those are NOT residue (D-04b, D-05)"
    - "No stored source_url is fragile/version-specific — every SCO source_url is a durable ByTheNumbers /d/<id> human-page URL; salaries provenance points to a durable publicpay/source page (D-04c)"
    - "Zero residue: no orphaned, placeholder, or test rows left by the backfill (e.g. the deleted Test city stays absent; no NULL-total or stub budget rows) (D-04d)"
    - "Read-only — no DB writes, no source-file changes; any defect is documented as a follow-up, not fixed here (D-08)"
  artifacts:
    - path: ".planning/phases/62-acfr-verification-source-chain-audit-uat/62-02-SUMMARY.md"
      provides: "The documented source-chain audit — per-cohort counts of sourced vs NULL rows, the fragility scan result, the residue scan result — the VER-03 part-B evidence record"
      contains: "cohort row counts, SCO-NULL-source_url count (must be 0), expected custom-NULL count, fragile-url count (must be 0), residue findings"
  key_links:
    - from: "treasury.budgets / salaries rows (Phase 58/59/60 cohort)"
      to: "durable human-page source attribution (source_url for SCO, data_source for custom)"
      via: "read-only audit probes asserting non-NULL + durable + no residue"
      pattern: "SCO rows → /d/<id> ByTheNumbers page; custom rows → data_source label; salaries → publicpay provenance"
---

<objective>
Satisfy the source-chain-audit clause of VER-03 (Success Criterion #2): prove every backfilled
budget/salary row carries **durable** source attribution — non-NULL `source_url` on SCO rows
(`data_source` label on custom rows), no **fragile/version-specific** links, and **zero residue**
(no orphaned/placeholder/test rows) — across the full Phase 58/59/60 cohort, and document it. Phase 58
established SCO-NULL=0 at sample depth; this plan extends that to the full cohort and adds the fragility
check Phase 58 did not perform (D-04c). Read-only, $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md
@.planning/phases/58-la-county-parity-backfill/58-04-SUMMARY.md
@.planning/phases/59-remaining-ca-cities-history-linking/59-04-SUMMARY.md
@.planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md
@scripts/loadCASalaries.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Read-only production DB probe harness — reuse the Phase 61 .env-loading + service-key pattern.
     Schema `treasury`. NEVER use mcp__supabase-local (stale, D-09). NEVER write. -->

DB access: repo .env / .env.local → SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY; schema `treasury`;
  read-only .select() only.

Durability bar (D-04):
  DURABLE  = ByTheNumbers dataset page  https://bythenumbers.sco.ca.gov/d/<dataset-id>  (version-independent),
             a city adopted-budget landing page, or publicpay.ca.gov for salaries.
  FRAGILE  = export token / one-time download / session URL / API-or-CSV endpoint with a version or date
             query param / anything that 404s without state.

Known-good baselines (from upstream summaries — re-confirm live, do not assume):
  Phase 58: SCO-data_source rows with NULL source_url = 0; total NULL source_url = 37 (pre-existing
            non-SCO custom LA/LB/WeHo rows — expected, NOT residue); SCO rows with /d/ url = 3,854.
  Phase 59: 7 thin cities FY2003 rows carry /d/ju3w-4gxp (op) / /d/rrtv-rsj9 (rev); Test city deleted
            (0 dependents) and must stay absent.
  Phase 60: 98 cities salaries reach FY2009 via GCC/publicpay provenance.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit source attribution + NULL source_url across the full cohort</name>
  <files>(no files written — read-only DB probes)</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-04, D-05 — read in full)
    - .planning/phases/58-la-county-parity-backfill/58-04-SUMMARY.md (the SCO-NULL=0 / 37-custom-NULL baseline)
    - .planning/phases/61-enrichment-parity/61-01-PLAN.md (Task 2 verify block — the .env service-key probe snippet)
  </read_first>
  <action>
    Using read-only probes (production Treasury DB, schema `treasury`, service key), audit the full
    backfilled cohort:
    (1) Count budget rows (operating + revenue) for the LA County cohort (88 LA County cities + LA County
        gov) and the 7 Phase-59 thin cities, and salaries rows for the Phase-60 cohort.
    (2) Partition each by source attribution: SCO-`data_source` rows with non-NULL `source_url`;
        SCO-`data_source` rows with NULL `source_url` (MUST be 0, D-04b); non-SCO custom rows with NULL
        `source_url` but a populated `data_source` label (expected ~37 LA/LB/WeHo, D-05); and any row
        with BOTH NULL `source_url` AND NULL/empty `data_source` (a true gap — MUST be 0, D-04a).
    (3) Confirm salaries rows carry provenance (a `source_url` or `data_source` pointing to
        publicpay/GCC per loadCASalaries.js).
    Record the exact counts. The audit FAILS this task if any SCO row has NULL `source_url`, or any row
    has neither `source_url` nor `data_source`.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data}=await s.from('budgets').select('source_url,data_source,dataset_type').in('dataset_type',['operating','revenue','salaries']).limit(200000);const rows=data||[];const scoNull=rows.filter(r=>/sco|bythenumbers|\/d\//i.test((r.source_url||'')+(r.data_source||''))===false?false:(/\/d\//.test(r.source_url||'')===false&&/sco|bythenumbers/i.test(r.data_source||'')&&!r.source_url));const trueGap=rows.filter(r=>!r.source_url&&!r.data_source);console.log('total rows:',rows.length,'| true-gap (no source_url AND no data_source):',trueGap.length);process.exit(trueGap.length===0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Cohort row counts recorded (LA County op/rev, 7 thin cities op/rev, Phase-60 salaries).
    - SCO-`data_source` rows with NULL `source_url` = 0 (D-04b).
    - Rows with neither `source_url` nor `data_source` = 0 (D-04a; verify probe exits 0).
    - The NULL-`source_url` rows that exist are exactly the expected non-SCO custom rows carrying a `data_source` label (~37 LA/LB/WeHo, D-05) — enumerated, confirmed not residue.
    - Salaries rows carry publicpay/GCC provenance.
    - Read-only (no writes).
  </acceptance_criteria>
  <done>
    Full-cohort source-attribution audit confirms every backfilled row is sourced, SCO-NULL source_url = 0, and the only NULL-source_url rows are the documented custom rows — extending the Phase-58 sample result to the whole cohort.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fragility scan (durable human-page URLs) + zero-residue scan</name>
  <files>(no files written — read-only DB probes)</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-04c fragility, D-04d residue)
    - .planning/phases/59-remaining-ca-cities-history-linking/59-04-SUMMARY.md (Test city deletion — must stay absent)
    - scripts/loadCASalaries.js (salaries source_url/provenance shape)
  </read_first>
  <action>
    Fragility scan (D-04c): collect the DISTINCT `source_url` values across the cohort and classify each
    as DURABLE or FRAGILE per the D-04 bar. DURABLE = a ByTheNumbers `/d/<dataset-id>` human page, a city
    adopted-budget landing page, or publicpay.ca.gov. FRAGILE = export tokens, one-time/session download
    URLs, or API/CSV endpoints carrying a version or date query param. Report the distinct-URL count, the
    fragile count (MUST be 0), and list any fragile URL with its row context for follow-up.

    Zero-residue scan (D-04d): probe for backfill residue — (a) the deleted Test city stays absent (0
    municipalities named 'Test' with 0 budgets reappearing); (b) no budget rows with NULL/zero
    `total_amount` introduced by the backfill; (c) no orphaned budget rows whose `municipality_id` does
    not resolve to a municipality; (d) no placeholder/stub `data_source` labels (e.g. 'TODO', 'test',
    empty string). Record counts; residue MUST be 0.

    Document any FRAGILE url or residue finding as a follow-up flag (D-08) — do NOT fix it here (no DB
    writes, no source changes).
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data}=await s.from('budgets').select('source_url').not('source_url','is',null).limit(200000);const urls=[...new Set((data||[]).map(r=>r.source_url))];const fragile=urls.filter(u=>/[?&](version|rev|token|session|download|export|date)=/i.test(u)||/\.csv(\?|$)/i.test(u)||/\/api\//i.test(u));console.log('distinct source_urls:',urls.length,'| fragile:',fragile.length,fragile.slice(0,10));const {data:t}=await s.from('municipalities').select('id,name').eq('name','Test');console.log('Test cities present (must be 0):',(t||[]).length);process.exit(fragile.length===0&&(t||[]).length===0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Distinct `source_url` count recorded; fragile/version-specific count = 0 (D-04c; verify probe exits 0). Any fragile URL is listed with row context as a follow-up flag.
    - SCO source_urls confirmed as durable ByTheNumbers `/d/<id>` human pages.
    - Zero residue: Test city absent (probe = 0); no NULL/zero-total backfill rows; no orphaned-municipality_id rows; no placeholder/stub data_source labels (D-04d).
    - Any finding is documented for follow-up — not fixed (D-08); read-only, no writes.
  </acceptance_criteria>
  <done>
    The fragility scan confirms all SCO source_urls are durable human-page links (0 fragile) and the residue scan confirms the backfill left no orphaned/placeholder/test rows — the durability + cleanliness half of the source-chain audit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the source-chain audit + write the SUMMARY (VER-03 part-B evidence)</name>
  <files>.planning/phases/62-acfr-verification-source-chain-audit-uat/62-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-04 pass criteria)
    - $HOME/.claude/get-shit-done/templates/summary.md
    - The audit results from Tasks 1 + 2
  </read_first>
  <action>
    Write `62-02-SUMMARY.md`: cohort row counts; the source-attribution partition (SCO non-NULL,
    SCO-NULL=0, expected custom-NULL enumerated, true-gap=0); the fragility scan result (distinct URLs,
    fragile=0, any flagged); the residue scan result (Test absent, 0 orphans, 0 stubs). State the overall
    source-chain-audit verdict for SC#2: PASS when every backfilled row is sourced, SCO-NULL source_url=0,
    0 fragile URLs, and 0 residue. List any flags as follow-up candidates (not fixed, D-08). Record that
    this satisfies the source-chain-audit clause of VER-03 (the ACFR clause is covered by Plan 62-01) —
    together the two plans close VER-03.
  </action>
  <acceptance_criteria>
    - `62-02-SUMMARY.md` exists with cohort counts, the attribution partition, the fragility scan, and the residue scan.
    - The SC#2 verdict is stated with its pass rationale (all sourced, SCO-NULL=0, 0 fragile, 0 residue).
    - Any flag is listed as a documented follow-up (no fix attempted).
    - The summary states VER-03 part-B (source-chain audit) is satisfied and references Plan 62-01 for the ACFR clause; together they close VER-03.
  </acceptance_criteria>
  <done>
    The source-chain audit is fully documented in 62-02-SUMMARY.md with attribution, durability, and residue results plus an explicit SC#2 verdict — the VER-03 part-B evidence record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored source_url → citizen click | A fragile/version-specific URL passes a NULL check but 404s for a citizen later — defeats the always-sourced guarantee. |
| backfill loaders → production rows | Orphaned/placeholder/test rows left behind read as real data in the live app. |
| repo .env service key → production DB | Read-only here; a stray write would corrupt verified data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-A | Tampering / false PASS | counting NULL-source_url presence only, missing fragility | mitigate | D-04c: explicit fragility classifier (token/session/version/API/CSV patterns) over DISTINCT source_urls; fragile count must be 0; Phase 58 only did the NULL check — this adds durability. |
| T-62-02-B | Tampering | residue rows (orphans, stubs, resurrected Test city) | mitigate | D-04d residue scan: Test-city-absent probe, NULL/zero-total probe, orphaned-municipality_id probe, placeholder-label probe; all must be 0. |
| T-62-02-C | Tampering | accidental DB write during probing | mitigate | D-08 read-only: `.select()` only; no upsert/update/delete; verify snippets read-only. |
| T-62-02-D | Repudiation | undocumented audit result | mitigate | Exact counts + flagged items recorded in 62-02-SUMMARY.md as the durable VER-03 part-B evidence record. |
</threat_model>

<verification>
- Full Phase 58/59/60 cohort audited: every backfilled row sourced (source_url for SCO, data_source for custom); rows with neither = 0.
- SCO-sourced rows with NULL source_url = 0; the only NULL-source_url rows are the documented ~37 custom LA/LB/WeHo rows.
- Distinct source_urls all durable; fragile/version-specific count = 0.
- Zero residue: Test city absent, no NULL/zero-total backfill rows, no orphans, no placeholder labels.
- Read-only ($0, no writes); 62-02-SUMMARY.md records the VER-03 part-B evidence.
</verification>

<success_criteria>
- Source-chain-audit clause of VER-03 satisfied: every backfilled budget/salary row carries durable human-page attribution, zero residue (SC#2).
- SCO-NULL source_url = 0 at full-cohort depth; 0 fragile URLs (the durability check Phase 58 did not run).
- Read-only, $0 (D-08, D-10); defects documented as follow-ups, not fixed.
- 62-02-SUMMARY.md is the documented evidence record; together with 62-01 it closes VER-03.
</success_criteria>

<output>
Create `.planning/phases/62-acfr-verification-source-chain-audit-uat/62-02-SUMMARY.md` when done.
</output>
