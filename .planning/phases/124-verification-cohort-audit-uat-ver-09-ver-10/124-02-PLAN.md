---
phase: 124-verification-cohort-audit-uat-ver-09-ver-10
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase124-cohort-audit.mjs
  - .planning/phases/124-verification-cohort-audit-uat-ver-09-ver-10/124-COHORT-AUDIT.md
autonomous: true
requirements: [VER-09]
must_haves:
  truths:
    - "The full 50-node state cohort source-chain audit is clean: 0 NULL-basis, 0 fragile/residue, 0 out-of-window, 0 dup, 0 orphan — LOAD-01 holds cohort-wide with no manual re-clean"
    - "ALL 50 state nodes are confirmed on State-ACFR GAAP — the ACFR set is 50/50 and 0 states are NASBO-only"
    - "NASBORT-01 confirmed: exactly TWO NASBO-labelled rows exist in the whole cohort — NV FY2024 operating and KY FY2023 operating — and NEITHER has a same-year ACFR operating row (no node shows NASBO where ACFR now exists)"
    - "Every displayed state-node row carries a basis label (data_source + source_url + source_date)"
    - "Each newly-loaded state's + each deepened node's (CA FY2002-2025, FL FY2003-2024) EXACT loaded-FY set matches its recorded LOADLOG disposition — holes absent BY DESIGN, any extra/missing FY FAILs"
    - "Re-running a representative dollar-unit loader AND a representative hand-transcribed loader produces 0 net change (idempotency / never-overwrite guard), with the WR-05 data_sources-residue pattern explicitly re-checked after the re-run"
  artifacts:
    - path: "scripts/verify-phase124-cohort-audit.mjs"
      provides: "50-node cohort source-chain audit for the v2.15 final-tail cohort (all 50 ACFR, NASBO retired to 2 fallback rows)"
      contains: "WINDOW_BOUNDS"
    - path: ".planning/phases/124-verification-cohort-audit-uat-ver-09-ver-10/124-COHORT-AUDIT.md"
      provides: "The cohort audit report: per-invariant PASS/FAIL, 50/50-ACFR + NASBORT-01 confirmation, idempotency result, window-integrity reconciliation"
  key_links:
    - from: "scripts/verify-phase124-cohort-audit.mjs"
      to: "treasury.budgets + treasury.data_sources"
      via: "read-only invariant queries across all entity_type=state nodes"
      pattern: "budgets"
    - from: "124-COHORT-AUDIT.md NASBORT-01 section"
      to: "123-01-SUMMARY.md (isAcfrOccupied guard + 2 fallback rows)"
      via: "confirm the only 2 NASBO rows are NV FY2024 + KY FY2023, both without same-year ACFR"
      pattern: "NASBO"
---

<objective>
Run the full 50-node state cohort source-chain audit on the v2.15 final-tail cohort (VER-09 parts b+c): assert the Phase 102/106/110/116 invariant set (0 NULL / fragile / residue / out-of-window / dup / orphan; every row basis-labelled) over ALL 50 ACFR-state windows (29 pre-tail + the 21 new + the widened CA/FL deepening bounds), confirm **50/50 ACFR** (0 NASBO-only states), confirm **NASBORT-01** (exactly 2 NASBO-labelled rows — NV FY2024 op + KY FY2023 op — neither with same-year ACFR, i.e. no node shows NASBO where ACFR now exists), confirm loader idempotency / never-overwrite (0-change re-run of a dollar-unit + a hand-transcribed loader), and reconcile every newly-loaded + deepened state's exact loaded-FY set against its recorded LOADLOG disposition.

Purpose: Prove the whole cohort's source chain stayed clean and residue-free after the final 21-state tail + CA/FL deepening loads AND the NASBO retirement — the Phase 116 precedent re-run on the now-complete 50-ACFR surface, with the NASBO cohort collapsed from 21 states to 2 documented fallback rows.
Output: `scripts/verify-phase124-cohort-audit.mjs` + `124-COHORT-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/verify-phase116-cohort-audit.mjs
@.planning/phases/123-nasbo-retirement-nasbort-01/123-01-SUMMARY.md
@.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-03-SUMMARY.md

<interfaces>
<!-- verify-phase116-cohort-audit.mjs is the structural template (INV-1..INV-12, read-only, exit 0/2).
     Reuse it. The Phase 124 changes are: +21 window bounds, widen CA/FL, ACFR set → 50, and the
     BIG change: the NASBO cohort is retired — INV-7 flips from "21 NASBO states × 2 rows" to
     "exactly 2 NASBO rows in the whole cohort" + a NEW NASBORT-01 invariant. -->

WINDOW_BOUNDS must ADD the 21 tail states (bounds = min/max loaded FY; in-window holes are absent
rows and legal — the out-of-window invariant only fails rows OUTSIDE bounds), and WIDEN CA + FL:
  Batch-1: AK 2006–2025 · AR 2003–2024 · DE 2004–2025 (hole 2005) · HI 2005–2025 · ID 2004–2025
  Batch-2: IA 2002–2025 (hole 2008) · KS 2019–2025 · ME 2002–2025 · MS 2003–2024 · MT 2015–2025
  Batch-3: NE 2020–2025 · NV 2019–2024 (op to 2024, rev to 2023 — FY2024 op is the NASBO fallback) ·
           NH 2017–2024 · NM 2019–2024 (holes 2020, 2021) · ND 2021–2025
  Batch-4: OK 2002–2024 · RI 2006–2025 · SD 2002–2025 · VT 2015–2025 · WV 2020–2025 · WY 2005–2025
  WIDENED deepening: CA 2002–2025 (was 2008–2025) · FL 2003–2024 (was 2021–2024).
  Unchanged from 116: NY 2003–2024, TX 2015–2024, MN, OH, VA, PA, IL, NC, GA, MD, TN, WA, MI,
  NJ 2002–2025, CT 1988–2025, WI 2000–2025, MA 2001–2025 (holes 2002/04/05/2021),
  IN, AZ, OR, MO, CO, SC, KY 2002–2025 (op hole none/rev hole 2023), UT, AL, LA.

ACFR-GAAP set (INV-6) is now ALL 50 states. Every state's operating+revenue rows must carry
ACFR-GAAP provenance ("…State ACFR — General Fund (FY{fy} actual, GAAP basis)" or the pre-GASB-34
combined-statement basis for CT FY1988-2001 / WI FY2000-2001 / MA FY2001). The ONLY permitted
NASBO-labelled rows in the ENTIRE cohort are the 2 documented fallbacks (see NASBORT-01).

NASBORT-01 (NEW invariant, replaces the old "N NASBO states × 2 rows" INV-7): query every row with
a NASBO `data_source` across all 50 nodes. Assert EXACTLY 2 such rows — NV FY2024 operating and
KY FY2023 operating — and assert that for each, there is NO ACFR-labelled operating row at the same
(state, fiscal_year) key. Then, the general form of "no node shows NASBO where ACFR exists": for
every (state, fy) that HAS an ACFR operating row, assert there is 0 NASBO row at that same key.
This is the DB-side proof of the Phase 123 isAcfrOccupied guard. (Reference 123-01-SUMMARY.md:
guard added + only NV FY2024 / KY FY2023 remain, neither with same-year ACFR.)

50/50-ACFR invariant (NEW): count(distinct state where entity_type='state' AND an ACFR-labelled
operating row exists) === 50; and 0 states are ACFR-absent / NASBO-only.

Window-integrity (INV-11 generalized, per-state EXACT loaded-FY set, operating and revenue tracked
separately since NV/KY op≠rev): each of the 21 new states + CA + FL must match its recorded
LOADLOG disposition. Recorded in-window holes / partial windows to expect (NOT unrecorded gaps):
  AR FY2025 (garbled Type-3 font) · DE FY2005 (404) · HI FY2000-2004 (image-only, below floor) ·
  IA FY2008 (RC4-encrypted) · ME FY2000/2001 (pre-GASB-34, below floor) · MS FY2025 (not published) ·
  NV FY2024 op = NASBO fallback / NV rev ends FY2023 · NM FY2020/2021 (narrower single-agency doc) ·
  KY FY2023 op = NASBO fallback / KY FY2023 rev absent · FL FY2000-2002 (repair-pending, not loaded) ·
  CA ≤FY2001 (soft-404 floor) · NY ≤FY2002 / TX ≤FY2014 (unchanged floors).

Row-count expectations (op+rev; from 118-121 + 122 SUMMARYs) to confirm in the report:
  AK 20+20, AR 22+22, DE 21+21, HI 21+21, ID 22+22, IA 23+23, KS 7+7, ME 24+24, MS 22+22, MT 11+11,
  NE 6+6, NV 6op(5 ACFR + 1 NASBO FY2024)+5rev, NH 8+8, NM 4+4, ND 5+5,
  OK 23+23, RI 20+20, SD 24+24, VT 11+11, WV 6+6, WY 21+21,
  CA 24+24 (was 18+18), FL 22+22 (was 4+4). Cohort: 50 ACFR states, exactly 2 NASBO rows total.

FY-end / basis semantics to carry forward + confirm: MI Sep-30 and AL Sep-30 (source_date {FY}-09-30,
fiscal_year_start_month=10) unchanged from 116; ME is a NON-JUNE FY-end state — confirm its
fiscal_year_start_month matches its LOADLOG. Pre-GASB-34 basis-label distinctness (CT/WI/MA) is
unchanged from 116 — no NEW pre-34 labelled rows were added (all new-state floors are FY2002+
GASB-34-era or documented holes). GA F-97-01 supersede assertion carried forward unchanged (confirm
the tail + retirement did not disturb it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the cohort audit for the 50-ACFR final-tail cohort and run the full invariant set incl. NASBORT-01 + 50/50-ACFR</name>
  <read_first>
    - scripts/verify-phase116-cohort-audit.mjs (template — INV-1..INV-12, WINDOW_BOUNDS, ACFR_STATES / TRANCHE / DEEPENED sets, EXPECTED_FYS_OP/REV, read-only, exit 0/2)
    - .planning/phases/123-nasbo-retirement-nasbort-01/123-01-SUMMARY.md (the isAcfrOccupied guard + the DB-verified end state: 2 NASBO rows NV FY2024 / KY FY2023, both without same-year ACFR; MS/MT ACFR confirmed)
    - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-03-SUMMARY.md (CA 2002-2025, FL 2003-2024 new bounds + row counts)
    - the 118/119/120/121 batch SUMMARYs (per-state windows, row counts, holes, UNITS, Sep-30/non-June flags)
  </read_first>
  <action>
    Create scripts/verify-phase124-cohort-audit.mjs from verify-phase116-cohort-audit.mjs (reuse its createClient env-load, the invariant framework, and the exit-0/2 contract). Apply these changes:

    1. ADD the 21 tail-state WINDOW_BOUNDS from the interfaces block; WIDEN CA to 2002–2025 and FL to 2003–2024; keep all other 116 bounds unchanged. REMOVE the `_NASBO` default fallback (no state should fall through to it now — every state has an explicit window; if any state resolves to the default, that itself is a FAIL).
    2. SET the ACFR-GAAP set (INV-6) to all 50 states so every node's operating+revenue rows are asserted ACFR-GAAP-labelled (with the CT/WI/MA pre-GASB-34 combined-statement basis exception unchanged).
    3. REPLACE the old INV-7 ("N NASBO states × 2 rows") with NASBORT-01: query all rows with a NASBO data_source; assert EXACTLY 2 (NV FY2024 operating + KY FY2023 operating); assert neither has a same-year ACFR operating row; and assert generally that for every (state, fy) with an ACFR operating row there are 0 NASBO rows at that key.
    4. ADD the 50/50-ACFR invariant: count(distinct state with an ACFR operating row) === 50; 0 NASBO-only states.
    5. GENERALIZE the window-integrity invariant (INV-11) to all 21 new states + CA + FL: each state's EXACT loaded-FY set (operating and revenue tracked separately for NV + KY) matches its recorded LOADLOG disposition — the recorded holes/partial windows in the interfaces block are absent BY DESIGN; any extra or missing FY FAILs.
    6. Keep INV-1 (every state row has data_source + source_url + source_date), INV-2 (0 residue stale *-gf-* data_sources — LOAD-01 cohort-wide), INV-3 (0 out-of-window FYs against the updated bounds), INV-4 (0 dup (muni,fy,dataset) combos), INV-5 (0 orphan data_source_id). Carry forward MI + AL Sep-30 semantics and add the ME non-June fiscal_year_start_month check. Carry forward the GA F-97-01 supersede assertion. Confirm the pre-GASB-34 basis-label distinctness (CT/WI/MA) still holds.

    Run it read-only against the live treasury schema. The script must exit 0 only if all invariants PASS. If any invariant FAILs, that is a source-chain defect — fix in-phase (re-run the guarded loader / correct the data_sources row via the source-safe path, NEVER treasury_sync_city_budget) before sign-off, then re-run to confirm. Capture the per-invariant output for the report.
  </action>
  <verify>
    <automated>node scripts/verify-phase124-cohort-audit.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase124-cohort-audit.mjs exists; makes no writes (read-only).
    - WINDOW_BOUNDS contains all 21 new entries plus widened CA 2002–2025 / FL 2003–2024, and no state resolves to a NASBO default.
    - INV-6 set includes all 50 states; the audit reports 50 state nodes loaded.
    - NASBORT-01 passes: exactly 2 NASBO rows (NV FY2024 op + KY FY2023 op), neither with same-year ACFR, and 0 NASBO rows on any (state, fy) that has an ACFR operating row.
    - 50/50-ACFR invariant passes: distinct-ACFR-state count === 50; 0 NASBO-only states.
    - Row counts confirmed for all 21 new states + CA 24+24 / FL 22+22 (per the interfaces block).
    - Window-integrity passes for all 21 new states + CA + FL with the recorded holes absent-by-design; MI/AL Sep-30, ME non-June, GA supersede, and CT/WI/MA pre-34 label checks all present and passing.
    - All invariants print PASS and the script exits 0 (or any failure is fixed-in-phase via the source-safe path and the re-run exits 0).
  </acceptance_criteria>
  <done>The full cohort audit (INV-1..5 + INV-6 50-state + NASBORT-01 + 50/50-ACFR + window-integrity + Sep-30/non-June + GA supersede + pre-34) runs read-only over the 50-node cohort and exits 0; any defect found is fixed in-phase and re-confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm idempotency / never-overwrite (with WR-05 residue re-check), reconcile the window verdict, and write 124-COHORT-AUDIT.md</name>
  <read_first>
    - .planning/phases/120-acfr-upgrade-batch-3-ne-nv-nh-nm-nd-acfr-43-47/120-05-ND-LOADLOG.md (a dollar-unit UNITS=1 loader invocation + idempotency precedent)
    - .planning/phases/121-acfr-upgrade-batch-4-ok-ri-sd-vt-wv-wy-acfr-48-53/121-01-OK-LOADLOG.md (a hand-transcribed loader invocation)
    - .planning/phases/123-nasbo-retirement-nasbort-01/123-01-SUMMARY.md (the never-overwrite-ACFR guard — an unfiltered NASBO re-run must now be a no-op)
    - scripts/verify-phase116-cohort-audit.mjs and its 116-COHORT-AUDIT.md (report format + the WR-05 residue re-check precedent)
    - The Task 1 audit output
  </read_first>
  <action>
    (a) IDEMPOTENCY / NEVER-OVERWRITE: re-run two representative ACFR loaders — one dollar-unit state (ND, both op+rev, FY2025) and one hand-transcribed state (OK, both op+rev, one loaded FY) — using each loader's existing guarded never-overwrite path (source the .env first; main tree only; NEVER treasury_sync_city_budget). Confirm each reports "Loaded 0 rows" / 0 net change. THEN re-query treasury.data_sources for duplicate/residue rows on ND + OK (the WR-05 non-atomic-upsert pattern historically re-created residue rows during re-runs) — if any appear, clean via the source-safe path and re-run the Task 1 audit to confirm INV-2 passes. ALSO verify the NASBO-retirement guard: run `node scripts/loadStateGF.mjs` in dry-run/validate mode (its default no-DB-write path) and confirm the isAcfrOccupied guard logic would SKIP every ACFR-occupied node (0 intended writes to any of the 50 ACFR nodes) — the fallback loader can now only touch NV FY2024 / KY FY2023 or an absent node. Record all three results. A non-zero budgets change is a never-overwrite defect → fix in-phase before sign-off.

    (b) WINDOW-VERDICT RECONCILIATION: query the DB for the actual present FYs per new/deepened state and diff against the 118-121 + 122 SUMMARYs. Confirm: (i) every FY recorded as LOADED is present (row counts match the interfaces block); (ii) every absent FY is absent BY DESIGN per a recorded disposition (the holes/partial windows listed in the interfaces block) — NOT an unrecorded gap; (iii) NV FY2024 op + KY FY2023 op render as the honest NASBO fallbacks (disclosed label), and KY FY2023 revenue + NV FY2024 revenue are honestly absent. An honest, disclosed hole is a PASS; do NOT re-litigate gaps 118-122 judged unrecoverable.

    (c) WRITE 124-COHORT-AUDIT.md: headline verdict (N/N invariants + "50/50 ACFR, NASBO retired to 2 fallback rows"), the per-invariant PASS/FAIL table with row counts (all 21 new states + CA/FL widened + the NASBORT-01 + 50/50-ACFR results), the idempotency result including the explicit WR-05 residue re-check outcome AND the guard dry-run result, and the window-reconciliation section listing the deferred-by-design FYs so they are not re-litigated. This file + 124-REDERIVATION.md are the two inputs the UAT plan and the gsd-verifier read for VER-09.
  </action>
  <verify>
    <automated>node scripts/verify-phase124-cohort-audit.mjs; test -f .planning/phases/124-verification-cohort-audit-uat-ver-09-ver-10/124-COHORT-AUDIT.md && grep -qiE "idempoten|NASBORT|invariant|50/50" .planning/phases/124-verification-cohort-audit-uat-ver-09-ver-10/124-COHORT-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 124-COHORT-AUDIT.md exists with: per-invariant PASS/FAIL table, row-count confirmations (21 new states + CA 24+24 / FL 22+22), the NASBORT-01 + 50/50-ACFR results, idempotency result ("Loaded 0 rows" for ND + OK re-runs), the explicit WR-05 data_sources residue re-check outcome, the guard dry-run result (0 intended writes to ACFR nodes), and the window-verdict reconciliation.
    - Idempotency re-runs produced 0 net budgets change AND 0 surviving data_sources residue (or defects were fixed in-phase via the source-safe path and re-confirmed).
    - The window reconciliation confirms 0 unrecorded DB gaps; all absent FYs map to recorded dispositions; NV FY2024 + KY FY2023 confirmed as the only 2 honest NASBO fallbacks.
    - The audit script exits 0.
  </acceptance_criteria>
  <done>124-COHORT-AUDIT.md records all invariants clean, 50/50 ACFR + NASBORT-01 confirmed, idempotency 0-change with WR-05 re-checked and the guard proven a no-op on ACFR nodes, and the window verdict reconciled against the actual DB; defects (if any) fixed in-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → treasury schema (read) | Read-only invariant queries via service key (env-only). The audit makes NO writes. |
| idempotency re-run / in-phase fix → treasury (write) | Loader re-runs use the guarded never-overwrite RPC; the source-unsafe RPC must be avoided. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-04 | Tampering | idempotency re-run / in-phase fix writes | mitigate | All writes go through the loader's guarded never-overwrite path (treasury_sync_budget_tree) — NEVER treasury_sync_city_budget (project memory: not source-safe). The audit re-run confirms non-destructive. |
| T-124-05 | Tampering | WR-05 non-atomic data_sources upsert residue | mitigate | Explicit post-re-run data_sources residue query + source-safe cleanup + audit re-run (the standing v2.12 lesson, now a mandatory step). |
| T-124-06 | Elevation of Privilege | NASBO fallback loader overwriting an ACFR node | mitigate | NASBORT-01 invariant + the isAcfrOccupied guard dry-run prove an unfiltered NASBO re-run is a no-op on all 50 ACFR nodes; only NV FY2024 / KY FY2023 / absent nodes are writable. |
| T-124-07 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Read from env only; never inlined in script or report. Standing practice. |
| T-124-08 | Repudiation | audit verdict honesty | mitigate | The audit script exits 2 on any failing invariant (cannot silently "pass"); the report records the raw per-invariant output; idempotency re-runs are observable ("Loaded 0 rows"). |
| T-124-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses existing deps. No install task. |

Read-only audit; write paths are the idempotency re-run and conditional in-phase fixes, both gated to the source-safe never-overwrite RPC.
</threat_model>

<verification>
- scripts/verify-phase124-cohort-audit.mjs exits 0 with all invariants PASS over the 50-ACFR cohort, incl. NASBORT-01 + 50/50-ACFR + window-integrity.
- Idempotency re-run of a representative ND (dollar-unit) + OK (hand-transcribed) loader reports 0 net change; WR-05 residue re-check clean; the isAcfrOccupied guard dry-run is a no-op on all ACFR nodes.
- 124-COHORT-AUDIT.md reconciles the window verdict against the actual DB (0 unrecorded gaps); NV FY2024 + KY FY2023 confirmed the only 2 honest NASBO fallbacks.
</verification>

<success_criteria>
- VER-09 parts (b)+(c): full 50-node cohort source-chain audit clean (0 NULL/fragile/residue/out-of-window/dup/orphan), every displayed row basis-labelled, LOAD-01 holds cohort-wide with no manual re-clean; all 50 nodes confirmed on ACFR; NASBORT-01 confirmed (no node shows NASBO where ACFR now exists — only NV FY2024 + KY FY2023 honest fallbacks remain).
</success_criteria>

<output>
Create `.planning/phases/124-verification-cohort-audit-uat-ver-09-ver-10/124-02-SUMMARY.md` when done.
</output>
