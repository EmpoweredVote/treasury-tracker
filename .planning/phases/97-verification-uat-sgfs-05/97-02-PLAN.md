---
phase: 97-verification-uat-sgfs-05
plan: 02
type: execute
wave: 2
depends_on: ["97-01"]
autonomous: false
files_modified:
  - .planning/phases/97-verification-uat-sgfs-05/97-02-AUDIT.md
requirements: [SGFS-05]
must_haves:
  truths:
    - "A cohort-wide source-chain audit across ALL 50 state nodes (entity_type='state') confirms criterion 1: 0 unsourced rows (every displayed General Fund row carries a resolving source_url + source_date + canonical data_source with a basis label) and 0 round-number-estimate rows; the cohort breakdown is exactly 47 NASBO operating states (46 + Georgia, FY2023+FY2024 = 94 in-window rows) + MN/OH/VA real-ACFR (operating AND revenue), and nothing unsourced is displayed"
    - "Independent DB probes (read-only, mcp__supabase-local) confirm: 0 NULL source_url / source_date / data_source on any displayed state row (P4 carried from 96-07); 0 cohort revenue rows (the 234 unsourced revenue rows deleted in 96-07 stay deleted — MN/OH/VA revenue correctly NOT deleted); 0 operating rows outside the FY2023/FY2024 window for NASBO states (the 141 out-of-window rows stay deleted); 94 NASBO in-window operating rows across 47 distinct states; 0 fragile/orphan/residue rows (no orphan budgets, no orphan budget_categories, no duplicate (muni,fy,dataset), no numeric-garbage category labels)"
    - "The mixed-basis design is confirmed honest, not hidden: every NASBO state row carries the budgetary-basis label ('NASBO State Expenditure Report — General Fund (FY<y> actual, budgetary basis)' per 96 P3) and MN/OH/VA carry their GAAP/ACFR basis label; a probe enumerates the distinct data_source + basis-label strings cohort-wide and finds 0 rows missing a basis label"
    - "The negative-category edge is proven absent cohort-wide: a probe over all NASBO state operating categories finds 0 negative General Fund function amounts (the P2/D-96-08 clamp rule exists in buildOperatingTree but never fired) — recorded explicitly so the absent edge is verified, not skipped"
    - "D-97-03 operating-only revenue presentation verified: with cohort revenue rows deleted, each NASBO state node is operating-only; the audit confirms (from the frontend render path) that an operating-only node shows NO empty/broken revenue view, while MN/OH/VA still correctly show their real revenue. If a broken/empty revenue tab renders for operating-only nodes, the small reproducible idempotent suppression fix is applied (D-97-03/D-97-04) — presented to Chris at a BLOCKING checkpoint and approved before any write/code change, then re-verified"
    - "A written 97-02-AUDIT.md records every probe query + result, the basis-label enumeration, the negative-category absence proof, the D-97-03 operating-only-revenue finding (and any approved fix with before/after + idempotency evidence), and the SGFS-05 cohort-audit verdict"
  artifacts:
    - path: ".planning/phases/97-verification-uat-sgfs-05/97-02-AUDIT.md"
      provides: "The cohort-wide source-chain audit + basis-label + negative-category-absence + operating-only-revenue record (SGFS-05 criterion 1)"
      contains: "source-chain"
  key_links:
    - from: "All 50 state nodes (47 NASBO operating + MN/OH/VA ACFR op+rev)"
      to: "a clean, durable, residue-free source chain with honest per-node basis labels and no empty/broken operating-only revenue view"
      via: "read-only cohort probes + frontend operating-only-revenue check + the one optional Chris-approved suppression fix"
      pattern: "source-chain"
---

<objective>
SGFS-05 criterion 1: prove the whole 50-state General Fund node cohort is real + sourced + residue-free, the mixed basis is labelled (honest, not hidden), the negative-category edge is verified absent, and operating-only NASBO nodes render with no empty/broken revenue view. Then, if (and only if) the operating-only revenue view is broken, apply the one small Chris-approved in-phase suppression fix and re-verify.

Primarily read-only. Production writes / code changes are limited to a single Chris-approved D-97-03/D-97-04 fix (the operating-only revenue-view suppression, or another small source-stamp/basis-label defect surfaced by the audit); everything else is read-only. Apply any fix as a targeted, idempotent change — NOT a re-sync (treasury_sync_city_budget is not source-safe, [[project_sync_city_budget_not_source_safe]]).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/97-verification-uat-sgfs-05/97-CONTEXT.md
@.planning/phases/96-remaining-states-sgfs-04/96-VERIFICATION.md
@.planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md
@.planning/phases/96-remaining-states-sgfs-04/96-CONTEXT.md
@.planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md

<interfaces>
<!-- Production target kxsdzaojfaibhuzmclfq. Read-only except one approved fix. mcp__supabase-local execute_sql preferred. -->
- Cohort scope: treasury.municipalities entity_type='state'; treasury.budgets metadata gates visibility; provenance on line-item rows via source_url/source_date/data_source ([[reference_treasury_budgets_probe_columns]] — use total_budget/hierarchy column names). Expected cohort: 47 NASBO operating states (incl. GA) + MN/OH/VA (op+rev) = 50.
- Cohort-audit probes (read-only), extend the 5 Phase-96 probes to all 50 nodes: (a) NULL source_url/source_date/data_source on any displayed state row → expect 0; (b) cohort revenue rows for NASBO states → expect 0 (deleted 96-07; MN/OH/VA revenue must remain); (c) NASBO operating rows outside FY2023/FY2024 → expect 0; (d) NASBO in-window operating rows → expect 94 across 47 distinct states; (e) duplicate (muni,fy,dataset); (f) orphan budgets / orphan budget_categories; (g) numeric-garbage depth-0 category labels (regex ^-?[0-9]+$ → 0); (h) round-number-estimate detection — flag any state operating total that is a suspiciously round figure lacking a real source citation (expect 0; the 375 estimate rows were deleted 96-07).
- Basis-label probe: enumerate distinct data_source + basis-label strings cohort-wide; every NASBO row carries "NASBO State Expenditure Report — General Fund (FY<y> actual, budgetary basis)" (96 P3); MN/OH/VA carry their GAAP/ACFR basis label; 0 rows missing a basis label. This is the honest-mixed-basis check (CONTEXT watch-out).
- Negative-category probe: scan all NASBO state operating category amounts for any negative General Fund function → expect 0 (P2/D-96-08 clamp never fired, per 96-VERIFICATION.md). Record the absence explicitly.
- Fragile-link spot check: re-probe a sample (~5) of distinct state-node source_url values for HTTP 200 / live resolution (NASBO SER URL + the MN MMB / OH / VA ACFR source pages) — durable-human-page bar.
- D-97-03 operating-only revenue view: identify the frontend render path for a state node that has an operating dataset but NO revenue dataset (read the icicle/summary component that chooses Money Out / Money In tabs). Determine whether an operating-only node renders an empty/broken revenue tab. MN/OH/VA (which DO have revenue) must still render revenue. If broken → the suppression fix (hide/disable the revenue view when a node has no revenue dataset) is the candidate D-97-04 fix.
- In-phase fix policy (D-97-04): any fix is small, reproducible, idempotent, presented to Chris at a BLOCKING checkpoint and approved BEFORE applying, then re-verified. Anything larger becomes a documented follow-up, not in-phase scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cohort-wide source-chain audit probes (read-only)</name>
  <files>.planning/phases/97-verification-uat-sgfs-05/97-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/96-remaining-states-sgfs-04/96-07-LOAD-LOG.md (the 5 Phase-96 probes + expected counts to extend cohort-wide)
    - .planning/phases/96-remaining-states-sgfs-04/96-VERIFICATION.md (P1-P6 results going in; the negative-category absence)
    - .planning/phases/97-verification-uat-sgfs-05/97-CONTEXT.md (the criterion-1 hard bar; D-97-07 read-only target)
  </read_first>
  <action>
    Run the read-only cohort-audit probes (mcp__supabase-local) over all 50 state nodes and record results in 97-02-AUDIT.md: (a) NULL source_url/source_date/data_source → 0; (b) NASBO cohort revenue rows → 0 (and confirm MN/OH/VA revenue rows still present); (c) NASBO operating rows outside FY2023/FY2024 → 0; (d) NASBO in-window operating rows → 94 across 47 distinct states; (e) duplicate (muni,fy,dataset) → 0; (f) orphan budgets / orphan budget_categories → 0; (g) numeric-garbage depth-0 labels → 0; (h) round-number-estimate scan → 0. Then spot re-probe ~5 distinct state-node source_urls for HTTP 200. Flag every finding; the expected result is a fully clean cohort (all estimate/revenue/out-of-window rows already deleted 96-07).
  </action>
  <acceptance_criteria>
    - 97-02-AUDIT.md records every probe + result; 0 NULL provenance, 0 cohort revenue rows (MN/OH/VA revenue intact), 0 out-of-window NASBO operating, 94 in-window operating across 47 states
    - 0 duplicate (muni,fy,dataset); 0 orphan budgets/categories; 0 numeric-garbage labels; 0 round-number-estimate rows
    - ~5 state-node source_urls re-probed HTTP 200 (durable); the cohort = 50 nodes (47 NASBO + MN/OH/VA) confirmed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Basis-label honesty + negative-category-absence proof (read-only)</name>
  <files>.planning/phases/97-verification-uat-sgfs-05/97-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/94-extractor-policy-sgfs-01/94-01-POLICY.md (P2 negative-category render rule; P3 mandatory basis label)
    - .planning/phases/96-remaining-states-sgfs-04/96-CONTEXT.md (D-96-01 basis label, D-96-08 negative clamp)
  </read_first>
  <action>
    Enumerate distinct data_source + basis-label strings across all 50 state nodes (mcp__supabase-local); confirm every NASBO row carries the budgetary-basis label and MN/OH/VA carry their GAAP/ACFR basis label, with 0 rows missing a basis label — the mixed-basis design is labelled, not hidden (CONTEXT watch-out). Then scan all NASBO state operating category amounts for any negative General Fund function → confirm 0 (the P2/D-96-08 clamp rule exists in buildOperatingTree but never fired, per 96-VERIFICATION.md). Record both results in 97-02-AUDIT.md, with the negative-category edge explicitly logged as verified-absent (not skipped).
  </action>
  <acceptance_criteria>
    - Distinct data_source/basis-label strings enumerated; 0 state rows missing a basis label; NASBO=budgetary-basis label, MN/OH/VA=GAAP/ACFR label
    - 0 negative General Fund function amounts cohort-wide; the absence is recorded explicitly as a verified edge
    - 97-02-AUDIT.md records both the basis-label table and the negative-category probe result
  </acceptance_criteria>
</task>

<task type="checkpoint">
  <name>Task 3: D-97-03 operating-only revenue view — verify clean, fix if broken (Chris-approved)</name>
  <files>.planning/phases/97-verification-uat-sgfs-05/97-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/97-verification-uat-sgfs-05/97-CONTEXT.md (D-97-03 verify-clean-fix-if-broken; D-97-04 fix policy)
    - project_federal_tracker_ground_rules (never display unsourced/empty data — the guardrail)
    - project_flat_source_icicle_limitation (MN icicle/revenue is the differentiator that must still render)
  </read_first>
  <action>
    Read the frontend render path that chooses the Money Out (operating) / Money In (revenue) views for a state node (the icicle/summary component). Determine what an operating-only NASBO node (operating dataset present, NO revenue dataset) renders for its revenue view, and confirm MN/OH/VA (which have revenue) still render revenue correctly. Record the finding in 97-02-AUDIT.md.
    - If operating-only nodes render NO empty/broken revenue view → record PASS, no fix needed.
    - If an empty/broken revenue tab renders → propose the small, reproducible, idempotent suppression fix (hide/disable the revenue view when a node has no revenue dataset; MN/OH/VA unaffected). Present the exact diff + scope to Chris at this BLOCKING checkpoint and get approval BEFORE applying. After approval, apply the targeted fix, then re-verify (operating-only nodes show no broken revenue view; MN/OH/VA revenue still renders). Record before/after + the approval in 97-02-AUDIT.md. End the file with the SGFS-05 cohort-audit verdict.
    Any defect larger than a small idempotent fix is recorded as a documented follow-up, not applied in-phase.
  </action>
  <acceptance_criteria>
    - The operating-only revenue-view behavior is determined from the actual frontend render path and recorded (PASS, or defect + fix)
    - If a fix was applied: it was Chris-approved at the checkpoint before any write/code change, is idempotent (re-run/re-render = same result), MN/OH/VA revenue still renders, and before/after is recorded
    - 97-02-AUDIT.md ends with the SGFS-05 cohort-audit verdict (criterion 1)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Cohort-wide source chain is real + sourced + residue-free across all 50 state nodes: 0 unsourced/round-estimate rows, 0 NULL provenance, 0 cohort revenue rows (MN/OH/VA revenue intact), 0 out-of-window NASBO operating, 94 in-window operating across 47 states, 0 duplicate/orphan/residue, 0 numeric-garbage labels; sampled source_urls resolve.
- Mixed basis is labelled (honest, not hidden): 0 rows missing a basis label; NASBO=budgetary basis, MN/OH/VA=GAAP. Negative-category edge verified absent cohort-wide (0 negative GF functions).
- D-97-03 operating-only revenue view verified clean (or the one small Chris-approved idempotent suppression fix applied + re-verified); MN/OH/VA revenue still renders.
- 97-02-AUDIT.md records all probes + results + the basis-label table + the negative-category-absence proof + any fix + the SGFS-05 cohort-audit verdict.
- Live-app UAT + Chris sign-off is plan 97-03.
</verification>
</output>
