---
phase: 54-orange-county-entity-linking-enrichment
plan: "54-02"
type: execute
wave: 2
depends_on: ["54-01"]
files_modified: []
autonomous: false
requirements: [OC-04]
must_haves:
  truths:
    - "Every OC category name_key not already covered (universal NULL set or already city-scoped) receives plain-language enrichment, authored inline at $0 API cost"
    - "Enrichment depth, field richness, and universal-vs-city-scoped placement match LA County's current post-fix baseline"
    - "Generated text is strictly category-level — no city-specific names, figures, or facts ever stored in a universal (municipality_id IS NULL) record"
    - "A citizen viewing an OC city's budget sees plain-language category names + descriptions render automatically"
  artifacts:
    - path: "treasury.category_enrichment (OC rows)"
      provides: "Plain-language plain_name/short_description/description/tags/confidence for the OC gap set"
      contains: "rows on conflict (name_key, municipality_id) mirroring the enrichCategories.js write shape"
  key_links:
    - from: "treasury.category_enrichment (name_key, municipality_id)"
      to: "OC city budget_categories (name_key normalized parent|name or name)"
      via: "app reads enrichment by (name_key, this-city) OR (name_key, NULL universal)"
      pattern: "name_key = lower(trim(parent_name))|lower(trim(name)) OR lower(trim(name))"
---

<objective>
Execute Step 3 (category enrichment) of the runbook against Orange County — but per the locked
headline decision D-01, the executing agent AUTHORS the plain-language text INLINE at $0 API cost
and writes the rows directly to `treasury.category_enrichment`. The paid `enrichCategories.js`
Anthropic SDK path is NOT run. The script is read for its exact write shape only.

Purpose: Satisfy OC-04 — each OC city's budget categories carry plain-language enrichment
consistent with the LA County baseline — at zero API spend (Empowered Vote is an unfunded nonprofit
with a hard ~$5 gate and a history of overages).

Output: New `category_enrichment` rows for the OC gap set (name_keys not already covered),
upserted on conflict `(name_key, municipality_id)`, matching LA's depth/richness/placement, with
strictly category-level text (bleed-safe per D-05). No frontend work — enrichment renders automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-orange-county-entity-linking-enrichment/54-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/enrichCategories.js
@.planning/phases/54-orange-county-entity-linking-enrichment/54-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted from scripts/enrichCategories.js. REFERENCE ONLY — do NOT run its paid path. -->

category_enrichment write shape (saveEnrichment, lines 381–402) — mirror exactly:
  row = {
    name_key,                 // normalize(parent_name)|normalize(name) IF parent_name else normalize(name)
    municipality_id,          // NULL = universal (inherited by all cities); city id = that city only
    plain_name,               // 2–5 word citizen-friendly name (not the raw fund name)
    short_description,        // one sentence — what does this fund pay for?
    description,              // 2–3 sentences — what it is, why it exists, how it is funded
    tags,                     // array of 3–6 topic tags
    source,                   // script uses 'ai'; MATCH LA's actual stored value (probe LA first)
    confidence,               // 'high' | 'medium' | 'low'
    evidence_summary,         // brief note on basis/uncertainty (script maps confidence_reason here)
    generated_at,             // ISO timestamp
  }
  upsert onConflict: 'name_key,municipality_id'   (table accessed in schema `treasury`)

normalize() (lines 152–154):  (name || '').toLowerCase().trim()
  → name_key for a child category = `${lower.trim(parent_name)}|${lower.trim(name)}`; top-level = `${lower.trim(name)}`

Gap-detection logic to REPLICATE (do not run the script — re-implement the filter via probes):
  - getExistingEnrichments(muniId) (lines 236–242): the set of name_keys already enriched for a city =
    rows where municipality_id = muniId OR municipality_id IS NULL. (universal OR city-scoped)
  - getUniversalEnrichments() (lines 247–253): set of name_keys where municipality_id IS NULL.
  - getBudgetCategories(muniId) (lines 181–231): categories for budgets at fiscal_year = YEAR
    (script default YEAR=2025; for OC use the latest ByTheNumbers-loaded year — FY2024 — to match
    how the data was loaded; confirm by probe). Default DEPTH '0' = top-level (parent_id IS NULL).
  - CAFR skip (lines 421–426): if a city's categories are CAFR-format, the script skips all
    (universals cover CAFR). OC ByTheNumbers categories are the shared SCO taxonomy — confirm format.

The GAP SET to author = for each OC city, the name_keys present in its budget_categories that are
NOT in getExistingEnrichments(thatCity). After authoring a universal row, that name_key is covered
for ALL remaining cities — author universals first to maximize reuse, then any city-specific remainder.

DB access (production Treasury DB ONLY — NOT mcp__supabase-local):
  repo .env → SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY / SUPABASE_SERVICE_KEY. Service key required
  (anon key does not bypass RLS — writes fail silently).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the OC enrichment gap set is authored INLINE at $0 — no paid API call.</decision>
  <context>
    D-01 (locked) requires the executing agent to author enrichment text itself inline (no billed
    @anthropic-ai/sdk calls) and write rows directly to category_enrichment. This checkpoint exists
    so that BEFORE authoring, the operator confirms the computed gap-set size and the $0 plan, and so
    that the run cannot silently drift onto the paid path. Empowered Vote has a hard ~$5 gate and a
    history of overages. This task computes the gap set and the LA baseline, then PAUSES.
  </context>
  <options>
    <option id="proceed-inline-zero-cost">
      <name>Proceed: author the computed gap set inline at $0</name>
      <pros>Zero API spend; honors D-01; full control over bleed-safety (D-05) and LA-consistency (D-04).</pros>
      <cons>Agent context time spent authoring text per name_key.</cons>
    </option>
    <option id="stop-needs-paid-api">
      <name>STOP — some portion genuinely cannot be authored inline</name>
      <pros>Surfaces any case needing a paid call before spending.</pros>
      <cons>Requires explicit user approval before ANY spend, even under the $5 gate.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/54-orange-county-entity-linking-enrichment/54-CONTEXT.md (D-01, D-03, D-04, D-05 — read in full)
    - scripts/enrichCategories.js (lines 152–154 normalize, 181–253 gap/skip logic, 319–348 output schema, 381–402 write shape, 421–426 CAFR skip)
    - docs/socal-county-onboarding.md (Step 3)
  </read_first>
  <action>
    Do NOT run enrichCategories.js. Using DB probes against the production Treasury DB (repo .env
    service key, schema `treasury`):
    (1) Inspect LA County's CURRENT category_enrichment records (D-04 baseline): the LA cities'
    enriched name_keys, which are universal (municipality_id IS NULL) vs city-scoped, the depth/level
    enriched, the field richness (which of plain_name/short_description/description/tags/confidence/
    evidence_summary are populated), and the exact `source` value LA stores. Record this as the bar.
    (2) Determine the OC enrichment fiscal year (latest ByTheNumbers-loaded year, expected FY2024 —
    confirm via budgets probe) and confirm OC categories are the shared SCO taxonomy (not CAFR).
    (3) Compute the OC GAP SET: for each linked OC city, list its budget_categories name_keys at the
    chosen year/depth, subtract name_keys already in getExistingEnrichments(thatCity) = (city-scoped
    OR universal NULL). Report the distinct gap-set size, how many are reusable generic SCO names
    (eligible for universal placement) vs any with city specifics (must be city-scoped per D-05),
    and confirm the entire set can be authored inline (cost = $0).
    Then PAUSE for the operator to pick an option. If any item genuinely requires a paid API call,
    select STOP and surface it — do not auto-spend.
  </action>
  <acceptance_criteria>
    - LA baseline recorded: enriched depth/level, populated field set, universal-vs-city-scoped split,
      and LA's stored `source` value — this is the consistency bar for OC.
    - OC enrichment year confirmed (latest loaded, expected FY2024) and category format confirmed
      (shared SCO taxonomy, not CAFR-skip).
    - OC gap-set computed by replicating the script's skip filter (existing = city-scoped OR universal NULL);
      distinct gap-set name_key count reported, plus universal-eligible vs city-scoped-required counts.
    - Plan confirmed authorable inline at $0; no @anthropic-ai/sdk / ANTHROPIC_API_KEY call planned.
    - Operator selects an option; if STOP, the specific item needing paid spend is surfaced for approval.
  </acceptance_criteria>
  <resume-signal>Select: proceed-inline-zero-cost (confirms $0 inline authoring) or stop-needs-paid-api (describe the item needing spend).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the gap-set enrichment inline ($0) and upsert into category_enrichment</name>
  <files>(no files written — DB writes to treasury.category_enrichment only)</files>
  <read_first>
    - .planning/phases/54-orange-county-entity-linking-enrichment/54-CONTEXT.md (D-02 write shape, D-04 match LA, D-05 bleed-safety — read in full)
    - scripts/enrichCategories.js (lines 381–402 saveEnrichment write shape, 319–346 field semantics)
    - The LA-baseline record + OC gap set computed in Task 1
  </read_first>
  <action>
    Do NOT run enrichCategories.js or make any billed API call. For each name_key in the OC gap set
    computed in Task 1, the agent AUTHORS the text itself inline:
    plain_name (2–5 words), short_description (one sentence), description (2–3 sentences: what the
    fund/category is, why it exists, how it is funded), tags (3–6), confidence, evidence_summary.
    Match LA's depth, field richness, and placement convention (D-04). Set `source` to the same value
    LA stores (probed in Task 1) — an honest agent-authored marker, kept consistent with LA's records.
    Compute name_key exactly as the script does: `lower(trim(parent_name))|lower(trim(name))` for a
    child, `lower(trim(name))` for top-level.
    Placement (D-05 — HARD): generic SCO-taxonomy category names (uniform statewide) → write as
    UNIVERSAL (municipality_id IS NULL) so all 34 cities inherit them. Author universals FIRST to
    maximize reuse. Any text that would reference a city-specific name/figure/fact MUST be written
    CITY-SCOPED (municipality_id = that city's id) — and the text itself must still describe the
    category generically. NEVER store city-specific content in a universal record (this caused a
    prior production bleed bug). Strictly category-level text only — no city names, no dollar figures,
    no city-specific facts in any record.
    Upsert each row into treasury.category_enrichment on conflict `(name_key, municipality_id)`
    via the service key, mirroring the script's write shape exactly. Idempotent: re-running upserts
    the same rows without duplication.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data:bad}=await s.from('category_enrichment').select('name_key').is('municipality_id',null).limit(5000);console.log('universal rows present:',(bad||[]).length);process.exit(0);})"</automated>
  </verify>
  <acceptance_criteria>
    - Every OC gap-set name_key now resolves enrichment via (name_key, city) OR (name_key, NULL):
      a probe per linked OC city returns 0 budget_categories name_keys lacking coverage.
    - Authored rows mirror the script write shape: name_key (correctly normalized), municipality_id,
      plain_name (2–5 words), short_description (1 sentence), description (2–3 sentences), tags (3–6),
      source (= LA's value), confidence, evidence_summary, generated_at.
    - D-05 bleed-safety: every universal (municipality_id IS NULL) row authored in this task contains
      NO city-specific names, dollar figures, or city facts — text is strictly category-level. Any
      city-specific content lives only in a city-scoped row.
    - $0 cost: no @anthropic-ai/sdk import executed, no ANTHROPIC_API_KEY call made; enrichCategories.js not run.
    - Re-running the upsert produces no duplicate rows (idempotent on (name_key, municipality_id)).
  </acceptance_criteria>
  <done>
    OC gap-set enriched inline at $0; every linked OC city's categories resolve enrichment; universals
    are strictly category-level (bleed-safe); rows match LA's shape/depth/placement; upsert is idempotent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify OC enrichment coverage + bleed-safety (Step 4 enrichment verification)</name>
  <files>(no files written — DB read-only probes)</files>
  <read_first>
    - .planning/phases/54-orange-county-entity-linking-enrichment/54-CONTEXT.md (D-04, D-05)
    - docs/socal-county-onboarding.md (Step 4)
    - The LA-baseline record from Task 1 (consistency target)
  </read_first>
  <action>
    Run read-only DB probes against the production Treasury DB (schema `treasury`) to prove OC-04:
    (1) Coverage — for each linked OC city, every budget_categories name_key at the enrichment
    year/depth resolves to a category_enrichment row via (name_key, city) OR (name_key, NULL);
    report 0 uncovered.
    (2) Consistency with LA (D-04) — OC enriched depth, populated-field set, and universal-vs-city
    placement match the LA baseline captured in Task 1.
    (3) Bleed-safety (D-05) — scan all universal (municipality_id IS NULL) rows touched by this phase
    for city-specific tokens (city names from the OC set, dollar-sign figures, city-specific proper
    nouns); assert none are present. This mirrors the Phase 33 verifier pattern that checks CA
    universals contain no city-specific text.
    (4) Spot-check 2–3 representative gap-set categories: confirm plain_name is citizen-friendly and
    the description is generic to the SCO fund/category (no city specifics).
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data}=await s.from('category_enrichment').select('name_key,plain_name,description').is('municipality_id',null).limit(5000);const dollarLeak=(data||[]).filter(r=>/\$\s?\d/.test((r.description||'')+(r.plain_name||''))).map(r=>r.name_key);console.log('universal rows:',(data||[]).length,'| dollar-figure leaks in universals:',dollarLeak.length, dollarLeak.slice(0,10));process.exit(dollarLeak.length?1:0);})"</automated>
  </verify>
  <acceptance_criteria>
    - Coverage probe: every linked OC city has 0 budget_categories name_keys (at the enrichment
      year/depth) lacking a category_enrichment match via (name_key, city) OR (name_key, NULL).
    - Consistency probe: OC enriched depth + populated-field set + placement convention match the LA baseline.
    - Bleed-safety probe: 0 universal rows contain a city name from the OC set, a dollar-amount figure,
      or city-specific facts (automated check exits 0 — no dollar-figure leak in universals).
    - Spot-check: 2–3 sampled enrichment rows have citizen-friendly plain_name and strictly
      category-level description (recorded in the SUMMARY).
  </acceptance_criteria>
  <done>
    OC-04 proven: every linked OC city's categories carry enrichment consistent with LA; universals
    are strictly category-level with no city-specific bleed; spot-checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored text → production category_enrichment | The agent writes plain-language rows to a production table via the service key. Bad placement (city-specific text in a universal) bleeds across all cities. |
| repo .env service key → production Treasury DB | Writes bypass RLS; wrong DB target or anon key would mis-write or silently fail. |
| (forbidden) paid Anthropic SDK path | enrichCategories.js can spend money; D-01 forbids running it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06 | Tampering / Info disclosure | Universal (municipality_id IS NULL) enrichment rows | mitigate | D-05 enforced as task acceptance criteria: universals contain ONLY category-level text; city-specific content is city-scoped. Task 3 automated probe fails the build if any universal row carries a dollar figure; manual scan checks city-name/proper-noun tokens. Mirrors the Phase 33 universal-bleed verifier and the fix in project_enrichment_scoping_fix. |
| T-54-07 | Elevation of cost / Spoofing intent | Accidental run of the paid enrichCategories.js path | mitigate | D-01: the paid path is never run. The plan is gated by a blocking checkpoint:decision confirming $0 inline authoring; the run uses no ANTHROPIC_API_KEY. autonomous:false. Any item needing paid spend triggers STOP for explicit approval (no auto-spend under the $5 gate). |
| T-54-08 | Tampering | Existing LA / other-city enrichment + already-covered OC name_keys | mitigate | Gap filter (existing = city-scoped OR universal NULL) authored only the uncovered set; upsert on (name_key, municipality_id) is idempotent and does not clobber unrelated keys. |
| T-54-09 | Information disclosure | Wrong DB target (social DB) | mitigate | Use repo .env service key against prod Treasury DB only; mcp__supabase-local MUST NOT be used. Service key required (anon fails silently). |
| T-54-SC | Tampering | npm package installs | accept | No new package installs — @supabase/supabase-js already present; no `npm install` step. The forbidden @anthropic-ai/sdk path is not invoked. |
</threat_model>

<verification>
- Gap-set computed by replicating the script's skip filter (existing = city-scoped OR universal NULL); confirmed $0 inline.
- Coverage probe: every linked OC city has 0 uncovered category name_keys.
- Bleed-safety probe: 0 universal rows carry city-specific tokens / dollar figures (automated check exits 0).
- Consistency: OC depth/fields/placement match the LA baseline.
- Spot-check: sampled rows are citizen-friendly + strictly category-level.
</verification>

<success_criteria>
- Every OC gap-set name_key is enriched inline at $0 — no paid API call, enrichCategories.js not run (OC-04, D-01).
- Rows mirror the enrichCategories.js write shape and match LA's depth/richness/placement (D-02, D-04).
- All universal rows are strictly category-level — no city-specific bleed (D-05).
- Every linked OC city's categories resolve enrichment via (name_key, city) OR (name_key, NULL).
</success_criteria>

<output>
Create `.planning/phases/54-orange-county-entity-linking-enrichment/54-02-SUMMARY.md` when done.
</output>
