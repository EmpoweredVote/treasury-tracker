---
phase: 17-portland-or-budget-load
plan: 04
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Portland FY2025 and FY2026 operating budget rows exist in treasury.budgets"
    - "D-06: enrichCategories.js is run for Portland with a cost estimate produced before the live enrich (under the $5/run threshold)"
    - "Portland category_enrichment rows have plain_name values for the loaded fiscal years"
    - "A user can select Portland, OR in the app and see operating budget data with per-capita context and enriched category descriptions"
  artifacts:
    - path: ".planning/phases/17-portland-or-budget-load/17-VERIFICATION.md"
      provides: "Verification checklist confirming Portland data is live and correct"
  key_links:
    - from: "treasury.budgets"
      to: "Portland municipality_id"
      via: "data_source_id → municipality_id join"
      pattern: "municipality_id"
---

<objective>
Perform the live data load: run the Portland operating budget loader against the real DB, run category enrichment, and verify Portland appears correctly in the app with per-capita and enriched descriptions. Produce 17-VERIFICATION.md following the Phase 16 pattern.

Purpose: Plans 01-03 built and dry-ran the pipeline; this plan commits the data and confirms the goal is met end to end. The enrichment run incurs API cost (estimated well under the $5 threshold per RESEARCH) but a human checkpoint confirms cost before the AI run.

Output: Live Portland budget + enrichment data, plus 17-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-portland-or-budget-load/17-RESEARCH.md
@.planning/phases/16-california-cities-budget-load/16-VERIFICATION.md
@.planning/phases/17-portland-or-budget-load/17-01-SUMMARY.md
@.planning/phases/17-portland-or-budget-load/17-02-SUMMARY.md
@.planning/phases/17-portland-or-budget-load/17-03-SUMMARY.md
@scripts/enrichCategories.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load Portland operating budget for FY2025 and FY2026</name>
  <read_first>
    - scripts/processPortland.js (the loader being run live — confirm it writes to treasury.budgets via treasury_sync_budget_tree)
    - .planning/phases/17-portland-or-budget-load/17-02-SUMMARY.md (the dry-run per-FY totals and bureau counts to validate the live load against)
    - .planning/phases/16-california-cities-budget-load/16-VERIFICATION.md (the verification query pattern to replicate)
  </read_first>
  <action>
    Run a final dry-run to confirm the expected totals: `node scripts/processPortland.js --dry-run`. Compare against the totals recorded in 17-02-SUMMARY.

    Run the live load: `node scripts/processPortland.js` (no --dry-run). This calls treasury_sync_budget_tree for FY2025 and FY2026 operating data. The loader deletes-then-inserts per data_source_id + fiscal_year, so it is idempotent — run it a second time and confirm row counts are unchanged.

    Verify in the DB:
    - `SELECT fiscal_year, dataset_type, total_budget FROM treasury.budgets b JOIN treasury.data_sources d ON b.data_source_id=d.id JOIN treasury.municipalities m ON d.municipality_id=m.id WHERE m.name='Portland' AND m.state='OR' ORDER BY fiscal_year;` returns operating rows for 2025 and 2026.
    - The FY2026 total is in the expected range for Portland's program/operating budget (RESEARCH reports program expenses ~$4.49B for the full city; the bureau-summed operating total should be a plausible billions-scale figure, NOT a sub-million figure that would indicate a thousands-vs-dollars error).

    Record actual loaded totals and bureau counts for the VERIFICATION doc.
  </action>
  <verify>
    <automated>node scripts/processPortland.js</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processPortland.js` exits 0
    - `treasury.budgets` contains operating rows for Portland (state OR) with fiscal_year 2025 AND 2026
    - Re-running the loader leaves the budget row count unchanged (idempotent)
    - FY2026 operating total is a billions-scale figure (sanity check against full-dollar amounts; not off by 1000x)
  </acceptance_criteria>
  <done>Portland FY2025 and FY2026 operating budgets are loaded and idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Run category enrichment for Portland (cost estimate first)</name>
  <read_first>
    - scripts/enrichCategories.js (the --city/--state/--year flags and --dry-run support; how it counts categories to enrich)
    - .planning/phases/17-portland-or-budget-load/17-RESEARCH.md ("Enrichment Cost Estimate" — expected ~20-35 categories, cost well under $5)
  </read_first>
  <action>
    First, dry-run to count categories and estimate cost: `node scripts/enrichCategories.js --city Portland --state OR --year 2026 --dry-run` and the same for --year 2025. Compute the cost estimate from the category count (RESEARCH: ~$0.25/1M tokens, ~1000 tokens/call) and record it. Per the $5/run threshold (and feedback_api_cost_threshold), the estimate should be well under $1 — if the estimate unexpectedly exceeds $5, STOP and surface to the user before running.

    Run enrichment live for both fiscal years:
    - `node scripts/enrichCategories.js --city Portland --state OR --year 2026`
    - `node scripts/enrichCategories.js --city Portland --state OR --year 2025`
    Enrichment is idempotent via name_key upsert scoped to municipality_id, so re-runs are safe.

    Verify: `SELECT COUNT(*), COUNT(plain_name) FROM treasury.category_enrichment ce JOIN treasury.municipalities m ON ce.municipality_id=m.id WHERE m.name='Portland' AND m.state='OR';` shows enriched rows with non-null plain_name, scoped to Portland's municipality_id (confirm no NULL municipality_id rows were created — see project memory on enrichment scoping fix).
  </action>
  <verify>
    <automated>node scripts/enrichCategories.js --city Portland --state OR --year 2026 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run prints a category count and an estimated cost recorded in the SUMMARY
    - After the live run, `category_enrichment` has rows with non-null `plain_name` scoped to Portland's municipality_id
    - No category_enrichment rows for Portland have a NULL municipality_id (avoids the cross-city bleed bug noted in project memory)
  </acceptance_criteria>
  <done>Portland categories have AI-enriched plain-language descriptions, scoped correctly to Portland.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Portland, OR operating budget (FY2025 + FY2026) loaded into treasury.budgets, population 635749 set for per-capita display, categories AI-enriched, and the city picker now labels OR as "Oregon".
  </what-built>
  <how-to-verify>
    1. Start the app locally (e.g. `npm run dev`) or open treasurytracker.empowered.vote if the load targeted production.
    2. Open the city/entity picker. Confirm Portland appears under "Oregon" (not "OR").
    3. Select Portland, OR. Confirm operating budget data renders with bureau/service-area categories.
    4. Confirm a per-capita figure displays (population 635,749 applied).
    5. Confirm category descriptions show enriched plain-language text (not raw bureau codes).
    6. Switch fiscal years and confirm both FY2025 and FY2026 are selectable with data.
  </how-to-verify>
  <resume-signal>Type "approved" if Portland displays correctly, or describe what is wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 17-VERIFICATION.md</name>
  <read_first>
    - .planning/phases/16-california-cities-budget-load/16-VERIFICATION.md (the verification doc structure to replicate)
  </read_first>
  <action>
    Create .planning/phases/17-portland-or-budget-load/17-VERIFICATION.md following the Phase 16 VERIFICATION.md structure. Include: the DB verification queries and their actual results (budget totals per FY, enrichment counts, population), the enrichment cost actually incurred, the working PDF URLs used, the human-verify checkpoint outcome, and any follow-ups (revenue budget deferred per D-03; any tech-debt notes). Confirm the goal statement from ROADMAP Phase 17 is met.
  </action>
  <verify>
    <automated>node -e "process.exit(require('fs').existsSync('.planning/phases/17-portland-or-budget-load/17-VERIFICATION.md')?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/17-portland-or-budget-load/17-VERIFICATION.md exists
    - It records actual FY2025/FY2026 budget totals, enrichment counts, population value, and enrichment cost
    - It notes revenue budget as a deferred follow-up (D-03)
    - It states whether the Phase 17 ROADMAP goal is met
  </acceptance_criteria>
  <done>A verification record exists documenting the live Portland data and confirming the phase goal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enrichCategories.js → Anthropic API | AI calls cost money; cost gated by estimate + $5 threshold rule |
| loader → Supabase | Live writes via service key (existing trusted pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Denial of Service (cost) | enrichCategories.js Anthropic spend | mitigate | Dry-run cost estimate before live run; stop-and-surface if > $5 (feedback_api_cost_threshold) |
| T-17-08 | Integrity | enrichment NULL municipality_id bleed | mitigate | Verify all Portland enrichment rows have non-null municipality_id (per project memory enrichment-scoping fix) |
</threat_model>

<verification>
- Portland operating budget rows exist for FY2025 and FY2026 (billions-scale totals)
- Portland category_enrichment rows have non-null plain_name, correctly scoped municipality_id
- Portland population 635749 applied; per-capita renders in app
- City picker shows "Oregon"; human-verify checkpoint approved
- 17-VERIFICATION.md created
</verification>

<success_criteria>
Citizens can select Portland, OR in the app and view AI-enriched operating budget data with per-capita context for FY2025 and FY2026 — the Phase 17 goal — with a verification record on file.
</success_criteria>

<output>
Create `.planning/phases/17-portland-or-budget-load/17-04-SUMMARY.md` and `.planning/phases/17-portland-or-budget-load/17-VERIFICATION.md` when done.
</output>
