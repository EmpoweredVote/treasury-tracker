---
phase: 17-portland-or-budget-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedPortlandOregon.js
  - src/components/EntitySwitcher.tsx
  - docs/Portland/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Python 3 and pdfplumber are confirmed available in the environment"
    - "Portland FY2024-25 and FY2025-26 Adopted Budget Vol 1 PDFs are downloaded to docs/Portland/"
    - "pdfplumber extract_tables output for the bureau-summary pages is captured so the extractor logic can be written deterministically"
    - "Portland, OR exists as a municipality row with population 635749, population_year 2024"
    - "An operating data_source row exists for Portland with api_type pdf_download"
    - "City picker shows 'Oregon' (not 'OR') for Portland"
  artifacts:
    - path: "scripts/seedPortlandOregon.js"
      provides: "Idempotent Portland municipality + operating data_source seeder"
      min_lines: 60
    - path: "docs/Portland/"
      provides: "Downloaded FY2025 + FY2026 Vol 1 budget PDFs"
    - path: "src/components/EntitySwitcher.tsx"
      provides: "OR: 'Oregon' STATE_LABELS entry"
      contains: "OR: 'Oregon'"
  key_links:
    - from: "scripts/seedPortlandOregon.js"
      to: "treasury.municipalities"
      via: "supabase.schema('treasury').from('municipalities')"
      pattern: "from\\('municipalities'\\)"
    - from: "scripts/seedPortlandOregon.js"
      to: "treasury.data_sources"
      via: "supabase.schema('treasury').from('data_sources')"
      pattern: "from\\('data_sources'\\)"
---

<objective>
Establish the Portland, OR foundation: verify the PDF toolchain, download and inspect the two Adopted Budget Volume 1 PDFs, seed the Portland municipality + operating data_source row, and add the Oregon state label to the city picker.

Purpose: Plan 02 (extractor) cannot be written deterministically until the real PDF table structure is inspected (RESEARCH Open Question 2; PATTERNS note 2). Plan 02 and Plan 03 both require the Portland municipality row to exist first (FK dependency). This plan removes those blockers in one wave.

Output: docs/Portland/ with two PDFs, captured pdfplumber table output, a Portland municipality row, an operating data_source row, and the EntitySwitcher label fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-portland-or-budget-load/17-RESEARCH.md
@.planning/phases/17-portland-or-budget-load/17-PATTERNS.md
@scripts/seedCaliforniaCities.js
@scripts/processFremont.js
@src/components/EntitySwitcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify toolchain, download PDFs, and capture pdfplumber table structure</name>
  <read_first>
    - scripts/extractFremont.py (the pdfplumber pattern that extractPortland.py will adapt — how extract_tables/extract_text is used)
    - scripts/processFremont.js (lines 302-309 — docs/Fremont/ directory discovery convention to replicate for docs/Portland/)
    - .planning/phases/17-portland-or-budget-load/17-RESEARCH.md ("Data Source Details" section — the two PDF URLs)
  </read_first>
  <action>
    Confirm Python 3 and pdfplumber are present by running `python --version` (or `python3 --version`) and `python -c "import pdfplumber; print(pdfplumber.__version__)"`. If pdfplumber is missing, run `pip install pdfplumber` and record this in the SUMMARY.

    Create the directory docs/Portland/. Download both Adopted Budget Volume 1 PDFs into it using the URLs from RESEARCH "Data Source Details":
    - FY 2025-26: https://www.portland.gov/budget/documents/fy-2025-26-city-portland-adopted-budget-vol-1-city-summaries-and-bureau/download → save as docs/Portland/fy2025-26-vol1.pdf
    - FY 2024-25: https://www.portland.gov/budget/2024-2025-budget/documents/fy-2024-25-volume-1-city-portland-city-summaries-and-bureau/download → save as docs/Portland/fy2024-25-vol1.pdf

    Verify each download returned a real PDF (content starts with %PDF, file size matches RESEARCH: ~6.39 MB and ~5.07 MB). RESEARCH Pitfall 2 warns Portland CMS URLs are unstable — if either returns 404 or non-PDF content-type, locate the current URL from portland.gov/budget and record the working URL in the SUMMARY.

    Write a throwaway inspection script (e.g. scripts/_inspect-portland-temp.py — delete or leave clearly marked as temporary) that opens fy2025-26-vol1.pdf with pdfplumber and prints, for the pages containing the bureau/service-area summary tables, the output of page.extract_tables() and page.extract_text(). Capture which pages hold the bureau-level "Adopted" dollar tables and whether extract_tables() yields clean rows (bureau name + adopted amount) or requires text-line parsing. This captured structure is the input Plan 02 needs — record a concise summary (page numbers, column order, sample rows) in the SUMMARY under a "PDF Structure Findings" heading.

    Note for downstream: Portland amounts are in FULL DOLLARS (not thousands) per PATTERNS note 5 — do NOT plan a multiply-by-1000 step. Portland fiscal year "FY 2025-26" maps to integer fiscal_year=2026; "FY 2024-25" maps to 2025 (PATTERNS note 4).
  </action>
  <verify>
    <automated>python -c "import pdfplumber; print('ok')" && node -e "const fs=require('fs');const f1='docs/Portland/fy2025-26-vol1.pdf';const f2='docs/Portland/fy2024-25-vol1.pdf';if(!fs.existsSync(f1)||!fs.existsSync(f2))process.exit(1);const h=fs.readFileSync(f1).subarray(0,4).toString();if(h!=='%PDF')process.exit(1);console.log('PDFs present and valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import pdfplumber"` exits 0
    - docs/Portland/fy2025-26-vol1.pdf and docs/Portland/fy2024-25-vol1.pdf both exist and begin with the bytes %PDF
    - SUMMARY contains a "PDF Structure Findings" section naming the page numbers of the bureau summary tables and a sample of extracted rows (bureau name + adopted dollar amount)
    - SUMMARY records whether extract_tables() yields clean rows or text-line parsing is required
  </acceptance_criteria>
  <done>Both PDFs are on disk and valid, pdfplumber is confirmed importable, and the bureau-table structure is documented for Plan 02.</done>
</task>

<task type="auto">
  <name>Task 2: Query api_type values, write seedPortlandOregon.js, add Oregon to STATE_LABELS</name>
  <read_first>
    - scripts/seedCaliforniaCities.js (the seeder pattern: Supabase client init, upsertMunicipality select-then-insert/update, upsertDataSourceByName, treasury_list_source_ids verification — replicate exactly for a single Portland municipality)
    - scripts/processFremont.js (line 155 confirms api_type 'pdf_download' and line 167 the filter — Portland must match this value)
    - src/components/EntitySwitcher.tsx (lines 21-25 — current STATE_LABELS map)
  </read_first>
  <action>
    Before seeding, query the database to confirm the api_type Portland should use: run `SELECT DISTINCT api_type FROM treasury.data_sources;` (via the supabase MCP execute_sql tool or a one-off node script). Confirm 'pdf_download' is the value used by Fremont's PDF sources and use it for Portland (RESEARCH Open Question 1; PATTERNS note 3). If 'pdf_download' is absent, use the exact string the existing PDF sources use.

    Create scripts/seedPortlandOregon.js adapting scripts/seedCaliforniaCities.js. Single municipality entry: { name: 'Portland', state: 'OR', entity_type: 'city', population: 635749, population_year: 2024 }. Use the analog's upsertMunicipality (select on name+state via .maybeSingle(), update if exists else insert). Chain .schema('treasury') on every query per the Fremont schema-scoping convention.

    Seed ONE operating data_source row (revenue is out of scope per D-03). Use a factory like PORTLAND_OPERATING(municipalityId) returning:
    - name: 'Portland Operating Budget'
    - api_type: 'pdf_download'
    - dataset_type: 'operating'
    - base_url: the working FY2025-26 Vol 1 URL confirmed in Task 1
    - dataset_id: 'portland_adopted_budget_vol1'
    - fiscal_years: [2025, 2026]
    - municipality_id: municipalityId
    Insert the municipality FIRST, then the data_source (FK ordering). Use the analog's upsertDataSourceByName select-then-insert/update pattern for idempotency. End with a treasury_list_source_ids verification block that asserts 'Portland Operating Budget' is present and prints its api_type and dataset_type.

    Note: processPortland.js (Plan 02) creates per-fiscal-year data_source rows of its own; this seeder establishes the municipality + a base operating source row. Keep the seeder idempotent so re-running after Plan 02 does not duplicate.

    In src/components/EntitySwitcher.tsx, add the line `OR: 'Oregon',` to the STATE_LABELS map immediately after the `TX: 'Texas',` entry. This is the only change to that file (D-07, same pattern as commit 4b26866).
  </action>
  <verify>
    <automated>node scripts/seedPortlandOregon.js && grep -c "OR: 'Oregon'" src/components/EntitySwitcher.tsx</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seedPortlandOregon.js contains the string "pdf_download" and "Portland Operating Budget"
    - Running `node scripts/seedPortlandOregon.js` exits 0 and prints "OK: Portland Operating Budget" with api_type=pdf_download
    - Re-running `node scripts/seedPortlandOregon.js` a second time exits 0 and does not create a duplicate (verified via treasury_list_source_ids showing a single Portland Operating Budget row)
    - `SELECT id FROM treasury.municipalities WHERE name='Portland' AND state='OR'` returns exactly one row with population=635749
    - src/components/EntitySwitcher.tsx contains `OR: 'Oregon'` (grep count >= 1)
  </acceptance_criteria>
  <done>Portland municipality and operating data_source rows exist and are idempotent; the city picker labels OR as Oregon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portland.gov → local disk | Public HTTPS PDF download, no auth, untrusted file content parsed by pdfplumber |
| script → Supabase (service key) | Writes via SUPABASE_SERVICE_KEY, existing trusted pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | Downloaded PDF (wrong/404 content served by CMS) | mitigate | Verify %PDF magic bytes and file size before use (Task 1 verify); record working URL |
| T-17-02 | Information Disclosure | SUPABASE_SERVICE_KEY in scripts | accept | Read from process.env only, never hardcoded — matches existing loader pattern |
| T-17-SC | Tampering | pip install pdfplumber | accept | pdfplumber already required by extractFremont.py; no new package introduced beyond existing project dependency |
</threat_model>

<verification>
- `python -c "import pdfplumber; print('ok')"` succeeds
- Both PDFs present in docs/Portland/ and begin with %PDF
- `node scripts/seedPortlandOregon.js` is idempotent (run twice, no duplicate sources)
- EntitySwitcher.tsx contains `OR: 'Oregon'`
- SUMMARY documents the PDF bureau-table structure for Plan 02
</verification>

<success_criteria>
Toolchain verified, PDFs downloaded and structurally documented, Portland municipality + operating data_source seeded idempotently, and Oregon added to the city picker labels.
</success_criteria>

<output>
Create `.planning/phases/17-portland-or-budget-load/17-01-SUMMARY.md` when done. MUST include a "PDF Structure Findings" section (page numbers, column order, sample bureau rows, whether extract_tables() is clean) and the confirmed api_type value and working PDF URLs.
</output>
