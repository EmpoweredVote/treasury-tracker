---
phase: 17-portland-or-budget-load
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scripts/extractPortland.py
  - scripts/processPortland.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "extractPortland.py emits JSON of bureau-level budget rows (bureau, service_area, adopted_amount, fiscal_year) from a Portland Vol 1 PDF"
    - "processPortland.js builds a budget tree and loads it via treasury_sync_budget_tree for both FY2025 and FY2026"
    - "Portland amounts are stored in full dollars (no thousands multiplication)"
    - "Running the loader twice does not duplicate budget rows (idempotent)"
  artifacts:
    - path: "scripts/extractPortland.py"
      provides: "pdfplumber bureau-table extractor outputting JSON to stdout"
      min_lines: 50
    - path: "scripts/processPortland.js"
      provides: "Node loader: runs Python extractor, builds tree, calls treasury_sync_budget_tree"
      min_lines: 80
  key_links:
    - from: "scripts/processPortland.js"
      to: "scripts/extractPortland.py"
      via: "execSync python invocation + JSON.parse"
      pattern: "execSync.*extractPortland"
    - from: "scripts/processPortland.js"
      to: "treasury_sync_budget_tree"
      via: "supabase.rpc"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Build the Portland PDF extraction + load pipeline: a pdfplumber Python extractor and a Node.js loader that calls treasury_sync_budget_tree, following the Fremont pattern. Verify end-to-end with --dry-run; do NOT perform the final live DB load here (Plan 04 owns the live load + verification).

Purpose: This is the core data pipeline. It must be written against the real PDF structure documented in 17-01-SUMMARY, not assumptions (RESEARCH Open Question 2).

Output: scripts/extractPortland.py and scripts/processPortland.js, validated against the downloaded PDFs via dry-run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-portland-or-budget-load/17-RESEARCH.md
@.planning/phases/17-portland-or-budget-load/17-PATTERNS.md
@.planning/phases/17-portland-or-budget-load/17-01-SUMMARY.md
@scripts/extractFremont.py
@scripts/processFremont.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write extractPortland.py (pdfplumber bureau-table extractor)</name>
  <read_first>
    - scripts/extractFremont.py (parse_money, parse_fy, col_type, extract_budget, the json.dumps stdout convention — replicate structure)
    - .planning/phases/17-portland-or-budget-load/17-01-SUMMARY.md ("PDF Structure Findings" — the actual page numbers, column order, and sample rows from the Portland Vol 1 PDF; this drives the detection logic)
    - docs/Portland/fy2025-26-vol1.pdf (the target PDF — inspect directly if SUMMARY findings are insufficient)
  </read_first>
  <action>
    Create scripts/extractPortland.py adapting scripts/extractFremont.py. Reuse parse_money (handles $, commas, parentheses-as-negative). Implement parse_fy for Portland's "FY YYYY-YY" format: strip the "FY " prefix and return the ENDING year as an integer (e.g. "FY 2025-26" → 2026, "FY 2024-25" → 2025) per PATTERNS note 4 and RESEARCH Pitfall 1.

    Implement extract_budget(pdf_path) to walk pdf.pages and, on the bureau-summary pages identified in 17-01-SUMMARY, extract for each bureau: bureau name, service area (the parent grouping the bureau falls under), and the Adopted dollar amount. Use page.extract_tables() if the SUMMARY found clean tables; otherwise use text-line parsing per the SUMMARY's findings. Amounts are FULL DOLLARS — do NOT multiply by 1000 (PATTERNS note 5; Fremont's toFullDollars is for thousands-denominated data and must NOT be ported).

    Output JSON to stdout (json.dumps(data, indent=2)) as a list of objects: { "bureau": str, "service_area": str, "adopted_amount": int, "fiscal_year": int, "page_num": int }. Match the stdout/argv usage convention from extractFremont.py: usage error to stderr + sys.exit(1) when no PDF arg given.

    Guard against RESEARCH Pitfall 3 (merged cells / Nones): skip rows where adopted_amount parses to 0 or bureau is empty/numeric, and emit those skipped lines to stderr so the dry-run can surface extraction gaps.
  </action>
  <verify>
    <automated>python scripts/extractPortland.py "docs/Portland/fy2025-26-vol1.pdf" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const a=JSON.parse(s);if(!Array.isArray(a)||a.length===0)process.exit(1);const r=a[0];if(typeof r.bureau!=='string'||typeof r.adopted_amount!=='number'||r.fiscal_year!==2026)process.exit(1);console.log('rows='+a.length+' sample='+JSON.stringify(r))})"</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/extractPortland.py "docs/Portland/fy2025-26-vol1.pdf"` prints a JSON array with length > 0
    - Each element has string `bureau`, string `service_area`, integer `adopted_amount`, integer `fiscal_year`
    - fiscal_year equals 2026 for the FY2025-26 PDF (NOT 202526)
    - No element has adopted_amount of 0 or an empty bureau (skipped rows go to stderr)
    - extractPortland.py does NOT contain a multiply-by-1000 / toFullDollars conversion
  </acceptance_criteria>
  <done>The Python extractor produces clean bureau-level JSON in full dollars with correct integer fiscal years.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write processPortland.js (Node loader → treasury_sync_budget_tree) and dry-run validate</name>
  <read_first>
    - scripts/processFremont.js (extractPDF execSync invocation, ensureMunicipality lookup, upsertDataSource, loadFiscalYear with treasury_sync_budget_tree RPC call, parseArgs CLI + docs/Fremont/ discovery — replicate all of these)
    - scripts/extractPortland.py (the JSON schema this loader consumes: bureau, service_area, adopted_amount, fiscal_year, page_num)
    - .planning/phases/17-portland-or-budget-load/17-01-SUMMARY.md (confirmed api_type value 'pdf_download' and the working PDF URLs)
  </read_first>
  <action>
    Create scripts/processPortland.js adapting scripts/processFremont.js. Set up the Supabase client from process.env (SUPABASE_URL with the project fallback, SUPABASE_SERVICE_KEY/SUPABASE_SERVICE_ROLE_KEY), exit(2) if key missing. Chain .schema('treasury') on every query (RPC needs no schema).

    extractPDF(pdfPath): execSync `python "<ROOT>/scripts/extractPortland.py" "<pdfPath>"` with maxBuffer 8*1024*1024, encoding utf8, JSON.parse the result.

    ensureMunicipality(): look up Portland by name='Portland', state='OR'; if missing, error "run seedPortlandOregon.js first" and exit(2).

    Build the budget tree from extracted rows. Tree node shape required by treasury_sync_budget_tree (per PATTERNS): top-level nodes are { n: <Bureau name>, a: <adopted_amount full dollars>, i: [] }. Group bureaus under their service_area: emit one parent node per service area with child line items, OR use bureau as the top node with service_area carried as grouping — follow the exact node shape the RPC expects as shown in processFremont.js's tree assembly. Compute the per-fiscal-year total as the sum of node amounts and rowCount as the node count.

    upsertDataSource(muniId, fiscalYear, 'operating'): name `Portland Operating Budget FY${fiscalYear}`, api_type 'pdf_download', dataset_type 'operating', dataset_id `fy${fiscalYear}`, base_url the working Vol1 URL for that FY, fiscal_years [fiscalYear], municipality_id muniId — select-then-insert/update for idempotency (Fremont pattern lines 152-179).

    loadFiscalYear: for idempotence, delete existing budgets for that data_source_id + fiscal_year, then call treasury.rpc('treasury_sync_budget_tree', { p_data_source_id, p_fiscal_year, p_dataset_type:'operating', p_total, p_tree, p_row_count, p_triggered_by:'bulk_load' }). Check both rpcErr and rpc?.error.

    CLI: parseArgs with 'dry-run' boolean (default false) and optional 'pdf' string override, strict:false. Discover PDFs from docs/Portland/ via readdirSync filtering .pdf (create-dir-not-needed; Plan 01 created it). Map each PDF to its fiscal year by filename (fy2025-26 → 2026, fy2024-25 → 2025). When --dry-run, print the assembled tree, per-FY total, and rowCount to stdout and perform NO DB writes. Do NOT run a live load in this plan — Plan 04 owns that.
  </action>
  <verify>
    <automated>node scripts/processPortland.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/processPortland.js --dry-run` exits 0 and prints a per-fiscal-year total and row count for both FY2025 and FY2026 without writing to the DB
    - scripts/processPortland.js contains "treasury_sync_budget_tree" and "pdf_download"
    - scripts/processPortland.js contains "execSync" invoking extractPortland.py
    - The dry-run output shows fiscal_year values of 2025 and 2026 (never 202425/202526)
    - The loader errors with a clear "run seedPortlandOregon.js first" message if the Portland municipality is absent
  </acceptance_criteria>
  <done>The Node loader assembles a correct full-dollar budget tree from the Python extractor and is ready for a live load via Plan 04, validated by a clean dry-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF content → extractor | Untrusted file content parsed by pdfplumber; malformed rows handled defensively |
| Node loader → Supabase | Writes via service key (existing trusted pattern); dry-run path performs no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | execSync python invocation with file path arg | mitigate | PDF path comes from controlled docs/Portland/ readdir, not user input; quoted argument |
| T-17-04 | Denial of Service | execSync maxBuffer overflow on large PDF output | mitigate | maxBuffer 8MB matches Fremont; extractor emits compact JSON (bureau rows only, not full text) |
</threat_model>

<verification>
- `python scripts/extractPortland.py docs/Portland/fy2025-26-vol1.pdf` emits valid bureau JSON with fiscal_year=2026
- `node scripts/processPortland.js --dry-run` prints totals and row counts for both fiscal years with no DB writes
- No thousands-multiplication in either script (full-dollar amounts)
</verification>

<success_criteria>
A working extractor + loader pipeline validated by dry-run against both downloaded PDFs, producing full-dollar bureau-level budget trees keyed to fiscal_year 2025 and 2026, with no live DB writes performed yet.
</success_criteria>

<output>
Create `.planning/phases/17-portland-or-budget-load/17-02-SUMMARY.md` when done. Record the dry-run per-FY totals and bureau counts so Plan 04 can sanity-check the live load against them.
</output>
