---
phase: 17-portland-or-budget-load
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - scripts/loadORPopulation.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "Portland, OR municipality row has population 635749 and population_year 2024"
    - "Population is derived from the Census sub-est2024_41.csv file, not hardcoded into the DB write"
    - "Only the SUMLEV=162 incorporated-place row is used (county partition rows ignored)"
  artifacts:
    - path: "scripts/loadORPopulation.js"
      provides: "Census Oregon subcounty population loader for Portland"
      min_lines: 100
  key_links:
    - from: "scripts/loadORPopulation.js"
      to: "treasury.municipalities"
      via: "update population where name=Portland and state=OR"
      pattern: "\\.eq\\('state', 'OR'\\)"
---

<objective>
Load Portland's 2024 Census population into the municipalities row by adapting the Texas population loader for Oregon (FIPS 41).

Purpose: Per-capita display (matching all prior cities) requires a population value sourced from the Census file rather than hardcoded (D-05; RESEARCH anti-pattern "Hardcoding population").

Output: scripts/loadORPopulation.js, with Portland's municipalities row updated to population 635749, population_year 2024.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-portland-or-budget-load/17-RESEARCH.md
@.planning/phases/17-portland-or-budget-load/17-PATTERNS.md
@scripts/loadTXPopulation.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loadORPopulation.js and load Portland population</name>
  <read_first>
    - scripts/loadTXPopulation.js (the full loader to copy: CSV download, header validation at columns 0/8/15, SUMLEV=162 filter, normalizeCensusName, DB update loop — only state-specific values change)
    - .planning/phases/17-portland-or-budget-load/17-PATTERNS.md (loadORPopulation.js section — exact lines to change and which to keep verbatim)
  </read_first>
  <action>
    Create scripts/loadORPopulation.js by copying scripts/loadTXPopulation.js and applying only these targeted changes (PATTERNS loadORPopulation.js section):

    Config block:
    - CSV_URL = 'https://www2.census.gov/programs-surveys/popest/datasets/2020-2024/cities/totals/sub-est2024_41.csv'
    - CSV_PATH = 'C:/tmp/sub-est2024_41.csv'
    - POP_YEAR = 2024
    - EXPECTED_CITIES = ['Portland']
    - KNOWN_VALUES = { Portland: 635749 }

    Keep VERBATIM (Oregon file uses the identical Census layout — verified in RESEARCH):
    - normalizeCensusName (strips " city"/" town"/" village" suffix → "Portland city" becomes "Portland")
    - header validation: header[0]==='SUMLEV', header[8]==='NAME', header[15]==='POPESTIMATE2024'
    - the SUMLEV filter `if (cols[0] !== '162') continue;` (RESEARCH Pitfall 4 — Portland spans 3 counties; do NOT sum county partition rows)

    DB update: change the state filter from `.eq('state', 'TX')` to `.eq('state', 'OR')` in BOTH the current-value select and the update call.

    Run `node scripts/loadORPopulation.js`. It downloads the Census CSV, finds the SUMLEV=162 Portland row, validates the parsed value against KNOWN_VALUES (635749), and updates the municipalities row. If a --dry-run flag exists in the analog, run that first to confirm the parsed value is 635749 before writing.
  </action>
  <verify>
    <automated>node scripts/loadORPopulation.js && grep -c "\.eq('state', 'OR')" scripts/loadORPopulation.js</automated>
  </verify>
  <acceptance_criteria>
    - scripts/loadORPopulation.js contains CSV_URL ending in `sub-est2024_41.csv`
    - scripts/loadORPopulation.js contains `.eq('state', 'OR')` (grep count >= 1) and contains the SUMLEV `'162'` filter
    - Running `node scripts/loadORPopulation.js` exits 0 and reports Portland population 635749
    - `SELECT population, population_year FROM treasury.municipalities WHERE name='Portland' AND state='OR'` returns population=635749, population_year=2024
    - The script does NOT write a value > 635749 (would indicate county rows were summed)
  </acceptance_criteria>
  <done>Portland's municipalities row has Census-derived population 635749 for 2024, enabling per-capita display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| census.gov → local disk | Public HTTPS CSV download, no auth |
| script → Supabase | population UPDATE via service key, scoped to Portland/OR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering | Census CSV layout drift | mitigate | Header validation at columns 0/8/15 aborts if format changed (kept verbatim from TX loader) |
| T-17-06 | Integrity | Summing county partition rows | mitigate | SUMLEV=162 filter ensures only the incorporated-place total is used; KNOWN_VALUES sanity check |
</threat_model>

<verification>
- `node scripts/loadORPopulation.js` exits 0 and reports 635749
- Portland municipalities row: population=635749, population_year=2024
- loadORPopulation.js retains the SUMLEV=162 filter and header validation
</verification>

<success_criteria>
Portland's population is loaded from the Census Oregon subcounty file (635749, vintage 2024) using the tested TX loader pattern with only state-specific values changed.
</success_criteria>

<output>
Create `.planning/phases/17-portland-or-budget-load/17-03-SUMMARY.md` when done.
</output>
