---
phase: 67-socal-verification-source-chain-audit-uat
plan: "67-02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "Across the full SoCal backfill cohort (Phase 63 cities op/rev, Phase 64 county-gov op/rev, Phase 65 salaries, Phase 66 enrichment), every backfilled row carries source attribution (source_url for SCO rows, data_source label for custom/salary rows) (D-04)"
    - "Zero SCO-sourced rows have NULL source_url, EXCEPT the documented pre-existing custom-source rows (Riverside custom GF FY2023/24, San Diego custom + FY2025) which carry a data_source label and are NOT residue (D-05)"
    - "No stored source_url is fragile/version-specific (export tokens, session URLs, versioned API/CSV endpoints); zero residue (no orphaned/placeholder/test rows from the backfill) (D-04)"
    - "Read-only only; production DB; $0 (D-08, D-09, D-10)"
  artifacts:
    - path: ".planning/phases/67-socal-verification-source-chain-audit-uat/67-02-SUMMARY.md"
      provides: "The source-chain durability audit result: per-phase cohort counts, NULL-source_url breakdown, fragility scan, residue scan — the VER-05 part-B evidence record"
      contains: "per-phase cohort counts, SCO NULL-source_url count (must be 0 excluding documented custom rows), fragile-URL count (must be 0), residue count (must be 0), the documented custom-source exceptions"
  key_links:
    - from: "the full SoCal backfilled cohort (treasury.budgets + category_enrichment for the 95 cities + 8 county govs)"
      to: "durable human-page source attribution (durable /d/ URLs, publicpay.ca.gov, data_source labels), zero residue"
      via: "read-only attribution + NULL-source_url + fragility + residue scans (reuse the Phase 62 audit probes parameterized to the SoCal ids)"
      pattern: "scan attribution + NULL source_url → fragility + zero-residue scan → document the durable source chain"
---

<objective>
Satisfy VER-05 part B: a full-cohort source-chain durability audit of the SoCal backfill — every
backfilled budget/salary/enrichment row carries durable human-page source attribution (no
fragile/version-specific links), zero residue. Read-only, production DB, $0. (Reconciliation is 67-01;
UAT is 67-03.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-02-SUMMARY.md
@.planning/phases/63-socal-county-cities-load-linking/VERIFICATION.md
@.planning/phases/65-socal-salaries-sweep/VERIFICATION.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- PRODUCTION DB read-only via repo .env, schema treasury. NEVER mcp__supabase-local.
     Columns: source_url, data_source, total_budget; exact-count head queries (1000-row cap). No writes. -->

Durable sources (D-04): city op/rev = /d/ju3w-4gxp + /d/rrtv-rsj9 ; county-gov op/rev = /d/uctr-c2j8 + /d/emxv-k8xv ;
  salaries data_source = "CA State Controller — Government Compensation in California (publicpay.ca.gov)" ; enrichment source='ai' (universal).
Cohort (D-05): Phase 63 (95 cities op/rev) + 64 (8 county-gov op/rev incl Alameda+Sacramento) + 65 (95 cities salaries) + 66 (185 enrichment universals).
Known non-residue NULL-source_url: Riverside custom GF FY2023/24, San Diego custom + FY2025 op/rev (carry data_source label).
Reuse Phase 62 audit probes (scripts/audit_task1*.mjs, audit_task2.mjs) parameterized to the SoCal municipality ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit source attribution + NULL source_url across the full SoCal cohort</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - .planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md (D-04, D-05)
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-02-SUMMARY.md (audit method)
    - scripts/audit_task1.mjs (reusable attribution / NULL-source_url scan)
  </read_first>
  <action>
    Source .env. Resolve the full SoCal cohort municipality ids (95 cities under the 6 SoCal county nodes +
    the 8 county-government entities incl. Alameda + Sacramento). With READ-ONLY probes (schema treasury):
    count per-phase cohort rows — city op/rev (Phase 63), county-gov op/rev (Phase 64), salaries (Phase 65),
    and universal enrichment authored in Phase 66. For budgets, count rows by source attribution: SCO rows
    carrying a /d/ source_url, salaries rows carrying the GCC data_source label, and NULL-source_url rows.
    Confirm NULL-source_url SCO rows = 0 EXCEPT the documented pre-existing custom-source rows (Riverside
    custom GF FY2023/24, San Diego custom + FY2025) which carry a data_source label — enumerate those
    explicitly so they are accounted for, not silent.
  </action>
  <acceptance_criteria>
    - Per-phase cohort counts recorded (63 city op/rev, 64 county-gov op/rev, 65 salaries, 66 enrichment).
    - NULL-source_url rows enumerated: every one is either an SCO row (must be 0) or a documented custom-source row carrying a data_source label; the documented custom exceptions are listed explicitly.
  </acceptance_criteria>
  <done>Source attribution coverage + the NULL-source_url breakdown across the full SoCal cohort is established read-only, with custom-source exceptions accounted for.</done>
</task>

<task type="auto">
  <name>Task 2: Fragility scan (durable human-page URLs) + zero-residue scan</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - The Task 1 attribution results
    - scripts/audit_task2.mjs (fragility / residue scan patterns)
  </read_first>
  <action>
    With READ-ONLY probes: (a) Fragility — for every distinct source_url across the SoCal cohort, assert it
    is a durable human page (ByTheNumbers /d/<id>, publicpay.ca.gov, or an official adopted-budget landing
    page) and NOT fragile/version-specific (no export tokens, session URLs, /resource/*.json API endpoints,
    or version/date query params). Count fragile URLs (must be 0). (b) Zero residue — confirm the backfill
    left no orphaned/placeholder/test rows: no SoCal budget row with a null/zero total where it should have
    data, no enrichment row with empty plain_name/description, no category row orphaned from a budget, no
    duplicate (municipality_id, fiscal_year, dataset_type) rows. Record counts (all must be 0).
  </action>
  <acceptance_criteria>
    - Distinct source_urls across the cohort are all durable human pages; fragile/version-specific count = 0.
    - Zero-residue scan finds 0 orphaned/placeholder/test/duplicate rows from the backfill.
  </acceptance_criteria>
  <done>The SoCal source chain is durable (no fragile links) and residue-free.</done>
</task>

<task type="auto">
  <name>Task 3: Document the source-chain audit + write 67-02-SUMMARY (VER-05 part-B evidence)</name>
  <files>.planning/phases/67-socal-verification-source-chain-audit-uat/67-02-SUMMARY.md</files>
  <read_first>
    - The Task 1 + Task 2 results
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 67-02-SUMMARY.md: per-phase cohort counts; SCO NULL-source_url count (0, excluding the documented
    custom rows which are listed); fragile-URL count (0); residue count (0); the durable source map (which
    /d/ URL or data_source label backs each dataset). State VER-05 part B satisfied (durable source chain,
    zero residue). Note any anomaly as a documented follow-up. Confirm read-only, $0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/67-socal-verification-source-chain-audit-uat/67-02-SUMMARY.md';const t=fs.readFileSync(p,'utf8');const ok=/VER-05/.test(t)&&/residue/i.test(t)&&/fragil/i.test(t)&&/source_url/i.test(t);console.log('SUMMARY has VER-05 + residue + fragility + source_url:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - 67-02-SUMMARY.md records per-phase counts, NULL-source_url breakdown (SCO=0 + listed custom exceptions), fragile=0, residue=0; VER-05 part B stated.
    - Verify probe passes (SUMMARY references VER-05, residue, fragility, source_url).
  </acceptance_criteria>
  <done>The source-chain durability audit is documented as the VER-05 part-B evidence record.</done>
</task>

</tasks>

<verification>
- Every backfilled SoCal row carries durable attribution; SCO NULL-source_url = 0 (custom rows carry data_source labels, documented).
- No fragile/version-specific source_url; zero residue (no orphan/placeholder/test/duplicate rows).
- Read-only, production DB, $0; 67-02-SUMMARY.md is the VER-05 part-B evidence.
</verification>

<success_criteria>
- VER-05 part B satisfied: full-cohort source-chain durability audit passes — durable human-page attribution everywhere, zero residue.
- No DB writes / source changes (D-08); production DB (D-09); $0 (D-10).
- 67-02-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/67-socal-verification-source-chain-audit-uat/67-02-SUMMARY.md` when done.
</output>
