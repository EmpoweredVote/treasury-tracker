---
phase: 38-ma-city-budget-load
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - scripts/output/ma_dls_progress.json
autonomous: false
requirements: [MA-01, MA-02, MA-03]
must_haves:
  truths:
    - "All 351 MA cities have revenue (Money In) budget rows for FY2021–FY2025 where source data exists (MA-02)"
    - "All MA cities with non-zero federal grants have operating (Money Out) budget rows for FY2021–FY2025 (MA-01)"
    - "A MASSACHUSETTS group appears in the city picker listing the loaded MA cities (MA-03)"
    - "data_sources.fiscal_years arrays contain all five years {2021,2022,2023,2024,2025} after the full load (LOAD-03)"
    - "An interrupted --load run resumes from the last successful city via ma_dls_progress.json (LOAD-02)"
  artifacts:
    - path: "scripts/output/ma_dls_progress.json"
      provides: "Crash-resume checkpoint ledger keyed by DOR code, rebuilt during the load runs"
  key_links:
    - from: "scripts/scrapeMaDLS.js --load"
      to: "treasury.budgets / treasury.budget_categories"
      via: "treasury_sync_budget_tree RPC"
      pattern: "treasury_sync_budget_tree"
    - from: "treasury.budgets (MA cities)"
      to: "EntitySwitcher.tsx MASSACHUSETTS group"
      via: "getCities() SQL join with HAVING COUNT(b.id) > 0"
      pattern: "STATE_NAMES\\['MA'\\]"
---

<objective>
Load all 10 MA DLS JSON files (5 revenue-by-source + 5 special-revenue expenditures, FY2021–FY2025) into the database via `treasury_sync_budget_tree`, then verify the data is visible and correct: DB row counts (MA-01, MA-02), the MASSACHUSETTS city-picker group with a human spot-check of 3 representative cities (MA-03), and the LOAD-03 fiscal_years accumulation. This plan also performs the first-ever LIVE LOAD-02 checkpoint-resume confirmation.

Purpose: This is the payload of Phase 38 — it makes Massachusetts data actually visible in the app. MA-03 requires no code changes; STATE_NAMES['MA'] already exists and cities appear automatically once treasury.budgets rows exist.
Output: ~1,000–1,755 operating budget rows and ~1,755 revenue budget rows for MA cities; rebuilt ma_dls_progress.json checkpoint; MASSACHUSETTS group live in the picker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ma-city-budget-load/38-RESEARCH.md
@.planning/phases/38-ma-city-budget-load/38-VALIDATION.md
@.planning/phases/38-ma-city-budget-load/38-01-SUMMARY.md
@.planning/phases/37-ma-loader-hardening/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live LOAD-02 resume confirmation, then load revenue-by-source FY2021–FY2025 (MA-02)</name>
  <files>scripts/output/ma_dls_progress.json, (DB writes via treasury_sync_budget_tree — treasury.budgets, treasury.budget_categories, treasury.data_sources)</files>
  <read_first>
    - scripts/scrapeMaDLS.js (loadToSupabase: readProgress/writeProgress lines ~757–800, the --load --file branch, existingDs UPDATE path, fiscal_years append-dedup from LOAD-03)
    - .planning/phases/37-ma-loader-hardening/37-02-SUMMARY.md (LOAD-02 checkpoint structure, LOAD-03 fiscal_years append-dedup implementation)
    - .planning/phases/38-ma-city-budget-load/38-RESEARCH.md (Wave 2 load commands, Pitfall 3 progress-file-missing-for-FY2025, Live LOAD-02 Resume Test section, Pitfall 4 fiscal_years)
  </read_first>
  <action>
    Confirm `.env` has SUPABASE_URL and SUPABASE_SERVICE_KEY available (required by --load; export or rely on the script's dotenv). Run from C:/treasury-tracker.

    STEP A — Live LOAD-02 resume test (first live confirmation; Phase 37 only verified code-structurally). If a stale `scripts/output/ma_dls_progress.json` exists, delete it first so this is a clean run. Start the FY2025 revenue load and interrupt it:
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2025.json`
    Interrupt with Ctrl+C after roughly 10–20 cities report success (watch stdout for per-city load confirmations). Then re-run the exact same command:
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2025.json`
    Confirm stdout reports cities being skipped from the checkpoint (e.g. "Skipped N already loaded") and that the run completes the remaining cities. This proves LOAD-02 works live. Re-running is safe — treasury_sync_budget_tree is idempotent (upsert) and the LOAD-03 dedup guard prevents duplicate fiscal_years entries.

    STEP B — Load the remaining four revenue-by-source FYs in order:
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2021.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2022.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2023.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_revenue-by-source_2024.json`

    Do NOT delete ma_dls_progress.json between these runs — it is the crash-resume ledger. Every --load run hits the existingDs UPDATE path (all 702 data_source rows already exist from the Phase 37 test run), appending each FY to fiscal_years. Skip any FY whose source file Plan 01 flagged as <300 records only if it is empty; otherwise load it.
  </action>
  <verify>
    <automated>SELECT COUNT(*) FROM treasury.budgets b JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.state='MA' AND b.dataset_type='revenue';</automated>
  </verify>
  <acceptance_criteria>
    - During STEP A re-run, stdout contains a skip message indicating cities already loaded were skipped from the checkpoint (LOAD-02 confirmed live)
    - Supabase MCP query `SELECT COUNT(*) FROM treasury.budgets b JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.state='MA' AND b.dataset_type='revenue'` returns ≥ 351 (MA-02)
    - Supabase MCP query `SELECT fiscal_years FROM treasury.data_sources WHERE api_type='ma-dls' AND dataset_type='revenue' ORDER BY created_at LIMIT 5` returns arrays containing all of 2021,2022,2023,2024,2025 (order may vary) — LOAD-03 confirmed, no overwrite
    - `scripts/output/ma_dls_progress.json` exists after the runs
  </acceptance_criteria>
  <done>All 5 revenue-by-source FYs loaded; LOAD-02 resume confirmed live; MA revenue budget count ≥ 351; fiscal_years arrays contain all 5 years.</done>
</task>

<task type="auto">
  <name>Task 2: Load special-revenue expenditures FY2021–FY2025 and verify operating row counts (MA-01, SC-5)</name>
  <files>scripts/output/ma_dls_progress.json, (DB writes via treasury_sync_budget_tree — treasury.budgets, treasury.budget_categories, treasury.data_sources)</files>
  <read_first>
    - scripts/scrapeMaDLS.js (--load branch, dataset_type:'operating' mapping for special-revenue report)
    - .planning/phases/38-ma-city-budget-load/38-RESEARCH.md (Wave 2 load commands, Pitfall 2 federal-grants-not-GF, Pitfall 5 SC-5 row count, Scope Clarification — 59 all-zero cities expected)
  </read_first>
  <action>
    Load the five special-revenue expenditures FYs in order from C:/treasury-tracker:
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_special-revenue_2021_expenditures.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_special-revenue_2022_expenditures.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_special-revenue_2023_expenditures.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_special-revenue_2024_expenditures.json`
    `node scripts/scrapeMaDLS.js --load --file scripts/output/ma_dls_special-revenue_2025_expenditures.json`

    These load as dataset_type:'operating' (the accepted operating proxy — Schedule A federal grants, since GF Expenditures was excluded in Phase 37). Cities with all-zero federal grants produce no budget rows for that FY — this is expected per Pitfall 2 (59 cities had zero in FY2025). Do not treat a lower-than-351 operating count as a failure. Do NOT delete ma_dls_progress.json between runs.

    After all 10 load runs complete, run the three verification queries via Supabase MCP (see acceptance criteria) and record the actual counts in the SUMMARY.
  </action>
  <verify>
    <automated>SELECT COUNT(*) FROM treasury.budget_categories bc JOIN treasury.budgets b ON b.id=bc.budget_id JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.state='MA' AND b.dataset_type='operating';</automated>
  </verify>
  <acceptance_criteria>
    - Supabase MCP query `SELECT COUNT(*) FROM treasury.budgets b JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.state='MA' AND b.dataset_type='operating'` returns ≥ 292 (MA-01; allows for all-zero-grant cities being excluded)
    - Supabase MCP query `SELECT COUNT(*) FROM treasury.budget_categories bc JOIN treasury.budgets b ON b.id=bc.budget_id JOIN treasury.municipalities m ON m.id=b.municipality_id WHERE m.state='MA' AND b.dataset_type='operating'` returns > 1,000 (SC-5)
    - Supabase MCP query `SELECT fiscal_years FROM treasury.data_sources WHERE api_type='ma-dls' AND dataset_type='operating' ORDER BY created_at LIMIT 5` returns arrays containing 2021,2022,2023,2024,2025 (LOAD-03 for operating data_sources)
  </acceptance_criteria>
  <done>All 5 special-revenue expenditures FYs loaded as operating; MA operating budget count ≥ 292; budget_categories operating count > 1,000; fiscal_years arrays complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check — MASSACHUSETTS picker group + Boston/Worcester/Springfield (MA-03)</name>
  <read_first>
    - .planning/phases/38-ma-city-budget-load/38-VALIDATION.md (Manual-Only Verifications table)
    - .planning/phases/38-ma-city-budget-load/38-RESEARCH.md (MA-03 City Picker Analysis — zero-code section)
  </read_first>
  <what-built>
    Tasks 1–2 loaded revenue + operating budget rows for 351 MA cities across FY2021–FY2025. STATE_NAMES['MA']='Massachusetts' already exists and the EntitySwitcher getCities() query uses HAVING COUNT(b.id) > 0, so the MASSACHUSETTS group and its cities appear automatically now that budget rows exist — no frontend code was changed.
  </what-built>
  <how-to-verify>
    1. Open the app at treasurytracker.empowered.vote (or the local dev server if running).
    2. Open the city picker / EntitySwitcher and confirm a "MASSACHUSETTS" group header appears with a large list of cities (expect ~350; some all-zero-grant cities with no revenue either may not appear — note the approximate count).
    3. Click "Boston": confirm a Money In (revenue) tab shows data for at least one FY between 2021–2025, and that the year selector lists FY2021–FY2025.
    4. Click "Boston" → Money Out (operating) tab: confirm it shows operating data for at least one FY (Boston receives federal grants, so it should have operating rows).
    5. Repeat the picker + Money In check for "Worcester" and "Springfield".
    6. Note: per RESEARCH Pitfall 2, some smaller cities may show only a Money In tab (no operating data for years with zero federal grants) — this is expected, not a bug.
  </how-to-verify>
  <resume-signal>Type "approved" if MASSACHUSETTS appears and Boston/Worcester/Springfield show data, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scrapeMaDLS.js --load → Supabase | Writes via service-role key (bypasses RLS — same as every other loader); reads local JSON files only |
| treasury.budgets → EntitySwitcher (read path) | Public read of already-published budget data; no new endpoint, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Elevation of Privilege | Service-role key in --load | accept | Service-role write is the established pattern for all loaders; key lives in .env (gitignored), never exposed to client. Same posture as Phases 5–37. |
| T-38-02-02 | Tampering | Idempotent re-load creating duplicates | mitigate | treasury_sync_budget_tree is an upsert (idempotent per prior states); LOAD-03 dedup guard prevents duplicate fiscal_years. STEP A re-run explicitly relies on and confirms this. |
| T-38-02-03 | Information Disclosure | MA budget data exposed in picker | accept | All data is public government financial data intended for public transparency — the app's core value. |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). |

Per security_config: no changes to authentication, session management, API endpoints, or user-facing input surfaces. No high-severity threats present.
</threat_model>

<verification>
- MA revenue budget count ≥ 351 (MA-02)
- MA operating budget count ≥ 292 (MA-01, accounting for all-zero-grant cities)
- MA operating budget_categories count > 1,000 (SC-5)
- data_sources.fiscal_years arrays contain {2021,2022,2023,2024,2025} for both dataset types (LOAD-03)
- LOAD-02 resume confirmed live (skip message on re-run)
- Human spot-check: MASSACHUSETTS group present; Boston/Worcester/Springfield show Money In + (where applicable) Money Out across FY2021–FY2025 (MA-03)
</verification>

<success_criteria>
- All 10 MA DLS JSON files loaded into the DB
- MA-01: operating budget rows present for cities with federal grants, FY2021–FY2025
- MA-02: revenue budget rows present for all 351 cities where source data exists, FY2021–FY2025
- MA-03: MASSACHUSETTS group visible in city picker; representative cities open with data
- LOAD-02 and LOAD-03 confirmed working live
</success_criteria>

<output>
Create `.planning/phases/38-ma-city-budget-load/38-02-SUMMARY.md` when done, recording all DB counts, fiscal_years verification results, the LOAD-02 live-resume observation, and any all-zero-city caveats.
</output>
