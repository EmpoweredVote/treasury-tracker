---
phase: 38-ma-city-budget-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/output/ma_dls_revenue-by-source_2021.json
  - scripts/output/ma_dls_revenue-by-source_2022.json
  - scripts/output/ma_dls_revenue-by-source_2023.json
  - scripts/output/ma_dls_revenue-by-source_2024.json
  - scripts/output/ma_dls_special-revenue_2021_expenditures.json
  - scripts/output/ma_dls_special-revenue_2022_expenditures.json
  - scripts/output/ma_dls_special-revenue_2023_expenditures.json
  - scripts/output/ma_dls_special-revenue_2024_expenditures.json
autonomous: true
requirements: [MA-01, MA-02]
must_haves:
  truths:
    - "Eight JSON scrape files for FY2021–FY2024 (both reports) exist on disk alongside the two FY2025 files already present"
    - "Each revenue-by-source JSON file contains ~351 city records"
    - "Each special-revenue expenditures JSON file contains ~351 city records"
  artifacts:
    - path: "scripts/output/ma_dls_revenue-by-source_2021.json"
      provides: "FY2021 revenue-by-source scrape for 351 MA cities"
    - path: "scripts/output/ma_dls_revenue-by-source_2024.json"
      provides: "FY2024 revenue-by-source scrape for 351 MA cities"
    - path: "scripts/output/ma_dls_special-revenue_2021_expenditures.json"
      provides: "FY2021 special-revenue (operating proxy) scrape for 351 MA cities"
    - path: "scripts/output/ma_dls_special-revenue_2024_expenditures.json"
      provides: "FY2024 special-revenue (operating proxy) scrape for 351 MA cities"
  key_links:
    - from: "scripts/scrapeMaDLS.js"
      to: "dls-gw.dor.state.ma.us"
      via: "outbound HTTP scrape (--scrape --report X --fy Y)"
      pattern: "--scrape"
---

<objective>
Scrape MA DLS data for FY2021–FY2024 (both report types) for all 351 Massachusetts municipalities, producing 8 JSON output files on disk. FY2025 for both reports is already on disk from a prior Phase 37 run, so this plan produces the remaining 8 of the 10 total files needed for the Wave 2 load.

Purpose: The bulk load in Plan 02 reads these JSON files. Without the FY2021–FY2024 scrapes, only FY2025 data could be loaded, missing 4 of the 5 fiscal years required by MA-01 and MA-02.
Output: 8 JSON files in `scripts/output/` (4 revenue-by-source + 4 special-revenue expenditures), each containing ~351 city records.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ma-city-budget-load/38-RESEARCH.md
@.planning/phases/37-ma-loader-hardening/37-01-SUMMARY.md
@.planning/phases/37-ma-loader-hardening/37-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrape revenue-by-source FY2021–FY2024 (MA-02)</name>
  <files>scripts/output/ma_dls_revenue-by-source_2021.json, scripts/output/ma_dls_revenue-by-source_2022.json, scripts/output/ma_dls_revenue-by-source_2023.json, scripts/output/ma_dls_revenue-by-source_2024.json</files>
  <read_first>
    - scripts/scrapeMaDLS.js (REPORTS[] definition lines 82–110, scrape loop, output filename pattern line 458; note revenue-by-source has supportsType:false so NO --type flag is used and filename has no _expenditures suffix)
    - .planning/phases/38-ma-city-budget-load/38-RESEARCH.md (Wave 1 scrape commands, Pitfall 1)
  </read_first>
  <action>
    Run the four revenue-by-source scrape commands in order, one fiscal year at a time (the CLI scrapes one FY per invocation — there is no multi-FY loop). From the repo root C:/treasury-tracker, run exactly:

    `node scripts/scrapeMaDLS.js --scrape --report revenue-by-source --fy 2021`
    `node scripts/scrapeMaDLS.js --scrape --report revenue-by-source --fy 2022`
    `node scripts/scrapeMaDLS.js --scrape --report revenue-by-source --fy 2023`
    `node scripts/scrapeMaDLS.js --scrape --report revenue-by-source --fy 2024`

    Do NOT pass --type for revenue-by-source (supportsType is false; passing --type produces the wrong filename). Do NOT pass --load in these commands — scrape only, no DB writes. Each run writes `scripts/output/ma_dls_revenue-by-source_{fy}.json` with ~351 records.

    If any single FY run returns 0 records or far fewer than ~351 (per RESEARCH assumption A1, FY2021 portal coverage is ASSUMED), do not abort the remaining years — record the actual record count for that FY in the SUMMARY and continue. The portal may not have full data for the earliest year; that is a documented acceptable degradation (at minimum FY2025 already on disk loads). Note any FY with <300 records as a caveat.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['2021','2022','2023','2024'].forEach(y=>{const f='scripts/output/ma_dls_revenue-by-source_'+y+'.json';const n=JSON.parse(fs.readFileSync(f,'utf8')).length;console.log(f,n);if(n<1){process.exit(1);}})"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "['2021','2022','2023','2024'].forEach(y=>require('fs').accessSync('scripts/output/ma_dls_revenue-by-source_'+y+'.json'))"` exits 0 (all four files exist)
    - Each file parses as a JSON array; FY2022–FY2024 each contain ≥ 300 records (FY2021 may be lower per A1 — record exact count in SUMMARY)
    - No file is named `ma_dls_revenue-by-source_202X_expenditures.json` (the _expenditures suffix must NOT appear for revenue-by-source — confirms --type was not passed)
  </acceptance_criteria>
  <done>Four revenue-by-source JSON files (FY2021–FY2024) exist in scripts/output/, each a JSON array of city records, with record counts logged for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Scrape special-revenue expenditures FY2021–FY2024 (MA-01)</name>
  <files>scripts/output/ma_dls_special-revenue_2021_expenditures.json, scripts/output/ma_dls_special-revenue_2022_expenditures.json, scripts/output/ma_dls_special-revenue_2023_expenditures.json, scripts/output/ma_dls_special-revenue_2024_expenditures.json</files>
  <read_first>
    - scripts/scrapeMaDLS.js (REPORTS[] special-revenue entry, supportsType:true, amountType handling line 376, filename suffix line 458)
    - .planning/phases/38-ma-city-budget-load/38-RESEARCH.md (Pitfall 1 filename pattern, Pitfall 2 special-revenue is federal grants not GF, Scope Clarification section)
  </read_first>
  <action>
    Run the four special-revenue scrape commands in order, one fiscal year at a time. The special-revenue report has supportsType:true, so --type Expenditures MUST be passed to produce the deterministic filename `ma_dls_special-revenue_{fy}_expenditures.json` (omitting --type changes the filename and breaks the Wave 2 load file paths). From C:/treasury-tracker, run exactly:

    `node scripts/scrapeMaDLS.js --scrape --report special-revenue --fy 2021 --type Expenditures`
    `node scripts/scrapeMaDLS.js --scrape --report special-revenue --fy 2022 --type Expenditures`
    `node scripts/scrapeMaDLS.js --scrape --report special-revenue --fy 2023 --type Expenditures`
    `node scripts/scrapeMaDLS.js --scrape --report special-revenue --fy 2024 --type Expenditures`

    Do NOT pass --load. Scrape only. This is Schedule A Part 3 (federal grant expenditures), the accepted operating proxy labeled dataset_type:'operating' (GF Expenditures was excluded in Phase 37). Many cities legitimately have all-zero rows for years with no federal grants — per RESEARCH (59 cities had zero in FY2025). All-zero records are expected, not errors; the record COUNT should still be ~351 (one record per city, including the all-zero ones). Log actual record counts per FY for the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['2021','2022','2023','2024'].forEach(y=>{const f='scripts/output/ma_dls_special-revenue_'+y+'_expenditures.json';const n=JSON.parse(fs.readFileSync(f,'utf8')).length;console.log(f,n);if(n<1){process.exit(1);}})"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "['2021','2022','2023','2024'].forEach(y=>require('fs').accessSync('scripts/output/ma_dls_special-revenue_'+y+'_expenditures.json'))"` exits 0 (all four files exist with the _expenditures suffix)
    - Each file parses as a JSON array; FY2022–FY2024 each contain ≥ 300 records (FY2021 may be lower per A1 — record exact count in SUMMARY)
    - `ls scripts/output/ma_dls_*_202{1,2,3,4}*.json` lists exactly 8 files total (4 revenue-by-source from Task 1 + 4 special-revenue from this task)
  </acceptance_criteria>
  <done>Four special-revenue expenditures JSON files (FY2021–FY2024) exist in scripts/output/ with the _expenditures suffix, each a JSON array of ~351 city records (all-zero records included), counts logged for the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scrapeMaDLS.js → dls-gw.dor.state.ma.us | Outbound HTTP GET/POST to a public government portal; no auth, no inbound user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | Scraped data from public portal | accept | Public, read-only government data; written to local JSON only (no DB writes in this plan). Sanity-checked by record count in acceptance criteria. |
| T-38-02 | Denial of Service | MA DLS portal availability | accept | Outbound scrape only; portal confirmed reachable in Phase 37. If unreachable, plan halts with no partial DB state (scrape-only, no writes). |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A). No new dependencies introduced. |

Per security_config: this phase makes no changes to authentication, session management, API endpoints, or user-facing input surfaces. All operations are outbound HTTP GET to a public portal. No high-severity threats present.
</threat_model>

<verification>
- 8 new JSON files (4 revenue-by-source + 4 special-revenue expenditures, FY2021–FY2024) exist in scripts/output/
- Combined with the 2 pre-existing FY2025 files, 10 total MA DLS JSON files are present for the Wave 2 load
- `ls scripts/output/ma_dls_*_202*.json` returns 10 files
- Record counts per FY logged in SUMMARY (caveat noted for any FY with <300 records)
</verification>

<success_criteria>
- All 8 FY2021–FY2024 JSON files written to disk and parse as non-empty JSON arrays
- revenue-by-source files have no _expenditures suffix; special-revenue files have the _expenditures suffix
- No DB writes occurred (this is a scrape-only plan)
- Wave 2 (Plan 02) load can proceed against all 10 files
</success_criteria>

<output>
Create `.planning/phases/38-ma-city-budget-load/38-01-SUMMARY.md` when done, including per-FY record counts and any low-coverage caveats.
</output>
