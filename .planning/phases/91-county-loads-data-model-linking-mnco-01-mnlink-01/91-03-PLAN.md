---
phase: 91-county-loads-data-model-linking-mnco-01-mnlink-01
plan: 03
type: execute
wave: 3
depends_on: ["91-02"]
files_modified:
  - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-INTEGRATION.md
autonomous: false
requirements: [MNLINK-01]
must_haves:
  truths:
    - "The live ev-accounts-api returns the data that drives the existing frontend for MN: MN cities carry county_id, MN counties are entity_type='county' selectable entities under the Minnesota state node, and the Cities-in-County payload lists a county's member cities — confirmed by hitting the live API, NOT by reading the DB only (MNLINK-01 render half)"
    - "The EXISTING frontend renders the US -> Minnesota -> '<Name> County' -> city breadcrumb and the Cities-in-County panel for a sample county (e.g. Hennepin County) with NO new/changed frontend code (CONTEXT D-09 — reuse only); any gap is a data gap, fixed in the data layer (91-02), not a rebuild"
    - "A committed 91-INTEGRATION.md records the API probe results + the human visual spot-check (breadcrumb + Cities-in-County panel for >=1 county) — the in-phase render evidence; full visual UAT across anchors is deferred to Phase 93"
  artifacts:
    - path: ".planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-INTEGRATION.md"
      provides: "The in-phase integration-render evidence: live API probes (county_id, state node, cities-in-county) + a human breadcrumb/panel spot-check"
      contains: "Hennepin"
  key_links:
    - from: "municipalities.county_id + Minnesota state node + county rows (91-02 data layer)"
      to: "the existing breadcrumb + CitiesInCountyPanel rendering US->Minnesota->county->city in the live app"
      via: "ev-accounts-api (state-agnostic, already serves Ohio/LA counties) -> existing frontend components (no rebuild)"
      pattern: "CitiesInCountyPanel"
---

<objective>
Verify the Phase 91 data layer makes the EXISTING frontend render the MN county hierarchy — the render half of MNLINK-01 — WITHOUT building or changing any frontend code (CONTEXT D-09). The breadcrumb + Cities-in-County panel already exist (Phase 25 LA County / Ohio 86) and the `ev-accounts-api` already serves `county_id` + state nodes state-agnostically; this plan confirms MN data flows through them. Output: a committed `91-INTEGRATION.md` with API probe results + a human visual spot-check. Full visual UAT across all anchors is Phase 93.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md

<interfaces>
<!-- Reuse-only verification. The frontend + API already serve Ohio/LA counties; this confirms MN. -->

- The treasury app + `ev-accounts-api` already render county breadcrumbs + a Cities-in-County panel for Ohio (Phase 86) and LA County (Phase 25). The API is state-agnostic — once MN has county rows + city.county_id + the Minnesota state node (all done in 91-02), MN should render with NO code change.
- API probes: hit the SAME live endpoints the app uses (e.g. the cities/entities endpoint that returns `county_id`, and the county/cities-in-county endpoint) for a MN sample — confirm MN cities return county_id, MN counties are present + selectable under "Minnesota", and a county returns its member cities. (Find the exact endpoint paths from the app/API source — do not assume; mirror how Phase 86-03 probed for Ohio.)
- This is verification, not implementation: if a probe reveals MN data is missing/wrong, the fix belongs in the 91-02 data layer (re-run the loader/linker), NOT in new frontend code. If a genuine frontend gap exists (some component hard-codes a state), record it as a finding for a follow-up — do not rebuild here.
- 91-INTEGRATION.md = API probe outputs + a short human visual checklist (breadcrumb path + Cities-in-County panel for >=1 county, e.g. Hennepin County showing Minneapolis/Bloomington/etc.).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live API probes — MN county_id, state node, cities-in-county</name>
  <files>.planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-INTEGRATION.md</files>
  <read_first>
    - .planning/milestones/v2.8-phases/86-county-loads-data-model-linking/86-03-PLAN.md (how Ohio's frontend/API render was verified — the endpoints + checks to mirror)
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md (D-09 reuse-only)
    - the app/API source for the cities + county endpoints (locate the endpoint that returns county_id and the cities-in-county payload — do not assume the path)
  </read_first>
  <action>
    Identify the live endpoints the frontend uses for entity listing (county_id) and the Cities-in-County panel (mirror the Phase 86-03 Ohio probes). Hit them for a MN sample and record results in `91-INTEGRATION.md`:
    - The entities/cities payload includes MN cities with a non-null `county_id` (e.g. Minneapolis → Hennepin County id).
    - The "Minnesota" state node is present + selectable; MN counties (entity_type='county') appear under it.
    - A county's cities-in-county payload lists its member cities (e.g. Hennepin County → Minneapolis, Bloomington, Minnetonka, St. Louis Park).
    If any probe fails because of missing/wrong DATA, note it and (if needed) re-run the 91-02 loader/linker to fix the data — do NOT change frontend code. Capture the raw probe results.
  </action>
  <acceptance_criteria>
    - `91-INTEGRATION.md` records live API probe results showing: a MN city with non-null county_id, the Minnesota state node present, and a sample county returning ≥1 member city
    - Any failing probe is traced to a data gap (fixed via 91-02 re-run) — no frontend code was modified
    - The probes hit the real endpoints the app uses (paths cited), not just the DB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Human visual spot-check (breadcrumb + Cities-in-County panel) + write 91-INTEGRATION.md</name>
  <files>.planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-INTEGRATION.md</files>
  <read_first>
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-INTEGRATION.md (the API probe results from Task 1)
  </read_first>
  <action>
    Present a short human visual checklist for the live app (treasurytracker.empowered.vote) and record the outcome in `91-INTEGRATION.md`:
    - Select a MN county (e.g. Hennepin County) → the US → Minnesota → Hennepin County breadcrumb renders.
    - The Cities-in-County panel lists member cities (Minneapolis et al.) and they navigate to the city page.
    - A MN city page (Minneapolis) shows a "Hennepin County →" context link.
    Mark each as PASS/observed (or note a gap). This is an in-phase render confirmation, NOT the full UAT (Phase 93). Commit `91-INTEGRATION.md` with the API + visual evidence.

    NOTE (autonomous:false): this task needs a human visual confirmation in the live app — pause and ask the user to eyeball the breadcrumb + panel, then record their observation.
  </action>
  <acceptance_criteria>
    - `91-INTEGRATION.md` is committed and contains both the API probe results and the human visual spot-check outcome (breadcrumb + Cities-in-County panel for ≥1 MN county)
    - The breadcrumb US → Minnesota → "<Name> County" → city and the Cities-in-County panel are confirmed rendering (or any gap is recorded as a Phase 93 follow-up)
    - No frontend code was created or modified (reuse-only, D-09)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Live API probes confirm MN cities carry county_id, the Minnesota state node is present + selectable, and counties return their member cities — via the same endpoints the app uses (MNLINK-01 render half).
- The existing breadcrumb (US → Minnesota → "<Name> County" → city) + Cities-in-County panel render for a sample county with NO new frontend code (D-09).
- `91-INTEGRATION.md` committed with API + human visual evidence.
- Full visual UAT across all RCV anchors + the source-chain audit + ACFR reconciliation are Phase 93.
</verification>
