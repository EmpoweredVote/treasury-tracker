---
phase: 91-county-loads-data-model-linking-mnco-01-mnlink-01
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - scripts/linkMNCitiesToCounties.js
  - scripts/mnCountyResidual.json
  - scripts/loadMNOSABatch.js
autonomous: false
requirements: [MNCO-01, MNLINK-01]
must_haves:
  truths:
    - "Every filing MN county government is live in production with operating + revenue across FY2013–FY2021, entity_type='county', stored as '<Name> County', per-capita from the Population column, every row stamped data_source='Minnesota Office of the State Auditor City/County Finances Report' + county source_url + source_date (MNCO-01)"
    - "Every MN city's municipalities.county_id is set to its parent '<County> County' municipality, sourced DIRECTLY from the city workbook ParentEntityName column via entityCounty() (CONTEXT D-04) — no authored map; linking is idempotent (set-if-different) and cities whose ParentEntityName has no matching loaded county are recorded as a link-residual, never linked to a phantom county"
    - "The Minnesota state node (pre-existing, entity_type='state', pop 5,706,494) is verified present; counties keep county_id NULL; the resulting graph reads US -> Minnesota -> '<Name> County' -> city (CONTEXT D-05)"
    - "The load + link are source-safe + idempotent: a second run creates 0 new municipalities, changes 0 county_id values, and leaves per-(county,FY,dataset) row counts unchanged; the never-overwrite guard protects any richer pre-existing source"
    - "County population reflects the latest filed FY (refreshMNPopulations --entity-type county), and counties enumerated but absent of a financial total are written to a committed scripts/mnCountyResidual.json (no phantom municipalities; CONTEXT D-08)"
  artifacts:
    - path: "scripts/linkMNCitiesToCounties.js"
      provides: "The city->county linking pass — reads each MN city's ParentEntityName from a city workbook and sets municipalities.county_id to the matching '<County> County' row; idempotent; records unmatched cities as a link-residual"
      contains: "county_id"
    - path: "scripts/mnCountyResidual.json"
      provides: "Committed county source-gap residual (counties with no financial total; expect none) — no phantom municipalities"
      contains: "residual"
  key_links:
    - from: "MN county workbooks (FY2013–2021) + city workbook ParentEntityName column"
      to: "treasury.budgets county rows (entity_type='county') + municipalities.county_id on every MN city, under the Minnesota state node"
      via: "loadMNOSABatch --entity-type county (live) -> linkMNCitiesToCounties (entityCounty -> '<County> County' -> county_id)"
      pattern: "county_id"
---

<objective>
Run the county load **live** for FY2013–2021 (MNCO-01) using the 91-01 tooling, **link every MN city to its parent county** by reading the city workbook's `ParentEntityName` column and setting `municipalities.county_id` (MNLINK-01 data layer), verify the Minnesota state node, refresh county populations, and commit the county residual. The frontend/integration render check is plan 91-03; ACFR reconciliation + UAT are Phase 93.

This delivers MNCO-01 (counties loaded, sourced, per-capita) and the data half of MNLINK-01 (state node verified + city→county `county_id` set + graph resolves).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md
@scripts/loadMNOSABatch.js
@scripts/loadMNOSA.js
@scripts/refreshMNPopulations.js
@scripts/linkOhioCitiesToCounties.js
@scripts/mnOsaDatasets.json

<interfaces>
<!-- Production writes. The 91-01 tooling is the tool; this plan runs it live + adds linking. -->

- Live writes require the gitignored `.env` SUPABASE_SERVICE_KEY (SUPABASE_URL=https://kxsdzaojfaibhuzmclfq.supabase.co — production) and run SERIALLY on the main tree (D-07). Idempotent + never-overwrite guarded.
- County load: `node scripts/loadMNOSABatch.js --fy <YYYY> --entity-type county` for each FY 2013–2021 (driver downloads the county workbook via county_url; writes importEntity entityType='county', stored "<Name> County"). County data exists FY2013–2017 + FY2019–2021 (gap 2018) — a FY with no county_url is skipped with a logged note (not an error).
- Linking source (D-04): `entityCounty(cityWorkbook, cityName, 'city')` returns the bare county (e.g. Minneapolis→"Hennepin"); the parent county municipality is stored "<County> County" ("Hennepin County"). MN city rows already exist (Phase 90, 858 cities). County rows exist after the county load above. The most recent city workbook (`_mn-recon/cired_23_data.xlsx`) is the link source (ParentEntityName is stable across years; fall back to an older FY for a city absent from 2023).
- DB model: `treasury.municipalities.county_id` FK + `entity_type` CHECK('county','state') already exist. The Minnesota state node already exists (id d4b4897d, name "Minnesota", entity_type='state', pop 5,706,494) — VERIFY, do not recreate (D-05). Use `total_budget`/`hierarchy` for budget probes; prefer `mcp__supabase-local` execute_sql for read-only probes.
- mnCountyResidual.json shape mirrors scripts/mnCityResidual.json / scripts/ohioCountyResidual.json.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire county workbooks + full-range county dry-run pre-flight (zero writes)</name>
  <files>scripts/mnOsaDatasets.json</files>
  <read_first>
    - scripts/mnOsaDatasets.json (the county_url entries FY2013–2021; county_floor 2013)
    - scripts/loadMNOSABatch.js (--entity-type county acquisition + dry-run, from 91-01)
  </read_first>
  <action>
    Ensure every available FY2013–2021 county workbook is present under gitignored `_mn-recon/` (the driver downloads on demand; pre-fetch surfaces dead URLs). Then, BEFORE any write, run `node scripts/loadMNOSABatch.js --fy <YYYY> --entity-type county --dry-run` for each FY 2013–2021 (skip the 2018 gap — no county_url): confirm ~85–87 counties/yr, county basis all-unknown (no GAAPInd), zero failures, zero writes, and a sample county (Aitkin/Hennepin) ties. Capture per-year processed counts + the cross-FY county residual for Task 5. Note any non-resolving URL.
  </action>
  <acceptance_criteria>
    - Every available FY2013–2021 county workbook opens with exceljs (Governmental Funds present)
    - A county dry-run for each available FY exits 0 with zero writes and zero unhandled failures; the 2018 gap is logged (no county_url), not an error
    - Per-year county counts captured for the SUMMARY; a sample county ties to its row totals
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live county load FY2013–2021 (serial, idempotent, sourced, entity_type='county')</name>
  <files>scripts/loadMNOSABatch.js</files>
  <read_first>
    - scripts/loadMNOSABatch.js (live path; --entity-type county; failures log)
    - scripts/loadMNOSA.js (importEntity entityType + municipalityName threading)
    - auto-memory project_utah_loader_entity_type_and_display_names (entity_type='county' mandatory)
  </read_first>
  <action>
    With `.env` SUPABASE_SERVICE_KEY sourced, run the county load LIVE (no --dry-run) on the main tree, SERIALLY, for each available FY 2013 → 2021 (skip 2018): `node scripts/loadMNOSABatch.js --fy <YYYY> --entity-type county`. Each county writes operating + revenue as `entity_type='county'` named "<Name> County" with the county source_url. Per-county errors append to `scripts/load-mn-counties.failures.txt` without aborting. Then run `node scripts/refreshMNPopulations.js --entity-type county` to set each county's population to its latest filed FY (D-06). Capture per-FY processed/written counts.

    NOTE (autonomous:false): production-write task — confirm target Supabase (kxsdzaojfaibhuzmclfq) with the user before the first live FY, then proceed (idempotent + guarded).
  </action>
  <acceptance_criteria>
    - The live county load completes for all available FY2013–2021; counties exist as entity_type='county' named "<Name> County" with operating + revenue rows, each stamped the MN OSA data_source + non-null county source_url + source_date
    - No phantom city rows for counties (every "<Name> County" row is entity_type='county')
    - After refreshMNPopulations --entity-type county, sampled counties have non-zero population (per-capita); Hennepin County population is plausible (~1.28M)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: City→county linking from ParentEntityName (scripts/linkMNCitiesToCounties.js)</name>
  <files>scripts/linkMNCitiesToCounties.js</files>
  <read_first>
    - scripts/linkOhioCitiesToCounties.js (the full linker shape: build city→county map, load city+county munis, set-if-different idempotent county_id update, link-residual, --dry-run) — adapt the SOURCE from OI_Demographics tab to the in-row ParentEntityName column
    - scripts/loadMNOSA.js (entityCounty reads ParentEntityName; enumerateEntities; getSupabase)
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md (D-04 sourced linking via ParentEntityName, no authored map; D-05 graph + counties NULL)
  </read_first>
  <action>
    Create `scripts/linkMNCitiesToCounties.js` (ESM; reuse getSupabase + the guarded set-if-different update; mirror linkOhioCitiesToCounties.js). Build a city→county map from the most recent city workbook (`_mn-recon/cired_23_data.xlsx`) using `entityCounty(wb, cityName, 'city')` for every enumerated city (fall back to an older FY workbook for a city absent from 2023). For every MN city municipality: map its bare ParentEntityName ("Hennepin") to the parent municipality "<County> County" ("Hennepin County", entity_type='county', state='MN'); if found and county_id differs → UPDATE municipalities.county_id (idempotent set-if-different, guarded `.eq('entity_type','city')`); if blank or no matching county → link-residual (no phantom). Support `--dry-run`. Counties keep county_id NULL. Run live after the dry-run looks right; capture linked-count + residual.
  </action>
  <acceptance_criteria>
    - `node scripts/linkMNCitiesToCounties.js --dry-run` prints a plausible link count (most of the 858 cities) + any unmatched residual, zero writes
    - After the live run, a DB probe shows the large majority of MN cities have a non-null county_id → an entity_type='county' MN municipality (e.g. Minneapolis.county_id → "Hennepin County"; Saint Paul → "Ramsey County")
    - Counties have county_id NULL; no phantom county municipality created by the linker
    - Re-running the linker changes 0 county_id values (idempotent)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify the Minnesota state node + the US→MN→county→city graph</name>
  <files>scripts/linkMNCitiesToCounties.js</files>
  <read_first>
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md (D-05 pre-existing MN state node — verify, do not duplicate)
    - feedback_supabase_migration_mcp (read-only probes via mcp__supabase-local)
  </read_first>
  <action>
    Read-only: confirm exactly ONE "Minnesota" entity_type='state' node exists (id d4b4897d, pop 5,706,494); do NOT create a second. Confirm a full chain resolves for a sample: Minneapolis (city) → county_id → "Hennepin County" (entity_type='county') and the county is under MN. Confirm counties have county_id NULL. If the state node were somehow missing or duplicated, report it (the only write here would be ensuring a single node — but it already exists, so expect verify-only).
  </action>
  <acceptance_criteria>
    - Exactly one MN entity_type='state' node ("Minnesota") exists; no duplicate created
    - Minneapolis → "Hennepin County" (county, entity_type='county') chain resolves; Saint Paul → "Ramsey County"
    - All MN counties have county_id NULL (top sub-state tier)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: In-phase verification + commit scripts/mnCountyResidual.json</name>
  <files>scripts/mnCountyResidual.json</files>
  <read_first>
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md (D-08 residual)
    - scripts/mnCityResidual.json (the committed residual-file shape to mirror)
    - reference_treasury_budgets_probe_columns (total_budget/hierarchy)
  </read_first>
  <action>
    Run read-only verification + an idempotency re-run:
    - **County spot-check (MNCO-01):** read back a county's stored FY2021 operating + revenue totals (e.g. Hennepin County) and confirm they match the workbook; county rows are sourced + have non-zero population.
    - **Coverage:** count distinct MN counties with budgets + per-(FY,dataset) row counts; confirm ~85–87/yr across FY2013–2021.
    - **Linking proof (MNLINK-01):** count MN cities with non-null county_id; confirm the majority link; confirm Minneapolis→"Hennepin County".
    - **Idempotency:** re-run one county FY load + the linker; confirm 0 new municipalities, 0 changed county_id, unchanged row counts.
    - **Residual:** write + commit `scripts/mnCountyResidual.json` (note + counties array of {name, reason}) for counties enumerated with no financial total (expect none); confirm none exist as phantom municipalities.
  </action>
  <acceptance_criteria>
    - A sampled county's stored FY2021 totals match the workbook; county rows sourced + non-zero population
    - MN counties with budgets ≈85–87/yr; every county row has a non-null source_url + the MN OSA data_source
    - Minneapolis.county_id → "Hennepin County"; the majority of MN cities have non-null county_id
    - Idempotency re-run: 0 new munis, 0 changed county_id, unchanged per-(FY,dataset) county row counts
    - `scripts/mnCountyResidual.json` committed; listed counties (if any) do not exist as municipalities
  </acceptance_criteria>
</task>

</tasks>

<verification>
- County load ran serially FY2013–2021; counties live as entity_type='county' ("<Name> County"), sourced + per-capita (MNCO-01).
- Every (most) MN city has county_id → its "<County> County" parent, sourced from the city workbook ParentEntityName column (no authored map); counties standalone; the Minnesota state node verified → US→Minnesota→county→city graph (MNLINK-01 data layer).
- Idempotent + source-safe: re-run creates 0 munis, changes 0 county_id, unchanged row counts.
- `scripts/mnCountyResidual.json` committed; no phantom county municipalities.
- Frontend/integration render check is plan 91-03; ACFR reconciliation + UAT are Phase 93.
</verification>
