---
phase: 91-county-loads-data-model-linking-mnco-01-mnlink-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadMNOSA.js
  - scripts/loadMNOSABatch.js
  - scripts/refreshMNPopulations.js
  - scripts/loadMNOSA.test.mjs
autonomous: true
requirements: [MNCO-01]
must_haves:
  truths:
    - "scripts/loadMNOSA.js importEntity gains a municipalityName override: when provided it is used as the DB name (treasury_ensure_municipality p_name) while entityName remains the workbook row-lookup key — so a county whose workbook Entity Name is 'Aitkin' is stored as 'Aitkin County' (CONTEXT D-02). All Phase 89/90 city behavior is unchanged when municipalityName is omitted (no regression)"
    - "scripts/loadMNOSABatch.js supports --entity-type county: acquires the county workbook from resolveSourceUrl(fy,'county') (the cored_/county_ files), enumerates the county roster, and loops importEntity with entityType='county' + municipalityName='<Name> County', writing entity_type='county' (never a phantom city row); --entity-type city is unchanged"
    - "scripts/refreshMNPopulations.js reconciles the workbook county name ('Aitkin') with the DB county name ('Aitkin County') so the county population refresh actually matches + updates rows (CONTEXT D-06); --entity-type city is unchanged"
    - "A county FY2021 --dry-run processes ~87 counties with zero writes, zero failures, county basis null (no GAAPInd — D-03), and a sample county (Aitkin) tree sum ties to its row Total Revenues/Total Expenditures; offline tests lock the municipalityName override + county enumeration"
  artifacts:
    - path: "scripts/loadMNOSABatch.js"
      provides: "Batch driver extended for --entity-type county (county workbook acquisition + '<Name> County' canonical naming + entity_type='county' loop)"
      contains: "entity-type"
    - path: "scripts/loadMNOSA.js"
      provides: "importEntity with a municipalityName DB-name override (workbook-lookup vs DB-name split) for county canonical naming"
      contains: "municipalityName"
  key_links:
    - from: "MN OSA county workbooks (cored_<YY>/county_<YY>, Governmental Funds sheet)"
      to: "per-county (operating tree, revenue tree, population) ready to write as entity_type='county' '<Name> County'"
      via: "loadMNOSABatch --entity-type county -> importEntity(entityType:'county', municipalityName:'<Name> County') (dry-run in this plan)"
      pattern: "loadMNOSABatch"
---

<objective>
Extend the Phase 89/90 tooling for COUNTIES so Phase 91-02 can live-load them. Three small, additive changes — no new tree/parse/write logic:
1. Add a `municipalityName` DB-name override to `importEntity` (workbook row-lookup name vs canonical DB name) so counties store as "`<Name> County`" (CONTEXT D-02 — MN has same-named cities + counties, so bare names collide).
2. Teach `loadMNOSABatch.js` `--entity-type county` (acquire the county workbook, enumerate, loop `importEntity` with `entityType='county'` + `municipalityName`).
3. Fix `refreshMNPopulations.js` so the county population refresh reconciles the workbook name ("Aitkin") with the DB name ("Aitkin County").

No live load happens here — prove a county FY2021 `--dry-run` parses the full roster + ties on a sample, and lock the new behavior with offline tests. The live FY2013–2021 county load + state node + linking are plan 91-02.

Phase 89 already proved `importEntity(entityType:'county')` ties on Aitkin/Anoka FY2021; this plan only adds the canonical-naming override + the batch/refresh county wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md
@scripts/loadMNOSA.js
@scripts/loadMNOSABatch.js
@scripts/refreshMNPopulations.js
@scripts/loadOhioAOS.js

<interfaces>
<!-- All county parse/tie logic already exists (Phase 89). This plan adds a DB-name override +
     county wiring in the batch driver + a county-name fix in the refresh pass. -->

importEntity municipalityName override (mirror Ohio importCity, loadOhioAOS.js lines ~471–518):
- Current `importEntity(supabase, workbook, { entityName, fiscalYear, sourceUrl, sourceDate, dryRun, entityType })` uses `entityName` for BOTH the workbook row lookup AND `treasury_ensure_municipality` p_name.
- Add `municipalityName` (optional): when set, it is the DB name (p_name); `entityName` stays the workbook lookup key. Default null → use entityName (preserves all Phase 89/90 city calls exactly). Include municipalityName (or entityName) as the summary's display name.

loadMNOSABatch --entity-type county:
- Add `entityType` to opts (default 'city') + `--entity-type` CLI option (mirror loadOhioAOSBatch CLI).
- `acquireWorkbook(fy, file, entityType)`: for county, use `resolveSourceUrl(fy,'county')` and a local cache filename `_mn-recon/county_<YY>_data.xlsx` (the manifest URL handles the cored_/county_/dash differences — the local filename is just a cache path).
- Loop: for each enumerated county name (workbook gives bare "Aitkin"), call importEntity with `entityType:'county'`, `municipalityName: \`${name} County\`` (only append " County" if not already suffixed). source_url = resolveSourceUrl(fy,'county').
- Basis tally: counties have no GAAPInd → all 'unknown'/null (D-03); that is expected, not a failure.

refreshMNPopulations county-name fix (D-06):
- The DB county row name is "<Name> County" but enumerateEntities returns "Aitkin". When `entityType==='county'`, match the DB muni by stripping a trailing " County" from the muni name before normalizing (or append " County" to the workbook key). Ensure the county population refresh actually updates rows.

county FY2021 workbook is already at `_mn-recon/county_21_data.xlsx` (Phase 89). The county Total Revenues/Total Expenditures tie was proven in Phase 89 (Aitkin/Anoka). NOTE: county files have NO GAAPInd / NO ParentEntityName (Phase 89 D-08) — handled by the loader returning null/''.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add municipalityName DB-name override to importEntity</name>
  <files>scripts/loadMNOSA.js</files>
  <read_first>
    - scripts/loadMNOSA.js (importEntity current signature + the treasury_ensure_municipality call)
    - scripts/loadOhioAOS.js (importCity municipalityName override — lines ~471–518: cityName for lookup, municipalityName for p_name, dbName fallback)
    - .planning/phases/91-county-loads-data-model-linking-mnco-01-mnlink-01/91-CONTEXT.md (D-02 canonical "<Name> County")
  </read_first>
  <action>
    Add an optional `municipalityName` to `importEntity`'s opts (default null). Use `const dbName = municipalityName || entityName;` for `treasury_ensure_municipality` p_name; keep `entityName` as the workbook row-lookup key passed to buildRevenueTree/buildExpenditureTree/entityPopulation/entityCounty/entityBasis. Reflect dbName in the returned summary's display name field. Do NOT change any other behavior — when municipalityName is omitted (every Phase 89/90 city call) behavior is identical.
  </action>
  <acceptance_criteria>
    - `importEntity` accepts `municipalityName`; with it set, `treasury_ensure_municipality` would be called with p_name=municipalityName (verify by reading the code path) while tree/population lookups still use entityName
    - Omitting municipalityName preserves the exact prior behavior (dbName === entityName)
    - `node --test scripts/loadMNOSA.test.mjs` still passes (no city regression)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Teach loadMNOSABatch.js --entity-type county</name>
  <files>scripts/loadMNOSABatch.js</files>
  <read_first>
    - scripts/loadMNOSABatch.js (acquireWorkbook, the loop, the CLI)
    - scripts/loadOhioAOSBatch.js (the --entity-type county CLI + county canonical naming via municipalityName)
    - scripts/loadMNOSA.js (resolveSourceUrl(fy,'county'), importEntity with municipalityName + entityType)
  </read_first>
  <action>
    Add `entityType` to `loadMNOSABatch` opts (default 'city') and a `--entity-type <city|county>` CLI option (validate city|county). In `acquireWorkbook`, when entityType='county' resolve `resolveSourceUrl(fy,'county')` and cache to `_mn-recon/county_<YY>_data.xlsx`. In the loop, for counties compute `municipalityName = name.endsWith(' County') ? name : \`${name} County\`` and call `importEntity(supabase, workbook, { entityName: name, municipalityName, fiscalYear, sourceUrl: resolveSourceUrl(fy, entityType), sourceDate, dryRun, entityType })`. The basis tally for counties will be all-unknown (no GAAPInd) — keep counting, do not treat as failure. Keep `--entity-type city` behavior identical.
  </action>
  <acceptance_criteria>
    - `node scripts/loadMNOSABatch.js --fy 2021 --entity-type county --file _mn-recon/county_21_data.xlsx --dry-run` exits 0, processes ~87 counties, zero failures, zero writes, and the summary shows county basis all-unknown (no GAAPInd)
    - The dry-run output / results store the DB name as "<Name> County" (e.g. an Aitkin entry → "Aitkin County") while reading the workbook row "Aitkin"
    - `--entity-type city` still works unchanged (a quick `--fy 2023 --file _mn-recon/cired_23_data.xlsx --dry-run --limit 5` is sane)
    - No phantom-city risk: the county path threads entityType='county' into importEntity
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix refreshMNPopulations county name reconciliation + county dry-run proof + offline tests</name>
  <files>scripts/refreshMNPopulations.js, scripts/loadMNOSA.test.mjs</files>
  <read_first>
    - scripts/refreshMNPopulations.js (the latest-FY map + the DB-muni match by normalizeLabel)
    - scripts/loadMNOSA.js (enumerateEntities returns bare names; entityPopulation)
    - scripts/loadMNOSA.test.mjs (test structure; HAVE_COUNTY gate)
  </read_first>
  <action>
    Fix `refreshMNPopulations.js` so that for `--entity-type county` the DB muni name ("Aitkin County") matches the workbook-derived key ("Aitkin"): when building/looking up the latest-population map for counties, normalize both sides by stripping a trailing " County" (or appending it consistently) before comparing. Verify with a county dry-run that it would update >0 rows the first time (and 0 after a real refresh in 91-02).

    Add offline tests to scripts/loadMNOSA.test.mjs (gated on HAVE_COUNTY): a `loadMNOSABatch({fy:2021, entityType:'county', file:'_mn-recon/county_21_data.xlsx', dryRun:true})` run returns `processed >= 80`, `failures.length === 0`, and a results entry whose DB name is "Aitkin County" (municipalityName applied) while its totals tie (rev/op finite, > 0). Also assert importEntity with municipalityName sets the summary display name to the override.
  </action>
  <acceptance_criteria>
    - `node scripts/loadMNOSABatch.js --fy 2021 --entity-type county --file _mn-recon/county_21_data.xlsx --dry-run` shows ~87 counties, 0 failures, an "Aitkin County" entry with finite non-zero totals
    - `refreshMNPopulations.js --entity-type county --dry-run` reports a would-update count > 0 BEFORE 91-02's live refresh (county names reconcile; not all "no source match")
    - `node --test scripts/loadMNOSA.test.mjs` exits 0 with the new county-batch + municipalityName cases passing and all prior city cases green
    - No DB writes occur in this plan
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadMNOSA.test.mjs` → all pass (Phase 89/90 + new county-batch + municipalityName cases).
- `node scripts/loadMNOSABatch.js --fy 2021 --entity-type county --file _mn-recon/county_21_data.xlsx --dry-run` → ~87 counties, "<Name> County" DB names, county basis unknown (no GAAPInd), 0 failures, 0 writes, Aitkin ties.
- importEntity gains the municipalityName override (city behavior unchanged); loadMNOSABatch gains --entity-type county; refreshMNPopulations reconciles county names.
- No DB writes — the live FY2013–2021 county load + state node + linking + residual are plan 91-02.
</verification>
