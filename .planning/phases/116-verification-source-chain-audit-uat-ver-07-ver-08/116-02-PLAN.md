---
phase: 116-verification-source-chain-audit-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase116-cohort-audit.mjs
  - .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-COHORT-AUDIT.md
autonomous: true
requirements: [VER-07, VER-08]

must_haves:
  truths:
    - "The full 50-node cohort passes the invariant set (INV-1 every state row has data_source + source_url + source_date; INV-2 0 residue stale *-gf-* data_sources; INV-3 0 out-of-window FYs against the updated bounds; INV-4 0 dup (muni,fy,dataset); INV-5 0 orphan data_source_id; INV-6 all 29 ACFR states' rows ACFR-basis-labelled with 0 NASBO labels; INV-7 a NASBO control state untouched)"
    - "The 10 tranche-3 window bounds are added (IN 2002-2025, AZ 2002-2024, OR 2022-2025, MO 2012-2025, CO 2023-2025, SC 2002-2025, KY 2002-2025 [hole FY2023], UT 2019-2025, AL 2002-2025, LA 2002-2025) and the 4 deepened bounds are widened (NJ 2002-2025, CT 1988-2025, WI 2000-2025, MA 2001-2025 [holes FY2002/2004/2005/2021])"
    - "NEW pre-GASB-34 basis-label invariant (INV-8): CT FY1988-2001, WI FY2000-2001, and MA FY2001 rows carry the distinct 'pre-GASB-34 combined statement basis' label — visibly different from the 'GAAP basis' label on the same node's modern years — while every other displayed row is GAAP-basis-labelled (CT FY2006 = GAAP, not pre-34)"
    - "AL Sep-30 invariant: all AL rows have source_date {fy}-09-30 and fiscal_year_start_month = 10"
    - "LOAD-01 proven end-to-end (VER-08): after re-running one representative loader per batch (a tranche-3 state + a deepened state) for one FY via the guarded treasury_sync_budget_tree path, the re-run writes 0 net change AND treasury.data_sources has 0 residue rows with NO manual re-clean (the phase-111 atomic-lifecycle fix holds — the key regression vs the WR-05 debt of prior tranches)"
    - "Honest-hole reconciliation: every FY the loadlogs say is LOADED is present in the DB; every absent FY is absent BY DESIGN per a recorded disposition (KY FY2023, MA FY2002/04/05/2021, AZ FY2025 not-yet-sourced, and the recon/format-locked window floors) — an honest disclosed hole is a PASS, not a gap"
    - "Cohort shape confirmed: 29 ACFR states + 21 NASBO states (each NASBO state exactly 2 operating rows, operating-only, untouched); 0 anomalies across all invariants"
  artifacts:
    - path: "scripts/verify-phase116-cohort-audit.mjs"
      provides: "Read-only 50-node cohort source-chain audit for the tranche-3-augmented + deepened cohort, incl. pre-GASB-34 label invariant"
      contains: "pre-GASB-34"
    - path: ".planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-COHORT-AUDIT.md"
      provides: "Per-invariant results, row-count table, hole reconciliation, LOAD-01 idempotency+0-residue-no-manual-reclean evidence, headline verdict"
      contains: "INV-"
  key_links:
    - from: "scripts/verify-phase116-cohort-audit.mjs"
      to: "treasury.budgets + treasury.data_sources"
      via: "read-only invariant queries over the 50-node cohort"
      pattern: "data_sources"
    - from: "representative loaders (1 per batch)"
      to: "treasury.budgets"
      via: "guarded never-overwrite treasury_sync_budget_tree re-run (idempotency + LOAD-01 residue proof)"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Run the full 50-node cohort source-chain audit on the tranche-3-augmented + deepened cohort (VER-07 parts b+c, VER-08): assert the Phase 102/106/110 invariant set over the 29 ACFR-state windows (19 prior + 10 tranche-3) plus the 4 widened deepening windows, add a NEW pre-GASB-34 basis-label distinctness invariant (INV-8), confirm loader idempotency, and prove LOAD-01 end-to-end (0 data_sources residue with NO manual re-clean — the phase-111 atomic-lifecycle fix means the WR-05 "re-flag expected every audit" debt of prior tranches should finally be gone).

Purpose: prove the whole cohort's source chain stayed clean and residue-free after the tranche-3 + deepening loads — the 110 10/10-invariant precedent re-run on the larger surface, with LOAD-01 now provable end-to-end.
Output: `scripts/verify-phase116-cohort-audit.mjs` + `116-COHORT-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-RESEARCH.md
@scripts/verify-phase110-cohort-audit.mjs
@.planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-VERIFICATION.md

<interfaces>
<!-- verify-phase110-cohort-audit.mjs is the STRUCTURAL TEMPLATE (INV-1..INV-7 + MI Sep-30 + GA supersede,
     read-only, exit 0/2, createClient env-load). Changes for 116: +10 tranche-3 window bounds, +4 widened
     deepening bounds, ACFR set → 29 states, a NEW pre-GASB-34 label invariant (INV-8), AL Sep-30 check, and
     the LOAD-01 end-to-end residue proof (WR-05 is FIXED as of phase 111 — expect 0 residue, NO manual clean). -->

WINDOW_BOUNDS — ADD the 10 tranche-3 states (in-window holes are legal absent rows; INV-3 only fails rows
OUTSIDE bounds):
  IN 2002–2025 · AZ 2002–2024 · OR 2022–2025 · MO 2012–2025 · CO 2023–2025 ·
  SC 2002–2025 · KY 2002–2025 (hole 2023) · UT 2019–2025 · AL 2002–2025 · LA 2002–2025
WIDEN the 4 deepening bounds (were narrower after v2.13):
  NJ 2002–2025 (was 2020–2025) · CT 1988–2025 (was 2002–2025) · WI 2000–2025 (was 2002–2025) ·
  MA 2001–2025 (holes 2002, 2004, 2005, 2021)
Keep the other 15 ACFR bounds unchanged (CA/NY/FL/TX/MN/OH/VA/PA/IL + NC/GA/MD/TN/WA/MI). All NASBO FY2023–2024.

ACFR-GAAP-labelled set (INV-6) is now 29 states: the 19 prior + IN AZ OR MO CO SC KY UT AL LA. Each carries
ACFR provenance ("…State ACFR — General Fund (FY{fy} actual, GAAP basis)"); 0 NASBO labels on any of the 29.

NEW pre-GASB-34 label invariant (INV-8): rows for CT FY1988–2001, WI FY2000–2001, MA FY2001 carry a data_source
containing "pre-GASB-34 combined statement basis" (exact: "{State} State CAFR — General Fund (FY{fy} actual,
pre-GASB-34 combined statement basis)"), and those same nodes' modern years carry "GAAP basis" — the two distinct
so the app can render them differently. CT FY2006 is GASB-34-era → GAAP basis (NOT pre-34).

AL Sep-30 invariant: all AL rows have source_date {fy}-09-30 and fiscal_year_start_month = 10 (same class as MI).

INV-7 NASBO control: pick dynamically from the 21 remaining NASBO states (NOT any of the 29 ACFR states) —
exactly 2 NASBO operating rows, operating-only, NASBO provenance, untouched.

Row-count expectations to confirm (op+rev unless noted; read the SUMMARYs to confirm exact counts):
  Tranche-3: IN 24+24, AZ 23+23, OR 4+4, MO 14+14, CO 3+3, SC 24+24, KY 23+23 (FY2023 hole), UT 7+7,
             AL 24+24, LA 24+24.
  Deepening deltas: NJ now 24+24, CT now 38+38, WI now 26+26, MA now 21+21 (holes 2002/04/05/2021).
  Cohort: 29 ACFR states + 21 NASBO states (2 op rows each), 0 anomalies.

LOAD-01 end-to-end (VER-08, the headline regression): re-run ONE representative loader per batch for a single FY —
e.g. a tranche-3 state (SC or IN) FY2025 and a deepened state (CT FY2025) — op+rev, via each loader's guarded
never-overwrite path (treasury_sync_budget_tree; source .env first; main tree only; NEVER treasury_sync_city_budget).
Expect "0 rows / 0 net change". THEN re-query treasury.data_sources for residue on those states' dataset_ids:
expect 0 residue rows WITH NO MANUAL RE-CLEAN. Prior tranches (102/106/110) had to re-clean WR-05 residue after
every idempotency re-run; phase 111 made the data_sources lifecycle atomic/ephemeral, so this run should be the
first cohort audit where residue is 0 without intervention — record this explicitly as the LOAD-01 proof.

Hole reconciliation (PASS conditions — do NOT re-litigate): confirm (i) every loadlog-LOADED FY is present;
(ii) every absent FY is absent BY DESIGN — KY FY2023 (broken-font, NASBO retained), MA FY2002/2004/2005/2021
(115-03 documented-unrecoverable), AZ FY2025 (not sourced at load), window floors (OR 2022, CO 2023, MO 2012,
UT 2019; deepening floors CT 1988 / WI 2000 = pre-GASB-34/multi-file edge, NJ 2002 = archive edge);
(iii) the node renders each non-contiguous range honestly (per-FY rows + label + chip, no interpolation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the cohort audit for the tranche-3 + deepening cohort and run the full invariant set</name>
  <read_first>
    - scripts/verify-phase110-cohort-audit.mjs (template — INV-1..INV-7, MI Sep-30, GA supersede, WINDOW_BOUNDS, ACFR set, read-only, exit 0/2)
    - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-VERIFICATION.md and .planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-VERIFICATION.md (tranche-3 windows, row counts, clamp/hole facts, AL Sep-30)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-02-CT-WI-LOADLOG.md and 115-03-MA-LOADLOG.md (pre-34 basis label string, deepened windows, MA holes)
  </read_first>
  <action>
    Create scripts/verify-phase116-cohort-audit.mjs from verify-phase110-cohort-audit.mjs (reuse its createClient env-load, the invariants, and the exit-0/2 contract). Apply:

    1. ADD the 10 tranche-3 WINDOW_BOUNDS and WIDEN the 4 deepening bounds (NJ 2002-2025, CT 1988-2025, WI 2000-2025, MA 2001-2025 with holes) per the interfaces block; keep the other 15 ACFR bounds and the NASBO 2023-2024 default.
    2. EXPAND the ACFR-GAAP set to all 29 states so INV-6 covers each state's operating+revenue rows (ACFR label present, 0 NASBO labels).
    3. ADD INV-8 pre-GASB-34 label invariant: CT FY1988-2001, WI FY2000-2001, MA FY2001 rows contain "pre-GASB-34 combined statement basis"; those nodes' modern years contain "GAAP basis"; the two are distinct. CT FY2006 = GAAP.
    4. ADD the AL Sep-30 check: all AL rows source_date {fy}-09-30, fiscal_year_start_month = 10. (Keep the existing MI Sep-30 check.)
    5. UPDATE INV-7 to pick a NASBO control from the 21 remaining NASBO states (none of the 29 ACFR states).
    6. Keep INV-1..INV-5 (data_source/url/date present; 0 residue *-gf-* data_sources; 0 out-of-window FYs; 0 dup (muni,fy,dataset); 0 orphan data_source_id) and confirm the row-count table.

    Run read-only against the live treasury schema. Exit 0 only if all invariants PASS. Any FAIL is a source-chain defect → fix in-phase via the source-safe path (NEVER treasury_sync_city_budget), then re-run. Capture per-invariant output for the report.
  </action>
  <verify>
    <automated>node scripts/verify-phase116-cohort-audit.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase116-cohort-audit.mjs exists; makes no writes (read-only)
    - WINDOW_BOUNDS has all 10 tranche-3 entries + the 4 widened deepening bounds + the 15 unchanged ACFR bounds
    - INV-6 set includes all 29 ACFR states; INV-7 uses a NASBO (non-ACFR) control
    - INV-8 pre-GASB-34 label invariant present and passing (CT/WI/MA pre-34 rows distinct from GAAP)
    - AL Sep-30 check present and passing; MI Sep-30 check retained
    - Row counts confirmed per the interfaces table; all invariants print PASS and the script exits 0 (or any failure fixed-in-phase via the source-safe path and the re-run exits 0)
  </acceptance_criteria>
  <done>The full invariant set (incl. INV-8 pre-34 label + AL Sep-30) runs read-only over the 50-node cohort and exits 0; any defect fixed in-phase and re-confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: LOAD-01 idempotency + 0-residue-no-manual-reclean proof, hole reconciliation, write 116-COHORT-AUDIT.md</name>
  <read_first>
    - .planning/milestones/v2.13-phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md (report format + how WR-05 residue re-clean was previously handled — this run should NOT need it)
    - .planning/phases/111-loader-debt-atomic-data-sources-upsert-load-01 (LOAD-01 fix context — the atomic ephemeral data_sources lifecycle)
    - The Task 1 audit output
  </read_first>
  <action>
    Re-run one representative loader per batch for a single FY via the guarded path: a tranche-3 state (e.g. SC FY2025 op+rev) and a deepened state (e.g. CT FY2025 op+rev), through treasury_sync_budget_tree (source .env first; main tree; NEVER treasury_sync_city_budget). Confirm each reports 0 net change. THEN re-query treasury.data_sources for residue on those states' dataset_ids and confirm 0 residue rows WITH NO MANUAL RE-CLEAN — the LOAD-01 end-to-end proof (contrast with 102/106/110 which required a re-clean after every idempotency run). Re-run the Task 1 audit to confirm INV-2 still passes.

    Reconcile holes: confirm every loadlog-LOADED FY is present in the DB and every absent FY is absent-by-design per a recorded disposition (KY FY2023, MA FY2002/04/05/2021, AZ FY2025, and the recon/format-locked floors). Do not re-litigate gaps 113/114/115 judged unrecoverable.

    Write 116-COHORT-AUDIT.md: per-invariant PASS/FAIL (INV-1..INV-8 + AL/MI Sep-30 + any supersede), the row-count table (29 ACFR + 21 NASBO), the hole-reconciliation table, the LOAD-01 idempotency+residue evidence (with the "no manual re-clean" call-out), and a headline verdict. This file + 116-REDERIVATION.md are what the gsd-verifier reads for VER-07/VER-08.
  </action>
  <verify>
    <automated>node scripts/verify-phase116-cohort-audit.mjs; test -f .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-COHORT-AUDIT.md && grep -qi "INV-" .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-COHORT-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 116-COHORT-AUDIT.md exists with per-invariant results (INV-1..INV-8 + AL/MI Sep-30), the 29-ACFR/21-NASBO row-count table, the hole-reconciliation table, and a headline verdict
    - The LOAD-01 proof is recorded: idempotency re-run = 0 net change AND 0 data_sources residue with an explicit "no manual re-clean" statement
    - Every absent FY has a recorded absent-by-design disposition; every loaded FY confirmed present
    - The cohort audit script exits 0 (or any defect fixed-in-phase via the source-safe path and re-confirmed)
  </acceptance_criteria>
  <done>Cohort audit exits 0 over the 50-node cohort; LOAD-01 proven end-to-end (0 residue, no manual re-clean); holes reconciled; 116-COHORT-AUDIT.md written.</done>
</task>

<task type="auto">
  <name>Task 3: Commit audit harness + report, SUMMARY</name>
  <read_first>
    - .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-COHORT-AUDIT.md
  </read_first>
  <action>
    Commit the audit harness (feat) and the audit report (docs) atomically per the executor commit protocol. Write 116-02-SUMMARY.md per the summary template with the invariant tally, the LOAD-01 end-to-end result, and the cohort shape.
  </action>
  <verify>
    <automated>git log --oneline --grep="116-02" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - feat + docs commits exist for 116-02
    - 116-02-SUMMARY.md exists with Self-Check section
  </acceptance_criteria>
  <done>Audit harness + report committed; SUMMARY written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local script → treasury.budgets + treasury.data_sources (read) | Read-only invariant queries via service key (env-only). |
| representative loader re-run → treasury.budgets (write) | Idempotency re-run via guarded treasury_sync_budget_tree only; never treasury_sync_city_budget (not source-safe). |

## STRIDE Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-116-02 | Tampering | idempotency re-run writes | mitigate | Only the guarded never-overwrite path (treasury_sync_budget_tree) for a single FY; expect 0 net change; NEVER treasury_sync_city_budget. |
| T-116-03 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Read from ../.env only; never inlined. |
| T-116-SC | Tampering | package installs | mitigate | No new packages — Node built-ins + existing deps only. |
</threat_model>

<verification>
- scripts/verify-phase116-cohort-audit.mjs runs read-only over the 50-node cohort and exits 0 (all invariants incl. INV-8 pre-34 label + AL Sep-30 pass).
- LOAD-01 proven end-to-end: idempotency re-run = 0 net change, 0 data_sources residue with no manual re-clean.
- 116-COHORT-AUDIT.md records per-invariant results, row counts, hole reconciliation, and the LOAD-01 evidence.
</verification>

<success_criteria>
- VER-07 parts (b)(c): 50-state cohort source-chain audit clean (0 NULL/fragile/residue/out-of-window/dup/orphan; every displayed row basis-labelled incl. distinct pre-GASB-34 label; un-upgraded NASBO states untouched).
- VER-08: 0 data_sources residue with no manual re-clean (LOAD-01 proven end-to-end).
</success_criteria>

<output>
Create `.planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-02-SUMMARY.md` when done.
</output>
