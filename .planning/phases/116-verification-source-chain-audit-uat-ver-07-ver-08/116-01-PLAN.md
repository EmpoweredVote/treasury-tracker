---
phase: 116-verification-source-chain-audit-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase116-rederive.mjs
  - .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-REDERIVATION.md
autonomous: true
requirements: [VER-07]

must_haves:
  truths:
    - "Every sampled FY's General-Fund printed total, blind-re-extracted from the source ACFR/CAFR PDF, ties to the live treasury.budgets value at exactly 0 delta — or a delta explained verbatim from the loadlog's documented printed-vs-line-sum rounding notes (WI pre-34 −2K is the known candidate)"
    - "The re-derivation imports ZERO scripts/process*.js loaders AND ZERO shared parser modules (maAcfrExtract.mjs, pre34Extract.mjs, njAcfrExtract.mjs) — it re-keys the GF column with its own minimal extraction, so a transposed-digit transcription error cannot hide behind the loader's self-tie"
    - "The sample covers every tranche-3 state's bookends (oldest loaded FY + newest loaded FY) on BOTH revenue and operating, plus documented random middles for the deep windows, plus EVERY clamp year (MO 6 years, CO both years, UT FY2022) and the AZ FY2024 Drive-link caveat year"
    - "The sample covers the deepened years: NJ new floor (FY2002) + a middle, CT pre-34 bookends (FY1988 + FY2001) + FY2006 (OCR-recovered), WI pre-34 (FY2000 + FY2001), MA FY2001 (pre-34) + FY2014 (recovered)"
    - "Per-state quirks are honored: NJ full-dollars (×1), AL Sep-30 FY-end, thousands (×1000) for the rest; pre-34 years re-keyed from the 'All Governmental Fund Types' Combined Statement General column"
    - "Any non-zero delta is either explained-and-recorded (verbatim, with loadlog reference) or fixed in-phase via the guarded loader path then re-tied — no silent tolerance band"
  artifacts:
    - path: "scripts/verify-phase116-rederive.mjs"
      provides: "Loader-independent blind ACFR re-derivation harness for the tranche-3 + deepening sample"
      contains: "pdftotext"
    - path: ".planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-REDERIVATION.md"
      provides: "The independent re-derivation log: per-FY independent total, live DB total, delta, disposition; headline verdict"
      contains: "Delta"
  key_links:
    - from: "scripts/verify-phase116-rederive.mjs"
      to: "source ACFR/CAFR PDFs"
      via: "pdftotext -table re-extraction (no loader/parser import)"
      pattern: "pdftotext"
    - from: "scripts/verify-phase116-rederive.mjs"
      to: "treasury.budgets"
      via: "read-only DB of stored total_budget per (municipality_id, fiscal_year, dataset_type)"
      pattern: "budgets"
---

<objective>
Independently re-derive (VER-07 part a) a risk-weighted sample of the tranche-3 upgrades (IN, AZ, OR, MO, CO from Phase 113; SC, KY, UT, AL, LA from Phase 114) AND the phase-115 deepened years (NJ FY2002–2019, CT FY1988–2001 + FY2006, WI FY2000–2001, MA FY2001 + FY2014) by BLIND re-extraction from each source PDF, then diff the independent General-Fund printed total against the live treasury.budgets value. This catches a transposed-digit transcription error that a loader's own GF-tie gate (which checks its map against itself) cannot.

Output: `scripts/verify-phase116-rederive.mjs` (loader-independent harness) + `116-REDERIVATION.md` (per-FY tie log). This is the phase-110 mold (49/49) scaled to the tranche-3 + deepening surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-RESEARCH.md
@scripts/verify-phase110-rederive.mjs
@.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-VERIFICATION.md

<interfaces>
<!-- verify-phase110-rederive.mjs is the STRUCTURAL TEMPLATE. Reuse its env-load (../.env.local/../.env),
     DB read, pdftotext-via-execFileSync GF-column "Total revenues"/"Total expenditures" line extraction,
     exact-0 gate, and soft-404 guard. Changes for 116: the tranche-3 + deepening sample, per-state quirks,
     and the pre-GASB-34 Combined-Statement re-key for CT/WI/MA pre-34 years. -->

INDEPENDENCE RULE (blind method): import ZERO scripts/process*.js loaders AND ZERO shared parser modules —
NOT maAcfrExtract.mjs, NOT pre34Extract.mjs, NOT njAcfrExtract.mjs (those ARE the loaders' extraction paths;
reusing them re-tests the loader against itself). Re-key the GF-column printed totals with the harness's own
minimal extraction, exactly like the 102/106/110 harnesses.

TOLERANCE (exact-0 bar, carried from 106/110): PASS only on abs(delta) === 0. The ONLY acceptable non-zero
dispositions are documented printed-vs-line-sum rounding notes from the loadlogs — WI pre-34 −2K (115-02 loadlog)
is the known candidate; scan the tranche-3 loadlogs for any others — each recorded verbatim with reference.
Clamp years (MO's 6, CO both, UT FY2022) use the printed root/control total (which nets the negative) as the bar,
matching how the loader stored total_budget. No tolerance band.

SAMPLE (risk-weighted; each entry checks BOTH revenue and operating unless marked):
  Tranche-3 (Phase 113/114):
    IN  FY2002 + FY2025 + middle
    AZ  FY2002 + FY2024 (Drive-link caveat year — re-derive from the same Drive PDF the loadlog records)
    OR  FY2022 + FY2025
    MO  FY2012 + FY2025 + ALL 6 clamp years (printed-root bar)
    CO  FY2023 + FY2025 (both TABOR-clamped — printed-root bar)
    SC  FY2002 + FY2025 + middle
    KY  FY2002 + FY2025 + middle (do NOT sample FY2023 — honest hole)
    UT  FY2019 + FY2025 + FY2022 clamp
    AL  FY2002 + FY2025 + middle (Sep-30 FY-end → source_date {fy}-09-30)
    LA  FY2002 + FY2025 + middle
  Deepening (Phase 115):
    NJ  FY2002 (new floor) + FY2010 (middle of new range)   [FULL DOLLARS ×1]
    CT  FY1988 + FY2001 (pre-34) + FY2006 (OCR)             [pre-34 years: Combined-Statement re-key]
    WI  FY2000 + FY2001 (pre-34)                            [WI pre-34 −2K rounding note may apply]
    MA  FY2001 (pre-34) + FY2014 (recovered)
  Document the random-middle choices inline so the sample is reproducible. ~50–60 FY-dataset checks.

PER-YEAR URLs + expected values: read each state's LOADLOG (113-0X-{ST}-LOADLOG.md, 114-0X-{ST}-LOADLOG.md,
115-01-NJ / 115-02-CT-WI / 115-03-MA -LOADLOG.md) — these record the URLs actually fetched and the printed totals.
Do NOT use the loader SOURCES maps. NJ FY2018/2019 URLs have literal-space filenames (%20). AZ FY2024 is a
Google-Drive link. Node ids (sanity prefixes): IN 7eb77ada, AZ 866036ee, OR 7686da27, MO 21892bb7, CO 89d2aff1,
SC f0024b19, KY 6d9dfe88, UT 740cffee, AL bc953061, LA b7e9e7cd; NJ/CT/WI/MA resolve by name.

Pre-34 re-key: the CT FY1988–2001 / WI FY2000–2001 / MA FY2001 statements are the "Combined Statement of
Revenues, Expenditures, and Changes in Fund Balances — All Governmental Fund Types" format; General is the 1st
numeric column; anchor on that title (NOT the Higher-Education/proprietary Combined Statement variants). Same
exact-0 bar (WI −2K note excepted).

Fetch guard (soft-404): require Content-Type application/pdf + multi-MB payload, never HTTP 200 alone.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the loader-independent re-derivation harness for the tranche-3 + deepening sample</name>
  <read_first>
    - scripts/verify-phase110-rederive.mjs (structural template — env load, DB read, pdftotext GF-column extraction, exact-0 gate, soft-404 guard)
    - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-01-IN-LOADLOG.md through 113-05-CO-LOADLOG.md (per-year URLs, units, clamp years, AZ Drive caveat, expected totals)
    - .planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-01-SC-LOADLOG.md through 114-05-LA-LOADLOG.md (URLs, KY FY2023 hole, UT clamp, AL Sep-30, expected totals)
    - .planning/phases/115-deepening-recoverable-holes-pre-gasb-34-extractor-deep-02-de/115-01-NJ-LOADLOG.md, 115-02-CT-WI-LOADLOG.md, 115-03-MA-LOADLOG.md (deepened per-year URLs, pre-34 basis, WI −2K note, OCR FY2006 provenance)
  </read_first>
  <action>
    Create scripts/verify-phase116-rederive.mjs modeled on verify-phase110-rederive.mjs. Reuse its env-loading, DB read of stored total_budget per (municipality_id, fiscal_year, dataset_type) resolving municipality_id by entity name at runtime, its pdftotext `-table` GF-column "Total revenues"/"Total expenditures" extraction, and the soft-404 fetch guard. Import ZERO loader modules and ZERO parser modules (process*.js, maAcfrExtract.mjs, pre34Extract.mjs, njAcfrExtract.mjs) — re-key the GF column independently.

    Encode the risk-weighted sample from the interfaces block (~50–60 FY-dataset checks): tranche-3 bookends + documented middles + every clamp year (MO 6, CO 2, UT FY2022) + AZ FY2024 Drive-caveat year; deepening NJ FY2002+FY2010, CT FY1988+FY2001+FY2006, WI FY2000+FY2001, MA FY2001+FY2014. Record the random-middle choices inline for reproducibility. Skip KY FY2023 (honest hole).

    Per-state handling: NJ full dollars (×1); all others thousands (×1000). AL source_date {fy}-09-30. Pre-34 years (CT FY1988–2001, WI FY2000–2001, MA FY2001) re-key from the "All Governmental Fund Types" Combined Statement General (1st) column — anchor on that exact title, not the Higher-Ed/proprietary variants. AZ FY2024 fetched from the Drive URL in the loadlog; if the Drive link is dead, mark that one check BLOCKED-with-reason (not a silent skip). Per-year URLs come from the LOADLOGs, not loader SOURCES. NJ FY2018/2019 %20 filenames.

    Set the PASS bar to abs(delta) === 0 exactly. Clamp years use the printed root/control total as the bar. Print PASS/FAIL + delta per check; exit 0 only if every check is exact-0 (or a documented-rounding exception); exit 2 if any unexplained non-zero surfaces (so Task 2 dispositions it).
  </action>
  <verify>
    <automated>node scripts/verify-phase116-rederive.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase116-rederive.mjs exists and imports no scripts/process*.js and no maAcfrExtract/pre34Extract/njAcfrExtract module (grep the source: 0 such imports)
    - The script runs the full sample across all 10 tranche-3 states + 4 deepened states and prints a per-check delta
    - PASS gate is abs(delta) === 0 exactly (no tolerance band in the source; documented-rounding exceptions listed explicitly)
    - NJ checks use multiplier 1 (full dollars); pre-34 CT/WI/MA years re-key from the "All Governmental Fund Types" Combined Statement
    - All fetches enforce the application/pdf + payload-size guard; AZ FY2024 uses the Drive URL from the loadlog
    - Random-middle choices are documented inline
  </acceptance_criteria>
  <done>Harness runs every sampled FY blind from source, compares to live DB at exact-0, exits 0 (all tie) or 2 (a delta surfaced for Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Disposition every delta and write 116-REDERIVATION.md</name>
  <read_first>
    - .planning/milestones/v2.13-phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-REDERIVATION.md (log-format precedent + verdict headline)
    - The Task 1 harness output (per-check delta table)
    - The relevant per-state LOADLOG for any non-zero delta (its documented rounding notes are the only pre-approved explanations)
  </read_first>
  <action>
    Run scripts/verify-phase116-rederive.mjs and capture every result. Write 116-REDERIVATION.md: a table State | FY | Dataset | Independent re-extracted total | Live DB total_budget | Delta | Disposition, covering all sampled FYs. Record the random-middle choices and the clamp-year dispositions (MO/CO/UT — printed-root bar nets the clamped negative).

    For any non-zero delta: either (a) EXPLAIN with a documented cause (loadlog printed-vs-line-sum rounding — WI pre-34 −2K is the known candidate — recorded verbatim with source location), or (b) declare a defect and FIX in-phase via the loader's guarded never-overwrite path (treasury_sync_budget_tree; source .env; main tree; NEVER treasury_sync_city_budget), re-run the harness to confirm exact-0, and record before/after. No numeric tolerance band.

    State the headline verdict at top (e.g. "N/N exact ties, 0 deltas" or "N-k/N exact, k explained: <reasons>"). This file is read by the cohort audit (Plan 02), UAT prep (Plan 03), and the gsd-verifier for VER-07 part (a).
  </action>
  <verify>
    <automated>test -f .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-REDERIVATION.md && grep -qiE "Delta|tie" .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-REDERIVATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 116-REDERIVATION.md exists with the full per-FY sample table (State | FY | Dataset | independent total | DB total | delta | disposition) covering all 10 tranche-3 states + 4 deepened states
    - Every row has delta = $0 OR a documented explanation (verbatim, with loadlog reference) OR a recorded in-phase fix (before/after + re-run confirmation)
    - No numeric tolerance band appears anywhere in the disposition logic
    - The random-middle-year choices are recorded so the sample is reproducible
    - The harness exits 0 after any in-phase fixes, or the only non-zero rows are explicitly explained
  </acceptance_criteria>
  <done>116-REDERIVATION.md records the independent tie for every sampled FY; all deltas are 0, explained, or fixed-in-phase; harness exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Commit harness + rederivation log, SUMMARY</name>
  <read_first>
    - .planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-REDERIVATION.md
  </read_first>
  <action>
    Commit the harness (feat) and the rederivation log (docs) atomically per the executor commit protocol. Write 116-01-SUMMARY.md per the summary template with the headline verdict and any explained/fixed deltas.
  </action>
  <verify>
    <automated>git log --oneline --grep="116-01" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - feat + docs commits exist for 116-01
    - 116-01-SUMMARY.md exists with Self-Check section
  </acceptance_criteria>
  <done>Harness + log committed; SUMMARY written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| local script → source ACFR/CAFR PDFs (public web + AZ Google Drive) | Untrusted external bytes; hosts can return HTTP 200 + HTML (soft-404). |
| local script → treasury.budgets (read) | Read-only DB via service key (env-only). No write in this plan. |

## STRIDE Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-116-01 | Tampering | source PDF fetch (soft-404) | mitigate | Filter by Content-Type application/pdf + multi-MB payload, not HTTP status; a soft-404 HTML page FAILs, never silently ties to 0. AZ Drive link handled explicitly (BLOCKED-with-reason if dead). |
| T-116-02 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Read from ../.env.local/../.env only; never inlined. Read-only DB use. |
| T-116-SC | Tampering | package installs | mitigate | No new packages — Node built-ins + existing deps only. |
</threat_model>

<verification>
- scripts/verify-phase116-rederive.mjs runs the full tranche-3 + deepening sample blind from source and exits 0 (every retained check exact-0 or explicitly explained).
- 116-REDERIVATION.md records each tie with delta + disposition; any non-zero is loadlog-documented rounding or fixed-in-phase.
- No loader/parser import in the harness (independence proven); no tolerance band.
</verification>

<success_criteria>
- VER-07 part (a): every newly loaded state-FY in the sample (tranche 3 + deepening) reconciled independently from its own ACFR at exact-0 (or documented-explained), recorded in 116-REDERIVATION.md.
</success_criteria>

<output>
Create `.planning/phases/116-verification-source-chain-audit-uat-ver-07-ver-08/116-01-SUMMARY.md` when done.
</output>
