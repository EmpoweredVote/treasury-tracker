---
phase: 27-carry-forwards-longview-tx-revenue-state-labels
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified: []
autonomous: false
requirements: [CARRY-01, CARRY-02]
must_haves:
  truths:
    - "Longview TX Money In tab shows revenue categories with enrichment descriptions in the live app"
    - "City picker state group headers display 'California', 'Texas', and 'Oregon' (not CA/TX/OR abbreviations)"
  artifacts:
    - path: "src/components/EntitySwitcher.tsx"
      provides: "STATE_LABELS map with all 4 full state names"
      contains: "TX: 'Texas', CA: 'California', OR: 'Oregon'"
  key_links:
    - from: "EntitySwitcher.tsx STATE_LABELS"
      to: "city picker state group header rendering"
      via: "STATE_LABELS[state] || state at line 144"
      pattern: "STATE_LABELS\\[state\\]"
---

<objective>
Human visual verification of two carry-forward items in the live app at
treasurytracker.empowered.vote: (1) Longview TX Money In tab shows revenue categories with
enrichment descriptions (CARRY-01 end-to-end proof), and (2) city picker state group headers
display full state names — California, Texas, Oregon — not abbreviations (CARRY-02).

Purpose: Both items require a human with a browser because the site is a React SPA — automated
WebFetch returns only a shell HTML page before JS hydrates. Code correctness is already confirmed;
this plan verifies the live deployed state.

Output: Human confirmation that both items display correctly, or identification of a deployment
issue requiring remediation.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-carry-forwards-longview-tx-revenue-state-labels/27-RESEARCH.md
@.planning/phases/27-carry-forwards-longview-tx-revenue-state-labels/27-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Live app visual verification — Longview Money In + STATE_LABELS</name>

  <read_first>
    Read 27-01-SUMMARY.md to confirm enrichment ran successfully (>= 15 rows written) before
    doing the app check. If enrichment failed in Plan 01, Money In descriptions will be empty
    and that is a Plan 01 issue, not a deployment issue.

    Read 27-RESEARCH.md section "CARRY-02: STATE_LABELS — Detailed Findings" to understand
    what to look for and the fallback if abbreviations appear.
  </read_first>

  <what-built>
    Plan 27-01 fixed 2 corrupted Longview revenue category names in the DB (Police, Library)
    and ran enrichCategories.js --city Longview --state TX --year 2026, writing 15 enrichment
    rows to treasury.category_enrichment. EntitySwitcher.tsx already contains the correct
    STATE_LABELS map (IN/CA/TX/OR → full names) — no code change was needed for CARRY-02.
  </what-built>

  <how-to-verify>
    Open treasurytracker.empowered.vote in a browser. Perform the following checks in order:

    CHECK 1 — STATE_LABELS (CARRY-02):
    1. Click the city/entity selector (top of page — the dropdown that shows the current city name).
    2. Observe the group headers above city lists.
    3. PASS: Headers read "California", "Texas", "Oregon" (full names, not abbreviations).
    4. FAIL: Headers read "CA", "TX", "OR" (abbreviations still showing).

    If CHECK 1 FAILS: The code is correct at HEAD but the build may be stale. Report which
    state abbreviations you see. Claude will investigate the deployment state (check git log,
    verify last deploy, determine if a redeploy is needed). Do NOT make code changes —
    EntitySwitcher.tsx is correct.

    CHECK 2 — Longview Money In (CARRY-01 end-to-end):
    1. Select "Longview" from the city picker (under Texas).
    2. Click the "Money In" tab (or equivalent revenue tab).
    3. Verify a revenue total appears (should be in the ~$87.6M range).
    4. Expand or scroll through categories — look for category names like "General Revenue",
       "Information Services", "Police", "Library", etc.
    5. PASS: Categories are visible AND at least some have plain-language description text below
       the category name (the enrichment description, e.g. "Funds received from property taxes...").
    6. PARTIAL PASS: Categories visible but NO descriptions shown — enrichment may not be
       surfacing in the UI; report this.
    7. FAIL: Money In tab is empty or shows 0 — report this (unexpected; revenue was verified
       in DB during research).
  </how-to-verify>

  <acceptance_criteria>
    - CHECK 1 PASS: City picker shows "California", "Texas", "Oregon" as state group headers
    - CHECK 2 PASS or PARTIAL PASS: Longview Money In tab shows at least one revenue category
      (regardless of whether descriptions are visible — category presence proves the data load)
    - If CHECK 1 FAILS: Provide which abbreviations appear so deployment investigation can begin
    - If CHECK 2 FAILS entirely (empty tab): Report so DB state can be re-investigated
  </acceptance_criteria>

  <resume-signal>
    Type one of:
      "approved" — both checks passed
      "partial: [describe what you saw]" — one check passed, one didn't
      "failed: [describe what you saw]" — one or both checks failed with details
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → treasurytracker.empowered.vote | Public read-only SPA; no auth required for city picker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | Live site data visibility | accept | Revenue categories are public budget data — no PII, no auth required |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
Both CARRY-01 and CARRY-02 are verified when:
1. Longview Money In tab shows revenue categories (confirms end-to-end: DB → API → UI)
2. City picker shows full state names (confirms EntitySwitcher.tsx is deployed)

If either fails, root cause is noted and a remediation task is created (deploy trigger,
redeploy, or targeted investigation).
</verification>

<success_criteria>
Phase 27 is complete when:
1. CARRY-01: Longview TX Money In tab shows revenue categories; enrichment descriptions visible
2. CARRY-02: City picker state headers show "California", "Texas", "Oregon" (not abbreviations)
</success_criteria>

<output>
Create .planning/phases/27-carry-forwards-longview-tx-revenue-state-labels/27-02-SUMMARY.md when done
</output>
