---
phase: 92-enrichment-parity-mnenr-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/mnEnrichment92.mjs
  - scripts/loadMNEnrichment92.mjs
  - scripts/loadMNEnrichment92.test.mjs
autonomous: false
requirements: [MNENR-01]
must_haves:
  truths:
    - "data/mnEnrichment92.mjs exports a hand-authored CONCEPTS map keyed by normalized last-segment (~90 concepts → {plain_name, short_description, description, tags, confidence}) covering EVERY live MN category leaf-concept across depth 0/1/2; every description is concept-level, entity- AND state-neutral, bleed-safe (no locality names, no $ figures, no MN-specific facts), authored inline at $0 (NO Anthropic API call)"
    - "scripts/loadMNEnrichment92.mjs derives the live worklist from the DB (MN city+county municipalities → budgets → budget_categories ALL depths → distinct composite link_key, excluding 'total'), resolves each live key's last segment to a CONCEPT, builds one universal (municipality_id NULL) row PER live composite key (name_key = the full composite), and ABORTS if any live key's concept is missing (100% coverage gate, NO fallback)"
    - "Two pre-write guards also abort: a $-figure leak guard and an MN-locality-name guard (against the live MN municipalities.name list minus a common-English-word skip-set incl. 'minnesota'); writes use DELETE-THEN-INSERT over the authored composite keys (NULLS DISTINCT index — upsert would duplicate universal rows)"
    - "--apply writes the universal rows to treasury.category_enrichment; a dry-run (no --apply) derives the worklist + asserts coverage + runs both guards with ZERO DB writes; the loader exports pure helpers (buildRows, findDollarLeaks, findLocalityLeaks) for offline tests, guarded by the entry-script idiom"
    - "Offline node --test cases pass: buildRows over the live-key set yields 0 missing; a simulated live key whose last-segment is not in CONCEPTS is reported missing (gate works); findDollarLeaks/findLocalityLeaks catch a seeded $-figure and a seeded MN locality name (and do NOT flag a skip-set word like 'minnesota')"
  artifacts:
    - path: "data/mnEnrichment92.mjs"
      provides: "The hand-authored, state-neutral MN enrichment CONCEPTS map (~90 last-segment concepts) covering the full 3-level MN vocabulary"
      contains: "CONCEPTS"
    - path: "scripts/loadMNEnrichment92.mjs"
      provides: "The MN enrichment loader — all-depth composite-key worklist + last-segment coverage gate + leak guards + delete-then-insert universal writes"
      contains: "findLocalityLeaks"
    - path: "scripts/loadMNEnrichment92.test.mjs"
      provides: "Offline tests for the coverage gate + leak guards + buildRows"
      contains: "buildRows"
  key_links:
    - from: "The live MN 3-level category vocabulary (136 composite keys across depth 0/1/2, city+county)"
      to: "universal (NULL municipality_id) category_enrichment rows the app/API join renders at every icicle level"
      via: "CONCEPTS map (last-segment) → resolve per composite key → coverage gate + leak guards → delete-then-insert into treasury.category_enrichment"
      pattern: "loadMNEnrichment92"
---

<objective>
Author standardized, bleed-safe, state-neutral plain-language enrichment for the FULL live MN category vocabulary (136 composite keys across depth 0/1/2, cities + counties), at $0 inline (NO paid API), and load it as universal `category_enrichment` rows — with a 100% coverage gate (abort on any unmapped live key) and delete-then-insert writes (NULLS-DISTINCT-safe). Delivers MNENR-01.

Mirror Ohio Phase 87 (`scripts/loadOhioEnrichment87.mjs` + `data/ohioEnrichment87.mjs` + test) — with two MN adaptations: (1) MN trees are 3-LEVEL, so the worklist collects ALL depths and keys by the composite `link_key` (`parent|child[|leaf]`), not depth-0 plain names; (2) author by CONCEPT keyed by normalized last-segment (~90 concepts, since `current`/`capital` and most leaves recur or are unique) and expand to one universal row per live composite key — the coverage gate runs over all 136 live keys (each must resolve its last segment to a concept).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/92-enrichment-parity-mnenr-01/92-CONTEXT.md
@scripts/loadOhioEnrichment87.mjs
@scripts/loadOhioEnrichment87.test.mjs
@data/ohioEnrichment87.mjs

<interfaces>
<!-- The loader + map + test are a near-copy of the Ohio Phase 87 trio, adapted for 3-level composites. -->

Production target: SUPABASE_URL=https://kxsdzaojfaibhuzmclfq.supabase.co (.env) + service key. Prefer mcp__supabase-local for read-only verification probes.

category_enrichment row shape: { name_key, municipality_id (NULL universal), plain_name, short_description, description, tags (text[]), source ('ai'), confidence, evidence_summary, generated_at }. Index (name_key, municipality_id) is NULLS DISTINCT → delete-then-insert (NOT upsert).

Live worklist (the MN difference vs Ohio): MN municipalities (entity_type IN city,county; 945) → budgets (operating, revenue) → budget_categories across ALL depths (0,1,2) → distinct lower(link_key) (composite, `|`-separated), excluding 'total'. Paginate the 1000-row cap. Extracted counts (2026-06-27): 25 depth-0 + 72 depth-1 + 39 depth-2 = 136 distinct composite keys.

Authoring model (D-02): CONCEPTS keyed by normalized last-segment (the substring after the last `|`, lowercased). buildRows: for each live composite key, take its last segment → look up CONCEPTS → emit a row with name_key = the FULL composite key, text from the concept, plain_name from the leaf name (title-cased ok). `missing` = live keys whose last-segment has no concept (coverage gap → abort, no fallback). EXPECTED_KEYS may be the live-derived set; the gate is the enforcement.

Leak guards (mirror Ohio): findDollarLeaks (`$<digit>`); findLocalityLeaks against the live MN municipalities.name list with a common-word skip-set — MN has 945 entities incl. many common-word names (Savage, Climax, Hope, Center, Lake, Franklin, Two Harbors, Eagle Lake, Greenfield, Richfield, Sleepy Eye, …) + the state node 'minnesota'. Build the skip-set from the names actually flagged at dry-run (extend until generic civic text is clean).

Concept voice (D-03): concept-level, "local government"; `current` = day-to-day operating spending on the service; `capital` = capital outlay / equipment / construction for the service; intergovernmental aids explained generically (e.g. "State Local Government Aid" = unrestricted state aid to local governments). No locality names, no $ figures, no MN-specific facts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author data/mnEnrichment92.mjs (last-segment CONCEPTS covering the full MN vocabulary)</name>
  <files>data/mnEnrichment92.mjs</files>
  <read_first>
    - data/ohioEnrichment92.mjs is N/A — read data/ohioEnrichment87.mjs (the export shape, voice, tags/confidence conventions)
    - .planning/phases/92-enrichment-parity-mnenr-01/92-CONTEXT.md (D-02 concept model; D-03 neutrality rules; the depth-0/1/2 vocabulary)
    - scripts/loadMNOSA.js (the canonical leaf/group labels in mnOsaTreeMap — the concepts to describe)
  </read_first>
  <action>
    Create `data/mnEnrichment92.mjs` exporting `SOURCE='ai'` and `CONCEPTS` — an object keyed by each normalized last-segment concept (lowercased leaf name) → {plain_name, short_description, description, tags:[...], confidence}. Author one accurate, concept-level, entity- and state-neutral, bleed-safe row per concept (no locality names, no $ figures, no MN-specific facts; "local government" voice). Cover every live leaf-concept across depth 0/1/2: the depth-0 groups (taxes, intergovernmental, charges for services, public safety, streets & highways, sanitation, health, library, park & recreation, housing & urban redevelopment, economic development, conservation of natural resources, airport, transit, cemetery, education, general government, debt service, other & unallocated, …), the depth-1/2 leaves (propertytaxes, salestax, tax increments, franchisefees, hotelmoteltax, gambling/gravel/wheelage tax, special assessments, licenses and permits, the federal/state/county-local grant types incl. state local government aid + county program aid + the credits/aids, charges-for-services fee types, fines and forfeits, adminfines, interest earnings, all-other-* catch-alls, the street/highway sub-functions, human services sub-functions, unallocated pension/insurance, …), and the generic `current` / `capital` operating-vs-investment leaves. Do NOT author specifics you can't source — `all other …` and `unallocated …` are honest catch-alls. Author the `--apply`-driven additions iteratively (Task 3 dry-run reports any missing concept).
  </action>
  <acceptance_criteria>
    - `node -e "import('./data/mnEnrichment92.mjs').then(m=>console.log(Object.keys(m.CONCEPTS).length))"` prints a count ≥ 85 (the ~90 last-segment concepts)
    - Every CONCEPTS entry has non-empty plain_name + short_description + description + tags
    - No description contains a `$<digit>` figure or an obvious MN locality name; voice is state-neutral ("local government", not "Minnesota"/"the city of X")
    - `current` and `capital` concepts exist (generic operating-vs-investment); intergovernmental aid concepts (e.g. 'state local government aid') are explained generically
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build scripts/loadMNEnrichment92.mjs + offline tests (mirror Ohio loader, all-depth composite worklist)</name>
  <files>scripts/loadMNEnrichment92.mjs, scripts/loadMNEnrichment92.test.mjs</files>
  <read_first>
    - scripts/loadOhioEnrichment87.mjs (buildRows/findDollarLeaks/findLocalityLeaks; live-worklist derivation; coverage gate; delete-then-insert; entry-guard idiom)
    - scripts/loadOhioEnrichment87.test.mjs (offline test structure)
    - .planning/phases/92-enrichment-parity-mnenr-01/92-CONTEXT.md (D-01 all-depth composite worklist; D-02 last-segment resolver; D-05 guards)
  </read_first>
  <action>
    Create `scripts/loadMNEnrichment92.mjs` mirroring loadOhioEnrichment87.mjs with the MN adaptations:
    - Import CONCEPTS from data/mnEnrichment92.mjs. Add `lastSegment(key)` = substring after the last `|`, lowercased.
    - Export pure `buildRows(liveKeys)` → {rows, missing}: for each live composite key, resolve lastSegment → CONCEPTS; if present emit a row {name_key: <full composite>, municipality_id:null, plain_name, short_description, description, tags, source:'ai', confidence, evidence_summary, generated_at}; else push to `missing`. Dedup by name_key.
    - Export `findDollarLeaks(rows)` and `findLocalityLeaks(rows, localityNames)` (MN skip-set incl. 'minnesota' + common-word city names) — same shape as Ohio.
    - `main()`: derive the live MN worklist over ALL depths (entity_type IN city,county; operating+revenue; ALL depths; distinct composite link_key; paginated), buildRows, print the coverage report, ABORT on any missing / $-leak / locality-leak, and on `--apply` delete-then-insert the universal rows. Entry-guard main() so tests import pure helpers without DB.
    - EVIDENCE string = an MN-parity analog of the Ohio one.

    Create `scripts/loadMNEnrichment92.test.mjs` (node:test): buildRows over a sample of real live keys (incl. depth-1/2 composites like `taxes|propertytaxes`, `intergovernmental|state grants|state local government aid`) → 0 missing; buildRows over [...,'fake|__nope__'] → missing includes that key (gate fires); findDollarLeaks catches a seeded "$5"; findLocalityLeaks catches a seeded MN city name and does NOT flag 'minnesota' (skip-set).
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadMNEnrichment92.test.mjs` exits 0, all cases pass
    - `node -e "import('./scripts/loadMNEnrichment92.mjs').then(m=>console.log(typeof m.buildRows, typeof m.findDollarLeaks, typeof m.findLocalityLeaks))"` prints `function function function`
    - The loader collects ALL depths (not depth=0 only), keys rows by the full composite link_key, resolves via last-segment, has a delete-then-insert path (no upsert), and aborts on coverage/leak with NO fallback
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Dry-run coverage + guards, then live --apply + verify (idempotent)</name>
  <files>scripts/loadMNEnrichment92.mjs</files>
  <read_first>
    - scripts/loadMNEnrichment92.mjs (dry-run vs --apply paths)
    - reference_category_enrichment_nulls_distinct (why delete-then-insert)
    - feedback_supabase_migration_mcp (read-only probes)
  </read_first>
  <action>
    With .env sourced: run the DRY-RUN (`node scripts/loadMNEnrichment92.mjs`) and confirm the live worklist derives the 136 composite keys, MISSING coverage = 0, $-leaks = 0, locality-leaks = 0, zero writes. If the worklist surfaces a key whose last-segment concept is missing, ADD the concept to data/mnEnrichment92.mjs (state-neutral) and re-run — never fall back. If the locality guard false-positives on a common-word city name, extend the skip-set. Once clean, run `--apply` to delete-then-insert the universal rows. Then verify (mcp__supabase-local read-only): the count of universal (municipality_id IS NULL) category_enrichment rows for the 136 MN keys = 136 with no duplicate name_key among universal rows; spot-read 3-4 rows at different depths (e.g. `taxes`, `taxes|propertytaxes`, `intergovernmental|state grants|state local government aid`, a `…|current`) for state-neutral, $-free, locality-free text; confirm a depth-2 icicle leaf resolves to a universal row. Re-run `--apply` and confirm still 136 (idempotent).
  </action>
  <acceptance_criteria>
    - Dry-run: 136 live keys, 0 missing, 0 $-leaks, 0 locality-leaks, 0 writes
    - After --apply: the 136 MN composite keys each have exactly one universal (NULL municipality_id) category_enrichment row, no duplicate name_key among universal rows
    - Spot-checked rows at depth 0/1/2 (incl. a current/capital leaf) have state-neutral, $-free, locality-free text
    - Re-running --apply is idempotent (still 136 universal rows, no duplicates)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadMNEnrichment92.test.mjs` → all pass (coverage gate + leak guards + buildRows over composite keys).
- Dry-run derives the 136 live MN composite keys with 0 missing / 0 leaks / 0 writes; --apply writes exactly 136 universal category_enrichment rows via delete-then-insert (no duplicates, NULLS-DISTINCT-safe).
- Every authored concept is state-neutral, $-free, locality-free (bleed-safe by construction + enforced by guards); the coverage gate aborts on any unmapped live key (no fallback); $0 — inline-authored, no API — MNENR-01 met.
- Out of scope: ACFR reconciliation + source-chain audit + UAT (Phase 93).
</verification>
