---
phase: 119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42
plan: "119-05"
type: execute
wave: 5
depends_on: ["119-04"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processMTAcfr.js
  - scripts/processMTRevenueAcfr.js
  - .planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-05-MT-LOADLOG.md
autonomous: true
requirements: [ACFR-42]
user_setup: []

must_haves:
  truths:
    - "Montana state node (6e085a8b-97e3-479d-8879-9bb7ff4f9fb1) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across FY2016–FY2025, every loaded FY tying exactly ($0) to the printed GENERAL total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MT units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL column): FY2025 = $3,453,804K, FY2016 = $2,039,879K (store ×1,000 raw dollars)"
    - "MT reports ANNUAL GAAP financials despite a BIENNIAL budget cycle — the pre-recon annual-vs-biennial risk is RESOLVED: each FY has its own single-year ACFR (2015.pdf..2025.pdf individually on doa.mt.gov/SFSD/ACFR-PAFR). Load each FY as a distinct single-year actual; do NOT split or double a biennium"
    - "GENERAL column is the 1st of many (General | State Special Revenue | Federal Special Revenue | … | Total) of the Governmental Funds Statement of Rev/Exp/Changes — extract_gf.py's position-anchor MUST select the 1st numeric token, NOT the Total"
    - "Source URLs are OPAQUE per-year filenames, NOT derivable — the per-FY SOURCES map is enumerated from the doa.mt.gov/SFSD/ACFR-PAFR list; each FY's URL verified individually"
    - "Window is FY2016–FY2025 (10 yr): the archive lists back to 2015.pdf but FY2016 was the first year confirmed cleanly `-table`-extractable in recon; FY2015 may be re-attempted at load (honest hole if it won't cleanly tie)"
    - "Pre-existing NASBO operating rows (MT FY2023 $2,617,000K, FY2024 $2,684,000K) are REPLACED in place at the same (6e085a8b, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (MT ACFR GF ~1.29× NASBO — federal in MT's Federal Special Revenue major fund, so the General column stays close to NASBO) recorded at load; GAAP basis label verified on live rows"
    - "Any negative GENERAL category year renders via the P2 clamp; interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on MT once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMTAcfr.js"
      provides: "MT GF spending-by-function loader (operating), UNITS=1000, 1st-of-many column, tie-verified FY2016–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMTRevenueAcfr.js"
      provides: "MT GF revenue-by-source loader (revenue), UNITS=1000, tie-verified FY2016–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-05-MT-LOADLOG.md"
      provides: "Per-FY load disposition, annual-vs-biennial resolution note, NASBO-replacement + ~1.29× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMTAcfr.js / processMTRevenueAcfr.js"
      to: "treasury.budgets (MT state node 6e085a8b)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['MT']"
      to: "scripts/processMTAcfr.js + scripts/processMTRevenueAcfr.js"
      via: "gen_state.py code generation from mt/mt_all.json + enumerated per-FY SOURCES map (opaque filenames)"
      pattern: "General"
---

<objective>
Upgrade the **Montana** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processMTAcfr.js` (GF spending-by-function, `operating`) + `scripts/processMTRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading MT ACFR PDFs FY2016–FY2025 into gitignored `_acfr-work/mt/`, extracting the **GENERAL column** (1st of many) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**Annual-vs-biennial is RESOLVED.** Montana adopts its budget biennially but publishes ANNUAL single-year GAAP ACFRs — each FY (2015.pdf..2025.pdf) is a distinct single-year report on `doa.mt.gov/SFSD/ACFR-PAFR`. Load each FY as its own single-year actual; do NOT split or double a biennium. No FY-attribution exception is triggered.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $3,453,804K, FY2016 $2,039,879K. **1st-of-many column** — extract_gf.py's position-anchor must select the 1st numeric token (GENERAL), NOT the Total. **Source URLs are opaque per-year filenames**, NOT derivable — enumerate the per-FY SOURCES map from `doa.mt.gov/SFSD/ACFR-PAFR` and verify each URL individually. Window FY2016–FY2025 (10 yr; the archive lists 2015.pdf but FY2016 was the first cleanly `-table`-extractable year — re-attempt FY2015 at load, honest hole if it won't tie). Scope note: MT ACFR GF ~1.29× NASBO (federal in MT's Federal Special Revenue fund; General stays near NASBO) — accept-and-relabel honestly.

Output: both loaders live-loaded FY2016–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `119-05-MT-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['MT']` (mirror the 'AR'/'DE'/'CO' entries): name='Montana', abbr='MT', population (from loadStateGF.mjs), node='6e085a8b-97e3-479d-8879-9bb7ff4f9fb1', years=[FY2016..FY2025 that cleanly tie (+FY2015 if it ties)], tolerance (small), data='mt/mt_all.json', out='MT', base_url='https://doa.mt.gov/SFSD/ACFR-PAFR', sources={ enumerated per-FY opaque-filename URL map }, UNITS=1000. Use head_note for the ~1.29× note + annual-vs-biennial resolution; neg_note for the clamp note. `py -3 gen_state.py MT` emits scripts/processMTAcfr.js + processMTRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For MT the layout is wide (General | State Special Revenue | Federal Special Revenue | … | Total): isolate the 1st numeric token (GENERAL), not the Total. Confirm at both bookends before transcribing all years. Feeds mt/mt_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'mt-acfr-gf-operating' (name 'Montana General Fund Operating Budget'), 'mt-acfr-gf-revenue' (name 'Montana General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (live-confirmed): MT has 2 NASBO operating rows only — FY2023 $2,617,000K, FY2024 $2,684,000K (data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH2-SOURCES.md MT Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. GENERAL = 1st of many (General | State Special Revenue | Federal Special Revenue | … | Total). Units = THOUSANDS. FY-end June 30.
- Window FY2016–FY2025 (10 yr; archive confirms back to 2015.pdf but FY2016 was the first cleanly `-table`-extractable this pass; re-attempt FY2015 at load).
- URLs — OPAQUE per-year filenames, NOT derivable. Confirmed samples: FY2025 `https://doa.mt.gov/_docs/sfsd/sab/Montana-ACFR-2025-sig-on-file1.pdf`; FY2016 `https://doa.mt.gov/_docs/sfsd/sab/Documents/2016_ACFR.pdf`. Full enumerated list (FY2015–FY2025) on `https://doa.mt.gov/SFSD/ACFR-PAFR`.
- Landing: `https://doa.mt.gov/SFSD/ACFR-PAFR`.
- Bookend ties (GENERAL column Total revenues, thousands): FY2025 = 3,453,804 (diff $0); FY2016 = 2,039,879 (diff $0).
- **Annual-vs-biennial RESOLVED:** MT budgets biennially but reports GAAP financials ANNUALLY — each FY is a distinct single-year ACFR. No FY-attribution exception.
- Investment income positive at both bookends. No P2 clamp needed for confirmed years; check interior years at load.
- Scope vs NASBO ~1.29× (FY2025 ACFR $3.45B vs FY2024 NASBO $2.68B) — federal reported in MT's Federal Special Revenue major fund, so the General column stays close to NASBO's own-source concept. Accept-and-relabel honestly.
</source_facts>
</context>

<tasks>

<task id="119-05-01" type="auto">
  <name>Task 1: Generate both MT loaders (UNITS=1000, 1st-of-many column) via gen_state.py + download/extract/transcribe FY2016–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/mt/ (gitignored PDFs + pdftotext + mt_all.json), scripts/processMTAcfr.js, scripts/processMTRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'AR'/'DE'/'CO' entry; confirm UNITS + sources + head_note wiring)
    - _acfr-work/extract_gf.py (the GF-column extractor — isolating the 1st numeric token in a wide multi-column table)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (MT Detail Block: enumerated opaque-filename URLs, 1st-of-many layout, bookend ties, annual-vs-biennial resolution)
    - scripts/processARAcfr.js (a recent gen_state.py output — the exact generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. Enumerate the MT per-FY PDF URLs from the `doa.mt.gov/SFSD/ACFR-PAFR` list (opaque per-year filenames are NOT derivable — each must be read off the list and verified; note FY2025 = `Montana-ACFR-2025-sig-on-file1.pdf` and FY2016 = `Documents/2016_ACFR.pdf` are structurally different paths). For each FY 2016–2025 (and re-attempt FY2015), download the MT ACFR PDF to `_acfr-work/mt/MT{YYYY}.pdf`. Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. Run `pdftotext -table` + `extract_gf.py` on each; because MT has a wide layout, confirm the position-anchor isolates the 1st numeric token (GENERAL) and NOT the Total for BOTH the revenues and expenditures sections — verify at both bookends before transcribing all years. Load each FY as a distinct single-year actual (annual reporting — do NOT split/double a biennium). Assemble `_acfr-work/mt/mt_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, verbatim names); document any hand-patch inline. Add `CONFIGS['MT']` to `_acfr-work/gen_state.py` (node 6e085a8b…, years = the tying FY2016..FY2025 (+FY2015 if it ties), UNITS=1000, sources = enumerated opaque-filename map, base_url the ACFR-PAFR page, head_note the ~1.29× + annual-vs-biennial-resolution notes). Run `py -3 gen_state.py MT` → scripts/processMTAcfr.js + processMTRevenueAcfr.js (dataset_ids mt-acfr-gf-operating / mt-acfr-gf-revenue). Cross-check FY2025 Total revenues stored = 3,453,804×1000 and FY2016 = 2,039,879×1000 — if either bookend mismatches (a common failure is grabbing the Total column), STOP and re-inspect column selection before proceeding. OMIT (honest hole) FY2015 or any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processMTAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processMTAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processMTRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '3,453,804,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/mt/` holds a real PDF per FY2016–2025 (FY2015 if it ties); soft-404/HTML rejected; each opaque-filename URL verified against the ACFR-PAFR list.
    - `node scripts/processMTAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2025` prints 3,453,804,000 and `--fy 2016` prints 2,039,879,000 (thousands ×1,000).
    - The extracted column is GENERAL (1st of many), NOT Total — confirmed at both bookends; each FY loaded as a distinct single-year actual.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['MT']`; `mt/mt_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("Montana State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='mt-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both MT loaders exist (gen_state.py), UNITS=1000, GENERAL (1st of many) isolated, mt_all.json holds every tied FY2016–FY2025 (FY2015 re-attempted), each FY a distinct single-year actual, both bookends dry-run-tie, P2 clamp wired, no live write.</done>
</task>

<task id="119-05-02" type="auto">
  <name>Task 2: Live-load MT (operating + revenue) FY2016–FY2025, NASBO replaced in place</name>
  <files>(runs scripts/processMTAcfr.js, scripts/processMTRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMTAcfr.js + scripts/processMTRevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; MT node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the MT node (6e085a8b) and RECORD the pre-load NASBO operating rows (FY2023 $2,617,000K, FY2024 $2,684,000K: total + data_source) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2016–FY2025 minus honest holes), run `node scripts/processMTAcfr.js --fy <YYYY>` then `node scripts/processMTRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (6e085a8b, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded MT totals vs pre-load NASBO totals to confirm the ~1.29× divergence is the expected (federal-in-separate-fund) mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "MT row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the MT node has, per loaded FY, an operating row "Montana State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both with non-null source_url/source_date.
    - MT FY2025 revenue row total = 3,453,804,000 and FY2016 = 2,039,879,000 (thousands ×1,000).
    - MT FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (MT, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.29× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed MT FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="119-05-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG + phase close-out note</name>
  <files>.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-05-MT-LOADLOG.md</files>
  <read_first>
    - scripts/processMTAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. MT --fy 2025 operating + revenue) and confirm 0 net change AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded MT FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) MT has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (MT, fy)); (c) honest-hole FYs ABSENT; (d) Money In — MT has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for MT; (f) cohort untouched: spot-check a sample of existing ACFR nodes (e.g. AK b268c415, MS ebec9e07 from 119-04) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 119-05-MT-LOADLOG.md: FYs loaded, FYs skipped (+reason incl. FY2015 disposition), the annual-vs-biennial resolution note, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Since this is the last plan in Phase 119, add a one-line batch close-out note: all 5 Batch-2 states (IA/KS/ME/MS/MT) loaded, handing the batch to Phase 124 for re-derivation + cohort audit + Chris UAT. Do NOT load any year beyond the tied window; do NOT touch any state other than MT.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 119-05-MT-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of MT --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded MT FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; MT has ZERO remaining "NASBO" operating rows; exactly one operating row per (MT, fy).
    - MT has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for MT (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 119-05-MT-LOADLOG.md has a finalized "Load Disposition" section + the Batch-2 close-out note.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms MT fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 119-05-MT-LOADLOG.md finalized with the Batch-2 close-out note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doa.mt.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the MT node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-05-A | Tampering | units mis-scale (the ×1,000) inflates or deflates MT 1000× | mitigate | UNITS=1000 hard-set; bookend totals (3,453,804K / 2,039,879K) asserted in dry-run + live |
| T-119-05-B | Tampering | grabbing the Total column instead of GENERAL (1st) in the wide layout | mitigate | extract_gf.py position-anchor isolates the 1st numeric token; bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-119-05-C | Tampering | biennial-budget confusion → doubling/splitting a two-year figure into one FY | mitigate | annual GAAP reporting confirmed; each FY loaded from its own single-year ACFR; per-FY bookend ties gate |
| T-119-05-D | Tampering | wrong opaque filename (paths differ across years) → wrong-year or 404 PDF | mitigate | each per-FY URL verified against the doa.mt.gov/SFSD/ACFR-PAFR list; %PDF magic + size guard; honest hole if a year 404s |
| T-119-05-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (MT, fy), zero NASBO labels |
| T-119-05-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-119-05-G | Info Disclosure | silently inflating MT (~1.29×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-119-05-H | Tampering | write to a state other than MT | mitigate | loader resolves only the MT node id; Task 3 spot-checks cohort unchanged |
| T-119-05-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both MT loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processMTRevenueAcfr.js --dry-run --fy 2025` → 3,453,804,000; `--fy 2016` → 2,039,879,000.
- Extracted column is GENERAL (1st of many), not Total — confirmed at both bookends; each FY a distinct single-year actual.
- Live: MT node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (MT, fy)).
- Re-run MT --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for MT (LOAD-01).
- MT ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.29×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 119-05-MT-LOADLOG.md finalized with the Batch-2 close-out note.
</verification>

<success_criteria>
The Montana state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2016–FY2025 (FY2015 re-attempted; honest holes logged), stored in thousands (UNITS=1000), the GENERAL column (1st of many) correctly isolated, each FY loaded as a distinct single-year actual (annual-vs-biennial resolved), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.29× accept-relabel divergence recorded at load, the P2 clamp wired for any negative year, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Completes Batch 2 (IA/KS/ME/MS/MT) and hands all five to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-05-SUMMARY.md` when done — record the MT FY window actually transcribed, any FY omitted as a non-tying honest hole (incl. FY2015 disposition), the two bookend tie confirmations, the annual-vs-biennial resolution note, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run + 0-residue result, the Money-In auto-enable confirmation, and the Batch-2 close-out note.
</output>
