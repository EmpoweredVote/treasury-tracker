---
phase: 119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42
plan: "119-03"
type: execute
wave: 3
depends_on: ["119-02"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processMEAcfr.js
  - scripts/processMERevenueAcfr.js
  - .planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-03-ME-LOADLOG.md
autonomous: true
requirements: [ACFR-40]
user_setup: []

must_haves:
  truths:
    - "Maine state node (53f26018-1d20-4f6a-9c0e-400bfb91199a) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2000–FY2025, the deepest in Batch 2), every loaded FY tying exactly ($0) to the printed General total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "ME units are THOUSANDS: UNITS=1000. Bookend ties (General column): FY2025 = $6,194,288K, FY2002 = $2,302,006K (store ×1,000 raw dollars)"
    - "General column is the 1st of 6 (General | Highway | Federal | Other Special Revenue | Other Governmental Funds | Total) of the Governmental Funds Statement of Rev/Exp/Changes — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are DERIVABLE: `https://www.maine.gov/osc/sites/maine.gov.osc/files/inline-files/acfr{YYYY}.pdf` for FY2000–FY2025 EXCEPT the FY2020 exception `acfr2020v2_0.pdf` (must special-case in the SOURCES map)"
    - "ME FY-end confirmed June 30 (the pre-recon 'non-June one to watch' flag is RESOLVED): each PDF's cover asserts 'For the Fiscal Year Ended June 30, {YYYY}' — assert this at load; do NOT store a July–June-shifted year"
    - "Pre-existing NASBO operating rows (ME FY2023 $4,304,000K, FY2024 $4,980,000K) are REPLACED in place at the same (53f26018, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (ME ACFR GF ~1.24× NASBO — federal reported in ME's own major funds, so the General column stays close to NASBO) recorded at load; GAAP basis label verified on live rows"
    - "Any negative General category year renders via the P2 clamp (slice→0, signed magnitude in label, parent nets it); interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on ME once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMEAcfr.js"
      provides: "ME GF spending-by-function loader (operating), UNITS=1000, derivable-URL SOURCES with FY2020 exception, tie-verified FY2000–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMERevenueAcfr.js"
      provides: "ME GF revenue-by-source loader (revenue), UNITS=1000, tie-verified FY2000–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-03-ME-LOADLOG.md"
      provides: "Per-FY load disposition, FY2020-exception + June-30-confirmed notes, NASBO-replacement + ~1.24× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMEAcfr.js / processMERevenueAcfr.js"
      to: "treasury.budgets (ME state node 53f26018)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['ME']"
      to: "scripts/processMEAcfr.js + scripts/processMERevenueAcfr.js"
      via: "gen_state.py code generation from me/me_all.json + derivable acfr{YYYY}.pdf SOURCES map (FY2020 exception)"
      pattern: "General"
---

<objective>
Upgrade the **Maine** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processMEAcfr.js` (GF spending-by-function, `operating`) + `scripts/processMERevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading ME ACFR PDFs FY2000–FY2025 into gitignored `_acfr-work/me/`, extracting the **General column** (1st of 6) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed General total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**URL is DERIVABLE with one exception.** `https://www.maine.gov/osc/sites/maine.gov.osc/files/inline-files/acfr{YYYY}.pdf` covers FY2000–FY2025, EXCEPT **FY2020 = `acfr2020v2_0.pdf`** — the SOURCES map must special-case that one year. ME is the deepest window in Batch 2 (target FY2000–FY2025, 26 yr).

**ME FY-end is confirmed June 30** — the pre-recon "non-June one to watch" flag is RESOLVED. Each PDF cover asserts "For the Fiscal Year Ended June 30, {YYYY}"; assert this at load and store the calendar year the FY ends (June 30), not a shifted year.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $6,194,288K, FY2002 $2,302,006K. Scope note: ME ACFR GF ~1.24× NASBO (federal in ME's own major funds; General stays near NASBO) — accept-and-relabel honestly.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `119-03-ME-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['ME']` (mirror the 'AR'/'DE'/'CO' entries): name='Maine', abbr='ME', population (from loadStateGF.mjs), node='53f26018-1d20-4f6a-9c0e-400bfb91199a', years=[FY2000..FY2025 that cleanly tie], tolerance (small), data='me/me_all.json', out='ME', base_url='https://www.maine.gov/osc/financial-reporting/annual-comprehensive-financial-report', sources={ derivable acfr{YYYY}.pdf per FY WITH the FY2020=acfr2020v2_0.pdf exception }, UNITS=1000. Use head_note for the ~1.24× note + FY2020 exception + June-30-confirmed note; neg_note for the clamp note. `py -3 gen_state.py ME` emits scripts/processMEAcfr.js + processMERevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For ME the layout is 6 columns: isolate the 1st numeric token (General), not the 6th (Total). Feeds me/me_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES with the FY2020 special-case, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'me-acfr-gf-operating' (name 'Maine General Fund Operating Budget'), 'me-acfr-gf-revenue' (name 'Maine General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (live-confirmed): ME has 2 NASBO operating rows only — FY2023 $4,304,000K, FY2024 $4,980,000K (data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH2-SOURCES.md ME Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. General = 1st of 6 (General | Highway | Federal | Other Special Revenue | Other Governmental Funds | Total). Units = THOUSANDS. FY-end June 30 (confirmed — the pre-recon "one to watch" is resolved).
- Clean window FY2000–FY2025 (26 yr — deepest in Batch 2; archive goes to FY2000).
- URLs — DERIVABLE: `https://www.maine.gov/osc/sites/maine.gov.osc/files/inline-files/acfr{YYYY}.pdf` for FY2000–FY2025 EXCEPT **FY2020 = `acfr2020v2_0.pdf`** (special-case). Landing: `https://www.maine.gov/osc/financial-reporting/annual-comprehensive-financial-report`.
- Bookend ties (General column Total revenues, thousands): FY2025 = 6,194,288 (diff $0); FY2002 = 2,302,006 (diff $0).
- Investment income positive at both bookends. No P2 clamp needed for confirmed years; check interior years at load.
- Scope vs NASBO ~1.24× (FY2025 ACFR $6.19B vs FY2024 NASBO $4.98B) — federal reported in ME's Federal/Highway/Other major funds, so the General column stays near NASBO's own-source concept. Accept-and-relabel honestly.
</source_facts>
</context>

<tasks>

<task id="119-03-01" type="auto">
  <name>Task 1: Generate both ME loaders (UNITS=1000, derivable URL + FY2020 exception) via gen_state.py + download/extract/transcribe FY2000–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/me/ (gitignored PDFs + pdftotext + me_all.json), scripts/processMEAcfr.js, scripts/processMERevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'AR'/'DE'/'CO' entry; confirm UNITS + sources wiring incl. how DE's FY-naming exceptions were handled per-year)
    - _acfr-work/extract_gf.py (the GF-column extractor — isolating the 1st numeric token in a 6-column table)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (ME Detail Block: derivable URL + FY2020 exception, 6-fund layout, bookend ties, June-30 confirmation)
    - scripts/processDEAcfr.js (a recent gen_state.py output with a per-year URL naming exception — the exact generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. For each FY 2000–2025, download the ME ACFR PDF to `_acfr-work/me/ME{YYYY}.pdf` from `…/inline-files/acfr{YYYY}.pdf`, EXCEPT FY2020 which is `…/inline-files/acfr2020v2_0.pdf` (special-case in the SOURCES map). Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. **Assert each PDF cover says "For the Fiscal Year Ended June 30, {YYYY}"** and store the FY as the year the fiscal year ends (June 30) — do NOT shift. Run `pdftotext -table` + `extract_gf.py` on each; isolate the General column (1st of 6) for BOTH the revenues and expenditures sections — verify at both bookends before transcribing all years. Assemble `_acfr-work/me/me_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, verbatim names); document any hand-patch inline. Add `CONFIGS['ME']` to `_acfr-work/gen_state.py` (node 53f26018…, years = the tying FY2000..FY2025, UNITS=1000, sources = derivable acfr{YYYY}.pdf map WITH the FY2020 exception, base_url the OSC landing page, head_note the ~1.24× + FY2020-exception + June-30-confirmed notes). Run `py -3 gen_state.py ME` → scripts/processMEAcfr.js + processMERevenueAcfr.js (dataset_ids me-acfr-gf-operating / me-acfr-gf-revenue). Cross-check FY2025 Total revenues stored = 6,194,288×1000 and FY2002 = 2,302,006×1000 — if either bookend mismatches, STOP and re-inspect column selection before proceeding. OMIT (honest hole) any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processMEAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processMEAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processMERevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '6,194,288,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/me/` holds a real PDF per FY2000–2025; FY2020 fetched via the `acfr2020v2_0.pdf` exception; soft-404/HTML rejected.
    - Each PDF cover confirmed "For the Fiscal Year Ended June 30, {YYYY}"; stored FY is the June-30 end year.
    - `node scripts/processMEAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2025` prints 6,194,288,000 and `--fy 2002` prints 2,302,006,000 (thousands ×1,000).
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['ME']` with the FY2020 SOURCES exception; `me/me_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("Maine State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='me-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both ME loaders exist (gen_state.py), UNITS=1000, derivable SOURCES with the FY2020 exception, me_all.json holds every tied FY2000–FY2025, June-30 FY-end confirmed, both bookends dry-run-tie, P2 clamp wired, no live write.</done>
</task>

<task id="119-03-02" type="auto">
  <name>Task 2: Live-load ME (operating + revenue) FY2000–FY2025, NASBO replaced in place</name>
  <files>(runs scripts/processMEAcfr.js, scripts/processMERevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMEAcfr.js + scripts/processMERevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; ME node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the ME node (53f26018) and RECORD the pre-load NASBO operating rows (FY2023 $4,304,000K, FY2024 $4,980,000K: total + data_source) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2000–FY2025 minus honest holes), run `node scripts/processMEAcfr.js --fy <YYYY>` then `node scripts/processMERevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (53f26018, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded ME totals vs pre-load NASBO totals to confirm the ~1.24× divergence is the expected mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "ME row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the ME node has, per loaded FY, an operating row "Maine State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both with non-null source_url/source_date.
    - ME FY2025 revenue row total = 6,194,288,000 and FY2002 = 2,302,006,000 (thousands ×1,000).
    - ME FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (ME, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.24× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed ME FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="119-03-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-03-ME-LOADLOG.md</files>
  <read_first>
    - scripts/processMEAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. ME --fy 2025 operating + revenue) and confirm 0 net change AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded ME FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) ME has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (ME, fy)); (c) honest-hole FYs ABSENT; (d) Money In — ME has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for ME; (f) cohort untouched: spot-check a sample of existing ACFR nodes (e.g. AK b268c415, KS bb3dcf05 from 119-02) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 119-03-ME-LOADLOG.md: FYs loaded, FYs skipped (+reason), the FY2020-exception + June-30-confirmed notes, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than ME.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 119-03-ME-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of ME --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded ME FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; ME has ZERO remaining "NASBO" operating rows; exactly one operating row per (ME, fy).
    - ME has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for ME (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 119-03-ME-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms ME fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 119-03-ME-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maine.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the ME node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-03-A | Tampering | units mis-scale (the ×1,000) inflates or deflates ME 1000× | mitigate | UNITS=1000 hard-set; bookend totals (6,194,288K / 2,302,006K) asserted in dry-run + live |
| T-119-03-B | Tampering | extracting the wrong column (Highway/Federal/Total instead of General) | mitigate | extract_gf.py isolates the 1st numeric token of the 6-column statement; bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-119-03-C | Tampering | FY2020 fetched via the standard acfr2020.pdf pattern (404) → missing year | mitigate | SOURCES map special-cases FY2020 = acfr2020v2_0.pdf; %PDF magic + size guard; honest hole if a year 404s |
| T-119-03-D | Tampering | wrong FY attribution if ME were non-June (pre-recon flag) | mitigate | each PDF cover asserted "Fiscal Year Ended June 30, {YYYY}"; FY stored as the June-30 end year |
| T-119-03-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (ME, fy), zero NASBO labels |
| T-119-03-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-119-03-G | Info Disclosure | silently inflating ME (~1.24×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-119-03-H | Tampering | write to a state other than ME | mitigate | loader resolves only the ME node id; Task 3 spot-checks cohort unchanged |
| T-119-03-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both ME loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processMERevenueAcfr.js --dry-run --fy 2025` → 6,194,288,000; `--fy 2002` → 2,302,006,000.
- FY2020 fetched via acfr2020v2_0.pdf; every PDF cover confirmed June-30 FY-end.
- Live: ME node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (ME, fy)).
- Re-run ME --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for ME (LOAD-01).
- ME ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.24×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 119-03-ME-LOADLOG.md finalized.
</verification>

<success_criteria>
The Maine state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2000–FY2025, the deepest in Batch 2; honest holes logged), stored in thousands (UNITS=1000), the derivable SOURCES map special-casing FY2020, June-30 FY-end confirmed, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.24× accept-relabel divergence recorded at load, the P2 clamp wired for any negative year, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands ME to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-03-SUMMARY.md` when done — record the ME FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the FY2020-exception + June-30-confirmed notes, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
