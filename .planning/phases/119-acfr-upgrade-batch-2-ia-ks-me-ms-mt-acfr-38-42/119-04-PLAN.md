---
phase: 119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42
plan: "119-04"
type: execute
wave: 4
depends_on: ["119-03"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processMSAcfr.js
  - scripts/processMSRevenueAcfr.js
  - .planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-04-MS-LOADLOG.md
autonomous: true
requirements: [ACFR-41]
user_setup: []

must_haves:
  truths:
    - "Mississippi state node (ebec9e07-a79e-44b0-b5d5-2551625d4b8e) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across FY2003–FY2024, every loaded FY tying exactly ($0) to the printed General total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MS units are THOUSANDS: UNITS=1000. Bookend ties (General column): FY2024 = $22,709,403K, FY2003 = $9,707,864K (store ×1,000 raw dollars)"
    - "MS FY2024 REQUIRES the P2 clamp — Investment income is NEGATIVE (−$434,060K). The FY2024 revenue tree must render via clampForRender (that slice→0, signed magnitude in the label, parent total nets the negative back so the printed $22,709,403K total still ties). This is the load-flagged year from 117-RECON; the clamp is REQUIRED here, not just a safety net"
    - "General column is the 1st (near-single-fund) of the Governmental Funds Statement of Rev/Exp/Changes (FY2024: General | Permanent | Totals; FY2003: General | Health Care | Capital Projects | Nonmajor | Totals) — GF is the dominant/only major fund. NOT the Statement of Activities"
    - "FY2025 is an HONEST HOLE — not yet published on dfa.ms.gov as of the 117 recon; the window ends at FY2024. The load must re-check dfa.ms.gov/publications for a published FY2025 and load it if present + tying; otherwise log the hole"
    - "Source URLs are OPAQUE per-year filenames, NOT derivable — the per-FY SOURCES map is enumerated from the dfa.ms.gov/publications list; each FY's URL verified individually"
    - "Pre-existing NASBO operating rows (MS FY2023 $6,315,000K, FY2024 $6,635,000K) are REPLACED in place at the same (ebec9e07, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence recorded PROMINENTLY: MS ACFR GF ~3.42× NASBO — near-single-fund GF folds in federal/intergovernmental NASBO excludes; GAAP basis label verified on live rows"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on MS once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMSAcfr.js"
      provides: "MS GF spending-by-function loader (operating), UNITS=1000, near-single-fund, tie-verified FY2003–FY2024"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMSRevenueAcfr.js"
      provides: "MS GF revenue-by-source loader (revenue), UNITS=1000, P2 clamp exercised on FY2024 negative Investment income, tie-verified FY2003–FY2024"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-04-MS-LOADLOG.md"
      provides: "Per-FY load disposition, FY2024 P2-clamp exercise record, FY2025-hole note, NASBO-replacement + prominent ~3.42× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMSAcfr.js / processMSRevenueAcfr.js"
      to: "treasury.budgets (MS state node ebec9e07)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['MS']"
      to: "scripts/processMSAcfr.js + scripts/processMSRevenueAcfr.js"
      via: "gen_state.py code generation from ms/ms_all.json + enumerated per-FY SOURCES map (opaque filenames)"
      pattern: "General"
---

<objective>
Upgrade the **Mississippi** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processMSAcfr.js` (GF spending-by-function, `operating`) + `scripts/processMSRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading MS ACFR PDFs FY2003–FY2024 into gitignored `_acfr-work/ms/`, extracting the **General column** (near-single-fund, 1st column) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed General total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**FY2024 REQUIRES the P2 clamp.** MS FY2024 GF Investment income is NEGATIVE (−$434,060K). The FY2024 revenue tree must render through `clampForRender` — that negative slice renders at 0 with its signed magnitude in the label, and the parent total nets it back so the printed **$22,709,403K** total still ties exactly. This is the load-flagged year from 117-RECON; exercising the clamp here is REQUIRED, not merely a safety net. Confirm clampForRender fires on FY2024 in the dry-run.

**FY2025 is an honest hole** — not yet published on `dfa.ms.gov` as of the 117 recon; the window ends at FY2024. Re-check `dfa.ms.gov/publications` for a published, tying FY2025 at load and load it if present; otherwise log the hole.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2024 $22,709,403K, FY2003 $9,707,864K. **Source URLs are opaque per-year filenames**, NOT derivable — enumerate the per-FY SOURCES map from `dfa.ms.gov/publications` and verify each URL individually. Scope note is PROMINENT: MS ACFR GF ~3.42× NASBO (near-single-fund GF folds in federal/intergovernmental NASBO excludes) — accept-and-relabel honestly with a prominent basis note.

Output: both loaders live-loaded FY2003–FY2024 (FY2025 hole logged), NASBO replaced in place, Money In auto-enabled; `119-04-MS-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['MS']` (mirror the 'AR'/'DE'/'CO' entries): name='Mississippi', abbr='MS', population (from loadStateGF.mjs), node='ebec9e07-a79e-44b0-b5d5-2551625d4b8e', years=[FY2003..FY2024 that cleanly tie], tolerance (small), data='ms/ms_all.json', out='MS', base_url='https://www.dfa.ms.gov/publications', sources={ enumerated per-FY opaque-filename URL map }, UNITS=1000. Use head_note for the PROMINENT ~3.42× note + the FY2024 negative-Investment-income clamp note; neg_note for the clamp specifics. `py -3 gen_state.py MS` emits scripts/processMSAcfr.js + processMSRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For MS the General column is 1st and GF is the dominant/near-only major fund (FY2024: General | Permanent | Totals; FY2003: General | Health Care | Capital Projects | Nonmajor | Totals). Capture the NEGATIVE Investment income line verbatim (signed) for FY2024 so clampForRender can net it. Feeds ms/ms_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY. clampForRender must handle a negative category so the parent total still ties.
- data_sources dataset_ids: 'ms-acfr-gf-operating' (name 'Mississippi General Fund Operating Budget'), 'ms-acfr-gf-revenue' (name 'Mississippi General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (live-confirmed): MS has 2 NASBO operating rows only — FY2023 $6,315,000K, FY2024 $6,635,000K (data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH2-SOURCES.md MS Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. General = 1st column, near-single-fund (FY2024: General | Permanent | Totals; FY2003: General | Health Care | Capital Projects | Nonmajor | Totals). Units = THOUSANDS. FY-end June 30.
- Clean window FY2003–FY2024 (22 yr; archive goes to FY1996; FY2025 not yet published as of the recon).
- URLs — OPAQUE per-year filenames, NOT derivable. Confirmed samples: FY2024 `https://www.dfa.ms.gov/sites/default/files/Financial%20Reporting%20Home/Publications/ACFR/FY24%20%20ACFR%20Final.pdf`; FY2003 `.../ACFR/2003-cafr.pdf`. Full enumerated list (FY1996–FY2024) on `https://www.dfa.ms.gov/publications`.
- Landing: `https://www.dfa.ms.gov/publications`.
- Bookend ties (General column Total revenues, thousands): FY2024 = 22,709,403 (diff $0, P2 clamp req'd); FY2003 = 9,707,864 (diff $0).
- **FY2024 Investment income = −$434,060K (NEGATIVE) → P2 clamp REQUIRED** for the FY2024 revenue tree. Check every interior year for further negatives at load.
- Scope vs NASBO ~3.42× (FY2024 ACFR $22.71B vs FY2024 NASBO $6.64B) — near-single-fund GF folds in federal/intergovernmental NASBO's narrower budgetary concept excludes. Accept-and-relabel with a PROMINENT basis note.
</source_facts>
</context>

<tasks>

<task id="119-04-01" type="auto">
  <name>Task 1: Generate both MS loaders (UNITS=1000, near-single-fund, FY2024 P2 clamp) via gen_state.py + download/extract/transcribe FY2003–FY2024 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ms/ (gitignored PDFs + pdftotext + ms_all.json), scripts/processMSAcfr.js, scripts/processMSRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'AR'/'DE'/'CO' entry; confirm UNITS + sources + head_note/neg_note wiring)
    - _acfr-work/extract_gf.py (the GF-column extractor — isolating the 1st numeric token; preserving a signed negative line item)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (MS Detail Block: enumerated opaque-filename URLs, near-single-fund layout, bookend ties, FY2024 negative Investment income)
    - scripts/processARAcfr.js (a recent gen_state.py output — the generated loader shape incl. clampForRender + ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. Enumerate the MS per-FY PDF URLs from the `dfa.ms.gov/publications` list (opaque per-year filenames are NOT derivable — each must be read off the list and verified). For each FY 2003–2024, download the MS ACFR PDF to `_acfr-work/ms/MS{YYYY}.pdf`. Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. Run `pdftotext -table` + `extract_gf.py` on each; isolate the General column (1st, near-single-fund) for BOTH the revenues and expenditures sections — verify at both bookends before transcribing all years. **Capture MS FY2024 Investment income as a SIGNED negative (−$434,060K) so clampForRender can net it.** Assemble `_acfr-work/ms/ms_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, verbatim names, signed); document any hand-patch inline. Also RE-CHECK `dfa.ms.gov/publications` for a published FY2025 ACFR — if present, add it to the window and tie it; if absent, log the honest hole. Add `CONFIGS['MS']` to `_acfr-work/gen_state.py` (node ebec9e07…, years = the tying FY2003..FY2024 (+FY2025 if published), UNITS=1000, sources = enumerated opaque-filename map, base_url the publications page, head_note the PROMINENT ~3.42× note + FY2024 clamp note). Run `py -3 gen_state.py MS` → scripts/processMSAcfr.js + processMSRevenueAcfr.js (dataset_ids ms-acfr-gf-operating / ms-acfr-gf-revenue). Cross-check FY2024 Total revenues stored = 22,709,403×1000 and FY2003 = 9,707,864×1000 — if either bookend mismatches, STOP and re-inspect column selection. **Confirm the FY2024 dry-run exercises clampForRender on the negative Investment income slice AND still ties to 22,709,403K.** OMIT (honest hole) any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processMSAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processMSAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processMSRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '22,709,403,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ms/` holds a real PDF per FY2003–2024; soft-404/HTML rejected; each opaque-filename URL verified against the publications list; FY2025 re-checked (loaded if published+tying, else logged as a hole).
    - `node scripts/processMSAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2024` prints 22,709,403,000 and `--fy 2003` prints 9,707,864,000 (thousands ×1,000).
    - The FY2024 dry-run exercises the P2 clamp on the negative Investment income slice and still ties to 22,709,403K.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['MS']`; `ms/ms_all.json` exists with per-FY operating + revenue trees (FY2024 Investment income stored signed-negative).
    - Header/head_note records the prominent ~3.42× accept-relabel note; revenue RPC uses p_dataset_type:'revenue', dataset_id='ms-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both MS loaders exist (gen_state.py), UNITS=1000, near-single-fund General isolated, ms_all.json holds every tied FY2003–FY2024 (FY2025 hole re-checked), FY2024 P2 clamp exercised + still ties, both bookends dry-run-tie, prominent divergence note recorded, no live write.</done>
</task>

<task id="119-04-02" type="auto">
  <name>Task 2: Live-load MS (operating + revenue) FY2003–FY2024, NASBO replaced in place</name>
  <files>(runs scripts/processMSAcfr.js, scripts/processMSRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMSAcfr.js + scripts/processMSRevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle + clampForRender)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; MS node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the MS node (ebec9e07) and RECORD the pre-load NASBO operating rows (FY2023 $6,315,000K, FY2024 $6,635,000K: total + data_source) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2003–FY2024 minus honest holes), run `node scripts/processMSAcfr.js --fy <YYYY>` then `node scripts/processMSRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (ebec9e07, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. **For FY2024, confirm the loaded revenue tree total = $22,709,403K with the negative Investment income slice clamped (parent nets it).** If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded MS totals vs pre-load NASBO totals to confirm the ~3.42× divergence is the near-single-fund/federal-consolidation mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "MS row existence + NASBO-replacement + FY2024 clamp + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the MS node has, per loaded FY, an operating row "Mississippi State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both with non-null source_url/source_date.
    - MS FY2024 revenue row total = 22,709,403,000 (negative Investment income clamped, parent nets it) and FY2003 = 9,707,864,000 (thousands ×1,000).
    - MS FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (MS, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~3.42× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed MS FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-labelled + per-year-sourced; FY2024 negative-Investment-income slice clamped and still ties; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="119-04-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-04-MS-LOADLOG.md</files>
  <read_first>
    - scripts/processMSAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. MS --fy 2024 operating + revenue — the clamp year) and confirm 0 net change AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded MS FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) MS has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (MS, fy)); (c) honest-hole FYs (incl. FY2025 if unpublished) ABSENT; (d) Money In — MS has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for MS; (f) cohort untouched: spot-check a sample of existing ACFR nodes (e.g. AK b268c415, ME 53f26018 from 119-03) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 119-04-MS-LOADLOG.md: FYs loaded, FYs skipped (+reason incl. the FY2025 hole), the FY2024 P2-clamp exercise record, pre-load-NASBO-vs-loaded-ACFR totals with the prominent accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than MS.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 119-04-MS-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of MS --fy 2024 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded MS FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; MS has ZERO remaining "NASBO" operating rows; exactly one operating row per (MS, fy).
    - MS has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for MS (LOAD-01 holds).
    - Honest-hole FYs (incl. FY2025 if unpublished) are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 119-04-MS-LOADLOG.md has a finalized "Load Disposition" section incl. the FY2024 clamp record.
  </acceptance_criteria>
  <done>A representative-FY re-run (the FY2024 clamp year) produces 0 net change with 0 residue; the DB query confirms MS fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes (incl. FY2025) absent, and the rest of the cohort unchanged; 119-04-MS-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dfa.ms.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the MS node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-04-A | Tampering | units mis-scale (the ×1,000) inflates or deflates MS 1000× | mitigate | UNITS=1000 hard-set; bookend totals (22,709,403K / 9,707,864K) asserted in dry-run + live |
| T-119-04-B | Tampering | negative Investment income (FY2024) dropped or unsigned → total mis-ties or a negative slice renders wrong | mitigate | FY2024 Investment income captured signed-negative; clampForRender slices→0 with signed label, parent nets it; dry-run asserts FY2024 still ties to 22,709,403K |
| T-119-04-C | Tampering | extracting the wrong column (Permanent/Nonmajor/Totals instead of General) | mitigate | extract_gf.py isolates the 1st numeric token (General); bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-119-04-D | Tampering | wrong opaque filename → wrong-year or 404 PDF | mitigate | each per-FY URL verified against the dfa.ms.gov/publications list; %PDF magic + size guard; honest hole if a year 404s |
| T-119-04-E | Tampering | inventing an FY2025 that is not actually published | mitigate | FY2025 loaded ONLY if found published on dfa.ms.gov AND tying; otherwise logged as an honest hole (absent) |
| T-119-04-F | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (MS, fy), zero NASBO labels |
| T-119-04-G | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-119-04-H | Info Disclosure | silently inflating MS (~3.42×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; prominent GAAP basis label on live rows |
| T-119-04-I | Tampering | write to a state other than MS | mitigate | loader resolves only the MS node id; Task 3 spot-checks cohort unchanged |
| T-119-04-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both MS loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processMSRevenueAcfr.js --dry-run --fy 2024` → 22,709,403,000 (with the negative Investment income clamped); `--fy 2003` → 9,707,864,000.
- FY2024 exercises the P2 clamp and still ties.
- Live: MS node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (MS, fy)).
- FY2025 loaded only if published + tying, else absent (honest hole).
- Re-run MS --fy 2024 → "Loaded 0 rows"; 0 data_sources residue for MS (LOAD-01).
- MS ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~3.42×) recorded PROMINENTLY against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 119-04-MS-LOADLOG.md finalized.
</verification>

<success_criteria>
The Mississippi state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2003–FY2024 (FY2025 honest hole unless published; other honest holes logged), stored in thousands (UNITS=1000), the near-single-fund General column correctly isolated, the FY2024 negative Investment income handled via the P2 clamp so the total still ties, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~3.42× accept-relabel divergence recorded PROMINENTLY at load, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands MS to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-04-SUMMARY.md` when done — record the MS FY window actually transcribed, any FY omitted as a non-tying honest hole (incl. the FY2025 published/unpublished disposition), the two bookend tie confirmations, the FY2024 P2-clamp exercise record, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR ~3.42× accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
