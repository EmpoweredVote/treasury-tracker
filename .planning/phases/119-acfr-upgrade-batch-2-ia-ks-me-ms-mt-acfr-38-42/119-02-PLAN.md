---
phase: 119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42
plan: "119-02"
type: execute
wave: 2
depends_on: ["119-01"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processKSAcfr.js
  - scripts/processKSRevenueAcfr.js
  - .planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-02-KS-LOADLOG.md
autonomous: true
requirements: [ACFR-39]
user_setup: []

must_haves:
  truths:
    - "Kansas state node (bb3dcf05-586c-4e68-85d3-26a6199cc4ab) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across FY2019–FY2025, every loaded FY tying exactly ($0) to the printed General column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "KS units are THOUSANDS: UNITS=1000. Bookend ties (General column): FY2025 = $10,352,600K, FY2019 = $7,539,362K (store ×1,000 raw dollars)"
    - "KS is a SHALLOW window: FY2019–FY2025 (7 years) — the current admin.ks.gov archive does not list pre-FY2019; that is the durable window, not an honest hole to chase further back this pass"
    - "General column is the 1st of an 8-column WIDE layout (General | Social Services | Health and Environment | Transportation | Executive | Commerce | Non-major | Total) of the Governmental Funds Statement of Rev/Exp/Changes — extract_gf.py's position-anchor MUST select the 1st numeric token, NOT the Total (8th)"
    - "Source URLs are OPAQUE hash-based file IDs (admin.ks.gov/browse/files/<hash>/download), NOT derivable — the per-FY SOURCES map is enumerated from the admin.ks.gov ACFR categories page; each FY's URL verified individually"
    - "Pre-existing NASBO operating rows (KS FY2023 $8,727,000K, FY2024 $9,365,000K) are REPLACED in place at the same (bb3dcf05, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (KS ACFR GF ~1.11× NASBO — the NARROWEST divergence in Batch 2; KS breaks federal into its own major funds so the General column stays close to NASBO's own-source concept) recorded at load; GAAP basis label verified on live rows"
    - "Any negative General category year renders via the P2 clamp (slice→0, signed magnitude in label, parent nets it); interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on KS once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processKSAcfr.js"
      provides: "KS GF spending-by-function loader (operating), UNITS=1000, 8-fund-wide 1st-column extraction, tie-verified FY2019–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processKSRevenueAcfr.js"
      provides: "KS GF revenue-by-source loader (revenue), UNITS=1000, tie-verified FY2019–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-02-KS-LOADLOG.md"
      provides: "Per-FY load disposition, shallow-window note, NASBO-replacement + ~1.11× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processKSAcfr.js / processKSRevenueAcfr.js"
      to: "treasury.budgets (KS state node bb3dcf05)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['KS']"
      to: "scripts/processKSAcfr.js + scripts/processKSRevenueAcfr.js"
      via: "gen_state.py code generation from ks/ks_all.json + enumerated per-FY SOURCES map (opaque hash IDs)"
      pattern: "General"
---

<objective>
Upgrade the **Kansas** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processKSAcfr.js` (GF spending-by-function, `operating`) + `scripts/processKSRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading KS ACFR PDFs FY2019–FY2025 into gitignored `_acfr-work/ks/`, extracting the **General column** (1st of an 8-column WIDE layout) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed General total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**KS is a shallow window: FY2019–FY2025 (7 yr).** The current `admin.ks.gov` archive does not list pre-FY2019 — that is the durable window, not a hole to chase further back this pass.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $10,352,600K, FY2019 $7,539,362K. **Wide 8-fund layout** — extract_gf.py's position-anchor must select the 1st numeric token (General), NOT the 8th (Total). **Source URLs are opaque hash-based file IDs** (`admin.ks.gov/browse/files/<hash>/download`), NOT derivable — enumerate the per-FY SOURCES map from the admin.ks.gov ACFR categories page and verify each URL individually. Scope note: KS ACFR GF ~1.11× NASBO (the narrowest divergence in Batch 2; KS splits federal into its own major funds so the General column stays near NASBO's own-source concept) — accept-and-relabel honestly.

Output: both loaders live-loaded FY2019–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `119-02-KS-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['KS']` (mirror the 'AR'/'DE'/'CO' entries): name='Kansas', abbr='KS', population (from loadStateGF.mjs), node='bb3dcf05-586c-4e68-85d3-26a6199cc4ab', years=[FY2019..FY2025 that cleanly tie], tolerance (small), data='ks/ks_all.json', out='KS', base_url='https://www.admin.ks.gov/offices/accounts-reports/state-agencies/finance/annual-comprehensive-financial-report/annual-comprehensive-financial-report---acfr/categories/5cdd672f16a4499194349dadf359b1b3', sources={ enumerated per-FY opaque-hash URL map }, UNITS=1000. Use head_note for the ~1.11× narrow-divergence note; neg_note for the clamp note. `py -3 gen_state.py KS` emits scripts/processKSAcfr.js + processKSRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor. For KS the layout is WIDE (8 columns): the anchor must isolate the 1st numeric token (General), not the 8th (Total). Confirm the anchor picks General at both bookends before transcribing all years. Feeds ks/ks_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ks-acfr-gf-operating' (name 'Kansas General Fund Operating Budget'), 'ks-acfr-gf-revenue' (name 'Kansas General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (live-confirmed): KS has 2 NASBO operating rows only — FY2023 $8,727,000K, FY2024 $9,365,000K (data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH2-SOURCES.md KS Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances - Governmental Funds. General = 1st of 8 (General | Social Services | Health and Environment | Transportation | Executive | Commerce | Non-major | Total). Units = THOUSANDS. FY-end June 30.
- Durable window FY2019–FY2025 (7 yr; current site archive does not list pre-FY2019).
- URLs — OPAQUE hash-based file IDs, NOT derivable. Confirmed samples: FY2025 `https://www.admin.ks.gov/browse/files/d2d39a0deef8464faaba21b8f4e69a24/download`; FY2019 `https://www.admin.ks.gov/browse/files/2bd8990b55c94ceaa02fb136b6a2b111/download`. Full list (FY2019–FY2025) on the ACFR categories page `https://www.admin.ks.gov/offices/accounts-reports/state-agencies/finance/annual-comprehensive-financial-report/annual-comprehensive-financial-report---acfr/categories/5cdd672f16a4499194349dadf359b1b3`.
- Landing: the admin.ks.gov Accounts & Reports ACFR categories page (above).
- Bookend ties (General column Total revenues, thousands): FY2025 = 10,352,600 (line items Property tax 21 + Income/inheritance tax 5,981,722 + Sales/excise tax 3,718,917 + Gross receipts tax 218,826 + Charges for services 71,832 + Operating grants 0 + Capital grants 0 + Investment earnings 305,819 + Other revenues 55,463; diff $0); FY2019 = 7,539,362 (diff $0).
- Investment earnings positive at both bookends. No P2 clamp needed for confirmed years; check interior years at load.
- Scope vs NASBO ~1.11× (FY2025 ACFR $10.35B vs FY2024 NASBO $9.37B) — the NARROWEST divergence in Batch 2; KS reports federal under separate major funds (Social Services, Health & Environment, etc.), so the General column stays close to NASBO's own-source concept. Accept-and-relabel honestly.
</source_facts>
</context>

<tasks>

<task id="119-02-01" type="auto">
  <name>Task 1: Generate both KS loaders (UNITS=1000, 8-fund 1st-column) via gen_state.py + download/extract/transcribe FY2019–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ks/ (gitignored PDFs + pdftotext + ks_all.json), scripts/processKSAcfr.js, scripts/processKSRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'AR'/'DE'/'CO' entry pattern; confirm UNITS + sources + head_note wiring)
    - _acfr-work/extract_gf.py (the GF-column extractor — how the position-anchor isolates the 1st numeric token in a wide multi-column table)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (KS Detail Block: enumerated opaque-hash URLs, 8-fund layout, bookend ties)
    - scripts/processARAcfr.js (a recent gen_state.py output — the exact generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. Enumerate the KS per-FY PDF URLs from the admin.ks.gov ACFR categories page (opaque hash `browse/files/<hash>/download` IDs are NOT derivable — each must be read off the categories page and verified). For each FY 2019–2025, download the KS ACFR PDF to `_acfr-work/ks/KS{YYYY}.pdf`. Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. Run `pdftotext -table` + `extract_gf.py` on each; because KS has a WIDE 8-column layout, confirm the position-anchor isolates the 1st numeric token (General) and NOT the 8th (Total) for BOTH the revenues and expenditures sections — verify at both bookends before transcribing all years. Assemble `_acfr-work/ks/ks_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, verbatim names); document any hand-patch inline with printed-row evidence. Add `CONFIGS['KS']` to `_acfr-work/gen_state.py` (node bb3dcf05…, years = the tying FY2019..FY2025, UNITS=1000, sources = enumerated opaque-hash map, base_url the ACFR categories page, head_note the ~1.11× narrow-divergence note). Run `py -3 gen_state.py KS` → scripts/processKSAcfr.js + processKSRevenueAcfr.js (dataset_ids ks-acfr-gf-operating / ks-acfr-gf-revenue). Cross-check FY2025 Total revenues stored = 10,352,600×1000 and FY2019 = 7,539,362×1000 — if either bookend mismatches (a common failure is grabbing the Total column), STOP and re-inspect column selection before proceeding. OMIT (honest hole) any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processKSAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processKSAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processKSRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '10,352,600,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ks/` holds a real PDF per FY2019–2025; soft-404/HTML rejected; each opaque-hash URL verified against the categories page.
    - `node scripts/processKSAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2025` prints 10,352,600,000 and `--fy 2019` prints 7,539,362,000 (thousands ×1,000).
    - The extracted column is General (1st of 8), NOT Total — confirmed at both bookends.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['KS']`; `ks/ks_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("Kansas State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='ks-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both KS loaders exist (gen_state.py), UNITS=1000, General (1st of 8) isolated, ks_all.json holds every tied FY2019–FY2025, both bookends dry-run-tie, P2 clamp wired, narrow-divergence note recorded, no live write.</done>
</task>

<task id="119-02-02" type="auto">
  <name>Task 2: Live-load KS (operating + revenue) FY2019–FY2025, NASBO replaced in place</name>
  <files>(runs scripts/processKSAcfr.js, scripts/processKSRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processKSAcfr.js + scripts/processKSRevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; KS node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the KS node (bb3dcf05) and RECORD the pre-load NASBO operating rows (FY2023 $8,727,000K, FY2024 $9,365,000K: total + data_source) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2019–FY2025 minus honest holes), run `node scripts/processKSAcfr.js --fy <YYYY>` then `node scripts/processKSRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (bb3dcf05, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded KS totals vs pre-load NASBO totals to confirm the ~1.11× divergence is the expected (federal-in-separate-funds) mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "KS row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the KS node has, per loaded FY, an operating row "Kansas State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both with non-null source_url/source_date.
    - KS FY2025 revenue row total = 10,352,600,000 and FY2019 = 7,539,362,000 (thousands ×1,000).
    - KS FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (KS, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.11× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed KS FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="119-02-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-02-KS-LOADLOG.md</files>
  <read_first>
    - scripts/processKSAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. KS --fy 2025 operating + revenue) and confirm 0 net change AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded KS FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) KS has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (KS, fy)); (c) honest-hole FYs ABSENT; (d) Money In — KS has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for KS; (f) cohort untouched: spot-check a sample of existing ACFR nodes (e.g. the Batch-1 AK b268c415, IA 6e71a93f from 119-01) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 119-02-KS-LOADLOG.md: FYs loaded, FYs skipped (+reason), the shallow-window note, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than KS.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 119-02-KS-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of KS --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded KS FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; KS has ZERO remaining "NASBO" operating rows; exactly one operating row per (KS, fy).
    - KS has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for KS (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 119-02-KS-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms KS fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 119-02-KS-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin.ks.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the KS node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02-A | Tampering | units mis-scale (the ×1,000) inflates or deflates KS 1000× | mitigate | UNITS=1000 hard-set; bookend totals (10,352,600K / 7,539,362K) asserted in dry-run + live |
| T-119-02-B | Tampering | grabbing the Total (8th) column instead of General (1st) in the wide layout | mitigate | extract_gf.py position-anchor isolates the 1st numeric token; bookend ties (which only match if General, not Total, is selected) gate before transcription; validate() exit-2 on non-tie |
| T-119-02-C | Tampering | wrong statement (Statement of Activities / budgetary schedule) | mitigate | extract targets the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate |
| T-119-02-D | Tampering | wrong opaque hash ID → wrong-year or 404 PDF | mitigate | each per-FY URL verified against the admin.ks.gov categories page; %PDF magic + size guard; honest hole if a year 404s |
| T-119-02-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (KS, fy), zero NASBO labels |
| T-119-02-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-119-02-G | Info Disclosure | silently inflating KS (~1.11×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-119-02-H | Tampering | write to a state other than KS | mitigate | loader resolves only the KS node id; Task 3 spot-checks cohort unchanged |
| T-119-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both KS loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processKSRevenueAcfr.js --dry-run --fy 2025` → 10,352,600,000; `--fy 2019` → 7,539,362,000.
- Extracted column is General (1st of 8), not Total — confirmed at both bookends.
- Live: KS node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (KS, fy)).
- Re-run KS --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for KS (LOAD-01).
- KS ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.11×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 119-02-KS-LOADLOG.md finalized.
</verification>

<success_criteria>
The Kansas state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2019–FY2025 (shallow durable window; honest holes logged), stored in thousands (UNITS=1000), the General column (1st of 8) correctly isolated, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.11× accept-relabel divergence recorded at load, the P2 clamp wired for any negative year, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands KS to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-02-SUMMARY.md` when done — record the KS FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the General-not-Total column confirmation, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
