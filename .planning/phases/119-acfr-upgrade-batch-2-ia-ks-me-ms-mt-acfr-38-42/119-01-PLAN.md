---
phase: 119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42
plan: "119-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processIAAcfr.js
  - scripts/processIARevenueAcfr.js
  - .planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-01-IA-LOADLOG.md
autonomous: true
requirements: [ACFR-38]
user_setup: []

must_haves:
  truths:
    - "Iowa state node (6e71a93f-a43d-4972-a239-85ddbebe2545) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY2002–FY2025), every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "IA units are THOUSANDS: UNITS=1000. Bookend ties (GENERAL FUND, NET REVENUES total line): FY2025 = $24,251,676K, FY2002 = $9,752,220K (store ×1,000 raw dollars)"
    - "IA REVENUE ties to the NET REVENUES line, NOT gross: NET REVENUES = GROSS REVENUES − Revenue Refunds. FY2025 gross $26,648,950K − refunds $2,397,274K = NET $24,251,676K; FY2002 gross $10,494,374K − refunds $742,154K = NET $9,752,220K. The stored revenue tree total must equal NET REVENUES; Revenue Refunds is a contra line handled as recon documents (do NOT store gross as the total)"
    - "GENERAL FUND column is the 1st of the Governmental Funds Statement of Rev/Exp/Changes (FY2025: General Fund | Tobacco Settlement Authority | Tobacco Collections Fund | Nonmajor | Total; FY2002: 4-column) — NOT the Statement of Activities, NOT a budgetary schedule"
    - "Source URLs are OPAQUE ePrints numeric IDs (publications.iowa.gov/<id>/…), NOT derivable — the per-FY SOURCES map is enumerated from the das.iowa.gov/acfr-archive landing table; each FY's URL verified individually"
    - "Pre-existing NASBO operating rows (IA FY2023 $8,216,000K, FY2024 $8,560,000K) are REPLACED in place at the same (6e71a93f, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence (IA ACFR GF ~2.83× NASBO — federal/intergovernmental 'Receipts from other entities' consolidated inside GAAP GF) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (slice→0, signed magnitude in label, parent nets it); IA investment income positive at both bookends (safety net); interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the ~34 existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on IA once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processIAAcfr.js"
      provides: "IA GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processIARevenueAcfr.js"
      provides: "IA GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, NET-REVENUES tie, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-01-IA-LOADLOG.md"
      provides: "Per-FY load disposition, NET-REVENUES tie handling note, NASBO-replacement confirmation, ~2.83× accept-relabel record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processIAAcfr.js / processIARevenueAcfr.js"
      to: "treasury.budgets (IA state node 6e71a93f)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['IA']"
      to: "scripts/processIAAcfr.js + scripts/processIARevenueAcfr.js"
      via: "gen_state.py code generation from ia/ia_all.json + enumerated per-FY SOURCES map (opaque ePrints IDs)"
      pattern: "General Fund"
---

<objective>
Upgrade the **Iowa** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processIAAcfr.js` (GF spending-by-function, `operating`) + `scripts/processIARevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading IA ACFR PDFs FY2002–FY2025 into gitignored `_acfr-work/ia/`, extracting the **GENERAL FUND column** (1st of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**IA revenue ties to the NET REVENUES line, not gross.** Iowa's GF revenue section prints GROSS REVENUES (sum of Taxes, Receipts from other entities, Investment income, Fees/licenses/permits, Refunds & reimbursements, Sales/rents/services, Miscellaneous, Contributions), then subtracts a **Revenue Refunds** contra line to reach **NET REVENUES**. The stored revenue tree total must equal NET REVENUES (FY2025 $24,251,676K, FY2002 $9,752,220K), matching the recon ties — do NOT store GROSS as the total.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $24,251,676K, FY2002 $9,752,220K. **Source URLs are opaque ePrints numeric IDs** (`publications.iowa.gov/<id>/…`), NOT derivable — enumerate the per-FY SOURCES map from the `das.iowa.gov/acfr-archive` landing table and verify each URL individually. Scope note: IA ACFR GF ~2.83× NASBO (federal 'Receipts from other entities' folded into GAAP GF) — accept-and-relabel honestly.

Output: both loaders live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `119-01-IA-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an older loader. -->
- `_acfr-work/gen_state.py` — add a `CONFIGS['IA']` entry (mirror the recent 'AR'/'DE'/'CO' entries): name='Iowa', abbr='IA', population (from loadStateGF.mjs), node='6e71a93f-a43d-4972-a239-85ddbebe2545', years=[the FYs that cleanly tie], tolerance (small; IA tied at $0 so a few ×1,000 units is safe), data='ia/ia_all.json', out='IA', base_url='https://das.iowa.gov/state-employees/state-accounting/state-financial-reports', sources={ enumerated per-FY opaque-ID URL map }, UNITS=1000. Use `head_note` for the ~2.83× scope note + NET-REVENUES handling; `neg_note` for the investment-income clamp note. Then `py -3 gen_state.py IA` emits scripts/processIAAcfr.js + processIARevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor (anchors on the Total-revenues row's right edge). For IA, capture the revenue line items AND the Revenue Refunds contra line + NET REVENUES so the revenue tree total resolves to NET REVENUES. Feeds ia/ia_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }. Document any hand-patch inline with printed-row evidence.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the ephemeral data_sources lifecycle (delete-stale-by-dataset_id → insert → RPC/stamp → delete-by-id at end — leaves 0 residue). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ia-acfr-gf-operating' (name 'Iowa General Fund Operating Budget'), 'ia-acfr-gf-revenue' (name 'Iowa General Fund Revenue'), api_type='pdf_download', base_url = the DAS landing page.
- NASBO baseline (live-confirmed): IA has 2 NASBO operating rows only — FY2023 $8,216,000K, FY2024 $8,560,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows, NO data_sources rows (clean node).
- DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH2-SOURCES.md IA Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF = 1st column (FY2025: General Fund | Tobacco Settlement Authority | Tobacco Collections Fund | Nonmajor | Total; FY2002 4-column). Units = THOUSANDS. FY-end June 30.
- Clean window FY2002–FY2025 (24 yr; archive goes to FY1997, pre-FY2002 not verified this pass — out of scope, not a blocker).
- URLs — OPAQUE ePrints numeric IDs, NOT derivable. Confirmed samples: FY2025 `https://publications.iowa.gov/54805/1/ACFR%20FY2025%20-%20Protected%2012.22.2025.pdf`; FY2002 `https://publications.iowa.gov/5514/2/FY02_CAFR.pdf`. Full per-year enumeration on landing `https://das.iowa.gov/acfr-archive` (table back to FY1997).
- Landing: `https://das.iowa.gov/state-employees/state-accounting/state-financial-reports`
- REVENUE TIES TO NET REVENUES: FY2025 GF line items sum to GROSS REVENUES $26,648,950K (Taxes 11,938,586 + Receipts from other entities 10,668,647 + Investment income 485,838 + Fees/licenses/permits 1,723,499 + Refunds & reimbursements 924,381 + Sales/rents/services 33,129 + Miscellaneous 874,870); NET REVENUES = gross − Revenue Refunds $2,397,274K = $24,251,676K (diff $0). FY2002 gross $10,494,374K − Revenue Refunds $742,154K = NET $9,752,220K (diff $0).
- Investment income positive at both bookends (FY2025 +$485,838K, FY2002 +$80,099K). No P2 clamp needed for confirmed years; check interior years at load.
- Scope vs NASBO ~2.83× (FY2025 ACFR $24.25B vs FY2024 NASBO $8.56B) — GAAP GF folds in ~$10.7B 'Receipts from other entities' (federal/intergovernmental) NASBO's narrower operating concept excludes. Accept-and-relabel honestly.
</source_facts>
</context>

<tasks>

<task id="119-01-01" type="auto">
  <name>Task 1: Generate both IA loaders (UNITS=1000, NET-REVENUES tie) via gen_state.py + download/extract/transcribe FY2002–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ia/ (gitignored PDFs + pdftotext + ia_all.json), scripts/processIAAcfr.js, scripts/processIARevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'AR'/'DE'/'CO' entry pattern; confirm how UNITS + sources + tolerance + head_note/neg_note are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how categories + a contra line are emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH2-SOURCES.md (IA Detail Block: enumerated opaque-ID per-year URLs, units=thousands, bookend ties, NET-REVENUES arithmetic)
    - scripts/processARAcfr.js (a recent gen_state.py output — the exact generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). Enumerate the IA per-FY PDF URLs from the `das.iowa.gov/acfr-archive` table (opaque `publications.iowa.gov/<id>/…` IDs are NOT derivable — each must be read off the landing table and verified). For each FY across the durable window (target FY2002–FY2025), download the IA ACFR PDF to `_acfr-work/ia/IA{YYYY}.pdf`. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, and run `extract_gf.py` to isolate the GENERAL FUND column (1st column) for BOTH the revenues section and the expenditures section. **For revenue, capture the line items PLUS the Revenue Refunds contra line + the printed NET REVENUES total; the stored revenue tree total must equal NET REVENUES (gross − Revenue Refunds), not gross.** Assemble `_acfr-work/ia/ia_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names (thousands, unscaled at this stage); document any hand-patch inline with printed-row evidence. Add a `CONFIGS['IA']` entry to `_acfr-work/gen_state.py` (node 6e71a93f…, years=[the FYs that cleanly tie], UNITS=1000, tolerance small, data='ia/ia_all.json', out='IA', base_url the DAS landing page, sources=enumerated opaque-ID map, head_note recording the ~2.83× accept-relabel + NET-REVENUES handling, neg_note the investment-income clamp note). Run `py -3 gen_state.py IA` to emit `scripts/processIAAcfr.js` + `scripts/processIARevenueAcfr.js` (dataset_ids ia-acfr-gf-operating / ia-acfr-gf-revenue). Cross-check FY2025 NET REVENUES stored = 24,251,676×1000 and FY2002 = 9,752,220×1000 — if either bookend does not match, STOP and re-inspect column/NET-vs-gross selection before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processIAAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processIAAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processIARevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '24,251,676,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ia/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected; each opaque-ID URL verified against the archive table.
    - `node scripts/processIAAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints TOTAL 24,251,676,000 and `--fy 2002` prints 9,752,220,000 (NET REVENUES ×1,000).
    - Revenue tree total equals NET REVENUES (gross − Revenue Refunds), not gross, at both bookends.
    - Both loaders carry `UNITS = 1000`; `_acfr-work/gen_state.py` has a `CONFIGS['IA']` entry; `_acfr-work/ia/ia_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("Iowa State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='ia-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both IA loaders exist (generated by gen_state.py) with UNITS=1000, enumerated opaque-ID SOURCES map, ia_all.json holds every tied FY with revenue resolving to NET REVENUES, both bookends (24,251,676K / 9,752,220K) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="119-01-02" type="auto">
  <name>Task 2: Live-load IA (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processIAAcfr.js, scripts/processIARevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processIAAcfr.js + scripts/processIARevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; IA node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the IA node (6e71a93f) and RECORD the pre-load NASBO operating rows (FY2023 $8,216,000K, FY2024 $8,560,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. For each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processIAAcfr.js --fy <YYYY>` then `node scripts/processIARevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (6e71a93f, fy, 'operating'); revenue inserts net-new at (6e71a93f, fy, 'revenue')), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded IA GF operating + revenue totals against the pre-load NASBO totals to confirm the ~2.83× accept-relabel divergence is the expected federal-inside-GAAP-GF mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "IA row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the IA node has, per loaded FY, an operating row "Iowa State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - IA FY2025 revenue row total = 24,251,676,000 and FY2002 = 9,752,220,000 (NET REVENUES ×1,000).
    - IA FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (IA, fy), GAAP-labelled; no "NASBO" label remains on the IA node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~2.83× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed IA FY has operating + revenue rows in treasury.budgets, thousands-scaled (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="119-01-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-01-IA-LOADLOG.md</files>
  <read_first>
    - scripts/processIAAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the existing ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. IA --fy 2025 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded IA FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) IA has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (IA, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — IA has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for IA; (f) cohort untouched: spot-check that a sample of the existing ACFR nodes (e.g. CA e1007bf5, the Batch-1 AK b268c415) + one un-upgraded NASBO state are unchanged (row counts + a couple data_source labels). Append a "Load Disposition" section to 119-01-IA-LOADLOG.md: FYs loaded, FYs skipped (+reason), the NET-REVENUES tie handling note, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than IA.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 119-01-IA-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of IA --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded IA FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; IA has ZERO remaining "NASBO" operating rows; exactly one operating row per (IA, fy).
    - IA has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for IA (LOAD-01 holds).
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 119-01-IA-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms IA fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 119-01-IA-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publications.iowa.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the IA node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01-A | Tampering | units mis-scale (applying/omitting the ×1,000) inflates or deflates IA 1000× | mitigate | UNITS=1000 hard-set; bookend totals (24,251,676K / 9,752,220K) asserted in dry-run + live |
| T-119-01-B | Tampering | storing GROSS REVENUES instead of NET REVENUES (over-states IA revenue by the Revenue Refunds contra) | mitigate | revenue tree total resolves to NET REVENUES (gross − Revenue Refunds); bookends asserted against NET; validate() exit-2 on non-tie |
| T-119-01-C | Tampering | extracting the wrong statement/column (Statement of Activities, or Tobacco/Nonmajor/Total instead of GENERAL FUND) | mitigate | extract_gf.py isolates the 1st column of the Governmental Funds statement; bookend ties gate before transcription |
| T-119-01-D | Tampering | wrong opaque ePrints ID → wrong-year or 404 PDF | mitigate | each per-FY URL verified against the das.iowa.gov/acfr-archive table; %PDF magic + size guard; honest hole if a year 404s |
| T-119-01-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (IA, fy), zero NASBO labels |
| T-119-01-F | Tampering | data_sources residue left behind | mitigate | generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-119-01-G | Info Disclosure | silently inflating IA (~2.83×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-119-01-H | Tampering | write to a state other than IA | mitigate | loader resolves only the IA node id; Task 3 spot-checks cohort unchanged |
| T-119-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both IA loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processIARevenueAcfr.js --dry-run --fy 2025` → 24,251,676,000 (NET REVENUES); `--fy 2002` → 9,752,220,000.
- Revenue tree total = NET REVENUES (gross − Revenue Refunds) at both bookends.
- Live: IA node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (IA, fy)).
- Re-run IA --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for IA (LOAD-01).
- IA ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~2.83×) recorded against pre-load NASBO totals.
- Existing ACFR nodes + a sample NASBO state unchanged.
- 119-01-IA-LOADLOG.md finalized.
</verification>

<success_criteria>
The Iowa state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY2002–FY2025, honest holes logged), stored in thousands (UNITS=1000), revenue resolving to NET REVENUES, every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~2.83× accept-relabel divergence recorded at load, the P2 clamp wired for any negative year, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands IA to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/119-acfr-upgrade-batch-2-ia-ks-me-ms-mt-acfr-38-42/119-01-SUMMARY.md` when done — record the IA FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend NET-REVENUES tie confirmations, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
