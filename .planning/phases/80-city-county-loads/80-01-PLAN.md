---
phase: 80-city-county-loads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadVAComparativeReport.js
  - scripts/loadVAComparativeReportBatch.js
  - scripts/loadVAComparativeReport.test.mjs
autonomous: true
requirements: [VALOAD-04]
must_haves:
  truths:
    - "Section-aware locality lookup: the loader can resolve a locality row within a SPECIFIC report section (Cities §1 vs Counties §3) so homonym counties (Fairfax, Franklin, Richmond, Roanoke) resolve to the county row, not the same-named city row (CONTEXT D-04). Sectioning is by the 'No.' column resetting to 1 — NOT by 'Total' delimiter rows, because Exhibit H has none (D-03)."
    - "A batch driver scripts/loadVAComparativeReportBatch.js opens each FY workbook once, segments the roster into cities (§1, 38) and counties (§3, 95), and loops the existing buildExpenditureTree/buildRevenueTree/localityPopulation + write path once per (locality, dataset) — the proven single-locality loader stays intact"
    - "Counties are stored as '<name> County' with entity_type='county'; cities are stored bare with entity_type='city' (CONTEXT D-05) — the XLSX match name (bare col-2 value) is decoupled from the DB display name"
    - "The batch driver reuses the Phase 79 never-overwrite guard + per-FY source stamping (data_source='Virginia APA Comparative Report', source_url from vaApaDatasets.json, source_date) and supports --dry-run (whole-roster parse, zero writes) and an entity-type filter (cities-only / counties-only)"
  artifacts:
    - path: "scripts/loadVAComparativeReportBatch.js"
      provides: "The Phase 80 batch driver — roster segmentation + city/county loop over the Phase 79 tree-builders/write path"
      contains: "loadVAComparativeReportBatch"
    - path: "scripts/loadVAComparativeReport.js"
      provides: "Phase 79 loader extended with section-scoped lookup + an exported importLocality/write helper reused by the batch driver"
      contains: "section"
    - path: "scripts/loadVAComparativeReport.test.mjs"
      provides: "Offline tests extended with segmentation counts, homonym safety, and county display-name cases"
      contains: "Fairfax"
  key_links:
    - from: "VA APA Comparative Report XLSX (full roster)"
      to: "a deterministic list of {matchName, displayName, entityType} for 38 cities + 95 counties"
      via: "segment Exhibit C by No.-reset → §1 cities / §3 counties; counties get '<name> County' + entity_type county"
      pattern: "loadVAComparativeReportBatch"
---

<objective>
Build the Phase 80 bulk-load tooling: make the Phase 79 loader **section-aware** (so homonym counties resolve correctly) and add a **batch driver** that segments the report's locality roster and iterates the proven tree-builders + write path over all 38 cities and 95 counties. No live bulk load in this plan — prove the whole roster parses via `--dry-run` and lock the behavior with offline tests. The live load + in-phase verification happen in plan 80-02.

The only genuinely new logic is (1) section-scoped locality lookup for homonym safety and (2) the roster-segmentation loop with city/county display-name + entity-type rules. Everything else — the two RPCs, the never-overwrite guard, per-FY sourcing, the function→activity / source→sub-source trees, Exhibit H population — is reused verbatim from Phase 79.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/80-city-county-loads/80-CONTEXT.md
@scripts/loadVAComparativeReport.js
@scripts/loadVAComparativeReport.test.mjs
@scripts/vaApaDatasets.json

<interfaces>
<!-- Phase 79 already proved the parse + write path. This plan adds section-awareness + a roster loop. -->

Phase 79 exports (scripts/loadVAComparativeReport.js):
- buildExpenditureTree(workbook, localityName) → { tree, total }
- buildRevenueTree(workbook, localityName) → { tree, total }
- localityPopulation(workbook, localityName) → number|null
- cellNum(cell), cellText(cell), DATA_SOURCE_NAME='Virginia APA Comparative Report'
- internal (not yet exported): findHeaderRow(ws), findLocalityRow(ws, headerRow, name), importDataset(...), findConflictingBudget(...), getSupabase()

Report structure (verified, FY2024 sample _va-recon/fy2024-comparative-report.xlsx):
- Localities appear in three numbered sections in EVERY exhibit, uniform order: §1 Cities (38, Alexandria…Winchester) → §3 Counties (95, Accomack…York) → §5 Towns (37). The "No." (col 1) RESETS to 1 at the start of each section. Header/footnote/Total rows have a non-numeric col-1.
- Header row varies per sheet (Exhibit C=5, B=6, H=5) — `findHeaderRow` already scans for the "No." row, keep using it.
- Exhibit H has NO "Total" delimiter rows → segment ONLY by No.-reset (do not depend on "Total").
- Homonyms (city §1 AND county §3, same bare col-2 name): Fairfax, Franklin, Richmond, Roanoke.

Write path (reuse Phase 79 importDataset): treasury_ensure_municipality(p_name, p_state='VA', p_entity_type, p_population) → municipality_id; never-overwrite guard (findConflictingBudget) → skip if a different data_source owns (muni, fy, dataset); else treasury_sync_city_budget(... p_data_source_name, p_source_url, p_source_date). p_name = DISPLAY name (county → "<name> County"); the XLSX lookup uses the MATCH name (bare col-2 value).

Source manifest (scripts/vaApaDatasets.json): years[].xlsxUrl + datasetUrl per fiscal_year (2023, 2024). Use the per-FY URL as source_url (CONTEXT D-07).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make locality lookup section-aware (homonym safety)</name>
  <files>scripts/loadVAComparativeReport.js</files>
  <read_first>
    - scripts/loadVAComparativeReport.js (findLocalityRow, findHeaderRow, cellText, cellNum — current global top-down match; buildExpenditureTree/buildRevenueTree/localityPopulation call findLocalityRow)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-03 segment by No.-reset not Total; D-04 section-aware lookup mandatory for homonyms)
    - _va-recon/fy2024-comparative-report.xlsx (verify the No.-reset segmentation across Exhibit C, B, B2, C6, H before coding)
  </read_first>
  <action>
    Add section-scoped lookup to scripts/loadVAComparativeReport.js WITHOUT breaking the existing single-locality path:
    - Add a helper `sectionIndexOfRow(ws, headerRow, targetRow)` OR `findLocalityRowInSection(ws, headerRow, name, sectionIndex)` where sectionIndex 0=Cities, 1=Counties, 2=Towns. Determine section boundaries by walking data rows below headerRow and incrementing the section counter each time the col-1 "No." value resets to numeric 1 after previously being a number > 1. Treat rows with a NON-NUMERIC col-1 (section headers like "County of:", footnotes, "Total", "Grand Total") as skip-rows that do not advance or reset the section.
    - Give buildExpenditureTree, buildRevenueTree, localityPopulation an OPTIONAL `sectionIndex` parameter (default behavior when omitted = current global first-match, so Phase 79 callers/tests are unchanged). When sectionIndex is provided, resolve the locality row only within that section, and apply the same scoping when drilling sub-exhibits (C1–C8 for expenditure, B2 for revenue) — each sub-exhibit has the same 3-section structure, so reuse the same sectionIndex.
    - Do NOT change the existing default-call signatures used by the 7 Phase 79 tests; they must still pass.
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadVAComparativeReport.test.mjs` still passes the original 7 Phase 79 cases (no regression on the default/global path)
    - With sectionIndex=1 (Counties), `buildExpenditureTree(wb, "Fairfax", 1)` and `buildExpenditureTree(wb, "Fairfax", 0)` (Cities) return DIFFERENT `total` values (county ≠ city) on the FY2024 sample
    - The section helper skips non-numeric col-1 rows: a locality name that only appears in the Towns section is NOT found when sectionIndex=0 or 1
    - localityPopulation with sectionIndex=1 for a homonym county returns the COUNTY population (different from the city's)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Export a shared importLocality write helper</name>
  <files>scripts/loadVAComparativeReport.js</files>
  <read_first>
    - scripts/loadVAComparativeReport.js (importDataset, findConflictingBudget, getSupabase, main() — the existing per-locality write sequence; DATA_SOURCE_NAME)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-05 display vs match name + entity_type; D-06 never-overwrite; D-07 sourcing; D-08 per-FY population)
  </read_first>
  <action>
    Refactor the live-write sequence inside main() into an EXPORTED async function `importLocality(supabase, workbook, opts)` where opts = { matchName, displayName, entityType, state='VA', fiscalYear, sourceUrl, sourceDate, sectionIndex, dryRun }. It must:
    - Build the trees: `buildExpenditureTree(workbook, matchName, sectionIndex)`, `buildRevenueTree(workbook, matchName, sectionIndex)`, `localityPopulation(workbook, matchName, sectionIndex)`.
    - If dryRun: return a summary object { displayName, entityType, fiscalYear, operatingTotal, revenueTotal, population, expFunctions, revSources } and perform ZERO writes/guard reads.
    - Else: `treasury_ensure_municipality(p_name=displayName, p_state=state, p_entity_type=entityType, p_population=population||0)`; then for 'operating' and 'revenue' call the existing `importDataset(...)` (which already runs the never-overwrite guard + treasury_sync_city_budget with the source columns). Return per-dataset write/skip status.
    - Keep main() (the single-locality CLI) working by having it call importLocality with displayName=matchName=values.locality, entityType=values['entity-type'], sectionIndex derived from entity-type (city→0, county→1, town→2) or omitted. Export importDataset, findConflictingBudget, getSupabase, and importLocality.
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/loadVAComparativeReport.js').then(m=>console.log(typeof m.importLocality, typeof m.getSupabase, typeof m.importDataset))"` prints `function function function`
    - `node scripts/loadVAComparativeReport.js --file _va-recon/fy2024-comparative-report.xlsx --locality Alexandria --fy 2024 --dry-run` still prints operating $863,578,347 / revenue $874,230,660 / pop 158591 and writes nothing (single-locality path unbroken)
    - importLocality in dryRun mode returns an object with finite numeric operatingTotal and revenueTotal and performs no Supabase RPC mutation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Build the batch driver (roster segmentation + city/county loop)</name>
  <files>scripts/loadVAComparativeReportBatch.js</files>
  <read_first>
    - scripts/loadVAComparativeReport.js (the now-exported importLocality, section helpers, getSupabase, cellText)
    - scripts/vaApaDatasets.json (per-FY xlsxUrl/datasetUrl → source_url)
    - scripts/loadCountyBudget.js (county DB-name convention "<county> County", entity_type='county')
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-02 cities+counties only; D-03 roster from sections; D-05 naming/entity-type)
  </read_first>
  <action>
    Create `scripts/loadVAComparativeReportBatch.js` (ESM). Export `enumerateRoster(workbook)` and `loadVAComparativeReportBatch(opts)`:
    - `enumerateRoster(workbook)` → reads Exhibit C, segments by No.-reset into sections, and returns ordered arrays { cities:[name…], counties:[name…], towns:[name…] } of the bare col-2 names. Skip non-numeric-No. rows (headers/footnotes/Total/Grand Total). Assert/return counts so the caller can sanity-check (expect 38 cities, 95 counties on FY2024).
    - `loadVAComparativeReportBatch(opts)` where opts = { file, fiscalYear, sourceUrl, entityTypes=['city','county'], dryRun, limit }:
        - open the workbook once (ExcelJS readFile)
        - build the work list: for each city → { matchName, displayName=matchName, entityType:'city', sectionIndex:0 }; for each county → { matchName, displayName:`${matchName} County`, entityType:'county', sectionIndex:1 }. Filter by `entityTypes`. `limit` (optional) caps the count for smoke runs.
        - get one supabase client (skip when dryRun)
        - loop SERIALLY calling importLocality(...); collect {displayName, entityType, operatingTotal, revenueTotal, population, status}. Log a one-line progress per locality and a final summary (loaded / skipped-never-overwrite / errored counts).
    - CLI flags (node:util parseArgs): `--file <xlsx>`, `--fy <YYYY>`, `--source-url <url>` (default: look up vaApaDatasets.json by --fy), `--entity-type <city|county>` (repeatable or comma list; default both), `--limit <N>`, `--dry-run`. Default source_url resolves from vaApaDatasets.json for the given --fy.
    - Reuse the missing-SUPABASE_SERVICE_KEY guard (only when not --dry-run).
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/loadVAComparativeReportBatch.js').then(m=>console.log(typeof m.enumerateRoster, typeof m.loadVAComparativeReportBatch))"` prints `function function`
    - `enumerateRoster(wb)` on the FY2024 sample returns cities.length===38 and counties.length===95 (and towns.length===37); cities[0]==="Alexandria", counties includes "Fairfax", "Richmond", "Accomack", "York"
    - `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2024-comparative-report.xlsx --fy 2024 --dry-run` exits 0, processes 133 localities, prints a final summary line with counts, and writes nothing
    - In the dry-run output the county row for "Fairfax County" shows a different operating/revenue total than the city row "Fairfax"
    - With `--entity-type county --limit 3 --dry-run` only 3 county rows are processed, each displayName ending in " County"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Extend offline tests — segmentation, homonym safety, county naming</name>
  <files>scripts/loadVAComparativeReport.test.mjs</files>
  <read_first>
    - scripts/loadVAComparativeReport.test.mjs (existing node:test structure; how it loads _va-recon sample and skips gracefully if absent)
    - scripts/loadVAComparativeReportBatch.js (enumerateRoster export under test)
    - scripts/loadVAComparativeReport.js (section-aware builders under test)
  </read_first>
  <action>
    Extend scripts/loadVAComparativeReport.test.mjs (keep the 7 existing tests). Add cases that assert hard when _va-recon/fy2024-comparative-report.xlsx is present and skip gracefully when absent:
    - enumerateRoster returns 38 cities / 95 counties / 37 towns; cities[0]==='Alexandria'; counties contains 'Fairfax' and 'Richmond'.
    - Homonym safety: buildExpenditureTree(wb,'Fairfax',1).total !== buildExpenditureTree(wb,'Fairfax',0).total; same for 'Richmond'. (county total !== city total)
    - Section scoping: a Towns-only name resolves under sectionIndex 2 but throws/【not-found】 under sectionIndex 0.
    - localityPopulation(wb,'Fairfax',1) !== localityPopulation(wb,'Fairfax',0).
    - Backward-compat: the original global calls (no sectionIndex) still return the Alexandria FY2024 totals.
    Run with `node --test scripts/loadVAComparativeReport.test.mjs`.
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadVAComparativeReport.test.mjs` exits 0 with ALL tests passing (original 7 + new section/homonym/roster cases)
    - At least one test asserts county-vs-city total divergence for a homonym (Fairfax or Richmond)
    - At least one test asserts enumerateRoster counts (38 / 95 / 37)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadVAComparativeReport.test.mjs` → all pass (7 original + new section/homonym/roster tests).
- `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2024-comparative-report.xlsx --fy 2024 --dry-run` → 133 localities parsed (38 cities + 95 counties), final summary printed, zero writes; "Fairfax County" total ≠ "Fairfax" city total.
- Single-locality loader (`--locality Alexandria … --dry-run`) still reproduces $863,578,347 / $874,230,660 / pop 158591 — no Phase 79 regression.
- No live DB writes in this plan (dry-run only); the live bulk load + idempotency + spot-check are plan 80-02.
</verification>
