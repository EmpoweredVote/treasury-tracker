---
phase: 80-city-county-loads
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - scripts/vaApaDatasets.json
autonomous: true
requirements: [VALOAD-01, VALOAD-02, VALOAD-04]
must_haves:
  truths:
    - "All 38 Virginia independent cities (incl. Alexandria + Falls Church) are loaded with operating (expenditure) + revenue datasets for FY2023 and FY2024, every row sourced (data_source='Virginia APA Comparative Report', per-FY source_url, source_date) — VALOAD-01"
    - "All 95 Virginia counties are loaded with the same two datasets for FY2023 and FY2024, stored as '<name> County' with entity_type='county', every row sourced — VALOAD-02"
    - "Per-capita population is set on each locality from each FY's Exhibit H figure (per-FY, not a fixed vintage) so per-capita renders (CONTEXT D-08)"
    - "Re-running the batch loader changes nothing: the never-overwrite guard skips rows already owned by another source and idempotent re-writes produce identical trees/totals (VALOAD-04)"
    - "A spot-check confirms loaded FY2024 totals match the published report: Alexandria operating $863,578,347 / revenue $874,230,660, plus one county cross-read"
  artifacts:
    - path: "scripts/vaApaDatasets.json"
      provides: "FY→source-URL manifest, confirmed/used for FY2023 + FY2024 live loads (source_url provenance)"
      contains: "xlsxUrl"
  key_links:
    - from: "FY2023 + FY2024 APA Comparative Report XLSX"
      to: "treasury.budgets operating+revenue rows for 133 VA cities+counties × 2 FY, fully sourced"
      via: "loadVAComparativeReportBatch over cities (§1) and counties (§3), idempotent + never-overwrite"
      pattern: "loadVAComparativeReportBatch"
---

<objective>
Run the Phase 80 batch loader live against production Supabase to load all 38 cities and 95 counties for FY2023 and FY2024, then verify the load in-phase: DB row counts, a published-report spot-check (Alexandria FY2024 exact + one county), per-capita population set per FY, every row sourced, and idempotency (a second run changes nothing). This delivers VALOAD-01 (cities), VALOAD-02 (counties), and demonstrates VALOAD-04 (idempotent) live.

Live writes require the gitignored `.env` SUPABASE_SERVICE_KEY and run SERIALLY on the main tree. The writes are safe to repeat: the never-overwrite guard protects any richer pre-existing source and the loader is idempotent. The deeper source-chain audit + UAT are Phase 83 — this plan only does the in-phase spot-check (ROADMAP SC#4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-city-county-loads/80-CONTEXT.md
@scripts/loadVAComparativeReportBatch.js
@scripts/loadVAComparativeReport.js
@scripts/vaApaDatasets.json

<interfaces>
<!-- The batch driver (plan 80-01) does the work; this plan acquires the data, runs it live, and verifies. -->

Source manifest (scripts/vaApaDatasets.json):
- FY2024: xlsxUrl …/download/fy2024-comparative-report.xlsx (final)
- FY2023: xlsxUrl …/download/fy2023-comparative-report.xlsx
Both confirmed in Phase 79 to share the same 19-sheet structure (loader works unchanged across both).

Batch driver CLI (plan 80-01):
- node scripts/loadVAComparativeReportBatch.js --file <xlsx> --fy <YYYY> [--source-url <url>] [--entity-type city|county] [--limit N] [--dry-run]
- source_url defaults to the vaApaDatasets.json entry for --fy.

Verification probes (read-only, treasury schema):
- Count rows: SELECT count(*) FROM treasury.budgets WHERE data_source='Virginia APA Comparative Report' GROUP BY fiscal_year, dataset_type.
- Sourcing: 0 rows with NULL/empty source_url among VA APA rows.
- Spot-check: the operating/revenue total for Alexandria FY2024 and one county FY2024.
- Use the read-only DB access pattern used by recent phases (mcp__supabase-local execute_sql or a small read-only node probe with the .env service key).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire both FY workbooks</name>
  <files>scripts/vaApaDatasets.json</files>
  <read_first>
    - scripts/vaApaDatasets.json (xlsxUrl for FY2023 and FY2024)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-01 FY2023+FY2024 scope; D-07 per-FY source_url)
  </read_first>
  <action>
    Ensure both fiscal-year XLSX files are present locally in the gitignored working dir `_va-recon/` (the FY2024 file is already there from Phase 79). Download the FY2023 workbook from its vaApaDatasets.json `xlsxUrl` to `_va-recon/fy2023-comparative-report.xlsx` if missing (curl/Invoke-WebRequest). Confirm each opens with exceljs and contains "Exhibit C" + "Exhibit B" + "Exhibit H". Do not commit the XLSX (gitignored); vaApaDatasets.json is the committed provenance record — verify its two URLs still resolve and leave the file as-is (no edits unless a URL changed).
  </action>
  <acceptance_criteria>
    - `_va-recon/fy2023-comparative-report.xlsx` and `_va-recon/fy2024-comparative-report.xlsx` both exist
    - `node -e "import('exceljs').then(async({default:E})=>{const w=new E.Workbook();await w.xlsx.readFile('_va-recon/fy2023-comparative-report.xlsx');console.log(['Exhibit C','Exhibit B','Exhibit H'].every(s=>!!w.getWorksheet(s)))})"` prints `true`
    - `enumerateRoster` on the FY2023 workbook returns 38 cities and 95 counties (roster stable across both years)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Dry-run the full roster on both years (pre-flight)</name>
  <files>scripts/loadVAComparativeReportBatch.js</files>
  <read_first>
    - scripts/loadVAComparativeReportBatch.js (loadVAComparativeReportBatch + CLI)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-04 homonym safety; D-05 naming)
  </read_first>
  <action>
    Before any write, dry-run the batch loader for FY2024 and FY2023 across the full cities+counties roster and eyeball the summaries. Confirm 133 localities parse per year, the Alexandria FY2024 totals match ($863,578,347 / $874,230,660), homonym counties show county-distinct totals, and no locality errors out (Education-leaf fallback is fine). Capture the per-year processed/parsed counts for the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2024-comparative-report.xlsx --fy 2024 --dry-run` exits 0, processes 133 localities (38 cities + 95 counties), zero errors, zero writes
    - `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2023-comparative-report.xlsx --fy 2023 --dry-run` exits 0, processes 133 localities, zero writes
    - Alexandria FY2024 dry-run line shows operating 863578347 and revenue 874230660; "Fairfax County" total ≠ "Fairfax" city total
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live bulk load — cities + counties, FY2023 and FY2024</name>
  <files>scripts/loadVAComparativeReportBatch.js</files>
  <read_first>
    - scripts/loadVAComparativeReportBatch.js (live write path via importLocality; missing-key guard)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-06 never-overwrite/idempotent; D-07 sourcing; D-08 per-FY population)
  </read_first>
  <action>
    With the gitignored `.env` SUPABASE_SERVICE_KEY sourced, run the batch loader live (no --dry-run) on the main tree, SERIALLY, for all four (entity-type × FY) combinations:
    - FY2024 cities, FY2024 counties (file _va-recon/fy2024-comparative-report.xlsx, --fy 2024)
    - FY2023 cities, FY2023 counties (file _va-recon/fy2023-comparative-report.xlsx, --fy 2023)
    source_url defaults from vaApaDatasets.json per FY. Counties are written as "<name> County" / entity_type=county; cities bare / entity_type=city. Watch the final summary counts (loaded vs skipped-never-overwrite vs errored) for each run; investigate any errored locality (re-run that one with the single-locality loader to diagnose) — a never-overwrite SKIP is expected & fine for any locality already owned by a richer source.
  </action>
  <acceptance_criteria>
    - All four runs exit 0; combined summary shows the 38 cities and 95 counties processed for each of FY2023 and FY2024 with 0 hard errors
    - Read-only probe: `SELECT fiscal_year, dataset_type, count(*) FROM treasury.budgets WHERE data_source='Virginia APA Comparative Report' GROUP BY 1,2 ORDER BY 1,2` returns rows for fiscal_year 2023 and 2024, dataset_type operating and revenue, with per-(year,dataset) counts ≈133 (133 = 38 cities + 95 counties; minor shortfall acceptable only for a locality genuinely absent from that FY's report — document any)
    - A homonym county exists as its own municipality row: a `municipalities` row name='Fairfax County' entity_type='county' AND a distinct name='Fairfax' entity_type='city'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: In-phase verification — spot-check, sourcing, per-capita, idempotency</name>
  <files>scripts/vaApaDatasets.json</files>
  <read_first>
    - scripts/loadVAComparativeReportBatch.js
    - .planning/ROADMAP.md (Phase 80 success criteria 1–4)
    - .planning/phases/80-city-county-loads/80-CONTEXT.md (D-06/07/08)
  </read_first>
  <action>
    Run read-only verification probes and an idempotency re-run:
    - Spot-check (SC#4): confirm the stored Alexandria FY2024 operating total = $863,578,347 and revenue = $874,230,660; pick one county (e.g. Fairfax County FY2024) and cross-read its stored operating/revenue total against the published report value (from the dry-run / the XLSX) — they must match.
    - Sourcing (SC#1): 0 VA APA rows with NULL/empty source_url; data_source uniformly 'Virginia APA Comparative Report'.
    - Per-capita (SC#2): the municipalities rows for the loaded localities have a non-zero population sourced from Exhibit H (per-FY); confirm at least the spot-check localities.
    - Idempotency (SC#3 / VALOAD-04): re-run one full (entity-type × FY) load and confirm the summary reports the rows as written-identical / no duplicate municipalities created, and the row counts from Task 3's probe are unchanged.
    Record results for the SUMMARY (phase 83 will do the full source-chain audit + UAT — this is the in-phase gate only).
  </action>
  <acceptance_criteria>
    - Stored Alexandria FY2024 operating=863578347 and revenue=874230660 (exact); the chosen county's stored FY2024 totals match the report
    - `SELECT count(*) FROM treasury.budgets WHERE data_source='Virginia APA Comparative Report' AND (source_url IS NULL OR source_url='')` returns 0
    - The idempotency re-run creates 0 new municipalities and leaves the per-(year,dataset) row counts unchanged from Task 3
    - At least the spot-check localities have a non-zero, Exhibit-H-sourced population set
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Read-only probe shows operating+revenue rows for FY2023 and FY2024 across ~133 cities+counties, all `data_source='Virginia APA Comparative Report'`, 0 NULL source_url.
- Alexandria FY2024 stored totals = $863,578,347 / $874,230,660 (exact); one county FY2024 total cross-reads to the report.
- Homonym counties stored as distinct "<name> County" municipalities, totals ≠ their same-named cities.
- Per-capita population set per FY from Exhibit H.
- Idempotency: a re-run adds no municipalities and changes no row counts (VALOAD-04).
- Full source-chain audit + Chris UAT are Phase 83 — not repeated here.
</verification>
