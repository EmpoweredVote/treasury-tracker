---
phase: 122-deepening-existing-acfr-node-pre-window-holes-deep-05
plan: "122-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processFLAcfr.js
  - scripts/processFLRevenueAcfr.js
  - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-02-FL-LOADLOG.md
autonomous: true
requirements: [DEEP-05]
user_setup: []

must_haves:
  truths:
    - "D-04: the existing Florida STATE node adb19ea0-de7c-4cd5-9445-cbf2108a8a1a gains up to FY2003–FY2020 (18 clean years) on BOTH dataset_types — operating (processFLAcfr.js) + revenue (processFLRevenueAcfr.js) — extending its window from FY2021–FY2024 to FY2003–FY2024 (22yr); the pre-existing FY2021–FY2024 rows are untouched (byte-identical totals + source stamps)"
    - "D-04 units: FL uses UNITS = 1_000 (data objects hold RAW THOUSANDS; ×1,000 → dollars, unchanged from existing FL years). Bookend tie targets are the printed GENERAL FUND column figures: FY2003 Total revenues $19,857,818K → data object 19_857_818; FY2020 $40,534,343K → 40_534_343"
    - "D-04 filename map (NO single rule — alternates cafr{YYYY}.pdf ↔ {YYYY}cafr.pdf year-to-year): the loader MUST use the explicit per-year map from 117-DEEPEN-SOURCES.md, probing BOTH variants per year, never guessing a formula. All under https://www.myfloridacfo.com/docs-sf/default-source/transparency-docs/cafr/. Content-Type application/pdf distinguishes a real file from the 3,224-byte HTML soft-404"
    - "D-07 tie discipline: every added FY's transcribed GF categories tie to the printed GENERAL FUND column Total (revenues AND expenditures); the GENERAL FUND value is the 1st numeric token per row under -table. validate() refuses to write on >$10M mismatch; FY2003 + FY2020 confirmed exact-$0 in recon. Extraction = pdftotext -table on local PDFs in gitignored _acfr-tmp/fl/, 1st column via extract_gf.py"
    - "D-08 P2 clamp: FL GF carries negative 'Investment earnings (losses)' (and occasionally 'Other') in down-market years — every added FY's negatives render via the existing clampForRender path (slice→0, signed magnitude in label, parent nets), checked at EVERY added year"
    - "D-08 idempotent never-overwrite: extending the years array + SOURCES + data objects updates the SAME persistent data_sources rows (fl-acfr-gf-operating / fl-acfr-gf-revenue) keyed by dataset_id — no new/orphan rows (LOAD-01 holds); a second live run of a representative added FY = 0 net change"
    - "D-09 honest holes: FL FY2000–FY2002 have durable application/pdf URLs but pdftotext fails on damaged xref — a REPAIR-PENDING hole (optional qpdf --qdf --replace-input attempt; default = log, not attempt); any FY2003–FY2020 year that 404s or won't tie is OMITTED and logged with its reason. Nothing faked"
    - "Money In stays enabled on FL (already has revenue rows from FY2021+; adding earlier years is additive)"
  artifacts:
    - path: "scripts/processFLAcfr.js"
      provides: "FL GF operating loader extended toward FY2003–FY2024 (SOURCES per-year map + EXPENDITURES + main() years array include the tied 2003–2020 years), UNITS=1000"
      contains: "treasury_sync_budget_tree"
      min_lines: 167
    - path: "scripts/processFLRevenueAcfr.js"
      provides: "FL GF revenue loader extended toward FY2003–FY2024 (SOURCES + REVENUE + main() years array include the tied 2003–2020 years), UNITS=1000, P2 clamp wired"
      contains: "clampForRender"
      min_lines: 167
    - path: ".planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-02-FL-LOADLOG.md"
      provides: "Per-FY load disposition FY2003–FY2020, per-year filename resolution (which variant hit), bookend ties, per-year P2 clamp results, FY2000–FY2002 repair-pending disposition, idempotency re-run, pre-existing-window-untouched confirmation"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processFLAcfr.js / processFLRevenueAcfr.js"
      to: "treasury.budgets (FL STATE node adb19ea0-de7c-4cd5-9445-cbf2108a8a1a)"
      via: "treasury_sync_budget_tree RPC keyed (data_source_id, fy, dataset_type) + post-RPC source-stamp UPDATE; data_sources upsert by dataset_id; UNITS=1000"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Deepen the **Florida** State-ACFR node `adb19ea0-de7c-4cd5-9445-cbf2108a8a1a` by **up to 18 clean years (FY2003–FY2020)** — extending both `scripts/processFLAcfr.js` (GF spending-by-function, `operating`) and `scripts/processFLRevenueAcfr.js` (GF revenue-by-source, `revenue`) from FY2021–FY2024 to **FY2003–FY2024 (22yr)**. For each year resolve the actual filename from the **per-year map** (no single rule — alternates `cafr{YYYY}.pdf` ↔ `{YYYY}cafr.pdf`), download to gitignored `_acfr-tmp/fl/`, extract the **GENERAL FUND column** (1st numeric token per row) via `pdftotext -table` + `extract_gf.py`, transcribe into `EXPENDITURES` / `REVENUE` as **raw thousands** (UNITS=1000, matching existing FL years), dry-run-tie every year to its printed GENERAL FUND Total, then live-load and verify idempotency + P2 clamp + pre-existing-window-untouched.

Bookends (recon-confirmed exact-$0): **FY2003 Total revenues $19,857,818K**, **FY2020 $40,534,343K**. FY2000–FY2002 are a documented **repair-pending hole** (durable URL, corrupted xref) — optional `qpdf` repair only; default is log-not-attempt.

Output: both FL loaders live-loaded across the tied FY2003–FY2020 window (honest holes logged), `122-02-FL-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md

<interfaces>
<!-- Extend the EXISTING pilot loaders in place — do NOT create new files or clone gen_state.py. -->
- `scripts/processFLAcfr.js` — the current `SOURCES` is built from `[2021,2022,2023,2024]` via a formula (`fye-{YYYY}-…pdf`). Replace/augment it with an EXPLICIT per-year map for FY2003–FY2020 (each entry the recon-confirmed filename under `${FL_BASE}/…`), keeping the FY2021–FY2024 formula entries. Add FY2003–FY2020 keys to the `EXPENDITURES` object (verbatim ACFR function names, RAW THOUSANDS), prepend the tied years to the `main()` `years` array (currently `[2021,2022,2023,2024]`), and add them to the srcPayload `fiscal_years`. `UNITS = 1_000`, `validate()` (±$10M), `buildTree()` are unchanged.
- `scripts/processFLRevenueAcfr.js` — identical shape: explicit per-year `SOURCES` map, `REVENUE` object (raw thousands), extended `years` array + srcPayload `fiscal_years`. `clampForRender` P2 path already wired — negative "Investment earnings (losses)"/"Other" route through it automatically.
- `_acfr-work/extract_gf.py` — position-anchored GENERAL FUND (1st) column extractor. The `-table` layout indents smaller GF numbers but the per-FY sum ties exactly (see existing FL comment).
- Local PDF workdir `_acfr-tmp/fl/` (already gitignored). Flags: `--dry-run`, `--fy YYYY`.
- Node: adb19ea0-de7c-4cd5-9445-cbf2108a8a1a (hardcoded STATE_NODE_ID in both loaders — do NOT change). FY-end Jun 30.
- data_sources dataset_ids (already exist): 'fl-acfr-gf-operating', 'fl-acfr-gf-revenue'.
</interfaces>

<source_facts>
<!-- From 117-DEEPEN-SOURCES.md FL row + per-year filename map (FY2000–FY2020) — USE these, do not re-research. -->
- FL new durable old-end = **FY2003** (clean). Add up to FY2003–FY2020 (18). Window → FY2003–FY2024 (22yr).
- Bookend ties (GENERAL FUND col Total revenues): FY2003 $19,857,818K (7-line sum ties exactly); FY2020 $40,534,343K (7-line sum ties exactly). FY2010 + FY2015 spot-confirmed extractable. Current-end FY2021+ already tied (unchanged).
- Per-year filename map (all under `https://www.myfloridacfo.com/docs-sf/default-source/transparency-docs/cafr/`; confirmed via curl -I Content-Type application/pdf):
  FY2003 cafr2003.pdf · FY2004 cafr2004.pdf · FY2005 cafr2005.pdf · FY2006 cafr2006.pdf · FY2007 cafr2007.pdf · FY2008 cafr2008.pdf · FY2009 cafr2009.pdf · FY2010 cafr2010.pdf · FY2011 cafr2011.pdf · FY2012 cafr2012.pdf · FY2013 2013cafr.pdf · FY2014 2014cafr.pdf · FY2015 2015cafr.pdf · FY2016 2016cafr.pdf · FY2017 2017cafr.pdf · FY2018 cafr2018.pdf · FY2019 cafr2019.pdf · FY2020 2020cafr.pdf.
  (Convention flips at FY2013 and FY2018 — NO single rule; probe both `cafr{YYYY}.pdf` / `{YYYY}cafr.pdf` per year and confirm application/pdf before parsing.)
- FY2000–FY2002 (cafr2000.pdf / cafr2001.pdf / cafr2002.pdf): 200 OK, genuine PDF v1.3, 16.5–23.9MB, but `pdftotext` fails (damaged xref, both -table and plain modes). REPAIR-PENDING hole — pre-GASB-34 layout status unconfirmed pending repair. Optional qpdf repair; else log honestly.
- Units: FL stored raw thousands with UNITS=1000 (same as existing FY2021–FY2024).
- P2 precedent: FY2021 negative "Investment earnings (losses)" −398,287k; FY2022 negatives −1,573,844k + −56,189k. Older years likely carry the same down-market negatives.
</source_facts>
</context>

<tasks>

<task id="122-02-01" type="auto">
  <name>Task 1: Resolve per-year filenames + download + extract + transcribe FL FY2003–FY2020 into both loaders, dry-run-tie every year</name>
  <files>scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js, _acfr-tmp/fl/ (gitignored PDFs + pdftotext), .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-02-FL-LOADLOG.md</files>
  <read_first>
    - scripts/processFLAcfr.js (SOURCES formula + EXPENDITURES object + main() years array + UNITS=1000 + validate/buildTree)
    - scripts/processFLRevenueAcfr.js (SOURCES + REVENUE + clampForRender P2 path + FY2021/FY2022 negative precedent)
    - _acfr-work/extract_gf.py (GENERAL FUND 1st-column extractor — GF value is the 1st numeric token per row)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md (FL row + FY2000–FY2020 per-year filename map + FY2003/FY2020 bookend sums + FY2000–FY2002 repair-pending note)
  </read_first>
  <action>
    Confirm `_acfr-tmp/fl/` is gitignored. For each FY 2003–2020, resolve the filename from the D-04 per-year map (probe both `cafr{YYYY}.pdf` and `{YYYY}cafr.pdf`, accept the one returning Content-Type application/pdf) and download to `_acfr-tmp/fl/fl{YYYY}.pdf`. Soft-404 guard: reject unless `%PDF` + application/pdf + size >> the 3,224-byte HTML soft-404. Run `pdftotext -table` (NOT -layout), locate the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*, run `extract_gf.py` to isolate the GENERAL FUND (1st) column for BOTH revenues and expenditures (the 1st numeric token per row). Transcribe each year into `processFLAcfr.js` `EXPENDITURES[fy]` and `processFLRevenueAcfr.js` `REVENUE[fy]` as **raw thousands** (UNITS applies ×1000), verbatim ACFR names, `total` = printed GENERAL FUND Total expenditures / Total revenues. Replace the formula `SOURCES` with an explicit per-year map (FY2003–FY2020 recon filenames + retained FY2021–FY2024) in BOTH files, prepend the tied years to each `main()` `years` array, and extend the srcPayload `fiscal_years`. **Check every added year for negative "Investment earnings (losses)"/"Other"** and confirm clampForRender routing. Cross-check FY2003 revenue total (raw) = 19,857,818 and FY2020 = 40,534,343. FY2000–FY2002: attempt a single cheap `qpdf --qdf --replace-input` repair; if it still won't extract, OMIT + log as repair-pending (do NOT fake). OMIT + log any FY2003–FY2020 year that 404s or won't tie. Run both loaders `--dry-run`: every tied FY prints "validation: PASS", NO "sum ≠ total". Create `122-02-FL-LOADLOG.md` recording per-year filename-variant-hit + tie diff.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node scripts/processFLAcfr.js --dry-run 2>&1 | grep -Eq 'FY2003 validation: PASS' && node scripts/processFLAcfr.js --dry-run 2>&1 | grep -Eq 'FY2020 validation: PASS' && ! node scripts/processFLRevenueAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-tmp/fl/` holds a real PDF (`%PDF` + application/pdf) for each tied FY2003–FY2020 year; soft-404/HTML rejected.
    - `node scripts/processFLAcfr.js --dry-run` and `processFLRevenueAcfr.js --dry-run` print "validation: PASS" for every tied FY, NO "sum ≠ total"; FY2003 revenue raw total = 19,857,818 and FY2020 = 40,534,343.
    - Both loaders' `SOURCES` are an explicit per-year map (FY2003–FY2020 recon filenames + FY2021–FY2024), `main()` `years` and srcPayload `fiscal_years` include the tied years; UNITS=1000 and STATE_NODE_ID (adb19ea0…) unchanged.
    - Any negative GF line in an added year routes through clampForRender.
    - FY2000–FY2002 disposition recorded (repair attempted-and-failed → repair-pending, or repaired → loaded); `122-02-FL-LOADLOG.md` records each tied FY's filename-variant + tie diff.
  </acceptance_criteria>
  <done>Both FL loaders carry the tied FY2003–FY2020 years (raw thousands, UNITS=1000) with an explicit per-year SOURCES map; both bookends dry-run-tie ($0); P2 clamp confirmed; FY2000–FY2002 dispositioned; no live write yet.</done>
</task>

<task id="122-02-02" type="auto">
  <name>Task 2: Live-load FL FY2003–FY2020 (operating + revenue) onto the existing node, pre-existing window untouched</name>
  <files>(runs scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processFLAcfr.js (--fy path + node lookup + RPC + source-stamp + data_sources upsert)
    - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md (D-08 idempotency + untouched contract)
  </read_first>
  <action>
    Source `.env`/`.env.local` first. Record the pre-load FL node state: count operating + revenue rows on adb19ea0… and capture FY2021 + FY2024 totals + source stamps. For each tied added FY 2003–2020, run operating then revenue loaders live (`--fy YYYY`). Each must pass `validate()`, write via `treasury_sync_budget_tree` keyed to the FL node, stamp per-year `source_url`/`source_date`/`data_source` (GAAP-basis label), and update the existing `fl-acfr-gf-operating`/`fl-acfr-gf-revenue` data_sources rows' `fiscal_years` + `last_synced_at`. Abort + log any year failing the tie (no partial-write). After load, re-capture FY2021 + FY2024 and confirm unchanged.
  </action>
  <verify>
    <automated>echo "FL node verification performed in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the FL node (adb19ea0…) has, per tied FY 2003–2020, an operating row + a revenue row, each GAAP-basis-labelled ("Florida State ACFR — General Fund … (FY{fy} actual, GAAP basis)") with non-null source_url/source_date.
    - FL FY2003 revenue = 19,857,818,000 stored dollars (19,857,818 raw ×1000) and FY2020 = 40,534,343,000.
    - Pre-existing FY2021 + FY2024 operating + revenue rows byte-identical to pre-load.
    - No new data_sources rows — only fl-acfr-gf-operating / fl-acfr-gf-revenue updated (fiscal_years now include the added years).
  </acceptance_criteria>
  <done>Each tied FL FY2003–FY2020 has operating + revenue rows on adb19ea0…, dollars (×1000), GAAP-labelled + sourced; the pre-existing FY2021–FY2024 window provably untouched; no orphan data_sources rows.</done>
</task>

<task id="122-02-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + P2 + window-untouched verification (DB query) + finalize FL LOADLOG</name>
  <files>.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-02-FL-LOADLOG.md</files>
  <read_first>
    - scripts/processFLAcfr.js (RPC key + data_sources upsert-by-dataset_id lifecycle)
    - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md (LOAD-01 + honest-hole contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative added FY live (e.g. FL `--fy 2003` op + rev), confirm 0 net change. Query treasury.budgets + treasury.data_sources to confirm: (a) every tied FL FY2003–FY2020 has op + rev rows on adb19ea0…, GAAP-labelled, non-null sources, dollar scale (×1000); (b) the window is contiguous FY2003–FY2024 minus any logged hole; (c) FY2021 + FY2024 unchanged; (d) exactly two FL data_sources rows (fl-acfr-gf-operating, fl-acfr-gf-revenue) — 0 residue (LOAD-01); (e) FL still ≥1 revenue row (Money In). Append "Load Disposition" to `122-02-FL-LOADLOG.md`: FYs loaded/omitted+reason, per-year filename-variant hit, both bookend ties ($0), per-year P2 clamp results, FY2000–FY2002 repair-pending disposition, idempotency re-run, pre-existing-window-untouched confirmation. Do NOT load below FY2003 unless a FY2000–FY2002 repair succeeded (then load + tie it too); touch no state other than FL.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 122-02-FL-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of FL `--fy 2003` → 0 net change.
    - Every tied FL FY2003–FY2020 has op + rev rows on adb19ea0…, GAAP-labelled, non-null sources, dollar scale.
    - FY2021 + FY2024 unchanged; window FY2003–FY2024 (holes logged with reason).
    - Exactly two FL data_sources rows; 0 residue (LOAD-01).
    - FL ≥1 revenue row (Money In).
    - `122-02-FL-LOADLOG.md` finalized with per-year filename hits, bookend ties, P2 results, FY2000–FY2002 disposition, idempotency.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change; DB query confirms FL extended toward FY2003–FY2024, pre-existing window untouched, 0 residue, Money In on; `122-02-FL-LOADLOG.md` finalized with the FY2000–FY2002 repair-pending disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| myfloridacfo.com PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the FL STATE node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-02-A | Tampering | wrong filename variant guessed → 404/soft-404 parsed as data | mitigate | explicit per-year map; probe both variants; `%PDF` + application/pdf + size > 3,224-byte soft-404 guard; honest hole on failure |
| T-122-02-B | Tampering | corrupted-xref FY2000–FY2002 PDF yields garbled figures | mitigate | pdftotext failure → repair-pending log, NOT a partial transcription; optional qpdf repair must still tie before any write |
| T-122-02-C | Tampering | units mis-scale — entering full dollars on a UNITS=1000 state (1000× inflation) | mitigate | data objects raw-thousands; bookends asserted raw (19,857,818 / 40,534,343); UNITS unchanged; dry-run cross-check |
| T-122-02-D | Tampering | negative investment-earnings line dropped or mis-signed | mitigate | per-year negative check; existing clampForRender keeps signed magnitude, parent nets |
| T-122-02-E | Tampering | wrong statement/column (not the 1st GENERAL FUND numeric token) | mitigate | extract_gf.py isolates 1st column; bookend $0 tie gates before write |
| T-122-02-F | Tampering | overwriting pre-existing FY2021–FY2024 rows | mitigate | Tasks 2–3 capture + re-assert FY2021/FY2024 byte-identical; RPC keyed per-FY only adds new years |
| T-122-02-G | Info Disclosure | data_sources residue / orphan rows | mitigate | upsert by dataset_id (2 rows total); Task 3 asserts 0 residue (LOAD-01) |
| T-122-02-SC | Tampering | npm/pip installs (qpdf, etc.) | accept | qpdf is a standalone CLI if used; no npm/pip packages added |
</threat_model>

<verification>
- Both FL loaders `--dry-run`: every tied FY2003–FY2020 PASS, no "sum ≠ total".
- FY2003 revenue raw total = 19,857,818; FY2020 = 40,534,343.
- Live: FL node op + rev rows per tied FY, GAAP-labelled, non-null sources; FY2021 + FY2024 byte-identical to pre-load.
- Re-run FL `--fy 2003` → 0 net change; exactly two FL data_sources rows (0 residue, LOAD-01).
- Negative GF lines route through clampForRender; FY2000–FY2002 repair-pending disposition logged (not faked).
- FL ≥1 revenue row → Money In stays enabled.
- `122-02-FL-LOADLOG.md` finalized.
</verification>

<success_criteria>
The Florida State-ACFR node (adb19ea0-de7c-4cd5-9445-cbf2108a8a1a) is extended by the tied FY2003–FY2020 years toward a FY2003–FY2024 window (honest holes logged, FY2000–FY2002 dispositioned as repair-pending or repaired-and-loaded), dollars (UNITS=1000), every added FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, both bookends tying at exact $0, negative lines P2-clamped, the pre-existing FY2021–FY2024 window provably untouched, idempotent never-overwrite proven, 0 data_sources residue, Money In still enabled. Hands FL to Phase 124 for independent re-derivation.
</success_criteria>

<output>
Create `.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-02-SUMMARY.md` when done — record the FL FY window added (FY2003–FY2020, holes logged), the per-year filename-variant map actually hit, both bookend ties (raw-thousands $0), per-year P2 clamp results, the FY2000–FY2002 repair-pending (or repaired) disposition, the pre-existing-window-untouched confirmation, and the idempotency re-run + 0-residue result.
</output>
