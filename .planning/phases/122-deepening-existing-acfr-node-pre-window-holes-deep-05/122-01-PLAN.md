---
phase: 122-deepening-existing-acfr-node-pre-window-holes-deep-05
plan: "122-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processCA.js
  - scripts/processCARevenueAcfr.js
  - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-01-CA-LOADLOG.md
autonomous: true
requirements: [DEEP-05]
user_setup: []

must_haves:
  truths:
    - "D-01: the existing California STATE node e1007bf5-bac9-4b1c-878e-f6834885f850 gains FY2002–FY2007 (6 clean years) on BOTH dataset_types — operating (processCA.js) + revenue (processCARevenueAcfr.js) — extending its window to FY2002–FY2025; the pre-existing FY2008–FY2025 rows are untouched (byte-identical totals + source stamps)"
    - "D-01 units: CA stores FULL DOLLARS (no UNITS constant — printed ACFR thousands are ×1,000 by hand in the EXPENDITURES/REVENUE objects, exactly as FY2008–FY2025 already are). Bookend tie targets are the printed GENERAL column figures: FY2002 Total revenues $63,942,875K → stored 63_942_875_000; FY2007 Total revenues $96,309,497K → 96_309_497_000"
    - "D-03 URLs (all under https://www.sco.ca.gov/Files-ARD/CAFR/): FY2006–2007 = cafr{NN}.pdf (NO 'web' suffix — cafr06.pdf/cafr07.pdf); FY2002–2005 = {YYYY}_cafr{NN}.pdf (year-prefixed, e.g. 2002_cafr02.pdf). Each added year's SOURCES entry carries date {YYYY}-06-30 (Jun 30 FYE)"
    - "D-03 basis: NO pre-GASB-34 flag — FY2002 is the GASB-34 first year, already the modern 'Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds', column header 'General', identical shape to FY2008+. pre34Extract.mjs is NOT used"
    - "D-07 tie discipline: every added FY's transcribed GF categories tie to the printed GENERAL-column Total (revenues AND expenditures) — validate() refuses to write on >$10M mismatch; FY2002 + FY2007 confirmed exact-$0 in recon. Extraction = pdftotext -table (NOT -layout) on local PDFs in gitignored _acfr-tmp/ca/, 1st ('General') column via extract_gf.py"
    - "D-08 P2 clamp: any negative GF revenue/expenditure line in FY2002–FY2007 (CA market-loss investment-income years) renders via the existing clampForRender path (slice→0, signed magnitude in label, parent nets) — checked at EVERY added year, not just bookends"
    - "D-08 idempotent never-overwrite: extending the years array + SOURCES + data objects updates the SAME persistent data_sources rows (ca-acfr-gf-operating / ca-acfr-gf-revenue) keyed by dataset_id — no new/orphan data_sources rows (LOAD-01 holds); a second live run of a representative added FY = 0 net change"
    - "D-09 honest holes: CA ≤FY2001 is a documented durable floor (SCO soft-404 at every naming variant — HTTP-200 text/html 11,561 bytes) — NOT attempted, NOT faked; any FY2002–FY2007 year that 404s or won't tie is OMITTED and logged with its reason"
    - "Money In stays enabled on CA (already has revenue rows; adding 6 more years is additive)"
  artifacts:
    - path: "scripts/processCA.js"
      provides: "CA GF operating loader extended to FY2002–FY2025 (SOURCES + EXPENDITURES + main() years array all include 2002–2007)"
      contains: "treasury_sync_budget_tree"
      min_lines: 350
    - path: "scripts/processCARevenueAcfr.js"
      provides: "CA GF revenue loader extended to FY2002–FY2025 (SOURCES + REVENUE + main() years array all include 2002–2007), P2 clamp wired"
      contains: "clampForRender"
      min_lines: 432
    - path: ".planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-01-CA-LOADLOG.md"
      provides: "Per-FY load disposition FY2002–FY2007, bookend ties, per-year P2 clamp results, idempotency re-run result, pre-existing-window-untouched confirmation"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processCA.js / processCARevenueAcfr.js"
      to: "treasury.budgets (CA STATE node e1007bf5-bac9-4b1c-878e-f6834885f850)"
      via: "treasury_sync_budget_tree RPC keyed (data_source_id, fy, dataset_type) + post-RPC source-stamp UPDATE; data_sources upsert by dataset_id"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Deepen the **California** State-ACFR node `e1007bf5-bac9-4b1c-878e-f6834885f850` by **6 clean years (FY2002–FY2007)** — extending both `scripts/processCA.js` (GF spending-by-function, `operating`) and `scripts/processCARevenueAcfr.js` (GF revenue-by-source, `revenue`) from FY2008–FY2025 to **FY2002–FY2025**. Download each year's SCO ACFR PDF into gitignored `_acfr-tmp/ca/`, extract the **GENERAL column** (1st) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, transcribe each year's categories into the `EXPENDITURES` / `REVENUE` objects (**full dollars — printed thousands ×1,000**, matching FY2008+), dry-run-tie every year to its printed GENERAL Total, then live-load and verify idempotency + P2 clamp + pre-existing-window-untouched.

Bookends (recon-confirmed exact-$0): **FY2002 Total revenues $63,942,875K**, **FY2007 $96,309,497K**. **No pre-GASB-34 flag** (FY2002 is the GASB-34 boundary year, modern layout). CA ≤FY2001 is a documented soft-404 floor — do NOT attempt.

Output: both CA loaders live-loaded FY2002–FY2007 (honest holes logged), `122-01-CA-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md

<interfaces>
<!-- Extend the EXISTING pilot loaders in place — do NOT create new files or clone gen_state.py. -->
- `scripts/processCA.js` — add FY2002–FY2007 keys to (a) the `SOURCES` map, (b) the `EXPENDITURES` data object (verbatim ACFR function names, full-dollar totals), and (c) the hardcoded `years` array in `main()` (currently `[2008,…,2025]` → prepend `2002,2003,2004,2005,2006,2007`). Same edits mirror into the `data_sources` `fiscal_years` array in the srcPayload. `validate()` (±$10M) and `buildTree()` are unchanged and already handle new years.
- `scripts/processCARevenueAcfr.js` — identical shape: extend `SOURCES`, the `REVENUE` object, the `years` array, and the srcPayload `fiscal_years`. The P2 `clampForRender` path is already wired — negative investment-income lines route through it automatically.
- `_acfr-work/extract_gf.py` — position-anchored GENERAL-column (1st) extractor; run against the `pdftotext -table` output per FY.
- Local PDF workdir `_acfr-tmp/ca/` (already gitignored). Flags on both loaders: `--dry-run`, `--fy YYYY`.
- Node: e1007bf5-bac9-4b1c-878e-f6834885f850 (hardcoded STATE_NODE_ID in both loaders — do NOT change).
- data_sources dataset_ids (already exist, upserted by dataset_id): 'ca-acfr-gf-operating', 'ca-acfr-gf-revenue'.
</interfaces>

<source_facts>
<!-- From 117-DEEPEN-SOURCES.md CA row + one-line load statement — USE these, do not re-research. -->
- CA new durable old-end = **FY2002** (GASB-34 boundary; modern GENERAL-column layout, no pre-34 flag). Add FY2002–FY2007 (6). Full available public archive = FY2002–FY2025.
- URL patterns: FY2006–2007 `https://www.sco.ca.gov/Files-ARD/CAFR/cafr{NN}.pdf` (no 'web'); FY2002–2005 `https://www.sco.ca.gov/Files-ARD/CAFR/{YYYY}_cafr{NN}.pdf` (e.g. 2002_cafr02.pdf, 2003_cafr03.pdf … 2005_cafr05.pdf).
- Bookend ties (GENERAL col Total revenues): FY2002 $63,942,875K (7-line column sum ties exactly); FY2007 $96,309,497K (13-line sum ties exactly). Current-end FY2025 already tied (unchanged).
- CA ≤FY2001: every naming variant returns the SCO soft-404 (HTTP-200, Content-Type text/html, exactly 11,561 bytes). Durable floor at FY2002. Excluded (D-06 soft-404 ≠ document).
- Units: CA stored full-dollar (thousands ×1,000) — same as the pre-existing FY2008–FY2025 rows.
</source_facts>
</context>

<tasks>

<task id="122-01-01" type="auto">
  <name>Task 1: Download + extract + transcribe CA FY2002–FY2007 into both loaders, dry-run-tie every year</name>
  <files>scripts/processCA.js, scripts/processCARevenueAcfr.js, _acfr-tmp/ca/ (gitignored PDFs + pdftotext), .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-01-CA-LOADLOG.md</files>
  <read_first>
    - scripts/processCA.js (SOURCES map + EXPENDITURES object + main() years array + validate/buildTree — full-dollar, no UNITS)
    - scripts/processCARevenueAcfr.js (SOURCES + REVENUE + clampForRender P2 path)
    - _acfr-work/extract_gf.py (position-anchored GENERAL-column extractor)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-DEEPEN-SOURCES.md (CA row: FY2002/FY2007 bookend sums, URL patterns, soft-404 floor)
  </read_first>
  <action>
    Confirm `_acfr-tmp/ca/` is gitignored. For each FY 2002–2007, resolve the URL per D-03 (FY2006–07 `cafr{NN}.pdf` no-web; FY2002–05 `{YYYY}_cafr{NN}.pdf`) and download to `_acfr-tmp/ca/ca{YYYY}.pdf`. Soft-404 guard: reject unless the response is `%PDF` magic + Content-Type application/pdf + size well above the 11,561-byte SCO HTML soft-404. Run `pdftotext -table` (NOT -layout), locate the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances*, run `extract_gf.py` to isolate the GENERAL (1st) column for BOTH revenues and expenditures. Transcribe each year's categories into `processCA.js` `EXPENDITURES[fy]` and `processCARevenueAcfr.js` `REVENUE[fy]` as **full dollars (printed thousands ×1,000)**, verbatim ACFR names, with `total` = the printed GENERAL Total expenditures / Total revenues (×1,000). Add matching `SOURCES[fy]` entries (url + `{YYYY}-06-30` date) in both files, prepend `2002,2003,2004,2005,2006,2007` to each `main()` `years` array, and add them to the srcPayload `fiscal_years`. **Check every added year's revenue lines for negatives** (market-loss investment income) — confirm they route through `clampForRender`. Cross-check FY2002 revenue total stored = 63,942,875,000 and FY2007 = 96,309,497,000 (NOT ×1000 too many, NOT raw thousands). OMIT + log any year that 404s or won't tie; do NOT attempt ≤FY2001. Run both loaders `--dry-run`: every added FY prints "validation: PASS", NO "sum ≠ total". Create `122-01-CA-LOADLOG.md` and record each year's tie diff.
  </action>
  <verify>
    <automated>cd /c/treasury-tracker && node scripts/processCA.js --dry-run 2>&1 | grep -Eq 'FY2002 validation: PASS' && node scripts/processCA.js --dry-run 2>&1 | grep -Eq 'FY2007 validation: PASS' && ! node scripts/processCARevenueAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processCARevenueAcfr.js --dry-run --fy 2002 2>&1 | grep -q '63,942,875,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-tmp/ca/` holds a real PDF (`%PDF` + application/pdf) for each loadable FY2002–FY2007; soft-404/HTML rejected; ≤FY2001 not attempted.
    - `node scripts/processCA.js --dry-run` and `processCARevenueAcfr.js --dry-run` print "validation: PASS" for every added FY, NO "sum ≠ total".
    - `processCARevenueAcfr.js --dry-run --fy 2002` prints 63,942,875,000 and `--fy 2007` prints 96,309,497,000 (full dollars).
    - Both loaders' `SOURCES`, data objects, `main()` `years` array, and srcPayload `fiscal_years` all include 2002–2007; STATE_NODE_ID unchanged (e1007bf5…); no UNITS constant introduced.
    - Any negative GF line in an added year routes through `clampForRender`.
    - `122-01-CA-LOADLOG.md` records each added FY's tie diff (bookends $0) + any omitted year with reason.
  </acceptance_criteria>
  <done>Both CA loaders carry FY2002–FY2007 (full-dollar) with per-year SOURCES; both bookends dry-run-tie ($0); P2 clamp path confirmed for negatives; no ≤FY2001 attempt; no live write yet.</done>
</task>

<task id="122-01-02" type="auto">
  <name>Task 2: Live-load CA FY2002–FY2007 (operating + revenue) onto the existing node, pre-existing window untouched</name>
  <files>(runs scripts/processCA.js, scripts/processCARevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processCA.js (--fy path + node lookup + RPC + source-stamp + data_sources upsert)
    - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md (D-08 idempotency + untouched contract)
  </read_first>
  <action>
    Source `.env`/`.env.local` first. Record the pre-load CA node state: count operating + revenue rows on e1007bf5… and capture FY2008 + FY2025 totals + source stamps (to prove they stay byte-identical). For each added FY 2002–2007, run operating then revenue loaders live (`--fy YYYY`). Each must pass `validate()`, write via `treasury_sync_budget_tree` keyed to the CA node, stamp the per-year `source_url`/`source_date`/`data_source` (GAAP-basis label), and update the (already-existing) `ca-acfr-gf-operating` / `ca-acfr-gf-revenue` data_sources rows' `fiscal_years` + `last_synced_at`. Abort + log any year failing the tie (do NOT partial-write). After load, re-capture the FY2008 + FY2025 rows and confirm they are unchanged.
  </action>
  <verify>
    <automated>echo "CA node verification performed in Task 3 via DB query"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the CA node (e1007bf5…) has, per added FY 2002–2007, an operating row + a revenue row, each GAAP-basis-labelled ("California State ACFR — General Fund … (FY{fy} actual, GAAP basis)") with non-null source_url/source_date.
    - CA FY2002 revenue = 63,942,875,000 and FY2007 = 96,309,497,000 (full dollars).
    - Pre-existing FY2008 + FY2025 operating + revenue rows are byte-identical to their pre-load totals + source stamps (window extended, not rewritten).
    - No new data_sources rows created — only ca-acfr-gf-operating / ca-acfr-gf-revenue updated (fiscal_years now include 2002–2007).
  </acceptance_criteria>
  <done>Each loadable CA FY2002–FY2007 has operating + revenue rows on e1007bf5…, full-dollar, GAAP-labelled + sourced; the pre-existing FY2008–FY2025 window is provably untouched; no orphan data_sources rows.</done>
</task>

<task id="122-01-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + P2 + window-untouched verification (DB query) + finalize CA LOADLOG</name>
  <files>.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-01-CA-LOADLOG.md</files>
  <read_first>
    - scripts/processCA.js (RPC key + data_sources upsert-by-dataset_id lifecycle)
    - .planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-CONTEXT.md (LOAD-01 + honest-hole contract)
  </read_first>
  <action>
    Idempotency: re-run ONE representative added FY live (e.g. CA `--fy 2002` op + rev), confirm 0 net change. Query treasury.budgets + treasury.data_sources to confirm: (a) every loadable CA FY2002–FY2007 has op + rev rows on e1007bf5…, GAAP-labelled, non-null sources, full-dollar scale; (b) the window is now contiguous FY2002–FY2025 (or documents any interior hole with reason); (c) FY2008 + FY2025 unchanged; (d) exactly two CA data_sources rows (ca-acfr-gf-operating, ca-acfr-gf-revenue) — 0 residue (LOAD-01); (e) CA still ≥1 revenue row (Money In stays on). Append "Load Disposition" to `122-01-CA-LOADLOG.md`: FYs loaded/omitted+reason, both bookend ties ($0), per-year P2 clamp results, idempotency re-run result, pre-existing-window-untouched confirmation, and the CA ≤FY2001 honest-floor note. Do NOT load beyond FY2002 (no ≤FY2001); touch no state other than CA.
  </action>
  <verify>
    <automated>echo "DB verification; Load Disposition appended to 122-01-CA-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of CA `--fy 2002` → 0 net change.
    - Every loadable CA FY2002–FY2007 has op + rev rows on e1007bf5…, GAAP-labelled, non-null sources, full-dollar.
    - FY2008 + FY2025 rows unchanged; window is FY2002–FY2025 (interior holes, if any, logged with reason).
    - Exactly two CA data_sources rows; 0 residue (LOAD-01).
    - CA ≥1 revenue row (Money In).
    - `122-01-CA-LOADLOG.md` finalized with bookend ties, P2 results, idempotency, and the ≤FY2001 honest-floor note.
  </acceptance_criteria>
  <done>Representative-FY re-run = 0 net change; DB query confirms CA extended to FY2002–FY2025, pre-existing window untouched, 0 residue, Money In on; `122-01-CA-LOADLOG.md` finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sco.ca.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the CA STATE node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01-A | Tampering | soft-404 HTML parsed as a PDF (CA ≤FY2001 or a missing year) → garbage figures | mitigate | `%PDF` + Content-Type application/pdf + size > 11,561-byte soft-404 guard; honest hole on failure; ≤FY2001 not attempted |
| T-122-01-B | Tampering | units mis-scale — entering raw thousands (or ×1000 twice) on a full-dollar state | mitigate | bookends asserted full-dollar (63,942,875,000 / 96,309,497,000); no UNITS constant introduced; dry-run cross-check |
| T-122-01-C | Tampering | negative investment-income line dropped or mis-signed (market-loss years) | mitigate | per-year negative check; existing clampForRender keeps signed magnitude, parent nets |
| T-122-01-D | Tampering | wrong statement/column (Statement of Activities, budgetary schedule, or non-General column) | mitigate | extract_gf.py isolates 1st (General) column; bookend $0 tie gates before write |
| T-122-01-E | Tampering | overwriting the pre-existing FY2008–FY2025 rows | mitigate | Tasks 2–3 capture + re-assert FY2008/FY2025 byte-identical; RPC keyed per-FY only adds new years |
| T-122-01-F | Info Disclosure | data_sources residue / orphan rows | mitigate | upsert by dataset_id (2 rows total); Task 3 asserts 0 residue (LOAD-01) |
| T-122-01-SC | Tampering | npm/pip installs | accept | no new packages (poppler + node already present) |
</threat_model>

<verification>
- Both CA loaders `--dry-run`: every added FY2002–FY2007 PASS, no "sum ≠ total".
- `processCARevenueAcfr.js --dry-run --fy 2002` → 63,942,875,000; `--fy 2007` → 96,309,497,000 (full dollars).
- Live: CA node op + rev rows per added FY, GAAP-labelled, non-null sources; FY2008 + FY2025 byte-identical to pre-load.
- Re-run CA `--fy 2002` → 0 net change; exactly two CA data_sources rows (0 residue, LOAD-01).
- Any negative GF line routes through clampForRender; CA ≤FY2001 honest floor documented, not attempted.
- CA ≥1 revenue row → Money In stays enabled.
- `122-01-CA-LOADLOG.md` finalized.
</verification>

<success_criteria>
The California State-ACFR node (e1007bf5-bac9-4b1c-878e-f6834885f850) is extended by FY2002–FY2007 to a contiguous FY2002–FY2025 window (honest interior holes, if any, logged), full-dollar, every added FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, both bookends tying at exact $0, no pre-GASB-34 flag, negative lines P2-clamped, the pre-existing FY2008–FY2025 window provably untouched, idempotent never-overwrite proven, 0 data_sources residue, Money In still enabled, and CA ≤FY2001 documented as an honest soft-404 floor. Hands CA to Phase 124 for independent re-derivation.
</success_criteria>

<output>
Create `.planning/phases/122-deepening-existing-acfr-node-pre-window-holes-deep-05/122-01-SUMMARY.md` when done — record the CA FY window added (FY2002–FY2007), any honest interior holes + the ≤FY2001 floor, both bookend ties (full-dollar $0), per-year P2 clamp results, the pre-existing-window-untouched confirmation, the idempotency re-run + 0-residue result, and note the stale-window premise correction (D-02) as a resolved deviation.
</output>
