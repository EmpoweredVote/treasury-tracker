---
phase: 108-acfr-upgrade-batch-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processNCAcfr.js
  - scripts/processNCRevenueAcfr.js
  - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-03-NC-LOADLOG.md
autonomous: true
requirements: [ACFR-11, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "North Carolina state node (dd5281e8-6988-4f42-b83c-4fed43c7ada4) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full clean window FY2012–FY2025, every loaded FY tying to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NC units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to 75,416,082K and FY2020 to 44,930,429K — extracted from the GENERAL FUND column ONLY (1st of General Fund | Highway Fund | Other | Total), NOT a multi-column sum"
    - "The pre-existing NASBO operating rows (NC FY2023, FY2024) are REPLACED in place by the ACFR operating rows (same (dd5281e8, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; the 9 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (NC ACFR GF ~2.58× NASBO — the largest in Batch 1) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (ACFR-20); NC investment earnings positive in confirmed years, older years checked at load"
    - "Money In auto-enables on NC once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNCAcfr.js"
      provides: "NC GF spending-by-function loader (operating), thousands×1000, tie-verified FY2012–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNCRevenueAcfr.js"
      provides: "NC GF revenue-by-source loader (revenue), thousands×1000, tie-verified FY2012–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-03-NC-LOADLOG.md"
      provides: "Per-FY load disposition, enumerated-URL record, NASBO-replacement, accept-relabel divergence, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNCAcfr.js / processNCRevenueAcfr.js"
      to: "treasury.budgets (NC state node dd5281e8)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map (14 enumerated per-year URLs)"
      to: "ncosc.gov archive PDFs"
      via: "explicit per-year archive URLs (no derivable pattern)"
      pattern: "ncosc.gov/sites/default/files"
---

<objective>
Upgrade the **North Carolina** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processNCAcfr.js` (`operating`) and `scripts/processNCRevenueAcfr.js` (`revenue`) — downloading the NC ACFR PDFs across the full clean window **FY2012–FY2025** (14 years) into gitignored `_acfr-work/nc/`, extracting the **GENERAL FUND column** (1st of 4+: General Fund | Highway Fund | Other | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing verbatim line items, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**CRITICAL — extract the GENERAL FUND column ONLY.** Recon caught an initial multi-column sum error here: NC's statement has 4+ fund columns and summing across them overshoots. Take the FIRST numeric token (General Fund) per row; the bookend ties (75,416,082K / 44,930,429K) are the guard. **No derivable URL pattern** — all 14 per-year URLs must be enumerated from the ncosc.gov archive page (NOT the per-year marketing pages, which redirect to the current year).

Output: both NC loaders live-loaded across the extractable FY2012–FY2025 window (holes logged), NASBO replaced in place, Money In auto-enabled; `108-03-NC-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- NC loaders mirror the IL loaders file-for-file. -->
From scripts/processILAcfr.js (spend-by-function analog → processNCAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='North Carolina', state='NC', entity_type='state' (.single()). Node id = dd5281e8-6988-4f42-b83c-4fed43c7ada4 (assert match).
  - UNITS = 1_000 (thousands → ×1,000). Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands.
  - SOURCES = { [fy]: { url, date } } with ALL 14 per-year URLs explicitly enumerated (no derived pattern); dataSource(fy)="North Carolina State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES/REVENUE maps + validate() exit-2 + buildTree ×UNITS + root {n:'North Carolina General Fund Budget'} + RPC + source-stamp, as in IL.
  - data_sources.dataset_id='nc-acfr-gf-operating' / 'nc-acfr-gf-revenue'; names 'North Carolina General Fund Operating Budget' / 'North Carolina General Fund Revenue'; base_url=ncosc.gov archive landing.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue analog → processNCRevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender + "(net loss — shown at 0)" + signed-net root (ACFR-20).
POPULATION = 10_439_388 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): NC has 2 NASBO operating rows only — FY2023 $26,775M, FY2024 $29,216M, data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$75.4B FY2025) is ~2.58× (largest Batch-1 divergence — large federal Medicaid/education grant flows inside GAAP GF), accept-and-relabel per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH1-SOURCES.md NC Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Rev/Exp/Changes. NOT Statement of Activities, NOT budgetary comparison.
- GF column = 1st of (General Fund | Highway Fund | Other | Total). Take the FIRST numeric token — do NOT sum across fund columns (the recon-caught multi-column error).
- Units = thousands. FY-end June 30. Clean window = FY2012–FY2025 (14 yrs).
- No derivable URL pattern — enumerate from https://www.ncosc.gov/annual-report-and-popular-report-archives. Confirmed sample URLs:
  - FY2025: https://www.ncosc.gov/sites/default/files/2025-11/ncacfr2025.pdf
  - FY2024: https://www.ncosc.gov/sites/default/files/2025-01/ncacfr2024.pdf
  - FY2023: https://www.ncosc.gov/sites/default/files/2023-12/ncacfr2023.pdf
  - FY2020: https://www.ncosc.gov/sites/default/files/2021-01/ncacfr2020.pdf
  - FY2012–FY2019 + FY2021–FY2022: enumerate the remaining per-year URLs from the archive page at load.
- Do NOT use per-year marketing pages (`/public-information/{YYYY}-annual-comprehensive-financial-report`) — they redirect to the current year.
- Bookend ties (GENERAL FUND Total revenues, thousands): NC FY2025 = 75,416,082K (diff $0); FY2020 = 44,930,429K (diff $0).
- Investment earnings positive in FY2025; check older years at load (P2 clamp safety net).
- Scope vs NASBO ~2.58× — accept-and-relabel honestly (ACFR-19).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both NC loaders (IL template) + enumerate 14 archive URLs + download/extract/transcribe FY2012–FY2025 (GF column only) + dry-run tie</name>
  <files>scripts/processNCAcfr.js, scripts/processNCRevenueAcfr.js, _acfr-work/nc/</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template)
    - scripts/processILRevenueAcfr.js (the exact revenue template incl. clampForRender)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (NC Detail Block: enumerated archive URLs, GF-column-only caution, bookend ties)
  </read_first>
  <action>
    Fetch the ncosc.gov archive page and enumerate the per-year ACFR PDF URLs FY2012–FY2025 (use the confirmed samples for FY2025/2024/2023/2020; resolve the rest from the archive — NOT the redirecting marketing pages). Download each to `_acfr-work/nc/NC{YYYY}.pdf` with a soft-404 guard (%PDF magic + size >~500KB; reject HTML). Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, and isolate the GENERAL FUND column — the FIRST numeric token of (General Fund | Highway Fund | Other | Total) per row — for BOTH revenues and expenditures. DO NOT sum across fund columns. Copy processILAcfr.js → scripts/processNCAcfr.js and processILRevenueAcfr.js → scripts/processNCRevenueAcfr.js; set STATE_NAME='North Carolina', STATE_ABBR='NC', POPULATION=10_439_388, UNITS=1_000. Build SOURCES with all 14 enumerated per-year URLs (source_date=`${fy}-06-30`). Transcribe the GENERAL FUND line items (verbatim names, thousands) into EXPENDITURES + REVENUE, each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('nc-acfr-gf-operating' / 'nc-acfr-gf-revenue'). Keep clampForRender in the revenue loader. Add a header comment recording the accept-and-relabel scope note (NC ~2.58× NASBO — largest Batch-1 divergence, federal Medicaid/education grants inside GAAP GF, ACFR-19) and the GF-column-only caution. Cross-check FY2025 Total revenues = 75,416,082K and FY2020 = 44,930,429K before transcribing the rest — if either overshoots, you likely summed multiple columns; re-extract GF only. Any FY that fails clean extraction or its exact tie → OMIT + note (honest hole). Run both loaders `--dry-run`; confirm every present FY PASS.
  </action>
  <verify>
    <automated>node scripts/processNCAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNCAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNCRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '75,416,082,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nc/` holds a real PDF (%PDF, >~500KB) per extractable FY; soft-404 HTML rejected.
    - `node scripts/processNCAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO "sum ≠ total"; `processNCRevenueAcfr.js --dry-run --fy 2025` → 75,416,082,000; `--fy 2020` → 44,930,429,000.
    - SOURCES contains ≥ 10 explicitly enumerated ncosc.gov URLs (no derived template); each contains `ncosc.gov/sites/default/files`.
    - Header comment records the GF-column-only caution and the ~2.58× accept-relabel note (ACFR-19); UNITS=1_000.
  </acceptance_criteria>
  <done>Both NC loaders exist on the IL template with all 14 archive URLs enumerated, every transcribed FY dry-run-ties to its printed GENERAL FUND total (GF column only) with the FY2025 75,416,082K / FY2020 44,930,429K bookends, the clamp path + GF-only + accept-relabel notes recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load NC (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNCAcfr.js, scripts/processNCRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNCAcfr.js + scripts/processNCRevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; NC node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the NC node (dd5281e8) and RECORD the pre-load NASBO operating rows (FY2023 $26,775M, FY2024 $29,216M) — never-overwrite baseline + accept-relabel comparator. Then for each NC FY present after Task 1, run `node scripts/processNCAcfr.js --fy <YYYY>` then `node scripts/processNCRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (dd5281e8, fy, 'operating') for FY2023/FY2024; all other FYs operating + all revenue net-new. Each stamps source_url/source_date/data_source (GAAP label). Abort+note any FY failing the live tie; never force-write. Record loaded NC GF totals against pre-load NASBO totals to confirm the ~2.58× accept-relabel divergence (federal inside GAAP GF — ACFR-19).
  </action>
  <verify>
    <automated>echo "NC row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NC node has, per loaded FY, an operating row "North Carolina State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - NC FY2025 revenue row total = 75,416,082,000; FY2020 = 44,930,429,000.
    - NC FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (NC, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~2.58× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>Each transcribed NC FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO rows replaced in place (no duplicates); the accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-03-NC-LOADLOG.md</files>
  <read_first>
    - scripts/processNCAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NC --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded NC FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) NC has NO remaining "NASBO" operating rows (one operating row per (NC, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — NC ≥1 revenue row; (e) cohort untouched (RECON-08): 9 existing ACFR nodes + a sample NASBO state unchanged. Append a "Load Disposition" section to 108-03-NC-LOADLOG.md: FYs loaded (of the 14-yr window), FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than NC.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 108-03-NC-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of NC --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded NC FY has both operating + revenue rows, GAAP-labelled, non-null sources; NC has ZERO remaining "NASBO" operating rows; one operating row per (NC, fy).
    - NC ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 108-03-NC-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms NC fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, Money In enabled), honest holes absent, cohort unchanged; 108-03-NC-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ncosc.gov PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the NC node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03-A | Tampering | **multi-column sum — summing General Fund + Highway + Other overshoots the GF total** (the recon-caught NC error) | mitigate | Task 1 takes ONLY the 1st numeric token (General Fund); bookend ties (75,416,082K / 44,930,429K) gate before transcription; validate() exit-2 |
| T-108-03-B | Tampering | wrong statement (Statement of Activities / budgetary) | mitigate | Task 1 targets the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate |
| T-108-03-C | Tampering | wrong/redirecting URL (marketing page → current-year content) | mitigate | enumerate archive URLs only; %PDF magic + size guard; per-FY bookend/tie sanity |
| T-108-03-D | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-108-03-E | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (NC, fy), zero NASBO |
| T-108-03-F | Info Disclosure | silently inflating NC (~2.58×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-108-03-G | Tampering | write to a state other than NC | mitigate | loader resolves only NC node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-108-03-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both NC loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; GF column only; UNITS=1_000.
- `processNCRevenueAcfr.js --dry-run --fy 2025` → 75,416,082,000; `--fy 2020` → 44,930,429,000.
- Live: NC node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run NC --fy 2025 → "Loaded 0 rows" (idempotent).
- NC ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~2.58×) recorded (ACFR-19).
- 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 108-03-NC-LOADLOG.md finalized.
</verification>

<success_criteria>
The North Carolina state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across the extractable FY2012–FY2025 window (honest holes logged), GF column only, thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place (RECON-08), the ~2.58× accept-relabel divergence recorded (ACFR-19), the P2 clamp wired (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands NC to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-03-SUMMARY.md` when done — record the NC FY window actually transcribed (of the 14-yr window), FYs omitted as honest holes, the two bookend tie confirmations, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
