---
phase: 40-ma-county-seeding-city-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedMACountyLinks.js
autonomous: true
requirements:
  - COUNTY-01
  - COUNTY-02
  - COUNTY-03
  - UI-01
  - UI-02

must_haves:
  truths:
    - "5 MA county rows exist in treasury.municipalities with entity_type='county', state='MA', and 2024 Census population > 0"
    - "97 MA cities have county_id pointing to one of the 5 county rows (per-county: Barnstable=15, Bristol=20, Dukes=7, Norfolk=28, Plymouth=27)"
    - "Cities in dissolved counties (the remaining 254) have county_id=NULL — no unintended links"
    - "County pages auto-show CitiesInCountyPanel and per-capita (no frontend changes needed — data-driven from Phase 25)"
    - "City pages for linked cities auto-show county breadcrumb chip (no frontend changes needed)"
  artifacts:
    - path: "scripts/seedMACountyLinks.js"
      provides: "Idempotent seeder: INSERT 5 county rows + UPDATE county_id for 97 MA cities"
      exports: []
  key_links:
    - from: "scripts/seedMACountyLinks.js Step 1"
      to: "treasury.municipalities"
      via: "supabase.schema('treasury').from('municipalities').insert(COUNTY_ROWS)"
      pattern: "entity_type.*county.*state.*MA"
    - from: "scripts/seedMACountyLinks.js Step 2"
      to: "treasury.municipalities city rows"
      via: "supabase.update({ county_id }).eq('state','MA').in('name', cityList)"
      pattern: "county_id.*state.*MA"
---

<objective>
Create scripts/seedMACountyLinks.js — an idempotent seeder that:
1. INSERTs 5 MA county municipality rows (Barnstable County, Bristol County, Dukes County, Norfolk County, Plymouth County) with 2024 Census populations
2. UPDATEs county_id for all 97 MA cities in those counties
3. Verifies the result via DB queries before exiting

Purpose: Once county rows exist and city county_ids are set, the existing county breadcrumb chip (Phase 25) and CitiesInCountyPanel (Phase 25) activate automatically for MA — zero frontend changes needed. Per-capita activates automatically on county pages when population > 0. This unblocks Phase 41 (county budget load) and Phase 42 (enrichment + final verification).

Output: scripts/seedMACountyLinks.js (new file)
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-ma-county-seeding-city-linking/40-RESEARCH.md

<interfaces>
<!-- Key contracts extracted from the canonical reference implementation. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From scripts/seedLACountyLinks.js (canonical county seeding pattern):
- Env loading: SUPABASE_URL from process.env; SUPABASE_KEY from SUPABASE_SERVICE_KEY || SUPABASE_SERVICE_ROLE_KEY; hard exit if missing
- NOTE: seedLACountyLinks.js does NOT have a loadEnv() call — it expects env vars to be set in the shell. seedMACountyLinks.js MUST add the loadEnv() pattern from loadMAPopulation.js (lines 14-25) so it can read .env/.env.local automatically like all other Phase 38/39 scripts.
- Client: createClient(SUPABASE_URL, SUPABASE_KEY) — no { db: { schema } } option in seedLACountyLinks.js; uses .schema('treasury') prefix on each query instead
- parseArgs: options: { 'dry-run': { type: 'boolean' } }, strict: false
- Step 1 idempotency: fetch all existing state='MA' rows → build Set of lowercase names → filter COUNTY_ROWS for missing → insert only missing
- countyIdMap: built from both already-existing rows (matched from fetch) and newly-inserted rows
- Step 2 UPDATE pattern: .schema('treasury').from('municipalities').update({ county_id: <id> }).eq('state', 'MA').in('name', <cityArray>).select('id, name')
- After each UPDATE: log count, WARN if count != expected
- Step 3 (DB verification): must be a live SELECT, not just a summary of what was done

From scripts/loadMAPopulation.js:
- loadEnv() function (lines 14-25): reads .env.local then .env from project root; skips if key already set; safe to copy verbatim
- SUPABASE_URL fallback: process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co'
- createClient with { db: { schema: 'treasury' } } (alternative pattern — either works)

Phase 25 county_id column: UUID REFERENCES treasury.municipalities(id) — FK is live and enforced; county rows MUST be inserted before city UPDATEs run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/seedMACountyLinks.js</name>
  <files>scripts/seedMACountyLinks.js</files>
  <read_first>
    - scripts/seedLACountyLinks.js — copy structure, env setup, Step 1 idempotency pattern, Step 2 UPDATE pattern, dry-run handling exactly
    - scripts/loadMAPopulation.js lines 14-25 — copy loadEnv() function verbatim (seedLACountyLinks.js lacks this; seedMACountyLinks.js needs it)
    - .planning/phases/40-ma-county-seeding-city-linking/40-RESEARCH.md — COUNTY_ROWS values, complete city lists for all 5 counties, Pitfalls 1-6, DB verification queries
  </read_first>
  <action>
    Create scripts/seedMACountyLinks.js as a new file. It must be a copy of seedLACountyLinks.js's structure with MA-specific data substituted in. Exact requirements:

    HEADER: JSDoc comment explaining the three operations: INSERT 5 MA county rows, UPDATE county_id for 97 MA cities, DB verify. Mention Nantucket exclusion (consolidated town-county). Usage block with --dry-run example.

    ENV LOADING: Copy loadEnv() from loadMAPopulation.js lines 14-25 verbatim and call it before reading process.env. Then: SUPABASE_URL = process.env.SUPABASE_URL || 'https://kxsdzaojfaibhuzmclfq.supabase.co'; SUPABASE_KEY = process.env.SUPABASE_SERVICE_KEY || process.env.SUPABASE_SERVICE_ROLE_KEY; exit(1) if SUPABASE_KEY missing.

    CLIENT: const supabase = createClient(SUPABASE_URL, SUPABASE_KEY); (same as seedLACountyLinks.js — no db.schema option; use .schema('treasury') on each query)

    CONSTANTS — COUNTY_ROWS (5 rows, all required fields):
    - { name: 'Barnstable County', state: 'MA', entity_type: 'county', population: 232570, population_year: 2024 }
    - { name: 'Bristol County',    state: 'MA', entity_type: 'county', population: 588593, population_year: 2024 }
    - { name: 'Dukes County',      state: 'MA', entity_type: 'county', population: 21061,  population_year: 2024 }
    - { name: 'Norfolk County',    state: 'MA', entity_type: 'county', population: 740754, population_year: 2024 }
    - { name: 'Plymouth County',   state: 'MA', entity_type: 'county', population: 542090, population_year: 2024 }

    CONSTANTS — CITY LISTS (use DLS DB names, not Census Gazetteer display names):
    BARNSTABLE_CITIES (15): 'Barnstable', 'Bourne', 'Brewster', 'Chatham', 'Dennis', 'Eastham', 'Falmouth', 'Harwich', 'Mashpee', 'Orleans', 'Provincetown', 'Sandwich', 'Truro', 'Wellfleet', 'Yarmouth'
    BRISTOL_CITIES (20): 'Acushnet', 'Attleboro', 'Berkley', 'Dartmouth', 'Dighton', 'Easton', 'Fairhaven', 'Fall River', 'Freetown', 'Mansfield', 'New Bedford', 'North Attleborough', 'Norton', 'Raynham', 'Rehoboth', 'Seekonk', 'Somerset', 'Swansea', 'Taunton', 'Westport'
    DUKES_CITIES (7): 'Aquinnah', 'Chilmark', 'Edgartown', 'Gosnold', 'Oak Bluffs', 'Tisbury', 'West Tisbury'
    NORFOLK_CITIES (28): 'Avon', 'Bellingham', 'Braintree', 'Brookline', 'Canton', 'Cohasset', 'Dedham', 'Dover', 'Foxborough', 'Franklin', 'Holbrook', 'Medfield', 'Medway', 'Millis', 'Milton', 'Needham', 'Norfolk', 'Norwood', 'Plainville', 'Quincy', 'Randolph', 'Sharon', 'Stoughton', 'Walpole', 'Wellesley', 'Westwood', 'Weymouth', 'Wrentham'
    PLYMOUTH_CITIES (27): 'Abington', 'Bridgewater', 'Brockton', 'Carver', 'Duxbury', 'East Bridgewater', 'Halifax', 'Hanover', 'Hanson', 'Hingham', 'Hull', 'Kingston', 'Lakeville', 'Marion', 'Marshfield', 'Mattapoisett', 'Middleborough', 'Norwell', 'Pembroke', 'Plymouth', 'Plympton', 'Rochester', 'Rockland', 'Scituate', 'Wareham', 'West Bridgewater', 'Whitman'

    STEP 1 — INSERT county rows (idempotent):
    - Fetch all existing municipalities where state='MA' via .schema('treasury').from('municipalities').select('id, name').eq('state', 'MA')
    - Build existingNames Set (lowercase), filter COUNTY_ROWS to find missingCounties
    - Log already-present vs will-insert counts
    - Build countyIdMap from already-existing rows (name → id via find())
    - If missingCounties.length > 0 and not dryRun: insert, map name → id into countyIdMap
    - If dryRun: log would-insert, set countyIdMap entries to placeholder strings
    - Destructure: barnstableId, bristolId, dukesId, norfolkId, plymouthId from countyIdMap

    STEP 2 — UPDATE county_id for each county's city list (5 sequential UPDATEs):
    For each of 5 counties: .schema('treasury').from('municipalities').update({ county_id: <id> }).eq('state', 'MA').in('name', <CITY_LIST>).select('id, name')
    After each UPDATE: log actual count, WARN if count != expected (15/20/7/28/27). For Dukes specifically: warn "Gosnold may not be in DB — Dukes count 6 is acceptable" if count is 6. Exit(1) on supabase error.
    In dry-run mode: log would-update with city count, skip actual update.

    STEP 3 — DB verification (runs after live writes, skipped in dry-run):
    Run two SELECT queries via mcp__supabase-local__execute_sql or supabase client:
    Query A: SELECT COUNT(*) as total FROM treasury.municipalities WHERE state='MA' AND county_id IS NOT NULL
      → log count, warn if != 97 (acceptable: 96 if Gosnold missing)
    Query B: SELECT m2.name AS county, COUNT(m1.id) AS city_count FROM treasury.municipalities m1 JOIN treasury.municipalities m2 ON m1.county_id = m2.id WHERE m1.state='MA' AND m1.entity_type='city' GROUP BY m2.name ORDER BY m2.name
      → log per-county breakdown; warn if any county count != expected
    Query C: SELECT name, population, population_year FROM treasury.municipalities WHERE state='MA' AND entity_type='county' ORDER BY name
      → log all 5 rows to confirm population values match Census 2024

    NOTE: Run verification queries using the supabase JS client (same createClient instance), not mcp__supabase-local — the script is a CLI tool, not an MCP context.

    The script must end with a clear DONE/summary line showing total cities linked.
  </action>
  <verify>
    <automated>node scripts/seedMACountyLinks.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seedMACountyLinks.js exists
    - Dry-run exits 0 and prints all 5 county rows it would insert (Barnstable County, Bristol County, Dukes County, Norfolk County, Plymouth County)
    - Dry-run output includes per-county city counts: 15, 20, 7, 28, 27
    - No fenced code blocks, no syntax errors (node --check scripts/seedMACountyLinks.js exits 0)
    - loadEnv() is present and called before any process.env reads
    - SUPABASE_URL fallback 'https://kxsdzaojfaibhuzmclfq.supabase.co' is present
    - All 5 COUNTY_ROWS entries have population > 0 and population_year: 2024
    - NORFOLK_CITIES contains exactly 28 names
    - PLYMOUTH_CITIES contains exactly 27 names
    - State filter .eq('state', 'MA') is present on every UPDATE query (prevents cross-state contamination — Pitfall 5)
    - Dukes County UPDATE has a Gosnold-specific warning for count=6
  </acceptance_criteria>
  <done>Script passes --dry-run with no errors and shows correct county names and city counts</done>
</task>

<task type="auto">
  <name>Task 2: Live run — INSERT county rows + UPDATE city links + DB verify</name>
  <files>scripts/seedMACountyLinks.js</files>
  <read_first>
    - scripts/seedMACountyLinks.js — the file just created in Task 1; confirm it looks correct before running live
    - .env (root) — confirm SUPABASE_SERVICE_KEY or SUPABASE_SERVICE_ROLE_KEY is present
  </read_first>
  <action>
    Execute the seeder live (no --dry-run flag). The script handles the full workflow internally:
    - Step 1 inserts 5 county rows into treasury.municipalities
    - Step 2 updates county_id for 97 MA cities (5 sequential UPDATEs)
    - Step 3 runs DB verification queries and logs counts

    Run command: node scripts/seedMACountyLinks.js

    After the script completes, run these verification queries via mcp__supabase-local__execute_sql to independently confirm results:

    Query 1 (COUNTY-01): SELECT name, population, population_year FROM treasury.municipalities WHERE state='MA' AND entity_type='county' ORDER BY name;
    Expected: 5 rows with populations 232570, 588593, 21061, 740754, 542090 and population_year=2024

    Query 2 (COUNTY-02 overall): SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND county_id IS NOT NULL;
    Expected: 97 (or 96 if Gosnold not in DB — both are acceptable)

    Query 3 (COUNTY-02 per-county): SELECT m2.name AS county, COUNT(m1.id) AS city_count FROM treasury.municipalities m1 JOIN treasury.municipalities m2 ON m1.county_id = m2.id WHERE m1.state='MA' AND m1.entity_type='city' GROUP BY m2.name ORDER BY m2.name;
    Expected: Barnstable County=15, Bristol County=20, Dukes County=6 or 7, Norfolk County=28, Plymouth County=27

    Query 4 (no cross-state bleed): SELECT state, COUNT(*) FROM treasury.municipalities WHERE county_id IN (SELECT id FROM treasury.municipalities WHERE state='MA' AND entity_type='county') GROUP BY state;
    Expected: only state='MA' row — no other states

    Query 5 (dissolved county cities still NULL): SELECT COUNT(*) FROM treasury.municipalities WHERE state='MA' AND entity_type='city' AND county_id IS NULL;
    Expected: approximately 254 (351 - 97 or 351 - 96)

    If any verification query shows unexpected results, diagnose from the script output logs before marking complete.
  </action>
  <verify>
    <automated>node scripts/seedMACountyLinks.js --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 (or 0 with Gosnold warning only)
    - Query 1 returns exactly 5 rows; all populations match Census 2024 values (232570, 588593, 21061, 740754, 542090); all population_year=2024
    - Query 2 returns 97 (or 96 if Gosnold absent — explicitly acceptable per RESEARCH.md)
    - Query 3 shows correct per-county breakdown: Barnstable=15, Bristol=20, Dukes=6 or 7, Norfolk=28, Plymouth=27
    - Query 4 shows only state='MA' — no cross-state county_id contamination
    - Query 5 shows ~254 MA cities still have county_id=NULL (dissolved counties untouched)
    - Idempotency: running node scripts/seedMACountyLinks.js a second time exits 0 with "Nothing to insert" and per-county UPDATE warnings showing counts match expected (already set — UPDATE is idempotent on same values)
  </acceptance_criteria>
  <done>All 5 DB verification queries pass with expected counts; second run is idempotent</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human spot-check — county breadcrumb + CitiesInCountyPanel + per-capita in live app</name>
  <what-built>5 MA county rows inserted with 2024 Census population; 97 MA cities linked via county_id FK. The Phase 25 county breadcrumb and CitiesInCountyPanel components are data-driven and activate automatically when county_id is non-null.</what-built>
  <how-to-verify>
    Open treasurytracker.empowered.vote and verify all of the following:

    1. COUNTY BREADCRUMB on city pages (COUNTY-03):
       - Open Boston → confirm county breadcrumb chip does NOT appear (Boston is in Suffolk County, which is dissolved — county_id=NULL)
       - Open Plymouth → confirm "Plymouth County →" breadcrumb chip appears below the city name
       - Open Taunton → confirm "Bristol County →" breadcrumb chip appears
       - Open Edgartown → confirm "Dukes County →" breadcrumb chip appears
       - Open Quincy → confirm "Norfolk County →" breadcrumb chip appears
       - Open Barnstable (the city) → confirm "Barnstable County →" breadcrumb chip appears

    2. COUNTY PAGES — CitiesInCountyPanel (UI-01):
       - Click "Plymouth County →" breadcrumb from Plymouth city page → confirm it opens the Plymouth County page
       - Confirm CitiesInCountyPanel shows listed cities (e.g., Brockton, Plymouth, Rockland — should be "Available now" chips)
       - Open Norfolk County page → confirm 28 cities are listed in CitiesInCountyPanel

    3. PER-CAPITA ON COUNTY PAGES (UI-02):
       - On Plymouth County page → confirm per-capita ($/resident) shows a figure (based on population=542090)
       - On Norfolk County page → confirm per-capita shows a figure (based on population=740754)
       - Note: per-capita will show $0 or "no data" for budget — that is expected since Phase 41 loads budget data. The key check is that the population figure is not 0 and the per-capita slot renders.

    4. NO REGRESSION — existing entities:
       - Open Los Angeles (CA) → confirm "Los Angeles County →" breadcrumb still shows (Phase 25 regression check)
       - Open a TX city (e.g., Dallas) → confirm no county breadcrumb appears (TX has no county rows)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any specific issue found (which city page, which check failed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script → Supabase DB | Service-role key used for direct DB writes; script runs locally, not exposed to internet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | county_id UPDATE without state filter | mitigate | .eq('state', 'MA') present on every UPDATE (Pitfall 5); verified in acceptance criteria |
| T-40-02 | Tampering | Nantucket accidentally inserted as county row | mitigate | COUNTY_ROWS constant contains exactly 5 entries; Nantucket not in list; verified by dry-run output showing 5 rows |
| T-40-03 | Information Disclosure | SUPABASE_SERVICE_KEY in shell | accept | Key is in .env (gitignored); same risk posture as all other seeders in this project |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; N/A |
</threat_model>

<verification>
All three success criteria verified when:
- DB has exactly 5 rows with entity_type='county', state='MA', population > 0, population_year=2024
- SELECT COUNT(*) WHERE state='MA' AND county_id IS NOT NULL returns 97 (or 96 if Gosnold absent)
- Per-county breakdown matches: Barnstable=15, Bristol=20, Dukes=6 or 7, Norfolk=28, Plymouth=27
- Human confirms breadcrumb appears on Plymouth, Taunton, Edgartown, Quincy, Barnstable city pages
- Human confirms CitiesInCountyPanel on county pages shows linked cities
- Human confirms per-capita slot renders (non-zero population) on at least one county page
- Boston (Suffolk County, dissolved) shows NO breadcrumb
- LA County breadcrumb on Los Angeles city page is unchanged (no regression)
</verification>

<success_criteria>
Phase 40 is complete when:
1. 5 MA county rows exist in treasury.municipalities with entity_type='county', state='MA', population from 2024 Census
2. 97 MA cities have county_id set (96 acceptable if Gosnold absent from DB)
3. Per-county DB counts match: Barnstable=15, Bristol=20, Dukes=6-7, Norfolk=28, Plymouth=27
4. No cross-state contamination (only MA cities have county_id pointing to MA county rows)
5. ~254 MA cities in dissolved counties retain county_id=NULL
6. Human verified: county breadcrumb appears on Plymouth, Taunton, Edgartown, Quincy, Barnstable city pages
7. Human verified: CitiesInCountyPanel visible on county pages listing linked cities
8. Human verified: per-capita renders on county pages (population > 0 activates the display)
9. No regression on LA County breadcrumb or TX/OR cities
</success_criteria>

<output>
Create .planning/phases/40-ma-county-seeding-city-linking/40-01-SUMMARY.md when done.
</output>
