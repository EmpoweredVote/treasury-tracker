---
phase: 118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37
plan: "118-01"
subsystem: state-acfr-loaders
tags: [acfr, gasb, state-general-fund, nasbo-replace, money-in, alaska, load-01]

# Dependency graph
requires:
  - phase: 117-recon-source-location-roster-lock-overlap-scope-pre-flight-r
    provides: 117-BATCH1-SOURCES.md — AK source location, GF column, bookend ties, orphan-row finding
provides:
  - "Alaska state node live on full State-ACFR GAAP (GF revenue-by-source + spending-by-function), FY2006–FY2025, 40 rows, Money In enabled"
affects: [124-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "build_state.py — reusable download + pdftotext -table + extract_gf + per-year tie-gate + {st}_all.json assembler; serves the whole Batch-1 tranche"
    - "Landing-page href enumeration to discover the real durable per-year URL naming (AK 2-digit {YY}cafr.pdf pre-2010, 4-digit cafr FY2010–2019, acfr FY2020+) — the recon's 'FY1998' claim was the stale-URL trap"
    - "Orphaned data_sources cleanup gated on 0 referencing budgets rows before a new ACFR load (WR-05/LOAD-01)"

key-files:
  created:
    - scripts/processAKAcfr.js
    - scripts/processAKRevenueAcfr.js
    - _acfr-work/build_state.py
    - .planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-01-AK-LOADLOG.md
  modified:
    - _acfr-work/gen_state.py

key-decisions:
  - "Durable clean window = FY2006–FY2025 (20 contiguous years). FY1998/99 pre-GASB-34 no-tie; FY2000–2005 not linked — honest holes, far above the recency floor."
  - "AK ACFR GF ~1.32× NASBO — Federal Grants in Aid consolidated into the GAAP GF; accepted-and-relabelled honestly (TX precedent)."
  - "Pre-load: deleted 2 orphaned data_sources rows (ak-ugf-revenue/ak-ugf-operating, 0 referencing budgets) before the ACFR load."

requirements-completed: [ACFR-33]

# Metrics
duration: ~40min
spend: $0 (pdftotext + node + free public PDFs; no AI)
---

# 118-01 SUMMARY — Alaska ACFR Upgrade (ACFR-33)

Alaska is live on full State-ACFR GAAP. Both loaders (`scripts/processAKAcfr.js` operating, `scripts/processAKRevenueAcfr.js` revenue) were generated by `gen_state.py CONFIGS['AK']` from `_acfr-work/ak/ak_all.json` (20 tie-verified years), then live-loaded.

## What shipped
- **Window:** FY2006–FY2025 (20 contiguous years), operating + revenue = **40 rows** on the AK node.
- **Bookends tied at $0:** FY2025 GF Total revenues $8,378,945K; FY2020 $6,063,851K.
- **NASBO replaced in place:** FY2023 ($7.45B→$12.39B) and FY2024 ($6.34B→$11.81B) operating rows now GAAP-labelled; 0 NASBO labels remain on AK.
- **Task-0 cleanup:** 2 orphaned `data_sources` rows deleted (0 referencing budgets), NASBO budgets untouched at delete time.
- **Money In auto-enabled** (20 revenue rows).
- **Idempotency:** FY2025 re-run → 40 rows unchanged. **0 `data_sources` residue** (LOAD-01).
- **Cohort untouched:** CA=36, PA=20 (RECON-11 baseline); Iowa still NASBO.
- **Scope:** ~1.32× NASBO, accept-relabel honest, GAAP basis on every row.

## Holes (honest)
- FY1998/FY1999: `{YY}cafr.pdf` resolve but pre-GASB-34 layout — no clean tie.
- FY2000–FY2005: not linked on the DOF reports page.

## Handoff
Hands AK to **Phase 124** (independent re-derivation + cohort audit + Chris UAT). The `build_state.py` driver + the landing-page URL-enumeration pattern carry forward to 118-02..05.
