---
phase: 118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37
plan: "118-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processAKAcfr.js
  - scripts/processAKRevenueAcfr.js
  - .planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-01-AK-LOADLOG.md
autonomous: true
requirements: [ACFR-33]
user_setup: []

must_haves:
  truths:
    - "Alaska state node (b268c415-0058-4fea-8ba1-24f49fb434b4) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its clean durable window (target FY1998–FY2025, as deep as durable URLs + per-FY ties allow), every loaded FY tying exactly ($0) to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "AK units are THOUSANDS: UNITS=1000. Bookend ties: FY2025 GF Total revenues = $8,378,945K, FY2020 = $6,063,851K (store ×1,000 raw dollars)"
    - "GENERAL FUND column is the 1st of 4 (General Fund | Alaska Permanent Fund | Nonmajor Funds | Total Governmental Funds) of the Governmental Funds Statement of Rev/Exp/Changes — NOT the Statement of Activities, NOT a budgetary schedule"
    - "PRE-LOAD CLEANUP (AK-only, WR-05/LOAD-01): the 2 orphaned data_sources rows `ak-ugf-revenue` and `ak-ugf-operating` (no matching budgets rows) are DELETED before writing the new `ak-acfr-gf-operating`/`ak-acfr-gf-revenue` rows — no orphaned metadata left alongside the new ACFR rows"
    - "Pre-existing NASBO operating rows (AK FY2023 $7,450,000K, FY2024 $6,339,000K) are REPLACED in place by the ACFR operating rows at the same (b268c415, fy, 'operating') key — not duplicated, not left with a stale NASBO label"
    - "Accept-and-relabel scope divergence (AK ACFR GF ~1.32× NASBO — federal/intergovernmental consolidated inside GAAP GF) recorded at load against pre-load NASBO totals; GAAP basis label verified on live rows — node not silently inflated"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (slice→0, signed magnitude in label, parent total nets it); AK investment income is positive at both bookends so the clamp is a safety net; interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; 0 data_sources residue after the run (LOAD-01); the 29 existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on AK once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processAKAcfr.js"
      provides: "AK GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processAKRevenueAcfr.js"
      provides: "AK GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-01-AK-LOADLOG.md"
      provides: "Per-FY load disposition, data_sources cleanup confirmation, NASBO-replacement confirmation, accept-relabel divergence record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processAKAcfr.js / processAKRevenueAcfr.js"
      to: "treasury.budgets (AK state node b268c415)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['AK']"
      to: "scripts/processAKAcfr.js + scripts/processAKRevenueAcfr.js"
      via: "gen_state.py code generation from ak/ak_all.json + per-year SOURCES map (cafr→acfr naming switch at FY2020)"
      pattern: "General Fund"
---

<objective>
Upgrade the **Alaska** state node from NASBO operating-only to full State-ACFR GAAP by generating two loaders via the proven `_acfr-work/gen_state.py` tooling — `scripts/processAKAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processAKRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the AK ACFR PDFs into gitignored `_acfr-work/ak/`, extracting the **GENERAL FUND column** (1st of 4: General Fund | Alaska Permanent Fund | Nonmajor Funds | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading into `treasury.budgets` (NASBO operating replaced in place, revenue net-new) and verifying idempotency + zero data_sources residue + cohort-untouched.

**AK-ONLY PRE-LOAD CLEANUP (WR-05/LOAD-01):** Alaska carries 2 orphaned `data_sources` rows — `ak-ugf-revenue` and `ak-ugf-operating` — with NO matching `budgets` rows (dead metadata from an abandoned HTML loader). DELETE both before writing the new ACFR `data_sources` rows, so no orphaned `sync_status`/`rows_synced` metadata remains alongside the ACFR-sourced rows.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 GF Total revenues $8,378,945K, FY2020 $6,063,851K. **Naming exception:** FY2020–FY2025 use `{YYYY}acfr.pdf`; FY1998–FY2019 use `{YYYY}cafr.pdf` (the SOURCES map must special-case the cafr→acfr switch — MA FY2017 precedent).

Output: `scripts/processAKAcfr.js` + `scripts/processAKRevenueAcfr.js` live-loaded across the tied window (honest holes logged), NASBO replaced in place, Money In auto-enabled; `118-01-AK-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md

<interfaces>
<!-- The v2.14 tooling. Clone via gen_state.py — do NOT hand-copy an IL-era loader. -->
- `_acfr-work/gen_state.py` — parameterized generator. Add a `CONFIGS['AK']` entry (mirror the existing `'OR'`/`'CO'`/`'MO'` entries): name='Alaska', abbr='AK', population (from loadStateGF.mjs), node='b268c415-0058-4fea-8ba1-24f49fb434b4', years=[loaded FYs], tolerance (dollars; AK tied at $0 so a few ×1,000 units is safe), data='ak/ak_all.json', out='AK', base_url='https://doa.alaska.gov/dof/reports/annualreport.html', sources={ per-FY URL map }. Then `py -3 gen_state.py AK` emits scripts/processAKAcfr.js + processAKRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-aware GF-column extractor (anchors on the Total-revenues row's right edge). Feeds ak/ak_all.json { [fy]: { operating:{total,categories:[{name,total}]}, revenue:{total,categories:[{name,total}]} } }. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape: UNITS constant (set 1000), SOURCES map, validate(fy) (category sum vs printed total, exit(2) on non-tie), buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE, clampForRender (revenue), and the **ephemeral data_sources lifecycle** (WR-05 fix: delete-stale-by-dataset_id → insert → RPC/stamp → delete-by-id at end — leaves 0 residue). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ak-acfr-gf-operating' (name 'Alaska General Fund Operating Budget'), 'ak-acfr-gf-revenue' (name 'Alaska General Fund Revenue'), api_type='pdf_download', base_url = the DOF landing page.
- NASBO baseline (117-RECON): AK has 2 NASBO operating rows only — FY2023 $7,450,000K, FY2024 $6,339,000K (data_source "NASBO State Expenditure Report…", data_source_id=null), NO revenue rows.
- DB access for verification + the pre-load data_sources cleanup: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 117-BATCH1-SOURCES.md AK Detail Block — USE these, do not re-research. -->
- Statement: Statement 1.13 — Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances. GF = 1st numeric token of (General Fund | Alaska Permanent Fund | Nonmajor Funds | Total). Units = THOUSANDS. FY-end June 30.
- URL (FY2020–FY2025): https://doa.alaska.gov/dof/reports/resource/{YYYY}acfr.pdf
- URL (FY1998–FY2019): https://doa.alaska.gov/dof/reports/resource/{YYYY}cafr.pdf  ← "cafr" not "acfr" (naming exception; SOURCES map must switch per year).
- Landing: https://doa.alaska.gov/dof/reports/annualreport.html
- Bookend ties (GF column Total revenues, thousands): FY2025 = 8,378,945; FY2020 = 6,063,851 (both diff $0 in recon).
- Investment: FY2025 Interest & Investment Income (Loss) = +$350,330K; FY2020 = +$273,988K (both positive). No P2 clamp needed for confirmed years; check interior years at load.
- Scope vs NASBO ~1.32× (FY2025 ACFR $8.38B vs FY2024 NASBO $6.34B) — federal grants-in-aid ($4.83B) consolidated inside GAAP GF. Accept-and-relabel honestly.
- Clean window FY1998–FY2025 (28 yr). Load as deep as durable + per-FY tie allows; pre-FY1998 not published (out of scope, not a blocker).
- **AK data_sources residue:** ak-ugf-revenue (revenue, 2022–2026, rows_synced 30) + ak-ugf-operating (operating, 2022–2026, rows_synced 60) — both is_enabled, both orphaned (0 budgets rows reference them). DELETE both pre-load.
</source_facts>
</context>

<tasks>

<task id="118-01-00" type="auto">
  <name>Task 0: AK-only pre-load data_sources cleanup — delete the 2 orphaned rows</name>
  <files>(read-only DB probe + scoped DELETE via mcp__supabase-local__execute_sql)</files>
  <read_first>
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (AK finding — orphaned data_sources residue; Overlap Resolution + NASBO-replace rule item 7)
  </read_first>
  <action>
    Via mcp__supabase-local__execute_sql (schema treasury): FIRST confirm the orphan state — SELECT dataset_id, name, dataset_type, is_enabled, rows_synced FROM treasury.data_sources WHERE dataset_id IN ('ak-ugf-revenue','ak-ugf-operating'); and confirm ZERO budgets rows reference either (SELECT count(*) FROM treasury.budgets WHERE data_source_id IN (SELECT id FROM treasury.data_sources WHERE dataset_id IN ('ak-ugf-revenue','ak-ugf-operating'))) — expect 0. Also confirm AK's current budgets rows are the 2 NASBO operating rows only (FY2023 $7,450,000K, FY2024 $6,339,000K, data_source_id NULL). ONLY after confirming both orphans have 0 referencing budgets rows, DELETE the 2 rows: DELETE FROM treasury.data_sources WHERE dataset_id IN ('ak-ugf-revenue','ak-ugf-operating'). Do NOT touch any budgets row, any other data_sources row, or any other state. Record the pre-cleanup state + the delete confirmation for the LOADLOG.
  </action>
  <verify>
    <automated>echo "cleanup verified in Task 3 via mcp__supabase-local execute_sql: 0 rows where dataset_id in (ak-ugf-revenue, ak-ugf-operating)"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-delete probe confirmed both `ak-ugf-revenue` and `ak-ugf-operating` had 0 referencing `budgets` rows (safe to delete).
    - After the DELETE, `SELECT count(*) FROM treasury.data_sources WHERE dataset_id IN ('ak-ugf-revenue','ak-ugf-operating')` = 0.
    - AK's 2 NASBO operating budgets rows (FY2023/FY2024) are UNCHANGED by this task; no other state or row touched.
  </acceptance_criteria>
  <done>The 2 orphaned AK data_sources rows are deleted (after confirming 0 referencing budgets rows); AK's NASBO budgets rows untouched; the cleanup is recorded for the LOADLOG.</done>
</task>

<task id="118-01-01" type="auto">
  <name>Task 1: Generate both AK loaders (UNITS=1000) via gen_state.py + download/extract/transcribe FY1998–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ak/ (gitignored PDFs + pdftotext + ak_all.json), scripts/processAKAcfr.js, scripts/processAKRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'OR'/'CO' entry pattern; confirm how UNITS + sources + tolerance are wired into the generated loader)
    - _acfr-work/extract_gf.py (the GF-column extractor — how it anchors on the Total-revenues row; how categories are emitted)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (AK Detail Block: per-year URLs incl. cafr→acfr switch, units=thousands, bookend ties)
    - scripts/processCOAcfr.js (a recent gen_state.py output — the exact generated loader shape incl. the ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already). For each FY across the durable window (target FY1998–FY2025), download the AK ACFR PDF to `_acfr-work/ak/AK{YYYY}.pdf` using `…/resource/{YYYY}acfr.pdf` for FY2020–FY2025 and `…/resource/{YYYY}cafr.pdf` for FY1998–FY2019 (cafr→acfr naming switch). Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, and run `extract_gf.py` to isolate the GENERAL FUND column (1st numeric token of General Fund | Alaska Permanent Fund | Nonmajor | Total) for BOTH the revenues section and the expenditures section. Assemble `_acfr-work/ak/ak_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } with verbatim line-item names (thousands, unscaled at this stage); document any hand-patch inline with printed-row evidence. Add a `CONFIGS['AK']` entry to `_acfr-work/gen_state.py`: name='Alaska', abbr='AK', population (from loadStateGF.mjs), node='b268c415-0058-4fea-8ba1-24f49fb434b4', years=[the FYs that cleanly tie], tolerance (small), data='ak/ak_all.json', out='AK', base_url='https://doa.alaska.gov/dof/reports/annualreport.html', sources={ per-FY URL with the cafr/acfr switch }, UNITS=1000. Run `py -3 gen_state.py AK` to emit `scripts/processAKAcfr.js` + `scripts/processAKRevenueAcfr.js` (dataset_ids ak-acfr-gf-operating / ak-acfr-gf-revenue). Cross-check FY2025 Total revenues stored = 8,378,945×1000 and FY2020 = 6,063,851×1000 — if either bookend does not match the extracted GF column, STOP and re-inspect column selection before proceeding. OMIT (honest hole) any interior FY that 404s/soft-404s/won't tie, and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie with no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processAKAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processAKAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processAKRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '8,378,945,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ak/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY; soft-404/HTML rejected; naming switch (acfr FY2020–2025 / cafr FY1998–2019) applied.
    - `node scripts/processAKAcfr.js --dry-run` prints "validation: PASS" for every present FY, NO "sum ≠ total"; revenue `--fy 2025` prints TOTAL 8,378,945,000 and `--fy 2020` prints 6,063,851,000 (thousands ×1,000).
    - Both loaders carry `UNITS = 1000`; `_acfr-work/gen_state.py` has a `CONFIGS['AK']` entry; `_acfr-work/ak/ak_all.json` exists with per-FY operating + revenue trees.
    - dataSource labels are GAAP-basis-labelled ("Alaska State ACFR — General Fund … GAAP basis"); revenue RPC uses p_dataset_type:'revenue', dataset_id='ak-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both AK loaders exist (generated by gen_state.py) with UNITS=1000, SOURCES handles the cafr→acfr switch, ak_all.json holds every tied FY, both bookends (8,378,945K / 6,063,851K) dry-run-tie, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task id="118-01-02" type="auto">
  <name>Task 2: Live-load AK (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processAKAcfr.js, scripts/processAKRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processAKAcfr.js + scripts/processAKRevenueAcfr.js (the --fy path + post-RPC source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; AK node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Confirm Task 0's data_sources cleanup landed (0 rows for the 2 orphan dataset_ids). Query treasury.budgets (mcp__supabase-local__execute_sql) for the AK node (b268c415) and RECORD the pre-load NASBO operating rows (FY2023 $7,450,000K, FY2024 $6,339,000K: total + data_source) — the never-overwrite baseline + accept-relabel comparator. Then for each present FY (from Task 1's tied window, minus honest holes), run `node scripts/processAKAcfr.js --fy <YYYY>` then `node scripts/processAKRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (b268c415, fy, 'operating'); revenue inserts net-new at (b268c415, fy, 'revenue')), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row at end-of-run. If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded AK GF operating + revenue totals against the pre-load NASBO totals to confirm the ~1.32× accept-relabel divergence is the expected federal-inside-GAAP-GF mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "AK row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the AK node has, per loaded FY, an operating row "Alaska State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - AK FY2025 revenue row total = 8,378,945,000 and FY2020 = 6,063,851,000 (thousands ×1,000).
    - AK FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (AK, fy), GAAP-labelled; no "NASBO" label remains on the AK node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded with the ~1.32× divergence explained.
  </acceptance_criteria>
  <done>Each transcribed AK FY has operating + revenue rows in treasury.budgets, thousands-scaled (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO operating replaced in place (no duplicates); accept-relabel divergence recorded.</done>
</task>

<task id="118-01-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-01-AK-LOADLOG.md</files>
  <read_first>
    - scripts/processAKAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the 29 ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. AK --fy 2025 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place → "Loaded 0 rows") AND 0 data_sources residue afterward. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded AK FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) AK has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (AK, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — AK has ≥1 dataset_type='revenue' row; (e) LOAD-01 — 0 orphaned data_sources rows for AK (the 2 ugf orphans gone, the 2 new acfr rows either self-cleaned to 0 residue or leave no orphan); (f) cohort untouched: spot-check that a sample of the 29 existing ACFR nodes (e.g. CA e1007bf5, PA d4a4aadc) + one un-upgraded NASBO state are unchanged (row counts + a couple data_source labels). Append a "Load Disposition" section to 118-01-AK-LOADLOG.md: FYs loaded, FYs skipped (+reason), the data_sources cleanup confirmation, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than AK.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 118-01-AK-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of AK --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded AK FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; AK has ZERO remaining "NASBO" operating rows; exactly one operating row per (AK, fy).
    - AK has ≥1 revenue row (Money In auto-enable confirmed).
    - 0 data_sources residue for AK (the 2 ugf orphans deleted; no new orphan left) — LOAD-01 holds.
    - Honest-hole FYs are ABSENT from the DB.
    - Sample of the 29 existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 118-01-AK-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 residue; the DB query confirms AK fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled, orphan metadata gone), honest holes absent, and the rest of the cohort unchanged; 118-01-AK-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doa.alaska.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the AK node only (Tasks 0, 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01-A | Tampering | units mis-scale (applying/omitting the ×1,000) inflates or deflates AK 1000× | mitigate | UNITS=1000 hard-set; bookend totals (8,378,945K / 6,063,851K) asserted in dry-run + live |
| T-118-01-B | Tampering | extracting the wrong statement/column (Statement of Activities, or Alaska Permanent Fund / Nonmajor / Total instead of GENERAL FUND) | mitigate | extract_gf.py isolates the 1st numeric token of the Governmental Funds statement; bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-118-01-C | Tampering | soft-404 HTML saved as .pdf → garbage rows | mitigate | %PDF magic + size guard; non-PDF aborts that FY |
| T-118-01-D | Tampering | wrong filename era (acfr vs cafr) → missing years | mitigate | SOURCES map switches cafr(FY1998–2019)→acfr(FY2020–2025) per year; honest hole if a year 404s |
| T-118-01-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (AK, fy), zero NASBO labels |
| T-118-01-F | Tampering | data_sources residue — the 2 orphaned ugf rows left behind, or the new acfr rows leaving orphans | mitigate | Task 0 deletes the 2 orphans (after confirming 0 referencing budgets); generated loaders use the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-118-01-G | Info Disclosure | silently inflating AK (~1.32×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-118-01-H | Tampering | write to a state other than AK (the 29 ACFR nodes / NASBO states) | mitigate | loader resolves only the AK node id; Task 0 DELETE scoped to the 2 AK dataset_ids; Task 3 spot-checks cohort unchanged |
| T-118-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + the existing python tooling |
</threat_model>

<verification>
- Both AK loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processAKRevenueAcfr.js --dry-run --fy 2025` → 8,378,945,000; `--fy 2020` → 6,063,851,000.
- Pre-load: the 2 orphaned data_sources rows (ak-ugf-revenue / ak-ugf-operating) deleted (0 remaining), AK NASBO budgets rows untouched at delete time.
- Live: AK node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (AK, fy)).
- Re-run AK --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for AK (LOAD-01).
- AK ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.32×) recorded against pre-load NASBO totals.
- 29 existing ACFR nodes + a sample NASBO state unchanged.
- 118-01-AK-LOADLOG.md finalized.
</verification>

<success_criteria>
The Alaska state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its clean durable window (target FY1998–FY2025, honest holes logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, the 2 orphaned data_sources rows deleted (0 residue, LOAD-01), NASBO operating replaced in place, the ~1.32× accept-relabel divergence recorded at load, the P2 clamp wired for any negative year, idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands AK to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-01-SUMMARY.md` when done — record the AK FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the data_sources cleanup confirmation, the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
