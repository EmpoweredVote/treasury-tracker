---
phase: 118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37
plan: "118-03"
type: execute
wave: 3
depends_on: ["118-02"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processDEAcfr.js
  - scripts/processDERevenueAcfr.js
  - .planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-03-DE-LOADLOG.md
autonomous: true
requirements: [ACFR-35]
user_setup: []

must_haves:
  truths:
    - "Delaware state node (a7854fa3-8e68-4a0e-b92a-415bad6bccd2) shows ACFR-sourced GF revenue-by-source (revenue) AND GAAP spending-by-function (operating) across its clean window (FY2004–FY2025, minus the years not published in the archive), every loaded FY tying exactly ($0) to the printed GENERAL column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "DE units are THOUSANDS: UNITS=1000. Bookend ties: FY2025 GF Total revenues = $7,475,243K, FY2004 = $3,055,310K"
    - "GENERAL column is the 1st of 5 (General | Federal | Local School Districts | Capital Projects | Total Governmental Funds) of the Governmental Funds Statement of Rev/Exp/Changes — NOT the Statement of Activities"
    - "CRITICAL access requirement: every download from accountingfiles.delaware.gov MUST send a `Referer: https://accounting.delaware.gov/...` header — omitting it returns a 245-byte 'Request Rejected' HTML soft-404 at HTTP 200. Both a Content-Type (application/pdf) and a size (>~500KB) filter must be active so the soft-404 is rejected, never parsed"
    - "Pre-existing NASBO operating rows (DE FY2023, FY2024) REPLACED in place at (a7854fa3, fy, 'operating') — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence recorded: DE ACFR GF ~1.20× NASBO (smallest in the batch — Federal grants sit in their own major-fund column, so GF stays close to NASBO's own-source concept); GAAP basis label verified on live rows"
    - "Any negative GENERAL category year renders via the P2 clamp; DE investment income positive at both bookends (safety net); interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the 29 existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on DE once the revenue rows land (no frontend change)"
  artifacts:
    - path: "scripts/processDEAcfr.js"
      provides: "DE GF spending-by-function loader (operating), UNITS=1000, GF 1st-of-5, tie-verified across the loaded window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processDERevenueAcfr.js"
      provides: "DE GF revenue-by-source loader (revenue), UNITS=1000, GF 1st-of-5, tie-verified across the loaded window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-03-DE-LOADLOG.md"
      provides: "Per-FY load disposition (incl. unpublished-year holes), Referer-workaround confirmation, NASBO-replacement + ~1.20× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processDEAcfr.js / processDERevenueAcfr.js"
      to: "treasury.budgets (DE state node a7854fa3)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "curl download with Referer header"
      to: "accountingfiles.delaware.gov/docs/{YYYY}acfr.pdf"
      via: "Referer: https://accounting.delaware.gov/... required to bypass the WAF soft-404"
      pattern: "Referer"
---

<objective>
Upgrade the **Delaware** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processDEAcfr.js` (operating) + `scripts/processDERevenueAcfr.js` (revenue) — downloading DE ACFR PDFs FY2004–FY2025 into gitignored `_acfr-work/de/`, extracting the **GENERAL column** (1st of 5: General | Federal | Local School Districts | Capital Projects | Total) of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances (Deficits)* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**CRITICAL — Referer-header WAF:** `accountingfiles.delaware.gov` returns a 245-byte "Request Rejected" HTML soft-404 at HTTP 200 for any request lacking a `Referer` header pointing at `accounting.delaware.gov`. Every download MUST send `Referer: https://accounting.delaware.gov/reports-transparency/annual-comprehensive-financial-reports/`. The %PDF-magic + size guard independently rejects the soft-404 (245 bytes, text/html) so a naive fetch can't slip garbage through.

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $7,475,243K, FY2004 $3,055,310K. **Naming exception:** `{YYYY}acfr.pdf` FY2021–2025, `{YYYY}cafr.pdf` FY2004–2020. Several archive years (FY2005/2007/2008/2010/2011/2013) may not be published — spot-check and honest-hole any that 404. Scope ~1.20× NASBO (smallest in batch) — accept-and-relabel honestly.

Output: both loaders live-loaded across the published window, NASBO replaced in place, Money In auto-enabled; `118-03-DE-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['DE']` (mirror 'OR'/'CO'): name='Delaware', abbr='DE', population (loadStateGF.mjs), node='a7854fa3-8e68-4a0e-b92a-415bad6bccd2', years=[the published FY2004..FY2025 that tie], tolerance (small), data='de/de_all.json', out='DE', base_url='https://accounting.delaware.gov/reports-transparency/annual-comprehensive-financial-reports/', sources={ acfr/cafr per FY }, UNITS=1000. `py -3 gen_state.py DE` → scripts/processDEAcfr.js + processDERevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-anchor on the GENERAL column (1st of 5). Feed de/de_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'de-acfr-gf-operating' (name 'Delaware General Fund Operating Budget'), 'de-acfr-gf-revenue' (name 'Delaware General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (117-RECON): DE has 2 NASBO operating rows only (FY2023 + FY2024; FY2024 $6,232,000K), data_source_id=null, NO revenue rows, NO data_sources rows.
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH1-SOURCES.md DE Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances (Deficits). GF = 1st numeric token of (General | Federal | Local School Districts | Capital Projects | Total). Units = THOUSANDS. FY-end June 30.
- URL (FY2021–FY2025): https://accountingfiles.delaware.gov/docs/{YYYY}acfr.pdf
- URL (FY2004–FY2020): https://accountingfiles.delaware.gov/docs/{YYYY}cafr.pdf  ← "cafr" naming exception. FY2005/2007/2008/2010/2011/2013 may be missing — spot-check.
- **WAF: send `Referer: https://accounting.delaware.gov/reports-transparency/annual-comprehensive-financial-reports/` on EVERY request to accountingfiles.delaware.gov — else 245-byte "Request Rejected" HTML at HTTP 200 (soft-404).**
- Landing: https://accounting.delaware.gov/reports-transparency/annual-comprehensive-financial-reports/ (+ /archived-annual-comprehensive-financial-reports/)
- Bookend ties (GF column Total revenues, thousands): FY2025 = 7,475,243 (diff $0); FY2004 = 3,055,310 (diff $0).
- Investment: FY2025 Interest & Other Investment Income = +$238,663K; FY2004 = +$30,713K (both positive). No P2 clamp needed for confirmed years.
- Scope vs NASBO ~1.20× (FY2025 ACFR $7.48B vs FY2024 NASBO $6.23B) — smallest in the batch; Federal grants sit in their own column. Accept-and-relabel honestly.
</source_facts>
</context>

<tasks>

<task id="118-03-01" type="auto">
  <name>Task 1: Generate both DE loaders (UNITS=1000) via gen_state.py + Referer-header download/extract FY2004–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/de/ (gitignored PDFs + pdftotext + de_all.json), scripts/processDEAcfr.js, scripts/processDERevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy 'OR'/'CO'; UNITS + sources wiring)
    - _acfr-work/extract_gf.py (position-anchor extractor — GENERAL is 1st of 5)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (DE Detail Block: acfr/cafr URLs, Referer WAF, bookend ties)
    - scripts/processCOAcfr.js (recent gen_state.py output — generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. For each FY 2004–2025, download the DE ACFR PDF to `_acfr-work/de/DE{YYYY}.pdf` from `…/docs/{YYYY}acfr.pdf` (FY2021–2025) or `…/docs/{YYYY}cafr.pdf` (FY2004–2020), **sending `-H 'Referer: https://accounting.delaware.gov/reports-transparency/annual-comprehensive-financial-reports/'` on EVERY curl**. Soft-404 guard: assert each file is a real PDF (%PDF magic, size > ~500KB) — the 245-byte "Request Rejected" HTML (or any HTML) must abort that FY, not be parsed. Honest-hole any archive year that 404s / isn't published (FY2005/2007/2008/2010/2011/2013 candidates). Run `pdftotext -table` + `extract_gf.py`; isolate the GENERAL column (1st numeric token of General | Federal | Local School Districts | Capital Projects | Total) for BOTH revenues and expenditures. Assemble `_acfr-work/de/de_all.json` (thousands, verbatim names); document hand-patches inline. Add `CONFIGS['DE']` to gen_state.py (node a7854fa3…, years = the tying published FYs, UNITS=1000, sources = acfr/cafr map, base_url the DE landing page). Run `py -3 gen_state.py DE` → scripts/processDEAcfr.js + processDERevenueAcfr.js (dataset_ids de-acfr-gf-operating / de-acfr-gf-revenue). Cross-check FY2025 stored = 7,475,243×1000 and FY2004 = 3,055,310×1000 — if either bookend mismatches, STOP and re-inspect column selection. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processDEAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processDEAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processDERevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '7,475,243,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/de/` holds a real PDF (%PDF, >~500KB) per PUBLISHED FY; the 245-byte Referer soft-404 rejected; unpublished years honest-holed.
    - `node scripts/processDEAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2025` prints 7,475,243,000 and `--fy 2004` prints 3,055,310,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['DE']`; `de/de_all.json` exists.
    - The download command records the `Referer` header; revenue RPC uses p_dataset_type:'revenue', dataset_id='de-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both DE loaders exist (gen_state.py), UNITS=1000, GENERAL 1st-of-5 extracted with the Referer workaround, de_all.json holds every tied published FY, both bookends dry-run-tie, unpublished years honest-holed, P2 clamp wired, no live write.</done>
</task>

<task id="118-03-02" type="auto">
  <name>Task 2: Live-load DE (operating + revenue) across the published window, NASBO replaced in place</name>
  <files>(runs scripts/processDEAcfr.js, scripts/processDERevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processDEAcfr.js + scripts/processDERevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; DE node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the DE node (a7854fa3) and RECORD the pre-load NASBO operating rows (FY2023, FY2024: total + data_source; FY2024 $6,232,000K) — never-overwrite baseline + accept-relabel comparator. For each present FY (published FY2004–FY2025 minus holes), run `node scripts/processDEAcfr.js --fy <YYYY>` then `node scripts/processDERevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (a7854fa3, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded DE totals vs pre-load NASBO totals to confirm the ~1.20× divergence is the expected (Federal-in-own-column) mechanism.
  </action>
  <verify>
    <automated>echo "DE row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, the DE node has an operating row "Delaware State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both non-null source_url/source_date.
    - DE FY2025 revenue row total = 7,475,243,000 and FY2004 = 3,055,310,000.
    - DE FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (DE, fy), GAAP-labelled; no "NASBO" label on the DE node.
    - Pre-load NASBO totals + loaded ACFR totals recorded with the ~1.20× divergence note.
  </acceptance_criteria>
  <done>Each transcribed DE FY has operating + revenue rows (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO replaced in place; accept-relabel divergence recorded.</done>
</task>

<task id="118-03-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-03-DE-LOADLOG.md</files>
  <read_first>
    - scripts/processDEAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the 29 ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. DE --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows") AND 0 data_sources residue after. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql: (a) every loaded DE FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) DE has NO remaining "NASBO" operating rows (one operating row per (DE, fy)); (c) honest-hole FYs (unpublished years) ABSENT; (d) DE ≥1 revenue row (Money In); (e) 0 data_sources residue for DE (LOAD-01); (f) cohort untouched — spot-check a sample of the 29 ACFR nodes (CA e1007bf5, PA d4a4aadc) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 118-03-DE-LOADLOG.md: FYs loaded, unpublished-year holes, the Referer-workaround confirmation, pre-load-NASBO-vs-loaded-ACFR totals with the ~1.20× note, NASBO-replacement confirmation, idempotency re-run result. Do NOT load beyond the tied window; do NOT touch any state other than DE.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 118-03-DE-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of DE --fy 2025 → "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB: every loaded DE FY has both operating + revenue rows, GAAP-labelled, non-null sources; ZERO "NASBO" operating rows; one operating row per (DE, fy).
    - DE ≥1 revenue row (Money In auto-enable).
    - 0 data_sources residue for DE (LOAD-01).
    - Honest-hole (unpublished) FYs ABSENT.
    - Sample of the 29 ACFR nodes + one un-upgraded NASBO state unchanged.
    - 118-03-DE-LOADLOG.md has a finalized "Load Disposition" section incl. the Referer note.
  </acceptance_criteria>
  <done>A representative-FY re-run = 0 net change + 0 residue; DB confirms DE fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, Money In on), unpublished-year holes absent, cohort unchanged; 118-03-DE-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accountingfiles.delaware.gov PDF → loader transcription | untrusted external file (behind a WAF) parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the DE node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-03-A | Tampering | soft-404 WAF — 245-byte "Request Rejected" HTML at HTTP 200 saved as .pdf → garbage rows | mitigate | send `Referer: https://accounting.delaware.gov/...` on every request; %PDF magic + size (>~500KB) + Content-Type guard rejects the soft-404 regardless |
| T-118-03-B | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (7,475,243K / 3,055,310K) asserted dry-run + live |
| T-118-03-C | Tampering | extracting the wrong column (Federal / Local School Districts / Capital Projects / Total instead of GENERAL) or the Statement of Activities | mitigate | extract_gf.py anchors the 1st numeric token of the Governmental Funds statement; bookend ties gate; validate() exit-2 on non-tie |
| T-118-03-D | Tampering | wrong filename era (acfr vs cafr) or loading an unpublished year | mitigate | SOURCES switches acfr(FY2021–2025)/cafr(FY2004–2020); unpublished years honest-holed on 404 |
| T-118-03-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (DE, fy), zero NASBO labels |
| T-118-03-F | Info Disclosure | inflating DE (~1.20×) without recording the divergence | mitigate | Tasks 2–3 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows |
| T-118-03-G | Tampering | write to a state other than DE | mitigate | loader resolves only the DE node id; Task 3 spot-checks cohort unchanged |
| T-118-03-H | Tampering | data_sources residue | mitigate | generated loader uses the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-118-03-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling |
</threat_model>

<verification>
- Both DE loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processDERevenueAcfr.js --dry-run --fy 2025` → 7,475,243,000; `--fy 2004` → 3,055,310,000.
- Downloads send the Referer header; the 245-byte soft-404 rejected (never parsed); unpublished years honest-holed.
- Live: DE node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (DE, fy)).
- Re-run DE --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for DE (LOAD-01).
- DE ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.20×) recorded against pre-load NASBO totals.
- 29 existing ACFR nodes + a sample NASBO state unchanged.
- 118-03-DE-LOADLOG.md finalized.
</verification>

<success_criteria>
The Delaware state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across its published FY2004–FY2025 window (unpublished-year holes logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped via the Referer-header workaround, NASBO operating replaced in place, the ~1.20× accept-relabel divergence recorded at load, the P2 clamp wired, idempotent never-overwrite proven, 0 data_sources residue (LOAD-01), Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands DE to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-03-SUMMARY.md` when done — record the DE FY window transcribed, unpublished-year holes, the two bookend ties, the Referer-workaround confirmation, the NASBO-replacement confirmation, the ~1.20× accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
