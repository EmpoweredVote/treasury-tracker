---
phase: 118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37
plan: "118-04"
type: execute
wave: 4
depends_on: ["118-03"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processHIAcfr.js
  - scripts/processHIRevenueAcfr.js
  - .planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-04-HI-LOADLOG.md
autonomous: true
requirements: [ACFR-36]
user_setup: []

must_haves:
  truths:
    - "Hawaii state node (bf5b7221-9c8e-4df7-961d-e9c020ca733e) shows ACFR-sourced GF revenue-by-source (revenue) AND GAAP spending-by-function (operating) across FY2005–FY2025, every loaded FY tying exactly ($0) to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "HI units are THOUSANDS: UNITS=1000. Bookend ties: FY2025 GF Total revenues = $10,607,306K, FY2005 = $4,198,123K"
    - "GENERAL FUND is always the 1st column, but the total column count GROWS over time (4 cols FY2005 → 8 cols FY2025 as special-revenue funds like Med-Quest are broken out); the position-anchor (1st data column after the row label) handles this without per-year column-count config"
    - "GF-ALONE scope decision (D-09, UT precedent): the printed GENERAL FUND column ALONE is loaded (consistent with every other cohort state = the printed GF column), honestly relabelled — HI GAAP GF ~0.95× NASBO (NARROWER, because Med-Quest/Medicaid Special Revenue Fund ~$2.45B FY2025 is a separate major-fund column, not folded into GF). The node is NOT carved up or composited to match NASBO; the narrower-than-NASBO basis is documented and flagged for Chris UAT at Phase 124"
    - "FY2000–FY2004 are an HONEST HOLE — scanned image-only PDFs, zero embedded fonts, no text layer (out of scope for pdftotext-only / $0-no-AI). Window starts FY2005; the hole is documented"
    - "Per-year URLs are ENUMERATED (WordPress upload-date folders, NOT derivable from FY): each FY's exact folder path read off the archive page and pinned in the SOURCES map; every URL is a durable re-fetchable public URL (no Wayback-only)"
    - "Pre-existing NASBO operating rows (HI FY2023, FY2024) REPLACED in place at (bf5b7221, fy, 'operating') — not duplicated, no stale NASBO label"
    - "Any negative GENERAL FUND category year renders via the P2 clamp; HI investment income positive at both bookends (safety net); the 2008–2009 crisis-era interior years are plausible negatives — checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the 29 existing ACFR nodes + all un-upgraded NASBO states untouched; Money In auto-enables"
  artifacts:
    - path: "scripts/processHIAcfr.js"
      provides: "HI GF spending-by-function loader (operating), UNITS=1000, GF 1st col (growing column count), tie-verified FY2005–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processHIRevenueAcfr.js"
      provides: "HI GF revenue-by-source loader (revenue), UNITS=1000, GF 1st col, tie-verified FY2005–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-04-HI-LOADLOG.md"
      provides: "Per-FY load disposition, enumerated-URL map, FY2000–2004 image-only hole, GF-alone-vs-composite decision record (~0.95× narrower, flagged for UAT), NASBO-replacement + idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processHIAcfr.js / processHIRevenueAcfr.js"
      to: "treasury.budgets (HI state node bf5b7221)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['HI'] enumerated sources map"
      to: "ags.hawaii.gov/wp-content/uploads/{upload-date-folder}/acfr{YYYY}.pdf"
      via: "per-year WordPress upload-date folder path read off the archive page (non-derivable — enumerate, NC precedent)"
      pattern: "acfr"
---

<objective>
Upgrade the **Hawaii** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processHIAcfr.js` (operating) + `scripts/processHIRevenueAcfr.js` (revenue) — downloading HI ACFR PDFs FY2005–FY2025 into gitignored `_acfr-work/hi/`, extracting the **GENERAL FUND column** (always the 1st column; total column count grows 4→8 over time) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**Per-year URLs must be ENUMERATED** — Hawaii's PDFs live under WordPress upload-date folders NOT derivable from the fiscal year (e.g. FY2025 → `2026/02/acfr2025.pdf`, FY2024 → `2025/02/acfr2024.pdf`, FY2023 → `2024/04/acfr2023.pdf`, FY2005–FY2016 → `2012/09/acfr{YYYY}.pdf`; FY2017–FY2022 folders read off the archive page). Pin each year's exact durable URL in the SOURCES map (NC precedent).

**GF-alone scope decision (D-09, UT precedent):** Hawaii's GAAP General Fund is ~0.95× NASBO — NARROWER — because Med-Quest/Medicaid (~$2.45B FY2025) is a separate major-fund column. Load the printed GENERAL FUND column ALONE (consistent with every other cohort state), honestly relabelled as narrower than NASBO's operating concept. Do NOT composite GF+Med-Quest and do NOT carve GF down to NASBO. Document the narrower basis and flag it for Chris UAT at Phase 124.

**FY2000–FY2004 are an honest hole** (scanned image-only, no text layer). **Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2025 $10,607,306K, FY2005 $4,198,123K.

Output: both loaders live-loaded FY2005–FY2025, NASBO replaced in place, Money In auto-enabled; `118-04-HI-LOADLOG.md` with the load disposition + the GF-alone decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['HI']` (mirror 'OR'/'CO'): name='Hawaii', abbr='HI', population (loadStateGF.mjs), node='bf5b7221-9c8e-4df7-961d-e9c020ca733e', years=[FY2005..FY2025 that tie], tolerance (small), data='hi/hi_all.json', out='HI', base_url='https://ags.hawaii.gov/accounting/annual-financial-reports/', sources={ ENUMERATED per-FY upload-date-folder URLs }, UNITS=1000. `py -3 gen_state.py HI` → scripts/processHIAcfr.js + processHIRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — position-anchor on the 1st data column (GENERAL FUND); tolerant of the growing column count (no per-year column config). Feed hi/hi_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } }.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'hi-acfr-gf-operating' (name 'Hawaii General Fund Operating Budget'), 'hi-acfr-gf-revenue' (name 'Hawaii General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (117-RECON): HI has 2 NASBO operating rows only (FY2023 + FY2024; FY2024 $11,222,000K), data_source_id=null, NO revenue rows, NO data_sources rows. HI ACFR GF (~$10.6B) is NARROWER than NASBO (~0.95×).
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH1-SOURCES.md HI Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances. GF = 1st column (column count grows 4 in FY2005 → 8 in FY2025). Units = THOUSANDS. FY-end June 30.
- Enumerated URLs (durable): FY2025 → https://ags.hawaii.gov/wp-content/uploads/2026/02/acfr2025.pdf ; FY2024 → …/2025/02/acfr2024.pdf ; FY2023 → …/2024/04/acfr2023.pdf ; FY2005–FY2016 → …/2012/09/acfr{YYYY}.pdf ; FY2017–FY2022 folders read off the archive page at load.
- FY2000–FY2004: same 2012/09/ folder but SCANNED IMAGE-ONLY (zero fonts, no text) — honest hole.
- Landing: https://ags.hawaii.gov/accounting/annual-financial-reports/
- Bookend ties (GF column Total revenues, thousands): FY2025 = 10,607,306 (diff $0); FY2005 = 4,198,123 (diff $0).
- Investment: FY2025 Net increase in fair value of investments = +$12,735K; FY2005 Interest & investment income = +$25,170K (both positive). 2008–2009 crisis-era interior years are plausible negatives — P2 clamp ready.
- Scope vs NASBO ~0.95× (FY2025 ACFR $10.61B vs FY2024 NASBO $11.22B) — NARROWER; Med-Quest/Medicaid Special Revenue Fund ($2.45B FY2025) excluded. GF-alone load-time call (UT precedent): load GF alone, honest narrower label.
</source_facts>
</context>

<tasks>

<task id="118-04-01" type="auto">
  <name>Task 1: Generate both HI loaders (UNITS=1000) via gen_state.py + enumerate URLs / download / extract FY2005–FY2025 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/hi/ (gitignored PDFs + pdftotext + hi_all.json), scripts/processHIAcfr.js, scripts/processHIRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy 'OR'/'CO'; UNITS + sources wiring)
    - _acfr-work/extract_gf.py (position-anchor extractor — 1st data column, growing column count)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (HI Detail Block: enumerated upload-date-folder URLs, growing columns, GF-alone ~0.95×, image-only FY2000–2004)
    - scripts/processCOAcfr.js (recent gen_state.py output — generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. Enumerate the per-year URLs off the archive page (`ags.hawaii.gov/accounting/annual-financial-reports/`) for FY2017–FY2022 (the folders not already known); reuse the known folders for FY2023–2025 and FY2005–2016. For each FY 2005–2025, download the HI ACFR PDF to `_acfr-work/hi/HI{YYYY}.pdf`. Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. **Do NOT attempt FY2000–FY2004 (scanned image-only, no text layer) — record as honest hole.** Run `pdftotext -table` + `extract_gf.py`; isolate the GENERAL FUND column (1st data column, tolerant of the 4→8 column growth) for BOTH revenues and expenditures. **GF-alone decision:** load the printed GENERAL FUND column alone (do NOT composite Med-Quest); the ~0.95×-narrower-than-NASBO basis is intentional and will be relabelled honestly. Assemble `_acfr-work/hi/hi_all.json` (thousands, verbatim names); document hand-patches inline. Add `CONFIGS['HI']` to gen_state.py (node bf5b7221…, years = the tying FY2005..FY2025, UNITS=1000, sources = the ENUMERATED per-year URL map, base_url the archive page). Run `py -3 gen_state.py HI` → scripts/processHIAcfr.js + processHIRevenueAcfr.js (dataset_ids hi-acfr-gf-operating / hi-acfr-gf-revenue). Add a header comment recording the GF-alone / ~0.95× narrower accept-relabel note (Med-Quest excluded, UT precedent). Cross-check FY2025 stored = 10,607,306×1000 and FY2005 = 4,198,123×1000 — if either bookend mismatches, STOP and re-inspect column selection. OMIT (honest hole) any interior FY that 404s/won't tie/lacks a durable URL. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processHIAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processHIAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processHIRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '10,607,306,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/hi/` holds a real PDF per FY2005–2025; FY2000–2004 NOT present (image-only hole); soft-404/HTML rejected.
    - The SOURCES map enumerates a durable per-year URL for every loaded FY (no Wayback-only); FY2017–2022 folders resolved off the archive page.
    - `node scripts/processHIAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2025` prints 10,607,306,000 and `--fy 2005` prints 4,198,123,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['HI']`; `hi/hi_all.json` exists; header comment records the GF-alone ~0.95× narrower note.
  </acceptance_criteria>
  <done>Both HI loaders exist (gen_state.py), UNITS=1000, GENERAL FUND 1st column extracted across the growing column count, per-year URLs enumerated + durable, hi_all.json holds every tied FY2005–FY2025, both bookends dry-run-tie, FY2000–2004 image-only hole logged, GF-alone decision recorded, P2 clamp wired, no live write.</done>
</task>

<task id="118-04-02" type="auto">
  <name>Task 2: Live-load HI (operating + revenue) FY2005–FY2025, NASBO replaced in place</name>
  <files>(runs scripts/processHIAcfr.js, scripts/processHIRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processHIAcfr.js + scripts/processHIRevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; HI node id + pre-load NASBO totals + ~0.95× narrower note)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the HI node (bf5b7221) and RECORD the pre-load NASBO operating rows (FY2023, FY2024: total + data_source; FY2024 $11,222,000K) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2005–FY2025 minus holes), run `node scripts/processHIAcfr.js --fy <YYYY>` then `node scripts/processHIRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (bf5b7221, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded HI totals vs pre-load NASBO totals to confirm the ~0.95× NARROWER divergence is the expected Med-Quest-excluded GF-alone mechanism (not a transcription error / missing column).
  </action>
  <verify>
    <automated>echo "HI row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, the HI node has an operating row "Hawaii State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both non-null source_url/source_date.
    - HI FY2025 revenue row total = 10,607,306,000 and FY2005 = 4,198,123,000.
    - HI FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (HI, fy), GAAP-labelled; no "NASBO" label on the HI node.
    - Pre-load NASBO totals + loaded ACFR totals recorded with the ~0.95× NARROWER GF-alone divergence note.
  </acceptance_criteria>
  <done>Each transcribed HI FY has operating + revenue rows (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO replaced in place; the ~0.95× narrower GF-alone divergence recorded.</done>
</task>

<task id="118-04-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-04-HI-LOADLOG.md</files>
  <read_first>
    - scripts/processHIAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the 29 ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. HI --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows") AND 0 data_sources residue after. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql: (a) every loaded HI FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) HI has NO remaining "NASBO" operating rows (one operating row per (HI, fy)); (c) honest-hole FYs (FY2000–2004 + any interior) ABSENT; (d) HI ≥1 revenue row (Money In); (e) 0 data_sources residue for HI (LOAD-01); (f) cohort untouched — spot-check a sample of the 29 ACFR nodes (CA e1007bf5, PA d4a4aadc) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 118-04-HI-LOADLOG.md: FYs loaded, the enumerated-URL map, FY2000–2004 image-only hole, the GF-alone-vs-composite DECISION record (loaded GF alone, ~0.95× narrower than NASBO, Med-Quest excluded, flagged for Chris UAT at Phase 124), pre-load-NASBO-vs-loaded-ACFR totals, NASBO-replacement confirmation, idempotency re-run result. Do NOT load beyond the tied window; do NOT touch any state other than HI.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 118-04-HI-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of HI --fy 2025 → "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB: every loaded HI FY has both operating + revenue rows, GAAP-labelled, non-null sources; ZERO "NASBO" operating rows; one operating row per (HI, fy).
    - HI ≥1 revenue row (Money In auto-enable).
    - 0 data_sources residue for HI (LOAD-01).
    - Honest-hole FYs (FY2000–2004 + any interior) ABSENT.
    - Sample of the 29 ACFR nodes + one un-upgraded NASBO state unchanged.
    - 118-04-HI-LOADLOG.md has a finalized "Load Disposition" with the enumerated-URL map + GF-alone decision + FY2000–2004 hole.
  </acceptance_criteria>
  <done>A representative-FY re-run = 0 net change + 0 residue; DB confirms HI fully ACFR-sourced (operating + revenue, GAAP-labelled GF-alone, NASBO replaced, Money In on), image-only + interior holes absent, cohort unchanged; 118-04-HI-LOADLOG.md finalized with the GF-alone decision flagged for UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ags.hawaii.gov PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the HI node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-04-A | Tampering | wrong upload-date folder → 404 / wrong-year file | mitigate | per-year URLs enumerated off the archive page + pinned; %PDF magic + size guard; bookend + per-FY ties gate |
| T-118-04-B | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (10,607,306K / 4,198,123K) asserted dry-run + live |
| T-118-04-C | Tampering | picking the wrong column as the growing column count shifts (2nd/3rd col instead of GENERAL FUND) | mitigate | position-anchor on the 1st data column; per-FY tie to the printed GF total; validate() exit-2 on non-tie |
| T-118-04-D | Info Disclosure | compositing GF+Med-Quest (over-broad) OR carving GF down to NASBO — misrepresenting the basis | mitigate | load the printed GENERAL FUND column ALONE (UT precedent); honest ~0.95×-narrower label; decision recorded + flagged for UAT |
| T-118-04-E | Tampering | loading an image-only FY2000–2004 as garbage | mitigate | FY2000–2004 excluded (no text layer); documented honest hole; window starts FY2005 |
| T-118-04-F | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (HI, fy), zero NASBO labels |
| T-118-04-G | Tampering | write to a state other than HI | mitigate | loader resolves only the HI node id; Task 3 spot-checks cohort unchanged |
| T-118-04-H | Tampering | data_sources residue | mitigate | generated loader uses the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-118-04-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling |
</threat_model>

<verification>
- Both HI loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processHIRevenueAcfr.js --dry-run --fy 2025` → 10,607,306,000; `--fy 2005` → 4,198,123,000.
- Per-year URLs enumerated + durable; FY2000–2004 image-only hole logged.
- GF-alone decision: printed GENERAL FUND column alone loaded, ~0.95× narrower honest label, flagged for UAT.
- Live: HI node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (HI, fy)).
- Re-run HI --fy 2025 → "Loaded 0 rows"; 0 data_sources residue for HI (LOAD-01).
- HI ≥1 revenue row → Money In auto-enabled.
- 29 existing ACFR nodes + a sample NASBO state unchanged.
- 118-04-HI-LOADLOG.md finalized.
</verification>

<success_criteria>
The Hawaii state node is live on full State-ACFR GAAP (printed GENERAL FUND column: GF-alone revenue-by-source + GAAP spending-by-function) across FY2005–FY2025 (FY2000–2004 image-only hole logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped via enumerated durable URLs, NASBO operating replaced in place, the ~0.95×-NARROWER GF-alone basis recorded + flagged for Chris UAT, the P2 clamp wired, idempotent never-overwrite proven, 0 data_sources residue (LOAD-01), Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands HI to Phase 124 for independent re-derivation + cohort audit + Chris UAT (with the GF-alone call surfaced).
</success_criteria>

<output>
Create `.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-04-SUMMARY.md` when done — record the HI FY window transcribed, the enumerated-URL map, the FY2000–2004 image-only hole, the two bookend ties, the GF-alone-vs-composite decision (~0.95× narrower, flagged for UAT), the NASBO-replacement confirmation, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
