---
phase: 118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37
plan: "118-02"
type: execute
wave: 2
depends_on: ["118-01"]
files_modified:
  - _acfr-work/gen_state.py
  - scripts/processARAcfr.js
  - scripts/processARRevenueAcfr.js
  - .planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-02-AR-LOADLOG.md
autonomous: true
requirements: [ACFR-34]
user_setup: []

must_haves:
  truths:
    - "Arkansas state node (5efd2f95-6deb-4118-a07a-9f48cdca681c) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across FY2003–FY2024, every loaded FY tying exactly ($0) to the printed GENERAL FUND total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "AR units are THOUSANDS: UNITS=1000. Bookend ties: FY2024 GF Total revenues = $24,045,611K, FY2003 = $9,434,421K"
    - "AR is a SINGLE-fund state — the entire Governmental Funds Statement of Rev/Exp/Changes IS the General Fund (no major/nonmajor split, no 1st-of-N position-anchor needed); extract_gf.py's generic line-item parser applies directly"
    - "FY2025 is an HONEST HOLE — 2025-Arkansas-ACFR.pdf is a valid 49MB PDF but Type-3-font garbled (no ToUnicode CMap, pdftotext unreadable, KY FY2023 precedent). NOT loaded from that file; the load window ends at FY2024 with the hole documented (re-check for a corrected upload / browser-OCR at a future touch)"
    - "Pre-existing NASBO operating rows (AR FY2023, FY2024) REPLACED in place at the same (5efd2f95, fy, 'operating') key — not duplicated, no stale NASBO label"
    - "Accept-and-relabel scope divergence recorded PROMINENTLY: AR ACFR GF ~3.96× NASBO — the WIDEST divergence in the entire ACFR cohort to date (single reported General Fund folds in ~$11.2B intergovernmental/federal revenue NASBO excludes); GAAP basis label verified on live rows"
    - "Any negative GENERAL FUND category year renders via the P2 clamp; AR investment earnings positive at both bookends (safety net); interior years checked at load"
    - "Idempotent never-overwrite: a second live run of a representative FY = 0 net change; 0 data_sources residue (LOAD-01); the 29 existing ACFR nodes + all un-upgraded NASBO states untouched"
    - "Money In auto-enables on AR once the revenue rows land (no frontend change)"
  artifacts:
    - path: "scripts/processARAcfr.js"
      provides: "AR GF spending-by-function loader (operating), UNITS=1000, single-fund, tie-verified FY2003–FY2024"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processARRevenueAcfr.js"
      provides: "AR GF revenue-by-source loader (revenue), UNITS=1000, single-fund, tie-verified FY2003–FY2024"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-02-AR-LOADLOG.md"
      provides: "Per-FY load disposition, FY2025 garbled-hole record, NASBO-replacement + prominent ~3.96× accept-relabel note, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processARAcfr.js / processARRevenueAcfr.js"
      to: "treasury.budgets (AR state node 5efd2f95)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + source-stamp + ephemeral data_sources lifecycle"
      pattern: "treasury_sync_budget_tree"
    - from: "_acfr-work/gen_state.py CONFIGS['AR']"
      to: "scripts/processARAcfr.js + scripts/processARRevenueAcfr.js"
      via: "gen_state.py code generation from ar/ar_all.json + cafr{YYYY}.pdf SOURCES map (FY2003–2024)"
      pattern: "General Fund"
---

<objective>
Upgrade the **Arkansas** state node from NASBO operating-only to full State-ACFR GAAP via `_acfr-work/gen_state.py` — `scripts/processARAcfr.js` (GF spending-by-function, `operating`) + `scripts/processARRevenueAcfr.js` (GF revenue-by-source, `revenue`) — downloading AR ACFR PDFs FY2003–FY2024 into gitignored `_acfr-work/ar/`, extracting the **General Fund** figures (AR is a SINGLE-fund state — the whole Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balance* is the General Fund, no column split) via `pdftotext -table` + `extract_gf.py`, dry-run-tying every FY to its printed total, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**FY2025 is an honest hole:** `2025-Arkansas-ACFR.pdf` downloads as a valid 49MB PDF but is Type-3-font garbled (no ToUnicode CMap → `pdftotext` unreadable, KY FY2023 precedent). Do NOT load FY2025 from that file — the window ends at FY2024; document the hole for a future re-check (corrected upload or browser-OCR fallback).

**Units = THOUSANDS (UNITS=1000).** Bookend guards: FY2024 $24,045,611K, FY2003 $9,434,421K. **Scope note is PROMINENT:** AR ACFR GF is ~3.96× NASBO — the widest scope divergence in the entire cohort to date — because AR reports essentially all governmental activity (incl. ~$11.2B intergovernmental/federal) under a single "General Fund". Accept-and-relabel honestly with a prominent basis note.

Output: both loaders live-loaded FY2003–FY2024 (FY2025 hole logged), NASBO replaced in place, Money In auto-enabled; `118-02-AR-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md
@.planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md

<interfaces>
- `_acfr-work/gen_state.py` — add `CONFIGS['AR']` (mirror the 'OR'/'CO' entries): name='Arkansas', abbr='AR', population (loadStateGF.mjs), node='5efd2f95-6deb-4118-a07a-9f48cdca681c', years=[FY2003..FY2024 that cleanly tie], tolerance (small), data='ar/ar_all.json', out='AR', base_url='https://www.dfa.arkansas.gov/office/accounting/annual-comprehensive-financial-report/', sources={ cafr{YYYY}.pdf per FY }, UNITS=1000. `py -3 gen_state.py AR` emits scripts/processARAcfr.js + processARRevenueAcfr.js.
- `_acfr-work/extract_gf.py` — for AR the single-fund case is the SIMPLEST: no position-anchor / 1st-of-N logic needed (there is only one fund column). Feed ar/ar_all.json { [fy]: { operating:{total,categories}, revenue:{total,categories} } } straight from the parsed line items. Document any hand-patch inline.
- Generated loaders inherit the v2.14 shape (UNITS, SOURCES, validate exit-2 on non-tie, buildTree, treasury_sync_budget_tree RPC keyed (muni, fy, dataset), source-stamp, clampForRender, ephemeral data_sources lifecycle). Flags: --dry-run, --fy YYYY.
- data_sources dataset_ids: 'ar-acfr-gf-operating' (name 'Arkansas General Fund Operating Budget'), 'ar-acfr-gf-revenue' (name 'Arkansas General Fund Revenue'), api_type='pdf_download'.
- NASBO baseline (117-RECON): AR has 2 NASBO operating rows only (FY2023 + FY2024; FY2024 $6,075,000K), data_source_id=null, NO revenue rows, NO data_sources rows.
- DB verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 117-BATCH1-SOURCES.md AR Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balance(s), Governmental Fund(s). **Single fund** — the whole statement is the General Fund. NOT Statement of Activities.
- Units = THOUSANDS. FY-end June 30. Clean window FY2003–FY2024 (22 yr).
- URL (FY2003–FY2024): https://www.dfa.arkansas.gov/wp-content/uploads/cafr{YYYY}.pdf
- URL (FY2025): https://www.dfa.arkansas.gov/wp-content/uploads/2025-Arkansas-ACFR.pdf — **GARBLED (Type 3 fonts, no ToUnicode CMap). DO NOT USE.** Gap-logged.
- Landing: https://www.dfa.arkansas.gov/office/accounting/annual-comprehensive-financial-report/
- Bookend ties (GF Total revenues, thousands): FY2024 = 24,045,611 (diff $0); FY2003 = 9,434,421 (diff $0).
- Investment: FY2024 Investment earnings (loss) = +$442,735K; FY2003 = +$46,139K (both positive). No P2 clamp needed for confirmed years.
- Scope vs NASBO ~3.96× (FY2024 ACFR $24.05B vs FY2024 NASBO $6.08B) — the WIDEST in the cohort; single reported General Fund includes ~$11.2B intergovernmental/federal. Accept-and-relabel with a PROMINENT basis note.
</source_facts>
</context>

<tasks>

<task id="118-02-01" type="auto">
  <name>Task 1: Generate both AR loaders (UNITS=1000, single-fund) via gen_state.py + download/extract/transcribe FY2003–FY2024 + dry-run tie</name>
  <files>_acfr-work/gen_state.py, _acfr-work/ar/ (gitignored PDFs + pdftotext + ar_all.json), scripts/processARAcfr.js, scripts/processARRevenueAcfr.js</files>
  <read_first>
    - _acfr-work/gen_state.py (CONFIGS shape — copy the 'OR'/'CO' entry; confirm UNITS + sources wiring)
    - _acfr-work/extract_gf.py (line-item parser — for AR no position-anchor is needed, single fund column)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-BATCH1-SOURCES.md (AR Detail Block: cafr{YYYY}.pdf URLs, single-fund, bookend ties, FY2025 garbled)
    - scripts/processCOAcfr.js (a recent gen_state.py output — the exact generated loader shape incl. ephemeral data_sources lifecycle)
  </read_first>
  <action>
    Confirm `_acfr-work/` gitignored. For each FY 2003–2024, download the AR ACFR PDF to `_acfr-work/ar/AR{YYYY}.pdf` from `…/wp-content/uploads/cafr{YYYY}.pdf`. Soft-404 guard: assert real PDF (%PDF magic, size > ~500KB) — abort a FY on HTML. **Do NOT download or attempt FY2025 (`2025-Arkansas-ACFR.pdf`) — it is garbled; record it as an honest hole.** Run `pdftotext -table` + `extract_gf.py` on each; because AR is single-fund, take the sole fund column of the Governmental Funds Statement of Rev/Exp/Changes for BOTH the revenues and expenditures sections. Assemble `_acfr-work/ar/ar_all.json` = { [fy]: { operating:{total,categories}, revenue:{total,categories} } } (thousands, verbatim names); document any hand-patch inline. Add `CONFIGS['AR']` to `_acfr-work/gen_state.py` (node 5efd2f95…, years = the tying FY2003..FY2024, UNITS=1000, sources = cafr{YYYY}.pdf map, base_url the DFA landing page). Run `py -3 gen_state.py AR` → scripts/processARAcfr.js + processARRevenueAcfr.js (dataset_ids ar-acfr-gf-operating / ar-acfr-gf-revenue). Add a header comment recording the PROMINENT ~3.96× accept-relabel note (single-fund state, widest divergence in cohort). Cross-check FY2024 Total revenues stored = 24,045,611×1000 and FY2003 = 9,434,421×1000 — if either bookend mismatches, STOP and re-inspect before proceeding. OMIT (honest hole) any interior FY that 404s/won't tie, and note it. Run both loaders `--dry-run`; confirm every present FY prints a PASS tie, no "sum ≠ total".
  </action>
  <verify>
    <automated>node scripts/processARAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processARAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processARRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '24,045,611,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ar/` holds a real PDF per FY2003–2024; FY2025 NOT present (garbled hole); soft-404/HTML rejected.
    - `node scripts/processARAcfr.js --dry-run` prints "validation: PASS" every present FY, no "sum ≠ total"; revenue `--fy 2024` prints 24,045,611,000 and `--fy 2003` prints 9,434,421,000.
    - Both loaders carry `UNITS = 1000`; gen_state.py has `CONFIGS['AR']`; `ar/ar_all.json` exists (FY2025 absent).
    - Header comment records the prominent ~3.96× accept-relabel note; revenue RPC uses p_dataset_type:'revenue', dataset_id='ar-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both AR loaders exist (gen_state.py), UNITS=1000 single-fund, ar_all.json holds every tied FY2003–FY2024 (FY2025 honest hole), both bookends dry-run-tie, P2 clamp wired, prominent divergence note recorded, no live write.</done>
</task>

<task id="118-02-02" type="auto">
  <name>Task 2: Live-load AR (operating + revenue) FY2003–FY2024, NASBO replaced in place</name>
  <files>(runs scripts/processARAcfr.js, scripts/processARRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processARAcfr.js + scripts/processARRevenueAcfr.js (the --fy path + source-stamp + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (NASBO-replace rule; AR node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets (mcp__supabase-local__execute_sql) for the AR node (5efd2f95) and RECORD the pre-load NASBO operating rows (FY2023, FY2024: total + data_source; FY2024 $6,075,000K) — never-overwrite baseline + accept-relabel comparator. For each present FY (FY2003–FY2024 minus honest holes), run `node scripts/processARAcfr.js --fy <YYYY>` then `node scripts/processARRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (5efd2f95, fy, 'operating'); revenue net-new), stamp source_url/source_date/data_source (GAAP label), and self-clean its ephemeral data_sources row. If a year fails the tie unexpectedly, abort that year + note it — never force-write. Record loaded AR totals vs pre-load NASBO totals to confirm the ~3.96× divergence is the single-fund/federal-consolidation mechanism, not a transcription error.
  </action>
  <verify>
    <automated>echo "AR row existence + NASBO-replacement + source-stamp + 0 residue verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - Per loaded FY, the AR node has an operating row "Arkansas State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row, both non-null source_url/source_date.
    - AR FY2024 revenue row total = 24,045,611,000 and FY2003 = 9,434,421,000.
    - AR FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (AR, fy), GAAP-labelled; no "NASBO" label on the AR node.
    - Pre-load NASBO totals + loaded ACFR totals recorded with the prominent ~3.96× divergence note.
  </acceptance_criteria>
  <done>Each transcribed AR FY has operating + revenue rows (UNITS=1000), GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO replaced in place; the prominent ~3.96× accept-relabel divergence recorded.</done>
</task>

<task id="118-02-03" type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-02-AR-LOADLOG.md</files>
  <read_first>
    - scripts/processARAcfr.js (RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/117-recon-source-location-roster-lock-overlap-scope-pre-flight-r/117-RECON.md (untouched-nodes contract — the 29 ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. AR --fy 2024 operating + revenue) → 0 net change ("Loaded 0 rows") AND 0 data_sources residue after. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql: (a) every loaded AR FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) AR has NO remaining "NASBO" operating rows (one operating row per (AR, fy)); (c) honest-hole FYs (incl. FY2025) ABSENT; (d) AR ≥1 revenue row (Money In); (e) 0 data_sources residue for AR (LOAD-01); (f) cohort untouched — spot-check a sample of the 29 ACFR nodes (CA e1007bf5, PA d4a4aadc) + one un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 118-02-AR-LOADLOG.md: FYs loaded, FY2025 garbled-hole record, pre-load-NASBO-vs-loaded-ACFR totals with the prominent ~3.96× accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT load beyond the tied window; do NOT touch any state other than AR.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 118-02-AR-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of AR --fy 2024 → "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB: every loaded AR FY has both operating + revenue rows, GAAP-labelled, non-null sources; ZERO "NASBO" operating rows; one operating row per (AR, fy).
    - AR ≥1 revenue row (Money In auto-enable).
    - 0 data_sources residue for AR (LOAD-01).
    - Honest-hole FYs (incl. FY2025) ABSENT.
    - Sample of the 29 ACFR nodes + one un-upgraded NASBO state unchanged.
    - 118-02-AR-LOADLOG.md has a finalized "Load Disposition" section with the FY2025 hole + prominent divergence note.
  </acceptance_criteria>
  <done>A representative-FY re-run = 0 net change + 0 residue; DB confirms AR fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, Money In on), FY2025 + other holes absent, cohort unchanged; 118-02-AR-LOADLOG.md finalized with the prominent ~3.96× note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dfa.arkansas.gov PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the AR node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-02-A | Tampering | units mis-scale (×1,000) | mitigate | UNITS=1000 hard-set; bookends (24,045,611K / 9,434,421K) asserted dry-run + live |
| T-118-02-B | Tampering | extracting the wrong statement (Statement of Activities / budgetary schedule) instead of the Governmental Funds General Fund | mitigate | extract_gf.py on the Governmental Funds statement; bookend ties gate; validate() exit-2 on non-tie |
| T-118-02-C | Tampering | loading the GARBLED FY2025 PDF → garbage rows | mitigate | FY2025 explicitly excluded (Type-3-font garbled); documented honest hole; window ends FY2024 |
| T-118-02-D | Tampering | soft-404 HTML saved as .pdf | mitigate | %PDF magic + size guard aborts that FY |
| T-118-02-E | Tampering | NASBO row left alongside the ACFR row (duplicate) | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts one operating row per (AR, fy), zero NASBO labels |
| T-118-02-F | Info Disclosure | inflating AR ~3.96× without a prominent divergence record | mitigate | header comment + Tasks 2–3 record pre-load NASBO vs loaded ACFR with a PROMINENT note; GAAP basis label on live rows |
| T-118-02-G | Tampering | write to a state other than AR | mitigate | loader resolves only the AR node id; Task 3 spot-checks cohort unchanged |
| T-118-02-H | Tampering | data_sources residue | mitigate | generated loader uses the v2.14 ephemeral lifecycle; Task 3 asserts 0 residue (LOAD-01) |
| T-118-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling |
</threat_model>

<verification>
- Both AR loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1000.
- `processARRevenueAcfr.js --dry-run --fy 2024` → 24,045,611,000; `--fy 2003` → 9,434,421,000.
- FY2025 excluded (garbled honest hole).
- Live: AR node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (AR, fy)).
- Re-run AR --fy 2024 → "Loaded 0 rows"; 0 data_sources residue for AR (LOAD-01).
- AR ≥1 revenue row → Money In auto-enabled.
- Prominent accept-relabel divergence (~3.96×, widest in cohort) recorded against pre-load NASBO totals.
- 29 existing ACFR nodes + a sample NASBO state unchanged.
- 118-02-AR-LOADLOG.md finalized.
</verification>

<success_criteria>
The Arkansas state node is live on full State-ACFR GAAP (single-fund GF revenue-by-source + GAAP spending-by-function) across FY2003–FY2024 (FY2025 garbled-hole logged), stored in thousands (UNITS=1000), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~3.96× (widest-in-cohort) accept-relabel divergence recorded PROMINENTLY at load, the P2 clamp wired, idempotent never-overwrite proven, 0 data_sources residue (LOAD-01), Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands AR to Phase 124 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/118-acfr-upgrade-batch-1-ak-ar-de-hi-id-acfr-33-37/118-02-SUMMARY.md` when done — record the AR FY window transcribed, the FY2025 garbled hole, the two bookend ties, the NASBO-replacement confirmation, the prominent ~3.96× accept-relabel divergence, the idempotency re-run + 0-residue result, and the Money-In auto-enable confirmation.
</output>
