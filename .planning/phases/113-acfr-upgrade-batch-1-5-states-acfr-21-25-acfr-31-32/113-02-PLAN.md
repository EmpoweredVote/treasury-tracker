---
phase: 113-acfr-upgrade-batch-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processAZAcfr.js
  - scripts/processAZRevenueAcfr.js
  - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-02-AZ-LOADLOG.md
autonomous: true
requirements: [ACFR-22, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "Arizona state node (866036ee-20b2-4e3c-a4f3-5100659edf31) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its recon-locked window FY2002–FY2024, every FY tying to the printed General Fund column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "AZ units are THOUSANDS: UNITS=1_000. FY2024 GF Total revenues stored = 44,045,434,000 dollars and FY2002 = 11,655,423,000"
    - "AZ FY2024 URL durability decision (locked at plan time): first re-check whether AZ GAO has migrated the FY2024 PDF to its normal gao.az.gov/sites/default/files hosting; if yes use that durable URL; if not, LOAD FY2024 anyway using the Google Drive link with an explicit non-durability caveat comment in the loader SOURCES entry AND a caveat paragraph in the LOADLOG"
    - "The pre-existing NASBO operating rows (AZ FY2023 $16,001M, FY2024 $17,903M) are REPLACED in place by ACFR operating rows — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the az-acfr dataset_ids"
    - "The ~2.46× scope divergence (Intergovernmental federal passthrough $25,234,916K inside GAAP GF) is recorded at load against pre-load NASBO totals and relabelled honestly — the node total jumps but stays correct + sourced + basis-labelled (ACFR-31, TX precedent)"
    - "Any negative GF category year renders via the P2 clamp (ACFR-32); AZ bookends showed none — clamp is a safety net checked every year"
    - "Money In auto-enables on AZ once revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processAZAcfr.js"
      provides: "AZ GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified FY2002–FY2024"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processAZRevenueAcfr.js"
      provides: "AZ GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified FY2002–FY2024"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-02-AZ-LOADLOG.md"
      provides: "Per-FY load disposition incl. the FY2024 durability resolution, WAF access log, NASBO-replacement confirmation, accept-relabel divergence record, idempotency + 0-residue result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processAZAcfr.js / processAZRevenueAcfr.js"
      to: "treasury.budgets (AZ state node 866036ee)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map FY2024 entry"
      to: "gao.az.gov durable URL (preferred) or Google Drive fallback (caveated)"
      via: "FY2024 durability re-check → durable-or-caveated-Drive decision recorded in loader comment + LOADLOG"
      pattern: "FY2024"
---

<objective>
Upgrade the **Arizona** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processAZAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processAZRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the AZ ACFR PDFs FY2002–FY2024 into gitignored `_acfr-work/az/`, extracting the **General Fund column** (1st of 5: General Fund | Transportation & Highway Maintenance & Safety Fund | Land Endowments Fund | Other Governmental Funds | Total) of the Governmental Funds *Statement of Revenues, Expenditures and Changes in Fund Balances* via `pdftotext -table`, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**AZ is the most access-constrained state in Batch 1.** Two pinned obstacles: (1) `gao.az.gov` runs a Cloudflare bot-management WAF that intermittently 403-blocks plain `curl` — recon succeeded with a session-cookie + `Referer` combination but got re-blocked afterward; budget for fresh-session retries or a browser-download fallback. (2) **FY2024's PDF is currently hosted only on a Google Drive share link** — the locked decision (2026-07-02): re-check for a since-migrated durable gao.az.gov URL first; if none, load FY2024 from the Drive link with an explicit non-durability caveat in the loader + LOADLOG. FY2025 is NOT published (NASBO shows it as "Estimated") — the window intentionally ends at FY2024.

Output: both AZ loaders live-loaded across FY2002–FY2024 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `113-02-AZ-LOADLOG.md` with the load disposition + FY2024 durability resolution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md

<interfaces>
<!-- The EXACT analog pattern. AZ loaders mirror the IL loaders (explicit per-year SOURCES map, non-derivable URLs). -->
From scripts/processILAcfr.js (spend-by-function analog → processAZAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='Arizona', state='AZ', entity_type='state' (.single()). Node id = 866036ee-20b2-4e3c-a4f3-5100659edf31 (assert match).
  - UNITS = 1_000 (thousands → dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Arizona State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES / validate / buildTree / RPC treasury_sync_budget_tree + post-RPC source-stamp UPDATE — copy template shape. Root n='Arizona General Fund Budget'.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01)** — copy verbatim: delete-by-dataset_id → insert fresh → RPC → delete-by-id at end. 0 residue.
  - data_sources.dataset_id = 'az-acfr-gf-operating', name = 'Arizona General Fund Operating Budget', api_type='pdf_download', base_url = 'https://gao.az.gov/financials/acfr'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue analog → processAZRevenueAcfr.js):
  - dataSource(fy) = "Arizona State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='az-acfr-gf-revenue'; name='Arizona General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender + "(net loss — shown at 0)" negative-label path (ACFR-32).
POPULATION = 7_151_502 (from loadStateGF.mjs).
NASBO baseline (112-RECON.md Section 5): AZ node has exactly 2 NASBO operating rows — FY2023 $16,001M, FY2024 $17,903M, data_source_id=null, NO revenue rows, ZERO data_sources rows. Standard supersede.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 112-BATCH1-SOURCES.md AZ Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures and Changes in Fund Balances — Governmental Funds. GF column = 1st of 5; take the FIRST numeric token per row.
- Units = THOUSANDS. FY-end June 30 (source_date = ${fy}-06-30). Clean window = FY2002–FY2024 (FY2025 not yet published; FY2002 = pre-GASB-34 boundary, do not go earlier).
- URL resolution: per-year node page `https://gao.az.gov/resources/annual-comprehensive-financial-report-june-30-{YYYY}` links to that year's PDF under `gao.az.gov/sites/default/files/{upload-YYYY-MM}/` (non-derivable filename — must scrape each node page). Confirmed: FY2023 `.../2024-11/State%20of%20AZ%20-%20FY23%20ACFR%20Final%20-%20w%20sig.pdf`; FY2002 `.../2022-05/02-CAFRall_0.pdf`.
- **FY2024 (decision locked 2026-07-02):** currently only `https://drive.google.com/file/d/14FYCgTQPsu77pxLtz41E_Ba_0hCuMhwA` (non-durable). Re-check the FY2024 node page for a migrated gao.az.gov URL FIRST; else use the Drive link via the `uc?export=download` endpoint with the caveat documented (loader comment + LOADLOG paragraph). Do NOT skip FY2024.
- **WAF:** Cloudflare bot-management on gao.az.gov intermittently 403s plain curl ("Just a moment…" challenge). Working recipe from recon: establish a session via the homepage (cookie jar) + send a `Referer` header matching the resource's own node page. If re-blocked, retry with a fresh session; if persistently blocked, fall back to asking the user to browser-download the failing PDFs into `_acfr-work/az/` (checkpoint, not silent skip).
- Bookend ties (GF Total revenues, thousands): FY2024 = 44,045,434 (diff $0); FY2002 = 11,655,423 (diff $0).
- Negative-line risk: none in bookends (all revenue lines incl. Earnings on investments positive). Clamp = safety net; still check every year.
- Scope vs NASBO ~2.46×: "Intergovernmental" = $25,234,916K of the $44.05B FY2024 GF total — federal Medicaid/education passthrough consolidated into GAAP GF, excluded from NASBO's budgetary concept. Accept-and-relabel honestly (TX precedent, ACFR-31); record the mechanism at load.
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve per-year URLs (incl. FY2024 durability re-check) + download through the WAF + extract/transcribe FY2002–FY2024 + build both loaders + dry-run tie</name>
  <files>scripts/processAZAcfr.js, scripts/processAZRevenueAcfr.js, _acfr-work/az/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (spend-by-function template incl. ephemeral data_sources lifecycle)
    - scripts/processILRevenueAcfr.js (revenue template incl. clampForRender)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (AZ Detail Block: node-page pattern, WAF recipe, FY2024 Drive link, bookend ties)
  </read_first>
  <action>
    **FY2024 durability re-check FIRST:** fetch the FY2024 node page (`.../annual-comprehensive-financial-report-june-30-2024`) and check whether the PDF link now points at `gao.az.gov/sites/default/files/...`. If yes, use it (durability resolved — note in LOADLOG). If it still points at Google Drive, download via `https://drive.google.com/uc?export=download&id=14FYCgTQPsu77pxLtz41E_Ba_0hCuMhwA` and record the caveat (loader SOURCES comment: "non-durable Drive link as published; gao.az.gov re-check {date} found no migrated URL" + LOADLOG paragraph). Then for each FY 2002–2023, resolve the node page → PDF path and download to `_acfr-work/az/AZ{YYYY}.pdf` using the WAF recipe: cookie-jar session established on the homepage + per-request `Referer` = that year's node page URL; on 403, retry with a fresh session (2–3 attempts); if a year stays blocked, list the failing years and CHECKPOINT — ask the user to browser-download them into `_acfr-work/az/` rather than silently skipping. Soft-404/challenge guard: assert %PDF magic + size > ~500KB (a saved "Just a moment…" HTML page must abort that FY, not be parsed). Run `pdftotext -table` on each PDF, isolate the General Fund column (1st numeric token) for revenues + expenditures sections. Copy the IL templates → processAZAcfr.js / processAZRevenueAcfr.js; set STATE_NAME='Arizona', STATE_ABBR='AZ', POPULATION=7_151_502, UNITS=1_000; SOURCES FY2002–FY2024 with the exact resolved URLs (source_date=`${fy}-06-30`); dataset_ids 'az-acfr-gf-operating'/'az-acfr-gf-revenue'; keep ephemeral data_sources lifecycle + clampForRender verbatim. Transcribe verbatim GF line items (raw thousands) into EXPENDITURES/REVENUE. Header comment: the ~2.46× Intergovernmental accept-relabel note (ACFR-31). Cross-check bookends FY2024 = 44,045,434 and FY2002 = 11,655,423 — mismatch = STOP and re-inspect column selection. Non-tying/non-extracting in-between FYs → OMIT as honest holes. Run both loaders --dry-run: every present FY must PASS.
  </action>
  <verify>
    <automated>node scripts/processAZAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processAZRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '44,045,434'</automated>
  </verify>
  <acceptance_criteria>
    - FY2024 durability re-check performed and its outcome (migrated durable URL vs caveated Drive link) recorded in both the loader SOURCES comment and the LOADLOG draft.
    - `_acfr-work/az/` holds a real PDF per obtained FY 2002–2024 (%PDF magic, >~500KB); challenge-page HTML rejected; any WAF-blocked years resolved via user checkpoint or logged as honest holes with the block noted.
    - Both loaders `--dry-run` print "validation: PASS" for every present FY; revenue `--fy 2024` shows 44,045,434 (thousands) and `--fy 2002` shows 11,655,423.
    - Both loaders have UNITS=1_000 + the ephemeral data_sources lifecycle; dataSource(2024) === "Arizona State ACFR — General Fund (FY2024 actual, GAAP basis)"; revenue dataset_id='az-acfr-gf-revenue'.
    - FY2025 is NOT in SOURCES (not yet published); nothing earlier than FY2002.
  </acceptance_criteria>
  <done>Both AZ loaders exist on the IL template, SOURCES enumerates resolved per-year URLs FY2002–FY2024 with the FY2024 durability decision executed + recorded, every transcribed FY dry-run-ties with the 44,045,434K / 11,655,423K bookends, WAF-blocked years handled via checkpoint (not silent skips), and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load AZ (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processAZAcfr.js, scripts/processAZRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processAZAcfr.js + scripts/processAZRevenueAcfr.js (--fy + source-stamp path)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + Section 8 replacement contract)
  </read_first>
  <action>
    Source .env first. Pre-write query (mcp__supabase-local__execute_sql) on the AZ node (866036ee): RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $16,001M, FY2024 $17,903M). Then per present FY (FY2002–FY2024 minus honest holes): `node scripts/processAZAcfr.js --fy <YYYY>` then `node scripts/processAZRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO rows at (866036ee, fy, 'operating') for FY2023/FY2024, inserts net-new elsewhere; revenue inserts net-new. Stamp source_url/source_date/data_source per year. A year failing its tie live → abort that year, never force-write. Record loaded ACFR GF totals vs pre-load NASBO totals confirming the ~2.46× divergence is the pinned Intergovernmental mechanism (ACFR-31), not a transcription error.
  </action>
  <verify>
    <automated>echo "AZ row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, per loaded FY the AZ node has an operating row "Arizona State ACFR — General Fund (FY{fy} actual, GAAP basis)" and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - AZ FY2024 revenue row total = 44,045,434,000 dollars; FY2002 = 11,655,423,000.
    - AZ FY2023 + FY2024 NASBO operating rows are GONE (exactly ONE operating row per (AZ, fy); no "NASBO" label remains).
    - The FY2024 row's source_url matches the durability decision outcome (gao.az.gov if migrated, else the Drive link with the caveat recorded).
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~2.46× mechanism note (ACFR-31).
  </acceptance_criteria>
  <done>Each transcribed AZ FY has operating + revenue rows, GAAP-labelled + per-year-sourced; NASBO FY2023/FY2024 replaced in place; the accept-relabel divergence and FY2024 URL decision recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification + LOADLOG</name>
  <files>.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-02-AZ-LOADLOG.md</files>
  <read_first>
    - scripts/processAZAcfr.js (RPC keying + ephemeral lifecycle)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 7 untouched-nodes contract)
  </read_first>
  <action>
    Re-run ONE representative loaded FY live a second time (e.g. AZ --fy 2023 operating + revenue) → confirm 0 net change. DB queries (mcp__supabase-local__execute_sql): (a) every loaded AZ FY has BOTH rows, GAAP-labelled, non-null sources; (b) zero remaining "NASBO" operating rows, exactly one operating row per (AZ, fy); (c) honest-hole FYs absent; (d) `data_sources` has ZERO rows with dataset_id LIKE 'az-acfr-%'; (e) AZ has ≥1 revenue row (Money In); (f) cohort spot-check: 2–3 of the 19 existing ACFR nodes + one un-upgraded NASBO state unchanged. Append "Load Disposition" to 113-02-AZ-LOADLOG.md: FYs loaded/skipped (+reasons incl. any WAF-blocked years), the FY2024 durability resolution paragraph, pre-load-NASBO-vs-ACFR totals with the ~2.46× note, NASBO-replacement confirmation, idempotency + 0-residue result. Do NOT touch any state other than AZ.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 113-02-AZ-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of a representative FY reports 0 net change.
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'az-acfr-%'` returns 0.
    - Every loaded AZ FY has both rows GAAP-labelled with sources; zero NASBO labels; one operating row per (AZ, fy); honest holes absent.
    - AZ has ≥1 revenue row (Money In auto-enable confirmed).
    - Cohort sample unchanged.
    - 113-02-AZ-LOADLOG.md has a finalized "Load Disposition" incl. the FY2024 durability resolution.
  </acceptance_criteria>
  <done>Idempotency + 0-residue proven; DB confirms AZ fully ACFR-sourced with NASBO replaced, Money In enabled, cohort untouched; LOADLOG finalized with the FY2024 decision trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gao.az.gov / Google Drive PDF → loader transcription | untrusted external files parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the AZ node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02-A | Tampering | Cloudflare challenge HTML saved as .pdf → garbage rows | mitigate | %PDF magic + size guard; challenge page aborts that FY; WAF recipe + user browser-download checkpoint |
| T-113-02-B | Repudiation | FY2024 loaded from a non-durable Drive URL without a caveat → silent source-chain fragility | mitigate | durability re-check first; caveat REQUIRED in loader comment + LOADLOG if Drive is used (locked decision) |
| T-113-02-C | Tampering | wrong statement/column (Transportation/Land Endowments/Total instead of GF) | mitigate | 1st-numeric-token isolation; bookend ties gate; validate() exit-2 |
| T-113-02-D | Tampering | NASBO row duplicated instead of replaced | mitigate | RPC keyed (muni, fy, 'operating'); Task 3 asserts one operating row per (AZ, fy), zero NASBO labels |
| T-113-02-E | Info Disclosure | silently inflating AZ ~2.46× without recording the divergence | mitigate | pre-load NASBO recorded; GAAP basis label verified; mechanism note (Intergovernmental) in LOADLOG (ACFR-31) |
| T-113-02-F | Repudiation | data_sources residue (WR-05 regression) | mitigate | ephemeral lifecycle verbatim; Task 3 asserts 0 'az-acfr-%' rows |
| T-113-02-G | Tampering | write to a state other than AZ | mitigate | node resolution asserts 866036ee; cohort spot-check |
| T-113-02-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both AZ loaders `--dry-run`: every present FY PASS; bookends 44,045,434K (FY2024) / 11,655,423K (FY2002).
- FY2024 durability decision executed + recorded (durable URL or caveated Drive link).
- Live: operating + revenue per loaded FY, GAAP-labelled; NASBO FY2023/FY2024 replaced in place.
- Representative-FY re-run → 0 net change; 0 'az-acfr-%' data_sources rows.
- AZ ≥1 revenue row → Money In auto-enabled.
- ~2.46× divergence recorded (ACFR-31).
- Cohort sample unchanged; 113-02-AZ-LOADLOG.md finalized.
</verification>

<success_criteria>
The Arizona state node is live on full State-ACFR GAAP across FY2002–FY2024 (honest holes logged; FY2025 correctly absent as unpublished), every FY tied + GAAP-labelled + per-year-stamped, the FY2024 durability decision executed per the locked plan (re-check → durable-or-caveated-Drive), NASBO replaced in place, the ~2.46× accept-relabel divergence recorded (ACFR-31), P2 clamp wired (ACFR-32), idempotency + 0 data_sources residue proven, Money In auto-enabled, cohort untouched. Hands AZ to Phase 115 for independent re-derivation + audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-02-SUMMARY.md` when done — record the AZ FY window actually transcribed, WAF friction encountered + how resolved, the FY2024 durability resolution, honest holes, bookend ties, NASBO-replacement confirmation, the ~2.46× divergence record, idempotency + 0-residue result, and Money-In confirmation.
</output>
