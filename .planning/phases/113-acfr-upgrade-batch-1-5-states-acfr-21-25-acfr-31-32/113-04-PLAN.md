---
phase: 113-acfr-upgrade-batch-1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processMOAcfr.js
  - scripts/processMORevenueAcfr.js
  - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-04-MO-LOADLOG.md
autonomous: true
requirements: [ACFR-24, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "Missouri state node (21892bb7-1a1d-4038-8665-51c256ab5875) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its recon-locked window FY2012–FY2025, every FY tying to the printed General Fund column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MO units are THOUSANDS: UNITS=1_000. FY2024 GF Total revenues stored = 32,756,386,000 dollars and FY2012 = 18,068,155,000"
    - "The pre-existing NASBO operating rows (MO FY2023 $12,526M, FY2024 $14,561M) are REPLACED in place by ACFR operating rows — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the mo-acfr dataset_ids"
    - "The ~2.25× scope divergence (Contributions and Intergovernmental federal passthrough $18,773,418K inside GAAP GF) is recorded at load and relabelled honestly (ACFR-31, TX precedent)"
    - "Any negative GF category year renders via the P2 clamp (ACFR-32); MO bookends showed none but 'Net Increase (Decrease) in the Fair Value of Investments' CAN go negative — checked every loaded year"
    - "Money In auto-enables on MO once revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMOAcfr.js"
      provides: "MO GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified FY2012–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMORevenueAcfr.js"
      provides: "MO GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified FY2012–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-04-MO-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, divergence record, idempotency + 0-residue result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMOAcfr.js / processMORevenueAcfr.js"
      to: "treasury.budgets (MO state node 21892bb7)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "per-year node page acct.oa.mo.gov/media/report/annual-comprehensive-financial-report-fiscal-year-ended-june-30-{YYYY}"
      to: "non-derivable PDF path under acct.oa.mo.gov/sites/g/files/zuston241/files/"
      via: "node-page scrape per year (outer pattern derivable, inner filename not)"
      pattern: "acct.oa.mo.gov"
---

<objective>
Upgrade the **Missouri** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processMOAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processMORevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — resolving each year's PDF via the derivable node-page pattern, downloading FY2012–FY2025 into gitignored `_acfr-work/mo/`, extracting the **General Fund column** (1st of 6: General Fund | Public Education | Conservation and Environmental Protection | Missouri Road Fund | Non-Major Funds | Eliminations | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

Missouri is a straightforward enumerate-and-load: no CDN issues, no unit traps, and the statement layout has been stable since at least FY2012. Two quirks: (1) the per-year node page is on `acct.oa.mo.gov` even though page links show `oa.mo.gov`; (2) the statement is immediately followed by a reconciliation-to-Statement-of-Activities schedule that must NOT be extracted.

Output: both MO loaders live-loaded across FY2012–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `113-04-MO-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md

<interfaces>
<!-- MO loaders mirror the IL loaders (multi-column layout with an Eliminations column; per-year node-page resolution). -->
From scripts/processILAcfr.js (spend-by-function analog → processMOAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='Missouri', state='MO', entity_type='state' (.single()). Node id = 21892bb7-1a1d-4038-8665-51c256ab5875 (assert match).
  - UNITS = 1_000 (thousands → dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Missouri State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES / validate / buildTree / RPC + source-stamp — copy template shape. Root n='Missouri General Fund Budget'.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01)** — copy verbatim. 0 residue.
  - data_sources.dataset_id = 'mo-acfr-gf-operating', name = 'Missouri General Fund Operating Budget', api_type='pdf_download', base_url = 'https://oa.mo.gov/accounting/reports/annual-reports/annual-comprehensive-financial-reports'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue analog → processMORevenueAcfr.js):
  - dataSource(fy) = "Missouri State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='mo-acfr-gf-revenue'; name='Missouri General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender + "(net loss — shown at 0)" negative-label path (ACFR-32).
POPULATION = 6_154_913 (from loadStateGF.mjs).
NASBO baseline (112-RECON.md Section 5): MO node has exactly 2 NASBO operating rows — FY2023 $12,526M, FY2024 $14,561M, data_source_id=null, NO revenue rows, ZERO data_sources rows. Standard supersede.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 112-BATCH1-SOURCES.md MO Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. **The immediately-following reconciliation schedule to the Statement of Activities is NOT the target** (recon initially had to reject it). GF column = 1st of 6 (General Fund | Public Education | Conservation and Environmental Protection | Missouri Road Fund | Non-Major Funds | Eliminations | Total). Take the FIRST numeric token per row.
- Units = "In Thousands of Dollars". FY-end June 30 (source_date = ${fy}-06-30). Clean window = FY2012–FY2025 (14 years; pre-FY2012 not enumerated on the current listing page — out of scope).
- URL resolution: per-year node page `https://acct.oa.mo.gov/media/report/annual-comprehensive-financial-report-fiscal-year-ended-june-30-{YYYY}` (**derivable** — confirmed FY2012 + FY2024) links to a NON-derivable PDF filename under `acct.oa.mo.gov/sites/g/files/zuston241/files/{upload-date-folder}/`. Scrape each node page. Confirmed: FY2024 `.../2025-04/2024%20ACFR%20-%20Final%20for%20Internet.pdf`; FY2012 `.../2022-12/annual-comprehensive-financial-report-fy-end06302012.pdf`.
- Domain quirk: node-page links may render as `oa.mo.gov/media/report/...` but resolve only on the `acct.oa.mo.gov` subdomain. Several plausible `oa.mo.gov` paths 301-redirect to a generic Accounting page instead of 404ing — treat an HTML redirect landing as a failed resolution, not a source.
- No CDN/WAF issues; plain fetches with a browser UA worked throughout recon.
- Bookend ties (GF Total revenues, thousands): FY2024 = 32,756,386 (diff $0); FY2012 = 18,068,155 (diff $0). Statement column structure identical FY2012 vs FY2024 (stable layout).
- Negative-line risk: none in GF bookends; "Net Increase (Decrease) in the Fair Value of Investments" went negative in the Missouri Road Fund column FY2024 (−$129,262K) — proof the line CAN go negative; check the GF column every loaded year (ACFR-32 clamp is the render path).
- Scope vs NASBO ~2.25×: "Contributions and Intergovernmental" = $18,773,418K of the $32.76B FY2024 GF total — federal passthrough inside GAAP GF. Accept-and-relabel honestly (TX precedent, ACFR-31).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve node pages → PDFs + download/extract/transcribe FY2012–FY2025 + build both loaders + dry-run tie</name>
  <files>scripts/processMOAcfr.js, scripts/processMORevenueAcfr.js, _acfr-work/mo/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (spend-by-function template incl. ephemeral data_sources lifecycle)
    - scripts/processILRevenueAcfr.js (revenue template incl. clampForRender)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (MO Detail Block: node-page pattern, domain quirk, bookend ties)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. For each FY 2012–2025, fetch the node page `https://acct.oa.mo.gov/media/report/annual-comprehensive-financial-report-fiscal-year-ended-june-30-{YYYY}` (use `acct.oa.mo.gov`, NOT `oa.mo.gov`; treat a redirect to a generic Accounting page as a failed resolution), scrape the PDF href under `sites/g/files/zuston241/files/`, and download to `_acfr-work/mo/MO{YYYY}.pdf`. Soft-404 guard: %PDF magic + size > ~500KB. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances — explicitly SKIP the immediately-following reconciliation schedule — and isolate the General Fund column (1st numeric token) for revenues + expenditures sections. Copy the IL templates → processMOAcfr.js / processMORevenueAcfr.js; set STATE_NAME='Missouri', STATE_ABBR='MO', POPULATION=6_154_913, UNITS=1_000; SOURCES FY2012–FY2025 with the exact resolved PDF URLs (source_date=`${fy}-06-30`); dataset_ids 'mo-acfr-gf-operating'/'mo-acfr-gf-revenue'; keep the ephemeral data_sources lifecycle + clampForRender verbatim. Transcribe verbatim GF line items (raw thousands) into EXPENDITURES/REVENUE; scan every year's fair-value-of-investments line for negatives and note any in a header comment. Header comment: the ~2.25× Contributions-and-Intergovernmental accept-relabel note (ACFR-31). Cross-check bookends: FY2024 Total revenues = 32,756,386 and FY2012 = 18,068,155 — mismatch = STOP and re-inspect column selection (the Eliminations column and the reconciliation schedule are the likely traps). Non-tying/non-resolving in-between FYs → OMIT as honest holes. Run both loaders --dry-run: every present FY must PASS.
  </action>
  <verify>
    <automated>node scripts/processMOAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processMORevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '32,756,386'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/mo/` holds a real PDF per resolved FY 2012–2025 (%PDF magic, >~500KB); redirect/HTML landings rejected.
    - Both loaders `--dry-run` print "validation: PASS" for every present FY; revenue `--fy 2024` shows 32,756,386 (thousands) and `--fy 2012` shows 18,068,155.
    - Both loaders have UNITS=1_000 + the ephemeral data_sources lifecycle; every SOURCES url is on `acct.oa.mo.gov`.
    - dataSource(2024) === "Missouri State ACFR — General Fund (FY2024 actual, GAAP basis)"; revenue dataset_id='mo-acfr-gf-revenue'.
    - Nothing earlier than FY2012 in SOURCES.
  </acceptance_criteria>
  <done>Both MO loaders exist on the IL template, SOURCES enumerates the resolved acct.oa.mo.gov PDF URLs FY2012–FY2025, every transcribed FY dry-run-ties with the 32,756,386K / 18,068,155K bookends, the reconciliation-schedule trap avoided, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load MO (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processMOAcfr.js, scripts/processMORevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMOAcfr.js + scripts/processMORevenueAcfr.js (--fy + source-stamp path)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + Section 8 replacement contract)
  </read_first>
  <action>
    Source .env first. Pre-write query (mcp__supabase-local__execute_sql) on the MO node (21892bb7): RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $12,526M, FY2024 $14,561M). Then per present FY (FY2012–FY2025 minus honest holes): `node scripts/processMOAcfr.js --fy <YYYY>` then `node scripts/processMORevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO rows at (21892bb7, fy, 'operating') for FY2023/FY2024, inserts net-new elsewhere; revenue inserts net-new. Stamp source_url/source_date/data_source. A year failing its tie live → abort that year, never force-write. Record loaded ACFR GF totals vs pre-load NASBO totals confirming the ~2.25× divergence mechanism (ACFR-31).
  </action>
  <verify>
    <automated>echo "MO row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, per loaded FY the MO node has an operating row "Missouri State ACFR — General Fund (FY{fy} actual, GAAP basis)" and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - MO FY2024 revenue row total = 32,756,386,000 dollars; FY2012 = 18,068,155,000.
    - MO FY2023 + FY2024 NASBO operating rows are GONE (exactly ONE operating row per (MO, fy); no "NASBO" label remains).
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~2.25× mechanism note (ACFR-31).
  </acceptance_criteria>
  <done>Each transcribed MO FY has operating + revenue rows, GAAP-labelled + per-year-sourced; NASBO FY2023/FY2024 replaced in place; divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification + LOADLOG</name>
  <files>.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-04-MO-LOADLOG.md</files>
  <read_first>
    - scripts/processMOAcfr.js (RPC keying + ephemeral lifecycle)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 7 untouched-nodes contract)
  </read_first>
  <action>
    Re-run ONE representative FY live a second time (e.g. MO --fy 2024 operating + revenue) → confirm 0 net change. DB queries: (a) every loaded MO FY has BOTH rows, GAAP-labelled, non-null sources; (b) zero remaining "NASBO" operating rows, exactly one operating row per (MO, fy); (c) honest-hole FYs absent; (d) `data_sources` has ZERO rows with dataset_id LIKE 'mo-acfr-%'; (e) MO has ≥1 revenue row (Money In); (f) cohort spot-check: 2–3 of the 19 existing ACFR nodes + one un-upgraded NASBO state unchanged. Append "Load Disposition" to 113-04-MO-LOADLOG.md: FYs loaded/skipped (+reasons), pre-load-NASBO-vs-ACFR totals with the ~2.25× note, NASBO-replacement confirmation, idempotency + 0-residue result. Do NOT touch any state other than MO.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 113-04-MO-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of a representative FY reports 0 net change.
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'mo-acfr-%'` returns 0.
    - Every loaded MO FY has both rows GAAP-labelled with sources; zero NASBO labels; one operating row per (MO, fy); honest holes absent.
    - MO has ≥1 revenue row (Money In auto-enable confirmed).
    - Cohort sample unchanged.
    - 113-04-MO-LOADLOG.md has a finalized "Load Disposition".
  </acceptance_criteria>
  <done>Idempotency + 0-residue proven; DB confirms MO fully ACFR-sourced with NASBO replaced, Money In enabled, cohort untouched; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acct.oa.mo.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the MO node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-04-A | Tampering | extracting the adjacent reconciliation-to-Statement-of-Activities schedule instead of the target statement (recon nearly did) | mitigate | Task 1 explicitly skips the following schedule; bookend ties gate; validate() exit-2 |
| T-113-04-B | Tampering | oa.mo.gov 301-redirect HTML landing treated as a source / saved as .pdf | mitigate | acct.oa.mo.gov subdomain pinned; redirect landings = failed resolution; %PDF magic + size guard |
| T-113-04-C | Tampering | wrong column (Public Education/Road Fund/Eliminations/Total instead of GF) | mitigate | 1st-numeric-token isolation; bookend ties; stable-layout fact (FY2012=FY2024 structure) |
| T-113-04-D | Tampering | negative fair-value-of-investments year rendered wrong | mitigate | clampForRender + signed-label path (ACFR-32); every year's line scanned in Task 1 |
| T-113-04-E | Tampering | NASBO row duplicated instead of replaced | mitigate | RPC keyed (muni, fy, 'operating'); Task 3 asserts one operating row per (MO, fy), zero NASBO labels |
| T-113-04-F | Repudiation | data_sources residue (WR-05 regression) | mitigate | ephemeral lifecycle verbatim; Task 3 asserts 0 'mo-acfr-%' rows |
| T-113-04-G | Tampering | write to a state other than MO | mitigate | node resolution asserts 21892bb7; cohort spot-check |
| T-113-04-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both MO loaders `--dry-run`: every present FY PASS; bookends 32,756,386K (FY2024) / 18,068,155K (FY2012).
- Live: operating + revenue per loaded FY, GAAP-labelled; NASBO FY2023/FY2024 replaced in place.
- Representative-FY re-run → 0 net change; 0 'mo-acfr-%' data_sources rows.
- MO ≥1 revenue row → Money In auto-enabled.
- ~2.25× divergence recorded (ACFR-31).
- Cohort sample unchanged; 113-04-MO-LOADLOG.md finalized.
</verification>

<success_criteria>
The Missouri state node is live on full State-ACFR GAAP across FY2012–FY2025 (honest holes logged), every FY tied + GAAP-labelled + per-year-stamped, NASBO replaced in place, the ~2.25× accept-relabel divergence recorded (ACFR-31), P2 clamp wired (ACFR-32), idempotency + 0 data_sources residue proven, Money In auto-enabled, cohort untouched. Hands MO to Phase 115 for independent re-derivation + audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-04-SUMMARY.md` when done — record the MO FY window actually transcribed, honest holes, bookend ties, NASBO-replacement confirmation, the divergence record, idempotency + 0-residue result, and Money-In confirmation.
</output>
