---
phase: 113-acfr-upgrade-batch-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processINAcfr.js
  - scripts/processINRevenueAcfr.js
  - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-01-IN-LOADLOG.md
autonomous: true
requirements: [ACFR-21, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "Indiana state node (7eb77ada-b504-4531-98cc-8262cfb22ff5) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full recon-locked window FY2002–FY2025, every FY tying to the printed General Fund column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "IN units are THOUSANDS: UNITS=1_000 (same as IL template default). FY2024 GF Total revenues stored = 22,101,900,000 dollars (22,101,900K ×1,000) and FY2002 = 7,341,746,000"
    - "The pre-existing NASBO operating rows (IN FY2023 $26,397M, FY2024 $22,405M) are REPLACED in place by ACFR operating rows (same (7eb77ada, fy, 'operating') key) — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the in-acfr dataset_ids (Phase 111 LOAD-01 ephemeral lifecycle holds under real use)"
    - "IN's ~0.99× scope parity (Medicaid lives in a separate major fund, NOT inside GF) is recorded at load against the pre-load NASBO totals (ACFR-31 honest-relabel policy — here the honest finding is near-parity)"
    - "Any negative GF category year (the 'Investment income (loss)' header implies negatives possible) renders via the P2 clamp — clamped to 0, signed magnitude in label, parent total preserved (ACFR-32)"
    - "Money In auto-enables on IN once revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processINAcfr.js"
      provides: "IN GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processINRevenueAcfr.js"
      provides: "IN GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-01-IN-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, scope-parity record, idempotency + 0-residue re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processINAcfr.js / processINRevenueAcfr.js"
      to: "treasury.budgets (IN state node 7eb77ada)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map"
      to: "www.in.gov/comptroller/files/ PDFs"
      via: "per-year explicit filenames (naming convention shifts by era — 7 distinct patterns FY2002–FY2025)"
      pattern: "in.gov/comptroller/files"
---

<objective>
Upgrade the **Indiana** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processINAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processINRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the IN ACFR/CAFR PDFs FY2002–FY2025 into gitignored `_acfr-work/in/`, extracting the **General Fund column** (1st of 6+: General Fund | Public Welfare-Medicaid Assistance Fund | US Dept of HHS Fund | Motor Vehicle Highway Fund | … | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing verbatim line items, dry-run-tying every FY to its printed General Fund total, then live-loading into `treasury.budgets` (NASBO operating replaced in place, revenue net-new) and verifying idempotency + 0 `data_sources` residue + cohort-untouched.

Indiana is the cleanest, largest state in Batch 1: no CDN issues, standard thousands units, and near-parity NASBO scope (~0.99× — Medicaid is a separate major fund, not folded into GF). The work is volume: a 24-year window with 7 distinct filename eras.

Output: both IN loaders live-loaded across FY2002–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `113-01-IN-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md

<interfaces>
<!-- The EXACT analog pattern. IN loaders mirror the IL loaders file-for-file (standard thousands units). -->
From scripts/processILAcfr.js (spend-by-function analog → processINAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='Indiana', state='IN', entity_type='state' (.single()). Node id = 7eb77ada-b504-4531-98cc-8262cfb22ff5 (assert the resolved id matches).
  - UNITS = 1_000 (IN ACFR is in thousands — same as the IL template; store dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Indiana State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES = { [fy]: { total (printed General Fund Total expenditures, thousands), confidence:'actual', categories:[{name, total}] } }.
  - validate(fy): category sum vs printed total; tight tolerance (recon tied at $0) else process.exit(2).
  - buildTree(fy): children = categories.filter(>0).map → {n, a: total*UNITS, i:[]}; root {n:'Indiana General Fund Budget', a: total*UNITS, c:children}.
  - RPC treasury_sync_budget_tree(p_data_source_id, p_fiscal_year, p_dataset_type:'operating', p_total, p_tree, p_row_count, p_triggered_by:'bulk_load') → SELECT budgets row → UPDATE {source_url, source_date, data_source}.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01 — already in the IL template):** delete-by-dataset_id → insert fresh → RPC → delete-by-id at end of run. 0 residue. Copy this verbatim.
  - data_sources.dataset_id = 'in-acfr-gf-operating', name = 'Indiana General Fund Operating Budget', api_type='pdf_download', base_url = 'https://www.in.gov/comptroller/Annual-Comprehensive-Financial-Reports'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue-by-source analog → processINRevenueAcfr.js):
  - dataSource(fy) = "Indiana State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='in-acfr-gf-revenue'; name='Indiana General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); buildTree labels a negative "<name> (net loss — shown at 0)"; root total carries the signed net (ACFR-32).
  - REVENUE = { [fy]: { total (printed General Fund Total revenues, thousands), confidence:'actual', categories:[{name, total}] } }.
POPULATION = 6_785_528 (from loadStateGF.mjs).
NASBO baseline (from 112-RECON.md Section 5): IN node has exactly 2 NASBO operating rows — FY2023 $26,397M, FY2024 $22,405M ("NASBO State Expenditure Report…" text-stamp), data_source_id=null, NO revenue rows, ZERO data_sources rows. Clean NASBO-only node — standard ACFR-replaces-NASBO supersede, no in-place-upgrade complication.
DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 112-BATCH1-SOURCES.md IN Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. NOT Statement of Activities, NOT budgetary comparison.
- GF column = 1st of 6+ (General Fund | Public Welfare-Medicaid Assistance Fund | US Dept of HHS Fund | Motor Vehicle Highway Fund | Build Indiana Fund | … | Total). Take the FIRST numeric token per row.
- Units = THOUSANDS. FY-end June 30 (source_date = ${fy}-06-30). Clean window = FY2002–FY2025 (24 years; FY2001 exists but is pre-GASB-34-boundary — do NOT load, D-12).
- URLs — all direct on `https://www.in.gov/comptroller/files/`, no CDN block, no cookies/referer needed; filename era map (from recon — verify each resolves, expect minor per-year variance):
  - FY2002–2007: `State_of_Indiana_{YYYY}_CAFR.pdf`
  - FY2008–2015: `Entire_{YYYY}_CAFR.pdf`
  - FY2016–2018: `Entire-{YYYY}-CAFR.pdf`
  - FY2019–2020: `Entire-CAFR-{YYYY}.pdf`
  - FY2021: `Entire-2021-ACFR.pdf`
  - FY2022, FY2024: `{YYYY}-ACFR.pdf`
  - FY2023: `Entire-Annual-Comprehensive-Financial-Report-2023.pdf`
  - FY2025: `Fiscal-2025-Annual-Comprehensive-Financial-Report.pdf`
  - Archive listing (fallback to resolve any filename that 404s): `https://www.in.gov/comptroller/Annual-Comprehensive-Financial-Reports/archived-annual-comprehensive-financial-reports`
- Bookend ties (General Fund column Total revenues, thousands): FY2024 = 22,101,900 (diff $0); FY2002 = 7,341,746 (diff $0).
- Negative-line risk: none observed in bookends, but the revenue line is literally headed "Investment income (loss)" — check EVERY loaded year; P2 clamp is wired as the render path for any negative (ACFR-32).
- Scope vs NASBO ~0.99× — near parity (Medicaid = separate "Public Welfare-Medicaid Assistance Fund," $15,111,031K FY2024, NOT inside GF). Smallest divergence in the whole v2.14 tranche. Record the parity honestly at load (ACFR-31 policy: honest relabel — here the honest label confirms scope match).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both IN loaders + download/extract/transcribe FY2002–FY2025 + dry-run tie</name>
  <files>scripts/processINAcfr.js, scripts/processINRevenueAcfr.js, _acfr-work/in/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template — copy its shape: loadEnv, SOURCES, EXPENDITURES, validate, buildTree, ephemeral data_sources lifecycle, RPC + source-stamp, --dry-run/--fy)
    - scripts/processILRevenueAcfr.js (the exact revenue-by-source template incl. clampForRender + negative-label path)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (IN Detail Block: per-era filenames, units, bookend ties)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already present). For each FY 2002–2025, download the IN ACFR/CAFR PDF to `_acfr-work/in/IN{YYYY}.pdf` using the era filename map in source_facts; if a guessed filename 404s, resolve the actual link from the archive listing page rather than skipping. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML page must abort that FY, not be parsed. Run `pdftotext -table` (NOT -layout) on each, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, and isolate the General Fund column (1st numeric token) for BOTH the revenues section and the expenditures section — note the column lineup can shift across 24 years (Build Indiana Fund appears/disappears); anchor on the General Fund column position per year, not a fixed column count. Copy processILAcfr.js → scripts/processINAcfr.js and processILRevenueAcfr.js → scripts/processINRevenueAcfr.js; set STATE_NAME='Indiana', STATE_ABBR='IN', POPULATION=6_785_528, UNITS=1_000. Build SOURCES FY2002–FY2025 (source_date=`${fy}-06-30`, url = the exact resolved in.gov PDF). Transcribe extracted General Fund line items (verbatim names, raw thousands) into EXPENDITURES and REVENUE, each `total` = the printed General Fund Total. Set dataSource labels + dataset_ids ('in-acfr-gf-operating' / 'in-acfr-gf-revenue') per interfaces; keep the ephemeral data_sources lifecycle verbatim from the template. Keep clampForRender + the "(net loss — shown at 0)" path; scan every transcribed year's "Investment income (loss)" value and note any negative year in a header comment. Add a header comment recording the ~0.99× scope-parity finding (Medicaid separate fund, ACFR-31). Cross-check bookends: FY2024 Total revenues = 22,101,900 thousand and FY2002 = 7,341,746 thousand — if either fails, STOP and re-inspect column selection before transcribing further. If an in-between FY fails to cleanly extract or tie, OMIT it (honest hole) and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie.
  </action>
  <verify>
    <automated>node scripts/processINAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processINRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '22,101,900'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/in/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY 2002–2025; soft-404 HTML rejected.
    - `node scripts/processINAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO tie failure; `node scripts/processINRevenueAcfr.js --dry-run --fy 2024` shows GF Total revenues 22,101,900 (thousands) and `--fy 2002` shows 7,341,746.
    - Both loaders have `UNITS = 1_000` and the ephemeral data_sources create/delete lifecycle (grep: `data_sources').delete()` appears both before insert and at end of run).
    - dataSource(2024) === "Indiana State ACFR — General Fund (FY2024 actual, GAAP basis)"; revenue loader uses p_dataset_type:'revenue', dataset_id='in-acfr-gf-revenue'.
    - FY2001 is NOT in SOURCES (pre-GASB-34 boundary honored).
  </acceptance_criteria>
  <done>Both IN loaders exist on the IL template (thousands units, ephemeral data_sources lifecycle intact), SOURCES enumerates the resolved per-era in.gov URLs FY2002–FY2025, every transcribed FY dry-run-ties to its printed General Fund total with the 22,101,900K / 7,341,746K bookends, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load IN (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processINAcfr.js, scripts/processINRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processINAcfr.js + scripts/processINRevenueAcfr.js (the --fy + post-RPC source-stamp path; confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + Section 8 replacement contract)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets (mcp__supabase-local__execute_sql) for the IN node (7eb77ada) current rows and RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $26,397M, FY2024 $22,405M — record the exact totals the query returns) — the never-overwrite baseline + scope comparator. Then for each IN FY present after Task 1 (FY2002–FY2025 minus honest holes), run `node scripts/processINAcfr.js --fy <YYYY>` then `node scripts/processINRevenueAcfr.js --fy <YYYY>` (live, no --dry-run). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (7eb77ada, fy, 'operating') for FY2023/FY2024 and inserts net-new for other FYs; revenue inserts net-new at (7eb77ada, fy, 'revenue')), and stamp source_url/source_date/data_source (GAAP basis label). If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record the loaded IN GF totals against the pre-load NASBO totals confirming the ~0.99× near-parity (ACFR-31 record).
  </action>
  <verify>
    <automated>echo "IN row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the IN node has, per loaded FY, an operating row "Indiana State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - IN FY2024 revenue row total = 22,101,900,000 dollars (22,101,900K ×1,000) and FY2002 = 7,341,746,000.
    - IN FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (IN, fy); no "NASBO" label remains on the IN node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded in the load log with the ~0.99× parity note (ACFR-31).
  </acceptance_criteria>
  <done>Each transcribed IN FY has operating + revenue rows in treasury.budgets, dollar-scaled from thousands, GAAP-basis-labelled + per-year-sourced; FY2023/FY2024 NASBO operating rows replaced in place (no duplicates); scope parity recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-01-IN-LOADLOG.md</files>
  <read_first>
    - scripts/processINAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle — the never-overwrite + 0-residue basis)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 7 untouched-nodes contract — the 19 ACFR node ids + un-upgraded NASBO states)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. IN --fy 2024 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place identical data). Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded IN FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) IN has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (IN, fy)); (c) honest-hole FYs are ABSENT; (d) **treasury.data_sources has ZERO rows with dataset_id LIKE 'in-acfr-%'** (ephemeral lifecycle held — success criterion 2); (e) Money In auto-enable — IN has ≥1 dataset_type='revenue' row; (f) cohort untouched: spot-check 2–3 of the 19 existing ACFR nodes (e.g. CA, PA, NJ) and a sample un-upgraded NASBO state are unchanged (row counts + a couple of data_source labels). Append a "Load Disposition" section to 113-01-IN-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the parity note, the NASBO-replacement confirmation, the idempotency re-run result, and the 0-residue check. Do NOT load any year beyond the tied window; do NOT touch any state other than IN.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 113-01-IN-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of IN --fy 2024 (operating + revenue) reports 0 net change (idempotent never-overwrite).
    - DB query: every loaded IN FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; IN has ZERO remaining "NASBO" operating rows; exactly one operating row per (IN, fy).
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'in-acfr-%'` returns 0 (LOAD-01 lifecycle proven under real use).
    - IN has ≥1 revenue row (Money In auto-enable confirmed).
    - Honest-hole FYs (from Task 1) are ABSENT from the DB.
    - Sample of 19 existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 113-01-IN-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 data_sources residue; the DB query confirms IN fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, cohort unchanged; 113-01-IN-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the IN node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01-A | Tampering | extracting the wrong statement/column (Statement of Activities, or Medicaid/MVH/Total instead of General Fund; column lineup shifts across 24 years) | mitigate | Task 1 isolates 1st numeric token of the Governmental Funds Statement per year (not a fixed column count); bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-113-01-B | Tampering | wrong filename era → soft-404 HTML saved as .pdf → garbage rows | mitigate | era map + archive-listing fallback; %PDF magic + size guard; non-PDF aborts that FY |
| T-113-01-C | Tampering | NASBO row left alongside the ACFR row (duplicate) / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (IN, fy), zero NASBO labels |
| T-113-01-D | Tampering | negative "Investment income (loss)" year rendered as a phantom-positive or breaking the tree total | mitigate | clampForRender + signed-label path (ACFR-32); every year's investment line scanned in Task 1 |
| T-113-01-E | Repudiation | data_sources residue accumulating (WR-05 regression) | mitigate | ephemeral lifecycle copied verbatim from post-111 IL template; Task 3 asserts 0 rows for 'in-acfr-%' |
| T-113-01-F | Tampering | write to a state other than IN (19 ACFR nodes / NASBO states) | mitigate | loader resolves only name='Indiana'+state+entity_type='state' and asserts node id 7eb77ada; Task 3 spot-checks cohort unchanged |
| T-113-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext (system tool, prior precedent) |
</threat_model>

<verification>
- Both IN loaders `--dry-run`: every present FY "validation: PASS"; bookends 22,101,900K (FY2024) / 7,341,746K (FY2002).
- Live: IN node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (IN, fy)).
- Re-run IN --fy 2024 → 0 net change; `data_sources` has 0 'in-acfr-%' rows (LOAD-01 proven).
- IN ≥1 revenue row → Money In auto-enabled.
- ~0.99× scope parity recorded against pre-load NASBO totals (ACFR-31).
- 19 existing ACFR nodes + a sample NASBO state unchanged.
- 113-01-IN-LOADLOG.md finalized.
</verification>

<success_criteria>
The Indiana state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2002–FY2025 (honest holes logged), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~0.99× scope parity recorded (ACFR-31), the P2 clamp wired (ACFR-32), idempotent never-overwrite with 0 data_sources residue proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands IN to Phase 115 for independent re-derivation + cohort audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-01-SUMMARY.md` when done — record the IN FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the NASBO-replacement confirmation, the scope-parity record, the idempotency + 0-residue re-run result, and the Money-In auto-enable confirmation.
</output>
