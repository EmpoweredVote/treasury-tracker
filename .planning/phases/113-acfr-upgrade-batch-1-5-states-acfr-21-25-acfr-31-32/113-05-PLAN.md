---
phase: 113-acfr-upgrade-batch-1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processCOAcfr.js
  - scripts/processCORevenueAcfr.js
  - .planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-05-CO-LOADLOG.md
autonomous: true
requirements: [ACFR-25, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "Colorado state node (89d2aff1-6980-4c20-80fe-513618bce8ac) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its recon-locked window FY2023–FY2025, every FY tying to the printed General Funds column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "CO units are THOUSANDS: UNITS=1_000. FY2024 GF Total revenues stored = 26,271,588,000 dollars"
    - "The FY2024 'TABOR Excess Revenue' line (−$1,214,908K, a standalone NEGATIVE revenue line) renders via the P2 clamp — clamped to 0 with the signed magnitude in the label, and the ROOT total carries the signed net so the printed Total Revenues still ties exactly (ACFR-32 — this is the tranche's primary live exercise of the clamp)"
    - "Every loaded year is checked for BOTH TABOR presentation forms: a standalone negative line AND/OR the refund netted into Individual Income Tax (the FY2023 form) — the check is not just for a line labeled 'TABOR'"
    - "The pre-existing NASBO operating rows (CO FY2023 $13,647M, FY2024 $14,513M) are REPLACED in place by ACFR operating rows — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the co-acfr dataset_ids"
    - "The ~1.81× scope divergence (Federal Grants and Contracts $9,692,569K inside GAAP GF) is recorded at load and relabelled honestly (ACFR-31)"
    - "Money In auto-enables on CO once revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processCOAcfr.js"
      provides: "CO GF spending-by-function loader (dataset_type='operating'), UNITS=1000, Referer-header fetch, tie-verified FY2023–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processCORevenueAcfr.js"
      provides: "CO GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, P2 clamp exercised on TABOR, tie-verified FY2023–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-05-CO-LOADLOG.md"
      provides: "Per-FY load disposition, TABOR-form check per year, NASBO-replacement confirmation, divergence record, idempotency + 0-residue result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processCOAcfr.js / processCORevenueAcfr.js"
      to: "treasury.budgets (CO state node 89d2aff1)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "every osc.colorado.gov fetch"
      to: "the ACFR PDFs"
      via: "mandatory Referer header = https://osc.colorado.gov/financial-operations/financial-reports/acfr (403 without it)"
      pattern: "Referer"
---

<objective>
Upgrade the **Colorado** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template (recon maps CO closest to the PA mold — regular fund-column layout, explicit SOURCES map) — `scripts/processCOAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processCORevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the CO ACFR PDFs FY2023–FY2025 into gitignored `_acfr-work/co/` (every fetch carries the mandatory `Referer` header), extracting the **General Funds column** (plural — 1st of 4: General Funds | Federal Special Revenue Fund | Highway Users Tax Fund | Other Governmental Funds | Total) of the Governmental Funds *Statement Of Revenues, Expenditures, And Changes In Fund Balances* via `pdftotext -table`, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + 0 residue + cohort-untouched.

**Colorado is the tranche's live P2-clamp exercise (ACFR-32).** FY2024 has a standalone negative revenue line — "TABOR Excess Revenue" = −$1,214,908K (the constitutional refund mechanism). It must render clamped-to-0 with the signed magnitude in the label while the root total carries the signed net so the printed Total Revenues ties exactly. TABOR's presentation varies by year (FY2023 netted it into Individual Income Tax instead) — every loaded year is checked for both forms. The window is honestly shallow (3 years — pre-FY2023 vanished in a site migration).

Output: both CO loaders live-loaded across FY2023–FY2025, TABOR handled per-form, NASBO replaced in place, Money In auto-enabled; `113-05-CO-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md

<interfaces>
<!-- CO loaders follow the IL/PA template shape (recon maps CO → PA mold; the IL and PA loaders share the same structure — use whichever reads cleaner, cite in a header comment). -->
From scripts/processILAcfr.js or scripts/processPAAcfr.js (spend-by-function analog → processCOAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='Colorado', state='CO', entity_type='state' (.single()). Node id = 89d2aff1-6980-4c20-80fe-513618bce8ac (assert match).
  - UNITS = 1_000 (thousands → dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Colorado State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES / validate / buildTree / RPC + source-stamp — copy template shape. Root n='Colorado General Fund Budget'.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01)** — copy verbatim. 0 residue.
  - data_sources.dataset_id = 'co-acfr-gf-operating', name = 'Colorado General Fund Operating Budget', api_type='pdf_download', base_url = 'https://osc.colorado.gov/financial-operations/financial-reports/acfr'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js / processPARevenueAcfr.js (revenue analog → processCORevenueAcfr.js):
  - dataSource(fy) = "Colorado State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='co-acfr-gf-revenue'; name='Colorado General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); a negative category renders "<name> (net refund — shown at 0; actual −$X)"-style signed label per the established "(net loss — shown at 0)" pattern; the ROOT total carries the signed net (ACFR-32). For CO the TABOR line is the live case: child clamped to 0 with the −$1,214,908K magnitude in the label, root total = printed 26,271,588K.
POPULATION = 5_773_714 (from loadStateGF.mjs).
NASBO baseline (112-RECON.md Section 5): CO node has exactly 2 NASBO operating rows — FY2023 $13,647M, FY2024 $14,513M, data_source_id=null, NO revenue rows, ZERO data_sources rows. Standard supersede.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 112-BATCH1-SOURCES.md CO Detail Block — USE these, do not re-research. -->
- Statement: Statement Of Revenues, Expenditures, And Changes In Fund Balances — Governmental Funds. **The adjacent "…Reconciled To Statement Of Activities" schedule on the following page is NOT the target** (recon initially mistook it). GF column = 1st of 4, header literally "General Funds" (plural — cosmetic quirk, same concept). Take the FIRST numeric token per row.
- Units = THOUSANDS. FY-end June 30 (source_date = ${fy}-06-30). Clean window = FY2023–FY2025 ONLY (3 years — pre-FY2023 not hosted under the current osc.colorado.gov domain; site-migration gap logged in recon; D-12 permits the shallow window).
- URLs (explicit, no derivable pattern):
  - FY2023: `https://osc.colorado.gov/sites/osc/files/acfr23.pdf`
  - FY2024: `https://osc.colorado.gov/sites/osc/files/documents/FY2024%20ACFR%20Final%20-%20Color%20Corrected_ADA.pdf`
  - FY2025: `https://osc.colorado.gov/sites/osc/files/documents/FY2025%20ACFR_ADA_1.30.26.pdf`
- **Access: EVERY fetch to osc.colorado.gov requires a `Referer` header matching `https://osc.colorado.gov/financial-operations/financial-reports/acfr`** — direct requests without it return 403 (mild WAF rule; no cookies/session needed, much less severe than AZ).
- Bookend ties: FY2024 GF Total Revenues = 26,271,588 thousand (diff $0, extracted at recon). FY2023 PDF confirmed live + downloadable (55MB) but NOT text-extracted at recon — its tie happens fresh at load; FY2025 confirmed present, also extracted fresh at load.
- **TABOR (the ACFR-32 live case):** FY2024 General Funds column contains "TABOR Excess Revenue" = −$1,214,908K, a standalone negative revenue line. FY2023 has NO standalone line — the refund was netted into Individual Income Tax revenue that year. Presentation varies by year: check EVERY loaded year for (a) a standalone negative line (any label, not just "TABOR") and (b) the netted-into-tax-revenue form (note it in the LOADLOG when found; no clamp needed for the netted form since the line stays positive).
- Scope vs NASBO ~1.81×: "Federal Grants and Contracts" = $9,692,569K of the $26.27B FY2024 GF total — federal passthrough inside GAAP GF. Accept-and-relabel honestly (TX precedent, ACFR-31).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both CO loaders (Referer fetch + TABOR clamp) + download/extract/transcribe FY2023–FY2025 + dry-run tie</name>
  <files>scripts/processCOAcfr.js, scripts/processCORevenueAcfr.js, _acfr-work/co/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (spend-by-function template incl. ephemeral data_sources lifecycle)
    - scripts/processILRevenueAcfr.js (revenue template incl. clampForRender + negative-label path — the TABOR render path)
    - scripts/processPAAcfr.js (the recon-mapped closest mold — compare shape before choosing the base)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (CO Detail Block: 3 exact URLs, Referer rule, TABOR forms, bookend tie)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored. Download the 3 CO ACFR PDFs to `_acfr-work/co/CO{YYYY}.pdf` using the EXACT URLs in source_facts, sending `Referer: https://osc.colorado.gov/financial-operations/financial-reports/acfr` on every request (403 without it). Soft-404 guard: %PDF magic + size > ~500KB. Run `pdftotext -table` on each, locate the Statement Of Revenues, Expenditures, And Changes In Fund Balances — Governmental Funds — explicitly SKIP the "…Reconciled To Statement Of Activities" schedule on the following page — and isolate the **General Funds** column (1st numeric token; header is plural) for revenues + expenditures sections. Copy the IL (or PA — whichever reads cleaner, note the choice) templates → processCOAcfr.js / processCORevenueAcfr.js; set STATE_NAME='Colorado', STATE_ABBR='CO', POPULATION=5_773_714, UNITS=1_000; SOURCES = exactly FY2023–FY2025 (source_date=`${fy}-06-30`); dataset_ids 'co-acfr-gf-operating'/'co-acfr-gf-revenue'; keep the ephemeral data_sources lifecycle + clampForRender verbatim. Transcribe verbatim General-Funds line items (raw thousands, INCLUDING negative values with their sign — e.g. FY2024 TABOR Excess Revenue = -1214908) into EXPENDITURES/REVENUE; `total` = the printed General Funds Total (the signed sum must tie — validate() operates on signed values, clamping is render-only). For EVERY year record the TABOR form found: standalone negative line (clamp path) vs netted-into-Individual-Income-Tax (no clamp; note it). Header comment: the ~1.81× Federal-Grants accept-relabel note (ACFR-31) + the TABOR-form-per-year table. Cross-check the extracted bookend: FY2024 Total revenues = 26,271,588 — mismatch = STOP and re-inspect column selection (the reconciliation schedule and the plural-header quirk are the traps). FY2023 and FY2025 tie fresh against their own printed totals. Run both loaders --dry-run: all 3 FYs must PASS, and the FY2024 revenue dry-run must show the TABOR line clamped in the tree (label carries the signed magnitude) while the root total remains the printed 26,271,588K.
  </action>
  <verify>
    <automated>node scripts/processCOAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processCORevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '26,271,588' && grep -q 'TABOR' scripts/processCORevenueAcfr.js</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/co/` holds 3 real PDFs (%PDF magic, >~500KB) fetched with the Referer header; 403/HTML landings rejected.
    - Both loaders `--dry-run` print "validation: PASS" for all 3 FYs; revenue `--fy 2024` root total shows 26,271,588 (thousands) with the TABOR child clamped to 0 and its label carrying the −1,214,908K magnitude.
    - REVENUE[2024].categories contains the TABOR Excess Revenue entry with a NEGATIVE total (signed transcription); validate() ties on signed values.
    - Both loaders have UNITS=1_000 + the ephemeral data_sources lifecycle; SOURCES has exactly 3 entries (2023, 2024, 2025).
    - The fetch path sends the Referer header (grep: `Referer` appears in the download code or documented fetch recipe).
    - dataSource(2024) === "Colorado State ACFR — General Fund (FY2024 actual, GAAP basis)"; revenue dataset_id='co-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both CO loaders exist, all 3 FYs dry-run-tie on signed values with the FY2024 26,271,588K bookend, the TABOR negative line is transcribed signed + rendered clamped with the signed label (ACFR-32 exercised), the per-year TABOR-form check is recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load CO (operating + revenue) FY2023–FY2025, NASBO replaced in place</name>
  <files>(runs scripts/processCOAcfr.js, scripts/processCORevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processCOAcfr.js + scripts/processCORevenueAcfr.js (--fy + source-stamp path)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + Section 8 replacement contract)
  </read_first>
  <action>
    Source .env first. Pre-write query (mcp__supabase-local__execute_sql) on the CO node (89d2aff1): RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $13,647M, FY2024 $14,513M). Then per FY 2023–2025: `node scripts/processCOAcfr.js --fy <YYYY>` then `node scripts/processCORevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO rows at (89d2aff1, fy, 'operating') for FY2023/FY2024, inserts net-new for FY2025; revenue inserts net-new all 3 years. Stamp source_url/source_date/data_source. A year failing its tie live → abort that year, never force-write. Record loaded ACFR GF totals vs pre-load NASBO totals confirming the ~1.81× divergence mechanism (ACFR-31), and confirm the stored FY2024 revenue tree carries the clamped TABOR child + signed root.
  </action>
  <verify>
    <automated>echo "CO row existence + NASBO-replacement + TABOR render + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, per FY 2023–2025 the CO node has an operating row "Colorado State ACFR — General Fund (FY{fy} actual, GAAP basis)" and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - CO FY2024 revenue row total = 26,271,588,000 dollars (signed net incl. TABOR), and its stored tree contains the TABOR child clamped to 0 with the signed-magnitude label.
    - CO FY2023 + FY2024 NASBO operating rows are GONE (exactly ONE operating row per (CO, fy); no "NASBO" label remains).
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.81× mechanism note (ACFR-31).
  </acceptance_criteria>
  <done>All 3 CO FYs have operating + revenue rows, GAAP-labelled + per-year-sourced; the TABOR clamp is live in the stored FY2024 tree; NASBO FY2023/FY2024 replaced in place; divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification + LOADLOG</name>
  <files>.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-05-CO-LOADLOG.md</files>
  <read_first>
    - scripts/processCOAcfr.js (RPC keying + ephemeral lifecycle)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 7 untouched-nodes contract)
  </read_first>
  <action>
    Re-run ONE representative FY live a second time (e.g. CO --fy 2024 operating + revenue) → confirm 0 net change (the clamped TABOR tree must be byte-identical on re-run — clamp determinism is part of idempotency). DB queries: (a) all 3 CO FYs have BOTH rows, GAAP-labelled, non-null sources; (b) zero remaining "NASBO" operating rows, exactly one operating row per (CO, fy); (c) no pre-FY2023 rows; (d) `data_sources` has ZERO rows with dataset_id LIKE 'co-acfr-%'; (e) CO has ≥1 revenue row (Money In); (f) cohort spot-check: 2–3 of the 19 existing ACFR nodes + one un-upgraded NASBO state unchanged. Append "Load Disposition" to 113-05-CO-LOADLOG.md: 3 FYs loaded, the per-year TABOR-form table (standalone-negative vs netted), the pre-FY2023 site-migration exclusion note, pre-load-NASBO-vs-ACFR totals with the ~1.81× note, NASBO-replacement confirmation, idempotency + 0-residue result. Do NOT touch any state other than CO.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 113-05-CO-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of a representative FY reports 0 net change (clamped tree identical).
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'co-acfr-%'` returns 0.
    - All 3 CO FYs have both rows GAAP-labelled with sources; zero NASBO labels; one operating row per (CO, fy); no pre-FY2023 rows.
    - CO has ≥1 revenue row (Money In auto-enable confirmed).
    - Cohort sample unchanged.
    - 113-05-CO-LOADLOG.md has a finalized "Load Disposition" incl. the per-year TABOR-form table.
  </acceptance_criteria>
  <done>Idempotency + 0-residue proven (clamp deterministic); DB confirms CO fully ACFR-sourced FY2023–FY2025 with NASBO replaced, TABOR honestly rendered, Money In enabled, cohort untouched; LOADLOG finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| osc.colorado.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the CO node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05-A | Tampering | **TABOR negative line dropped, sign-flipped, or silently absorbed → FY2024 total off by ~$2.4B or a dishonest tree** | mitigate | signed transcription required; validate() ties on signed values; render-only clamp with signed label (ACFR-32); dry-run + stored-tree assertions |
| T-113-05-B | Tampering | missing the netted-TABOR form in FY2023/FY2025 (checking only for a line labeled "TABOR") | mitigate | per-year two-form check mandated; LOADLOG table records the form found each year |
| T-113-05-C | Tampering | extracting the "…Reconciled To Statement Of Activities" schedule (recon initially mistook it) | mitigate | Task 1 explicitly skips it; bookend tie gates; validate() exit-2 |
| T-113-05-D | Tampering | 403 HTML (missing Referer) saved as .pdf | mitigate | Referer header on every fetch; %PDF magic + size guard |
| T-113-05-E | Tampering | NASBO row duplicated instead of replaced | mitigate | RPC keyed (muni, fy, 'operating'); Task 3 asserts one operating row per (CO, fy), zero NASBO labels |
| T-113-05-F | Info Disclosure | silently inflating CO ~1.81× without recording the divergence | mitigate | pre-load NASBO recorded; GAAP basis label verified; mechanism note in LOADLOG (ACFR-31) |
| T-113-05-G | Repudiation | data_sources residue (WR-05 regression) | mitigate | ephemeral lifecycle verbatim; Task 3 asserts 0 'co-acfr-%' rows |
| T-113-05-H | Tampering | write to a state other than CO | mitigate | node resolution asserts 89d2aff1; cohort spot-check |
| T-113-05-SC | Tampering | npm/pip installs | accept | no new packages |
</threat_model>

<verification>
- Both CO loaders `--dry-run`: all 3 FYs PASS on signed values; FY2024 bookend 26,271,588K; TABOR child clamped with signed label, root carries signed net.
- Live: operating + revenue per FY 2023–2025, GAAP-labelled; NASBO FY2023/FY2024 replaced in place; stored FY2024 tree carries the clamped TABOR child.
- Representative-FY re-run → 0 net change; 0 'co-acfr-%' data_sources rows.
- CO ≥1 revenue row → Money In auto-enabled.
- ~1.81× divergence recorded (ACFR-31); per-year TABOR-form table in LOADLOG.
- Cohort sample unchanged; 113-05-CO-LOADLOG.md finalized.
</verification>

<success_criteria>
The Colorado state node is live on full State-ACFR GAAP across its honest recon-locked window FY2023–FY2025, every FY tied on signed values + GAAP-labelled + per-year-stamped, the FY2024 TABOR Excess Revenue negative line rendered via the P2 clamp with the signed magnitude preserved and the root total tying to the printed statement (ACFR-32's primary live exercise), both TABOR presentation forms checked every year, NASBO replaced in place, the ~1.81× divergence recorded (ACFR-31), idempotency + 0 data_sources residue proven, Money In auto-enabled, cohort untouched. Hands CO to Phase 115 for independent re-derivation + audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/113-acfr-upgrade-batch-1-5-states-acfr-21-25-acfr-31-32/113-05-SUMMARY.md` when done — record the 3 FYs loaded, the per-year TABOR-form findings + clamp render confirmation, bookend tie, NASBO-replacement confirmation, the divergence record, idempotency + 0-residue result, and Money-In confirmation.
</output>
