---
phase: 114-acfr-upgrade-batch-2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processSCAcfr.js
  - scripts/processSCRevenueAcfr.js
  - .planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-01-SC-LOADLOG.md
autonomous: true
requirements: [ACFR-26, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "South Carolina state node (f0024b19-1b89-4bdf-af47-d2e28c21278f) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its recon-locked window FY2002–FY2025, every FY tying to the printed General Fund column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "SC units are THOUSANDS: UNITS=1_000. FY2025 GF Total revenues stored = 20,731,521,000 dollars (20,731,521K ×1,000) and FY2002 = 5,763,261,000"
    - "The pre-existing NASBO operating rows (SC FY2023 $12,089M, FY2024 $14,189M) are REPLACED in place by ACFR operating rows (same (f0024b19, fy, 'operating') key) — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the sc-acfr dataset_ids"
    - "SC's ~1.46× scope divergence is recorded at load against the pre-load NASBO totals with its DRIVER documented: GAAP-vs-budgetary basis consolidation, NOT federal passthrough (federal inside GF is only ~$46M) — unlike most other Batch-2 states (ACFR-31 honest relabel)"
    - "Any negative GF category year renders via the P2 clamp — clamped to 0, signed magnitude in label, parent total preserved (ACFR-32)"
    - "Money In auto-enables on SC once revenue rows land (data-driven, no frontend change)"
    - "FY1993–FY2001 are NOT loaded (pre-GASB-34 boundary at FY2002 for this tranche — deeper SC history is Phase 115 extractor territory)"
  artifacts:
    - path: "scripts/processSCAcfr.js"
      provides: "SC GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processSCRevenueAcfr.js"
      provides: "SC GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-01-SC-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, scope-divergence record (basis-driver), idempotency + 0-residue re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processSCAcfr.js / processSCRevenueAcfr.js"
      to: "treasury.budgets (SC state node f0024b19)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map"
      to: "cg.sc.gov ACFR archive PDFs"
      via: "per-year explicit URLs enumerated from the landing page (naming shifts by era; FY2025 statement lives in a split part-PDF)"
      pattern: "cg.sc.gov"
---

<objective>
Upgrade the **South Carolina** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processSCAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processSCRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the SC ACFR/CAFR PDFs FY2002–FY2025 into gitignored `_acfr-work/sc/`, extracting the **General Fund column** (1st of 5: General Fund | Departmental Program Services | Local Government Infrastructure | DOT Special Revenue | Nonmajor Governmental | Totals; FY2002 had a 6th State Tobacco Settlement column) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* (Exhibit B-2) via `pdftotext -table` + `_acfr-work/extract_gf.py`, dry-run-tying every FY to its printed General Fund total, then live-loading into `treasury.budgets` (NASBO operating replaced in place, revenue net-new) and verifying idempotency + 0 `data_sources` residue + cohort-untouched.

SC is the deepest-archive state in Batch 2 (full run live back to FY1993), but this tranche stops at the FY2002 pre-GASB-34 boundary — 24 years. The one structural trap: **the FY2025 ACFR is split into 9 part-PDFs; the target statement lives in `039-191-ACFR-FY2025-BasicFinancialStatements.pdf`**, not a combined file.

Output: both SC loaders live-loaded across FY2002–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `114-01-SC-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md

<interfaces>
<!-- The EXACT analog pattern. SC loaders mirror the IL loaders file-for-file (standard thousands units). -->
From scripts/processILAcfr.js (spend-by-function analog → processSCAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='South Carolina', state='SC', entity_type='state' (.single()). Node id = f0024b19-1b89-4bdf-af47-d2e28c21278f (assert the resolved id matches).
  - UNITS = 1_000 (SC ACFR is in thousands; store dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "South Carolina State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES = { [fy]: { total (printed General Fund Total expenditures, thousands), confidence:'actual', categories:[{name, total}] } }.
  - validate(fy): category sum vs printed total; tight tolerance (recon tied at $0) else process.exit(2).
  - buildTree(fy): children = categories.filter(>0).map → {n, a: total*UNITS, i:[]}; root {n:'South Carolina General Fund Budget', a: total*UNITS, c:children}.
  - RPC treasury_sync_budget_tree(p_data_source_id, p_fiscal_year, p_dataset_type:'operating', p_total, p_tree, p_row_count, p_triggered_by:'bulk_load') → SELECT budgets row → UPDATE {source_url, source_date, data_source}.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01):** delete-by-dataset_id → insert fresh → RPC → delete-by-id at end of run. 0 residue. Copy verbatim.
  - data_sources.dataset_id = 'sc-acfr-gf-operating', name = 'South Carolina General Fund Operating Budget', api_type='pdf_download', base_url = 'https://cg.sc.gov/financial-reports/annual-comprehensive-financial-reports-acfrs'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue-by-source analog → processSCRevenueAcfr.js):
  - dataSource(fy) = "South Carolina State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='sc-acfr-gf-revenue'; name='South Carolina General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); buildTree labels a negative "<name> (net loss — shown at 0)"; root total carries the signed net (ACFR-32).
Tooling (Phase 113 shortcut — reuse, do not rebuild): `_acfr-work/extract_gf.py` (GF-column extractor over `pdftotext -table` output: label + FIRST numeric token per leaf row, parens=negative, JSON out with per-section totals) and `_acfr-work/gen_state.py` (generates both process{ST}Acfr.js/process{ST}RevenueAcfr.js from `{st}_all.json` + a CONFIGS entry — add an 'SC' config with node id, population, years, per-year sources, head_note documenting the scope finding).
POPULATION = 5_118_425 (from loadStateGF.mjs).
NASBO baseline (from 112-RECON.md Section 5): SC node has exactly 2 NASBO operating rows — FY2023 $12,089M, FY2024 $14,189M, data_source_id=null, NO revenue rows, ZERO data_sources rows. Clean NASBO-only node — standard ACFR-replaces-NASBO supersede.
DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 112-BATCH2-SOURCES.md SC Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — GOVERNMENTAL FUNDS (Exhibit B-2). NOT Statement of Activities, NOT budgetary comparison.
- GF column = 1st of 5 ("General Fund"): General Fund | Departmental Program Services | Local Government Infrastructure | DOT Special Revenue | Nonmajor Governmental | Totals. FY2002 had a 6th column (State Tobacco Settlement, since folded elsewhere) — anchor on the GF column position (1st), not a fixed column count.
- Units = THOUSANDS. FY-end June 30 (source_date = ${fy}-06-30). Clean window for this tranche = FY2002–FY2025 (24 years). SC's archive goes back to FY1993 but FY2002 is the declared pre-GASB-34 boundary — do NOT load FY1993–FY2001 (Phase 115 territory, D-12).
- URLs — all on `cg.sc.gov`, enumerable from the landing page `https://cg.sc.gov/financial-reports/annual-comprehensive-financial-reports-acfrs`; naming shifts by era (`SC%20FY%20{YYYY}%20CAFR.pdf` 2000s-era, `001-316-ACFR-FY2024.pdf` single-file FY2019–2024, split part-files FY2025). Confirmed anchors:
  - FY2025 (SPLIT-FILE — target this part specifically): `https://cg.sc.gov/sites/cg/files/Documents/Financial%20Reports/Annual%20Comprehensive%20Financial%20Reports%20(ACFRs)/Annual%20Comprehensive%20Financial%20Reports%20(2010%20-%202019)/ACFR%20Current%20Year/039-191-ACFR-FY2025-BasicFinancialStatements.pdf`
  - FY2024: `https://cg.sc.gov/sites/cg/files/Documents/Publications%20and%20Reports/Annual%20Accountability%20Reports/001-316-ACFR-FY2024.pdf`
  - FY2023: `.../001-308-CAFR-FY2023.pdf`
  - FY2002 (bookend): `https://cg.sc.gov/sites/cg/files/Documents/Financial%20Reports/Annual%20Comprehensive%20Financial%20Reports%20(ACFRs)/Annual%20Comprehensive%20Financial%20Reports%20(2000%20-%202009)/SC%20FY%202002%20CAFR.pdf`
  - FY2019 has TWO live URL variants — pick the canonical spaced-filename variant (`001 - 302 - CAFR - FY 2019.pdf`).
- Bookend ties (General Fund column Total revenues, thousands): FY2025 = 20,731,521 (diff $0); FY2002 = 5,763,261 (diff $0).
- Negative-line risk: none observed in bookends ("Interest and other investment income" positive both). NOTE: FY2002 GF ENDING FUND BALANCE was a deficit $(139,951)K — a structural fact for the loader header comment, NOT a revenue-line P2 clamp issue. Still scan every transcribed year; the clamp path stays wired (ACFR-32).
- Scope vs NASBO ~1.46× ($20.73B ACFR FY2025 vs $14.19B NASBO FY2024). DRIVER IS UNUSUAL: federal revenue inside the GF column is only ~$46M — the divergence is GAAP-vs-budgetary basis consolidation (transfers/interest/tobacco-adjacent revenue), NOT federal passthrough. Document this differing driver explicitly (ACFR-31 honest relabel, TX precedent).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both SC loaders + download/extract/transcribe FY2002–FY2025 + dry-run tie</name>
  <files>scripts/processSCAcfr.js, scripts/processSCRevenueAcfr.js, _acfr-work/sc/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template: loadEnv, SOURCES, EXPENDITURES, validate, buildTree, ephemeral data_sources lifecycle, RPC + source-stamp, --dry-run/--fy)
    - scripts/processILRevenueAcfr.js (the exact revenue-by-source template incl. clampForRender + negative-label path)
    - _acfr-work/extract_gf.py + _acfr-work/gen_state.py (the Phase 113 extraction/generation tooling — reuse, do not rebuild)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH2-SOURCES.md (SC Detail Block: URLs, part-file trap, units, bookend ties)
  </read_first>
  <action>
    Enumerate the per-year SC PDF URLs FY2002–FY2025 from the cg.sc.gov landing page (era naming shifts; FY2019 = the canonical spaced-filename variant; FY2025 = the `039-191-ACFR-FY2025-BasicFinancialStatements.pdf` part-file, NOT a combined ACFR). Download each to `_acfr-work/sc/SC{YYYY}.pdf`. Soft-404 guard: assert %PDF magic bytes + size > ~500KB — a saved HTML page aborts that FY, never parsed. Run `pdftotext -table` on each, locate Exhibit B-2 (Statement of Revenues, Expenditures, and Changes in Fund Balances — GOVERNMENTAL FUNDS), and extract the General Fund column (1st numeric token per row) for BOTH revenues and expenditures via `_acfr-work/extract_gf.py` into `_acfr-work/sc/sc_all.json` — column lineup shifts across 24 years (FY2002 has the extra State Tobacco Settlement column); anchor on GF column position per year, not a fixed count. Add an 'SC' CONFIGS entry to `_acfr-work/gen_state.py` (name='South Carolina', abbr='SC', population=5_118_425, node='f0024b19-1b89-4bdf-af47-d2e28c21278f', years 2002–2025, per-year sources map, head_note recording the ~1.46× basis-driver scope finding + the FY2002 GF ending-fund-balance-deficit structural note) and generate `scripts/processSCAcfr.js` + `scripts/processSCRevenueAcfr.js`; set dataSource labels + dataset_ids ('sc-acfr-gf-operating' / 'sc-acfr-gf-revenue') per interfaces; keep the ephemeral data_sources lifecycle + clampForRender + "(net loss — shown at 0)" path verbatim from the template. source_date = `${fy}-06-30`. Cross-check bookends: FY2025 Total revenues = 20,731,521 thousand and FY2002 = 5,763,261 thousand — if either fails, STOP and re-inspect column selection before transcribing further. If an in-between FY fails to cleanly extract or tie, OMIT it (honest hole) and note it. Do NOT include any year before FY2002 in SOURCES. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie.
  </action>
  <verify>
    <automated>node scripts/processSCAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processSCRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '20,731,521'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/sc/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY 2002–2025; the FY2025 file is the BasicFinancialStatements part-PDF; soft-404 HTML rejected.
    - `node scripts/processSCAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO tie failure; `node scripts/processSCRevenueAcfr.js --dry-run --fy 2025` shows GF Total revenues 20,731,521 (thousands) and `--fy 2002` shows 5,763,261.
    - Both loaders have `UNITS = 1_000` and the ephemeral data_sources create/delete lifecycle (delete appears both before insert and at end of run).
    - dataSource(2025) === "South Carolina State ACFR — General Fund (FY2025 actual, GAAP basis)"; revenue loader uses p_dataset_type:'revenue', dataset_id='sc-acfr-gf-revenue'.
    - No FY earlier than 2002 in SOURCES (pre-GASB-34 boundary honored despite the FY1993+ archive).
  </acceptance_criteria>
  <done>Both SC loaders exist on the IL template (thousands units, ephemeral data_sources lifecycle intact), SOURCES enumerates the resolved per-era cg.sc.gov URLs FY2002–FY2025 (FY2025 = part-file), every transcribed FY dry-run-ties to its printed General Fund total with the 20,731,521K / 5,763,261K bookends, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load SC (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processSCAcfr.js, scripts/processSCRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processSCAcfr.js + scripts/processSCRevenueAcfr.js (the --fy + post-RPC source-stamp path; confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + untouched-nodes contract)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets (mcp__supabase-local__execute_sql) for the SC node (f0024b19) current rows and RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $12,089M, FY2024 $14,189M — record the exact totals the query returns). Then for each SC FY present after Task 1 (FY2002–FY2025 minus honest holes), run `node scripts/processSCAcfr.js --fy <YYYY>` then `node scripts/processSCRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (f0024b19, fy, 'operating') for FY2023/FY2024, net-new otherwise; revenue net-new), and stamp source_url/source_date/data_source (GAAP basis label). If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record the loaded SC GF totals against the pre-load NASBO totals confirming the ~1.46× divergence WITH the basis-consolidation (not federal-passthrough) driver note (ACFR-31 record).
  </action>
  <verify>
    <automated>echo "SC row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the SC node has, per loaded FY, an operating row "South Carolina State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - SC FY2025 revenue row total = 20,731,521,000 dollars and FY2002 = 5,763,261,000.
    - SC FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (SC, fy); no "NASBO" label remains on the SC node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded in the load log with the ~1.46× basis-driver note (ACFR-31).
  </acceptance_criteria>
  <done>Each transcribed SC FY has operating + revenue rows in treasury.budgets, dollar-scaled from thousands, GAAP-basis-labelled + per-year-sourced; FY2023/FY2024 NASBO operating rows replaced in place (no duplicates); scope divergence + driver recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-01-SC-LOADLOG.md</files>
  <read_first>
    - scripts/processSCAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (untouched-nodes contract — the now-24 ACFR node cohort + un-upgraded NASBO states)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. SC --fy 2024 operating + revenue) and confirm 0 net change. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded SC FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) SC has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (SC, fy)); (c) honest-hole FYs are ABSENT; (d) treasury.data_sources has ZERO rows with dataset_id LIKE 'sc-acfr-%'; (e) Money In auto-enable — SC has ≥1 dataset_type='revenue' row; (f) cohort untouched: spot-check 2–3 of the 24 existing ACFR nodes (e.g. IN from Phase 113, CA, PA) and a sample un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 114-01-SC-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the ~1.46× basis-driver note, the NASBO-replacement confirmation, the idempotency re-run result, and the 0-residue check. Do NOT load any year beyond the tied window; do NOT touch any state other than SC.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 114-01-SC-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of SC --fy 2024 (operating + revenue) reports 0 net change (idempotent never-overwrite).
    - DB query: every loaded SC FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; SC has ZERO remaining "NASBO" operating rows; exactly one operating row per (SC, fy).
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'sc-acfr-%'` returns 0.
    - SC has ≥1 revenue row (Money In auto-enable confirmed).
    - Honest-hole FYs (from Task 1) are ABSENT from the DB; no FY earlier than 2002 present.
    - Sample of the 24 existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 114-01-SC-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 data_sources residue; the DB query confirms SC fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, cohort unchanged; 114-01-SC-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cg.sc.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the SC node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-01-A | Tampering | extracting the wrong statement/column (Statement of Activities, or a non-GF fund column; FY2002 has an extra Tobacco Settlement column) | mitigate | Task 1 isolates 1st numeric token of Exhibit B-2 per year (not a fixed column count); bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-114-01-B | Tampering | FY2025 combined-ACFR assumption → wrong/missing file (statement lives ONLY in the BasicFinancialStatements part-PDF) | mitigate | SOURCES pins the part-file URL explicitly; %PDF magic + size guard; non-PDF aborts that FY |
| T-114-01-C | Tampering | NASBO row left alongside the ACFR row (duplicate) / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (SC, fy), zero NASBO labels |
| T-114-01-D | Tampering | negative GF category year rendered as phantom-positive or breaking the tree total | mitigate | clampForRender + signed-label path (ACFR-32); every year's investment line scanned in Task 1 |
| T-114-01-E | Repudiation | data_sources residue accumulating (WR-05 regression) | mitigate | ephemeral lifecycle copied verbatim from post-111 IL template; Task 3 asserts 0 rows for 'sc-acfr-%' |
| T-114-01-F | Tampering | write to a state other than SC (24 ACFR nodes / NASBO states) | mitigate | loader resolves only name='South Carolina'+state+entity_type='state' and asserts node id f0024b19; Task 3 spot-checks cohort unchanged |
| T-114-01-G | Tampering | pre-GASB-34 years (FY1993–2001, live on cg.sc.gov) loaded past the tranche boundary | mitigate | SOURCES hard-stops at FY2002; Task 3 asserts no pre-2002 FY present |
| T-114-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + Phase-113 python tooling |
</threat_model>

<verification>
- Both SC loaders `--dry-run`: every present FY "validation: PASS"; bookends 20,731,521K (FY2025) / 5,763,261K (FY2002).
- Live: SC node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (SC, fy)).
- Re-run SC --fy 2024 → 0 net change; `data_sources` has 0 'sc-acfr-%' rows.
- SC ≥1 revenue row → Money In auto-enabled.
- ~1.46× scope divergence recorded with the basis-consolidation driver against pre-load NASBO totals (ACFR-31).
- 24 existing ACFR nodes + a sample NASBO state unchanged; no pre-2002 FY loaded.
- 114-01-SC-LOADLOG.md finalized.
</verification>

<success_criteria>
The South Carolina state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2002–FY2025 (honest holes logged), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.46× scope divergence recorded with its basis-consolidation driver (ACFR-31), the P2 clamp wired (ACFR-32), idempotent never-overwrite with 0 data_sources residue proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands SC to Phase 116 for independent re-derivation + cohort audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-01-SUMMARY.md` when done — record the SC FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations, the NASBO-replacement confirmation, the scope-divergence record (basis driver), the idempotency + 0-residue re-run result, and the Money-In auto-enable confirmation.
</output>
