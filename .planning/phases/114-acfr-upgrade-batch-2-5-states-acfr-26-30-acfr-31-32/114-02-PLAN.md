---
phase: 114-acfr-upgrade-batch-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processKYAcfr.js
  - scripts/processKYRevenueAcfr.js
  - .planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-02-KY-LOADLOG.md
autonomous: true
requirements: [ACFR-27, ACFR-31, ACFR-32]

must_haves:
  truths:
    - "Kentucky state node (6d9dfe88-f908-466c-95d5-66dce0777ee0) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full recon-locked window FY2002–FY2025, every FY tying to the printed General column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "KY units are THOUSANDS: UNITS=1_000. FY2024 GF Total revenues stored = 15,456,606,000 dollars (15,456,606K ×1,000) and FY2002 = 6,510,474,000"
    - "The pre-existing NASBO operating rows (KY FY2023 $14,350M, FY2024 $14,188M) are REPLACED in place by ACFR operating rows (same (6d9dfe88, fy, 'operating') key) — not duplicated, no NASBO label remains"
    - "Idempotent never-overwrite with 0 data_sources residue: a second live run of a representative FY produces 0 net change AND leaves 0 rows in treasury.data_sources for the ky-acfr dataset_ids"
    - "KY's ~1.09× near-parity (Federal is a separate major fund column, same mechanism as IN's Medicaid fund) is recorded at load against the pre-load NASBO totals (ACFR-31 — here the honest finding is near-parity)"
    - "Any negative GF category year renders via the P2 clamp — clamped to 0, signed magnitude in label, parent total preserved (ACFR-32)"
    - "Money In auto-enables on KY once revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processKYAcfr.js"
      provides: "KY GF spending-by-function loader (dataset_type='operating'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processKYRevenueAcfr.js"
      provides: "KY GF revenue-by-source loader (dataset_type='revenue'), UNITS=1000, tie-verified FY2002–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-02-KY-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, scope-parity record, idempotency + 0-residue re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processKYAcfr.js / processKYRevenueAcfr.js"
      to: "treasury.budgets (KY state node 6d9dfe88)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE + ephemeral data_sources create/delete"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map"
      to: "finance.ky.gov/.../financial-reporting-branch/ACFR/ PDFs"
      via: "per-year distinctly-named files, all enumerable from the single landing page (TLS quirk: curl needs -k in this environment; try Node fetch first)"
      pattern: "finance.ky.gov"
---

<objective>
Upgrade the **Kentucky** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processKYAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processKYRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the KY ACFR/CAFR PDFs FY2002–FY2025 into gitignored `_acfr-work/ky/`, extracting the **General column** (1st of 7: General | Transportation | Federal | Agency Revenue | Capital Projects | Debt Service | Non-Major Governmental | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + `_acfr-work/extract_gf.py`, dry-run-tying every FY to its printed General column total, then live-loading into `treasury.budgets` (NASBO operating replaced in place, revenue net-new) and verifying idempotency + 0 `data_sources` residue + cohort-untouched.

Kentucky is the KY-analog of Indiana in this batch: exceptionally complete archive (all 24 years from one landing page), ~1.09× near-parity with NASBO (Federal is a separate major fund column), no observed negative GF lines. The two access quirks: `finance.ky.gov` needed `curl -k` in this environment (local CA-bundle gap — try Node's native fetch first), and the FY2002 PDF is a 73MB scanned document whose OCR'd numeric table nonetheless extracts cleanly.

Output: both KY loaders live-loaded across FY2002–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `114-02-KY-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md
@.planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md

<interfaces>
<!-- The EXACT analog pattern. KY loaders mirror the IL loaders file-for-file (standard thousands units). -->
From scripts/processILAcfr.js (spend-by-function analog → processKYAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='Kentucky', state='KY', entity_type='state' (.single()). Node id = 6d9dfe88-f908-466c-95d5-66dce0777ee0 (assert the resolved id matches).
  - UNITS = 1_000 (KY ACFR is in thousands; store dollars).
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Kentucky State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES = { [fy]: { total (printed General column Total expenditures, thousands), confidence:'actual', categories:[{name, total}] } }.
  - validate(fy): category sum vs printed total; tight tolerance (recon tied at $0) else process.exit(2).
  - buildTree(fy): children = categories.filter(>0).map → {n, a: total*UNITS, i:[]}; root {n:'Kentucky General Fund Budget', a: total*UNITS, c:children}.
  - RPC treasury_sync_budget_tree(p_data_source_id, p_fiscal_year, p_dataset_type:'operating', p_total, p_tree, p_row_count, p_triggered_by:'bulk_load') → SELECT budgets row → UPDATE {source_url, source_date, data_source}.
  - **Ephemeral data_sources lifecycle (Phase 111 LOAD-01):** delete-by-dataset_id → insert fresh → RPC → delete-by-id at end of run. 0 residue. Copy verbatim.
  - data_sources.dataset_id = 'ky-acfr-gf-operating', name = 'Kentucky General Fund Operating Budget', api_type='pdf_download', base_url = 'https://finance.ky.gov/office-of-the-controller/office-of-statewide-accounting-services/financial-reporting-branch/Pages/annual-comprehensive-financial-reports.aspx'.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue-by-source analog → processKYRevenueAcfr.js):
  - dataSource(fy) = "Kentucky State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='ky-acfr-gf-revenue'; name='Kentucky General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); buildTree labels a negative "<name> (net loss — shown at 0)"; root total carries the signed net (ACFR-32).
Tooling (Phase 113 shortcut — reuse, do not rebuild): `_acfr-work/extract_gf.py` (GF-column extractor over `pdftotext -table` output) and `_acfr-work/gen_state.py` (add a 'KY' CONFIGS entry → generates both loaders from `ky/ky_all.json`).
POPULATION = 4_505_836 (from loadStateGF.mjs).
NASBO baseline (from 112-RECON.md Section 5): KY node has exactly 2 NASBO operating rows — FY2023 $14,350M, FY2024 $14,188M, data_source_id=null, NO revenue rows, ZERO data_sources rows. Clean NASBO-only node — standard ACFR-replaces-NASBO supersede.
DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 112-BATCH1-SOURCES.md KY Detail Block — USE these, do not re-research. -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds. NOT Statement of Activities, NOT budgetary comparison.
- GF column = "General", 1st of 7 (General | Transportation | Federal | Agency Revenue | Capital Projects | Debt Service | Non-Major Governmental Funds | Total). Take the FIRST numeric token per row.
- Units = THOUSANDS. FY-end June 30 (source_date = ${fy}-06-30). Clean window = FY2002–FY2025 (24 years; FY2001 exists on the page but is pre-GASB-34 boundary — do NOT load, D-12).
- URLs — every year a distinctly-named file directly under `finance.ky.gov/.../financial-reporting-branch/ACFR/`, ALL enumerable from the single landing page. Confirmed anchors:
  - FY2025: `.../ACFR/2025%20Commonwealth%20of%20Kentucky%20Annual%20Comprehensive%20Financial%20Report.pdf`
  - FY2024: `.../ACFR/2024%20Kentucky%20Annual%20Comprehensive%20Financial%20Report.pdf`
  - FY2023: `.../ACFR/2023%20Kentucky%20Annual%20Comprehensive%20Financial%20Report.pdf`
  - FY2002 (bookend): `.../ACFR/2002%20CAFR.pdf`
  - TRAP: the landing page also lists separately-issued "Supplemental Report" PDFs each year — these are NOT the ACFR; do not download/parse them.
- ACCESS NOTE: `finance.ky.gov` failed plain `curl` with a certificate-chain error in this environment (DigiCert wildcard cert is legitimate — local CA-bundle/intermediate gap). `curl -k` worked. Try Node's native https/fetch first; fall back to `curl -k` for downloads only (never disable TLS checks in the loader itself — downloads happen once, manually, into _acfr-work/).
- FY2002 PDF is a 73MB scanned document with an imperfect OCR text layer (garbled narrative, e.g. "CO MB IN ING") — but the numeric table extracted and summed cleanly via `pdftotext -table` at recon. Expect slow download + noisy text; the table is usable.
- Bookend ties (General column Total revenues, thousands): FY2024 = 15,456,606 (diff $0); FY2002 = 6,510,474 (diff $0).
- Negative-line risk: none in either bookend GF column. (The FEDERAL column showed −$26,499K "Interest and other investment income" in FY2024 — different fund column, not GF.) Still scan every transcribed year; clamp path stays wired (ACFR-32).
- Scope vs NASBO ~1.09× — second-smallest divergence in the tranche after IN's ~0.99×. Driver favorable: KY reports Federal funds through a separate major fund column ($20,593,582K FY2024), keeping the General column close to NASBO's budgetary GF concept. Record the near-parity honestly at load (ACFR-31).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both KY loaders + download/extract/transcribe FY2002–FY2025 + dry-run tie</name>
  <files>scripts/processKYAcfr.js, scripts/processKYRevenueAcfr.js, _acfr-work/ky/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template: loadEnv, SOURCES, EXPENDITURES, validate, buildTree, ephemeral data_sources lifecycle, RPC + source-stamp, --dry-run/--fy)
    - scripts/processILRevenueAcfr.js (the exact revenue-by-source template incl. clampForRender + negative-label path)
    - _acfr-work/extract_gf.py + _acfr-work/gen_state.py (the Phase 113 extraction/generation tooling — reuse, do not rebuild)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-BATCH1-SOURCES.md (KY Detail Block: landing page, TLS note, units, bookend ties)
  </read_first>
  <action>
    Enumerate the per-year KY ACFR PDF URLs FY2002–FY2025 from the single landing page (skip every "Supplemental Report" file — ACFR/CAFR main reports only). Download each to `_acfr-work/ky/KY{YYYY}.pdf` — try Node fetch first; if the local TLS chain fails, use `curl -k` for the one-time downloads only. Budget for the 73MB scanned FY2002 file. Soft-404 guard: assert %PDF magic bytes + size > ~500KB — a saved HTML page aborts that FY, never parsed. Run `pdftotext -table` on each, locate the Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances, and extract the General column (1st numeric token per row) for BOTH revenues and expenditures via `_acfr-work/extract_gf.py` into `_acfr-work/ky/ky_all.json` — anchor on the General column position per year, not a fixed column count (major-fund lineup may shift across 24 years). Add a 'KY' CONFIGS entry to `_acfr-work/gen_state.py` (name='Kentucky', abbr='KY', population=4_505_836, node='6d9dfe88-f908-466c-95d5-66dce0777ee0', years 2002–2025, per-year sources map, head_note recording the ~1.09× near-parity + Federal-separate-fund driver) and generate `scripts/processKYAcfr.js` + `scripts/processKYRevenueAcfr.js`; set dataSource labels + dataset_ids ('ky-acfr-gf-operating' / 'ky-acfr-gf-revenue') per interfaces; keep the ephemeral data_sources lifecycle + clampForRender + "(net loss — shown at 0)" path verbatim. source_date = `${fy}-06-30`. Cross-check bookends: FY2024 Total revenues = 15,456,606 thousand and FY2002 = 6,510,474 thousand — if either fails, STOP and re-inspect column selection before transcribing further. If an in-between FY fails to cleanly extract or tie (OCR-era years especially), OMIT it (honest hole) and note it. FY2001 must NOT be in SOURCES. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie.
  </action>
  <verify>
    <automated>node scripts/processKYAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processKYRevenueAcfr.js --dry-run --fy 2024 2>&1 | grep -q '15,456,606'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ky/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY 2002–2025; no "Supplemental Report" files parsed; soft-404 HTML rejected.
    - `node scripts/processKYAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO tie failure; `node scripts/processKYRevenueAcfr.js --dry-run --fy 2024` shows GF Total revenues 15,456,606 (thousands) and `--fy 2002` shows 6,510,474.
    - Both loaders have `UNITS = 1_000` and the ephemeral data_sources create/delete lifecycle (delete appears both before insert and at end of run).
    - dataSource(2024) === "Kentucky State ACFR — General Fund (FY2024 actual, GAAP basis)"; revenue loader uses p_dataset_type:'revenue', dataset_id='ky-acfr-gf-revenue'.
    - FY2001 is NOT in SOURCES (pre-GASB-34 boundary honored).
    - The loader itself contains NO TLS-verification bypass (any -k/rejectUnauthorized workaround is confined to the one-time manual downloads).
  </acceptance_criteria>
  <done>Both KY loaders exist on the IL template (thousands units, ephemeral data_sources lifecycle intact), SOURCES enumerates the per-year finance.ky.gov URLs FY2002–FY2025, every transcribed FY dry-run-ties to its printed General column total with the 15,456,606K / 6,510,474K bookends, the P2 clamp path is wired, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load KY (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processKYAcfr.js, scripts/processKYRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processKYAcfr.js + scripts/processKYRevenueAcfr.js (the --fy + post-RPC source-stamp path; confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (Section 5 NASBO baselines + untouched-nodes contract)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets (mcp__supabase-local__execute_sql) for the KY node (6d9dfe88) current rows and RECORD the pre-load NASBO operating rows (recon baseline: FY2023 $14,350M, FY2024 $14,188M — record the exact totals the query returns). Then for each KY FY present after Task 1, run `node scripts/processKYAcfr.js --fy <YYYY>` then `node scripts/processKYRevenueAcfr.js --fy <YYYY>` (live). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (6d9dfe88, fy, 'operating') for FY2023/FY2024, net-new otherwise; revenue net-new), and stamp source_url/source_date/data_source (GAAP basis label). If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record the loaded KY GF totals against the pre-load NASBO totals confirming the ~1.09× near-parity (ACFR-31 record).
  </action>
  <verify>
    <automated>echo "KY row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the KY node has, per loaded FY, an operating row "Kentucky State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - KY FY2024 revenue row total = 15,456,606,000 dollars and FY2002 = 6,510,474,000.
    - KY FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (KY, fy); no "NASBO" label remains on the KY node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded in the load log with the ~1.09× near-parity note (ACFR-31).
  </acceptance_criteria>
  <done>Each transcribed KY FY has operating + revenue rows in treasury.budgets, dollar-scaled from thousands, GAAP-basis-labelled + per-year-sourced; FY2023/FY2024 NASBO operating rows replaced in place (no duplicates); scope near-parity recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + 0-residue + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-02-KY-LOADLOG.md</files>
  <read_first>
    - scripts/processKYAcfr.js (confirm RPC keyed (muni, fy, dataset) + ephemeral data_sources lifecycle)
    - .planning/phases/112-recon-roster-lock-source-location-overlap-resolution-recon-0/112-RECON.md (untouched-nodes contract — the now-24 ACFR node cohort + un-upgraded NASBO states)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. KY --fy 2024 operating + revenue) and confirm 0 net change. Query treasury.budgets + treasury.data_sources via mcp__supabase-local__execute_sql to confirm: (a) every loaded KY FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) KY has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (KY, fy)); (c) honest-hole FYs are ABSENT; (d) treasury.data_sources has ZERO rows with dataset_id LIKE 'ky-acfr-%'; (e) Money In auto-enable — KY has ≥1 dataset_type='revenue' row; (f) cohort untouched: spot-check 2–3 of the 24 existing ACFR nodes and a sample un-upgraded NASBO state are unchanged. Append a "Load Disposition" section to 114-02-KY-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the near-parity note, the NASBO-replacement confirmation, the idempotency re-run result, and the 0-residue check. Do NOT load any year beyond the tied window; do NOT touch any state other than KY.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 114-02-KY-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of KY --fy 2024 (operating + revenue) reports 0 net change (idempotent never-overwrite).
    - DB query: every loaded KY FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; KY has ZERO remaining "NASBO" operating rows; exactly one operating row per (KY, fy).
    - `SELECT count(*) FROM treasury.data_sources WHERE dataset_id LIKE 'ky-acfr-%'` returns 0.
    - KY has ≥1 revenue row (Money In auto-enable confirmed).
    - Honest-hole FYs (from Task 1) are ABSENT from the DB.
    - Sample of the 24 existing ACFR nodes + one un-upgraded NASBO state unchanged.
    - 114-02-KY-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change with 0 data_sources residue; the DB query confirms KY fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, cohort unchanged; 114-02-KY-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finance.ky.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the KY node only (Tasks 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-02-A | Tampering | extracting the wrong statement/column (Statement of Activities, or Transportation/Federal/Total instead of General) | mitigate | Task 1 isolates 1st numeric token of the Governmental Funds Statement per year; bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-114-02-B | Tampering | Supplemental Report PDF parsed instead of the ACFR / soft-404 HTML saved as .pdf | mitigate | enumeration explicitly skips "Supplemental Report" files; %PDF magic + size guard; non-PDF aborts that FY |
| T-114-02-C | Tampering | NASBO row left alongside the ACFR row (duplicate) / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (KY, fy), zero NASBO labels |
| T-114-02-D | Tampering | OCR-era years (FY2002-class scans) transcribing garbled figures | mitigate | per-FY tie against the printed General column total is the gate — a non-tying OCR year is OMITTED as an honest hole, never force-written |
| T-114-02-E | Repudiation | data_sources residue accumulating (WR-05 regression) | mitigate | ephemeral lifecycle copied verbatim from post-111 IL template; Task 3 asserts 0 rows for 'ky-acfr-%' |
| T-114-02-F | Tampering | write to a state other than KY | mitigate | loader resolves only name='Kentucky'+state+entity_type='state' and asserts node id 6d9dfe88; Task 3 spot-checks cohort unchanged |
| T-114-02-G | Information Disclosure / Tampering | TLS-verification bypass (-k) leaking into runtime code | mitigate | -k confined to one-time manual downloads; acceptance criterion asserts the loaders contain no TLS bypass |
| T-114-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext + Phase-113 python tooling |
</threat_model>

<verification>
- Both KY loaders `--dry-run`: every present FY "validation: PASS"; bookends 15,456,606K (FY2024) / 6,510,474K (FY2002).
- Live: KY node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (KY, fy)).
- Re-run KY --fy 2024 → 0 net change; `data_sources` has 0 'ky-acfr-%' rows.
- KY ≥1 revenue row → Money In auto-enabled.
- ~1.09× near-parity recorded against pre-load NASBO totals (ACFR-31).
- 24 existing ACFR nodes + a sample NASBO state unchanged.
- 114-02-KY-LOADLOG.md finalized.
</verification>

<success_criteria>
The Kentucky state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2002–FY2025 (honest holes logged), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place, the ~1.09× near-parity recorded (ACFR-31), the P2 clamp wired (ACFR-32), idempotent never-overwrite with 0 data_sources residue proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands KY to Phase 116 for independent re-derivation + cohort audit + UAT.
</success_criteria>

<output>
Create `.planning/phases/114-acfr-upgrade-batch-2-5-states-acfr-26-30-acfr-31-32/114-02-SUMMARY.md` when done — record the KY FY window actually transcribed, any FY omitted as a non-tying honest hole (OCR-era years especially), the two bookend tie confirmations, the NASBO-replacement confirmation, the near-parity record, the idempotency + 0-residue re-run result, and the Money-In auto-enable confirmation.
</output>
