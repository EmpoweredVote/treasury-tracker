---
phase: 46-sourced-explainer-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md
autonomous: true
requirements:
  - SRC-01
must_haves:
  truths:
    - "A verified, parseable official source exists for each of the ~20 budget-function descriptions, with the exact fetch method demonstrated (one function's text actually extracted)"
    - "The agency path is confirmed end-to-end: USAspending /api/v2/agency/{code}/ mission text fetched for all 10 mapped agencies"
    - "The MTS-T5-department-name → USAspending-toptier-code mapping table exists for the top 10, each entry verified by fetching it"
    - "46-SOURCES.md records every source decision with URL, fetch method, and a sample extract"
  artifacts:
    - path: ".planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md"
      provides: "Source-of-record for every explainer input"
      contains: "toptier"
  key_links:
    - from: "46-SOURCES.md"
      to: "scripts/enrichFederalCategories.js (46-02)"
      via: "source URLs + extraction methods consumed by the pipeline"
      pattern: "function_descriptions"
---

<objective>
Pin the authoritative text source for every explainer BEFORE any generation: function descriptions (verify-first across A-11 / PBDB user guide / GovInfo), agency missions (verified endpoint, needs the name mapping), with sample extractions proving each method. SRC-01's "fetched authoritative text" is only as good as these decisions.

Output: 46-SOURCES.md — the pipeline's contract.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sourced-explainer-pipeline/46-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: function-description source (verify-first)</name>
  <files>.planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md</files>
  <action>
    Try in order until one yields clean per-function descriptive text (browser UA for whitehouse.gov):
    1. OMB Circular A-11 (whitehouse.gov; locate the functional-classification section — historically §79/§84 area; pdftotext extraction; functions appear as "050 National Defense" style headings with definition paragraphs)
    2. Public Budget Database user guide PDF (linked from the supplemental-materials page next to outlays.xlsx)
    3. GovInfo API (DATA_GOV_API_KEY in .env): Analytical Perspectives, functional-classification chapter
    ACCEPTANCE per source: extract the "Health" (550) and "Net Interest" (900) description paragraphs verbatim as samples; text must be a real definition (≥2 sentences), not a table fragment. Record in 46-SOURCES.md: chosen source, exact URL, extraction commands, both samples, and why higher-priority candidates failed (if they did).
    If ALL fail: STOP — do not fall back to model knowledge; report the blocker (Chris manual-download checkpoint becomes 46-02's precondition).
  </action>
  <verify><automated>46-SOURCES.md contains two verbatim sample paragraphs + extraction method for the chosen source</automated></verify>
  <acceptance_criteria>
    - Function text source proven by demonstration, not assumption
  </acceptance_criteria>
  <done>The function half of the pipeline has its sourced input.</done>
</task>

<task type="auto">
  <name>Task 2: agency mapping + mission fetches</name>
  <files>.planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md</files>
  <action>
    Build the explicit mapping for the top-10 agency-lens departments (46-CONTEXT list) → USAspending toptier codes (GET /api/v2/references/toptier_agencies/ for the directory). Special cases to resolve deliberately: "Department of Defense--Military Programs" (toptier 097), "Other Defense Civil Programs" (may have NO single toptier match — if so, record it as function-style sourcing or skip-with-rationale rather than force a wrong mapping). For each mapped agency: GET /api/v2/agency/{code}/, confirm non-empty mission text, record code + mission first sentence + congressional_justification_url in 46-SOURCES.md.
  </action>
  <verify><automated>46-SOURCES.md mapping table: 10 rows, each with toptier code + mission sample (or explicit skip rationale)</automated></verify>
  <acceptance_criteria>
    - Zero guessed mappings; every row demonstrated by an actual fetch
  </acceptance_criteria>
  <done>The agency half of the pipeline has its sourced input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .gov documents → extraction | PDF/JSON from whitehouse.gov/usaspending.gov/govinfo.gov only; samples human-readable in 46-SOURCES.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing | source URLs | mitigate | .gov-only allowlist; URLs recorded for the Phase 48 chain audit |
| T-46-SC | Tampering | installs | accept | pdftotext/openpyxl already present; no installs |
</threat_model>

<verification>
1. 46-SOURCES.md complete: function source demonstrated, 10 agency rows fetched, special cases resolved explicitly
</verification>

<success_criteria>
- 46-02 can run with zero source ambiguity; SRC-01's input side is locked
</success_criteria>

<output>
Create .planning/phases/46-sourced-explainer-pipeline/46-01-SUMMARY.md when done
</output>
