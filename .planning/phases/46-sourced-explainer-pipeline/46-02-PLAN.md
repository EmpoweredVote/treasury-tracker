---
phase: 46-sourced-explainer-pipeline
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - data/federal-enrichment.json
  - scripts/loadFederalEnrichment.js
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-04
must_haves:
  truths:
    - "Every federal function (~20) and mapped top agency has a category_enrichment row: source='hybrid', source_url = the fetched document, source_label naming it, evidence_summary stating what text was fetched, municipality_id = US id"
    - "data/federal-enrichment.json holds the full explainer text + citations, committed to git (reviewable, re-loadable)"
    - "Every explainer was authored ONLY from the verbatim extracts recorded in 46-SOURCES.md plus our own sourced DB figures — zero API calls, zero model-memory facts (claim-trace audit on 3 samples proves it)"
    - "Drilling into a function and an agency in the live app shows the explainer with its citation"
  artifacts:
    - path: "data/federal-enrichment.json"
      provides: "All Tier 1 explainers with citations — the reviewable source of record"
      contains: "evidence_summary"
    - path: "scripts/loadFederalEnrichment.js"
      provides: "Deterministic idempotent upsert of the JSON into category_enrichment"
      contains: "hybrid"
  key_links:
    - from: "data/federal-enrichment.json"
      to: "treasury.category_enrichment"
      via: "loadFederalEnrichment.js upsert on (name_key, municipality_id)"
      pattern: "name_key"
---

<objective>
Author the Tier 1 explainers INLINE (Chris 2026-06-12: no API calls) from the 46-SOURCES.md verbatim extracts, store them in a committed JSON file, and load them with a deterministic script. SRC-01's closed-input rule applies to the in-session author exactly as it would to an API model: fetched text + our sourced figures, nothing else.

Output: data/federal-enrichment.json (~30 explainers, fully cited); loadFederalEnrichment.js; rows live; explainers visible in the app. $0 API spend (SRC-04).
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sourced-explainer-pipeline/46-CONTEXT.md
@.planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: author data/federal-enrichment.json from the recorded extracts</name>
  <files>data/federal-enrichment.json</files>
  <read_first>
    - .planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md — the ONLY permitted text inputs (verbatim extracts per function/agency)
    - scripts/enrichCategories.js:382 — name_key derivation (normalize()) to match exactly
  </read_first>
  <action>
    For each of the ~20 functions and mapped top-10 agencies, write per the 46-CONTEXT editorial standard:
    { name_key, plain_name, short_description, description, tags, confidence, source: "hybrid", source_label, source_url, evidence_summary }
    AUTHORSHIP RULE (SRC-01, binding on the in-session author): every factual claim must come from (a) that target's verbatim extract in 46-SOURCES.md or (b) the category's own FY2025 figures from our DB (sourced; quote at most one per explainer). No memory facts, no opinions, no framing. If an extract is too thin for a 'high'/'medium'-confidence description, mark the target skipped-with-rationale in the JSON's _skipped array rather than padding.
    name_key MUST match the live depth-0 category names: derive from a SQL dump of the actual budget_categories roots (both lenses), normalized identically to enrichCategories.js — never typed from memory.
    evidence_summary format: "Authored inline from: <doc title> (<section>), fetched <date>; sourced figure: FY2025 outlays $X.XB."
  </action>
  <verify><automated>JSON validates; name_keys exactly match the SQL-dumped category roots; every entry has all citation fields</automated></verify>
  <acceptance_criteria>
    - ~30 entries, full citations, committed to git; thin sources skipped explicitly, never padded
  </acceptance_criteria>
  <done>The explainers exist as reviewable, sourced text.</done>
</task>

<task type="auto">
  <name>Task 2: loadFederalEnrichment.js + load + verify</name>
  <files>scripts/loadFederalEnrichment.js</files>
  <read_first>
    - scripts/enrichCategories.js saveEnrichment() — upsert shape (onConflict: name_key,municipality_id) + row fields
  </read_first>
  <action>
    Small deterministic loader: reads data/federal-enrichment.json, resolves the US municipality id, upserts every entry (generated_at = now, municipality_id = US id), --dry-run flag, prints per-row results + final count. No LLM, no fetch — pure file→DB.
    Run dry → live → re-run (idempotent). Verify:
    - SQL: row count matches JSON entries; all source='hybrid' with non-null source_url/source_label/evidence_summary; municipality_id = US id on every row; zero universal rows created.
    - Live app: drill Health (function lens) + Department of Veterans Affairs (agency lens) — explainer renders with the official source label in the attribution line.
    - Claim-trace audit: pick 3 explainers; map every factual claim to its 46-SOURCES.md extract line or DB figure; record the traces in the summary. Untraceable claim → fix the JSON and reload (the file is the fix surface — that's the point of committing it).
  </action>
  <verify>
    <automated>
      SQL counts + null checks green; idempotent re-run; 3 claim-traces recorded; app display confirmed
    </automated>
  </verify>
  <acceptance_criteria>
    - SRC-02 satisfied; SRC-01 spot-proven by trace; SRC-04 trivially documented ($0 — no API calls made)
  </acceptance_criteria>
  <done>Citizens drilling federal categories get sourced plain language; zero API spend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 46-SOURCES extracts → authored JSON | Closed-input authorship; claim-trace audit; full text in git review |
| JSON → DB | Deterministic loader; JSON-validated; idempotent upsert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03 | Info integrity | author memory leakage | mitigate | Closed-input rule + per-claim trace audit on 3 samples; JSON reviewable in git |
| T-46-04 | Cost | LLM spend | n/a | Zero API calls by design (Chris 2026-06-12) |
| T-46-SC | Tampering | installs | accept | None |
</threat_model>

<verification>
1. JSON committed, validated, name_keys SQL-matched
2. ~30 rows live, fully cited, US-scoped, idempotent
3. 3 claim-traces; app display both lenses
4. SRC-04: $0, no estimate needed beyond the statement
</verification>

<success_criteria>
- SRC-01, SRC-02, SRC-04 complete; the committed-JSON pattern proven for the future city backfill
</success_criteria>

<output>
Create .planning/phases/46-sourced-explainer-pipeline/46-02-SUMMARY.md when done
</output>
