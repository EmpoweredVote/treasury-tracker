---
phase: 46-sourced-explainer-pipeline
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - scripts/enrichFederalCategories.js
autonomous: false
requirements:
  - SRC-01
  - SRC-02
  - SRC-04
must_haves:
  truths:
    - "Every federal function (~20) and mapped top agency has a category_enrichment row: source='hybrid', source_url = the fetched document, source_label naming it, evidence_summary stating what text was fetched, municipality_id = US id"
    - "The generation prompt contains ONLY: the fetched official text, optional clearly-delimited sourced DB figures, and format instructions — verifiable by reading the prompt builder"
    - "The pipeline printed a cost estimate BEFORE generating and the actual cost after; both under the $5 gate"
    - "Drilling into a function and an agency in the live app shows the explainer with its citation"
  artifacts:
    - path: "scripts/enrichFederalCategories.js"
      provides: "Fetch-then-summarize enrichment pipeline (v2 standard)"
      contains: "evidence_summary"
  key_links:
    - from: "scripts/enrichFederalCategories.js"
      to: "treasury.category_enrichment"
      via: "upsert on (name_key, municipality_id)"
      pattern: "hybrid"
---

<objective>
Build and run the v2 pipeline: fetch the 46-SOURCES.md text per category → prompt the model to condense ONLY that text → store with full citation. This is the sourcing standard that later backfills to cities.

Output: enrichFederalCategories.js; ~30 enrichment rows live; explainers visible in the app with citations.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sourced-explainer-pipeline/46-CONTEXT.md
@.planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: enrichFederalCategories.js</name>
  <files>scripts/enrichFederalCategories.js</files>
  <read_first>
    - scripts/enrichCategories.js — reuse: loadEnv, Anthropic client, ENRICHMENT_MODEL, name_key normalize(), upsert shape, progress/idempotency, --dry-run
    - .planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md — the source contract
  </read_first>
  <action>
    New script (don't fork v1's evidence logic — different input model). Flow per target:
    1. FETCH the official text per 46-SOURCES.md (function definition paragraph / agency mission [+ first CJ overview text only if 46-SOURCES verified it]). Cache fetched text to a local dir so re-runs don't refetch.
    2. Optional sourced figures block: the category's own FY2025 amount + share from OUR database (these are sourced — chips exist); delimited in the prompt as the ONLY numbers the model may quote.
    3. PROMPT (the SRC-01 contract — keep it auditable in one function):
       "You are condensing OFFICIAL TEXT into a plain-language explainer… Use ONLY the official text below and the sourced figures block. If the text doesn't support a claim, omit the claim. No opinions, no framing, no facts from memory."
       → JSON: { plain_name, short_description, description, tags, confidence } per the 46-CONTEXT editorial standard.
    4. STORE: source='hybrid', source_url, source_label (e.g. "OMB Circular A-11" / "USAspending agency profile (official mission statement)"), evidence_summary = "Generated from: <doc title>, fetched <date>; sourced figures: FY2025 outlays", confidence as returned ('low' → skip + log, never publish).
    5. COST GATE (SRC-04): before any generation, print estimate (targets × est tokens × model rates); ABORT > $5.00; require --confirm-cost above $1.00. After the run, print actual token usage + cost.
    Flags: --dry-run (fetch + build prompts + estimate, no API calls, print one full sample prompt for audit), --only <name_key>, --limit N.
    Targets: every depth-0 category of the US operating dataset (functions) + the 46-SOURCES mapped agency-lens roots.
  </action>
  <verify><automated>--dry-run prints a sample prompt containing ONLY fetched text + delimited figures + instructions (audit by reading); cost estimate printed</automated></verify>
  <acceptance_criteria>
    - Prompt builder demonstrably closed to model-memory input; idempotent re-runs; cache prevents refetching
  </acceptance_criteria>
  <done>The v2 standard exists as runnable, auditable code.</done>
</task>

<task type="auto">
  <name>Task 2: run + verify display</name>
  <files>none</files>
  <action>
    Run --dry-run, audit the sample prompt + estimate (record both in the summary). Run live. Then verify:
    - SQL: enrichment rows for ~20 function name_keys + mapped agencies, all municipality_id = US id, all source='hybrid' with non-null source_url/source_label/evidence_summary, zero 'low' confidence published.
    - Live app: drill into Health (function lens) and Department of Veterans Affairs (agency lens) — explainer text renders; confirm the existing enrichment-attribution line shows the official source label.
    - Quality spot-audit (3 explainers vs their fetched text): every factual claim in the description must trace to the fetched text or the figures block; record the trace in the summary. Any untraceable claim → fix the prompt and regenerate THAT row.
    - Record actual cost vs estimate.
  </action>
  <verify>
    <automated>
      SQL counts + null checks; spot-audit traces recorded; cost ≤ estimate ≤ gate
    </automated>
  </verify>
  <acceptance_criteria>
    - SRC-02 satisfied (~20 functions + top agencies live); SRC-01 spot-proven; SRC-04 documented
  </acceptance_criteria>
  <done>Citizens drilling federal categories get sourced plain language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fetched .gov text → LLM prompt | Text passed verbatim; prompt instructs condense-only; spot-audit verifies |
| LLM output → DB | JSON-validated; 'low' confidence never published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02 | Tampering | prompt injection via fetched text | mitigate | .gov-only sources; output JSON-schema-validated; spot-audit human-readable |
| T-46-03 | Info integrity | model-memory leakage | mitigate | Closed prompt + per-claim trace audit on 3 samples |
| T-46-04 | Cost | runaway spend | mitigate | Pre-run estimate, $5 abort, $1 confirm (SRC-04) |
| T-46-SC | Tampering | installs | accept | Anthropic SDK already present |
</threat_model>

<verification>
1. ~30 rows, fully cited, US-scoped
2. Sample prompt audit + 3-explainer claim traces in summary
3. Cost: estimate + actual, under gate
4. Live display confirmed both lenses
</verification>

<success_criteria>
- SRC-01, SRC-02, SRC-04 complete; the v2 standard proven for the future city backfill
</success_criteria>

<output>
Create .planning/phases/46-sourced-explainer-pipeline/46-02-SUMMARY.md when done
</output>
