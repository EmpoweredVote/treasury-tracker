---
phase: 46-sourced-explainer-pipeline
plan: 03
type: execute
wave: 2
depends_on: [46-01]
autonomous: false
files_modified:
  - src/components/federal/MethodologyPanel.tsx
  - data/federal-enrichment.json
requirements:
  - SRC-03
must_haves:
  truths:
    - "An official record of DoD audit results is fetched and cited: dod_consecutive_failed_audits metric (numeric, label, source_url) exists"
    - "MethodologyPanel renders an audit section computed from that metric with its SourceChip — never hardcoded counts"
    - "The National Defense (function) and DoD (agency) enrichment descriptions each carry one factual audit-status sentence with the same citation"
    - "Phase verification: every 46 enrichment source_url sampled returns 200; per-requirement PASS lines recorded"
  artifacts:
    - path: ".planning/phases/46-sourced-explainer-pipeline/46-VERIFICATION.md"
      provides: "SRC-01..04 evidence"
      contains: "SRC-03"
  key_links:
    - from: "MethodologyPanel.tsx"
      to: "federal_context_metrics.dod_consecutive_failed_audits"
      via: "/federal/context metrics"
      pattern: "dod_consecutive_failed_audits"
---

<objective>
Transparency about opacity (ground rule 7): where the budget isn't auditable, say so with the official citation. DoD has never passed a full financial audit — that FACT must come from an official record we fetched, land as a metric + two enrichment sentences + a methodology section, and never from model memory.

Output: sourced audit metric; MethodologyPanel section; enrichment sentences; 46-VERIFICATION.md.
</objective>

<execution_context>
@C:/Users/Chris/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-sourced-explainer-pipeline/46-CONTEXT.md
@.planning/phases/46-sourced-explainer-pipeline/46-SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetch the official audit record (verify-first; human fallback)</name>
  <files>none (recon + one metric row)</files>
  <action>
    GAO blocks curl. Try in order:
    1. defense.gov / comptroller.defense.gov — DoD's own annual audit-results announcement (their CJ domain is reachable)
    2. dodig.mil — DoD Office of Inspector General audit reports
    3. WebFetch tool against gao.gov (different fetcher may pass; the High-Risk List / DoD financial management pages)
    4. Congress.gov API (key in .env) — hearing/report records on the DoD audit
    REQUIRED from whichever source: an official statement of the most recent audit result (disclaimer/failed opinion) and, if stated, the consecutive count. Extract the EXACT sentence(s), record verbatim + URL in the summary.
    If none reachable: CHECKPOINT — ask Chris to download the GAO/DoD OIG document in a browser to docs/federal/; cite the official URL even though fetched manually (record the manual step).
    Then upsert metric: dod_consecutive_failed_audits (value = the official count; if the official text states a result but no count, store value=1 for the latest audit with a label saying exactly what the source states — never infer a count), as_of_date = the audit FY, label quoting the source's framing, source_name (add registry row if a new source), source_url, source_date.
  </action>
  <verify><automated>Metric row exists; label/value match the recorded verbatim extract; source_url documented</automated></verify>
  <acceptance_criteria>
    - The audit claim is exactly as official records state it — no inferred numbers, no memory
  </acceptance_criteria>
  <done>The opacity fact has an official, fetched foundation.</done>
</task>

<task type="auto">
  <name>Task 2: surface it — MethodologyPanel + enrichment sentences</name>
  <files>src/components/federal/MethodologyPanel.tsx</files>
  <action>
    1. MethodologyPanel: new section "Can these numbers be audited?" rendered ONLY when metrics.dod_consecutive_failed_audits exists: states (computed from the metric's value/label) that the Department of Defense — the largest discretionary spender — has not passed its financial audit, per the cited official record; one sentence on what that means (the totals here are official Treasury figures; the audit concerns DoD's internal accounting). SourceChip from the metric. No editorializing.
    2. Append the audit-status sentence (same citation) to the National Defense function and DoD agency entries in data/federal-enrichment.json — authored inline from the Task 1 verbatim extract (closed-input rule), evidence_summary updated to name the audit source too — then reload via loadFederalEnrichment.js.
    3. tsc + build; push (Netlify).
  </action>
  <verify><automated>Panel section renders from the metric (dev check); both enrichment rows contain the audit sentence + citation; build green</automated></verify>
  <acceptance_criteria>
    - SRC-03 visible in UI at three touchpoints, all tracing to one official record
  </acceptance_criteria>
  <done>Opacity is disclosed where citizens will actually see it.</done>
</task>

<task type="auto">
  <name>Task 3: phase verification</name>
  <files>.planning/phases/46-sourced-explainer-pipeline/46-VERIFICATION.md</files>
  <action>
    - SRC-01: sample prompt audit reference + 3 claim-traces (from 46-02)
    - SRC-02: SQL counts (functions + agencies enriched, all cited, US-scoped)
    - SRC-03: metric + panel + 2 enrichment sentences, one citation chain
    - SRC-04: estimate vs actual cost
    - Source sweep: curl 5 sampled enrichment source_urls → 200
    - Regression: a city enrichment row (Plano) unchanged; city drill UI unaffected
  </action>
  <verify><automated>PASS line per SRC requirement with evidence</automated></verify>
  <acceptance_criteria>
    - Phase goal proven; anything deferred is explicit
  </acceptance_criteria>
  <done>Phase 46 closes with evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official audit record → metric + UI | Verbatim extract recorded; value never inferred beyond the text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-05 | Info integrity | inferred audit count | mitigate | Value only from explicit official statement; else value=1 + exact-language label |
| T-46-06 | Safety | editorializing on a charged topic | mitigate | Computed text from metric label; structural framing only (ground rule: never editorialize) |
| T-46-SC | Tampering | installs | accept | None |
</threat_model>

<verification>
1. One citation chain serving three UI touchpoints
2. 46-VERIFICATION.md complete with SRC-01..04 PASS lines
</verification>

<success_criteria>
- SRC-03 complete; Phase 46 verified end-to-end
</success_criteria>

<output>
Create .planning/phases/46-sourced-explainer-pipeline/46-03-SUMMARY.md when done
</output>
