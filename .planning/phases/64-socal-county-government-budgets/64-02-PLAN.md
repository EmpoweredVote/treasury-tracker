---
phase: 64-socal-county-government-budgets
plan: "64-02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CGB-01]
must_haves:
  truths:
    - "Both directory-only counties already in the DB (Alameda, Sacramento) have operating + revenue loaded for FY2003–2024 (where the SCO county feed provides it), all-governmental-funds basis, each row carrying the durable /d/<id> source_url + fetch date (D-02, D-03, D-07)"
    - "Per-year population came from the SCO county feed; each county entity has population > 0 (loaded FY2024 canary first), so the county page renders per-capita (D-04)"
    - "Each county page renders icicle/summary + per-capita instead of directory-only; any city rows under each county are untouched (D-08, D-12)"
    - "Read-only verification + production DB only; $0 spend (D-09, D-10)"
  artifacts:
    - path: ".planning/phases/64-socal-county-government-budgets/64-02-SUMMARY.md"
      provides: "The Alameda + Sacramento county-government budget-load result: per-county op/rev FY reach, row/source counts, population, canary totals vs loaded — the CGB-01 evidence record (directory-only two)"
      contains: "per-county dry-run canary FY2024 totals, loaded op/rev row counts + FY range, NULL source_url count (must be 0), population per county, any No-data FY gaps, per-capita confirmation"
  key_links:
    - from: "SCO ByTheNumbers county datasets (uctr-c2j8 operating / emxv-k8xv revenue), entity_name=<county>"
      to: "treasury.budgets (operating + revenue, FY2003–2024) on the existing entity_type='county' municipality"
      via: "loadCountyBudget.js --county <X> --fy <year> (canary FY2024 first, then backfill FY2003–2023)"
      pattern: "dry-run canary → load canary for real (locks current population) → backfill remaining years with durable source + per-year feed population (never-overwrite)"
---

<objective>
Satisfy CGB-01 (directory-only two): load the 2 directory-only counties already in the DB — Alameda and
Sacramento — with their own operating + revenue budget (FY2003–2024, all-governmental-funds basis) from
the SCO ByTheNumbers county datasets onto the existing county entity, using `loadCountyBudget.js` with
ZERO new code, so each page renders icicle/summary + per-capita instead of directory-only. Dry-run canary
first, FY2024 loaded first (locks current population), every row durably sourced, never overwrite,
read-only verification, $0. (The 6 SoCal counties are 64-01.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-socal-county-government-budgets/64-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/loadCountyBudget.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     NEVER mcp__supabase-local (stale, D-09). The loader does NOT auto-load .env — source it first:
       set -a; . ./.env; set +a
     SCO is flaky (D-06) — drive the loader per-FY in a bash retry loop until each year prints "Done.". -->

Tool (zero new code, runbook county-gov step):
  node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --dry-run                 # canary, zero writes
  node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --source-date 2026-06-17  # canary live (locks current pop)
  node scripts/loadCountyBudget.js --county "<X>" --fy <year> --source-date 2026-06-17 # backfill FY2003..2023, per-FY

Counties (SCO entity_name, NO "County" suffix): Alameda, Sacramento.
DB entity = "<X> County" (loader default; both already exist, entity_type='county', population=0, 0 existing op+rev rows — confirmed 2026-06-17).
County datasets: uctr-c2j8 (operating), emxv-k8xv (revenue) — both load by default. Feed carries per-year estimated_population (D-04).
Read-only probe harness: reuse the .env service-key snippet from 61-01-PLAN.md Task 2 / 63-01-PLAN.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run canary FY2024 for Alameda + Sacramento (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/64-socal-county-government-budgets/64-CONTEXT.md (D-01..D-12 — read in full)
    - docs/socal-county-onboarding.md (county-government budget step + Locked conventions)
    - scripts/loadCountyBudget.js (flags, county datasets, resolveCountyEntity error-if-missing, never-overwrite)
  </read_first>
  <action>
    Source .env (`set -a; . ./.env; set +a`). For EACH of Alameda and Sacramento, run the canary dry-run:
      node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --dry-run
    Read each output. Confirm: the county entity resolves (id printed, no "not found" error), the feed
    returns rows for FY2024, per-year `estimated_population` is present (>0), and operating + revenue
    category counts + totals are non-zero and plausible (all-governmental-funds). Record per-county the
    FY2024 op total, rev total, and feed population. SCO is flaky (D-06) — wrap each call in a retry loop
    (retry until output contains "Done."). Writes nothing.
  </action>
  <acceptance_criteria>
    - Both counties' canary dry-runs complete; each resolves its entity (no "not found"), returns FY2024 rows, shows feed population > 0, and reports non-zero operating + revenue totals.
    - Per-county FY2024 op/rev totals + feed population are recorded; no writes performed.
  </acceptance_criteria>
  <done>Alameda + Sacramento FY2024 canary totals + feed populations are enumerated from a no-write dry-run, confirming each entity + the population-from-feed assumption before any live write.</done>
</task>

<task type="auto">
  <name>Task 2: Live load — FY2024 canary first, then backfill FY2003–2023 (Alameda + Sacramento)</name>
  <files>(no repo files — production DB rows written to the 2 county entities)</files>
  <read_first>
    - The dry-run output from Task 1
    - .planning/phases/63-socal-county-cities-load-linking/63-01-SUMMARY.md (per-FY retry pattern precedent)
  </read_first>
  <action>
    Source .env. For EACH of Alameda and Sacramento, in this order so the stored population reflects the current year:
      1) Load the FY2024 canary for real:
         node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --source-date 2026-06-17
      2) Backfill FY2003–2023 one fiscal year at a time inside a bash retry loop (retry each --fy until its
         output contains "Done."; gap-fill any year that still fails). Operating + revenue both load by
         default. Use --source-date 2026-06-17 on every call.
    Capture each county's per-FY load lines (totals written, any "No data found" year gaps, any never-overwrite
    SKIP). Do NOT modify any city rows. The authoritative per-county DB probe runs in Task 3.
  </action>
  <acceptance_criteria>
    - For each of Alameda and Sacramento, FY2024 loaded first (population backfilled > 0), then FY2003–2023 loaded; every targeted year/type either wrote or logged a benign "No data found" gap.
    - The loader's console output is captured per county (totals per FY/dataset, any No-data gaps, any never-overwrite skips).
    - No DB writes beyond loadCountyBudget.js; production DB; city rows untouched.
  </acceptance_criteria>
  <done>Alameda + Sacramento county governments have operating + revenue FY2003–2024 loaded (FY2024 first), durably SCO-sourced, with per-year feed population.</done>
</task>

<task type="auto">
  <name>Task 3: Verify (read-only) + write 64-02-SUMMARY</name>
  <files>.planning/phases/64-socal-county-government-budgets/64-02-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/63-socal-county-cities-load-linking/VERIFICATION.md (probe + evidence-table shape)
  </read_first>
  <action>
    With a READ-ONLY probe (production DB, schema treasury, service key), for EACH of Alameda County and
    Sacramento County confirm: operating + revenue rows present across FY2003–2024 (where the feed provides),
    every row's source_url contains "/d/" (NULL source_url count must be 0), and population > 0 (per-capita
    renders). Spot-check one county's FY2024 op/rev totals against the Task 1 canary figures. Confirm city
    rows are untouched. Then write 64-02-SUMMARY.md with a per-county table: op rows, rev rows, FY range,
    NULL source_url count, population, any No-data FY gaps. State CGB-01 (directory-only two) satisfied;
    note all-governmental-funds basis.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const cnt=async(q)=>{const {count}=await q;return count;};let allOk=true;for(const name of ['Alameda County','Sacramento County']){const {data:c}=await s.from('municipalities').select('id,population').eq('name',name).eq('entity_type','county').maybeSingle();if(!c){console.log(name,'-> NO ENTITY');allOk=false;continue;}const op=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).eq('dataset_type','operating'));const rev=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).eq('dataset_type','revenue'));const nullUrl=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).in('dataset_type',['operating','revenue']).is('source_url',null));console.log(name.padEnd(22),'op='+op,'rev='+rev,'nullUrl='+nullUrl,'pop='+(c.population||0));if(!(op>0&&rev>0&&nullUrl===0&&c.population>0))allOk=false;}process.exit(allOk?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Both Alameda and Sacramento have operating > 0 AND revenue > 0 rows, NULL source_url = 0, and population > 0 (probe exits 0).
    - A spot-checked county's FY2024 op/rev totals match the Task 1 canary dry-run figures.
    - 64-02-SUMMARY.md records the per-county load + verification as the CGB-01 (directory-only two) evidence record, noting all-governmental-funds basis and any No-data FY gaps.
  </acceptance_criteria>
  <done>Alameda + Sacramento county governments are loaded, sourced, populated, and verified read-only; their pages render icicle/summary + per-capita; CGB-01 (directory-only two) documented.</done>
</task>

</tasks>

<verification>
- Alameda + Sacramento loaded op+rev FY2003–2024 (where SCO provides), all-governmental-funds, every row durably /d/-sourced + per-year feed population.
- Each county entity population > 0 (FY2024 loaded first); county pages render icicle/summary + per-capita (no longer directory-only); city rows untouched.
- Read-only verification, production DB, $0 spend; 64-02-SUMMARY.md is the CGB-01 (directory-only two) evidence.
</verification>

<success_criteria>
- CGB-01 (directory-only two) satisfied: Alameda + Sacramento county governments loaded (op+rev, FY2003–2024, all-funds basis, durably sourced, per-year population) → pages no longer directory-only.
- Never-overwrite preserved any pre-existing rows; city rows untouched (D-08); $0 (D-10); production DB only (D-09).
- 64-02-SUMMARY.md documents the result. Together with 64-01, CGB-01 is fully satisfied (8/8 counties).
</success_criteria>

<output>
Create `.planning/phases/64-socal-county-government-budgets/64-02-SUMMARY.md` when done.
</output>
