---
phase: 90-city-loads-mncity-01-mncity-02
plan: 02
type: execute
wave: 2
depends_on: ["90-01"]
files_modified:
  - scripts/mnCityBasis.json
  - scripts/mnCityResidual.json
  - scripts/loadMNOSABatch.js
autonomous: false
requirements: [MNCITY-01, MNCITY-02]
must_haves:
  truths:
    - "Every filing Minnesota city is live in production for the full FY2012–FY2023 range with operating + revenue, every row stamped data_source='Minnesota Office of the State Auditor City/County Finances Report' + the per-FY city source_url + source_date (MNCITY-01); the load ran serially via loadMNOSABatch across all available FY (CONTEXT D-01/D-05)"
    - "Per-capita renders: each loaded city/FY has population set from that FY's Population column (CONTEXT D-06), so the app computes per-capita without a missing-population gap"
    - "Per-entity GAAP/Cash basis (from GAAPInd) is recorded in a committed scripts/mnCityBasis.json keyed per (city, FY) — a city can be GAAP one year and Cash another; no DB schema change (CONTEXT D-02; MNCITY-02)"
    - "Cross-FY source-gap residual is written to a committed scripts/mnCityResidual.json (city + reason) and those cities are NOT created as municipalities — no phantom rows (CONTEXT D-03; MNCITY-02)"
    - "The load is idempotent and source-safe: a second full run creates 0 new municipalities, leaves per-(city,FY,dataset) row counts unchanged, and the never-overwrite guard skips any (muni,FY,dataset) already owned by a different/richer data_source (MNCITY-02)"
  artifacts:
    - path: "scripts/mnCityBasis.json"
      provides: "Committed per-(city,FY) GAAP/Cash basis record from GAAPInd (MNCITY-02) — no DB schema change"
      contains: "basis"
    - path: "scripts/mnCityResidual.json"
      provides: "Committed cross-FY source-gap residual — cities with no financial row in some/all FY (no phantom municipalities)"
      contains: "reason"
    - path: "scripts/loadMNOSABatch.js"
      provides: "The plan 90-01 batch driver, run live across FY2012–2023 (plus any minimal live-path fix surfaced during the real run)"
      contains: "loadMNOSABatch"
  key_links:
    - from: "FY2012–2023 MN OSA all-cities workbooks (cired_<YY>_data.xlsx)"
      to: "treasury.budgets operating + revenue rows for every filing MN city, sourced + per-capita"
      via: "loadMNOSABatch live serial run -> importEntity -> treasury_sync_city_budget with never-overwrite guard"
      pattern: "loadMNOSABatch"
---

<objective>
Run the Phase 90 batch loader **live** against production Supabase to load every filing Minnesota city — operating + revenue — across the full FY2012–FY2023 range (CONTEXT D-01), per-capita and sourced. Then verify in-phase: DB row/municipality counts, a published-figure read-back (Minneapolis FY2023 exact), per-capita population set, every row sourced, and idempotency (a second run changes nothing). Finally write the committed `scripts/mnCityBasis.json` (per-(city,FY) GAAP/Cash from GAAPInd) and `scripts/mnCityResidual.json` (cross-FY source-gap, no phantom municipalities).

This delivers MNCITY-01 (all cities loaded, sourced, per-capita) and MNCITY-02 (basis recorded per-entity + documented residual + idempotency). The batch driver (plan 90-01) does the work; this plan acquires all the data, runs it live, verifies, and records basis + residual. The deeper cross-phase source-chain audit + ACFR reconciliation + UAT are Phase 93 — this plan only does the in-phase spot-check (ROADMAP SC#1–4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-city-loads-mncity-01-mncity-02/90-CONTEXT.md
@scripts/loadMNOSABatch.js
@scripts/loadMNOSA.js
@scripts/mnOsaDatasets.json

<interfaces>
<!-- The batch driver (plan 90-01) is the tool; this plan supplies data + runs it live + verifies. -->

- Live writes require the gitignored `.env` `SUPABASE_SERVICE_KEY` and run SERIALLY on the main tree (CONTEXT D-05). Writes are safe to repeat — the never-overwrite guard (inside importDataset) protects any richer pre-existing source and importEntity/importDataset are idempotent.
- `scripts/mnOsaDatasets.json` (Phase 89) — the FY2012–2023 city_url manifest (all URLs HTTP-200 confirmed). `resolveSourceUrl(fy,'city')` reads it. Committed provenance for source_url — verify URLs still resolve; leave the file unchanged unless a URL moved.
- `loadMNOSABatch({ fy, dryRun, limit, file })` → `{ fy, processed, basis:{GAAP,Cash}, residual:[...], failures:[...], results:[...] }`. The CLI driver downloads the missing workbook into gitignored `_mn-recon/` via the manifest.
- Verification probes are read-only against production. Per auto-memory `feedback_supabase_migration_mcp`, prefer the `mcp__supabase-local` tools for DB reads/checks.
- Minneapolis FY2023 read-back target (from Phase 89 89-PROOF.md): revenue $1,192,133,233; operating (expenditure) $1,193,970,288; basis GAAP; parent county Hennepin; population 433,633.
- basis source = the per-city `summary.basis` returned by importEntity (from GAAPInd); residual source = the cross-FY accumulation of cities with no financial row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire all FY2012–2023 city workbooks into _mn-recon/ (pre-flight)</name>
  <files>scripts/loadMNOSABatch.js</files>
  <read_first>
    - scripts/mnOsaDatasets.json (the 12 city_url entries FY2012–2023; city_floor 2012)
    - scripts/loadMNOSABatch.js (the acquisition/download path added in 90-01)
  </read_first>
  <action>
    Ensure every FY2012–2023 city workbook is present locally in gitignored `_mn-recon/`. For each manifest entry, download `cired_<YY>_data.xlsx` from its `city_url` if missing (the driver does this on demand, but pre-fetching surfaces any dead URL before the live run). Confirm each downloaded file opens with exceljs and has a `Governmental Funds` sheet. Record any manifest URL that no longer resolves as a coverage-gap note for the SUMMARY (do NOT silently skip). Do not commit the XLSX files (gitignored); `scripts/mnOsaDatasets.json` stays the committed provenance — verify its URLs, leave it unchanged unless one moved.
  </action>
  <acceptance_criteria>
    - Every FY2012–2023 city workbook exists under `_mn-recon/` and opens with exceljs (Governmental Funds sheet present)
    - Any non-resolving manifest URL is captured in a note for the SUMMARY (coverage gap), not silently dropped
    - No XLSX file is staged for commit; `scripts/mnOsaDatasets.json` is unchanged (or a moved-URL fix only)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-range dry-run across FY2012–2023 (pre-flight, zero writes)</name>
  <files>scripts/loadMNOSABatch.js</files>
  <read_first>
    - scripts/loadMNOSABatch.js (loadMNOSABatch return shape + CLI)
    - .planning/phases/90-city-loads-mncity-01-mncity-02/90-CONTEXT.md (D-02 basis tally; D-03 residual)
  </read_first>
  <action>
    Before any write, run the batch loader in `--dry-run` for EACH fiscal year 2012 through 2023 and eyeball the per-FY summaries: a plausible city count per year (~800+ for recent years; older years may differ), the GAAP-vs-Cash basis distribution looks sane, Minneapolis FY2023 totals match the Phase 89 proof ($1,192,133,233 revenue / $1,193,970,288 operating), and no city errors (`failures` empty). Capture, per year, the processed count + basis distribution + residual count for the SUMMARY. Accumulate the cross-FY residual + the per-(city,FY) basis map for Task 4.
  </action>
  <acceptance_criteria>
    - A dry-run for each FY 2012–2023 exits 0 with zero writes and zero unhandled per-city failures
    - Minneapolis FY2023 dry-run shows revenue $1,192,133,233 and operating $1,193,970,288 (matches the Phase 89 proof)
    - Each year's processed count + basis distribution + residual count is captured for the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live load FY2012–2023 (serial, idempotent, source-safe)</name>
  <files>scripts/loadMNOSABatch.js</files>
  <read_first>
    - scripts/loadMNOSABatch.js (live write path; failures-log append)
    - scripts/loadMNOSA.js (importEntity -> treasury_ensure_municipality + importDataset never-overwrite guard)
    - auto-memory project_sync_city_budget_not_source_safe (the guard is mandatory — already inside importDataset)
  </read_first>
  <action>
    With the gitignored `.env` `SUPABASE_SERVICE_KEY` sourced, run the batch loader LIVE (no `--dry-run`) on the main tree, SERIALLY, for every fiscal year 2012 → 2023:
    `node scripts/loadMNOSABatch.js --fy <YYYY>` for each year (a thin wrapper loop is fine; keep it serial — no parallel RPC fan-out). Each (city, FY) writes operating + revenue under its GAAPInd basis with the per-FY city source_url. The never-overwrite guard skips any (muni,FY,dataset) already owned by a different data_source; per-city errors append to `scripts/load-mn-cities.failures.txt` without aborting the run. Capture per-FY processed/written counts + any failures for the SUMMARY.

    NOTE (autonomous:false): this is the production-write task — confirm the target Supabase project with the user before the first live FY; then proceed serially (writes are idempotent + guarded). ~853 cities × 12 FY × 2 datasets ≈ 20K RPCs (~30–60 min) — expected.
  </action>
  <acceptance_criteria>
    - The live run completes for all of FY2012–2023; every processed city/FY has operating + revenue rows in production, each stamped data_source='Minnesota Office of the State Auditor City/County Finances Report' + a non-null source_url + source_date
    - `scripts/load-mn-cities.failures.txt` contains no unexplained per-city failures (any entry is triaged in the SUMMARY)
    - The 5 RCV anchor cities (Minneapolis, St. Paul, St. Louis Park, Bloomington, Minnetonka) are all present with FY2023 data
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: In-phase verification + write scripts/mnCityBasis.json + scripts/mnCityResidual.json</name>
  <files>scripts/mnCityBasis.json, scripts/mnCityResidual.json</files>
  <read_first>
    - scripts/loadMNOSABatch.js (the basis tally + residual arrays from the runs)
    - .planning/phases/90-city-loads-mncity-01-mncity-02/90-CONTEXT.md (D-02 basis file; D-03 residual file; D-06 per-capita)
    - scripts/ohioCityResidual.json (the committed residual-file shape to mirror)
    - feedback_supabase_migration_mcp (prefer mcp__supabase-local tools for the read-only DB probes)
  </read_first>
  <action>
    Run read-only verification probes against production and an idempotency re-run:
    - **Spot-check (SC#1):** confirm the STORED Minneapolis FY2023 revenue total = $1,192,133,233 and operating total = $1,193,970,288 (read back the stored tree/totals, not just the loader output) — within rounding.
    - **Coverage (SC#1):** count distinct MN cities with budgets and per-(FY,dataset) row counts; confirm ~853 cities for recent years and that every loaded row has a non-null source_url + data_source.
    - **Per-capita (SC#2/D-06):** confirm sampled loaded city/FY rows have a non-zero population set (a few across years) so per-capita renders.
    - **Basis (SC#3/MNCITY-02):** write `scripts/mnCityBasis.json` from the accumulated per-(city,FY) basis tally — an object with a note + a `cities` map (or array of `{name, fy, basis}`) covering GAAP + Cash; confirm at least one Cash-basis city is recorded.
    - **Idempotency (SC#4):** re-run one FY live and confirm 0 new municipalities + unchanged per-(city,FY,dataset) row counts.
    - **Residual (SC#3/D-03):** write `scripts/mnCityResidual.json` from the accumulated cross-FY residual — a note + a `cities` array of `{ name, reason }` (e.g. "no Governmental Funds financial row in FY20XX"). Confirm none of these names exist as phantom municipalities in production. Commit both files.
  </action>
  <acceptance_criteria>
    - Stored Minneapolis FY2023 revenue = $1,192,133,233 and operating = $1,193,970,288 (read back from the DB, within rounding)
    - Every loaded MN city budget row has a non-null source_url + data_source='Minnesota Office of the State Auditor City/County Finances Report'; sampled rows have a non-zero population (per-capita renders)
    - `scripts/mnCityBasis.json` exists, is committed, records per-(city,FY) GAAP/Cash, and includes ≥1 Cash-basis city
    - The idempotency re-run creates 0 new municipalities and leaves per-(FY,dataset) row counts unchanged
    - `scripts/mnCityResidual.json` exists, is committed, lists each source-gap city + reason, and none of those names exist as MN municipalities (no phantom rows)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Live load ran serially for FY2012–2023; every filing city has sourced operating + revenue with per-FY source_url + source_date (MNCITY-01).
- Stored Minneapolis FY2023 totals match the Phase 89 proof (read back from DB); sampled rows have population set (per-capita renders, D-06).
- `scripts/mnCityBasis.json` records per-(city,FY) GAAP/Cash (≥1 Cash city); `scripts/mnCityResidual.json` documents source-gap cities — no phantom municipalities (MNCITY-02; D-02/D-03).
- Idempotency: a second run creates 0 new munis and changes no row counts; the never-overwrite guard protects richer pre-existing sources.
- Deeper cross-phase source-chain audit + ACFR reconciliation + UAT are Phase 93 — this plan does the in-phase spot-check only.
</verification>
