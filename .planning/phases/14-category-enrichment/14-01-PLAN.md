---
phase: 14-category-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/enrichCategories.js  # invoked, not modified
autonomous: false

must_haves:
  truths:
    - "Every Garland budget_categories row (60 rows, FY2025) has a non-blank description visible in the app"
    - "Every Wylie budget_categories row (44 rows, FY2026) has a non-blank description visible in the app"
    - "Garland enrichments in treasury.category_enrichment have municipality_id = 'fd659c24-4870-455f-936c-815ea516dce2' (NOT NULL)"
    - "Wylie enrichments in treasury.category_enrichment have municipality_id = '13c35569-f44d-4354-86f0-28f578c32669' (NOT NULL)"
    - "Re-running enrichment for Garland or Wylie does not create duplicate rows"
  artifacts:
    - path: "treasury.category_enrichment"
      provides: "Plain-language descriptions for Garland + Wylie categories"
      contains: "rows with municipality_id IN ('fd659c24-4870-455f-936c-815ea516dce2','13c35569-f44d-4354-86f0-28f578c32669')"
  key_links:
    - from: "scripts/enrichCategories.js --city flag"
      to: "treasury.category_enrichment.municipality_id"
      via: "City lookup in script joins municipality_id by name+state"
      pattern: "--city.*--state"
    - from: "treasury.category_enrichment"
      to: "app UI (category description display)"
      via: "Join via category_id; municipality_id scoping prevents bleed"
      pattern: "category_enrichment.*JOIN.*municipality_id"
---

<objective>
Run category enrichment for Garland TX and Wylie TX so citizens see plain-language descriptions for every budget category in those two cities.

Purpose: Closes requirements ENR-01 (Garland) and ENR-02 (Wylie). Garland is the largest of the 5 Collin County cities (60 categories) and Wylie is the second-largest (44 categories) — running them first de-risks the phase by exercising the full enrichment pipeline against the broadest category vocabulary before tackling the remaining 3 cities in 14-02.

Output: ~104 new rows in treasury.category_enrichment, all scoped to the correct municipality_id, visible as plain-language descriptions in the app at treasurytracker.empowered.vote.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/enrichCategories.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run enrichment for Garland and Wylie to confirm cost and scope</name>
  <files>scripts/enrichCategories.js (invoked, not modified)</files>
  <action>
Run the enrichment script in --dry-run mode for both Garland and Wylie to preview the work without making API calls or writing rows.

Execute (from project root, in this order):

1. `node scripts/enrichCategories.js --city "Garland" --state "TX" --dry-run`
2. `node scripts/enrichCategories.js --city "Wylie" --state "TX" --dry-run`

Capture stdout from each run. Confirm the dry-run reports:
- Garland: ~30 depth-0 categories targeted (script defaults to --depth 0)
- Wylie: ~20 depth-0 categories targeted
- Both runs target municipality_id matching the IDs in the planning context (Garland fd659c24-4870-455f-936c-815ea516dce2, Wylie 13c35569-f44d-4354-86f0-28f578c32669)
- No "NULL municipality_id" or "universal fallback" warnings appear

Estimated cost across this plan + 14-02: ~$0.60–$1.50 total (well under the $5 approval threshold from project memory). No pre-approval needed.

If the dry-run output shows any of:
- 0 categories targeted (means the city/state filter didn't match — STOP and surface error)
- Municipality_id reported as NULL or missing (means scoping is broken — STOP)
- Wildly more categories than expected (>2x — STOP and surface)

…stop and report the discrepancy before running the live enrichment.
  </action>
  <verify>
Both dry-run commands exit 0. Stdout includes a target count > 0 for each city and the correct municipality_id is referenced. No NULL/universal warnings.
  </verify>
  <done>
Dry-run output captured for both Garland and Wylie, target counts and municipality_ids match expectations, no scoping warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run live enrichment for Garland and Wylie, then verify DB scoping</name>
  <files>treasury.category_enrichment (DB rows inserted via scripts/enrichCategories.js)</files>
  <action>
Run the live enrichment for both cities, then verify rows landed in the DB with correct municipality_id scoping.

Step A — Live enrichment (run sequentially, NOT in parallel, to keep API rate manageable):

1. `node scripts/enrichCategories.js --city "Garland" --state "TX"`
2. `node scripts/enrichCategories.js --city "Wylie" --state "TX"`

Each command must exit 0. Capture stdout for the SUMMARY.md.

Important: do NOT pass --force. The script is idempotent — re-running on already-enriched categories is a no-op. Avoid --force because it would re-bill API calls unnecessarily.

Step B — DB verification via mcp__supabase-local__execute_sql (per project memory: prefer Supabase MCP over manual Dashboard checks).

Run these queries and confirm results:

1. Count + NULL check for Garland:
   ```sql
   SELECT
     COUNT(*) AS total,
     COUNT(*) FILTER (WHERE municipality_id IS NULL) AS null_count,
     COUNT(*) FILTER (WHERE description IS NULL OR description = '') AS blank_desc
   FROM treasury.category_enrichment
   WHERE municipality_id = 'fd659c24-4870-455f-936c-815ea516dce2';
   ```
   Expected: total >= 30, null_count = 0, blank_desc = 0.

2. Count + NULL check for Wylie:
   ```sql
   SELECT
     COUNT(*) AS total,
     COUNT(*) FILTER (WHERE municipality_id IS NULL) AS null_count,
     COUNT(*) FILTER (WHERE description IS NULL OR description = '') AS blank_desc
   FROM treasury.category_enrichment
   WHERE municipality_id = '13c35569-f44d-4354-86f0-28f578c32669';
   ```
   Expected: total >= 20, null_count = 0, blank_desc = 0.

3. Duplicate check (idempotency — re-run the enrichment for Garland once more, then confirm no duplicates):
   - Re-run: `node scripts/enrichCategories.js --city "Garland" --state "TX"` (no --force)
   - Query:
     ```sql
     SELECT category_id, COUNT(*) AS n
     FROM treasury.category_enrichment
     WHERE municipality_id = 'fd659c24-4870-455f-936c-815ea516dce2'
     GROUP BY category_id
     HAVING COUNT(*) > 1;
     ```
   - Expected: 0 rows (no duplicates).

If any expectation fails (NULL municipality_id, blank descriptions, duplicates), STOP and surface the failure before proceeding to Task 3.
  </action>
  <verify>
Both enrichment commands exit 0. SQL count queries return total > 0 for both cities. NULL municipality_id count = 0. Blank description count = 0. Duplicate query returns 0 rows after re-run.
  </verify>
  <done>
Garland and Wylie enrichments exist in treasury.category_enrichment with correct municipality_id scoping, non-blank descriptions, and idempotency confirmed via re-run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Category enrichment has been run for Garland TX and Wylie TX. Enrichment rows are in treasury.category_enrichment with correct municipality_id scoping (verified via SQL in Task 2). Re-run idempotency confirmed (no duplicates).

The app at treasurytracker.empowered.vote pulls enrichment descriptions from this table joined by municipality_id. Plain-language descriptions should now be visible for every Garland and Wylie category.
  </what-built>
  <how-to-verify>
1. Open https://treasurytracker.empowered.vote in a browser (signed in or out — both Inform tier and unauthenticated have full read access per project memory).

2. Navigate to Garland TX:
   - Find Garland in the city/municipality picker.
   - Open the operating budget view for FY2025.
   - Expand 5+ top-level categories (e.g., Police, Fire Department, Parks & Recreation, Library, Transportation).
   - Confirm each shows a plain-language description (not blank, not "TBD", not raw category name).

3. Navigate to Wylie TX:
   - Find Wylie in the city picker.
   - Open the operating budget view for FY2026.
   - Expand 5+ top-level categories (e.g., Police, Fire, Library, Parks, Finance).
   - Confirm each shows a plain-language description.

4. Bleed-through check (per project memory — prior bug caused city-specific descriptions to bleed into other cities via the universal NULL municipality_id fallback):
   - Open Garland's "Police" description.
   - Open Wylie's "Police" description.
   - Confirm the two descriptions are EITHER (a) genuinely city-specific and different OR (b) clearly correct for both contexts. If the Wylie description references "Garland" or vice-versa, that's bleed — flag it.

Expected outcome: All categories for both cities show meaningful plain-language descriptions. No bleed between cities.

If issues found: describe them ("Wylie Police description references Garland", "Some Garland categories still blank", etc.) and Claude will diagnose before proceeding to 14-02.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to 14-02 (Sachse + Murphy + Princeton), or describe issues for diagnosis.</resume-signal>
</task>

</tasks>

<verification>
1. Enrichment script exited 0 for both Garland and Wylie.
2. SQL confirms total enrichment rows > 0 for each city's municipality_id, with 0 NULL municipality_id rows and 0 blank descriptions.
3. Re-run of Garland enrichment produced no duplicates (idempotency confirmed).
4. Human-verify checkpoint approved — descriptions visible in app for both cities, no bleed-through.
</verification>

<success_criteria>
- [ ] ENR-01 satisfied: Garland enrichment complete, descriptions visible in app, municipality_id correctly scoped
- [ ] ENR-02 satisfied: Wylie enrichment complete, descriptions visible in app, municipality_id correctly scoped
- [ ] 0 enrichment rows for Garland or Wylie have NULL municipality_id
- [ ] 0 enrichment rows for Garland or Wylie have blank descriptions
- [ ] Re-running enrichment for Garland produced 0 duplicates
- [ ] Human verified plain-language descriptions visible in app for both cities
- [ ] No bleed-through between Garland and Wylie descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/14-category-enrichment/14-01-SUMMARY.md` capturing:
- Total enrichment rows created for Garland (count from SQL)
- Total enrichment rows created for Wylie (count from SQL)
- Actual API cost incurred (from script stdout if reported, else estimate)
- Any categories that required manual intervention or had low confidence
- Confirmation that bleed-through check passed
- Any deviations from the plan
</output>
