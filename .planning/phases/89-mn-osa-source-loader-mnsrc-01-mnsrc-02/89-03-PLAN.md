---
phase: 89-mn-osa-source-loader-mnsrc-01-mnsrc-02
plan: 03
type: execute
wave: 3
depends_on: ["89-02"]
files_modified:
  - scripts/loadMNOSA.test.mjs
autonomous: true
requirements: [MNSRC-01, MNSRC-02]
must_haves:
  truths:
    - "Offline unit tests scripts/loadMNOSA.test.mjs cover the pure parser logic with node:test + node:assert/strict: the formula-safe cellNum (rejects formula-without-result/richText/[object Object]), the 3-level tree build, the subtotal-vs-leaf de-dup (no double-count, D-03), and basis resolution from GAAPInd (D-07)"
    - "Data-backed tests assert HARD against the gitignored _mn-recon/ samples when present and SKIP gracefully when absent (fresh clone): Minneapolis revenue/expenditure tree sums tie to the row's own Total Revenues/Total Expenditures, Intergovernmental present as a 3-level group (D-04), a function has nested current/capital leaves (D-02), and no `& Other Sources/Uses`/`Total Current Expenditures`/`Total Capital Outlay` label appears as a tree node"
    - "node --test scripts/loadMNOSA.test.mjs exits 0 with all tests passing (the MNSRC-02 'offline unit tests pass' criterion)"
  artifacts:
    - path: "scripts/loadMNOSA.test.mjs"
      provides: "Offline unit tests for the MN OSA parser/tree/basis logic, asserting Minneapolis FY2023 tree-sum self-consistency + the D-03 double-count guard"
      contains: "buildRevenueTree"
  key_links:
    - from: "scripts/loadMNOSA.js exported pure functions"
      to: "node --test verification of tree build, double-count guard, and GAAPInd basis"
      via: "node:test against _mn-recon/ samples (skip-if-absent) + pure-helper assertions"
      pattern: "loadMNOSA.test"
---

<objective>
Lock the MN loader's pure logic behind offline unit tests — the "offline unit tests pass" half of MNSRC-02 and the regression net Phases 90/91 lean on. Mirror `scripts/loadOhioAOS.test.mjs`: pure-helper tests always run; data-backed tests assert hard against the gitignored `_mn-recon/` samples when present and skip gracefully when absent (the samples are gitignored, so a fresh clone must not fail). The MN-specific assertions beyond Ohio's: the 3-level tree shape (D-01/D-02), the subtotal-vs-leaf de-dup / double-count guard (D-03), and basis resolution from the per-row GAAPInd column (D-07).

No loader changes (that is 89-02) and no DB writes — pure offline verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-CONTEXT.md
@scripts/loadMNOSA.js
@scripts/loadOhioAOS.test.mjs

<interfaces>
<!-- node:test structure mirrors loadOhioAOS.test.mjs exactly: a SAMPLE path under _mn-recon/, a
     HAVE_SAMPLE existsSync gate, pure-helper tests that always run, data-backed tests gated on the sample. -->

Sample (gitignored — absent on fresh clone -> data-backed tests SKIP):
  _mn-recon/cired_23_data.xlsx        (city FY2023 — Minneapolis + a Cash-basis city tests)
  _mn-recon/<county file>             (county FY2023 — county layout test, if 89-01 pinned it)
Pure-helper tests (cellNum, tree-map-driven build on a synthetic row, double-count guard) always run.

Assertions to cover:
- cellNum: number -> number; {result:n} -> n; {formula} w/o result -> NaN; {richText} -> NaN; null -> NaN.
- DATA_SOURCE_NAME === 'Minnesota Office of the State Auditor City/County Finances Report'.
- buildRevenueTree(wb,"Minneapolis"): total === row Total Revenues (finite); Intergovernmental present as a node WITH children (3-level, D-04); every amount finite.
- buildExpenditureTree(wb,"Minneapolis"): total === row Total Expenditures; at least one function has nested current/capital leaves (D-02); NO node labeled `Total Current Expenditures`/`Total Capital Outlay`/`& Other Sources`/`& Other Uses` (D-03/D-05).
- Double-count guard: for a group with a subtotal_label, sum of placed children === workbook subtotal (within 0.5%).
- entityBasis(wb,"Minneapolis") in {'GAAP','Cash'}; a known Cash-basis entity returns 'Cash' (D-07).
- Tree variable-depth shape: leaves are {n,a} (no c); group/parent nodes carry c:[...].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Offline unit tests for the MN parser/tree/basis logic (assert Minneapolis FY2023 self-consistency)</name>
  <files>scripts/loadMNOSA.test.mjs</files>
  <read_first>
    - scripts/loadOhioAOS.test.mjs (node:test structure; the existsSync SAMPLE gate; how data-backed tests skip gracefully when absent and assert hard when present; the pure-helper tests that always run)
    - scripts/loadMNOSA.js (the exported functions under test: buildRevenueTree, buildExpenditureTree, entityPopulation, entityCounty, entityBasis, cellNum, DATA_SOURCE_NAME)
    - scripts/mnOsaTreeMap.json (the hierarchy the build is asserted against — subtotal_label groups, validation_only_totals)
    - .planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-CONTEXT.md (D-02/D-03/D-04/D-05/D-07 — what the assertions enforce)
  </read_first>
  <action>
    Create `scripts/loadMNOSA.test.mjs` using `node:test` + `node:assert/strict`, mirroring loadOhioAOS.test.mjs. Define `SAMPLE = _mn-recon/cired_23_data.xlsx` and a `HAVE_SAMPLE = existsSync(SAMPLE)` gate (plus the county sample if 89-01 pinned one).

    Pure-helper tests (always run): cellNum reads numbers + `{result}`, rejects `{formula}`-without-result / `{richText}` / null; DATA_SOURCE_NAME is the durable MN label. If feasible, exercise the tree builder on a small synthetic row/fixture to assert the D-03 double-count guard (children sum === subtotal) without needing the workbook.

    Data-backed tests (skip if `!HAVE_SAMPLE`, assert hard when present):
    - buildRevenueTree for Minneapolis: `total` === the row's Total Revenues (finite); an `Intergovernmental` node exists WITH non-empty `c` children (3-level, D-04); every leaf amount finite.
    - buildExpenditureTree for Minneapolis: `total` === the row's Total Expenditures; at least one function node has nested current/capital leaves (D-02); NO node labeled `Total Current Expenditures`, `Total Capital Outlay`, `Total Revenues & Other Sources`, or `Total Expenditures & Other Uses`.
    - The D-03 double-count guard holds on the real row: every group with a subtotal_label has children summing to the workbook subtotal within 0.5%.
    - entityBasis(wb,"Minneapolis") ∈ {'GAAP','Cash'}; entityPopulation finite; entityCounty non-empty.
    - A known Cash-basis city (the one 89-01/89-04 identified from GAAPInd) returns basis 'Cash' (D-07) — gate this on the sample too.
    Run with `node --test scripts/loadMNOSA.test.mjs`.
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadMNOSA.test.mjs` exits 0 with all tests passing (pure-helper tests run; data-backed tests run when `_mn-recon/cired_23_data.xlsx` is present, otherwise SKIP — they must NOT fail when the sample is absent)
    - The test file imports from `./loadMNOSA.js` and asserts the Minneapolis revenue total === row Total Revenues and expenditure total === row Total Expenditures (tree-sum self-consistency)
    - At least one test asserts the 3-level shape (Intergovernmental group has children; a function has nested current/capital leaves) and that no `& Other Sources/Uses` / `Total Current Expenditures` / `Total Capital Outlay` node is present
    - At least one test asserts the D-03 double-count guard (a subtotal_label group's children sum to the workbook subtotal) and one asserts GAAPInd-derived basis ∈ {'GAAP','Cash'}
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadMNOSA.test.mjs` -> all pass; data-backed tests assert hard against `_mn-recon/cired_23_data.xlsx` when present and skip cleanly when absent.
- Tests cover: cellNum formula-safety, Minneapolis tree-sum self-consistency (totals === row Total Revenues/Expenditures), 3-level shape (D-02/D-04), the D-03 double-count guard, and GAAPInd basis resolution (D-07).
- No loader changes, no DB writes.
- The MNSRC-02 "offline unit tests pass" criterion is satisfied here; the live de-risk dry-run proof gate (Minneapolis + Cash-basis city + county) is 89-04.
</verification>
