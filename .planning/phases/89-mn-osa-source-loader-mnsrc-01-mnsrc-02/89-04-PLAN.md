---
phase: 89-mn-osa-source-loader-mnsrc-01-mnsrc-02
plan: 04
type: execute
wave: 3
depends_on: ["89-02"]
files_modified:
  - .planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-PROOF.md
autonomous: true
requirements: [MNSRC-01, MNSRC-02]
must_haves:
  truths:
    - "The loader is PROVEN end-to-end on the RCV anchor: a --dry-run on Minneapolis (latest confirmed XLSX FY) parses a 3-level revenue + 3-level expenditure tree whose sums TIE to that row's own Total Revenues / Total Expenditures columns (the self-consistency de-risk gate) with zero writes — the explicit prove-before-bulk gate for the whole milestone (CONTEXT D-06)"
    - "The GAAPInd basis path is PROVEN: a --dry-run on one real Cash-basis (GAAPInd) city parses cleanly through the same builders, reports basis 'Cash', and its tree sums tie to its own row totals — de-risking the Phase 90 bulk's smaller/Cash entities before any load (CONTEXT D-07)"
    - "The county path is PROVEN: a --dry-run on ONE county (using the 89-01-pinned county file + the 89-01 county-layout notes, --entity-type county) parses cleanly and its tree sums tie to the county row's own totals — confirming the independently-verified county layout works end-to-end and foreclosing the Ohio county-layout defect (CONTEXT D-08)"
    - "A committed 89-PROOF.md records the three dry-run results (entity, FY, parsed totals vs row totals, basis, population, parent county), the pinned available XLSX FY range + floor, and any city-vs-county layout divergences — the auditable de-risk record handed to Phases 90/91; ZERO DB writes occur in this phase"
  artifacts:
    - path: ".planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-PROOF.md"
      provides: "The committed de-risk proof: Minneapolis + Cash-basis city + county dry-run figures (parsed vs row totals), pinned FY range, county-layout divergences"
      contains: "Minneapolis"
  key_links:
    - from: "scripts/loadMNOSA.js --dry-run on Minneapolis + a Cash city + one county"
      to: "89-PROOF.md de-risk record (tree sums tie to row totals; basis + county layout proven)"
      via: "three --dry-run invocations against _mn-recon/ samples, results captured, zero writes"
      pattern: "prove-before-bulk"
---

<objective>
Prove the 89-02 loader against the milestone's de-risk gate BEFORE any bulk load — the verification half of MNSRC-01/02 and the explicit prove-before-bulk gate for v2.9 (CONTEXT specifics). Three dry-runs, all zero-write, captured in a committed proof record:

1. **Prove the headline (Minneapolis).** A `--dry-run` on Minneapolis (latest confirmed XLSX FY) must parse a 3-level revenue + 3-level expenditure tree whose sums TIE to that row's own `Total Revenues` / `Total Expenditures` columns (self-consistency). If they don't tie, the loader (89-02) or the tree-map (89-01) is wrong and must be fixed before bulk. Minneapolis is the biggest MN city + longest-running RCV jurisdiction, and pre-stages the Phase 93 ACFR reconciliation (CONTEXT D-06).
2. **Prove the basis path (one Cash-basis city).** Pick a real Cash-basis city from the GAAPInd column and `--dry-run` it: it must parse cleanly through the same builders, report basis 'Cash', and tie to its own row totals — de-risking the Phase 90 bulk's smaller/Cash entities (CONTEXT D-07).
3. **Prove the county path (one county).** Using the 89-01-pinned county file + county-layout notes, `--dry-run` ONE county with `--entity-type county`: it must parse cleanly and tie to the county row's own totals — confirming the independently-verified county layout works end-to-end (CONTEXT D-08).

Output: `89-PROOF.md` with the three results + the pinned FY range + county-layout divergences. No bulk load (Phases 90/91), no DB writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-CONTEXT.md
@scripts/loadMNOSA.js
@scripts/mnOsaDatasets.json

<interfaces>
<!-- Three dry-runs against the gitignored _mn-recon/ samples from 89-01. The proof is tree-sum
     self-consistency (parsed total === the row's own Total column), not an external figure. -->

Commands (zero-write):
- Minneapolis: `node scripts/loadMNOSA.js --file _mn-recon/cired_23_data.xlsx --entity Minneapolis --fy 2023 --dry-run`
- Cash-basis city: inspect the GAAPInd column of the city workbook to pick a city whose GAAPInd marks Cash basis, then `node scripts/loadMNOSA.js --file _mn-recon/cired_23_data.xlsx --entity "<that city>" --fy 2023 --dry-run` -> expect basis 'Cash'.
- County: `node scripts/loadMNOSA.js --file _mn-recon/<county file> --entity "<a county>" --fy 2023 --entity-type county --dry-run`.

Pass criterion (all three): the printed revenue total === the entity row's `Total Revenues` column and the printed operating total === the row's `Total Expenditures` column (self-consistency, within rounding); the trees are well-formed (Intergovernmental present in revenue, current/capital leaves in expenditure, no `& Other`/validation-only nodes); basis/population/parent-county print; the resolved source_url is from the manifest. If any dry-run fails to tie, fix scripts/loadMNOSA.js and/or scripts/mnOsaTreeMap.json (this plan may edit them) until all three pass — then re-run 89-03's tests to confirm no regression.

89-PROOF.md captures: per-entity {name, fy, entity_type, parsed revenue total, row Total Revenues, parsed operating total, row Total Expenditures, basis, population, parent county, source_url}; the pinned available XLSX FY range + floor (from mnOsaDatasets.json); the city-vs-county layout divergences (from 89-01). This is the auditable handoff to Phases 90/91.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the three de-risk dry-runs (Minneapolis + Cash-basis city + one county) and confirm tree sums tie to row totals</name>
  <files>.planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-PROOF.md</files>
  <read_first>
    - scripts/loadMNOSA.js (the loader + exported parse functions from 89-02; the --dry-run print format + --entity-type county)
    - scripts/mnOsaDatasets.json (the FY range + the city/county source URLs)
    - .planning/phases/89-mn-osa-source-loader-mnsrc-01-mnsrc-02/89-CONTEXT.md (D-06 Minneapolis headline proof; D-07 Cash-basis city proof; D-08 county proof; prove-before-bulk gate)
    - _mn-recon/cired_23_data.xlsx + the county workbook (the live samples from 89-01)
  </read_first>
  <action>
    Run the Minneapolis dry-run and confirm the parsed revenue total === the row's `Total Revenues` column and the parsed operating total === the row's `Total Expenditures` column (self-consistency within rounding), with a well-formed 3-level tree (Intergovernmental group present, current/capital leaves present, no `& Other`/validation-only nodes), basis from GAAPInd, finite population, non-empty parent county, and a manifest-resolved source_url. Zero writes.

    Inspect the GAAPInd column to pick a real Cash-basis city, dry-run it, and confirm basis 'Cash' + its tree sums tie to its own row totals. Then dry-run ONE county (`--entity-type county`, using the 89-01 county file + layout notes) and confirm it parses cleanly and ties to the county row's own totals.

    If ANY dry-run fails to tie or the parser breaks (esp. on the county file or a Cash city), fix `scripts/loadMNOSA.js` and/or `scripts/mnOsaTreeMap.json` until all three pass, then re-run `node --test scripts/loadMNOSA.test.mjs` (89-03) to confirm no regression. Write `89-PROOF.md` capturing all three results (parsed totals vs row totals, basis, population, parent county, source_url), the pinned XLSX FY range + floor, and the city-vs-county layout divergences. Perform ZERO DB writes throughout.
  </action>
  <acceptance_criteria>
    - The Minneapolis dry-run exits 0, prints a revenue total equal to the row's `Total Revenues` and an operating total equal to the row's `Total Expenditures` (self-consistency within rounding), a 3-level tree with an Intergovernmental group + current/capital leaves and no `& Other Sources/Uses`/`Total Current Expenditures`/`Total Capital Outlay` nodes, a GAAPInd basis, finite population, non-empty parent county, and a source_url from the manifest — with zero writes
    - One real Cash-basis city dry-runs cleanly, reports basis 'Cash' (D-07), and its tree sums tie to its own row totals
    - One county dry-runs cleanly with `--entity-type county` (D-08) and its tree sums tie to the county row's own totals; if the county vocabulary diverged from cities, the loader handles it (per the 89-01 notes)
    - `89-PROOF.md` exists and records all three entities' {parsed totals vs row totals, basis, population, parent county, source_url}, the pinned available XLSX FY range + floor, and the city-vs-county layout divergences
    - `node --test scripts/loadMNOSA.test.mjs` still exits 0 (no regression from any fixes), and NO DB writes occurred (no `treasury_sync_city_budget` call outside dry-run)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Minneapolis FY2023 dry-run: 3-level revenue + expenditure trees whose sums tie to the row's own `Total Revenues`/`Total Expenditures` (the de-risk gate passes — D-06), zero writes.
- One Cash-basis city dry-runs cleanly with basis 'Cash' and ties to its row totals (D-07); one county dry-runs cleanly with --entity-type county and ties to its row totals (D-08).
- `89-PROOF.md` is committed with the three results + pinned FY range/floor + city-vs-county layout divergences — the auditable de-risk handoff to Phases 90/91.
- 89-03 tests still pass after any fixes; no DB writes in the phase.
- MNSRC-01 ("proven on a sample RCV city for FY2023 — ties to Total Revenues/Total Expenditures") + MNSRC-02 (county sample ties to its row totals; GAAP/Cash basis from GAAPInd) verification halves satisfied — Phase 89 ends with a proven loader + manifest + tree-map + tests + county-layout pin; bulk load is Phase 90.
</verification>
