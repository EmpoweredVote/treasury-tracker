---
phase: 99-california-texas-acfr-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processCA.js
  - scripts/processCARevenueAcfr.js
  - scripts/processTX.js
  - scripts/processTXRevenueAcfr.js
  - scripts/cleanupStaleStateGFDataSources.mjs
autonomous: true
requirements: [ACFR-01, ACFR-02, ACFR-05, RECON-03]

must_haves:
  truths:
    - "Four loader scripts exist (CA operating, CA revenue, TX operating, TX revenue), each --dry-run-validatable with no DB writes"
    - "Each script's per-FY GF column total ties to the ACFR printed Total before it would write (tie-check gate refuses to load on mismatch)"
    - "Negative GF categories (e.g. investment income) are clamped to 0 area with the signed magnitude preserved in the label (P2 / ACFR-05)"
    - "A stale-data_sources cleanup script can delete ca-lao-gf-operating + ca-dof-gf-revenue (CA) and tx-gf-operating + tx-gf-revenue (TX) idempotently"
  artifacts:
    - path: "scripts/processCA.js"
      provides: "CA GF operating (spend-by-function) loader, FY2020-2025, General column, thousands"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processCARevenueAcfr.js"
      provides: "CA GF revenue-by-source loader with P2 clamp, FY2020-2025"
      contains: "clampForRender"
    - path: "scripts/processTX.js"
      provides: "TX GF operating loader, FY2015-2024 (-FY2016), General Revenue Fund column, thousands"
      contains: "treasury_sync_budget_tree"
    - path: "scripts/processTXRevenueAcfr.js"
      provides: "TX GF revenue-by-source loader with P2 clamp, FY2015-2024 (-FY2016)"
      contains: "clampForRender"
    - path: "scripts/cleanupStaleStateGFDataSources.mjs"
      provides: "Idempotent delete of stale non-NASBO data_sources for CA + TX"
      contains: "ca-lao-gf-operating"
  key_links:
    - from: "scripts/processCA.js"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type='operating', p_fiscal_year, keyed (municipality_id, fy, 'operating')"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processCARevenueAcfr.js"
      to: "P2 clamp"
      via: "clampForRender + signed label on negative categories"
      pattern: "clampForRender"
---

<objective>
Build the four CA/TX ACFR loaders (operating + revenue per state) and the stale-`data_sources` cleanup script — all extraction + validation logic, NO production DB writes in this plan. The loaders are copied from the proven `processMN.js` (operating) and `processOHRevenueAcfr.js` (revenue) templates, configured per-state per Phase 98 recon, and proven via `--dry-run` with a hard per-FY tie-to-printed-total gate. Wave 2 (99-02 CA, 99-03 TX) runs these against the live table.

Purpose: Produce executable, dry-run-clean loaders so the live-write plans (99-02, 99-03) only flip the switch — extraction risk is fully retired here at $0 (pdftotext only, D-10).
Output: `scripts/processCA.js`, `scripts/processCARevenueAcfr.js`, `scripts/processTX.js`, `scripts/processTXRevenueAcfr.js`, `scripts/cleanupStaleStateGFDataSources.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-CONTEXT.md
@.planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md

<interfaces>
<!-- Template patterns to copy. Use these directly — do not re-explore the codebase. -->

From scripts/processMN.js (operating template — copy this for processCA.js / processTX.js):
  - loadEnv() reads ../.env.local then ../.env into process.env (run from repo root).
  - const SOURCES = { <fy>: { url, date }, ... }   // per-FY ACFR URL + fiscal-year-end date
  - const dataSource = (fy) => `State of <X> ACFR — General Fund (FY${fy} actual, GAAP basis)`
  - const EXPENDITURES = { <fy>: { total, confidence:'actual', categories:[{name,total,lineItems:[]}] } }
  - validate(fy): catSum must tie to total within $10M else return false
  - buildTree(fy): children = categories.filter(total>0).map → {n,a,i}; root = [{n:'<X> General Fund Budget', a:total, c:children}]
  - main(): resolve muni by name+state(+entity_type), find-or-create data_source, then per-FY:
      validate → buildTree → supabase.rpc('treasury_sync_budget_tree', {p_data_source_id, p_fiscal_year:fy, p_dataset_type:'operating', p_total:total, p_tree:jsonTree, p_row_count, p_triggered_by:'bulk_load'})
      → SELECT budgets WHERE muni+fy+dataset_type → UPDATE {source_url, source_date, data_source}

From scripts/processOHRevenueAcfr.js (revenue template — copy this for *RevenueAcfr.js — HAS the P2 clamp):
  - function clampForRender(amount) { return Math.max(amount, 0); }
  - const REVENUE = { <fy>: { total, confidence:'actual', categories:[{name,total}] } }   // total may be NEGATIVE per category
  - buildTree(fy): children = categories.map(cat => { rendered=clampForRender(cat.total); label = cat.total<0 ? `${name} (net loss — shown at 0)` : name; return {n:label, a:rendered, i:[]} })
  - p_dataset_type:'revenue' in the RPC call; post-RPC stamp identical to operating.

From scripts/loadStateGF.mjs (the NASBO loader being replaced — the replace MUST match this key):
  - State node resolve: .from('municipalities').eq('name',name).eq('state',abbr).eq('entity_type','state').single()
  - Operating rows keyed (municipality_id, fiscal_year, 'operating') via treasury_sync_budget_tree → RPC UPDATES IN PLACE (this is why ACFR operating replaces NASBO operating).
  - Exported pure helpers: clampForRender, categoryLabel(name,amount), buildCategoryLeaf, dataSourceLabel, sourceDate — reusable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CA loaders (operating + revenue), extract + tie-check FY2020-2025</name>
  <files>scripts/processCA.js, scripts/processCARevenueAcfr.js</files>
  <read_first>
    - scripts/processMN.js (operating template to copy → processCA.js)
    - scripts/processOHRevenueAcfr.js (revenue template to copy, has P2 clamp → processCARevenueAcfr.js)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md (CA window, URL pattern, GF column, units, soft-404 guard)
    - _acfr-tmp/ca/ca-acfr-2020.pdf, ca-acfr-2025.pdf (already downloaded bookends), ca-2025-gf-stmt.txt (already-extracted statement text)
  </read_first>
  <action>
    Copy processMN.js → scripts/processCA.js and processOHRevenueAcfr.js → scripts/processCARevenueAcfr.js. Set STATE_NAME='California', STATE_ABBR='CA'. Resolve the node by name+state+entity_type='state' (id e1007bf5-bac9-4b1c-878e-f6834885f850 per D-01). Window FY2020-2025 (D-02); SOURCES map = `https://www.sco.ca.gov/Files-ARD/ACFR/acfr{NN}web.pdf` for NN=20..25 with date `{FY}-06-30`. GF column = "General", 1st numeric col, units = thousands (×1000 → dollars) per D-03.
    Download the missing FY2021-2024 PDFs into _acfr-tmp/ca/ (FY2020+FY2025 already present — reuse). For EACH download apply the CA soft-404 guard: verify Content-Type=application/pdf OR size > ~1MB before trusting it; the SCO server returns HTTP 200 + an ~11,561-byte HTML body for missing files (never trust HTTP status alone).
    Extract each FY's GF column with `pdftotext -table` (NOT -layout — -layout floats numbers to wrong rows, D-09). Use the GF-statement page-finder (locate the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances; CA FY2025 is ~PDF p.64-65); narrow with -f/-l page range as light cleanup. Operating loader parses the EXPENDITURES section (spend-by-function) into EXPENDITURES{fy}; revenue loader parses the REVENUES section (rev-by-source) into REVENUE{fy}. Transcribe verbatim ACFR category names. data_source strings: `California State ACFR — General Fund (FY${fy} actual, GAAP basis)` (operating) and `California State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)` (revenue).
    Hard tie-check gate: each FY's extracted GF-column category sum MUST tie to the ACFR printed Total (revenue → printed "Total revenues"; operating → printed "Total expenditures") within $10M; validate(fy) returns false and the loader process.exit(2) on mismatch — refuse to write. Anchor on the recon-confirmed bookends: FY2020 Total revenues $155,923,876K and FY2025 $221,591,201K (exact). For any negative GF category year (watch investment income, D-15/ACFR-05) use the revenue template's clampForRender + signed label so it renders at 0 area with the true value in the label and the root total (which already nets the negative) is preserved.
    Do NOT write to the DB in this plan: run only `node scripts/processCA.js --dry-run` and `node scripts/processCARevenueAcfr.js --dry-run`. $0 spend — pdftotext only, no AI (D-10).
  </action>
  <verify>
    <automated>node scripts/processCA.js --dry-run && node scripts/processCARevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both CA loaders run --dry-run with FY2020-2025 each passing the tie-check (PASS printed per FY); FY2020 rev ties to $155,923,876K and FY2025 to $221,591,201K; any negative category prints "(net loss — shown at 0)"; no DB write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Build TX loaders (operating + revenue), extract + tie-check FY2015-2024, handle FY2016 gap</name>
  <files>scripts/processTX.js, scripts/processTXRevenueAcfr.js</files>
  <read_first>
    - scripts/processMN.js (operating template → processTX.js)
    - scripts/processOHRevenueAcfr.js (revenue template → processTXRevenueAcfr.js)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-ACFR-SOURCES.md (TX window, URL pattern, "General Revenue Fund" column, FY2016 gap log, ~3x scope note)
    - _acfr-tmp/tx/tx-acfr-2015.pdf, tx-acfr-2024.pdf (bookends), tx-2024-gf-stmt.txt
  </read_first>
  <action>
    Copy processMN.js → scripts/processTX.js and processOHRevenueAcfr.js → scripts/processTXRevenueAcfr.js. Set STATE_NAME='Texas', STATE_ABBR='TX'. Resolve node by name+state+entity_type='state'. Window FY2015-2024 (D-05); SOURCES map = `https://comptroller.texas.gov/transparency/reports/comprehensive-annual-financial/{YYYY}/96-471.pdf` with date `{FY}-08-31` (TX FYE Aug 31). GF column = "General Revenue Fund", 1st numeric col, units = thousands (×1000) per D-06.
    FY2016 handling (D-05): `96-471.pdf` returns HTTP 404 for FY2016. Try the alternate ACFR file-id on the same `/comprehensive-annual-financial/2016/` archive page (light cleanup, D-07-style); if not cleanly found, DROP FY2016 with an explicit gap-log note (a `// FY2016: dropped — 96-471.pdf 404, no clean alt file-id` comment + console.warn) and continue the other 9 FYs. Do not block the rest. (TX returns honest 404s — no soft-404 guard needed, unlike CA.)
    Download missing FY2017-2023 PDFs into _acfr-tmp/tx/ (FY2015+FY2024 present). Extract each FY's "General Revenue Fund" column with `pdftotext -table` (TX FY2024 stmt ~PDF p.52-53). Operating loader → EXPENDITURES{fy}; revenue loader → REVENUE{fy}. Transcribe verbatim category names.
    BASIS RELABEL (D-06): TX's ACFR General Revenue Fund is ~3x the NASBO TX GF ($161.4B FY2024 vs $50.5B) because it is a broader consolidated operating fund. Accept it as TX's GAAP general-fund-equivalent — do NOT carve it down. data_source strings must name the fund honestly: `Texas State ACFR — General Revenue Fund (FY${fy} actual, GAAP basis)` (operating) and `Texas State ACFR — General Revenue Fund Revenue (FY${fy} actual, GAAP basis)` (revenue). The node total will visibly jump on load; that is correct + sourced.
    Hard tie-check gate (within $10M, exit 2 on fail) tied to printed Totals; anchor on recon bookends: FY2015 Total revenues $95,574,830K and FY2024 $161,416,562K. P2 clamp on any negative category (ACFR-05). NO DB writes — run `--dry-run` only. $0 spend.
  </action>
  <verify>
    <automated>node scripts/processTX.js --dry-run && node scripts/processTXRevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both TX loaders run --dry-run; each available FY passes the tie-check; FY2015 rev ties to $95,574,830K and FY2024 to $161,416,562K; FY2016 is either located (alt file-id) or dropped with a gap-log note (does not abort the run); data_source strings say "General Revenue Fund"; no DB write occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Build the stale-data_sources cleanup script (dry-run only)</name>
  <files>scripts/cleanupStaleStateGFDataSources.mjs</files>
  <read_first>
    - scripts/loadStateGF.mjs (data_sources find/insert/update pattern + schema('treasury') usage)
    - .planning/phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md (the stale rows: CA ca-lao-gf-operating + ca-dof-gf-revenue; TX tx-gf-operating + tx-gf-revenue point at zero budgets rows)
  </read_first>
  <action>
    Create scripts/cleanupStaleStateGFDataSources.mjs (copy the env-load + createClient + schema('treasury') boilerplate from loadStateGF.mjs). It deletes the stale, non-NASBO, zero-row `data_sources` per D-04/D-07: for CA delete dataset_id IN ('ca-lao-gf-operating','ca-dof-gf-revenue'); for TX delete dataset_id IN ('tx-gf-operating','tx-gf-revenue'). Accept `--dry-run` (default off) and `--state CA|TX` filter.
    SAFETY: before deleting each data_source, assert it backs ZERO budgets rows (SELECT count budgets WHERE data_source_id = ds.id). If a target unexpectedly backs >0 rows, REFUSE to delete it and process.exit(2) (never delete a data_source that has live budgets — protects against deleting the NASBO row or anything mislabeled). NEVER touch the `*-gf-operating-nasbo` data_sources or any other state's rows. Idempotent: a second run finds the rows already gone and reports 0 deleted, exit 0.
    Run `--dry-run` only in this plan (it must print exactly which dataset_ids it WOULD delete + their 0-row assertion result). The actual deletes happen in 99-02 (CA) and 99-03 (TX).
  </action>
  <verify>
    <automated>node scripts/cleanupStaleStateGFDataSources.mjs --dry-run</automated>
  </verify>
  <done>--dry-run lists the 4 stale dataset_ids (ca-lao-gf-operating, ca-dof-gf-revenue, tx-gf-operating, tx-gf-revenue), confirms each backs 0 budgets rows, names no NASBO/other-state row, and performs no deletes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded PDF → loader | untrusted/maybe-missing remote PDF (CA soft-404 HTML body) crosses into the extractor |
| loader → treasury.budgets / data_sources | this plan does NOT cross it (dry-run only); 99-02/99-03 do |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Tampering | CA PDF download (soft-404 HTML masquerading as PDF) | mitigate | Content-Type=application/pdf OR size>~1MB check before extraction; reject HTML body |
| T-99-02 | Information disclosure | wrong fund column / wrong page extracted, mislabels figures | mitigate | per-FY tie-to-printed-total gate (≤$10M); exit 2 on mismatch — refuse to load |
| T-99-03 | Tampering | cleanup script deleting a data_source that backs live rows | mitigate | pre-delete 0-budgets-rows assertion; exit 2 if >0; never touch *-nasbo or other states |
| T-99-04 | Repudiation | unsourced figure shown as real | mitigate | every FY stamps data_source + source_url + source_date (per-year ACFR) in 99-02/99-03 |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed — copies existing scripts using already-vendored @supabase/supabase-js; pdftotext is a pre-existing system tool |
</threat_model>

<verification>
- `node scripts/processCA.js --dry-run` and `--dry-run` revenue: all FY2020-2025 PASS tie-check.
- `node scripts/processTX.js --dry-run` and revenue: all available FY PASS; FY2016 resolved or gap-logged.
- `node scripts/cleanupStaleStateGFDataSources.mjs --dry-run`: lists 4 stale ids, 0-row asserted, no deletes.
- No row written to treasury.budgets or treasury.data_sources by this plan.
</verification>

<success_criteria>
Four loaders + one cleanup script exist and pass `--dry-run` with hard per-FY tie-checks (anchored on the recon bookends), P2 clamp wired on negatives, TX GR-Fund honestly relabelled, FY2016 gap handled, CA soft-404 guarded, stale-row cleanup gated on a 0-row assertion. Zero production DB writes. $0 spend.
</success_criteria>

<output>
Create `.planning/phases/99-california-texas-acfr-upgrade-acfr-01-acfr-02-acfr-05-recon-/99-01-SUMMARY.md` when done — record per-FY extracted totals + tie results for CA and TX, the FY2016 disposition, and confirm the cleanup dry-run output.
</output>
