---
phase: 102-verification-source-chain-audit-uat
plan: 03
type: execute
wave: 2
depends_on: [102-01, 102-02]
files_modified: []
autonomous: false
requirements: [VER-02]

must_haves:
  truths:
    - "The live PRODUCTION app (treasurytracker.empowered.vote) is confirmed serving the phase-101 bundle before UAT begins (deploy propagated)"
    - "Per-node UAT evidence is gathered for all 4 upgraded nodes (CA/TX/NY/FL): Money In (revenue-by-source) enabled + renders, spending-by-function renders, basis label present + honest, source chip present (URL + date)"
    - "The 4 phase-101-deferred browser-smoke items are confirmed: NY FY2024 ≈ $93.9B revenue tree; FL FY2022 P2-clamp labels ('(net loss — shown at 0)') visible; ?dataset=revenue deep-link lands on revenue for an upgraded node; ?dataset=revenue falls back to operating (no empty card) on a NASBO node"
    - "The evidence is presented to Chris as a per-node checklist and his explicit sign-off is captured — sign-off is the gate that closes VER-02 and the milestone"
  artifacts: []
  key_links:
    - from: "live production app (phase-101 frontend)"
      to: "production API revenue/operating + basis/source data"
      via: "per-node UAT walkthrough confirming the upgraded nodes render the real ACFR revenue-by-source + spending-by-function with basis label + source chip"
      pattern: "treasurytracker.empowered.vote"
---

<objective>
Deliver VER-02: the live-app UAT and Chris's sign-off. Claude drives a guided walkthrough of the deployed production app across the 4 upgraded nodes, captures evidence (including the 4 browser-smoke checks deferred from phase 101), presents it as a per-node checklist, and Chris gives the explicit sign-off that closes the v2.11 milestone.

Purpose: confirm — on what users actually hit — that the upgrade renders correctly and honestly, then earn the human sign-off VER-02 requires.
Output: a per-node UAT checklist with captured evidence + Chris's recorded sign-off in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-CONTEXT.md

<interfaces>
<!-- Exact UAT targets — do not re-explore. -->
- **App:** treasurytracker.empowered.vote (production; serves the phase-101 bundle pushed at the close of Phase 101). API: https://ev-accounts-api.onrender.com.
- **Upgraded nodes:** California, Texas, New York, Florida (entity_type=state).
- **Per-node checks:** Money In card ENABLED + renders revenue-by-source; spending-by-function (operating) renders; basis label present + honest (ACFR GAAP); source chip present (URL + date).
- **Anchors:** NY FY2024 Money In ≈ $93.9B (Taxes — Personal income, Miscellaneous, …); FL FY2022 revenue shows the two clamped negatives at $0 with "(net loss — shown at 0)" labels.
- **Deep-link checks:** `?entity=<ny-slug>&dataset=revenue&year=2024` lands on revenue; `?entity=<nasbo-state-slug>&dataset=revenue` falls back to operating with NO disabled-but-active empty card. NASBO fallback node = any state NOT in CA/TX/NY/FL/MN/OH/VA.
- **Accepted, NOT failures:** flat revenue-tree no-drill-down ([[project_flat_source_icicle_limitation]]); TX GR-Fund ~3× NASBO scale (relabelled honestly).
- **Evidence sources:** the deployed app in a browser (visual) + the production API endpoints (`/api/treasury/cities`, `/api/treasury/*`) for value/label/source confirmation when a headless browser is unavailable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm prod deploy + gather per-node UAT evidence</name>
  <files>(no source files — evidence gathering)</files>
  <read_first>
    - .planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-CONTEXT.md (D-08 drive-on-prod, D-09 per-node evidence + the 4 folded-in phase-101 smoke items, D-10 sign-off gate; D-11 windows frozen as-loaded — no extension this phase)
    - .planning/phases/101-revenue-view-url-robustness-revux-01-revux-02/101-01-SUMMARY.md (what the phase-101 frontend change does + the smoke cases it deferred to here)
  </read_first>
  <action>
    First confirm the live production app is serving the phase-101 bundle (deploy propagated) — load treasurytracker.empowered.vote and/or confirm the production API returns `revenue` in `available_datasets` for CA/TX/NY/FL. Then gather evidence for each of the 4 upgraded nodes: Money In enabled + revenue-by-source renders; spending-by-function renders; basis label present + honest; source chip (URL + date) present. Capture the specific anchors: NY FY2024 ≈ $93.9B revenue tree; FL FY2022 clamp labels. Confirm the 2 deep-link cases (upgraded → revenue; NASBO → operating fallback, no empty card). If a headless browser is available, capture screenshots; otherwise capture the production-API responses (values, data_source/source_url/source_date, available_datasets) plus a code-level confirmation against the phase-101 frontend logic, and clearly label which evidence is visual vs API/code. Assemble it all into a per-node checklist. Do NOT make any backend or frontend change — this is confirmation only.
  </action>
  <verify>
    <automated>echo "Manual/evidence task — no automated gate; evidence assembled in SUMMARY for Chris's review"</automated>
  </verify>
  <done>Prod deploy confirmed live; a per-node checklist with captured evidence (visual and/or API+code, labelled) exists for all 4 nodes, including the NY $93.9B + FL clamp anchors and the 2 deep-link cases. No code/backend change made.</done>
</task>

<task type="checkpoint">
  <name>Task 2: Present the UAT checklist to Chris and capture sign-off</name>
  <files>(no source files — human sign-off gate)</files>
  <read_first>
    - (the per-node checklist assembled in Task 1)
  </read_first>
  <action>
    Present the per-node UAT checklist to Chris (the evidence from Task 1, organized per node with the anchors and deep-link results). Ask Chris to review against the live app and give an explicit sign-off (or list any item he wants corrected). This is a `checkpoint:human-verify` gate — VER-02 cannot close without Chris's sign-off. If Chris flags an item: record it; if it is a real frontend/data defect, route it (a frontend fix → a follow-up plan/phase; a data integrity issue → 102-02-class inline fix) rather than forcing sign-off.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — Chris sign-off required; no automated gate"</automated>
  </verify>
  <done>Chris has reviewed the per-node checklist against the live app and given an explicit sign-off (recorded verbatim in the SUMMARY), OR any flagged item is recorded and routed. VER-02 is closed only on sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live app / production API → UAT evidence | read-only observation; no writes |
| Chris sign-off → milestone close | human gate; the authority that closes VER-02 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-03-01 | Repudiation | sign-off claimed without real review | mitigate | capture Chris's sign-off verbatim + the evidence checklist it was given against, in the SUMMARY |
| T-102-03-02 | Spoofing (false-pass) | API/code evidence substituted for actual visual confirmation without disclosure | mitigate | label each evidence item visual vs API/code; Chris's sign-off is against the live app, not the harness's claim |
| T-102-03-03 | Information disclosure | none — all data is public finance data on a public app | accept | public-record only |
</threat_model>

<verification>
- Production deploy confirmed serving the phase-101 bundle (revenue in available_datasets for the 4 nodes).
- Per-node checklist complete for CA/TX/NY/FL with the NY $93.9B + FL clamp anchors and both deep-link cases; evidence labelled visual vs API/code.
- Chris's explicit sign-off captured verbatim in the SUMMARY (or flagged items recorded + routed).
</verification>

<success_criteria>
VER-02: live-app UAT across the 4 upgraded nodes (revenue-by-source + spending-by-function + basis label + source chip, plus the folded-in phase-101 smoke checks) completed and signed off by Chris on the live production app — closing the v2.11 milestone.
</success_criteria>

<output>
Create `.planning/phases/102-verification-source-chain-audit-uat-ver-01-ver-02/102-03-SUMMARY.md` — record the deploy confirmation, the per-node UAT checklist with evidence, and Chris's verbatim sign-off (or routed flags).
</output>
