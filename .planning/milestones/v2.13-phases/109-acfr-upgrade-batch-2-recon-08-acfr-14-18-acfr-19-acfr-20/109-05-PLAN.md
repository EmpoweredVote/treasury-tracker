---
phase: 109-acfr-upgrade-batch-2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processMIAcfr.js
  - scripts/processMIRevenueAcfr.js
  - .planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-05-MI-LOADLOG.md
autonomous: true
requirements: [ACFR-18, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Michigan state node (38c9f1ff-130e-423d-955a-6f0aa5aecae2) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across the FY2019–FY2025 window (7 yrs, D-01), every loaded FY tying to the printed GENERAL FUND (Fund 10) column total (D-04) within the documented GAAP thousands-rounding tolerance, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MI's September-30 FY-end is honored (D-03): every MI budgets row carries source_date = {FY}-09-30 (NOT June 30) and fiscal_year_start_month = 10 (propagated from the data_sources srcPayload through the treasury_sync_budget_tree RPC); ACFR 'FY2025' = Oct 2024–Sep 2025 = NASBO 'FY2025' label alignment confirmed"
    - "MI units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to the printed 53,788,610K (line-sum 53,788,611K — $1K documented GAAP rounding, TOL≤5K as in 108 MA/MD precedent) and FY2020 to 39,920,656K — extracted via the shared parser extractGovFundGeneralColumn (D-02) from the GENERAL FUND (Fund 10) column ONLY (1st of General Fund | School Aid Fund | Non-Major Funds | Totals; column headers are fund codes 10/20/30/70 — School Aid Fund [Fund 20] is NOT the GF), NOT a multi-column sum"
    - "The pre-existing NASBO operating rows (MI FY2023 $14,861M, FY2024 $15,129M) are REPLACED in place by the ACFR operating rows (same (38c9f1ff, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite (D-09): a second live run of a representative FY produces 0 net change; the 14 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (D-07) (MI ACFR GF ~3.56× NASBO — the LARGEST in the whole tranche, ~$30.3B 'From federal agencies' Medicaid/ARP passthrough inside the GAAP GF) is PROMINENTLY documented in the loader and recorded at load against the pre-load NASBO totals; GAAP basis label verified on the live rows (ACFR-19)"
    - "P2 clamp wired (D-06, ACFR-20) though MI has no standalone investment-income line (embedded in Miscellaneous — no named clamp risk); any negative category found at load still clamps"
    - "Money In auto-enables on MI once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMIAcfr.js"
      provides: "MI GF (Fund 10) spending-by-function loader (operating), parser-based, thousands×1000, Sep-30 FY-end semantics, tie-verified FY2019–FY2025"
      contains: "fiscal_year_start_month"
      min_lines: 100
    - path: "scripts/processMIRevenueAcfr.js"
      provides: "MI GF (Fund 10) revenue-by-source loader (revenue), parser-based, thousands×1000, Sep-30 FY-end semantics, tie-verified FY2019–FY2025"
      contains: "clampForRender"
      min_lines: 100
    - path: ".planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-05-MI-LOADLOG.md"
      provides: "Per-FY load disposition, Sep-30 FY-end + Fund-10 confirmation, NASBO-replacement, the ~3.56× accept-relabel record, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMIAcfr.js / processMIRevenueAcfr.js"
      to: "treasury.budgets (MI state node 38c9f1ff)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE (source_date={FY}-09-30)"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processMIAcfr.js / processMIRevenueAcfr.js"
      to: "scripts/maAcfrExtract.mjs"
      via: "import { extractGovFundGeneralColumn } — the 108-proven shared GF-column parser (D-02)"
      pattern: "extractGovFundGeneralColumn"
    - from: "data_sources srcPayload (fiscal_year_start_month: 10)"
      to: "treasury.budgets.fiscal_year_start_month"
      via: "treasury_sync_budget_tree RPC propagates v_ds.fiscal_year_start_month into budgets (migration 20260613120000)"
      pattern: "fiscal_year_start_month"
    - from: "SOURCES map (7 per-year URLs; FY2025 reversed name special-cased)"
      to: "michigan.gov budget archive PDFs"
      via: "base /budget/-/media/Project/Websites/budget/Archive/Annual-Comprehensive-Financial-Report/ + ACFR-FY{YYYY}.pdf (FY2019–FY2024) / FY-2025-ACFR.pdf (FY2025)"
      pattern: "michigan.gov"
---

<objective>
Upgrade the **Michigan** state node — the tranche's one structural exception (D-03) — from NASBO operating-only to full State-ACFR GAAP by building two dedicated parser-based loaders — `scripts/processMIAcfr.js` (`operating`) and `scripts/processMIRevenueAcfr.js` (`revenue`), NC-mold clones carrying MI-specific **September-30 FY-end semantics** — downloading the MI ACFR PDFs across the full available **FY2019–FY2025** window (7 years) into gitignored `_acfr-work/mi/`, extracting the **GENERAL FUND (Fund 10) column** (1st of General Fund | School Aid Fund | Non-Major Funds | Totals — column headers are fund codes, NOT standard names; School Aid Fund [Fund 20] is a separate major fund and NOT the GF) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + the shared `extractGovFundGeneralColumn` parser, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**Three MI-specific handles (D-03): (1) Sep-30 FY-end** — SOURCES dates + post-RPC source-stamp use `{FY}-09-30`; the data_sources srcPayload sets `fiscal_year_start_month: 10`, which the RPC propagates into budgets; FY labels align to NASBO's calendar-year designation (ACFR "FY2025" = Oct 2024–Sep 2025 = NASBO "FY2025" ✓). **(2) Fund-code column headers** — GF = Fund 10, first numeric column; the ~$19.5B School Aid Fund must NOT bleed in. **(3) The tranche's largest scope divergence (~3.56×)** — ~$30.3B federal Medicaid/ARP passthrough inside the GAAP GF vs NASBO's narrow $15.1B budgetary GF; document prominently (TX-trap at its most pronounced, ACFR-19). Also: FY2025's URL name is reversed (`FY-2025-ACFR.pdf` vs `ACFR-FY{YYYY}.pdf`), and the FY2025 bookend carries a documented $1K GAAP rounding diff (printed 53,788,610K vs line-sum 53,788,611K → TOL≤5K per the 108 MA/MD precedent).

This is one of five independent per-state plans (D-08) — a failure or hold here blocks only MI, not the other four states.

Output: both MI loaders live-loaded across the extractable FY2019–FY2025 window (holes logged per D-05), NASBO replaced in place, Money In auto-enabled; `109-05-MI-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- MI loaders are NC-mold clones with D-03 Sep-30 deltas. -->
From scripts/processNCAcfr.js (spend-by-function analog → processMIAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Michigan', state='MI', entity_type='state' (.single()). Node id = 38c9f1ff-130e-423d-955a-6f0aa5aecae2 (assert match).
  - import { extractGovFundGeneralColumn } from './maAcfrExtract.mjs' — parse each FY's pdftotext -table text (D-02).
  - UNITS = 1_000 (thousands → ×1,000). Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands — TOL = 5 (thousands) absorbing documented GAAP rounding, as in processMAAcfr.js / processMDAcfr.js.
  - SOURCES = { [fy]: { url, date } } with all 7 per-year URLs (FY2025 reversed name special-cased); **date=`${fy}-09-30`** (D-03 — NOT June 30); dataSource(fy)="Michigan State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - **data_sources srcPayload includes `fiscal_year_start_month: 10`** (the RPC propagates v_ds.fiscal_year_start_month into budgets — migration 20260613120000_add_budget_period_label.sql); post-RPC source-stamp UPDATE uses source_date=`${fy}-09-30`.
  - EXPENDITURES/REVENUE maps + validate() exit-2 + buildTree ×UNITS + root {n:'Michigan General Fund Budget'} + RPC + source-stamp, as in NC.
  - data_sources.dataset_id='mi-acfr-gf-operating' / 'mi-acfr-gf-revenue'; names 'Michigan General Fund Operating Budget' / 'Michigan General Fund Revenue'; base_url=https://www.michigan.gov/budget/fiscal-pages/reports/annual-comprehensive-financial-report.
  - flags: --dry-run, --fy YYYY.
From scripts/processNCRevenueAcfr.js (revenue analog → processMIRevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender + "(net loss — shown at 0)" + signed-net root (ACFR-20).
From scripts/processMAAcfr.js / processMDAcfr.js: the TOL = 5 (thousands) documented-GAAP-rounding pattern for the FY2025 $1K diff.
POPULATION = 10_077_331 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): MI has 2 NASBO operating rows only — FY2023 $14,861M, FY2024 $15,129M, data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$53.8B FY2025) is ~3.56× (LARGEST tranche divergence — ~$30.3B "From federal agencies" Medicaid/ARP passthrough inside GAAP GF; School Aid Fund $19.5B is a separate major fund NOT in the GF column), accept-and-relabel per ACFR-19 with prominent documentation.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH2-SOURCES.md MI Detail Block — USE these, but re-verify URLs at load (108 lesson). -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds (PDF page ~42 of FY2025 ACFR). NOT Statement of Activities, NOT budgetary comparison.
- GF column = GENERAL FUND (Fund 10), 1st of (General Fund | School Aid Fund | Non-Major Funds | Totals). Column headers are fund codes 10/20/30/70. Parser takes the FIRST numeric token — do NOT sum across columns; School Aid Fund (Fund 20, ~$19.5B) is NOT the GF.
- Units = thousands. **FY-end September 30** (unique in the whole roster): FY2025 = Oct 2024–Sep 2025; source_date = {FY}-09-30; NASBO FY label alignment confirmed (same calendar-year designation).
- URL base: https://www.michigan.gov/budget/-/media/Project/Websites/budget/Archive/Annual-Comprehensive-Financial-Report/ — per-year names:
  - FY2025: `FY-2025-ACFR.pdf` (REVERSED name — special-case) / FY2019–FY2024: `ACFR-FY{YYYY}.pdf`
  - `?rev=…&hash=…` query params seen in HTML are NOT required for direct download.
  - Landing (base_url): https://www.michigan.gov/budget/fiscal-pages/reports/annual-comprehensive-financial-report
- Bookend ties (GENERAL FUND Total revenues, thousands): FY2025 printed = 53,788,610K, line-sum = 53,788,611K (diff $1K — documented GAAP rounding, TOL≤5K); FY2020 = 39,920,656K (diff $0).
- Investment income NOT broken out (embedded in Miscellaneous) — no standalone negative line possible; clamp stays wired as safety net (ACFR-20).
- Scope vs NASBO ~3.56× (largest in tranche) — accept-and-relabel honestly with PROMINENT documentation (ACFR-19).
- Pre-FY2019 not on the michigan.gov archive — out of scope (deferred).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both dedicated MI loaders (NC mold + D-03 Sep-30 semantics) + download/extract FY2019–FY2025 (Fund-10 column via shared parser) + dry-run tie</name>
  <files>scripts/processMIAcfr.js, scripts/processMIRevenueAcfr.js, _acfr-work/mi/</files>
  <read_first>
    - scripts/processNCAcfr.js (the exact parser-based spend-by-function template)
    - scripts/processNCRevenueAcfr.js (the exact parser-based revenue template incl. clampForRender)
    - scripts/maAcfrExtract.mjs (extractGovFundGeneralColumn — the shared GF-column parser, D-02)
    - scripts/processMAAcfr.js (the TOL=5 documented-GAAP-rounding precedent for the MI FY2025 $1K diff)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md (MI Detail Block: Sep-30 FY-end, Fund-10 column, reversed FY2025 name, ~3.56× divergence)
  </read_first>
  <action>
    Download each MI ACFR to `_acfr-work/mi/MI{YYYY}.pdf` from the michigan.gov archive base — `ACFR-FY{YYYY}.pdf` for FY2019–FY2024, `FY-2025-ACFR.pdf` (reversed) for FY2025, no query params — with a soft-404 guard (%PDF magic + size >~500KB; reject HTML). If any URL 404s, re-enumerate from the michigan.gov archive page (load-time recon-correction expected). Run `pdftotext -table` per year (page ~42 for FY2025; bounds empirically), locate the Governmental Funds Statement of Rev/Exp/Changes, and feed the text to `extractGovFundGeneralColumn` to isolate the GENERAL FUND (Fund 10) column — the FIRST numeric token; the neighbors are School Aid Fund (Fund 20) and Non-Major Funds; do NOT sum across columns and do NOT let the $19.5B School Aid Fund bleed in. Copy processNCAcfr.js → scripts/processMIAcfr.js and processNCRevenueAcfr.js → scripts/processMIRevenueAcfr.js; set STATE_NAME='Michigan', STATE_ABBR='MI', POPULATION=10_077_331, UNITS=1_000, node id assert 38c9f1ff-130e-423d-955a-6f0aa5aecae2. Apply the D-03 Sep-30 deltas: SOURCES date=`${fy}-09-30` for every FY; the post-RPC budgets source-stamp UPDATE uses source_date=`${fy}-09-30`; the data_sources srcPayload includes `fiscal_year_start_month: 10` for BOTH datasets (the RPC propagates it into budgets per migration 20260613120000); add a header comment explaining MI's Oct 1–Sep 30 fiscal year and the NASBO FY-label alignment (ACFR "FY2025" = Oct 2024–Sep 2025 = NASBO "FY2025"). Set validate() TOL = 5 (thousands) with a comment citing the FY2025 printed-vs-line-sum $1K GAAP rounding (MA/MD precedent) — totals must otherwise tie exactly. Populate EXPENDITURES + REVENUE maps from the parser output (verbatim names, thousands), each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('mi-acfr-gf-operating' / 'mi-acfr-gf-revenue'); base_url=the michigan.gov landing page. Keep clampForRender in the revenue loader (ACFR-20 safety net — no named MI risk). Add a PROMINENT header block documenting the ~3.56× scope divergence (largest in tranche): ~$30.3B "From federal agencies" Medicaid/ARP passthrough inside the GAAP GF vs NASBO's narrow $15.1B budgetary GF; School Aid Fund excluded (ACFR-19). Cross-check FY2025 Total revenues = 53,788,610K printed (accept the $1K line-sum rounding within TOL) and FY2020 = 39,920,656K before processing the rest. Run both loaders `--dry-run`; confirm every present FY PASS.
  </action>
  <verify>
    <automated>node scripts/processMIAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processMIRevenueAcfr.js --dry-run --fy 2020 2>&1 | grep -q '39,920,656,000' && grep -q 'fiscal_year_start_month' scripts/processMIAcfr.js && grep -q '09-30' scripts/processMIAcfr.js && grep -q 'FY-2025-ACFR.pdf' scripts/processMIAcfr.js</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/mi/` holds a real PDF (%PDF, >~500KB) per extractable FY; soft-404 HTML rejected.
    - `node scripts/processMIAcfr.js --dry-run` prints "validation: PASS" for every present FY; `processMIRevenueAcfr.js --dry-run --fy 2020` → 39,920,656,000; FY2025 total ties to printed 53,788,610K within the documented TOL≤5K.
    - Both loaders import extractGovFundGeneralColumn from './maAcfrExtract.mjs' (D-02).
    - Every SOURCES entry has date `${fy}-09-30`; the FY2025 entry contains `FY-2025-ACFR.pdf`; srcPayload contains `fiscal_year_start_month: 10`.
    - Header comment documents the Oct 1–Sep 30 fiscal year + NASBO FY-label alignment, the Fund-10-only caution (School Aid Fund excluded), and the ~3.56× accept-relabel note prominently (ACFR-19); UNITS=1_000; TOL=5 with the rounding citation.
  </acceptance_criteria>
  <done>Both dedicated MI loaders exist on the NC mold with D-03 Sep-30 semantics (source_date {FY}-09-30, fiscal_year_start_month 10, FY-label alignment documented), every present FY dry-run-ties to its printed GENERAL FUND (Fund 10) total (within documented TOL for FY2025) with the 53,788,610K / 39,920,656K bookends, the ~3.56× divergence documented prominently, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load MI (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processMIAcfr.js, scripts/processMIRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMIAcfr.js + scripts/processMIRevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; MI node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the MI node (38c9f1ff) and RECORD the pre-load NASBO operating rows (FY2023 $14,861M, FY2024 $15,129M) — never-overwrite baseline + accept-relabel comparator. Then for each MI FY present after Task 1, run `node scripts/processMIAcfr.js --fy <YYYY>` then `node scripts/processMIRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (38c9f1ff, fy, 'operating') for FY2023/FY2024; all other FYs operating + all revenue net-new. Each stamps source_url + source_date=`${fy}-09-30` + data_source (GAAP label); fiscal_year_start_month=10 lands via the RPC. Abort+log any FY failing the live tie; never force-write. Record loaded MI GF totals against pre-load NASBO totals to confirm the ~3.56× accept-relabel divergence (federal Medicaid/ARP passthrough inside GAAP GF — the tranche's largest, ACFR-19).
  </action>
  <verify>
    <automated>echo "MI row existence + NASBO-replacement + Sep-30 source_date + fiscal_year_start_month verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the MI node has, per loaded FY, an operating row "Michigan State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - Every MI row's source_date = {fy}-09-30 (September 30 — D-03), NOT {fy}-06-30.
    - MI FY2025 revenue row total = 53,788,610,000 (printed total); FY2020 = 39,920,656,000.
    - MI FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (MI, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~3.56× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>Each transcribed MI FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced with Sep-30 dates; FY2023/FY2024 NASBO rows replaced in place (no duplicates); the tranche-largest accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Sep-30 semantics + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-05-MI-LOADLOG.md</files>
  <read_first>
    - scripts/processMIAcfr.js (confirm RPC keyed (muni, fy, dataset) + Sep-30 stamps)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. MI --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded MI FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) every MI row's source_date ends in -09-30 AND fiscal_year_start_month = 10 (D-03); (c) MI has NO remaining "NASBO" operating rows (one operating row per (MI, fy)); (d) honest-hole FYs ABSENT; (e) Money In auto-enable — MI ≥1 revenue row; (f) cohort untouched (RECON-08): the 14 existing ACFR nodes + a sample un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 109-05-MI-LOADLOG.md: FYs loaded (of the 7-yr window), FYs skipped (+reason), the Sep-30/Fund-10 confirmations, pre-load-NASBO-vs-loaded-ACFR totals with the prominent ~3.56× accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than MI.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 109-05-MI-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of MI --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded MI FY has both operating + revenue rows, GAAP-labelled, non-null sources; source_date = {fy}-09-30 on every row; fiscal_year_start_month = 10.
    - MI has ZERO remaining "NASBO" operating rows; one operating row per (MI, fy).
    - MI ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 109-05-MI-LOADLOG.md has a finalized "Load Disposition" section incl. Sep-30 + Fund-10 confirmations.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms MI fully ACFR-sourced (operating + revenue, GAAP-labelled, Sep-30-dated, fiscal_year_start_month=10, NASBO replaced, Money In enabled), honest holes absent, cohort unchanged; 109-05-MI-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| michigan.gov PDF → parser extraction | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the MI node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05-A | Tampering | **School Aid Fund (Fund 20, ~$19.5B) mistaken for / summed into the GF (fund-code headers)** | mitigate | shared parser takes ONLY the 1st numeric token = Fund 10 (D-02); bookend ties (53,788,610K / 39,920,656K) gate before load; validate() exit-2 |
| T-109-05-B | Tampering | **wrong FY-end — Jun-30 stamps on a Sep-30 state (D-03)** | mitigate | SOURCES dates + source-stamp UPDATE hardcode {FY}-09-30; fiscal_year_start_month=10 via srcPayload→RPC; Task 3 asserts -09-30 on every row |
| T-109-05-C | Tampering | wrong statement (Statement of Activities / budgetary) | mitigate | Task 1 targets the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate |
| T-109-05-D | Tampering | FY2025 reversed-name URL 404 / query-param confusion | mitigate | SOURCES special-cases FY-2025-ACFR.pdf, strips ?rev/hash; %PDF magic + size guard; per-FY tie catches wrong-year content |
| T-109-05-E | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-109-05-F | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (MI, fy), zero NASBO |
| T-109-05-G | Info Disclosure | silently inflating MI (~3.56× — the tranche's largest) without recording the divergence | mitigate | PROMINENT loader documentation + pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-109-05-H | Tampering | tolerance creep — TOL=5 absorbing real extraction errors, not just rounding | mitigate | TOL applies to the documented FY2025 $1K printed-vs-line-sum diff only; all other FYs tie at $0; any diff >5K exits 2 |
| T-109-05-I | Tampering | write to a state other than MI | mitigate | loader resolves only MI node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-109-05-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both MI loaders `--dry-run`: every present FY "validation: PASS"; Fund-10 column via shared parser only; UNITS=1_000; TOL=5 documented.
- `processMIRevenueAcfr.js --dry-run --fy 2025` ties printed 53,788,610K (within documented $1K rounding); `--fy 2020` → 39,920,656,000.
- Every SOURCES date + live source_date = {fy}-09-30; fiscal_year_start_month = 10 on loaded budgets rows (D-03).
- Live: MI node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run MI --fy 2025 → "Loaded 0 rows" (idempotent).
- MI ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~3.56×, tranche's largest) prominently recorded (ACFR-19); clamp wired (ACFR-20).
- 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 109-05-MI-LOADLOG.md finalized.
</verification>

<success_criteria>
The Michigan state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across the extractable FY2019–FY2025 window (honest holes logged; pre-FY2019 deferred), GENERAL FUND (Fund 10) column via the shared parser only (School Aid Fund excluded), thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + stamped with Sep-30 source_dates and fiscal_year_start_month=10 (D-03), NASBO operating replaced in place (RECON-08), the tranche-largest ~3.56× accept-relabel divergence prominently recorded (ACFR-19), the P2 clamp wired (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands MI to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-05-SUMMARY.md` when done — record the MI FY window actually loaded (of the 7-yr window), FYs omitted as honest holes, the two bookend tie confirmations (incl. the documented FY2025 $1K rounding), the Sep-30/fiscal_year_start_month confirmations, the NASBO-replacement confirmation, the ~3.56× accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
