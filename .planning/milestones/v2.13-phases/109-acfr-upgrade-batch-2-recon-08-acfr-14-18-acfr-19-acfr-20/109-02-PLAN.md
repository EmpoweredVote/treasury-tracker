---
phase: 109-acfr-upgrade-batch-2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processCTAcfr.js
  - scripts/processCTRevenueAcfr.js
  - .planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-02-CT-LOADLOG.md
autonomous: true
requirements: [ACFR-15, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Connecticut state node (d01de53e-d687-4825-bfe2-09f7694c28d6) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across the verified FY2019–FY2025 window PLUS every deep-history year (FY1988+, enumerated from the osc.ct.gov _reportsSource archive JSON) that extracts + ties (D-01), every loaded FY tying exactly to the printed GENERAL FUND column total (D-04), every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "CT units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to 26,074,183K and FY2019 to 20,776,288K — extracted via the shared parser extractGovFundGeneralColumn (D-02) from the GENERAL FUND column ONLY (1st of SEVEN: General | Debt Service | Transportation | Restricted Grants & Accounts | Grant & Loan Programs | Other | Total — the tranche's highest multi-column-sum risk), NOT a multi-column sum"
    - "The pre-existing NASBO operating rows (CT FY2023 $22,199M, FY2024 $22,779M) are REPLACED in place by the ACFR operating rows (same (d01de53e, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite (D-09): a second live run of a representative FY produces 0 net change; the 14 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (D-07) (CT ACFR GF ~1.14× NASBO — the tranche's smallest, $2.8B Federal Grants and Aid inside the GAAP GF) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (D-06, ACFR-20) — CT's older fiscal-stress years (2009–2017) are the tranche's named negative-investment-income watch; every loaded FY checked"
    - "Money In auto-enables on CT once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processCTAcfr.js"
      provides: "CT GF spending-by-function loader (operating), parser-based, thousands×1000, tie-verified FY2019–FY2025 + deep-history attempts"
      contains: "treasury_sync_budget_tree"
      min_lines: 100
    - path: "scripts/processCTRevenueAcfr.js"
      provides: "CT GF revenue-by-source loader (revenue), parser-based, thousands×1000, tie-verified FY2019–FY2025 + deep-history attempts"
      contains: "clampForRender"
      min_lines: 100
    - path: ".planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-02-CT-LOADLOG.md"
      provides: "Per-FY load disposition incl. deep-history attempts, archive-JSON-enumerated URL record, NASBO-replacement, accept-relabel divergence, P2 clamp dispositions, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processCTAcfr.js / processCTRevenueAcfr.js"
      to: "treasury.budgets (CT state node d01de53e)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processCTAcfr.js / processCTRevenueAcfr.js"
      to: "scripts/maAcfrExtract.mjs"
      via: "import { extractGovFundGeneralColumn } — the 108-proven shared GF-column parser (D-02)"
      pattern: "extractGovFundGeneralColumn"
    - from: "SOURCES map (per-year URLs enumerated from the _reportsSource archive JSON)"
      to: "osc.ct.gov ACFR/CAFR PDFs"
      via: "explicit per-year URLs from the osc.ct.gov/reports _reportsSource JSON blob (FY2022 'revised' suffix; pre-FY2021 CAFR naming)"
      pattern: "osc.ct.gov"
---

<objective>
Upgrade the **Connecticut** state node from NASBO operating-only to full State-ACFR GAAP by building two parser-based loaders on the NC mold — `scripts/processCTAcfr.js` (`operating`) and `scripts/processCTRevenueAcfr.js` (`revenue`) — enumerating per-year URLs from the **`_reportsSource` JavaScript JSON blob on osc.ct.gov/reports**, downloading into gitignored `_acfr-work/ct/`, extracting the **GENERAL FUND column** (1st of SEVEN columns — the tranche's highest multi-column-sum risk) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table` + the shared `extractGovFundGeneralColumn` parser, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**Window (D-01): verified FY2019–FY2025, then push into the deep enumerable history (FY1988+).** Each deep year loads only if it extracts cleanly AND ties exactly — failures become logged honest holes, no artificial floor, never forced. **CRITICAL — GF column only via the shared parser (D-02):** CT's 7-column statement makes summing across columns the tranche's worst trap; the parser's 1st-numeric-token rule + the bookend ties (26,074,183K / 20,776,288K) are the guard. **P2 watch (ACFR-20):** CT's 2009–2017 fiscal-stress years may carry negative investment-income lines — the clamp must be verified on any such year.

This is one of five independent per-state plans (D-08) — a failure or hold here blocks only CT, not the other four states.

Output: both CT loaders live-loaded across the verified window + every deep year that ties (holes logged per D-01/D-05), NASBO replaced in place, Money In auto-enabled; `109-02-CT-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- CT loaders mirror the NC loaders file-for-file (108's cleanest shared-parser reuse). -->
From scripts/processNCAcfr.js (spend-by-function analog → processCTAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Connecticut', state='CT', entity_type='state' (.single()). Node id = d01de53e-d687-4825-bfe2-09f7694c28d6 (assert match).
  - import { extractGovFundGeneralColumn } from './maAcfrExtract.mjs' — parse each FY's pdftotext -table text (D-02).
  - UNITS = 1_000 (thousands → ×1,000). Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands.
  - SOURCES = { [fy]: { url, date } } — per-year URLs enumerated from the _reportsSource JSON (FY2022 'revised' suffix; pre-FY2021 CAFR naming); date=`${fy}-06-30`; dataSource(fy)="Connecticut State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES/REVENUE maps + validate() exit-2 + buildTree ×UNITS + root {n:'Connecticut General Fund Budget'} + RPC + source-stamp, as in NC.
  - data_sources.dataset_id='ct-acfr-gf-operating' / 'ct-acfr-gf-revenue'; names 'Connecticut General Fund Operating Budget' / 'Connecticut General Fund Revenue'; base_url=https://osc.ct.gov/reports.
  - flags: --dry-run, --fy YYYY.
From scripts/processNCRevenueAcfr.js (revenue analog → processCTRevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender + "(net loss — shown at 0)" + signed-net root (ACFR-20 — CT's fiscal-stress years are the named watch).
POPULATION = 3_605_944 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): CT has 2 NASBO operating rows only — FY2023 $22,199M, FY2024 $22,779M, data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$26.1B FY2025) is ~1.14× (smallest tranche divergence — $2.8B Federal Grants and Aid inside GAAP GF), accept-and-relabel per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH2-SOURCES.md CT Detail Block — USE these, but re-verify URLs at load (108 lesson). -->
- Statement: Statement of Revenues, Expenditures, and Changes in Fund Balances — Governmental Funds (PDF page ~37 of FY2025 ACFR). NOT Statement of Activities.
- GF column = 1st of SEVEN (General | Debt Service | Transportation | Restricted Grants & Accounts | Grant & Loan Programs | Other Funds | Total). Parser takes the FIRST numeric token — do NOT sum across columns (highest-risk state for this trap).
- Units = thousands. FY-end June 30. Window: FY2019–FY2025 extraction-verified; FY1988+ enumerable from the _reportsSource JSON on https://osc.ct.gov/reports.
- Recon-confirmed explicit URLs (re-verify at load):
  - FY2025: https://osc.ct.gov/wp-content/uploads/2026/03/State-of-Connecticut-ACFR-2-27-26_Final.pdf
  - FY2024: https://osc.ct.gov/wp-content/uploads/2025/03/State-of-Connecticut-ACFR-FY-24-3-26-25.pdf
  - FY2023: https://osc.ct.gov/wp-content/uploads/2024/04/ACFR-FY2023-v11-2024-04-12.pdf
  - FY2022: https://osc.ct.gov/reports/ACFR-2022revised032227.pdf ("revised" suffix — use exact URL from JSON, never derived)
  - FY2021: https://osc.ct.gov/reports/ACFR2021.pdf / FY2020: https://osc.ct.gov/reports/CAFR2020.pdf / FY2019: https://osc.ct.gov/reports/CAFR-2019.pdf
  - FY2018: https://osc.ct.gov/reports/CAFR-2018rev040919.pdf / FY2017: https://osc.ct.gov/reports/2017CAFRrev012918.pdf / FY2016: https://osc.ct.gov/2016cafr/CAFR2016rev.pdf / FY2015: https://osc.ct.gov/2015cafr/cafr2015.pdf
  - FY2014–FY1988: enumerate from the _reportsSource JSON at load.
- Bookend ties (GENERAL FUND Total revenues, thousands): FY2025 = 26,074,183K (diff $0); FY2019 = 20,776,288K (diff $0).
- Investment Earnings positive in FY2025 (+$338,294K) and FY2019 (+$48,950K); **older fiscal-stress years (2009–2017) may be negative — P2 clamp watch (ACFR-20)**.
- Scope vs NASBO ~1.14× (smallest in tranche) — accept-and-relabel honestly (ACFR-19).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both CT loaders (NC parser-based mold) + enumerate URLs from _reportsSource JSON + download/extract FY2019–FY2025 then deep FY1988+ (GF column via shared parser) + dry-run tie</name>
  <files>scripts/processCTAcfr.js, scripts/processCTRevenueAcfr.js, _acfr-work/ct/</files>
  <read_first>
    - scripts/processNCAcfr.js (the exact parser-based spend-by-function template)
    - scripts/processNCRevenueAcfr.js (the exact parser-based revenue template incl. clampForRender)
    - scripts/maAcfrExtract.mjs (extractGovFundGeneralColumn — the shared GF-column parser, D-02)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md (CT Detail Block: explicit URLs FY2015–FY2025, _reportsSource JSON enumeration, 7-column caution, bookend ties)
  </read_first>
  <action>
    Fetch https://osc.ct.gov/reports and extract the `_reportsSource` JavaScript JSON blob; enumerate every per-year ACFR/CAFR PDF URL from FY2025 back to FY1988 (use the recon-confirmed explicit URLs for FY2015–FY2025; resolve the rest from the JSON — FY2022 MUST use the exact "revised"-suffix URL, never derived). Download each to `_acfr-work/ct/CT{YYYY}.pdf` with a soft-404 guard (%PDF magic + size >~500KB; reject HTML). Work newest→oldest: first the verified FY2019–FY2025 window, then push deeper year-by-year (FY2018, FY2017, … FY1988), stopping only at enumeration exhaustion — each deep year that fails download/extraction/tie becomes a logged honest hole (D-01 guard), never a forced load. Run `pdftotext -table` per year, locate the Governmental Funds Statement of Rev/Exp/Changes, and feed the text to `extractGovFundGeneralColumn` to isolate the GENERAL FUND column (1st numeric token of SEVEN) for BOTH revenues and expenditures. DO NOT sum across columns — CT's 7-column layout is the tranche's worst multi-column trap; if a total overshoots, re-extract GF only. Copy processNCAcfr.js → scripts/processCTAcfr.js and processNCRevenueAcfr.js → scripts/processCTRevenueAcfr.js; set STATE_NAME='Connecticut', STATE_ABBR='CT', POPULATION=3_605_944, UNITS=1_000, node id assert d01de53e-d687-4825-bfe2-09f7694c28d6. Build SOURCES with the enumerated per-year URLs (source_date=`${fy}-06-30`). Populate EXPENDITURES + REVENUE maps from the parser output (verbatim names, thousands), each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('ct-acfr-gf-operating' / 'ct-acfr-gf-revenue'); base_url=https://osc.ct.gov/reports. Keep clampForRender in the revenue loader and CHECK every extracted year for negative investment-income lines (2009–2017 fiscal-stress watch, ACFR-20). Add a header comment recording the accept-and-relabel scope note (CT ~1.14× NASBO — smallest tranche divergence, ACFR-19), the 7-column GF-only caution, and the FY2022-revised + CAFR-era naming notes. Cross-check FY2025 Total revenues = 26,074,183K and FY2019 = 20,776,288K before processing the rest. Run both loaders `--dry-run`; confirm every present FY PASS.
  </action>
  <verify>
    <automated>node scripts/processCTAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processCTRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '26,074,183,000' && node scripts/processCTRevenueAcfr.js --dry-run --fy 2019 2>&1 | grep -q '20,776,288,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ct/` holds a real PDF (%PDF, >~500KB) per extractable FY; soft-404 HTML rejected.
    - `node scripts/processCTAcfr.js --dry-run` prints "validation: PASS" for every present FY; `processCTRevenueAcfr.js --dry-run --fy 2025` → 26,074,183,000; `--fy 2019` → 20,776,288,000.
    - Both loaders import extractGovFundGeneralColumn from './maAcfrExtract.mjs' (D-02).
    - SOURCES covers at minimum FY2019–FY2025 (7 explicit osc.ct.gov URLs incl. the FY2022 "revised" exact URL) plus every deep year that tied; deep years that failed are ABSENT from SOURCES and logged.
    - Any FY with a negative investment-income line has the clamp path noted (ACFR-20).
    - Header comment records the 7-column GF-only caution and the ~1.14× accept-relabel note (ACFR-19); UNITS=1_000.
  </acceptance_criteria>
  <done>Both CT loaders exist on the NC parser-based mold with archive-JSON-enumerated URLs, every present FY dry-run-ties to its printed GENERAL FUND total (GF column only) with the FY2025 26,074,183K / FY2019 20,776,288K bookends, deep-history attempts logged (loads + holes), negative-year clamp dispositions noted, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load CT (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processCTAcfr.js, scripts/processCTRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processCTAcfr.js + scripts/processCTRevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; CT node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the CT node (d01de53e) and RECORD the pre-load NASBO operating rows (FY2023 $22,199M, FY2024 $22,779M) — never-overwrite baseline + accept-relabel comparator. Then for each CT FY present after Task 1, run `node scripts/processCTAcfr.js --fy <YYYY>` then `node scripts/processCTRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (d01de53e, fy, 'operating') for FY2023/FY2024; all other FYs operating + all revenue net-new. Each stamps source_url/source_date/data_source (GAAP label). Abort+log any FY failing the live tie; never force-write. Record loaded CT GF totals against pre-load NASBO totals to confirm the ~1.14× accept-relabel divergence (ACFR-19). If any loaded FY carries a clamped negative category, verify the rendered value is 0 with the signed magnitude in the label and the parent total intact (ACFR-20).
  </action>
  <verify>
    <automated>echo "CT row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the CT node has, per loaded FY, an operating row "Connecticut State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - CT FY2025 revenue row total = 26,074,183,000; FY2019 = 20,776,288,000.
    - CT FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (CT, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.14× divergence explained (ACFR-19); any negative-year clamp verified (ACFR-20).
  </acceptance_criteria>
  <done>Each transcribed CT FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO rows replaced in place (no duplicates); the accept-relabel divergence + any clamp dispositions recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-02-CT-LOADLOG.md</files>
  <read_first>
    - scripts/processCTAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. CT --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded CT FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) CT has NO remaining "NASBO" operating rows (one operating row per (CT, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — CT ≥1 revenue row; (e) cohort untouched (RECON-08): the 14 existing ACFR nodes + a sample un-upgraded NASBO state unchanged. Append a "Load Disposition" section to 109-02-CT-LOADLOG.md: FYs loaded (verified window + deep-history results with the deepest year reached), FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, P2 clamp dispositions for any negative years, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than CT.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 109-02-CT-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of CT --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded CT FY has both operating + revenue rows, GAAP-labelled, non-null sources; CT has ZERO remaining "NASBO" operating rows; one operating row per (CT, fy).
    - CT ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 109-02-CT-LOADLOG.md has a finalized "Load Disposition" section incl. the deepest loaded year.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms CT fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, Money In enabled), honest holes absent, cohort unchanged; 109-02-CT-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| osc.ct.gov PDF → parser extraction | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the CT node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-02-A | Tampering | **multi-column sum across CT's SEVEN fund columns — the tranche's worst version of this trap** | mitigate | shared parser takes ONLY the 1st numeric token (D-02); bookend ties (26,074,183K / 20,776,288K) gate before load; validate() exit-2 |
| T-109-02-B | Tampering | wrong statement (Statement of Activities / budgetary) | mitigate | Task 1 targets the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate |
| T-109-02-C | Tampering | wrong/derived URL (FY2022 "revised" suffix; CAFR-era naming) | mitigate | enumerate EXACT URLs from the _reportsSource JSON only; %PDF magic + size guard; per-FY tie catches wrong-year content |
| T-109-02-D | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-109-02-E | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (CT, fy), zero NASBO |
| T-109-02-F | Info Disclosure | negative fiscal-stress-era investment income rendered as a negative bar / silently dropped | mitigate | clampForRender: 0-clamp + signed magnitude in label + parent total intact (ACFR-20); per-year check logged |
| T-109-02-G | Tampering | write to a state other than CT | mitigate | loader resolves only CT node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-109-02-H | Info Disclosure | silently inflating CT (~1.14×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-109-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both CT loaders `--dry-run`: every present FY "validation: PASS"; GF column via shared parser only; UNITS=1_000.
- `processCTRevenueAcfr.js --dry-run --fy 2025` → 26,074,183,000; `--fy 2019` → 20,776,288,000.
- Deep-history attempts (FY2018 → FY1988) each resolved to loaded-and-tied OR logged hole — no forced loads.
- Live: CT node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place.
- Re-run CT --fy 2025 → "Loaded 0 rows" (idempotent).
- CT ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.14×) recorded (ACFR-19); negative-year clamp dispositions recorded (ACFR-20).
- 14 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 109-02-CT-LOADLOG.md finalized.
</verification>

<success_criteria>
The Connecticut state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across the verified FY2019–FY2025 window plus every deep-history year (FY1988+) that extracted + tied (honest holes logged), GF column via the shared parser only (7-column trap avoided), thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place (RECON-08), the ~1.14× accept-relabel divergence recorded (ACFR-19), fiscal-stress-era negative years clamped honestly (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands CT to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-02-SUMMARY.md` when done — record the CT FY window actually loaded (verified 7 yrs + deepest deep-history year reached), FYs omitted as honest holes, the two bookend tie confirmations, any P2 clamp years, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
