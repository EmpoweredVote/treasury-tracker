---
phase: 108-acfr-upgrade-batch-1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processMDAcfr.js
  - scripts/processMDRevenueAcfr.js
  - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-05-MD-LOADLOG.md
autonomous: true
requirements: [ACFR-13, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Maryland state node (8e597f8f-c696-47c0-9001-ed78a54f2228) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full clean window FY2022–FY2025, every loaded FY tying to the printed GENERAL FUND column total (within documented GAAP thousands rounding), every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MD units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to 48,689,018K (recon line-item sum 48,689,017K, diff $1 rounding) and FY2022 to 50,540,136K (sum 50,540,138K, diff $2 rounding) — the loader tolerance absorbs documented GAAP thousands rounding only"
    - "MD FY2022 renders via the P2 clamp: 'Interest and other investment income = -$275,992K' (negative) clamps to 0 for render with the signed magnitude in the label; the parent total nets it (ACFR-20)"
    - "The pre-existing NASBO operating rows (MD FY2023, FY2024) are REPLACED in place by the ACFR operating rows (same (8e597f8f, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; the 9 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (MD ACFR GF ~1.78× NASBO) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Money In auto-enables on MD once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMDAcfr.js"
      provides: "MD GF spending-by-function loader (operating), thousands×1000, tie-verified FY2022–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMDRevenueAcfr.js"
      provides: "MD GF revenue-by-source loader (revenue), thousands×1000, tie-verified FY2022–FY2025, FY2022 P2 clamp"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-05-MD-LOADLOG.md"
      provides: "Per-FY load disposition, FY2022 P2-clamp confirmation, NASBO-replacement, accept-relabel divergence, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMDAcfr.js / processMDRevenueAcfr.js"
      to: "treasury.budgets (MD state node 8e597f8f)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map (FY2024+ lowercase vs FY2022–2023 uppercase)"
      to: "marylandcomptroller.gov ACFR PDFs"
      via: "case-change filename special-case (acfr{YYYY}.pdf vs ACFR{YYYY}.pdf)"
      pattern: "reports/financial"
---

<objective>
Upgrade the **Maryland** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the PA template — `scripts/processMDAcfr.js` (`operating`) and `scripts/processMDRevenueAcfr.js` (`revenue`) — downloading the MD ACFR PDFs across the full clean window **FY2022–FY2025** (4 years) into gitignored `_acfr-work/md/`, extracting the **GENERAL FUND column** (1st of 6: General Fund | Special Revenue | Debt Service | Capital Projects | Enterprise | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing verbatim line items, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying the **FY2022 P2 clamp** + idempotency + cohort-untouched.

**MD-specific — FY2022 P2 clamp (ACFR-20).** MD FY2022 GF revenue has `Interest and other investment income = -$275,992K` (negative). The loader MUST clamp it to 0 for render, keep the signed magnitude in the label, and preserve the parent net total. **URL case changes at FY2024** — FY2024–FY2025 are lowercase `acfr{YYYY}.pdf`, FY2022–FY2023 are uppercase `ACFR{YYYY}.pdf`; the SOURCES map must special-case this. The bookend line-item sums differ from the printed totals by $1–$2K (documented GAAP thousands rounding) — the loader tolerance absorbs only this documented rounding, not transcription drift.

Output: both MD loaders live-loaded across FY2022–FY2025 (holes logged), NASBO replaced in place, FY2022 P2 clamp confirmed, Money In auto-enabled; `108-05-MD-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- MD loaders mirror the PA loaders file-for-file. -->
From scripts/processPAAcfr.js (spend-by-function analog → processMDAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Maryland', state='MD', entity_type='state' (.single()). Node id = 8e597f8f-c696-47c0-9001-ed78a54f2228 (assert match).
  - UNITS = 1_000. Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands — set small (a few thousand) to absorb documented GAAP rounding (MD bookends diff $1–$2K), NOT transcription drift.
  - SOURCES = { [fy]: { url, date } } with the FY2024+ lowercase vs FY2022–2023 uppercase case-change; dataSource(fy)="Maryland State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES/REVENUE + validate() exit-2 + buildTree ×UNITS + root {n:'Maryland General Fund Budget'} + RPC + source-stamp.
  - data_sources.dataset_id='md-acfr-gf-operating' / 'md-acfr-gf-revenue'; names 'Maryland General Fund Operating Budget' / 'Maryland General Fund Revenue'; base_url=marylandcomptroller ACFR landing.
  - flags: --dry-run, --fy YYYY.
From scripts/processPARevenueAcfr.js (revenue analog → processMDRevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender(amount)=Math.max(amount,0); negative → "<name> (net loss — shown at 0)"; root total carries signed net (ACFR-20) — this is the FY2022 investment-income case.
POPULATION = 6_177_224 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): MD has 2 NASBO operating rows only — FY2023 $27,972M, FY2024 $27,397M, data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$48.7B FY2025) is ~1.78× (federal intergovernmental inside GAAP GF), accept-and-relabel per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH1-SOURCES.md MD Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Rev/Exp/Changes. NOT Statement of Activities, NOT budgetary.
- GF column = 1st of (General Fund | Special Revenue | Debt Service | Capital Projects | Enterprise | Total). First numeric token per row.
- Units = thousands. FY-end June 30. Clean window = FY2022–FY2025 (4 yrs; pre-FY2022 not accessible — gap logged).
- URL FY2024–FY2025 (lowercase): https://www.marylandcomptroller.gov/content/dam/mdcomp/md/reports/financial/acfr{YYYY}.pdf
- URL FY2022–FY2023 (UPPERCASE): https://www.marylandcomptroller.gov/content/dam/mdcomp/md/reports/financial/ACFR{YYYY}.pdf  ← SOURCES map MUST special-case the case change at FY2024.
- Bookend ties (GENERAL FUND Total revenues, thousands): FY2025 = 48,689,018K (line-item sum 48,689,017K, diff $1 GAAP rounding); FY2022 = 50,540,136K (sum 50,540,138K, diff $2 GAAP rounding).
- **CRITICAL — FY2022 negative:** Interest and other investment income = -$275,992K → P2 clamp required. FY2025 investment income positive. Check FY2023/FY2024 at load.
- Scope vs NASBO ~1.78× — accept-and-relabel honestly (ACFR-19).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both MD loaders (PA template) + case-change SOURCES + download/extract/transcribe FY2022–FY2025 + FY2022 P2 clamp + dry-run tie</name>
  <files>scripts/processMDAcfr.js, scripts/processMDRevenueAcfr.js, _acfr-work/md/</files>
  <read_first>
    - scripts/processPAAcfr.js (the exact spend-by-function template)
    - scripts/processPARevenueAcfr.js (the exact revenue template incl. clampForRender + negative-label path — the FY2022 case)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (MD Detail Block: case-change URLs, FY2022 negative investment income, bookend ties + rounding)
  </read_first>
  <action>
    For each FY 2022–2025, download the MD ACFR PDF to `_acfr-work/md/MD{YYYY}.pdf` using lowercase `acfr{YYYY}.pdf` for FY2024–2025 and uppercase `ACFR{YYYY}.pdf` for FY2022–2023. Soft-404 guard (%PDF magic + size >~500KB; reject HTML). Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, isolate the GENERAL FUND column (1st of 6) for BOTH revenues and expenditures. Copy processPAAcfr.js → scripts/processMDAcfr.js and processPARevenueAcfr.js → scripts/processMDRevenueAcfr.js; set STATE_NAME='Maryland', STATE_ABBR='MD', POPULATION=6_177_224, UNITS=1_000. Build SOURCES FY2022–FY2025 (source_date=`${fy}-06-30`) with the case-change special-case. Set the validate() tolerance small enough to absorb only the documented $1–$2K GAAP thousands rounding (not transcription drift). Transcribe the GENERAL FUND line items (verbatim names, thousands) into EXPENDITURES + REVENUE, each `total` = printed General-Fund Total. **FY2022 REVENUE must include the `Interest and other investment income = -275,992` (thousands) line as a negative** so clampForRender renders it at 0 with the "(net loss — shown at 0)" label and the parent nets it (ACFR-20). Set dataSource labels + dataset_id ('md-acfr-gf-operating' / 'md-acfr-gf-revenue'). Add a header comment recording the accept-and-relabel scope note (MD ~1.78× NASBO, ACFR-19) AND the FY2022 P2-clamp note. Cross-check FY2025 = 48,689,018K and FY2022 = 50,540,136K (allow the $1–$2K documented rounding). Any FY that fails clean extraction or its tie beyond documented rounding → OMIT + note. Run both loaders `--dry-run`; confirm every present FY PASS and FY2022 shows the clamped negative.
  </action>
  <verify>
    <automated>node scripts/processMDAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && node scripts/processMDRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '48,689,018,000' && node scripts/processMDRevenueAcfr.js --dry-run --fy 2022 2>&1 | grep -qi 'net loss — shown at 0'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/md/` holds a real PDF (%PDF, >~500KB) per FY 2022–2025; soft-404 HTML rejected.
    - `node scripts/processMDAcfr.js --dry-run` prints "validation: PASS" for every present FY; `processMDRevenueAcfr.js --dry-run --fy 2025` → 48,689,018,000; `--fy 2022` → 50,540,136,000 (within documented rounding) AND prints the "(net loss — shown at 0)" clamp label for Interest and other investment income.
    - SOURCES[2024].url contains `/acfr2024.pdf` (lowercase); SOURCES[2023].url contains `/ACFR2023.pdf` (uppercase).
    - Header comment records the FY2022 P2-clamp note + the ~1.78× accept-relabel note (ACFR-19); UNITS=1_000; validate tolerance ≤ documented GAAP rounding.
  </acceptance_criteria>
  <done>Both MD loaders exist on the PA template with the FY2024 case-change SOURCES, every transcribed FY dry-run-ties to its printed GENERAL FUND total (within $1–$2K documented rounding) with the FY2025 48,689,018K / FY2022 50,540,136K bookends, the FY2022 negative investment income clamps to 0 with the signed label (ACFR-20), the accept-relabel note recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load MD (operating + revenue) across the tied window, NASBO replaced in place, FY2022 clamp persisted</name>
  <files>(runs scripts/processMDAcfr.js, scripts/processMDRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMDAcfr.js + scripts/processMDRevenueAcfr.js (the --fy + clampForRender + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; MD node id + pre-load NASBO totals; P2 clamp FY2022)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the MD node (8e597f8f) and RECORD the pre-load NASBO operating rows (FY2023 $27,972M, FY2024 $27,397M) — never-overwrite baseline + accept-relabel comparator. Then for each MD FY present after Task 1 (FY2022–FY2025), run `node scripts/processMDAcfr.js --fy <YYYY>` then `node scripts/processMDRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (8e597f8f, fy, 'operating') for FY2023/FY2024; FY2022/FY2025 operating + all revenue net-new. Each stamps source_url/source_date/data_source (GAAP label). Confirm the FY2022 revenue row's negative investment income persists clamped to 0 with the "(net loss — shown at 0)" label and the parent total nets it (ACFR-20). Abort+note any FY failing the live tie; never force-write. Record loaded MD GF totals against pre-load NASBO totals to confirm the ~1.78× accept-relabel divergence (ACFR-19).
  </action>
  <verify>
    <automated>echo "MD row existence + NASBO-replacement + FY2022 clamp + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the MD node has, per loaded FY, an operating row "Maryland State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - MD FY2025 revenue row total = 48,689,018,000; FY2022 = 50,540,136,000 (within documented rounding).
    - The FY2022 revenue tree renders the Interest-and-other-investment-income category at 0 with the signed-magnitude "(net loss — shown at 0)" label; the parent total nets it (ACFR-20).
    - MD FY2023 + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row per (MD, fy), GAAP-labelled; no "NASBO" label remains.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.78× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>Each transcribed MD FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO rows replaced in place; the FY2022 negative investment income persists clamped (ACFR-20); the accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-05-MD-LOADLOG.md</files>
  <read_first>
    - scripts/processMDAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. MD --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded MD FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) MD has NO remaining "NASBO" operating rows (one operating row per (MD, fy)); (c) the FY2022 clamp persisted (investment-income category at 0, signed label, parent nets); (d) honest-hole FYs ABSENT; (e) Money In auto-enable — MD ≥1 revenue row; (f) cohort untouched (RECON-08): 9 existing ACFR nodes + a sample NASBO state unchanged. Append a "Load Disposition" section to 108-05-MD-LOADLOG.md: FYs loaded, FYs skipped (+reason), the FY2022 P2-clamp confirmation, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than MD.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 108-05-MD-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of MD --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded MD FY has both operating + revenue rows, GAAP-labelled, non-null sources; MD has ZERO remaining "NASBO" operating rows; one operating row per (MD, fy).
    - The FY2022 investment-income category renders at 0 with the signed label; parent total nets it (ACFR-20).
    - MD ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 108-05-MD-LOADLOG.md has a finalized "Load Disposition" section incl. the FY2022 P2-clamp confirmation.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms MD fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced, FY2022 clamp persisted, Money In enabled), honest holes absent, cohort unchanged; 108-05-MD-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| marylandcomptroller.gov PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the MD node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05-A | Tampering | **FY2022 negative investment income stored unclamped/abs** → negative revenue displayed (invalid) or magnitude flipped | mitigate | clampForRender + "(net loss — shown at 0)" from the PA template; Task 1/2/3 confirm the FY2022 category renders 0 + signed label + net root (ACFR-20) |
| T-108-05-B | Tampering | wrong statement/column (Statement of Activities, or one of the 5 non-GF columns instead of GENERAL FUND) | mitigate | Task 1 takes 1st numeric token of the Governmental Funds Statement; bookend ties gate; validate() exit-2 |
| T-108-05-C | Tampering | wrong-case URL (lowercase for FY2022–2023 or uppercase for FY2024+) → 404 | mitigate | SOURCES special-cases the case change at FY2024; %PDF magic + size guard; acceptance asserts case per FY |
| T-108-05-D | Tampering | tolerance too loose (masking transcription drift as "rounding") | mitigate | validate tolerance ≤ the documented $1–$2K GAAP rounding only; Phase 110 re-derives independently |
| T-108-05-E | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-108-05-F | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (MD, fy), zero NASBO |
| T-108-05-G | Info Disclosure | silently inflating MD (~1.78×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-108-05-H | Tampering | write to a state other than MD | mitigate | loader resolves only MD node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-108-05-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both MD loaders `--dry-run`: every present FY "validation: PASS"; UNITS=1_000; tolerance ≤ documented rounding.
- `processMDRevenueAcfr.js --dry-run --fy 2025` → 48,689,018,000; `--fy 2022` → 50,540,136,000 + "(net loss — shown at 0)" clamp label.
- SOURCES: FY2024/FY2025 lowercase `acfr{YYYY}.pdf`, FY2022/FY2023 uppercase `ACFR{YYYY}.pdf`.
- Live: MD node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place; FY2022 clamp persisted.
- Re-run MD --fy 2025 → "Loaded 0 rows" (idempotent).
- MD ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.78×) recorded (ACFR-19).
- 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 108-05-MD-LOADLOG.md finalized.
</verification>

<success_criteria>
The Maryland state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2022–FY2025, thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped (within documented GAAP thousands rounding), NASBO operating replaced in place (RECON-08), the FY2022 negative investment income rendered honestly via the P2 clamp (ACFR-20), the ~1.78× accept-relabel divergence recorded (ACFR-19), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands MD to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-05-SUMMARY.md` when done — record the MD FY window actually transcribed, FYs omitted as honest holes, the two bookend tie confirmations (+ documented rounding), the FY2022 P2-clamp confirmation, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
