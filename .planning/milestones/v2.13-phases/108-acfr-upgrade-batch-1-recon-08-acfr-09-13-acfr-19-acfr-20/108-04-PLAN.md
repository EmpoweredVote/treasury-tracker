---
phase: 108-acfr-upgrade-batch-1
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processGAAcfr.js
  - scripts/processGARevenueAcfr.js
  - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-04-GA-LOADLOG.md
autonomous: true
requirements: [ACFR-12, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Georgia state node (6eb7dd4a-4dcf-4dcc-898f-45af9a3e20c3) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full clean window FY2021–FY2025, every loaded FY tying to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "GA units are thousands: UNITS=1_000. FY2025 GF Total revenues stored ties to 68,445,055K and FY2021 to 55,378,103K"
    - "The F-97-01 Medicaid fix (Phase 97 correction on GA's FY2023 NASBO operating row) is SUPERSEDED cleanly: the ACFR GAAP operating row REPLACES the (6eb7dd4a, 2023, 'operating') NASBO row in place — no orphan Medicaid-corrected NASBO row competing with the ACFR row (RECON-07)"
    - "The FY2023 + FY2024 NASBO operating rows are replaced in place by ACFR rows — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; the 9 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (GA ACFR GF ~1.98× NASBO) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (ACFR-20); GA investment positive in confirmed years, older checked at load"
    - "Money In auto-enables on GA once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processGAAcfr.js"
      provides: "GA GF spending-by-function loader (operating), thousands×1000, tie-verified FY2021–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processGARevenueAcfr.js"
      provides: "GA GF revenue-by-source loader (revenue), thousands×1000, tie-verified FY2021–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-04-GA-LOADLOG.md"
      provides: "Per-FY load disposition, F-97-01 supersede confirmation, NASBO-replacement, accept-relabel divergence, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processGAAcfr.js / processGARevenueAcfr.js"
      to: "treasury.budgets (GA state node 6eb7dd4a)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) — supersedes the F-97-01 NASBO FY2023 row in place"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map (5 Drupal slugs)"
      to: "sao.georgia.gov ACFR PDFs"
      via: "opaque Drupal document/download slugs (FY2023 uses -0 suffix)"
      pattern: "sao.georgia.gov/document/document"
---

<objective>
Upgrade the **Georgia** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processGAAcfr.js` (`operating`) and `scripts/processGARevenueAcfr.js` (`revenue`) — downloading the GA ACFR PDFs across the full clean window **FY2021–FY2025** (5 Drupal-slug URLs) into gitignored `_acfr-work/ga/`, extracting the **GENERAL FUND column** (1st of 3: General Fund | Other | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing verbatim line items, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying the **F-97-01 supersede** + idempotency + cohort-untouched.

**GA-specific — supersede F-97-01 (RECON-07).** GA's FY2023 NASBO operating row carries a Phase-97 Medicaid correction. The ACFR GAAP operating load writes the same `(6eb7dd4a, 2023, 'operating')` key, so the ACFR actual replaces the corrected NASBO row in place — the fix becomes moot once ACFR wins. Task 2 must confirm no orphan NASBO FY2023 row competes with the ACFR row. **URLs are opaque Drupal slugs** (not derivable; FY2023 uses a `-0` suffix) — enumerate all 5 explicitly.

Output: both GA loaders live-loaded across FY2021–FY2025 (holes logged), NASBO replaced in place (F-97-01 superseded), Money In auto-enabled; `108-04-GA-LOADLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- GA loaders mirror the IL loaders file-for-file. -->
From scripts/processILAcfr.js (spend-by-function analog → processGAAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Georgia', state='GA', entity_type='state' (.single()). Node id = 6eb7dd4a-4dcf-4dcc-898f-45af9a3e20c3 (assert match).
  - UNITS = 1_000. Store thousands in map, ×UNITS at buildTree; validate tolerance in thousands.
  - SOURCES = { [fy]: { url, date } } with all 5 Drupal slugs enumerated; dataSource(fy)="Georgia State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES/REVENUE + validate() exit-2 + buildTree ×UNITS + root {n:'Georgia General Fund Budget'} + RPC + source-stamp.
  - data_sources.dataset_id='ga-acfr-gf-operating' / 'ga-acfr-gf-revenue'; names 'Georgia General Fund Operating Budget' / 'Georgia General Fund Revenue'; base_url=sao.georgia.gov ACFR landing.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue analog → processGARevenueAcfr.js): dataSource(fy)="…General Fund Revenue…"; RPC p_dataset_type:'revenue'; clampForRender + "(net loss — shown at 0)" + signed-net root (ACFR-20).
POPULATION = 11_180_878 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): GA has 2 NASBO operating rows — FY2023 $29,266M (the F-97-01-corrected value, 2025-SER), FY2024 $34,594M — data_source_id=null, NO revenue rows, NO data_sources metadata rows. GA is the one non-cohort NASBO state (loaded separately). ACFR GF (~$68.4B FY2025) is ~1.98× (federal intergovernmental inside GAAP GF), accept-and-relabel per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH1-SOURCES.md GA Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Rev/Exp/Changes. NOT Statement of Activities.
- GF column = 1st of (General Fund | Other | Total). First numeric token per row.
- Units = thousands. FY-end June 30. Clean window = FY2021–FY2025 (5 yrs; pre-FY2021 not discoverable — gap logged).
- URLs (opaque Drupal slugs — enumerate; NOT derivable):
  - FY2025: https://sao.georgia.gov/document/document/fy-2025-acfr/download
  - FY2024: https://sao.georgia.gov/document/document/fy-2024-acfr/download
  - FY2023: https://sao.georgia.gov/document/document/fy-2023-acfr-0/download  (note the -0 suffix)
  - FY2022: https://sao.georgia.gov/document/document/fy-2022-acfr/download
  - FY2021: https://sao.georgia.gov/document/document/fy-2021-acfr/download
- Bookend ties (GENERAL FUND Total revenues, thousands): GA FY2025 = 68,445,055K (diff $0); FY2021 = 55,378,103K (diff $0).
- Investment positive in FY2025 + FY2021; P2 clamp safety net for any in-between negative.
- Scope vs NASBO ~1.98× — accept-and-relabel honestly (ACFR-19).
- F-97-01 supersede (RECON-07): GA FY2023 NASBO row carries the Phase-97 Medicaid fix ($29,266M); the ACFR replace at the same (muni, fy, 'operating') key supersedes it — verify no orphan.
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both GA loaders (IL template) + enumerate 5 Drupal URLs + download/extract/transcribe FY2021–FY2025 + dry-run tie</name>
  <files>scripts/processGAAcfr.js, scripts/processGARevenueAcfr.js, _acfr-work/ga/</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template)
    - scripts/processILRevenueAcfr.js (the exact revenue template incl. clampForRender)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (GA Detail Block: 5 Drupal slugs incl. FY2023 -0 suffix, bookend ties)
  </read_first>
  <action>
    For each FY 2021–2025, download the GA ACFR PDF to `_acfr-work/ga/GA{YYYY}.pdf` using the 5 explicit Drupal slugs (FY2023 uses the `-0` suffix). Soft-404 guard (%PDF magic + size >~500KB; reject HTML). Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, isolate the GENERAL FUND column (1st of General Fund | Other | Total) for BOTH revenues and expenditures. Copy processILAcfr.js → scripts/processGAAcfr.js and processILRevenueAcfr.js → scripts/processGARevenueAcfr.js; set STATE_NAME='Georgia', STATE_ABBR='GA', POPULATION=11_180_878, UNITS=1_000. Build SOURCES with the 5 enumerated slugs (source_date=`${fy}-06-30`). Transcribe the GENERAL FUND line items (verbatim names, thousands) into EXPENDITURES + REVENUE, each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('ga-acfr-gf-operating' / 'ga-acfr-gf-revenue'). Keep clampForRender in the revenue loader. Add a header comment recording the accept-and-relabel scope note (GA ~1.98× NASBO, federal inside GAAP GF, ACFR-19) AND the F-97-01 supersede note (RECON-07). Cross-check FY2025 = 68,445,055K and FY2021 = 55,378,103K before transcribing. Any FY that fails clean extraction or its exact tie → OMIT + note. Run both loaders `--dry-run`; confirm every present FY PASS.
  </action>
  <verify>
    <automated>node scripts/processGAAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processGAAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processGARevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '68,445,055,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ga/` holds a real PDF (%PDF, >~500KB) per FY 2021–2025; soft-404 HTML rejected.
    - `node scripts/processGAAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO "sum ≠ total"; `processGARevenueAcfr.js --dry-run --fy 2025` → 68,445,055,000; `--fy 2021` → 55,378,103,000.
    - SOURCES[2023].url contains `fy-2023-acfr-0/download` (the -0 suffix); SOURCES[2025].url contains `fy-2025-acfr/download`.
    - Header comment records the F-97-01 supersede note (RECON-07) and the ~1.98× accept-relabel note (ACFR-19); UNITS=1_000.
  </acceptance_criteria>
  <done>Both GA loaders exist on the IL template with all 5 Drupal slugs enumerated (FY2023 -0), every transcribed FY dry-run-ties to its printed GENERAL FUND total with the FY2025 68,445,055K / FY2021 55,378,103K bookends, the clamp path + F-97-01 supersede + accept-relabel notes recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load GA (operating + revenue), superseding F-97-01, NASBO replaced in place</name>
  <files>(runs scripts/processGAAcfr.js, scripts/processGARevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processGAAcfr.js + scripts/processGARevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (§2 GA F-97-01 supersede plan; NASBO-replace rule RECON-08)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets for the GA node (6eb7dd4a) and RECORD the pre-load NASBO operating rows (FY2023 $29,266M — the F-97-01-corrected value, FY2024 $34,594M) — never-overwrite baseline + accept-relabel comparator + the F-97-01 supersede baseline. Then for each GA FY present after Task 1, run `node scripts/processGAAcfr.js --fy <YYYY>` then `node scripts/processGARevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (6eb7dd4a, fy, 'operating') for FY2023 (the F-97-01 row) and FY2024; FY2021/FY2022/FY2025 operating + all revenue net-new. Each stamps source_url/source_date/data_source (GAAP label). Abort+note any FY failing the live tie; never force-write. Record loaded GA GF totals against pre-load NASBO totals to confirm the ~1.98× accept-relabel divergence (ACFR-19). Explicitly confirm in the load log that the FY2023 ACFR row REPLACED the F-97-01 NASBO row (no orphan — exactly one operating row at (GA, 2023)).
  </action>
  <verify>
    <automated>echo "GA row existence + F-97-01 supersede + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the GA node has, per loaded FY, an operating row "Georgia State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - GA FY2025 revenue row total = 68,445,055,000; FY2021 = 55,378,103,000.
    - GA FY2023 (F-97-01) + FY2024 NASBO operating rows GONE (replaced in place): exactly ONE operating row at (GA, 2023) and (GA, 2024), GAAP-labelled; no F-97-01 Medicaid-corrected NASBO orphan remains; no "NASBO" label on the GA node.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.98× divergence explained (ACFR-19) + the explicit F-97-01 supersede confirmation.
  </acceptance_criteria>
  <done>Each transcribed GA FY has operating + revenue rows in treasury.budgets, thousands×1000, GAAP-labelled + per-year-sourced; the F-97-01 NASBO FY2023 row + the FY2024 NASBO row replaced in place (no orphan, no duplicate); the accept-relabel divergence + supersede recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-04-GA-LOADLOG.md</files>
  <read_first>
    - scripts/processGAAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. GA --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded GA FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) GA has NO remaining "NASBO" operating rows and NO F-97-01 orphan (exactly one operating row per (GA, fy)); (c) honest-hole FYs ABSENT; (d) Money In auto-enable — GA ≥1 revenue row; (e) cohort untouched (RECON-08): 9 existing ACFR nodes + a sample NASBO state unchanged. Append a "Load Disposition" section to 108-04-GA-LOADLOG.md: FYs loaded, FYs skipped (+reason), the F-97-01 supersede confirmation, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than GA.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 108-04-GA-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of GA --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded GA FY has both operating + revenue rows, GAAP-labelled, non-null sources; GA has ZERO remaining "NASBO" operating rows and NO F-97-01 orphan; one operating row per (GA, fy).
    - GA ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 108-04-GA-LOADLOG.md has a finalized "Load Disposition" section incl. the F-97-01 supersede confirmation.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms GA fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO + F-97-01 replaced cleanly, Money In enabled), honest holes absent, cohort unchanged; 108-04-GA-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sao.georgia.gov PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the GA node only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-04-A | Tampering | **F-97-01 NASBO FY2023 row left as an orphan alongside the ACFR row** → two operating rows / stale Medicaid-corrected budgetary value competing with the GAAP actual | mitigate | RPC keyed (GA, 2023, 'operating') UPDATE-in-place supersedes it; Task 2/3 assert exactly one operating row at (GA, 2023), no NASBO orphan |
| T-108-04-B | Tampering | wrong statement/column (Statement of Activities, or Other / Total instead of GENERAL FUND) | mitigate | Task 1 takes 1st numeric token of the Governmental Funds Statement; bookend ties gate; validate() exit-2 |
| T-108-04-C | Tampering | wrong Drupal slug (FY2023 without -0) → 404 or wrong-year file | mitigate | SOURCES enumerates the exact 5 slugs incl. FY2023 -0; %PDF magic + size guard; per-FY tie sanity |
| T-108-04-D | Tampering | units mis-scale | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals asserted |
| T-108-04-E | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (GA, fy), zero NASBO |
| T-108-04-F | Info Disclosure | silently inflating GA (~1.98×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label (ACFR-19) |
| T-108-04-G | Tampering | write to a state other than GA | mitigate | loader resolves only GA node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-108-04-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both GA loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1_000.
- `processGARevenueAcfr.js --dry-run --fy 2025` → 68,445,055,000; `--fy 2021` → 55,378,103,000.
- Live: GA node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023 (F-97-01) + FY2024 replaced in place (no orphan).
- Re-run GA --fy 2025 → "Loaded 0 rows" (idempotent).
- GA ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.98×) recorded (ACFR-19); F-97-01 supersede confirmed.
- 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 108-04-GA-LOADLOG.md finalized.
</verification>

<success_criteria>
The Georgia state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2021–FY2025 (honest holes logged), thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place with the F-97-01 Medicaid fix superseded cleanly (RECON-07/08 — no orphan), the ~1.98× accept-relabel divergence recorded (ACFR-19), the P2 clamp wired (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands GA to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-04-SUMMARY.md` when done — record the GA FY window actually transcribed, FYs omitted as honest holes, the two bookend tie confirmations, the F-97-01 supersede confirmation, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
