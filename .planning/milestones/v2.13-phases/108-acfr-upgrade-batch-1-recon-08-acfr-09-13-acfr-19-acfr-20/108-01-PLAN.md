---
phase: 108-acfr-upgrade-batch-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processNJAcfr.js
  - scripts/processNJRevenueAcfr.js
  - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md
autonomous: true
requirements: [ACFR-09, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "New Jersey state node (91f310a1-bec9-404a-9825-82b1106c911f) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full clean window FY2020–FY2025, every FY tying to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "NJ units are DOLLARS (not thousands): UNITS=1 — the loader MUST NOT multiply by 1,000. FY2025 GF Total revenues stored = 60,979,024,211 (raw dollars) and FY2020 = 38,768,977,008"
    - "The pre-existing NASBO operating rows (NJ FY2023, FY2024) are REPLACED in place by the ACFR operating rows (same (91f310a1, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; the 9 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (NJ ACFR GF ~1.15× NASBO) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19) — the node is not silently inflated"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (clamped to 0, signed magnitude in label, parent total nets it) (ACFR-20); NJ investment earnings are positive in both bookends so clamp is a safety net"
    - "Money In auto-enables on NJ once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processNJAcfr.js"
      provides: "NJ GF spending-by-function loader (dataset_type='operating'), UNITS=1 (dollars), tie-verified FY2020–FY2025"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processNJRevenueAcfr.js"
      provides: "NJ GF revenue-by-source loader (dataset_type='revenue'), UNITS=1 (dollars), tie-verified FY2020–FY2025"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md"
      provides: "Per-FY load disposition, NASBO-replacement confirmation, accept-relabel divergence record, idempotency re-run result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processNJAcfr.js / processNJRevenueAcfr.js"
      to: "treasury.budgets (NJ state node 91f310a1)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map FY2025 entry"
      to: "nj.gov ACFR PDF"
      via: "FY2025 'FR'-infix-dropped filename special-case (NJFY2025Complete.pdf vs NJFRFY{YYYY}Complete.pdf)"
      pattern: "NJFY2025Complete"
---

<objective>
Upgrade the **New Jersey** state node from NASBO operating-only to full State-ACFR GAAP by building two loaders on the IL template — `scripts/processNJAcfr.js` (GF spending-by-function, `dataset_type='operating'`) and `scripts/processNJRevenueAcfr.js` (GF revenue-by-source, `dataset_type='revenue'`) — downloading the NJ ACFR PDFs FY2020–FY2025 into gitignored `_acfr-work/nj/`, extracting the **GENERAL FUND column** (1st of 4: General Fund | Transportation Trust Fund | Nonmajor | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing the verbatim line items, dry-run-tying every FY to its printed GENERAL FUND total, then live-loading into `treasury.budgets` (NASBO operating replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**CRITICAL — NJ reports in DOLLARS, not thousands.** This is the only Batch-1 state (and the only whole-tranche state) with this. The loader MUST set `UNITS=1` and MUST NOT apply the ×1,000 scaling every other ACFR loader uses. The bookend dollar totals (60,979,024,211 / 38,768,977,008) are the guard.

Output: `scripts/processNJAcfr.js` + `scripts/processNJRevenueAcfr.js` live-loaded across FY2020–FY2025 (honest holes logged), NASBO replaced in place, Money In auto-enabled; `108-01-NJ-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- The EXACT analog pattern. NJ loaders mirror the IL loaders file-for-file, EXCEPT UNITS=1. -->
From scripts/processILAcfr.js (spend-by-function analog → processNJAcfr.js):
  - loadEnv() reads ../.env.local then ../.env. Resolve node by name='New Jersey', state='NJ', entity_type='state' (.single()). Node id = 91f310a1-bec9-404a-9825-82b1106c911f (assert the resolved id matches).
  - **UNITS = 1** (NJ ACFR is already in DOLLARS — do NOT ×1,000). This is the single deviation from the IL template. Store the printed dollar figures verbatim.
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "New Jersey State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES = { [fy]: { total (printed General-Fund Total expenditures, dollars), confidence:'actual', categories:[{name, total}] } }.
  - validate(fy): category sum vs printed total; tight tolerance (a few dollars — NJ tied at $0 in recon) else process.exit(2).
  - buildTree(fy): children = categories.filter(>0).map → {n, a: total*UNITS, i:[]}; root {n:'New Jersey General Fund Budget', a: total*UNITS, c:children}.
  - RPC treasury_sync_budget_tree(p_data_source_id, p_fiscal_year, p_dataset_type:'operating', p_total, p_tree, p_row_count, p_triggered_by:'bulk_load') → SELECT budgets row → UPDATE {source_url, source_date, data_source}.
  - data_sources.dataset_id = 'nj-acfr-gf-operating', name = 'New Jersey General Fund Operating Budget', api_type='pdf_download', base_url = the nj.gov OMB landing page.
  - flags: --dry-run, --fy YYYY.
From scripts/processILRevenueAcfr.js (revenue-by-source analog → processNJRevenueAcfr.js):
  - dataSource(fy) = "New Jersey State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='nj-acfr-gf-revenue'; name='New Jersey General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); buildTree labels a negative "<name> (net loss — shown at 0)"; root total carries the signed net (ACFR-20).
  - REVENUE = { [fy]: { total (printed General-Fund Total revenues, dollars), confidence:'actual', categories:[{name, total}] } }. UNITS=1.
POPULATION = 9_288_994 (from loadStateGF.mjs).
NASBO baseline (from 107-RECON.md): NJ has 2 NASBO operating rows only — FY2023 $48,837M, FY2024 $52,996M (data_source "NASBO State Expenditure Report…"), data_source_id=null, NO revenue rows, NO data_sources metadata rows. ACFR GF (~$61B FY2025) is materially larger — the expected ~1.15× TX-trap divergence (federal/intergovernmental inside GAAP GF), accepted-and-relabelled per ACFR-19.
DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>

<source_facts>
<!-- From 107-BATCH1-SOURCES.md NJ Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Revenues, Expenditures, and Changes in Fund Balances. NOT Statement of Activities, NOT budgetary comparison.
- GF column = 1st of (General Fund | Transportation Trust Fund | Nonmajor | Total). Take the FIRST numeric token (General Fund) per row.
- Units = **DOLLARS**. FY-end June 30. Clean window = FY2020–FY2025 (6 years).
- URL FY2024 and older: https://www.nj.gov/treasury/omb/publications/{YY}fr/pdfs/NJFRFY{YYYY}Complete.pdf  ({YY}=24,23,22,21,20)
- URL FY2025 (SPECIAL — drops "FR" infix): https://www.nj.gov/treasury/omb/publications/25fr/pdfs/NJFY2025Complete.pdf  ← SOURCES map MUST enumerate FY2025 explicitly.
- Landing: https://www.nj.gov/treasury/omb/fr.shtml
- Bookend ties (GENERAL FUND column Total revenues, DOLLARS): NJ FY2025 = 60,979,024,211 (diff $0); FY2020 = 38,768,977,008 (diff $0).
- Investment: FY2025 Investment earnings +$952,995,499; FY2020 +$65,483,367 (positive). No P2 clamp needed for confirmed years; clamp is a safety net for any in-between year.
- Scope vs NASBO ~1.15× (smallest divergence in the tranche) — still requires honest accept-and-relabel (ACFR-19).
- Pre-FY2020: URL pattern not verified durable — low priority, extend at load only if accessible under the same pattern (honest hole otherwise).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both NJ loaders (UNITS=1 dollars) + download/extract/transcribe FY2020–FY2025 + dry-run tie</name>
  <files>scripts/processNJAcfr.js, scripts/processNJRevenueAcfr.js, _acfr-work/nj/ (gitignored PDFs + pdftotext output)</files>
  <read_first>
    - scripts/processILAcfr.js (the exact spend-by-function template — copy its shape: loadEnv, SOURCES, EXPENDITURES, validate, buildTree, RPC + source-stamp, --dry-run/--fy)
    - scripts/processILRevenueAcfr.js (the exact revenue-by-source template incl. clampForRender + negative-label path)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (NJ Detail Block: per-year URLs incl. FY2025 special-case, units=DOLLARS, bookend ties)
  </read_first>
  <action>
    Confirm `_acfr-work/` is gitignored (already present). For each FY 2020–2025, download the NJ ACFR PDF to `_acfr-work/nj/NJ{YYYY}.pdf` using `…/{YY}fr/pdfs/NJFRFY{YYYY}Complete.pdf` for FY2020–FY2024 and the special `…/25fr/pdfs/NJFY2025Complete.pdf` for FY2025. Soft-404 guard: assert each file is a real PDF (magic bytes `%PDF`, size > ~500KB) — a saved HTML 404 page must abort that FY, not be parsed. Run `pdftotext -table` (NOT -layout) on each, locate the Governmental Funds Statement of Rev/Exp/Changes, and isolate the GENERAL FUND column (1st numeric token of General Fund | Transportation Trust | Nonmajor | Total) for BOTH the revenues section and the expenditures section. Copy processILAcfr.js → scripts/processNJAcfr.js and processILRevenueAcfr.js → scripts/processNJRevenueAcfr.js; set STATE_NAME='New Jersey', STATE_ABBR='NJ', POPULATION=9_288_994, and **UNITS=1** (delete/override the ×1,000 — NJ is dollars). Build SOURCES FY2020–FY2025 (source_date=`${fy}-06-30`) with the FY2025 special-case filename. Transcribe the extracted GENERAL FUND line items (verbatim names, raw DOLLARS) into EXPENDITURES (operating loader) and REVENUE (revenue loader), each `total` = the printed General-Fund Total. Set dataSource labels + dataset_id ('nj-acfr-gf-operating' / 'nj-acfr-gf-revenue') per interfaces. Keep clampForRender + the "(net loss — shown at 0)" path in the revenue loader. Add a header comment recording the accept-and-relabel scope note (NJ ~1.15× NASBO, federal inside GAAP GF, ACFR-19). Cross-check FY2025 Total revenues = 60,979,024,211 and FY2020 = 38,768,977,008 — if either bookend does not match the extracted GF column, STOP and re-inspect column selection before transcribing. If an in-between FY fails to cleanly extract or tie, OMIT it (honest hole) and note it. Run both loaders `--dry-run` and confirm every present FY prints a PASS tie.
  </action>
  <verify>
    <automated>node scripts/processNJAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processNJAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processNJRevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '60,979,024,211'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/nj/` holds a real PDF (%PDF magic, >~500KB) per downloadable FY 2020–2025; soft-404 HTML rejected.
    - `node scripts/processNJAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO "sum ≠ total"; `node scripts/processNJRevenueAcfr.js --dry-run --fy 2025` prints TOTAL 60,979,024,211 (NOT ×1000 — dollars) and `--fy 2020` prints 38,768,977,008.
    - Both loaders have `UNITS = 1` (grep: `grep -E "UNITS *= *1\b" scripts/processNJAcfr.js` matches; NO `* 1_000` scaling on the stored total).
    - SOURCES[2025].url contains `NJFY2025Complete.pdf`; SOURCES[2024].url contains `NJFRFY2024Complete.pdf`.
    - dataSource(2025) === "New Jersey State ACFR — General Fund (FY2025 actual, GAAP basis)"; revenue RPC uses p_dataset_type:'revenue', dataset_id='nj-acfr-gf-revenue'.
  </acceptance_criteria>
  <done>Both NJ loaders exist on the IL template with UNITS=1 (dollars), SOURCES special-cases FY2025, every transcribed FY dry-run-ties to its printed GENERAL FUND total with the FY2025 60,979,024,211 / FY2020 38,768,977,008 dollar bookends, the P2 clamp path is wired, the accept-relabel note is recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load NJ (operating + revenue) across the tied window, NASBO replaced in place</name>
  <files>(runs scripts/processNJAcfr.js, scripts/processNJRevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processNJAcfr.js + scripts/processNJRevenueAcfr.js (the --fy + post-RPC source-stamp path; confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (NASBO-replace rule RECON-08; NJ node id + pre-load NASBO totals)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets (mcp__supabase-local__execute_sql) for the NJ node (91f310a1) current rows and RECORD the pre-load NASBO operating rows (FY2023 $48,837M, FY2024 $52,996M: total + data_source) — the never-overwrite baseline + accept-relabel comparator. Then for each NJ FY present after Task 1 (FY2020–FY2025 minus honest holes), run `node scripts/processNJAcfr.js --fy <YYYY>` then `node scripts/processNJRevenueAcfr.js --fy <YYYY>` (live, no --dry-run). Each must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (91f310a1, fy, 'operating'); revenue inserts net-new at (91f310a1, fy, 'revenue')), and stamp source_url/source_date/data_source (GAAP basis label). If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record the loaded NJ GF operating + revenue totals against the pre-load NASBO totals to confirm the ~1.15× accept-relabel divergence is the expected federal-inside-GAAP-GF mechanism (ACFR-19), not a transcription error.
  </action>
  <verify>
    <automated>echo "NJ row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the NJ node has, per loaded FY, an operating row "New Jersey State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both with non-null source_url/source_date.
    - NJ FY2025 revenue row total = 60,979,024,211 and FY2020 = 38,768,977,008 (raw dollars — NOT ×1000).
    - NJ FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (NJ, fy), GAAP-labelled; no "NASBO" label remains on the NJ node.
    - Pre-load NASBO totals + loaded ACFR GF totals both recorded in the load log with the ~1.15× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>Each transcribed NJ FY has operating + revenue rows in treasury.budgets, dollar-scaled (UNITS=1), GAAP-basis-labelled + per-year-sourced; FY2023/FY2024 NASBO operating rows replaced in place (no duplicates); the accept-relabel divergence recorded against pre-load NASBO totals.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md</files>
  <read_first>
    - scripts/processNJAcfr.js (confirm RPC keyed (muni, fy, dataset) — the never-overwrite basis)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract — the 9 ACFR node ids + un-upgraded NASBO states)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. NJ --fy 2025 operating + revenue) and confirm 0 net change (same total, row_count, source → UPDATE-in-place identical data → "Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded NJ FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) NJ has NO remaining "NASBO State Expenditure Report" operating rows (exactly one operating row per (NJ, fy)); (c) honest-hole FYs are ABSENT; (d) Money In auto-enable — NJ has ≥1 dataset_type='revenue' row; (e) cohort untouched (RECON-08): spot-check that the 9 existing ACFR nodes (e.g. CA e1007bf5, PA d4a4aadc) and a sample un-upgraded NASBO state are unchanged (row counts + a couple of data_source labels). Append a "Load Disposition" section to 108-01-NJ-LOADLOG.md: FYs loaded, FYs skipped (+reason), pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel divergence note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied window; do NOT touch any state other than NJ.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 108-01-NJ-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of NJ --fy 2025 (operating + revenue) reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded NJ FY has both operating + revenue rows, all GAAP-basis-labelled with non-null sources; NJ has ZERO remaining "NASBO" operating rows; exactly one operating row per (NJ, fy).
    - NJ has ≥1 revenue row (Money In auto-enable confirmed).
    - Honest-hole FYs (from Task 1) are ABSENT from the DB.
    - Sample of the 9 existing ACFR nodes + one un-upgraded NASBO state unchanged (RECON-08).
    - 108-01-NJ-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms NJ fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 108-01-NJ-LOADLOG.md has a finalized Load Disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nj.gov PDF → loader transcription | untrusted external file parsed into displayed public financial figures |
| loader → treasury.budgets / data_sources | live write scoped to the NJ node only (Task 2–3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01-A | Tampering | **units mis-scale — the fatal NJ trap: applying the ×1,000 that every other ACFR loader uses inflates NJ 1000×** | mitigate | UNITS=1 hard-set + acceptance grep; bookend dollar totals (60,979,024,211 / 38,768,977,008) asserted in dry-run + live |
| T-108-01-B | Tampering | extracting the wrong statement/column (Statement of Activities, or Transportation Trust / Nonmajor / Total instead of GENERAL FUND) | mitigate | Task 1 isolates 1st numeric token of the Governmental Funds Statement of Rev/Exp/Changes; bookend ties gate before transcription; validate() exit-2 on non-tie |
| T-108-01-C | Tampering | soft-404 HTML saved as .pdf → garbage rows | mitigate | %PDF magic + size guard; non-PDF aborts that FY |
| T-108-01-D | Tampering | wrong FY2025 URL (using the FR-infix pattern) → missing latest-year file | mitigate | SOURCES special-cases FY2025 to NJFY2025Complete.pdf; acceptance asserts it |
| T-108-01-E | Tampering | NASBO row left alongside the ACFR row (duplicate) / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (NJ, fy), zero NASBO labels |
| T-108-01-F | Info Disclosure | silently inflating NJ (~1.15×) without recording the divergence | mitigate | Tasks 1–2 record pre-load NASBO vs loaded ACFR; GAAP basis label on live rows (ACFR-19) |
| T-108-01-G | Tampering | write to a state other than NJ (the 9 ACFR nodes / NASBO states) | mitigate | loader resolves only name='New Jersey'+state+entity_type='state'; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-108-01-SC | Tampering | npm/pip installs | accept | no new packages — reuses @supabase/supabase-js + pdftotext (system tool, prior precedent) |
</threat_model>

<verification>
- Both NJ loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1 (dollars).
- `processNJRevenueAcfr.js --dry-run --fy 2025` → 60,979,024,211; `--fy 2020` → 38,768,977,008.
- Live: NJ node has operating + revenue rows per loaded FY, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place (zero NASBO labels, one operating row per (NJ, fy)).
- Re-run NJ --fy 2025 → "Loaded 0 rows" (idempotent never-overwrite).
- NJ ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.15×) recorded against pre-load NASBO totals (ACFR-19).
- 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 108-01-NJ-LOADLOG.md finalized.
</verification>

<success_criteria>
The New Jersey state node is live on full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across FY2020–FY2025 (honest holes logged), stored in RAW DOLLARS (UNITS=1), every FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place (RECON-08), the ~1.15× accept-relabel divergence recorded at load (ACFR-19), the P2 clamp wired for any negative year (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort demonstrably untouched. Hands NJ to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-SUMMARY.md` when done — record the NJ FY window actually transcribed, any FY omitted as a non-tying honest hole, the two bookend tie confirmations (dollars), the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run result, and the Money-In auto-enable confirmation.
</output>
