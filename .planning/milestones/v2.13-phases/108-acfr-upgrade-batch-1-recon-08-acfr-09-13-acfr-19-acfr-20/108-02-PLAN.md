---
phase: 108-acfr-upgrade-batch-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processMAAcfr.js
  - scripts/processMARevenueAcfr.js
  - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-MA-LOADLOG.md
autonomous: true
requirements: [ACFR-10, ACFR-19, ACFR-20, RECON-08]

must_haves:
  truths:
    - "Massachusetts state node (fd6b008f-4d35-4665-8c6a-0429de5a4e1f) shows ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating') across its full clean window FY2001–FY2025 (as deep as each year cleanly extracts + ties), every loaded FY tying to the printed GENERAL FUND column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "MA is an IN-PLACE upgrade of the existing state node (RECON-07): NO duplicate MA node, NO new municipality row. The v1.8 DLS data is city-level (unrelated); before writing, verify the MA state node holds only the 2 NASBO operating rows and NO pre-existing ACFR rows / no stale data_sources metadata"
    - "MA units are thousands: UNITS=1_000 (×1,000 to store dollars). FY2025 GF Total revenues stored ties to 61,907,573K and FY2015 to 35,029,512K"
    - "The pre-existing NASBO operating rows (MA FY2023, FY2024) are REPLACED in place by the ACFR operating rows (same (fd6b008f, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY produces 0 net change; the 9 existing ACFR nodes + all un-upgraded NASBO states are untouched (RECON-08)"
    - "The accept-and-relabel scope divergence (MA ACFR GF ~1.73× NASBO) is recorded at load against the pre-load NASBO totals and the GAAP basis label is verified on the live rows (ACFR-19)"
    - "Any negative GENERAL FUND category year renders via the P2 clamp (ACFR-20); MA embeds investment income in Miscellaneous (no standalone negative line) so clamp is a safety net"
    - "Money In auto-enables on MA once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: "scripts/processMAAcfr.js"
      provides: "MA GF spending-by-function loader (dataset_type='operating'), thousands×1000, tie-verified full window"
      contains: "treasury_sync_budget_tree"
      min_lines: 120
    - path: "scripts/processMARevenueAcfr.js"
      provides: "MA GF revenue-by-source loader (dataset_type='revenue'), thousands×1000, tie-verified full window"
      contains: "clampForRender"
      min_lines: 120
    - path: ".planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-MA-LOADLOG.md"
      provides: "Per-FY load disposition (full 25-yr window), in-place-upgrade confirmation, NASBO-replacement, accept-relabel divergence, idempotency result"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processMAAcfr.js / processMARevenueAcfr.js"
      to: "treasury.budgets (MA state node fd6b008f — in place)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) + post-RPC source-stamp UPDATE"
      pattern: "treasury_sync_budget_tree"
    - from: "SOURCES map FY2017 entry"
      to: "macomptroller.org ACFR PDF"
      via: "FY2017 no-hyphen filename special-case (acfr_fy2017.pdf vs acfr_fy-{YYYY}.pdf)"
      pattern: "acfr_fy2017"
---

<objective>
Upgrade the **Massachusetts** state node **in place** (RECON-07 — no duplicate node) from NASBO operating-only to full State-ACFR GAAP by building two loaders on the PA template — `scripts/processMAAcfr.js` (`operating`) and `scripts/processMARevenueAcfr.js` (`revenue`) — downloading the MA ACFR PDFs across the full clean window **FY2001–FY2025** into gitignored `_acfr-work/ma/`, extracting the **GENERAL FUND column** (1st of 3: General Fund | Other Governmental Funds | Total) of the Governmental Funds *Statement of Revenues, Expenditures, and Changes in Fund Balances* via `pdftotext -table`, transcribing verbatim line items, dry-run-tying every FY, then live-loading (NASBO replaced in place, revenue net-new) and verifying idempotency + cohort-untouched.

**Depth = FULL clean window (D-01).** MA's 25-year window (FY2001–FY2025) is the deepest in the tranche — each year is a hand-transcribed rev+spend block. "Full window" means *attempt* every year; any FY that fails clean `-table` extraction or its exact tie is dropped + logged (honest hole), never fudged. FY2001–FY2016 filenames are confirmed at load from the macomptroller landing page.

Output: `scripts/processMAAcfr.js` + `scripts/processMARevenueAcfr.js` live-loaded across the extractable FY2001–FY2025 window (holes logged), NASBO replaced in place, Money In auto-enabled; `108-02-MA-LOADLOG.md` with the load disposition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md

<interfaces>
<!-- MA loaders mirror the PA loaders file-for-file. -->
From scripts/processPAAcfr.js (spend-by-function analog → processMAAcfr.js):
  - loadEnv() → ../.env.local then ../.env. Resolve node by name='Massachusetts', state='MA', entity_type='state' (.single()). Node id = fd6b008f-4d35-4665-8c6a-0429de5a4e1f (assert match).
  - UNITS = 1_000 (MA ACFR in thousands → ×1,000 to store dollars). Store thousands in the map, ×UNITS at buildTree; validate tolerance in thousands.
  - SOURCES = { [fy]: { url, date } }; dataSource(fy) = "Massachusetts State ACFR — General Fund (FY${fy} actual, GAAP basis)".
  - EXPENDITURES = { [fy]: { total, confidence:'actual', categories:[{name,total}] } }; validate(fy) exit-2 on non-tie.
  - buildTree(fy): children = categories.filter(>0).map → {n, a: total*UNITS, i:[]}; root {n:'Massachusetts General Fund Budget', a: total*UNITS, c:children}.
  - RPC treasury_sync_budget_tree(...p_dataset_type:'operating'...) → SELECT budgets row → UPDATE {source_url, source_date, data_source}.
  - data_sources.dataset_id='ma-acfr-gf-operating', name='Massachusetts General Fund Operating Budget', api_type='pdf_download', base_url=macomptroller landing page.
  - flags: --dry-run, --fy YYYY.
From scripts/processPARevenueAcfr.js (revenue-by-source analog → processMARevenueAcfr.js):
  - dataSource(fy)="Massachusetts State ACFR — General Fund Revenue (FY${fy} actual, GAAP basis)"; dataset_id='ma-acfr-gf-revenue'; name='Massachusetts General Fund Revenue'; RPC p_dataset_type:'revenue'.
  - clampForRender(amount)=Math.max(amount,0); negative → "<name> (net loss — shown at 0)"; root total carries signed net (ACFR-20).
POPULATION = 7_029_917 (from loadStateGF.mjs).
NASBO baseline (107-RECON.md): MA state node holds ONLY 2 NASBO operating rows — FY2023 $34,287M, FY2024 $35,720M (data_source "NASBO…"), data_source_id=null, NO revenue rows, NO data_sources metadata rows (Phase 107 probe confirmed zero ma-% rows). ACFR GF (~$61.9B FY2025) is ~1.73× — expected TX-trap divergence (federal grants/lottery inside GAAP GF), accept-and-relabel per ACFR-19.
IN-PLACE UPGRADE (RECON-07): the v1.8 DLS load was city-level municipal data, NOT a state-ACFR node — no dual-node conflict. Write to the state node fd6b008f only. No stale-metadata deletion needed (unlike the CA v1.7 case).
DB access for verification: mcp__supabase-local__execute_sql.
</interfaces>

<source_facts>
<!-- From 107-BATCH1-SOURCES.md MA Detail Block — USE these, do not re-research. -->
- Statement: Governmental Funds Statement of Rev/Exp/Changes. NOT Statement of Activities.
- GF column = 1st of (General Fund | Other Governmental Funds | Total). First numeric token per row.
- Units = thousands. FY-end June 30. Clean window = FY2001–FY2025 (25 yrs; FY2015 old-end sampled, earlier durable).
- URL FY2018+ (hyphen): https://www.macomptroller.org/wp-content/uploads/acfr_fy-{YYYY}.pdf
- URL FY2017 (SPECIAL — no hyphen): https://www.macomptroller.org/wp-content/uploads/acfr_fy2017.pdf
- URL FY2001–FY2016: available on the landing page — confirm exact filenames at load (may vary; enumerate from https://www.macomptroller.org/resource-categories/annual-comprehensive-financial-reports/).
- Bookend ties (GENERAL FUND Total revenues, thousands): MA FY2025 = 61,907,573K (diff $0); FY2015 = 35,029,512K (diff $0).
- Investment income embedded in Miscellaneous — no standalone negative line; no P2 clamp risk (clamp is a safety net).
- Scope vs NASBO ~1.73× — accept-and-relabel honestly (ACFR-19).
</source_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build both MA loaders (PA template) + download/extract/transcribe full window FY2001–FY2025 + dry-run tie</name>
  <files>scripts/processMAAcfr.js, scripts/processMARevenueAcfr.js, _acfr-work/ma/</files>
  <read_first>
    - scripts/processPAAcfr.js (the exact spend-by-function template: loadEnv, SOURCES, EXPENDITURES, validate, buildTree, RPC + source-stamp, --dry-run/--fy)
    - scripts/processPARevenueAcfr.js (the exact revenue template incl. clampForRender + negative-label path + thousands ×1000 convention)
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (MA Detail Block: URL pattern incl. FY2017 no-hyphen special-case, units, bookend ties)
  </read_first>
  <action>
    For each FY across the full window FY2001–FY2025, resolve the ACFR PDF URL: `acfr_fy-{YYYY}.pdf` for FY2018+, the special `acfr_fy2017.pdf` (no hyphen) for FY2017, and for FY2001–FY2016 enumerate the exact filenames from the macomptroller landing page at load. Download each to `_acfr-work/ma/MA{YYYY}.pdf` with a soft-404 guard (%PDF magic + size >~500KB; reject HTML). Run `pdftotext -table` on each, locate the Governmental Funds Statement of Rev/Exp/Changes, isolate the GENERAL FUND column (1st of General Fund | Other Governmental | Total) for BOTH revenues and expenditures. Copy processPAAcfr.js → scripts/processMAAcfr.js and processPARevenueAcfr.js → scripts/processMARevenueAcfr.js; set STATE_NAME='Massachusetts', STATE_ABBR='MA', POPULATION=7_029_917, UNITS=1_000. Build SOURCES for every extractable FY (source_date=`${fy}-06-30`) with the FY2017 special-case. Transcribe the GENERAL FUND line items (verbatim names, thousands) into EXPENDITURES + REVENUE, each `total` = printed General-Fund Total. Set dataSource labels + dataset_id ('ma-acfr-gf-operating' / 'ma-acfr-gf-revenue'). Keep clampForRender in the revenue loader. Add a header comment recording the accept-and-relabel scope note (MA ~1.73× NASBO, federal/lottery inside GAAP GF, ACFR-19) AND the in-place-upgrade note (RECON-07 — state node fd6b008f, v1.8 DLS is unrelated city-level data). Cross-check FY2025 Total revenues = 61,907,573K and FY2015 = 35,029,512K before transcribing the rest. Any FY that fails clean extraction or its exact tie → OMIT + note (honest hole); do NOT force-fit — the 25-year depth is best-effort per D-01. Run both loaders `--dry-run`; confirm every present FY prints PASS.
  </action>
  <verify>
    <automated>node scripts/processMAAcfr.js --dry-run 2>&1 | grep -Eiq 'validation: PASS' && ! node scripts/processMAAcfr.js --dry-run 2>&1 | grep -qi 'sum.*≠.*total' && node scripts/processMARevenueAcfr.js --dry-run --fy 2025 2>&1 | grep -q '61,907,573,000'</automated>
  </verify>
  <acceptance_criteria>
    - `_acfr-work/ma/` holds a real PDF (%PDF, >~500KB) per extractable FY; soft-404 HTML rejected.
    - `node scripts/processMAAcfr.js --dry-run` prints "validation: PASS" for every present FY and NO "sum ≠ total"; `processMARevenueAcfr.js --dry-run --fy 2025` → TOTAL 61,907,573,000 (thousands ×1000); `--fy 2015` → 35,029,512,000.
    - SOURCES[2017].url contains `acfr_fy2017.pdf` (no hyphen); SOURCES[2018].url contains `acfr_fy-2018.pdf`.
    - Both loaders resolve node by name='Massachusetts'+state='MA'+entity_type='state'; UNITS=1_000.
    - Header comment records the in-place-upgrade note (RECON-07) and the ~1.73× accept-relabel note (ACFR-19).
  </acceptance_criteria>
  <done>Both MA loaders exist on the PA template, SOURCES special-cases FY2017 + enumerates FY2001–FY2016, every transcribed FY dry-run-ties to its printed GENERAL FUND total with the FY2025 61,907,573K / FY2015 35,029,512K bookends, the clamp path + in-place-upgrade + accept-relabel notes are recorded, and no live write occurred.</done>
</task>

<task type="auto">
  <name>Task 2: Verify no pre-existing ACFR rows, then live-load MA in place (operating + revenue), NASBO replaced</name>
  <files>(runs scripts/processMAAcfr.js, scripts/processMARevenueAcfr.js against live DB)</files>
  <read_first>
    - scripts/processMAAcfr.js + scripts/processMARevenueAcfr.js (the --fy + source-stamp path; RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (§1 MA in-place upgrade plan; NASBO-replace rule RECON-08)
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets + treasury.data_sources (mcp__supabase-local__execute_sql) for the MA node (fd6b008f) and CONFIRM the RECON-07 precondition: exactly the 2 NASBO operating rows (FY2023 $34,287M, FY2024 $35,720M), NO revenue rows, NO pre-existing ACFR operating rows, and ZERO `ma-%` data_sources metadata rows. RECORD the pre-load NASBO totals (never-overwrite baseline + accept-relabel comparator). If any pre-existing ACFR row or ma-% data_source is found, STOP and surface it (the in-place-upgrade assumption is violated). Then for each MA FY present after Task 1, run `node scripts/processMAAcfr.js --fy <YYYY>` then `node scripts/processMARevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (fd6b008f, fy, 'operating') for FY2023/FY2024; all other FYs operating + all revenue are net-new. Each stamps source_url/source_date/data_source (GAAP label). Abort+note any FY failing the live tie; never force-write. Record loaded MA GF totals against pre-load NASBO totals to confirm the ~1.73× accept-relabel divergence (federal inside GAAP GF — ACFR-19).
  </action>
  <verify>
    <automated>echo "MA row existence + in-place-upgrade + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - Pre-load probe confirmed: MA node had only 2 NASBO operating rows, no revenue, no pre-existing ACFR rows, zero ma-% data_sources (RECON-07 precondition) — recorded in the load log.
    - After load, the MA node has, per loaded FY, an operating row "Massachusetts State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO…") and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - MA FY2025 revenue row total = 61,907,573,000; FY2015 = 35,029,512,000.
    - MA FY2023 + FY2024 NASBO operating rows are GONE (replaced in place): exactly ONE operating row per (MA, fy), GAAP-labelled; no "NASBO" label remains; NO duplicate MA node created.
    - Pre-load NASBO vs loaded ACFR totals recorded with the ~1.73× divergence explained (ACFR-19).
  </acceptance_criteria>
  <done>The RECON-07 in-place precondition is verified; each transcribed MA FY has operating + revenue rows in treasury.budgets on the single existing node fd6b008f, thousands×1000, GAAP-labelled + per-year-sourced; FY2023/FY2024 NASBO rows replaced in place (no duplicate node); the accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query) + LOADLOG</name>
  <files>.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-MA-LOADLOG.md</files>
  <read_first>
    - scripts/processMAAcfr.js (confirm RPC keyed (muni, fy, dataset))
    - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md (RECON-08 untouched-nodes contract — 9 ACFR node ids)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY live a second time (e.g. MA --fy 2025 operating + revenue) → 0 net change ("Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded MA FY has BOTH operating + revenue rows, GAAP-labelled, non-null sources; (b) MA has NO remaining "NASBO" operating rows (one operating row per (MA, fy)); (c) exactly ONE MA state node exists (no duplicate created); (d) honest-hole FYs ABSENT; (e) Money In auto-enable — MA has ≥1 revenue row; (f) cohort untouched (RECON-08): the 9 existing ACFR nodes + a sample NASBO state unchanged (row counts + sample labels). Append a "Load Disposition" section to 108-02-MA-LOADLOG.md: FYs loaded (of the 25-yr window), FYs skipped (+reason/honest hole), the in-place-upgrade confirmation, pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel note, NASBO-replacement confirmation, idempotency re-run result. Do NOT touch any state other than MA.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 108-02-MA-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - Second live run of MA --fy 2025 (operating + revenue) → "Loaded 0 rows" / 0 net change.
    - DB query: every loaded MA FY has both operating + revenue rows, GAAP-labelled, non-null sources; MA has ZERO remaining "NASBO" operating rows; exactly one operating row per (MA, fy); exactly one MA state node.
    - MA ≥1 revenue row (Money In auto-enabled).
    - Honest-hole FYs ABSENT from the DB.
    - 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
    - 108-02-MA-LOADLOG.md has a finalized "Load Disposition" section.
  </acceptance_criteria>
  <done>A representative-FY re-run produces 0 net change; the DB query confirms MA fully ACFR-sourced in place (operating + revenue, GAAP-labelled, NASBO replaced, single node, Money In enabled), honest holes absent, cohort unchanged; 108-02-MA-LOADLOG.md finalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macomptroller.org PDF → loader transcription | untrusted external file parsed into displayed public figures |
| loader → treasury.budgets / data_sources | live write scoped to the MA state node fd6b008f only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-02-A | Tampering | duplicate MA node created (v1.8 DLS confusion) instead of in-place upgrade | mitigate | resolve the single existing state node fd6b008f by name+state+entity_type; Task 2 pre-probe confirms no pre-existing ACFR rows; Task 3 asserts exactly one MA node |
| T-108-02-B | Tampering | wrong statement/column (Statement of Activities, or Other Governmental / Total instead of GENERAL FUND) | mitigate | Task 1 isolates 1st numeric token of the Governmental Funds Statement; bookend ties gate; validate() exit-2 |
| T-108-02-C | Tampering | units mis-scale (forgetting ×1000, or double-scaling) | mitigate | UNITS=1_000; thousands in map, ×1000 at buildTree; bookend dollar totals (…,573,000 / …,512,000) asserted |
| T-108-02-D | Tampering | wrong FY2017 URL (hyphen) → missing file | mitigate | SOURCES special-cases FY2017 to acfr_fy2017.pdf; acceptance asserts it |
| T-108-02-E | Tampering | soft-404 HTML saved as .pdf | mitigate | %PDF magic + size guard |
| T-108-02-F | Tampering | NASBO row left as duplicate / ACFR under a new key | mitigate | RPC keyed (muni, fy, 'operating') UPDATE-in-place; Task 3 asserts one operating row per (MA, fy), zero NASBO labels |
| T-108-02-G | Info Disclosure | silently inflating MA (~1.73×) without recording the divergence | mitigate | pre-load NASBO vs loaded ACFR recorded; GAAP label on live rows (ACFR-19) |
| T-108-02-H | Tampering | write to a state other than MA | mitigate | loader resolves only MA node; Task 3 spot-checks cohort unchanged (RECON-08) |
| T-108-02-SC | Tampering | npm/pip installs | accept | no new packages — reuses existing tooling + pdftotext |
</threat_model>

<verification>
- Both MA loaders `--dry-run`: every present FY "validation: PASS", no "sum ≠ total"; UNITS=1_000.
- `processMARevenueAcfr.js --dry-run --fy 2025` → 61,907,573,000; `--fy 2015` → 35,029,512,000.
- Pre-load probe: MA node had only 2 NASBO rows, no revenue, no pre-existing ACFR rows, zero ma-% data_sources (RECON-07).
- Live: MA node has operating + revenue rows per loaded FY on the single node fd6b008f, GAAP-labelled, non-null sources; NASBO FY2023/FY2024 replaced in place; no duplicate node.
- Re-run MA --fy 2025 → "Loaded 0 rows" (idempotent).
- MA ≥1 revenue row → Money In auto-enabled.
- Accept-relabel divergence (~1.73×) recorded (ACFR-19).
- 9 existing ACFR nodes + a sample NASBO state unchanged (RECON-08).
- 108-02-MA-LOADLOG.md finalized.
</verification>

<success_criteria>
The Massachusetts state node is upgraded IN PLACE (no duplicate) to full State-ACFR GAAP (GF revenue-by-source + GAAP spending-by-function) across the extractable FY2001–FY2025 window (honest holes logged), thousands×1000, every loaded FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, NASBO operating replaced in place (RECON-07/08), the ~1.73× accept-relabel divergence recorded (ACFR-19), the P2 clamp wired (ACFR-20), idempotent never-overwrite proven, Money In auto-enabled, and the rest of the cohort untouched. Hands MA to Phase 110 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-SUMMARY.md` when done — record the MA FY window actually transcribed (of the 25-yr clean window), FYs omitted as honest holes, the two bookend tie confirmations, the in-place-upgrade confirmation, the NASBO-replacement confirmation, the accept-relabel divergence, the idempotency result, and the Money-In auto-enable confirmation.
</output>
