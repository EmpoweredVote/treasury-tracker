---
phase: 110-verification-source-chain-audit-uat-ver-05-ver-06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase110-cohort-audit.mjs
  - .planning/phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "The full 50-node state cohort source-chain audit is clean: 0 NULL-basis, 0 fragile/residue, 0 out-of-window, 0 dup, 0 orphan"
    - "Every displayed state-node row carries a basis label (data_source + source_url + source_date)"
    - "All 19 ACFR-state windows (9 pre-tranche + 10 new) pass the out-of-window invariant against their actual loaded bounds"
    - "The 31 un-upgraded NASBO states still pass — NASBO provenance, operating-only, untouched"
    - "Re-running a representative Batch-1 AND a representative Batch-2 loader produces 0 net change (idempotency), with the WR-05 data_sources-residue pattern explicitly re-checked after the re-run"
    - "Every recorded tranche-2 hole (MA FY2014/FY2021, CT FY2006, per-state window floors) matches what is actually missing in the DB and renders honestly"
  artifacts:
    - path: "scripts/verify-phase110-cohort-audit.mjs"
      provides: "50-node cohort source-chain audit adapted for the v2.13 tranche-2-augmented windows"
      contains: "WINDOW_BOUNDS"
    - path: ".planning/phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md"
      provides: "The cohort audit report: per-invariant PASS/FAIL, idempotency result, hole-verdict reconciliation"
  key_links:
    - from: "scripts/verify-phase110-cohort-audit.mjs"
      to: "treasury.budgets + treasury.data_sources"
      via: "read-only invariant queries across all entity_type=state nodes"
      pattern: "budgets"
    - from: "110-COHORT-AUDIT.md hole-verdict section"
      to: "108/109 SUMMARYs + LOADLOGs"
      via: "reconcile recorded holes against actual DB gaps"
      pattern: "hole"
---

<objective>
Run the full 50-node state cohort source-chain audit on the v2.13-augmented cohort (VER-05 parts b+c): assert the Phase 102/106 invariant set (0 NULL / fragile / residue / out-of-window / dup / orphan; every row basis-labelled; un-upgraded NASBO states untouched) over the 19 ACFR-state windows (9 pre-tranche + the 10 new), confirm loader idempotency (0-change re-run, one loader per batch), and reconcile the recorded tranche-2 holes (MA FY2014/FY2021, CT FY2006, per-state floors) against what is actually missing in the DB.

Purpose: Prove the whole cohort's source chain stayed clean and residue-free after the 242-row tranche-2 loads — the Phase 102 7/7-invariant precedent re-run on the larger surface.
Output: `scripts/verify-phase110-cohort-audit.mjs` + `110-COHORT-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-VERIFICATION.md
@scripts/verify-phase106-cohort-audit.mjs

<interfaces>
<!-- verify-phase106-cohort-audit.mjs is the structural template (7 invariants INV-1..INV-7,
     read-only, exit 0/2). Reuse it. The Phase 110 changes are: +10 window bounds, +10 ACFR
     states in INV-6, a NEW NASBO control (GA is no longer NASBO), and the WR-05 residue re-check. -->

WINDOW_BOUNDS must ADD the 10 tranche-2 states (bounds = min/max loaded FY; in-window holes are
absent rows and legal — INV-3 only fails rows OUTSIDE bounds):
  NJ 2020–2025 · MA 2007–2025 (holes 2014, 2021) · NC 2014–2025 · GA 2021–2025 · MD 2022–2025
  TN 2009–2025 · CT 2002–2025 (hole 2006) · WI 2002–2025 · WA 2020–2025 · MI 2019–2025
Existing v2.12 bounds unchanged: CA 2008–2025, NY 2003–2024, FL 2021–2024, TX 2015–2024,
MN 2008–2025, OH 2020–2025, VA 2022–2025, PA 2016–2025, IL 2021–2025.
All other NASBO states: FY2023–FY2024.

ACFR-GAAP-labelled set (INV-6) is now 19 states: CA TX NY FL MN OH VA PA IL + NJ MA NC GA MD TN CT WI WA MI.
Each new state's operating+revenue rows must carry ACFR-GAAP provenance ("…State ACFR — General
Fund (FY{fy} actual, GAAP basis)"); 0 NASBO labels remain on any of the 19 nodes.

INV-7 NASBO control: GA was the 106 control but is NOW an ACFR state — pick the control
dynamically from the 31 remaining NASBO states (e.g. CO or AZ): exactly 2 NASBO operating rows,
operating-only, NASBO provenance, untouched.

Row-count expectations (from 108/109 SUMMARYs) to confirm in the report:
  NJ 6+6, MA 17+17, NC 12+12, GA 5+5, MD 4+4, TN 17+17, CT 23+23, WI 24+24, WA 6+6, MI 7+7
  (op+rev; 121+121 new rows total). Cohort: 19 ACFR states + 31 NASBO states (2 op rows each), 0 anomalies.

MI-specific INV extension: all 14 MI rows must have source_date = {FY}-09-30 and
fiscal_year_start_month = 10 (September-30 FY-end, Phase 109 D-03).

Idempotency: re-run ONE representative loader per batch for a single FY — e.g. the NJ loader
(Batch-1) and the MI loader (Batch-2) for FY2025, both op+rev variants — via each loader's
guarded never-overwrite path (treasury_sync_budget_tree; source .env first; main tree only;
NEVER treasury_sync_city_budget — not source-safe). Expect "Loaded 0 rows" / 0 net change.
WR-05 WATCH (v2.12 lesson): the non-atomic data_sources upsert pattern re-created residue
data_sources rows during the 106 idempotency re-runs — after the re-run, explicitly re-query
data_sources for duplicate/residue rows on the re-run states and clean via the source-safe
path if any appear, then re-run the audit script to confirm INV-2 still passes.

Hole-verdict reconciliation: confirm (i) every FY the SUMMARYs say is LOADED is present in the DB;
(ii) every absent FY is absent BY DESIGN per a recorded disposition — in-window holes: MA FY2014 +
FY2021 (`-table` anchor quirks, recoverable follow-up), MA FY2001–2006 (older combined format),
CT FY2006 (scanned PDF), NC FY2012–2013 (older format); window floors: CT/WI pre-2002 (pre-GASB-34
Combined-Statement format), TN pre-2009, MI pre-2019 (unavailable), WA pre-2020 (deferred per
recon), NJ pre-2020, GA pre-2021, MD pre-2022 (recon-locked clean windows) — NOT unrecorded gaps;
(iii) the live node renders each non-contiguous range honestly (per-FY rows, basis label + source
chip, no interpolation). An honest, disclosed hole is a PASS; do NOT re-litigate gaps 108/109
judged unrecoverable.

GA supersede check (F-97-01): GA FY2023 operating must be the ACFR value $59,893,783K at the
same (6eb7dd4a…, 2023, 'operating') key with 0 NASBO rows on the GA node — no orphan competing row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the cohort audit for the tranche-2-augmented windows and run the 7-invariant set</name>
  <read_first>
    - scripts/verify-phase106-cohort-audit.mjs (template — INV-1..INV-7, WINDOW_BOUNDS, ACFR_STATES sets, read-only, exit 0/2)
    - .planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-VERIFICATION.md (Batch-2 windows, row counts, 19-ACFR/31-NASBO cohort shape, MI Sep-30 rule)
    - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-02-SUMMARY.md and 108-03-SUMMARY.md (MA/NC exact windows + in-window holes)
  </read_first>
  <action>
    Create scripts/verify-phase110-cohort-audit.mjs from verify-phase106-cohort-audit.mjs (reuse its createClient env-load, the 7 invariants, and the exit-0/2 contract). Apply these changes:

    1. ADD the 10 tranche-2 WINDOW_BOUNDS from the interfaces block (NJ 2020–2025 … MI 2019–2025); keep the 9 existing ACFR bounds and the NASBO 2023–2024 default unchanged.
    2. ADD the 10 new states to the ACFR-GAAP set so INV-6 covers all 19 states' operating+revenue rows (ACFR-GAAP label present, 0 NASBO labels).
    3. REPLACE the GA NASBO control in INV-7 with a dynamic control from the 31 remaining NASBO states (e.g. CO): exactly 2 NASBO operating rows, operating-only, untouched.
    4. ADD the MI Sep-30 check: all 14 MI rows have source_date = {FY}-09-30 and fiscal_year_start_month = 10.
    5. ADD the GA supersede assertion: GA FY2023 operating = $59,893,783,000 with 0 NASBO-labelled rows on the GA node.
    6. Keep INV-1 (every state row has data_source + source_url + source_date), INV-2 (0 residue stale *-gf-* data_sources), INV-3 (0 out-of-window FYs against the updated bounds), INV-4 (0 dup (muni,fy,dataset) combos), INV-5 (0 orphan data_source_id).

    Run it read-only against the live treasury schema. The script must exit 0 only if all invariants PASS. If any invariant FAILs, that is a source-chain defect — fix in-phase (re-run the guarded loader / correct the data_sources row via the source-safe path, NEVER treasury_sync_city_budget) before sign-off, then re-run to confirm. Capture the per-invariant output for the report.
  </action>
  <verify>
    <automated>node scripts/verify-phase110-cohort-audit.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase110-cohort-audit.mjs exists; makes no writes (read-only).
    - WINDOW_BOUNDS contains all 10 new entries (NJ 2020-2025, MA 2007-2025, NC 2014-2025, GA 2021-2025, MD 2022-2025, TN 2009-2025, CT 2002-2025, WI 2002-2025, WA 2020-2025, MI 2019-2025) plus the 9 existing ACFR bounds.
    - INV-6 set includes all 19 ACFR states; INV-7 uses a non-GA NASBO control.
    - Row counts confirmed: NJ 6+6, MA 17+17, NC 12+12, GA 5+5, MD 4+4, TN 17+17, CT 23+23, WI 24+24, WA 6+6, MI 7+7; 31 NASBO states with exactly 2 operating rows each.
    - MI Sep-30 check and GA FY2023 supersede check both present and passing.
    - All invariants print PASS and the script exits 0 (or any failure is fixed-in-phase via the source-safe path and the re-run exits 0).
  </acceptance_criteria>
  <done>The 7-invariant cohort audit (plus MI/GA extensions) runs read-only over the 50-node cohort and exits 0; any defect found is fixed in-phase and re-confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm idempotency (with WR-05 residue re-check), reconcile the hole verdict, and write 110-COHORT-AUDIT.md</name>
  <read_first>
    - .planning/phases/108-acfr-upgrade-batch-1-recon-08-acfr-09-13-acfr-19-acfr-20/108-01-NJ-LOADLOG.md (NJ loader invocation + idempotency precedent)
    - .planning/phases/109-acfr-upgrade-batch-2-recon-08-acfr-14-18-acfr-19-acfr-20/109-05-MI-LOADLOG.md (MI loader invocation + Sep-30 handling)
    - .planning/milestones/v2.12-phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-COHORT-AUDIT.md (report format + the WR-05 residue incident record)
    - The Task 1 audit output
  </read_first>
  <action>
    (a) IDEMPOTENCY: re-run one representative loader per batch — the NJ loaders (op+rev) for FY2025 and the MI loaders (op+rev) for FY2025 — using each loader's existing guarded never-overwrite path (source the .env first; main tree only; NEVER treasury_sync_city_budget). Confirm each reports "Loaded 0 rows" / 0 net change. THEN re-query treasury.data_sources for duplicate/residue rows on NJ + MI (the WR-05 non-atomic-upsert pattern re-created residue rows during the 106 re-runs) — if any appear, clean via the source-safe path and re-run the Task 1 audit to confirm INV-2 passes. Record both results. A non-zero budgets change is a never-overwrite defect → fix in-phase before sign-off.

    (b) HOLE-VERDICT RECONCILIATION: query the DB for the actual present FYs per tranche-2 state and diff against the 108/109 SUMMARYs. Confirm: (i) every FY recorded as LOADED is present (121 op + 121 rev rows); (ii) every absent FY is absent BY DESIGN per a recorded disposition (MA FY2014/FY2021 + FY2001-2006, CT FY2006, NC FY2012-2013, CT/WI pre-2002 pre-GASB-34, TN pre-2009, MI pre-2019, WA pre-2020, NJ pre-2020, GA pre-2021, MD pre-2022) — NOT an unrecorded gap; (iii) the overall per-state range renders honestly (per-FY rows, basis label + source chip make the window self-evident — no interpolation, no implied continuity across a hole). An honest, disclosed hole is a PASS.

    (c) WRITE 110-COHORT-AUDIT.md: headline verdict (N/N invariants), the per-invariant PASS/FAIL table with row counts (all 10 states + the 9 pre-tranche states + the NASBO control), the idempotency result including the explicit WR-05 residue re-check outcome, and the hole-reconciliation section listing the deferred-by-design FYs so they are not re-litigated. This file + 110-REDERIVATION.md are the two inputs the UAT plan and the gsd-verifier read for VER-05.
  </action>
  <verify>
    <automated>node scripts/verify-phase110-cohort-audit.mjs; test -f .planning/phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md && grep -qiE "idempoten|hole|invariant" .planning/phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-COHORT-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 110-COHORT-AUDIT.md exists with: per-invariant PASS/FAIL table, row-count confirmations (121+121 tranche-2 rows), idempotency result ("Loaded 0 rows" for NJ + MI FY2025 re-runs), the explicit WR-05 data_sources residue re-check outcome, and the hole-verdict reconciliation.
    - Idempotency re-runs produced 0 net budgets change AND 0 surviving data_sources residue (or defects were fixed in-phase via the source-safe path and re-confirmed).
    - The hole reconciliation confirms 0 unrecorded DB gaps; all absent FYs map to recorded dispositions.
    - The audit script exits 0.
  </acceptance_criteria>
  <done>110-COHORT-AUDIT.md records all invariants clean, idempotency 0-change with WR-05 re-checked, and the hole verdict reconciled against the actual DB; defects (if any) fixed in-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → treasury schema (read) | Read-only invariant queries via service key (env-only). The audit makes NO writes. |
| idempotency re-run / in-phase fix → treasury (write) | Loader re-runs use the guarded never-overwrite RPC; the source-unsafe RPC must be avoided. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-04 | Tampering | idempotency re-run / in-phase fix writes | mitigate | All writes go through the loader's guarded never-overwrite path (treasury_sync_budget_tree) — NEVER treasury_sync_city_budget (project memory: not source-safe). The audit re-run confirms non-destructive. |
| T-110-05 | Tampering | WR-05 non-atomic data_sources upsert residue | mitigate | Explicit post-re-run data_sources residue query + source-safe cleanup + audit re-run (the v2.12 lesson, now a mandatory step instead of an incident). |
| T-110-06 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Read from env only; never inlined in script or report. Standing practice. |
| T-110-07 | Repudiation | audit verdict honesty | mitigate | The audit script exits 2 on any failing invariant (cannot silently "pass"); the report records the raw per-invariant output; idempotency re-runs are observable ("Loaded 0 rows"). |
| T-110-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses existing deps. No install task. |

Read-only audit; write paths are the idempotency re-run and conditional in-phase fixes, both gated to the source-safe never-overwrite RPC.
</threat_model>

<verification>
- scripts/verify-phase110-cohort-audit.mjs exits 0 with all invariants PASS over the tranche-2-augmented windows.
- Idempotency re-run of representative NJ + MI loaders reports 0 net change; WR-05 residue re-check clean.
- 110-COHORT-AUDIT.md reconciles the hole verdict against the actual DB (0 unrecorded gaps).
</verification>

<success_criteria>
- VER-05 parts (b)+(c): full 50-node cohort source-chain audit clean (0 NULL/fragile/residue/out-of-window/dup/orphan), every displayed row basis-labelled, un-upgraded NASBO states still pass; idempotency + hole-verdict confirmed.
</success_criteria>

<output>
Create `.planning/phases/110-verification-source-chain-audit-uat-ver-05-ver-06/110-02-SUMMARY.md` when done.
</output>
