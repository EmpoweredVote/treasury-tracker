---
phase: 107-recon-acfr-source-location-roster-lock-overlap-resolution-re
plan: "107-03"
type: execute
wave: 2
depends_on: ["107-01", "107-02"]
files_modified:
  - .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md
autonomous: true
requirements: [RECON-06, RECON-07]
user_setup: []

must_haves:
  truths:
    - "Prior-load overlaps are resolved on paper: MA (v1.8 DLS node) is flagged for in-place upgrade with no duplicate MA node (Phase 98 CA precedent); GA (non-cohort NASBO + v2.10 F-97-01 Medicaid fix) is verified to have a clean ACFR supersede plan; any other pre-existing custom-source state node among the roster is identified"
    - "A read-only DB probe confirms which of the 10 roster states carry a non-NASBO/custom-source state node, and confirms the existing 9 ACFR nodes (MN/OH/VA/CA/TX/NY/FL/PA/IL) will be left undisturbed (the RECON-08 contract Phases 108/109 inherit — noted, not executed)"
    - "The final ~8-10-state roster is LOCKED, drawn only from the named 10, with each surviving state's clean window recorded; ≤2 non-cleanly-extracting states deferred to ACFRX-02; if >2 fail the tranche count floats down with no backfill (D-01)"
    - "The 108/109 batch split is LOCKED by GF size — Batch 1 (Phase 108) = surviving {NJ,MA,NC,GA,MD}; Batch 2 (Phase 109) = surviving {TN,CT,WI,WA,MI}; rebalanced around survivors if the roster shrinks (D-03)"
    - "107-RECON.md consolidates both batch SOURCES docs into the decision-ready Phase 108/109 handoff mirroring the Phase 98/103 RECON shape, with $0 spend and no-DB-write confirmed"
  artifacts:
    - path: ".planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md"
      provides: "Consolidated recon handoff: locked roster, locked batch split, overlap resolutions (MA/GA/other), RECON-08 untouched-nodes contract, per-state summary + loader mapping, open risks for Phases 108/109"
      contains: "Roster Lock"
  key_links:
    - from: "107-RECON.md locked roster + batch split"
      to: "Phase 108 + Phase 109 loads"
      via: "per-state window + template mapping + overlap-resolution plan"
      pattern: "Batch 1|Batch 2"
    - from: "107-RECON.md RECON-08 untouched-nodes contract"
      to: "the existing 9 ACFR nodes"
      via: "read-only DB confirmation that only roster states are touched"
      pattern: "MN.*OH.*VA.*CA.*TX.*NY.*FL.*PA.*IL"
---

<objective>
Close Phase 107 by resolving prior-load overlaps (RECON-07), locking the final roster + batch split (RECON-06), and consolidating both batch SOURCES docs into the decision-ready Phase 108/109 handoff. Uses a **read-only** DB probe to confirm which roster states carry a non-NASBO custom-source node (MA v1.8 DLS, GA F-97-01 Medicaid fix, and any others) and to confirm the existing 9 ACFR nodes will be left undisturbed.

Purpose: The overlap-resolution plan + locked roster + locked batch split are the final input contract Phases 108/109 inherit. **Documentation + read-only DB queries only — no DB writes, no NASBO mutations, no loader code, no frontend, $0.**
Output: `.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md` — the consolidated handoff mirroring the Phase 98/103 RECON shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-CONTEXT.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md
@.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md
@.planning/milestones/v2.11-phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md
@.planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-RECON.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase (read-only) → recon doc | read-only `execute_sql` results transcribed into local markdown |

This is a documentation + read-only-query plan: it runs read-only `SELECT` probes against `treasury.municipalities` / `treasury.budgets` / `treasury.data_sources` and writes local markdown. **No DB writes, no schema changes, no NASBO mutations, no secrets, no frontend.** The honest threat surface is minimal.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-04 | Tampering | an accidental non-SELECT statement mutates the DB during the overlap probe | mitigate | Use only read-only `SELECT` queries via the Supabase read path; no INSERT/UPDATE/DELETE/DDL in this plan; the whole phase is documentation-only per CONTEXT boundary. |
| T-107-05 | Information disclosure / Repudiation | mis-identifying which nodes are ACFR vs NASBO leads Phases 108/109 to disturb one of the existing 9 ACFR nodes | mitigate | Enumerate the 9 existing ACFR nodes (MN/OH/VA/CA/TX/NY/FL/PA/IL) explicitly from the DB and record the RECON-08 untouched-nodes contract; confirm the roster states are distinct from those 9. |

No package installs → no supply-chain checkpoint needed.
</threat_model>

<tasks>

<task id="107-03-01" type="auto">
  <name>Task 1: Read-only overlap probe — MA/GA/other custom nodes + RECON-08 untouched-nodes contract</name>
  <files>.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md</files>
  <read_first>
- .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-CONTEXT.md (D-10 overlaps = flag + plan not execute; RECON-07/RECON-08 scope)
- .planning/milestones/v2.11-phases/98-recon-ca-overlap-4-state-acfr-source-location-recon-01-recon/98-RECON.md (the CA in-place-upgrade overlap model + the MA v1.8 dual-node check precedent — the exact query approach + verdict shape to reuse)
- .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (MA ACFR source + GA Medicaid-fix supersede flag from 107-01)
  </read_first>
  <action>
Run **read-only** `SELECT` probes (no writes) against `treasury.municipalities` + `treasury.budgets` + `treasury.data_sources` for the 10 roster states, mirroring the 98-RECON MA/CA dual-node check. For EACH roster state's state node record: which `budgets` rows exist (dataset_type/fiscal_year/total_budget/data_source), and which `data_sources` metadata rows exist (dataset_id/api_type/dataset_type/fiscal_years) and whether each maps to live budgets rows. Then resolve the three RECON-07 items and write them into a `## Overlap Resolution (RECON-07)` section of `107-RECON.md`: (1) **Massachusetts** — confirm the current MA state node's rows (expect the two NASBO operating rows FY2023/FY2024 per 98-RECON) and identify any surviving v1.8 DLS custom-source `data_sources`/budgets rows; recommend the **in-place upgrade** (upgrade the single MA state node, no duplicate node — Phase 98 CA precedent) and list the concrete Phase-108 steps (add ACFR rev+operating rows, replace NASBO operating FY the ACFR covers, delete any stale v1.8 DLS `data_sources`, create a fresh `ma-acfr-*` data_source). (2) **Georgia** — confirm GA's current NASBO rows carry the v2.10 F-97-01 Medicaid fix and record how the ACFR operating replace supersedes it cleanly (the ACFR GAAP row replaces the fixed NASBO row keyed on (muni,fy,'operating') → the fix is moot once ACFR wins that state-FY); flag for confirmation at Phase-108 load. (3) **Any other pre-existing custom-source node** among the remaining 8 roster states — identify from the probe (a `data_sources` row whose `api_type` is not `nasbo-ser`, or a non-NASBO `data_source` string on budgets rows) and note the in-place-upgrade requirement, else record "none found". Finally, enumerate the existing **9 ACFR nodes** (MN/OH/VA/CA/TX/NY/FL/PA/IL) from the DB and write the **RECON-08 untouched-nodes contract**: Phases 108/109 touch ONLY the roster states; the 9 ACFR nodes + all un-upgraded NASBO states stay undisturbed (noted, not executed here).
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md -Pattern "Overlap Resolution|Massachusetts|Georgia|RECON-08" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- `107-RECON.md` has an `## Overlap Resolution (RECON-07)` section with the MA in-place-upgrade recommendation (no duplicate node) + concrete Phase-108 steps, backed by the read-only DB probe of the MA state node
- GA's F-97-01 Medicaid-fix supersede is verified on paper (ACFR replace keyed on (muni,fy,'operating') supersedes the fixed NASBO row) and flagged for Phase-108 confirmation
- Any other roster state with a pre-existing custom-source node is identified from the probe, or "none found" is recorded
- The 9 existing ACFR nodes are enumerated from the DB and the RECON-08 untouched-nodes contract is written (roster states only; 9 ACFR nodes + un-upgraded NASBO states undisturbed)
- Only read-only SELECT queries were run — no DB writes
  </acceptance_criteria>
  <done>Overlap resolution complete: MA in-place upgrade recommended, GA supersede verified, other custom nodes identified/none, RECON-08 untouched-nodes contract recorded — all from a read-only probe.</done>
</task>

<task id="107-03-02" type="auto">
  <name>Task 2: Lock the roster + batch split; write the consolidated Phase 108/109 handoff</name>
  <files>.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md</files>
  <read_first>
- .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-CONTEXT.md (D-01 ship-what-survives no-backfill, D-02 no-min-depth, D-03 batch split lock, D-07 recency floor)
- .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH1-SOURCES.md (NJ/MA/NC/GA/MD findings + gap logs)
- .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-BATCH2-SOURCES.md (TN/CT/WI/WA/MI findings + gap logs)
- .planning/milestones/v2.12-phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-RECON.md (the consolidated RECON handoff shape to mirror: per-state table, loader-reuse plan, NASBO-replace rule, greenlights, open risks, success-criteria coverage)
  </read_first>
  <action>
Consolidate both batch SOURCES docs into the decision-ready Phase 108/109 handoff, mirroring the `103-RECON.md` shape. Write these sections into `107-RECON.md`: (1) `## Roster Lock (RECON-06, D-01/D-02)` — for each of the 10 candidates decide IN or DEFERRED: a state is IN if it cleanly `pdftotext -table`-extracts AND clears the recency floor (FY2023+FY2024 covered, D-07); DEFERRED-to-ACFRX-02 if it can't cleanly extract at all OR fails the recency floor. Apply D-01: draw only from the named 10, no backfill to an 11th state; if ≤2 fail defer them, if >2 fail let the count float down. Record the locked roster as a table (state | IN/DEFERRED | clean window | reason if deferred). (2) `## Batch Split Lock (D-03)` — Batch 1 (Phase 108) = surviving {NJ,MA,NC,GA,MD}; Batch 2 (Phase 109) = surviving {TN,CT,WI,WA,MI}, size-order preserved, rebalanced around survivors if the roster shrank; record the final per-batch assignment. (3) `## Per-state summary + loader mapping` — a consolidated table (state | statement/GF column | units | FY-end | durable window | latest tie | old-end tie | scope-vs-NASBO magnitude + accept-relabel rec | closest loader template) pulled from the two batch docs. (4) `## NASBO-replace rule (RECON-08)` — restate: ACFR rows replace NASBO operating per state-FY keyed on (muni,fy,dataset) idempotent never-overwrite; revenue is a pure insert (new dataset); un-upgraded states stay on loadStateGF.mjs; the 9 ACFR nodes undisturbed. (5) `## Recency-floor verdicts (D-07)` — per-state greenlight/strand-flag. (6) `## Open Risks for 108/109` — carry forward scope-relabel confirmations (D-09), any negative-category P2-clamp anticipations (D-08.2), units traps (NY-style ×millions), soft-404 cautions, and any strand-flagged states. (7) `## Success-criteria coverage` — map back to Phase 107's 4 ROADMAP success criteria + RECON-06/RECON-07. Confirm $0 spend and no-DB-write at the top of the doc.
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-RECON.md -Pattern "Roster Lock|Batch Split Lock|NASBO-replace|Open Risks|Success-criteria" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <acceptance_criteria>
- `107-RECON.md` has a locked-roster table (each of the 10 marked IN or DEFERRED with clean window / deferral reason) honoring D-01 no-backfill + D-02 no-min-depth + D-07 recency floor
- The 108/109 batch split is locked (Batch 1 = surviving NJ/MA/NC/GA/MD; Batch 2 = surviving TN/CT/WI/WA/MI) per D-03, rebalanced if the roster shrank
- A consolidated per-state summary + loader-template mapping table is present, pulled from both batch SOURCES docs
- The RECON-08 NASBO-replace rule, recency-floor verdicts, open risks (scope-relabel/P2-clamp/units/soft-404/strand flags), and success-criteria coverage are all recorded
- Top of doc confirms $0 spend + no DB writes; the doc mirrors the 103-RECON handoff shape
  </acceptance_criteria>
  <done>Roster + batch split locked; consolidated 107-RECON.md handoff complete and decision-ready for Phases 108/109; Phase 107's 4 success criteria + RECON-06/07 covered; documentation-only.</done>
</task>

</tasks>

<verification>
- `107-RECON.md` resolves all three RECON-07 overlap items (MA in-place upgrade, GA clean supersede, other-custom-node identification) from a read-only DB probe, and records the RECON-08 untouched-nodes contract for the 9 existing ACFR nodes.
- The final roster is locked (IN/DEFERRED per state, D-01 no-backfill, D-02 no-min-depth, D-07 recency floor) and the 108/109 batch split is locked per D-03.
- The consolidated handoff mirrors the 103-RECON shape: per-state summary + loader mapping, NASBO-replace rule, recency-floor verdicts, open risks, success-criteria coverage.
- No DB writes (read-only SELECT only), no loader edits, no NASBO mutations, no frontend changes; $0 spend.
</verification>

<success_criteria>
Phase 107 is closed: overlaps resolved on paper, roster + batch split locked, and a decision-ready 107-RECON.md handoff produced for Phases 108/109 — all documentation-only with a read-only DB probe, $0 spend.
</success_criteria>

<output>
Create `.planning/phases/107-recon-acfr-source-location-roster-lock-overlap-resolution-re/107-03-SUMMARY.md` when done.
</output>
