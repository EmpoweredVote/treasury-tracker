---
phase: 51
plan: "51-03"
title: "Comparability notes UI — render sourced drift + TQ explanation on historical views"
wave: 2
depends_on: ["51-02"]
files_modified:
  - src/components/federal/ComparabilityNote.tsx
  - src/components/federal/FederalLanding.tsx
  - src/App.tsx
  - src/data/dataLoader.ts
  - src/types/budget.ts
autonomous: true
requirements: [CTX-02]
must_haves:
  - "A ComparabilityNote component renders the function/agency definition-drift notes on historical-year federal views"
  - "The FY1976 Transition Quarter view shows the sourced TQ explanation (replacing the neutral Phase 50 heading)"
  - "Each note carries a SourceChip linking to its official source (human page)"
  - "FY2025 default view stays clean; no regression; tsc + build green"
---

# Plan 51-03 — Comparability notes UI

**Mode:** standard · **Depends on:** 51-02 (the sourced content file)
**Goal:** Surface the sourced comparability content in-app (CTX-02 criterion 1): drift notes on
historical years, the TQ explanation on the Transition Quarter view — each with a source chip.

<threat_model>
**T-51-03 — Notes render unsourced or on the wrong views (clutter / wrong context).**
- *Mitigations:* notes are data-driven from `federal-comparability.json` (every line already sourced + chipped); gate drift notes to historical-year federal views (suppress on the FY2025 default to keep the headline clean); TQ note only on the TQ period; build/tsc + observed UAT.
- *Block on:* medium.
</threat_model>

## Tasks

<task id="51-03-01" type="execute">
<action>
Expose the comparability content to the frontend: serve `data/federal-comparability.json` (static import in the frontend, or via `loadFederalContext` if it should ride the existing payload — planner/executor choice; static import is simplest and $0). Add a `ComparabilityContent` type to `src/types/budget.ts`. Create `src/components/federal/ComparabilityNote.tsx` — a compact, expandable note that renders a title + sourced lines, each line with a `SourceChip` (sourceName/sourceUrl/fetchDate from the content entry). Mirror `MethodologyPanel.tsx` styling.
</action>
<read_first>
- src/components/federal/MethodologyPanel.tsx (styling + SourceChip usage analog)
- src/components/federal/SourceChip.tsx (props)
- data/federal-comparability.json (content shape from 51-02)
- src/types/budget.ts (where to add the type)
</read_first>
<acceptance_criteria>
- `ComparabilityNote.tsx` exists; renders title + sourced lines with SourceChips
- Content type added; content imported without a network/API change (or via context if chosen)
- `npm run build` green
</acceptance_criteria>
</task>

<task id="51-03-02" type="execute">
<action>
Wire rendering in the federal view: (1) on the **Transition Quarter** view, render the TQ explanation note in `FederalLanding.tsx` (where Phase 50 left a neutral heading); (2) on **historical annual years** (not the FY2025 default), render the function/agency definition-drift note near the lens view (App.tsx federal branch). Pass `isCurrent`/period info already available from Phase 50 (`parsePeriod`, `federalDenominators`/`isCurrent`). Keep the FY2025 default free of the drift note (or a single subtle line — planner's call).
</action>
<read_first>
- src/components/federal/FederalLanding.tsx (TQ heading branch from Phase 50)
- src/App.tsx (federal render region; parsePeriod / isCurrent; where the lens tree + ScaleToggle render ~876)
- src/utils/period.ts (parsePeriod for period detection)
</read_first>
<acceptance_criteria>
- TQ view shows the sourced transition-quarter explanation (with source chip)
- Historical annual years show the drift note; FY2025 default does not (or only a minimal line)
- No regression to FY2025 default / city / state; `npm run build` green
</acceptance_criteria>
</task>

## Verification
- Drift notes on historical years + TQ explanation on the TQ view, each sourced via SourceChip; default view clean; build green.
