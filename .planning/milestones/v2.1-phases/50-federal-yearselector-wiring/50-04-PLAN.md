---
phase: 50
plan: "50-04"
title: "Integration verification: year switching, TQ, source chips, no regression"
wave: 3
depends_on: ["50-01", "50-02", "50-03"]
files_modified: []
autonomous: false
requirements: [NAV-01, NAV-02]
must_haves:
  - "Every loaded federal period (FY1976–FY2025 + TQ) is selectable and switches all three lens trees"
  - "Landing bands + deficit strip + source chips reflect the selected year"
  - "FY1976 shows the real FY1976 (not TQ); the TQ entry shows TQ figures"
  - "No regression: FY2025 default federal view + a city + a state still work"
---

# Plan 50-04 — Integration verification + regression

**Mode:** standard · **Depends on:** 50-01/02/03 (+ backend deployed or local per 50-01-03)
**Goal:** Prove NAV-01/NAV-02 end-to-end and that nothing regressed, against the running app
(backend serving `period_label`).

<threat_model>
**T-50-04 — A "green build" hides a runtime regression for non-federal entities or the default view.**
- *Mitigations:* explicit observed checks across federal (multiple years + TQ), a city, and a state; confirm the FY2025 default is pixel-stable; verify the source chip updates per year.
- *Block on:* medium.
</threat_model>

## Tasks

<task id="50-04-01" type="execute">
<action>
Contract probe against the test target (deployed or local API from 50-01-03): confirm `/treasury/cities` US entity `available_datasets` rows carry `period_label` (one TQ row), and `/cities/:US/budgets?fiscal_year=1976` returns rows with `period_label` (real FY1976 = null, TQ = the label string). Record the JSON evidence.
</action>
<read_first>
- src/data/dataLoader.ts (API_BASE resolution)
- .planning/phases/50-federal-yearselector-wiring/50-01-PLAN.md (deploy/local decision)
</read_first>
<acceptance_criteria>
- API responses include period_label as specified (null for annual, TQ string for the TQ row)
- FY1976 operating rows: exactly one null-label + one TQ-label
</acceptance_criteria>
</task>

<task id="50-04-02" type="checkpoint" autonomous="false">
<action>
**Observed UAT with Chris (run the app — `npm run dev`).** Walk the federal view: (1) the YearSelector lists FY1976–FY2025 + a distinct "Transition Quarter" entry after FY1976; (2) switching among several years (e.g. FY2024, FY2000, FY1976) updates function, agency, and revenue trees; (3) the landing bands + deficit strip change with the year; (4) the source chip reflects the selected year's source; (5) selecting FY1976 shows the real FY1976 (~$397B operating) and the TQ entry shows the TQ (~$102B operating); (6) the FYTD strip appears only on the FY2025 default; (7) the TQ view hides the annual-summary bands. **Regression:** switch to a city and a state — year switching + default view still work; FY2025 federal default is unchanged. Capture screenshots.
</action>
<read_first>
- .planning/phases/50-federal-yearselector-wiring/50-RESEARCH.md (Validation Architecture → Behavior)
- .planning/phases/50-federal-yearselector-wiring/50-CONTEXT.md (success criteria + decisions)
</read_first>
<acceptance_criteria>
- All 7 federal behaviors confirmed; FY1976 vs TQ distinct and correct
- City + state + FY2025-default regression checks pass (Chris sign-off)
</acceptance_criteria>
</task>

## Verification
- NAV-01 (every period selectable, trees update) + NAV-02 (bands/deficit strip reflect year) confirmed live; no regression.
