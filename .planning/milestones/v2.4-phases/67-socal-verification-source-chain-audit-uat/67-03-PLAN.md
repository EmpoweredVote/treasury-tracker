---
phase: 67-socal-verification-source-chain-audit-uat
plan: "67-03"
type: execute
wave: 2
depends_on: ["67-01", "67-02"]
files_modified: []
autonomous: false
requirements: [VER-06]
must_haves:
  truths:
    - "A concrete, ordered guided UAT checklist exists covering every VER-06 item — FY2003 history depth, salaries dataset/tab, per-capita across backfilled years, enrichment rendering, breadcrumb chain (US → California → County → city), Cities-in-County panel — across a representative SoCal spread including a county created this milestone (D-06, D-07)"
    - "Chris drives the live app at treasurytracker.empowered.vote and reports per-item results; the agent records only what he reports (no browser automation) (D-06)"
    - "Chris's UAT sign-off is recorded at a blocking checkpoint before the phase can close (SC#4); a fail surfaces the specific item as a documented follow-up (D-08)"
  artifacts:
    - path: ".planning/phases/67-socal-verification-source-chain-audit-uat/67-03-UAT-CHECKLIST.md"
      provides: "The guided click-through checklist Chris walks against the live app (Task 1 artifact)"
      contains: "ordered navigation steps + expected results per VER-06 item, grouped by entity, across the SoCal spread; each UAT pick pre-verified to have the data via a read-only probe"
    - path: ".planning/phases/67-socal-verification-source-chain-audit-uat/67-03-SUMMARY.md"
      provides: "The recorded UAT result + Chris's sign-off — the VER-06 evidence record"
      contains: "per-item PASS/FAIL/PARTIAL as Chris reported, the sign-off decision, any documented follow-ups"
  key_links:
    - from: "the SoCal expansion rendered in the live app (treasurytracker.empowered.vote)"
      to: "Chris's recorded UAT sign-off at a blocking checkpoint"
      via: "guided checklist; Chris drives the app and reports per-item; agent records results + sign-off"
      pattern: "build checklist (data pre-verified) → Chris walks it → record results + sign-off at a blocking checkpoint"
---

<objective>
Satisfy VER-06: verify the SoCal expansion end-to-end in the live app and record Chris's UAT sign-off at a
blocking checkpoint. Produce a concrete guided checklist (Chris drives treasurytracker.empowered.vote),
record per-item results, and capture the sign-off. Runs after 67-01 (ACFR) + 67-02 (source-chain) so Chris
reviews already-audited data. Read-only; no browser automation; $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-03-PLAN.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-03-UAT-CHECKLIST.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- App URL: https://treasurytracker.empowered.vote ([[feedback_app_url]]). Chris drives the browser; the
     agent does NOT automate it. Read-only DB probes only to PRE-VERIFY each UAT pick has the data
     (the Phase 62 Inglewood lesson: a city with no budget rows correctly shows no Salaries tab).
     DatasetTabs.tsx gates the Salaries card on availableDatasets.includes('salaries'). -->

VER-06 items (D-07): FY2003 history depth | salaries dataset/tab | per-capita across backfilled years | enrichment (plain category names) | breadcrumb chain (US → California → County → city) | Cities-in-County panel.
Suggested SoCal spread: Riverside (city — FY2003 + per-capita + enrichment), Ventura County (county-gov page — icicle/summary + per-capita + Cities-in-County + breadcrumb), Oxnard (Ventura city — Salaries tab + breadcrumb into a county created this milestone), El Centro (Imperial — smallest cohort). Verify each pick has the data first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the guided UAT checklist (concrete, ordered, SoCal entity-spread; data pre-verified)</name>
  <files>.planning/phases/67-socal-verification-source-chain-audit-uat/67-03-UAT-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md (D-06, D-07)
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-03-UAT-CHECKLIST.md (format to mirror)
  </read_first>
  <action>
    Source .env. Pick the SoCal entity spread (D-07) and, with READ-ONLY probes, PRE-VERIFY each pick has
    the data it will be checked for (op/rev reaching FY2003, salaries rows FY2009–2024, population>0,
    county_id linked, enrichment present) — so the checklist never asks Chris to confirm something the data
    can't show (the Phase 62 Inglewood correction). Then write 67-03-UAT-CHECKLIST.md: an ordered
    click-through against treasurytracker.empowered.vote, grouped by entity, covering every VER-06 item —
    FY2003 history depth, salaries dataset/tab, per-capita across backfilled years, enrichment (plain
    category names), breadcrumb chain (US → California → County → city), Cities-in-County panel — with a
    concrete navigation step + expected result per row. Include a county CREATED this milestone (Ventura /
    Imperial) to exercise the new breadcrumb + Cities-in-County panel. Note expected reference values where
    helpful (e.g. an FY2024 total) from the loaded data.
  </action>
  <acceptance_criteria>
    - 67-03-UAT-CHECKLIST.md covers all 6 VER-06 items across a ≥3-entity SoCal spread including ≥1 county created this milestone; each item has a concrete navigation step + expected result.
    - Every UAT pick is pre-verified read-only to actually have the data being checked.
  </acceptance_criteria>
  <done>A concrete, data-verified guided UAT checklist exists, ready for Chris to walk.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Chris walks the checklist in the live app and signs off (BLOCKING)</name>
  <context>
    VER-06 requires Chris's recorded UAT sign-off in the live app. This blocking checkpoint presents the
    67-03-UAT-CHECKLIST.md for Chris to walk against treasurytracker.empowered.vote. Chris drives the
    browser and reports each item PASS / FAIL / PARTIAL; the agent records only what he reports (no browser
    automation). The phase cannot close until sign-off is recorded.
  </context>
  <decision>
    Present the checklist and ask Chris to walk it and report results. Then record his decision:
  </decision>
  <options>
    <option>
      <name>Sign off — all checklist items pass</name>
      <effect>VER-06 satisfied; record PASS for all items + the sign-off in 67-03-SUMMARY. Proceed to Task 3.</effect>
    </option>
    <option>
      <name>Sign off with documented follow-up flags — most pass, specific items flagged</name>
      <effect>VER-06 satisfied with caveats; record per-item results, the flagged items as documented follow-ups (D-08, fixed in a later phase/issue), and the conditional sign-off. Proceed to Task 3.</effect>
    </option>
    <option>
      <name>Do not sign off — a blocking defect makes the SoCal expansion unusable</name>
      <effect>VER-06 NOT satisfied; record the blocking defect. Do NOT mark the phase complete; the defect becomes a new fix phase/issue before re-running UAT.</effect>
    </option>
  </options>
  <acceptance_criteria>
    - Chris's per-item results and sign-off decision are recorded exactly as he reported them (no fabricated PASS).
    - On "do not sign off", the phase is not closed and the blocking defect is documented.
  </acceptance_criteria>
  <done>Chris's UAT sign-off (or non-sign-off) is recorded at the blocking checkpoint.</done>
</task>

<task type="auto">
  <name>Task 3: Record the UAT result + write 67-03-SUMMARY (VER-06 evidence)</name>
  <files>.planning/phases/67-socal-verification-source-chain-audit-uat/67-03-SUMMARY.md</files>
  <read_first>
    - The Task 2 recorded results + sign-off
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 67-03-SUMMARY.md: the per-item PASS/FAIL/PARTIAL exactly as Chris reported, the sign-off decision,
    and any documented follow-ups. State VER-06 satisfied (or not, with the blocking defect) per the
    checkpoint outcome. Confirm read-only, $0, no browser automation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/67-socal-verification-source-chain-audit-uat/67-03-SUMMARY.md';const t=fs.readFileSync(p,'utf8');const ok=/VER-06/.test(t)&&/(sign-?off|sign off)/i.test(t);console.log('SUMMARY has VER-06 + sign-off:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - 67-03-SUMMARY.md records the per-item UAT results + Chris's sign-off decision + any follow-ups; VER-06 status stated.
    - Verify probe passes (SUMMARY references VER-06 + sign-off).
  </acceptance_criteria>
  <done>The UAT result + Chris's sign-off are documented as the VER-06 evidence record.</done>
</task>

</tasks>

<threat_model>
| ID | Threat | Vector | Disposition | Mitigation |
|----|--------|--------|-------------|------------|
| T-67-03-A | Repudiation / false PASS | incomplete checklist coverage | mitigate | D-07: checklist must cover all 6 VER-06 items across a ≥3-entity SoCal spread incl. a county created this milestone; Task-1 acceptance criteria enforce coverage + data pre-verification before the checkpoint. |
| T-67-03-B | Spoofing | agent fabricating UAT results | mitigate | D-06 blocking checkpoint: Chris drives the live app and reports per-item; the agent records only what he reports; no browser automation. |
| T-67-03-C | False-negative UAT | a pick lacks the data being checked (e.g. no salaries) → looks like a render bug | mitigate | Task 1 pre-verifies every pick has the data via read-only probe (the Phase 62 Inglewood lesson). |
</threat_model>

<verification>
- Checklist covers all 6 VER-06 items across a ≥3-entity SoCal spread incl. a county created this milestone; each pick data-pre-verified.
- Chris drives the app; sign-off recorded at the blocking checkpoint (D-06); any fail documented as a follow-up (D-08).
- Read-only, $0, no browser automation; 67-03-SUMMARY.md is the VER-06 evidence.
</verification>

<success_criteria>
- VER-06 satisfied: live app verified end-to-end for the SoCal expansion across the required item + entity spread; Chris's sign-off recorded at the blocking checkpoint.
- No DB writes / source changes (D-08); $0 (D-10).
- 67-03-UAT-CHECKLIST.md + 67-03-SUMMARY.md document the result.
</success_criteria>

<output>
Create `.planning/phases/67-socal-verification-source-chain-audit-uat/67-03-UAT-CHECKLIST.md` (Task 1) and `.planning/phases/67-socal-verification-source-chain-audit-uat/67-03-SUMMARY.md` (Task 3) when done.
</output>
