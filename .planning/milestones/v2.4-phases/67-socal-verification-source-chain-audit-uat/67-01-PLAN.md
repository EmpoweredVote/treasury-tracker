---
phase: 67-socal-verification-source-chain-audit-uat
plan: "67-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "A representative sample (≥3 SoCal county governments + ≥3 SoCal cities across ≥3 counties) is reconciled against published ACFRs / adopted budgets on a basis-matched line, for the most recent FY with both an ACFR and a loaded SCO row (D-01, D-03)"
    - "Each reconciliation passes within a documented, explainable tolerance (basis scope / fund inclusion / restatement / timing) — NOT penny-exact; differences are explained, not merely flagged (D-02)"
    - "Read-only only: no DB writes, no source-file changes; production DB; $0 (free ACFR PDFs via WebFetch) (D-08, D-09, D-10)"
  artifacts:
    - path: ".planning/phases/67-socal-verification-source-chain-audit-uat/67-01-SUMMARY.md"
      provides: "The ACFR reconciliation result: per-entity chosen FY, loaded SCO figure, basis-matched ACFR line + source, delta + explanation — the VER-05 part-A evidence record"
      contains: "per-entity table (entity, FY, SCO figure, ACFR line + URL, delta, basis explanation, PASS/EXPLAINED), the tolerance reasoning, any entity that could not be reconciled (documented follow-up)"
  key_links:
    - from: "loaded SCO figures (treasury.budgets.total_budget) for the sample SoCal entities"
      to: "published ACFR / adopted-budget basis-matched lines (official city/county PDFs)"
      via: "read-only DB probe + WebFetch of the official ACFR; basis-matched comparison within a documented tolerance"
      pattern: "probe loaded figures + pick comparable FY → fetch ACFR + reconcile on the all-governmental-funds line → document delta + explanation"
---

<objective>
Satisfy VER-05 part A: reconcile a representative sample of SoCal county governments + cities against
published ACFRs / adopted budgets on a basis-matched comparison, for the most recent fiscal year with
both a published ACFR and a loaded SCO row. Document each delta with an explainable tolerance (not
penny-exact). Read-only, production DB, $0. (The source-chain audit is 67-02; UAT is 67-03.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md
@.planning/phases/64-socal-county-government-budgets/VERIFICATION.md
@.planning/phases/63-socal-county-cities-load-linking/VERIFICATION.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- PRODUCTION DB read-only via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     NEVER mcp__supabase-local. Probe columns: total_budget, source_url, data_source; exact-count head
     queries (avoid 1000-row cap). ACFR PDFs via WebFetch (free). No writes. -->

Basis (D-02): SCO ByTheNumbers = all-governmental-funds. ACFR governmental + proprietary statements are separate — pick the comparable line, same FY, document the basis.
Year (D-03): most recent FY with BOTH a published ACFR AND a loaded SCO row (expect FY2022/2023; FY2024 ACFRs may lag).
Sample (D-01): ≥3 county govs (suggest Riverside, San Diego, Ventura) + ≥3 cities (suggest Riverside, Oxnard, Chula Vista) across ≥3 counties.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe loaded SCO figures + select the basis-matched comparison year per sample entity</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - .planning/phases/67-socal-verification-source-chain-audit-uat/67-CONTEXT.md (D-01..D-03, D-08..D-10)
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md (reconciliation method)
    - .planning/phases/64-socal-county-government-budgets/VERIFICATION.md (county-gov loaded totals)
  </read_first>
  <action>
    Source .env. Choose the sample: ≥3 SoCal county governments + ≥3 cities across ≥3 counties (D-01).
    With a READ-ONLY probe (schema treasury), pull each entity's loaded operating + revenue total_budget
    by fiscal year (use total_budget; exact-count/head where counting). For each entity, identify the
    candidate fiscal years available, to pair against a published ACFR in Task 2. Record per entity: the
    available loaded FYs and the FY2024 (or latest) total as a reference. No writes.
  </action>
  <acceptance_criteria>
    - The sample (≥3 county govs + ≥3 cities, ≥3 counties) is fixed and each entity's loaded op/rev totals by FY are recorded read-only.
    - Candidate reconciliation FYs per entity are identified (recent years likely to have a published ACFR).
  </acceptance_criteria>
  <done>The reconciliation sample + each entity's loaded SCO figures are established read-only, ready to pair with published ACFRs.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch published ACFRs and reconcile each entity on a basis-matched line ($0)</name>
  <files>(no writes)</files>
  <read_first>
    - The Task 1 loaded figures
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md (basis-matching examples)
  </read_first>
  <action>
    For each sample entity, WebFetch the official published ACFR (or adopted budget) for the most recent FY
    that also has a loaded SCO row (D-03). Locate the basis-matched line (D-02): for county govs, total
    expenditures across all governmental funds (+ proprietary where SCO includes them); for cities likewise.
    Compare the loaded SCO total_budget to the ACFR line for the SAME FY. Compute the delta and EXPLAIN it
    (basis scope, fund inclusion, restatement, timing) — the reconciliation passes within a documented,
    explainable tolerance, not penny-exact. Record per entity: chosen FY, SCO figure, ACFR line + page/URL,
    delta (absolute + %), and the explanation. If an entity cannot be reconciled (no ACFR, or an
    unexplainable gap), document it as a follow-up rather than forcing a pass.
  </action>
  <acceptance_criteria>
    - Each sample entity is reconciled to a published ACFR/adopted-budget basis-matched line for a shared FY; delta + explanation recorded; the ACFR source URL captured.
    - Any non-reconcilable entity is documented as a follow-up (not forced to pass); $0 (WebFetch only).
  </acceptance_criteria>
  <done>Every sample entity is reconciled (or explicitly documented as a follow-up) on a basis-matched line within an explained tolerance.</done>
</task>

<task type="auto">
  <name>Task 3: Document the reconciliation + write 67-01-SUMMARY (VER-05 part-A evidence)</name>
  <files>.planning/phases/67-socal-verification-source-chain-audit-uat/67-01-SUMMARY.md</files>
  <read_first>
    - The Task 2 reconciliation results
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 67-01-SUMMARY.md: a per-entity table (entity, type, chosen FY, loaded SCO figure, basis-matched
    ACFR line + URL, delta absolute + %, basis explanation, PASS/EXPLAINED or FOLLOW-UP), plus the overall
    tolerance reasoning and the basis statement (all-governmental-funds vs ACFR fund statements). State
    VER-05 part A satisfied (sample reconciles on a basis-matched comparison within a documented tolerance).
    Note any documented follow-ups. Confirm read-only, $0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/67-socal-verification-source-chain-audit-uat/67-01-SUMMARY.md';const t=fs.readFileSync(p,'utf8');const ok=/VER-05/.test(t)&&/basis/i.test(t)&&(t.match(/PASS|EXPLAINED|FOLLOW-UP/gi)||[]).length>=6;console.log('SUMMARY has VER-05 + basis + >=6 entity verdicts:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - 67-01-SUMMARY.md records ≥6 entities with chosen FY, SCO vs ACFR figures, delta, and explanation; the tolerance reasoning is documented; VER-05 part A stated.
    - Verify probe passes (SUMMARY references VER-05, basis, ≥6 verdicts).
  </acceptance_criteria>
  <done>The ACFR reconciliation is documented as the VER-05 part-A evidence record.</done>
</task>

</tasks>

<verification>
- ≥3 county govs + ≥3 cities reconciled on a basis-matched ACFR line for a shared FY, within a documented explainable tolerance (not penny-exact).
- Differences explained (basis/fund/restatement/timing); any non-reconcilable entity documented as a follow-up.
- Read-only, production DB, $0; 67-01-SUMMARY.md is the VER-05 part-A evidence.
</verification>

<success_criteria>
- VER-05 part A satisfied: representative SoCal sample reconciles against published ACFRs on a basis-matched comparison, documented with explainable tolerance.
- No DB writes / source changes (D-08); production DB (D-09); $0 (D-10).
- 67-01-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/67-socal-verification-source-chain-audit-uat/67-01-SUMMARY.md` when done.
</output>
