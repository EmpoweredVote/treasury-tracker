---
phase: 64-socal-county-government-budgets
plan: "64-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [CGB-01]
must_haves:
  truths:
    - "Each of the 6 SoCal county governments (Riverside, San Bernardino, San Diego, Ventura, Santa Barbara, Imperial) has operating + revenue loaded for FY2003–2024 (where the SCO county feed provides it), all-governmental-funds basis, each row carrying the durable /d/<id> source_url + fetch date (D-02, D-03, D-07)"
    - "Per-year population came from the SCO county feed; each county entity has population > 0 (loaded FY2024 canary first so the stored population is current), so the county page renders per-capita (D-04)"
    - "Each county page renders icicle/summary + per-capita instead of directory-only; city rows under each county are untouched (the county-gov load writes only to the entity_type='county' row) (D-08, D-12)"
    - "Read-only verification + production DB only; $0 spend (free SCO source, no AI) (D-09, D-10)"
  artifacts:
    - path: ".planning/phases/64-socal-county-government-budgets/64-01-SUMMARY.md"
      provides: "The 6-SoCal-county-government budget-load result: per-county op/rev FY reach, row/source counts, population, canary totals vs loaded — the CGB-01 evidence record (SoCal six)"
      contains: "per-county dry-run canary FY2024 totals, loaded op/rev row counts + FY range, NULL source_url count (must be 0), population per county, any No-data FY gaps, per-capita confirmation"
  key_links:
    - from: "SCO ByTheNumbers county datasets (uctr-c2j8 operating / emxv-k8xv revenue), entity_name=<county>"
      to: "treasury.budgets (operating + revenue, FY2003–2024) on the existing entity_type='county' municipality"
      via: "loadCountyBudget.js --county <X> --fy <year> (canary FY2024 first, then backfill FY2003–2023)"
      pattern: "dry-run canary → load canary for real (locks current population) → backfill remaining years with durable source + per-year feed population (never-overwrite)"
---

<objective>
Satisfy CGB-01 (SoCal six): load each of the 6 SoCal county governments' own operating + revenue
budget (FY2003–2024, all-governmental-funds basis) from the SCO ByTheNumbers county datasets onto the
existing county entity — using the hardened Phase 57 tool `loadCountyBudget.js` with ZERO new code, so
each county page renders icicle/summary + per-capita instead of directory-only. Dry-run canary first,
FY2024 loaded first (locks current population), every row durably sourced, never overwrite, read-only
verification, $0. (Alameda + Sacramento are 64-02.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-socal-county-government-budgets/64-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/loadCountyBudget.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     NEVER mcp__supabase-local (stale, D-09). The loader does NOT auto-load .env — source it first:
       set -a; . ./.env; set +a
     SCO is flaky (D-06) — drive the loader per-FY in a bash retry loop until each year prints "Done.". -->

Tool (zero new code, runbook county-gov step):
  node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --dry-run                 # canary, zero writes
  node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --source-date 2026-06-17  # canary live (locks current pop)
  node scripts/loadCountyBudget.js --county "<X>" --fy <year> --source-date 2026-06-17 # backfill FY2003..2023, per-FY

Counties (SCO entity_name, NO "County" suffix): Riverside, San Bernardino, San Diego, Ventura, Santa Barbara, Imperial.
DB entity = "<X> County" (loader default; all 6 already exist, entity_type='county', population=0, 0 existing op+rev rows — confirmed 2026-06-17).
County datasets: uctr-c2j8 (operating), emxv-k8xv (revenue) — both load by default. Feed carries per-year estimated_population (D-04).
Read-only probe harness: reuse the .env service-key snippet from 61-01-PLAN.md Task 2 / 63-01-PLAN.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run canary FY2024 for all 6 SoCal counties (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/64-socal-county-government-budgets/64-CONTEXT.md (D-01..D-12 — read in full)
    - docs/socal-county-onboarding.md (county-government budget step + Locked conventions)
    - scripts/loadCountyBudget.js (flags, county datasets, resolveCountyEntity error-if-missing, never-overwrite)
  </read_first>
  <action>
    Source .env (`set -a; . ./.env; set +a`). For EACH county in Riverside, San Bernardino, San Diego,
    Ventura, Santa Barbara, Imperial, run the canary dry-run:
      node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --dry-run
    Read each output. Confirm: the county entity resolves (id printed, no "not found" error), the feed
    returns rows for FY2024, per-year `estimated_population` is present (population-from-feed line, >0),
    and operating + revenue category counts + totals are non-zero and plausible (all-governmental-funds).
    Record per-county: FY2024 op total, rev total, and the feed population. Because SCO is flaky (D-06),
    wrap each call in a retry loop (retry until the output contains "Done."). Writes nothing.
  </action>
  <acceptance_criteria>
    - All 6 counties' canary dry-runs complete; each resolves its entity (no "not found"), returns FY2024 rows, shows feed population > 0, and reports non-zero operating + revenue totals.
    - Per-county FY2024 op/rev totals + feed population are recorded; no writes performed.
  </acceptance_criteria>
  <done>The 6 SoCal counties' FY2024 canary totals + feed populations are enumerated from a no-write dry-run, confirming each entity + the population-from-feed assumption before any live write.</done>
</task>

<task type="auto">
  <name>Task 2: Live load — FY2024 canary first, then backfill FY2003–2023 (all 6 counties)</name>
  <files>(no repo files — production DB rows written to the 6 county entities)</files>
  <read_first>
    - The dry-run output from Task 1
    - .planning/phases/63-socal-county-cities-load-linking/63-01-SUMMARY.md (per-FY retry pattern precedent)
  </read_first>
  <action>
    Source .env. For EACH of the 6 counties, in this order so the stored population reflects the current year:
      1) Load the FY2024 canary for real:
         node scripts/loadCountyBudget.js --county "<X>" --fy 2024 --source-date 2026-06-17
         (this backfills the entity population from the FY2024 feed value, first-non-zero-wins per D-04).
      2) Backfill the remaining years FY2003–2023 one fiscal year at a time inside a bash retry loop
         (retry each --fy until its output contains "Done."; gap-fill any year that still fails after the
         loop). Operating + revenue both load by default. Use --source-date 2026-06-17 on every call.
    Capture each county's per-FY load lines (totals written, any "No data found" year gaps, any never-overwrite
    SKIP). Do NOT modify any city rows. The authoritative per-county DB probe runs in Task 3.
  </action>
  <acceptance_criteria>
    - For each of the 6 counties, FY2024 loaded first (population backfilled > 0), then FY2003–2023 loaded; every targeted year/type either wrote or logged a benign "No data found" gap.
    - The loader's console output is captured per county (totals per FY/dataset, any No-data gaps, any never-overwrite skips).
    - No DB writes beyond loadCountyBudget.js; production DB; city rows untouched.
  </acceptance_criteria>
  <done>All 6 SoCal county governments have operating + revenue FY2003–2024 loaded (FY2024 first), durably SCO-sourced, with per-year feed population.</done>
</task>

<task type="auto">
  <name>Task 3: Verify (read-only) + write 64-01-SUMMARY</name>
  <files>.planning/phases/64-socal-county-government-budgets/64-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/63-socal-county-cities-load-linking/VERIFICATION.md (probe + evidence-table shape)
  </read_first>
  <action>
    With a READ-ONLY probe (production DB, schema treasury, service key), for EACH of the 6 county entities
    confirm: operating + revenue rows present across FY2003–2024 (where the feed provides), every row's
    source_url contains "/d/" (NULL source_url count must be 0), and population > 0 (per-capita renders).
    Spot-check one county's FY2024 op/rev totals against the Task 1 canary dry-run figures (should match).
    Confirm city rows are untouched (no city municipality gained or lost op/rev from this load — county-gov
    load targets only the entity_type='county' row). Then write 64-01-SUMMARY.md with a per-county table:
    op rows, rev rows, FY range, NULL source_url count, population, any No-data FY gaps. State CGB-01
    (SoCal six) satisfied; note all-governmental-funds basis.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const cnt=async(q)=>{const {count}=await q;return count;};let allOk=true;for(const name of ['Riverside County','San Bernardino County','San Diego County','Ventura County','Santa Barbara County','Imperial County']){const {data:c}=await s.from('municipalities').select('id,population').eq('name',name).eq('entity_type','county').maybeSingle();if(!c){console.log(name,'-> NO ENTITY');allOk=false;continue;}const op=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).eq('dataset_type','operating'));const rev=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).eq('dataset_type','revenue'));const nullUrl=await cnt(s.from('budgets').select('id',{count:'exact',head:true}).eq('municipality_id',c.id).in('dataset_type',['operating','revenue']).is('source_url',null));console.log(name.padEnd(22),'op='+op,'rev='+rev,'nullUrl='+nullUrl,'pop='+(c.population||0));if(!(op>0&&rev>0&&nullUrl===0&&c.population>0))allOk=false;}process.exit(allOk?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 SoCal counties have operating > 0 AND revenue > 0 rows, NULL source_url = 0, and population > 0 (probe exits 0).
    - A spot-checked county's FY2024 op/rev totals match the Task 1 canary dry-run figures.
    - 64-01-SUMMARY.md records the per-county load + verification as the CGB-01 (SoCal six) evidence record, noting all-governmental-funds basis and any No-data FY gaps.
  </acceptance_criteria>
  <done>The 6 SoCal county governments are loaded, sourced, populated, and verified read-only; their pages render icicle/summary + per-capita; CGB-01 (SoCal six) documented.</done>
</task>

</tasks>

<verification>
- 6 SoCal counties loaded op+rev FY2003–2024 (where SCO provides), all-governmental-funds, every row durably /d/-sourced + per-year feed population.
- Each county entity population > 0 (FY2024 loaded first); county pages render icicle/summary + per-capita (no longer directory-only); city rows untouched.
- Read-only verification, production DB, $0 spend; 64-01-SUMMARY.md is the CGB-01 (SoCal six) evidence.
</verification>

<success_criteria>
- CGB-01 (SoCal six) satisfied: Riverside, San Bernardino, San Diego, Ventura, Santa Barbara, Imperial county governments loaded (op+rev, FY2003–2024, all-funds basis, durably sourced, per-year population) → pages no longer directory-only.
- Never-overwrite preserved any pre-existing/custom rows; city rows untouched (D-08); $0 (D-10); production DB only (D-09).
- 64-01-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/64-socal-county-government-budgets/64-01-SUMMARY.md` when done.
</output>
