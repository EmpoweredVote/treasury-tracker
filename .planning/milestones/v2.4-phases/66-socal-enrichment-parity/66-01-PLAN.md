---
phase: 66-socal-enrichment-parity
plan: "66-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/socalEnrichment66.mjs
  - scripts/loadSoCalEnrichment66.mjs
  - data/socal-enrichment-66.expanded.json
autonomous: true
requirements: [ENR-03]
must_haves:
  truths:
    - "Every newly parity-loaded SoCal category — operating, revenue, AND salaries — has plain-language enrichment (plain name + description): op/rev confirmed already 100% covered by Phase 61 universal rows; the uncovered salary department name_keys shared by ≥2 SoCal cities are newly authored (D-01)"
    - "Authoring is hybrid + bleed-safe: universal rows (municipality_id IS NULL) are generic only — no $ figures, no city names, no city-specific facts; any city-specific category is city-scoped; no city's text appears on another city's categories (D-02)"
    - "Authored inline at ~$0 — the agent writes the text via the reused Phase 61 CONCEPTS library; the paid enrichCategories.js is NOT run (D-04)"
    - "Single-city salary department long tail documented as a deferred gap; production DB only (D-07, D-10)"
  artifacts:
    - path: ".planning/phases/66-socal-enrichment-parity/66-01-SUMMARY.md"
      provides: "The SoCal enrichment-parity result: residual gap set, rows authored (universal/city-scoped), bleed-safety spot-check across ≥3 cities, deferred tail — the ENR-03 evidence record"
      contains: "per-dataset uncovered counts (op/rev expected 0, salaries ≥2-city), rows authored + resolver via-breakdown, $-leak self-check (0), ≥3-city bleed spot-check, deferred single-city count, $0 confirmation"
    - path: "data/socalEnrichment66.mjs"
      provides: "Committed SoCal worklist + any new CONCEPT additions (reviewable authoring source)"
      contains: "the uncovered ≥2-city name_keys and their resolved concept mapping"
  key_links:
    - from: "uncovered SoCal name_keys (budget_categories.link_key) not in category_enrichment"
      to: "treasury.category_enrichment universal rows (municipality_id IS NULL, generic plain_name + description)"
      via: "reuse Phase 61 resolve() (explicit → keyword route → general_dept) + idempotent upsert on (name_key, municipality_id)"
      pattern: "measure residual vs existing enrichment → author generic universal rows via the concept library → bleed-safe self-check → verify ≥3 cities"
---

<objective>
Satisfy ENR-03: bring the parity-loaded SoCal categories (operating, revenue, salaries) to the OC/LA
enrichment baseline — standardized, plain-language, BLEED-SAFE enrichment, authored hybrid (universal for
generic taxonomy; city-scoped for anything city-specific) inline at ~$0. The op/rev taxonomy is already
100% covered by Phase 61's universal rows; the real work is authoring universal generic enrichment for the
uncovered salary department name_keys shared by ≥2 SoCal cities (~27), reusing the Phase 61 CONCEPTS
resolver, with the single-city long tail documented as deferred. No paid AI; production DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-socal-enrichment-parity/66-CONTEXT.md
@.planning/phases/61-enrichment-parity/61-01-SUMMARY.md
@scripts/loadCAParityEnrichment61.mjs
@data/caParityEnrichment61.mjs
@data/caParityEnrichment61_oprev.mjs
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     NEVER mcp__supabase-local. Loaders don't auto-load .env — source it first: set -a; . ./.env; set +a -->

Data model:
  treasury.budget_categories — categories (depth 0 = functional categories / departments); column `link_key` = enrichment key.
  treasury.category_enrichment — (name_key, municipality_id NULL=universal, plain_name, short_description, description, tags, source, confidence, evidence_summary, generated_at). Join: (name_key, municipality_id) two-tier muni→NULL fallback; dataset-independent.
  name_key = link_key = (name||'').toLowerCase().trim() ; children: `${normalize(parent)}|${normalize(child)}`.

Reuse (Phase 61, zero new concepts expected):
  data/caParityEnrichment61.mjs        → CONCEPTS (~60 generic municipal concepts)
  data/caParityEnrichment61_oprev.mjs  → EXPLICIT_ROWS, ROUTE_RULES
  scripts/loadCAParityEnrichment61.mjs → resolve() (explicit → route → general_dept), idempotent upsert, $-leak self-check, dry-run vs --apply

SoCal cohort = 95 cities under the 6 SoCal county nodes. Residual at probe (2026-06-17): op 0, rev 0, salaries 789 uncovered (27 shared by ≥2 cities, ~762 single-city tail).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-confirm the SoCal enrichment residual (gap analysis, no writes)</name>
  <files>(no writes — analysis only; may write a throwaway worklist under scripts/output/)</files>
  <read_first>
    - .planning/phases/66-socal-enrichment-parity/66-CONTEXT.md (D-01..D-10 — read in full)
    - .planning/phases/61-enrichment-parity/61-01-SUMMARY.md (gap-set method + strategy precedent)
    - scripts/loadCAParityEnrichment61.mjs + data/caParityEnrichment61*.mjs (CONCEPTS + resolve())
  </read_first>
  <action>
    Source .env. With a READ-ONLY probe (production, schema treasury): for the 95 SoCal cohort cities, gather
    the distinct depth-0 `budget_categories.link_key` per dataset (operating, revenue, salaries) WITH the
    count of distinct cities per key; load the full `category_enrichment` name_key set (paginate past the
    1000-row cap — universal + municipality-specific). Compute the uncovered residual per dataset and split
    salaries into ≥2-city vs single-city. Confirm op/rev uncovered ≈ 0 (Phase 61 covered the taxonomy) and
    capture the uncovered ≥2-city salary name_keys (~27 expected). Record the worklist (the name_keys to
    author). This follows the established Phase 61 strategy (op/rev full + salaries ≥2-city; single-city tail
    deferred) — no new human gate needed; record the decision inline.
  </action>
  <acceptance_criteria>
    - Full category_enrichment name_key set loaded (paginated, not capped at 1000); per-dataset uncovered residual computed for the 95-city cohort.
    - The uncovered ≥2-city salary name_keys are enumerated (the author worklist); op/rev residual recorded (expected 0); single-city tail counted.
    - No writes performed.
  </acceptance_criteria>
  <done>The exact SoCal enrichment residual + author worklist is established read-only, following the Phase 61 op/rev-full + salaries-≥2 strategy.</done>
</task>

<task type="auto">
  <name>Task 2: Author universal generic enrichment for the residual + idempotent upsert ($0)</name>
  <files>data/socalEnrichment66.mjs, data/socal-enrichment-66.expanded.json, scripts/loadSoCalEnrichment66.mjs</files>
  <read_first>
    - The Task 1 worklist
    - scripts/loadCAParityEnrichment61.mjs (mirror its resolve() + upsert + self-check structure)
    - data/caParityEnrichment61.mjs + data/caParityEnrichment61_oprev.mjs (CONCEPTS, EXPLICIT_ROWS, ROUTE_RULES)
  </read_first>
  <action>
    For each uncovered ≥2-city name_key, resolve it to a generic CONCEPT via the reused Phase 61 resolve()
    (explicit → keyword route → general_dept fallback). Build committed authoring artifacts mirroring Phase 61:
    `data/socalEnrichment66.mjs` (the worklist + any new CONCEPT additions — add a concept ONLY if a name_key
    fails to route, which is unlikely), a deterministic loader `scripts/loadSoCalEnrichment66.mjs` (importing
    the Phase 61 CONCEPTS/resolver), and an expanded JSON dump. Each authored row: municipality_id = NULL
    (universal), source='ai', confidence from the concept, evidence_summary noting it is generic/bleed-safe
    and not city-specific. Run a dry-run (print mapping + write JSON, NO DB write) and review it, THEN run
    `--apply` to idempotently upsert on (name_key, municipality_id) into treasury.category_enrichment.
    Author city-scoped rows (with municipality_id) ONLY for any genuinely city-specific category (expected
    none). Run the bleed-safety self-check: 0 authored rows may contain a `$`-figure or a city name.
    The paid enrichCategories.js is NOT run ($0).
  </action>
  <acceptance_criteria>
    - All uncovered ≥2-city salary name_keys (+ any op/rev residual) have a universal generic category_enrichment row after `--apply`; upsert is idempotent on (name_key, municipality_id).
    - Bleed self-check passes: 0 authored rows contain a `$`-figure or a city name; all universal rows are generic.
    - Committed authoring artifacts exist (data/socalEnrichment66.mjs, expanded JSON, loader); `git log` shows the commit; $0 spend (no Anthropic call).
  </acceptance_criteria>
  <done>The SoCal enrichment residual is authored as bleed-safe universal generic rows inline at $0, via committed reviewable artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Verify coverage + bleed-safety across ≥3 cities + write 66-01-SUMMARY</name>
  <files>.planning/phases/66-socal-enrichment-parity/66-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/61-enrichment-parity/61-01-SUMMARY.md (verification shape)
  </read_first>
  <action>
    With READ-ONLY probes: (1) re-run the residual analysis from Task 1 and confirm op/rev remain 100%
    covered and the ≥2-city salary departments are now covered (uncovered ≥2-city = 0). (2) Bleed-safety
    spot-check across ≥3 SoCal cities in different counties: for a department enriched by a new universal
    row, confirm the SAME generic plain_name/description resolves for each city and that no city's specific
    text appears on another city's category; confirm authored universal text contains no `$`-figures or city
    names. (3) Confirm the salaries Department→Position categories now show plain names (render-code level).
    Write 66-01-SUMMARY.md: per-dataset residual before/after, rows authored (universal/city-scoped) with
    resolver via-breakdown, bleed self-check result, the ≥3-city spot-check, the deferred single-city count,
    $0 confirmation. State ENR-03 satisfied.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const enr=new Set();let from=0;while(true){const {data}=await s.from('category_enrichment').select('name_key').range(from,from+999);if(!data||!data.length)break;data.forEach(e=>enr.add(e.name_key));if(data.length<1000)break;from+=1000;}let ids=[];for(const n of ['Riverside County','San Bernardino County','San Diego County','Ventura County','Santa Barbara County','Imperial County']){const {data:c}=await s.from('municipalities').select('id').eq('name',n).eq('entity_type','county').maybeSingle();const {data:cc}=await s.from('municipalities').select('id').eq('county_id',c.id).eq('entity_type','city');ids=ids.concat(cc.map(x=>x.id));}const dsK={operating:new Map(),revenue:new Map(),salaries:new Map()};for(let i=0;i<ids.length;i+=30){const {data:bs}=await s.from('budgets').select('id,municipality_id,dataset_type,fiscal_year').in('municipality_id',ids.slice(i,i+30)).in('dataset_type',['operating','revenue','salaries']).order('fiscal_year',{ascending:false});const seen=new Set();for(const r of (bs||[])){const k=r.municipality_id+'|'+r.dataset_type;if(seen.has(k))continue;seen.add(k);dsK[r.dataset_type].set(r.municipality_id,r.id);}}let bad=0;for(const dt of ['operating','revenue','salaries']){const bids=[...dsK[dt].values()];const kc=new Map();for(let i=0;i<bids.length;i+=20){const {data:cats}=await s.from('budget_categories').select('budget_id,name,link_key').in('budget_id',bids.slice(i,i+20)).eq('depth',0);for(const c of (cats||[])){const k=(c.link_key||(c.name||'').toLowerCase().trim());if(!kc.has(k))kc.set(k,new Set());kc.get(k).add(c.budget_id);}}const unc2=[...kc.keys()].filter(k=>!enr.has(k)&&kc.get(k).size>=2);console.log(dt,'uncovered>=2cities:',unc2.length);if(unc2.length)bad+=unc2.length;}process.exit(bad===0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Probe confirms 0 uncovered name_keys shared by ≥2 SoCal cities across operating, revenue, AND salaries (exit 0).
    - Bleed-safety spot-check across ≥3 cities passes: universal rows render identical generic text; no foreign-city text; no `$`-figures in authored text.
    - 66-01-SUMMARY.md documents residual before/after, rows authored, bleed check, ≥3-city spot-check, deferred tail, $0 — the ENR-03 evidence record.
  </acceptance_criteria>
  <done>SoCal categories are enriched to parity, bleed-safe, verified across ≥3 cities; ENR-03 satisfied and documented.</done>
</task>

</tasks>

<verification>
- Op/rev categories remain 100% enriched (Phase 61 universal rows); uncovered ≥2-city salary departments authored → 0 remaining.
- All authored universal rows are generic + bleed-safe (no $ figures, no city names); ≥3-city spot-check shows no cross-city bleed.
- Authored inline at $0 (no paid AI); single-city tail documented as deferred; production DB.
- 66-01-SUMMARY.md is the ENR-03 evidence.
</verification>

<success_criteria>
- ENR-03 satisfied: all newly parity-loaded SoCal op/rev/salaries categories carry plain-language enrichment, authored hybrid (generic universal + city-scoped where needed), bleed-safe (≥3-city spot-check), inline at ~$0.
- No city's text appears on another city's categories (D-02); production DB only (D-07).
- 66-01-SUMMARY.md documents the result; single-city salary tail deferred.
</success_criteria>

<output>
Create `.planning/phases/66-socal-enrichment-parity/66-01-SUMMARY.md` when done.
</output>
