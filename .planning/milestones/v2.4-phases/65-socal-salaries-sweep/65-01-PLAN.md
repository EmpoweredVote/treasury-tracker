---
phase: 65-socal-salaries-sweep
plan: "65-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SAL-07]
must_haves:
  truths:
    - "GCC salary source coverage for the 95 SoCal cities was confirmed against the GCC export (dry-run) BEFORE any write, on the PRODUCTION DB (SUPABASE_URL=kxsdzaojfaibhuzmclfq; local is stale) (D-04, D-05)"
    - "Salaries FY2009–2024 are loaded (dataset_type='salaries', names-free Department→Position total-comp tree) for the SoCal cities wherever GCC provides them, additive (operating/revenue untouched), with the never-overwrite guard preserving any different-source rows (D-02, D-06, D-08)"
    - "A sample city's latest-year (FY2024) total compensation reconciles to an independent re-aggregation of the official GCC export at ~$0 delta (D-07)"
    - "Per-city coverage and any GCC gaps are documented; the salaries dataset renders for a spot-checked city; $0 spend (D-10, D-11)"
  artifacts:
    - path: ".planning/phases/65-socal-salaries-sweep/65-01-SUMMARY.md"
      provides: "The SoCal salaries sweep result: per-county/per-city coverage (years covered vs gaps vs preserved), sample reconciliation (~$0 delta), render confirmation — the SAL-07 evidence record"
      contains: "production-target confirmation, dry-run coverage summary, per-county loaded rows + year reach, never-overwrite preserved set, sample-city reconciliation deltas, per-city gaps, render check"
  key_links:
    - from: "CA Government Compensation export (gcc.sco.ca.gov/RawExport/<year>_City.zip, FY2009–2024)"
      to: "treasury.budgets (dataset_type='salaries') on the 95 SoCal city municipalities"
      via: "sweepCASalaries.js --county <Name> (per-county, additive, never-overwrite)"
      pattern: "dry-run coverage confirm → per-county live sweep (names-free Dept→Position tree) → independent GCC re-aggregation reconcile (~$0 delta) + coverage doc"
---

<objective>
Satisfy SAL-07: load CA Government Compensation salaries (FY2009–2024) for the 95 newly-loaded SoCal
cities (Phase 63 cohort across the 6 SoCal counties) using the hardened Phase 60 tool
`sweepCASalaries.js` with ZERO new code — additive (dataset_type='salaries' only, never touches
operating/revenue), never-overwrite. Confirm GCC coverage against the PRODUCTION DB (local is stale)
BEFORE any write, reconcile a sample city's FY2024 total comp to the GCC source at ~$0 delta, and
document per-city coverage + gaps. $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-socal-salaries-sweep/65-CONTEXT.md
@scripts/sweepCASalaries.js
@scripts/loadCASalaries.js
@.planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- PRODUCTION DB ONLY via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     SUPABASE_URL MUST be kxsdzaojfaibhuzmclfq.supabase.co (production) — local Supabase salary state is
     STALE (D-04). NEVER mcp__supabase-local. The script does NOT auto-load .env — source it first:
       set -a; . ./.env; set +a -->

Tool (zero new code, Phase 60 sweep):
  node scripts/sweepCASalaries.js --county "<Name>" --dry-run                       # coverage, zero writes
  node scripts/sweepCASalaries.js --county "<Name>"                                 # live sweep FY2009–2024
  node scripts/sweepCASalaries.js --county "<Name>" --start-year 2024 --end-year 2024 --dry-run  # single-year canary

Counties (pass to --county WITHOUT "County" suffix): Riverside, San Bernardino, San Diego, Ventura, Santa Barbara, Imperial.
Cohort = 95 cities (28/24/18/10/8/7) read from DB by county_id (confirmed 2026-06-17).
Source: GCC City.zip FY2009–2024 (16 ZIPs, cached in os.tmpdir()/gcc-salary-cache — shared across the 6 per-county runs).
Writes dataset_type='salaries' only (additive). Never-overwrite: different-source (city,year) rows preserved; same-source GCC rows refresh.
Pre-existing: Riverside city + San Diego city already have 16 GCC salaries rows each (Phase 60) → refresh.
Read-only probe harness: reuse the .env service-key snippet from 63-01/64-01 plans.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm production target + dry-run GCC coverage for the 95-city cohort (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/65-socal-salaries-sweep/65-CONTEXT.md (D-01..D-11 — read in full)
    - scripts/sweepCASalaries.js (cohort-from-DB, --county, --dry-run, never-overwrite pre-pass)
    - .planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md (coverage-doc precedent)
  </read_first>
  <action>
    Source .env (`set -a; . ./.env; set +a`). FIRST assert the write target is PRODUCTION: confirm
    `new URL(process.env.SUPABASE_URL).host` === "kxsdzaojfaibhuzmclfq.supabase.co" (D-04) — if not, STOP
    (local is stale; never sweep against it). Then, for EACH of the 6 counties, run:
      node scripts/sweepCASalaries.js --county "<Name>" --dry-run
    Read each output. Confirm: the county resolves and its city cohort matches Phase 63 (Riverside 28,
    San Bernardino 24, San Diego 18, Ventura 10, Santa Barbara 8, Imperial 7 = 95 total), the never-overwrite
    guard line reports how many existing (city,year) rows will be preserved, and per-city/year GCC coverage +
    gaps are listed. Record the cohort count, coverage summary, and the protected/preserved set. Writes nothing.
  </action>
  <acceptance_criteria>
    - SUPABASE_URL host confirmed = kxsdzaojfaibhuzmclfq.supabase.co (production) before any further step.
    - All 6 counties' dry-runs complete; total cohort = 95 cities; per-city/year GCC coverage + gaps + the never-overwrite preserved set are recorded; no writes performed.
  </acceptance_criteria>
  <done>GCC source coverage for the 95 SoCal cities is confirmed against the production DB (dry-run) before any write — Success Criterion #1.</done>
</task>

<task type="auto">
  <name>Task 2: Live sweep salaries FY2009–2024 for the 6 SoCal counties</name>
  <files>(no repo files — production DB rows written, dataset_type='salaries')</files>
  <read_first>
    - The dry-run coverage output from Task 1
    - scripts/sweepCASalaries.js (syncCityYear writes salaries only; ZIP cache)
  </read_first>
  <action>
    Source .env. For EACH of the 6 counties run the live sweep across the full range (default 2009–2024):
      node scripts/sweepCASalaries.js --county "<Name>"
    The first county downloads the 16 GCC ZIPs (cached); the rest hit the cache. The sweep writes ONLY
    dataset_type='salaries' (additive — operating/revenue untouched) and skips any (city,year) row from a
    different source (never-overwrite). If a GCC year download fails, retry that county (the ZIP cache makes
    re-runs cheap; a single failed year is retryable). Capture per-county: cities covered, total salaries
    rows written, preserved (skipped) set, and per-city gap years. Riverside + San Diego cities' existing
    16 GCC rows refresh (same source) — expected, not a conflict.
  </action>
  <acceptance_criteria>
    - All 6 counties swept; the loader reports salaries rows written for the cohort cities across FY2009–2024 wherever GCC provides them; operating/revenue row counts are unchanged (additive).
    - Per-county coverage (covered years / gaps / preserved) is captured.
    - No DB writes beyond sweepCASalaries.js; production DB.
  </acceptance_criteria>
  <done>The 95 SoCal cities carry GCC salaries FY2009–2024 wherever the source provides them, additive and never-overwrite — Success Criterion #2.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile a sample (independent GCC re-aggregation) + coverage probe + write 65-01-SUMMARY</name>
  <files>.planning/phases/65-socal-salaries-sweep/65-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md (the ~$0-delta independent re-aggregation method)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Reconcile 2–3 sample SoCal cities (pick ones with substantial workforces, e.g. a large Riverside or
    San Bernardino city) for FY2024: re-aggregate the official GCC FY2024 City export for that city via a
    SEPARATE code path (a throwaway read-only probe — sum TOTAL_WAGES + TOTAL_BENEFITS over the city's rows
    in the cached 2024 GCC CSV; NOT a re-sum of the ingested DB tree) and compare to the DB-stored FY2024
    salaries total. Expect ~$0 delta (target exactly $0, per 60-03). Then run a read-only coverage probe:
    confirm the cohort cities have salaries rows reaching FY2009 where GCC provides, count per-city covered
    years, and list gaps. Confirm the salaries dataset renders for a spot-checked city at the render-code
    level (DatasetTabs.tsx shows the Salaries card when the city has a 'salaries' row with a populated tree).
    Write 65-01-SUMMARY.md: production-target confirmation, dry-run coverage summary, per-county loaded rows
    + year reach, preserved set, the sample reconciliation deltas, per-city gaps, render check. State SAL-07
    satisfied.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}if(new URL(process.env.SUPABASE_URL).host!=='kxsdzaojfaibhuzmclfq.supabase.co'){console.log('NOT PRODUCTION:',process.env.SUPABASE_URL);process.exit(1);}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const cnt=async(q)=>{const {count}=await q;return count;};let totCities=0,withSal=0;for(const n of ['Riverside County','San Bernardino County','San Diego County','Ventura County','Santa Barbara County','Imperial County']){const {data:c}=await s.from('municipalities').select('id').eq('name',n).eq('entity_type','county').maybeSingle();const {data:cities}=await s.from('municipalities').select('id').eq('county_id',c.id).eq('entity_type','city');const ids=cities.map(x=>x.id);totCities+=ids.length;const {data:salRows}=await s.from('budgets').select('municipality_id').eq('dataset_type','salaries').in('municipality_id',ids);const covered=new Set((salRows||[]).map(r=>r.municipality_id));withSal+=covered.size;console.log(n.padEnd(22),'cities='+ids.length,'with salaries='+covered.size);}console.log('TOTAL cohort='+totCities,'cities with >=1 salaries year='+withSal);process.exit(withSal>0&&totCities===95?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - 2–3 sample cities' FY2024 DB salaries total reconciles to the independent GCC re-aggregation at ~$0 delta (record each delta).
    - Coverage probe confirms the 95-city cohort has salaries coverage (most cities reaching FY2009; gaps listed); probe exits 0 (cohort=95, cities-with-salaries > 0).
    - 65-01-SUMMARY.md documents production-target, coverage, per-county results, preserved set, reconciliation, gaps, render check — the SAL-07 evidence record.
  </acceptance_criteria>
  <done>SoCal salaries are loaded, reconciled (~$0 delta) and documented per-city; the salaries dataset renders; SAL-07 satisfied.</done>
</task>

</tasks>

<verification>
- Production target confirmed before any write; GCC coverage dry-run read first (SC#1).
- Salaries FY2009–2024 loaded additive for the 95 SoCal cities wherever GCC provides, never-overwrite preserved different-source rows (SC#2).
- Sample city FY2024 reconciles to the GCC source at ~$0 delta (SC#3).
- Per-city coverage + gaps documented; salaries dataset renders for a spot-checked city; $0 (SC#4).
</verification>

<success_criteria>
- SAL-07 satisfied: GCC salaries FY2009–2024 loaded for the 95 SoCal cities (names-free Dept→Position total-comp, never-overwrite), reconciled ~$0 on a sample, coverage + gaps documented.
- Additive (operating/revenue untouched); production DB only (D-04); $0 (D-10).
- 65-01-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/65-socal-salaries-sweep/65-01-SUMMARY.md` when done.
</output>
