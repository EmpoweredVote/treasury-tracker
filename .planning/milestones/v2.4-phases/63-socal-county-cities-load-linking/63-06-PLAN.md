---
phase: 63-socal-county-cities-load-linking
plan: "63-06"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SOCAL-06]
must_haves:
  truths:
    - "Every Imperial County city the SCO ByTheNumbers feed reports has operating + revenue loaded for FY2003–2024 (where provided), each row carrying the durable /d/<id> source_url + fetch date and per-year estimated_population (D-02, D-07)"
    - "The Imperial County node is created by seedCountyLinks (none existed) and every loaded city is linked via county_id; breadcrumb (US → California → Imperial County → city) + Cities-in-County render (D-04)"
    - "Never-overwrite guard preserved any custom-source city; read-only verification; production DB only; $0 (D-08, D-09, D-10)"
  artifacts:
    - path: ".planning/phases/63-socal-county-cities-load-linking/63-06-SUMMARY.md"
      provides: "The Imperial County city-load result — the SOCAL-06 evidence record"
      contains: "dry-run city list + counts, loaded op/rev row counts, SCO source coverage, cities linked, any skipped/custom cities, per-capita spot-check"
  key_links:
    - from: "SCO ByTheNumbers feed (county='Imperial')"
      to: "treasury.budgets (op+rev FY2003–2024) + municipalities.county_id → Imperial County"
      via: "bulkLoadStateController.js --county \"Imperial\", then seedCountyLinks.js --county \"Imperial\""
      pattern: "load op+rev with durable source + per-year pop (never-overwrite), then create the county node + link member cities"
---

<objective>
Satisfy SOCAL-06: load operating + revenue (FY2003–2024) for every Imperial County city from the SCO
ByTheNumbers feed and link each to an Imperial County node — hardened v2.2 pipeline, ZERO new code. The
county node does NOT exist yet; seedCountyLinks creates it. Dry-run first, never overwrite custom cities,
every row durably sourced, read-only verification, $0. (Imperial is the smallest cohort — closes the
6-county SoCal city load.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/bulkLoadStateController.js
@scripts/seedCountyLinks.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env / .env.local SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema
     treasury. NEVER mcp__supabase-local (stale, D-08). The loader scripts read .env themselves. -->

Pipeline (zero new tooling, runbook Steps 1–2):
  node scripts/bulkLoadStateController.js --county "Imperial" --fy 2003 --fy 2004 --fy 2005 --fy 2006 --fy 2007 --fy 2008 --fy 2009 --fy 2010 --fy 2011 --fy 2012 --fy 2013 --fy 2014 --fy 2015 --fy 2016 --fy 2017 --fy 2018 --fy 2019 --fy 2020 --fy 2021 --fy 2022 --fy 2023 --fy 2024 [--dry-run --list-cities | --source-date YYYY-MM-DD]
  node scripts/seedCountyLinks.js --county "Imperial" [--dry-run]

SCO county field = "Imperial" (no suffix). DB county entity = "Imperial County"
  (does NOT exist yet — seedCountyLinks creates it). ~7 cities expected (confirm via dry-run).
Datasets: ju3w-4gxp (operating), rrtv-rsj9 (revenue) — single statewide feed filtered by county.
Read-only probe harness: reuse the .env service-key snippet from 61-01-PLAN.md Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run — enumerate Imperial County cities (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md (D-01..D-10)
    - docs/socal-county-onboarding.md (Steps 1–2)
    - scripts/bulkLoadStateController.js
  </read_first>
  <action>
    Run DRY-RUN with --list-cities for Imperial across FY2003–2024:
      node scripts/bulkLoadStateController.js --county "Imperial" --fy 2003 ... --fy 2024 --dry-run --list-cities
    Record the full city list, count, and new vs existing cities. Confirm a non-empty list. Writes nothing.
  </action>
  <acceptance_criteria>
    - Dry-run completes with a non-empty Imperial city list + per-FY counts; no writes.
    - City list + count recorded; new vs existing noted.
  </acceptance_criteria>
  <done>The Imperial city cohort + counts are enumerated from a no-write dry-run.</done>
</task>

<task type="auto">
  <name>Task 2: Live load Imperial operating + revenue FY2003–2024</name>
  <files>(no repo files — production DB rows written)</files>
  <read_first>
    - The dry-run output from Task 1
    - .planning/phases/61-enrichment-parity/61-01-PLAN.md (read-only .env service-key probe snippet)
  </read_first>
  <action>
    Run the real load (operating + revenue by default), recording the fetch date:
      node scripts/bulkLoadStateController.js --county "Imperial" --fy 2003 ... --fy 2024 --source-date <today YYYY-MM-DD>
    Record the loader's reported per-FY/dataset counts + any never-overwrite skips (authoritative DB probe in Task 3).
  </action>
  <acceptance_criteria>
    - Live load completes (exit 0); loader reports op + rev rows written across FY2003–2024.
    - Loader output captured; never-overwrite guard preserved any custom-source city; production DB; no writes beyond the loader.
  </acceptance_criteria>
  <done>Imperial cities' op + rev FY2003–2024 are loaded and SCO-sourced, custom cities preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Seed/link Imperial County + verify + write SUMMARY</name>
  <files>.planning/phases/63-socal-county-cities-load-linking/63-06-SUMMARY.md</files>
  <read_first>
    - scripts/seedCountyLinks.js
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Create the county node + link cities:
      node scripts/seedCountyLinks.js --county "Imperial" --dry-run   (review)
      node scripts/seedCountyLinks.js --county "Imperial"             (real)
    Verify (read-only probe): the Imperial County node now exists; every loaded city has county_id pointing
    to it; per-capita spot-check on a sample city. Then write 63-06-SUMMARY.md (dry-run list + count, loaded
    op/rev counts, SCO source coverage, cities linked, any skipped/custom, per-capita spot-check). State SOCAL-06 satisfied.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data:cnode}=await s.from('municipalities').select('id').eq('name','Imperial County').eq('entity_type','county');if(!cnode||!cnode.length){console.log('NO Imperial County node');process.exit(1);}const C=cnode[0].id;const {data:cities}=await s.from('municipalities').select('id,population').eq('county_id',C);const ids=(cities||[]).map(c=>c.id);const {data:b}=await s.from('budgets').select('fiscal_year,dataset_type,municipality_id').in('municipality_id',ids.length?ids:['00000000-0000-0000-0000-000000000000']).in('dataset_type',['operating','revenue']);const rows=b||[];const minFy=rows.length?Math.min(...rows.map(r=>r.fiscal_year)):null;const popOk=(cities||[]).filter(c=>c.population>0).length;console.log('Imperial-linked cities:',ids.length,'| op/rev rows:',rows.length,'| earliest FY:',minFy,'| cities w/ population:',popOk);process.exit(ids.length>0&&rows.length>0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - Imperial County node exists; all loaded cities linked via county_id; cities linked elsewhere reported (not repointed without --force).
    - Per-capita spot-check passes on a sample city.
    - 63-06-SUMMARY.md records the load + link + verification as the SOCAL-06 evidence record.
  </acceptance_criteria>
  <done>Imperial County cities loaded, sourced, linked, verified; SOCAL-06 satisfied and documented — 6-county SoCal city load complete.</done>
</task>

</tasks>

<verification>
- Imperial cities loaded op+rev FY2003–2024 (where SCO provides), every SCO row durably sourced + per-year population.
- County node created; all loaded cities linked; never-overwrite preserved custom cities.
- Breadcrumb + Cities-in-County render; per-capita spot-check passes.
- Read-only verification, production DB, $0; 63-06-SUMMARY.md is the SOCAL-06 evidence.
</verification>

<success_criteria>
- SOCAL-06 satisfied: Imperial County cities loaded (op+rev, FY2003–2024, sourced, per-year pop) + linked.
- Never-overwrite preserved custom data (D-10); $0 (D-09); production DB only (D-08).
- 63-06-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/63-socal-county-cities-load-linking/63-06-SUMMARY.md` when done.
</output>
