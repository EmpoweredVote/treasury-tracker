---
phase: 63-socal-county-cities-load-linking
plan: "63-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SOCAL-01]
must_haves:
  truths:
    - "Every Riverside County city the SCO ByTheNumbers feed reports has operating + revenue loaded for FY2003–2024 (where the feed provides it), each row carrying the durable ByTheNumbers /d/<id> source_url + fetch date and per-year estimated_population (D-02, D-07)"
    - "The never-overwrite guard left any pre-existing custom-source city's budget data unchanged; new cities were auto-created, existing ones updated only where the source matches (D-10)"
    - "Every loaded Riverside city is linked via county_id to the existing Riverside County node (e4906055-017e-4fde-af87-878760301c65); the breadcrumb chain (US → California → Riverside County → city) and Cities-in-County panel render (D-04)"
    - "Read-only verification + production DB only; $0 spend (free SCO source, no AI) (D-08, D-09)"
  artifacts:
    - path: ".planning/phases/63-socal-county-cities-load-linking/63-01-SUMMARY.md"
      provides: "The Riverside County city-load result: cities loaded (new vs existing), FY reach, row/source counts, linking result, per-capita spot-check — the SOCAL-01 evidence record"
      contains: "dry-run city list + counts, loaded op/rev row counts, NULL-source_url count (must be 0 for SCO rows), cities linked to Riverside County, any skipped/custom cities, per-capita spot-check"
  key_links:
    - from: "SCO ByTheNumbers feed (county='Riverside')"
      to: "treasury.budgets (operating + revenue, FY2003–2024) + municipalities.county_id → Riverside County"
      via: "bulkLoadStateController.js --county Riverside, then seedCountyLinks.js --county Riverside"
      pattern: "load op+rev with durable source + per-year pop (never-overwrite), then link every member city to the county node"
---

<objective>
Satisfy SOCAL-01: load operating + revenue (FY2003–2024) for every Riverside County city from the SCO
ByTheNumbers feed and link each to the Riverside County node — using the hardened v2.2 pipeline with
ZERO new code. Riverside County node already exists (reuse). Dry-run first, never overwrite custom-source
cities, every row durably sourced, read-only verification, $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/bulkLoadStateController.js
@scripts/seedCountyLinks.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env / .env.local SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema
     treasury. NEVER mcp__supabase-local (stale, D-08). The loader scripts read .env themselves. -->

Pipeline (zero new tooling, runbook Steps 1–2):
  node scripts/bulkLoadStateController.js --county "Riverside" --fy 2003 --fy 2004 --fy 2005 --fy 2006 --fy 2007 --fy 2008 --fy 2009 --fy 2010 --fy 2011 --fy 2012 --fy 2013 --fy 2014 --fy 2015 --fy 2016 --fy 2017 --fy 2018 --fy 2019 --fy 2020 --fy 2021 --fy 2022 --fy 2023 --fy 2024 [--dry-run --list-cities | --source-date YYYY-MM-DD]
  node scripts/seedCountyLinks.js --county "Riverside" [--dry-run]

SCO county field = "Riverside" (no "County" suffix). DB county entity = "Riverside County"
  (id e4906055-017e-4fde-af87-878760301c65, already exists — reuse). ~28 cities expected (confirm via dry-run).
Datasets: ju3w-4gxp (expenditures→operating), rrtv-rsj9 (revenues) — single statewide feed filtered by county.
Read-only probe harness: reuse the .env service-key snippet from 61-01-PLAN.md Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run — enumerate Riverside County cities (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md (D-01..D-10 — read in full)
    - docs/socal-county-onboarding.md (Steps 1–2)
    - scripts/bulkLoadStateController.js (flags + never-overwrite behavior)
  </read_first>
  <action>
    Run the loader in DRY-RUN with --list-cities for Riverside across FY2003–2024:
      node scripts/bulkLoadStateController.js --county "Riverside" --fy 2003 ... --fy 2024 --dry-run --list-cities
    Read the output. Record: the full list of Riverside cities the SCO feed reports, the city count, and
    which cities are new auto-creates vs already in the DB. Confirm the county field "Riverside" resolves
    cities (non-empty list). Writes nothing.
  </action>
  <acceptance_criteria>
    - Dry-run completes and lists a non-empty set of Riverside cities with per-FY counts; no writes performed.
    - The city list + count are recorded; new vs existing cities noted.
  </acceptance_criteria>
  <done>The Riverside city cohort + counts are enumerated from a no-write dry-run, ready for the live load.</done>
</task>

<task type="auto">
  <name>Task 2: Live load Riverside operating + revenue FY2003–2024</name>
  <files>(no repo files — production DB rows written)</files>
  <read_first>
    - The dry-run output from Task 1
    - .planning/phases/61-enrichment-parity/61-01-PLAN.md (read-only .env service-key probe snippet)
  </read_first>
  <action>
    Run the real load (operating + revenue both load by default), recording the fetch date:
      node scripts/bulkLoadStateController.js --county "Riverside" --fy 2003 ... --fy 2024 --source-date <today YYYY-MM-DD>
    Then, with a READ-ONLY probe (production DB, schema treasury, service key), confirm the loaded rows:
    operating + revenue rows present for Riverside cities reaching FY2003 where the feed provides it,
    every SCO row carrying a non-NULL /d/ source_url, and per-year population populated. Confirm the
    never-overwrite guard skipped (did not overwrite) any custom-source city. Record the loader's reported
    per-FY/dataset load counts (the authoritative per-county DB probe runs in Task 3 after linking, when
    county_id is populated).
  </action>
  <acceptance_criteria>
    - Live load completes (exit 0); the loader reports operating + revenue rows written across FY2003–2024.
    - The loader's console output is captured (cities loaded, rows per FY/dataset, any never-overwrite skips).
    - Never-overwrite guard preserved any custom-source city (no overwrite of a different-source row).
    - No DB writes beyond the loader; production DB.
  </acceptance_criteria>
  <done>Riverside cities' operating + revenue FY2003–2024 are loaded and SCO-sourced, with custom cities preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Seed/link Riverside County + verify + write SUMMARY</name>
  <files>.planning/phases/63-socal-county-cities-load-linking/63-01-SUMMARY.md</files>
  <read_first>
    - scripts/seedCountyLinks.js
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Link the cities to the county (Riverside County node already exists — reuse, idempotent):
      node scripts/seedCountyLinks.js --county "Riverside" --dry-run   (review)
      node scripts/seedCountyLinks.js --county "Riverside"             (real link)
    Verify (read-only probe): every loaded Riverside city has county_id = Riverside County
    (e4906055-017e-4fde-af87-878760301c65); spot-check per-capita (population non-zero) on a sample city;
    confirm the breadcrumb chain + Cities-in-County panel will render (county_id set, county entity present).
    Then write 63-01-SUMMARY.md: dry-run city list + count, loaded op/rev row counts, SCO source_url
    coverage (NULL SCO source_url must be 0), cities linked, any skipped/custom cities (e.g. preserved
    custom-source), per-capita spot-check. State SOCAL-01 satisfied.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data:cnode}=await s.from('municipalities').select('id').eq('name','Riverside County').eq('entity_type','county');if(!cnode||!cnode.length){console.log('NO Riverside County node');process.exit(1);}const C=cnode[0].id;const {data:cities}=await s.from('municipalities').select('id,population').eq('county_id',C);const ids=(cities||[]).map(c=>c.id);const {data:b}=await s.from('budgets').select('fiscal_year,dataset_type,source_url,municipality_id').in('municipality_id',ids.length?ids:['00000000-0000-0000-0000-000000000000']).in('dataset_type',['operating','revenue']);const rows=b||[];const minFy=rows.length?Math.min(...rows.map(r=>r.fiscal_year)):null;const scoNull=rows.filter(r=>/sco|bythenumbers|\/d\//i.test(r.source_url||'')).length===0?0:rows.filter(r=>(/sco|bythenumbers/i.test(r.source_url||'')||/\/d\//.test(r.source_url||''))===false).length;const popOk=(cities||[]).filter(c=>c.population>0).length;console.log('Riverside-linked cities:',ids.length,'| op/rev rows:',rows.length,'| earliest FY:',minFy,'| cities w/ population:',popOk);process.exit(ids.length>0&&rows.length>0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - All loaded Riverside cities are linked via county_id to Riverside County; cities already linked elsewhere reported (not repointed without --force).
    - Per-capita spot-check passes on a sample city (population non-zero across backfilled years).
    - 63-01-SUMMARY.md records the load + link + verification as the SOCAL-01 evidence record.
  </acceptance_criteria>
  <done>Riverside County cities are loaded, sourced, linked, and verified; SOCAL-01 satisfied and documented.</done>
</task>

</tasks>

<verification>
- Riverside cities loaded op+rev FY2003–2024 (where SCO provides), every SCO row durably sourced + per-year population.
- Never-overwrite preserved custom-source cities; all loaded cities linked to Riverside County.
- Breadcrumb + Cities-in-County render; per-capita spot-check passes.
- Read-only verification, production DB, $0 spend; 63-01-SUMMARY.md is the SOCAL-01 evidence.
</verification>

<success_criteria>
- SOCAL-01 satisfied: Riverside County cities loaded (op+rev, FY2003–2024, sourced, per-year pop) + linked.
- Never-overwrite preserved custom data (D-10); $0 (D-09); production DB only (D-08).
- 63-01-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/63-socal-county-cities-load-linking/63-01-SUMMARY.md` when done.
</output>
