---
phase: 63-socal-county-cities-load-linking
plan: "63-03"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SOCAL-03]
must_haves:
  truths:
    - "Every San Diego County city the SCO ByTheNumbers feed reports has operating + revenue loaded for FY2003–2024 (where provided), each row carrying the durable /d/<id> source_url + fetch date and per-year estimated_population (D-02, D-07)"
    - "The never-overwrite guard left the named custom-source city San Diego (and any other custom city) unchanged — San Diego's custom budget is preserved; it is only linked (D-04, D-10)"
    - "Every loaded San Diego city is linked via county_id to the existing San Diego County node (9290f46e-c1db-46e5-9523-470aadb075b3); breadcrumb + Cities-in-County render (D-04)"
    - "Read-only verification; production DB only; $0 (D-08, D-09)"
  artifacts:
    - path: ".planning/phases/63-socal-county-cities-load-linking/63-03-SUMMARY.md"
      provides: "The San Diego County city-load result — the SOCAL-03 evidence record"
      contains: "dry-run city list + counts, loaded op/rev row counts, SCO source coverage, cities linked, San Diego custom city preserved (not overwritten), per-capita spot-check"
  key_links:
    - from: "SCO ByTheNumbers feed (county='San Diego')"
      to: "treasury.budgets (op+rev FY2003–2024) + municipalities.county_id → San Diego County"
      via: "bulkLoadStateController.js --county \"San Diego\", then seedCountyLinks.js --county \"San Diego\""
      pattern: "load op+rev with durable source + per-year pop (never-overwrite preserves San Diego custom), then link member cities to the existing county node"
---

<objective>
Satisfy SOCAL-03: load operating + revenue (FY2003–2024) for every San Diego County city from the SCO
ByTheNumbers feed and link each to the San Diego County node — hardened v2.2 pipeline, ZERO new code.
The San Diego County node already exists (reuse). The named custom-source city San Diego is preserved by
the never-overwrite guard (it already carries a custom budget + SCO data + salaries) and is only linked.
Dry-run first, every row durably sourced, read-only verification, $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md
@docs/socal-county-onboarding.md
@scripts/bulkLoadStateController.js
@scripts/seedCountyLinks.js
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Production DB ONLY via repo .env / .env.local SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema
     treasury. NEVER mcp__supabase-local (stale, D-08). The loader scripts read .env themselves. -->

Pipeline (zero new tooling, runbook Steps 1–2):
  node scripts/bulkLoadStateController.js --county "San Diego" --fy 2003 --fy 2004 --fy 2005 --fy 2006 --fy 2007 --fy 2008 --fy 2009 --fy 2010 --fy 2011 --fy 2012 --fy 2013 --fy 2014 --fy 2015 --fy 2016 --fy 2017 --fy 2018 --fy 2019 --fy 2020 --fy 2021 --fy 2022 --fy 2023 --fy 2024 [--dry-run --list-cities | --source-date YYYY-MM-DD]
  node scripts/seedCountyLinks.js --county "San Diego" [--dry-run]

SCO county field = "San Diego" (no suffix). DB county entity = "San Diego County"
  (id 9290f46e-c1db-46e5-9523-470aadb075b3, already exists — reuse). ~18 cities expected (confirm via dry-run).
NOTE: San Diego CITY is a named custom-source city already carrying a custom budget + SCO op/rev + salaries —
  the never-overwrite guard MUST leave its custom rows untouched; seedCountyLinks only links it.
Datasets: ju3w-4gxp (operating), rrtv-rsj9 (revenue) — single statewide feed filtered by county.
Read-only probe harness: reuse the .env service-key snippet from 61-01-PLAN.md Task 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run — enumerate San Diego County cities (no writes)</name>
  <files>(no files written — dry-run only)</files>
  <read_first>
    - .planning/phases/63-socal-county-cities-load-linking/63-CONTEXT.md (D-01..D-10)
    - docs/socal-county-onboarding.md (Steps 1–2)
    - scripts/bulkLoadStateController.js
  </read_first>
  <action>
    Run DRY-RUN with --list-cities for San Diego across FY2003–2024:
      node scripts/bulkLoadStateController.js --county "San Diego" --fy 2003 ... --fy 2024 --dry-run --list-cities
    Record the full city list, count, and new vs existing cities. Confirm the San Diego city appears as an
    existing/custom city that the never-overwrite guard will protect. Writes nothing.
  </action>
  <acceptance_criteria>
    - Dry-run completes with a non-empty San Diego city list + per-FY counts; no writes.
    - City list + count recorded; San Diego custom city flagged as never-overwrite-protected.
  </acceptance_criteria>
  <done>The San Diego city cohort + counts are enumerated; the custom San Diego city is identified as protected.</done>
</task>

<task type="auto">
  <name>Task 2: Live load San Diego operating + revenue FY2003–2024</name>
  <files>(no repo files — production DB rows written)</files>
  <read_first>
    - The dry-run output from Task 1
    - .planning/phases/61-enrichment-parity/61-01-PLAN.md (read-only .env service-key probe snippet)
  </read_first>
  <action>
    Run the real load (operating + revenue by default), recording the fetch date:
      node scripts/bulkLoadStateController.js --county "San Diego" --fy 2003 ... --fy 2024 --source-date <today YYYY-MM-DD>
    Confirm via the loader output that the never-overwrite guard SKIPPED San Diego's custom-source rows
    (no overwrite). Record the loader's reported per-FY/dataset counts + skips (authoritative DB probe in Task 3).
  </action>
  <acceptance_criteria>
    - Live load completes (exit 0); loader reports op + rev rows written across FY2003–2024 for the county's cities.
    - Loader output captured; the never-overwrite guard demonstrably skipped San Diego's custom rows.
    - Production DB; no writes beyond the loader.
  </acceptance_criteria>
  <done>San Diego cities' op + rev FY2003–2024 are loaded and SCO-sourced; the custom San Diego city is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Seed/link San Diego County + verify + write SUMMARY</name>
  <files>.planning/phases/63-socal-county-cities-load-linking/63-03-SUMMARY.md</files>
  <read_first>
    - scripts/seedCountyLinks.js
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Link cities (San Diego County node exists — reuse, idempotent):
      node scripts/seedCountyLinks.js --county "San Diego" --dry-run   (review)
      node scripts/seedCountyLinks.js --county "San Diego"             (real)
    Verify (read-only probe): every loaded San Diego city has county_id = San Diego County
    (9290f46e-c1db-46e5-9523-470aadb075b3); the San Diego custom city's custom budget rows are intact
    (byte-for-byte unchanged); per-capita spot-check on a sample city. Then write 63-03-SUMMARY.md with the
    dry-run list, loaded counts, SCO source coverage, cities linked, San Diego-custom-preserved confirmation,
    per-capita spot-check. State SOCAL-03 satisfied.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const C='9290f46e-c1db-46e5-9523-470aadb075b3';const {data:cities}=await s.from('municipalities').select('id,name,population').eq('county_id',C);const ids=(cities||[]).map(c=>c.id);const {data:b}=await s.from('budgets').select('fiscal_year,dataset_type,municipality_id').in('municipality_id',ids.length?ids:['00000000-0000-0000-0000-000000000000']).in('dataset_type',['operating','revenue']);const rows=b||[];const minFy=rows.length?Math.min(...rows.map(r=>r.fiscal_year)):null;const popOk=(cities||[]).filter(c=>c.population>0).length;const sd=(cities||[]).find(c=>c.name==='San Diego');console.log('San Diego-linked cities:',ids.length,'| op/rev rows:',rows.length,'| earliest FY:',minFy,'| cities w/ population:',popOk,'| San Diego linked:',!!sd);process.exit(ids.length>0&&rows.length>0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - All loaded San Diego cities linked via county_id to San Diego County; the custom San Diego city linked, its custom budget preserved.
    - Per-capita spot-check passes on a sample city.
    - 63-03-SUMMARY.md records the load + link + verification as the SOCAL-03 evidence record.
  </acceptance_criteria>
  <done>San Diego County cities loaded, sourced, linked, verified; San Diego custom data preserved; SOCAL-03 satisfied.</done>
</task>

</tasks>

<verification>
- San Diego cities loaded op+rev FY2003–2024 (where SCO provides), every SCO row durably sourced + per-year population.
- Never-overwrite preserved the San Diego custom city; all loaded cities linked to San Diego County.
- Breadcrumb + Cities-in-County render; per-capita spot-check passes.
- Read-only verification, production DB, $0; 63-03-SUMMARY.md is the SOCAL-03 evidence.
</verification>

<success_criteria>
- SOCAL-03 satisfied: San Diego County cities loaded (op+rev, FY2003–2024, sourced, per-year pop) + linked.
- Never-overwrite preserved custom data incl. San Diego city (D-10); $0 (D-09); production DB only (D-08).
- 63-03-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/63-socal-county-cities-load-linking/63-03-SUMMARY.md` when done.
</output>
