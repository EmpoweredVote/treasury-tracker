---
phase: 104-deepen-the-4-pilots
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processCA.js
  - scripts/processCARevenueAcfr.js
  - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
autonomous: true
requirements: [DEEP-01, RECON-05, ACFR-08]

must_haves:
  truths:
    - "processCA.js + processCARevenueAcfr.js SOURCES objects + years arrays + fiscal_years extended with FY2008-FY2019 (12 added years) using the /Files-ARD/CAFR/cafr{NN}web.pdf URL (a DIFFERENT directory from the existing /Files-ARD/ACFR/ FY2020+ entries), WITHOUT altering the existing FY2020-FY2025 entries"
    - "Each added FY2008-FY2019 GENERAL-column block (operating + revenue) is transcribed from the CA ACFR and ties EXACTLY to the printed Total at validate(); a year that fails to -table-extract (incl. SCO soft-404 = HTTP 200 + HTML) or fails the tie is SKIPPED and logged to 104-DEEPEN-GAPLOG.md, never force-loaded"
    - "The window STOPS at FY2008 — FY2002-FY2007 variant-naming years are NOT pursued in this phase (D-01)"
    - "Negative GF categories in any added year render via clampForRender + '(net loss — shown at 0)' label, parent total preserved (ACFR-08)"
    - "Both loaders run --dry-run clean with NO production DB writes in this plan"
  artifacts:
    - path: "scripts/processCA.js"
      provides: "CA GF operating loader window extended to FY2008-FY2025 (added FY2008-FY2019), General column, thousands"
      contains: "Files-ARD/CAFR"
    - path: "scripts/processCARevenueAcfr.js"
      provides: "CA GF revenue-by-source loader extended to FY2008-FY2025 with P2 clamp"
      contains: "clampForRender"
    - path: ".planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md"
      provides: "Per-FY disposition log for skipped/non-tying CA added years (holes allowed, D-02/D-03)"
      contains: "CA"
  key_links:
    - from: "scripts/processCA.js"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type='operating', p_fiscal_year=<added fy>, keyed (municipality_id, fy, 'operating')"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processCARevenueAcfr.js"
      to: "P2 clamp"
      via: "clampForRender + signed label on negative categories"
      pattern: "clampForRender"
---

<objective>
Deepen California's ACFR window backward by 12 years — extend `scripts/processCA.js` (operating) and `scripts/processCARevenueAcfr.js` (revenue) to cover **FY2008–FY2019** in addition to the existing FY2020–FY2025, reusing the loaders unchanged in shape. The added years live under a DIFFERENT SCO directory (`/Files-ARD/CAFR/cafr{NN}web.pdf`) than the FY2020+ entries (`/Files-ARD/ACFR/`), so both directories coexist in the SOURCES object. All extraction + tie-check happens here via `--dry-run`, NO production DB writes. Live write is Wave 2 (104-04).

Purpose: Retire CA's deeper-history extraction risk at $0. The clean `cafr{NN}web.pdf` pattern is the FY2008 floor (D-01); the FY2002–FY2007 variant-naming years are explicitly deferred. Recon (103-DEEPEN-SOURCES.md) bookend-tied FY2008 ($97,774,378K General Total revenues).
Output: `scripts/processCA.js`, `scripts/processCARevenueAcfr.js` extended for FY2008–FY2019, both `--dry-run` clean, plus any skip recorded in `104-DEEPEN-GAPLOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-CONTEXT.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md
@.planning/followups/ca-acfr-reconciliation.md

<interfaces>
<!-- Existing CA loader shape (read the files; do NOT re-architect). -->
From scripts/processCA.js (operating — EXTEND in place):
  - STATE_NAME='California'; STATE_ABBR='CA'. const SOURCES = { 2020:{url,date}, ..., 2025:{...} } — an EXPLICIT object literal (not Object.fromEntries). The existing FY2020-2025 urls use `https://www.sco.ca.gov/Files-ARD/ACFR/acfr{NN}web.pdf`. ADD keys 2008..2019 using `https://www.sco.ca.gov/Files-ARD/CAFR/cafr{NN}web.pdf` (note CAFR not ACFR dir, cafr not acfr filename; NN = zero-padded 2-digit year, e.g. cafr08web.pdf … cafr19web.pdf). date = `${fy}-06-30` (CA FYE Jun 30) — match the existing entries' date convention.
  - const EXPENDITURES = { <fy>: {...} }; main() has a hardcoded years array [2020,2021,2022,2023,2024,2025] AND srcPayload.fiscal_years [2020..2025] — ADD 2008..2019 to BOTH. Follow the file's existing thousands scaling convention exactly (match the FY2020 block format).
  - validate(fy) ties category sum to printed Total or process.exit(2). flags: --dry-run, --fy <YYYY>.
From scripts/processCARevenueAcfr.js (revenue — same shape): const REVENUE={<fy>:{...}}; clampForRender(amount)=Math.max(amount,0); negative cats labelled `${name} (net loss — shown at 0)`.
From .planning/followups/ca-acfr-reconciliation.md: SCO soft-404 caution — a missing file returns HTTP 200 + an HTML page, NOT a 404. Filter downloads by Content-Type/size (a real ACFR PDF is multi-MB), not HTTP status. Treat an HTML/tiny response as a missing year → skip+log.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CA SOURCES with FY2008-FY2019 under /Files-ARD/CAFR/ (both loaders)</name>
  <files>scripts/processCA.js, scripts/processCARevenueAcfr.js</files>
  <read_first>
    - scripts/processCA.js (the SOURCES object literal, main() years array, srcPayload.fiscal_years — see the existing /Files-ARD/ACFR/ FY2020 entry)
    - scripts/processCARevenueAcfr.js (same)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (CA row: FY2008–FY2019 added, /Files-ARD/CAFR/cafr{NN}web.pdf, thousands, FY2008 $97,774,378K bookend, FY2002-07 optional/deferred)
  </read_first>
  <action>
    In BOTH scripts/processCA.js and scripts/processCARevenueAcfr.js, add SOURCES keys 2008,2009,2010,2011,2012,2013,2014,2015,2016,2017,2018,2019 with url `https://www.sco.ca.gov/Files-ARD/CAFR/cafr${NN}web.pdf` (NN = String(fy).slice(2), e.g. fy2008→'08'→cafr08web.pdf) and date `${fy}-06-30`. Leave the existing 2020..2025 entries (which use /Files-ARD/ACFR/acfr{NN}web.pdf) exactly as-is — both directories coexist (RECON-05 never-overwrite). Add 2008..2019 to the main() years array and srcPayload.fiscal_years in both files.
    Do NOT add FY2002–FY2007 (variant naming cafr06.pdf / 2002_cafr02.pdf) — that is deferred per D-01. Do NOT change validate(), buildTree(), clampForRender(), or the existing thousands scaling.
  </action>
  <verify>
    <automated>grep -c "Files-ARD/CAFR/cafr" scripts/processCA.js && grep -c "Files-ARD/CAFR/cafr" scripts/processCARevenueAcfr.js</automated>
  </verify>
  <done>Both CA loaders list FY2008–FY2019 in SOURCES under /Files-ARD/CAFR/cafr{NN}web.pdf, plus in main() years and srcPayload.fiscal_years; the FY2020–FY2025 /Files-ARD/ACFR/ entries are unchanged; no FY2002–FY2007 keys present.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe + tie-check FY2008-FY2019 GENERAL-column blocks (operating + revenue)</name>
  <files>scripts/processCA.js, scripts/processCARevenueAcfr.js, .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md</files>
  <read_first>
    - scripts/processCA.js (the EXPENDITURES{fy} block format for an existing year, e.g. FY2020)
    - scripts/processCARevenueAcfr.js (the REVENUE{fy} block format)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (CA columns: General | Federal | Transportation | Nonmajor Governmental | Total — read GENERAL with `pdftotext -table`)
    - .planning/followups/ca-acfr-reconciliation.md (SCO soft-404 = HTTP 200 + HTML; filter by Content-Type/size)
  </read_first>
  <action>
    For each FY2008–FY2019: download `https://www.sco.ca.gov/Files-ARD/CAFR/cafr${NN}web.pdf` into _acfr-tmp/ca/. VERIFY the response is a real PDF (multi-MB, Content-Type application/pdf) — a small/HTML response is a soft-404 → skip that year + log it (do NOT extract HTML). Extract the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances with `pdftotext -table` (narrow with -f/-l). Read the GENERAL column (first numeric data column). Transcribe verbatim category names + printed (thousands) figures into EXPENDITURES{fy} (processCA.js) and REVENUE{fy} (processCARevenueAcfr.js), matching the existing FY2020 block format and the file's thousands scaling.
    Tie policy (D-03 exact-else-skip): each retained added FY's category sum MUST tie EXACTLY to the printed Total at validate(); do NOT widen tolerance. A year that fails extraction (incl. soft-404), lacks a PDF, or fails the tie is removed from SOURCES/years and recorded in 104-DEEPEN-GAPLOG.md (state=CA, FY, reason) — holes allowed (D-02). Anchor: FY2008 GENERAL Total revenues = $97,774,378K → $97,774,378,000 after thousands scaling.
    Negative GF categories (CA market-loss years) render via clampForRender + `(net loss — shown at 0)`, root total preserving the net (ACFR-08). $0 spend — pdftotext only, no AI.
  </action>
  <verify>
    <automated>node scripts/processCA.js --dry-run && node scripts/processCARevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both CA loaders run --dry-run; every retained FY2008–FY2019 prints a PASS tie-check (FY2008 revenue ties to $97,774,378,000); soft-404 / non-tying years are absent from SOURCES and listed in 104-DEEPEN-GAPLOG.md with a reason; any negative category prints "(net loss — shown at 0)"; no DB write occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SCO ACFR PDF (or soft-404 HTML) → loader | untrusted/maybe-missing remote response crosses into the extractor |
| loader → treasury.budgets | NOT crossed in this plan (dry-run only); 104-04 crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02-A | Information disclosure | SCO soft-404 (HTTP 200 + HTML) extracted as if a real ACFR | mitigate | verify Content-Type/size is a multi-MB PDF before extracting; HTML/tiny → skip+log |
| T-104-02-B | Tampering | wrong directory/filename (acfr vs cafr, ACFR vs CAFR dir) → 404 or wrong doc | mitigate | added years pinned to /Files-ARD/CAFR/cafr{NN}web.pdf per recon; existing /Files-ARD/ACFR/ entries untouched |
| T-104-02-C | Tampering | a non-tying year force-loaded by loosening tolerance | mitigate | D-03 exact-else-skip — gate NOT widened; failing years go to the gap log |
| T-104-02-D | Information disclosure | wrong fund column extracted (Federal/Transportation instead of General) | mitigate | per-FY tie-to-printed-Total gate; GENERAL column (1st numeric) pinned; FY2008 must tie to $97,774,378,000 |
| T-104-02-SC | Tampering | npm/pip installs | accept | no new packages — edits existing scripts; pdftotext pre-existing |
</threat_model>

<verification>
- `node scripts/processCA.js --dry-run` and `node scripts/processCARevenueAcfr.js --dry-run`: all retained FY2008–FY2019 PASS the tie-check; FY2008 revenue = $97,774,378,000.
- FY2020–FY2025 entries unchanged (diff shows only additions); no FY2002–FY2007 keys.
- 104-DEEPEN-GAPLOG.md lists any skipped CA year + reason (soft-404, tie fail, etc.).
- No row written to treasury.budgets by this plan.
</verification>

<success_criteria>
CA's operating + revenue loaders are extended to FY2008–FY2019 under the /Files-ARD/CAFR/ directory (existing FY2020–FY2025 undisturbed, FY2002–FY2007 deferred), every retained added year passes an exact tie-check on `--dry-run`, soft-404 / non-tying years are skipped + logged, and the P2 clamp is wired. Zero production DB writes. $0 spend.
</success_criteria>

<output>
Create `.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-02-SUMMARY.md` when done — record each added FY's extracted GENERAL-column Total (revenue + operating), the tie result, any soft-404 encountered, any negative category clamped, and the list of any skipped years written to 104-DEEPEN-GAPLOG.md.
</output>
