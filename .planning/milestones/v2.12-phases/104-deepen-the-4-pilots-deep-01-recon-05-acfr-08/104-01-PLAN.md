---
phase: 104-deepen-the-4-pilots
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processNYAcfr.js
  - scripts/processNYRevenueAcfr.js
  - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
autonomous: true
requirements: [DEEP-01, RECON-05, ACFR-08]

must_haves:
  truths:
    - "processNYAcfr.js + processNYRevenueAcfr.js SOURCES/years/fiscal_years arrays extended with FY2003-FY2014 (12 added years) WITHOUT removing or altering the existing FY2015-FY2024 entries"
    - "Each added FY2003-FY2014 General-column block (operating + revenue) is transcribed from the NY ACFR and ties EXACTLY to the printed Total at the loader's validate() gate (printed millions granularity); a year that fails to -table-extract or fails the tie is SKIPPED and logged to 104-DEEPEN-GAPLOG.md, never force-loaded"
    - "NY figures stay millions-scaled (x1,000,000 via the existing UNITS const) for the added years"
    - "Negative GF categories in any added year render via the existing clampForRender + '(net loss — shown at 0)' label, parent total preserved (ACFR-08)"
    - "Both loaders run --dry-run clean with NO production DB writes in this plan"
  artifacts:
    - path: "scripts/processNYAcfr.js"
      provides: "NY GF operating loader window extended to FY2003-FY2024 (added FY2003-FY2014), General column, millions"
      contains: "2003"
    - path: "scripts/processNYRevenueAcfr.js"
      provides: "NY GF revenue-by-source loader extended to FY2003-FY2024 with P2 clamp, millions"
      contains: "clampForRender"
    - path: ".planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md"
      provides: "Per-FY disposition log for skipped/non-tying NY added years (holes allowed, D-02/D-03)"
      contains: "NY"
  key_links:
    - from: "scripts/processNYAcfr.js"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type='operating', p_fiscal_year=<added fy>, keyed (municipality_id, fy, 'operating')"
      pattern: "treasury_sync_budget_tree"
    - from: "scripts/processNYRevenueAcfr.js"
      to: "P2 clamp"
      via: "clampForRender + signed label on negative categories"
      pattern: "clampForRender"
---

<objective>
Deepen New York's ACFR window backward by 12 years — extend `scripts/processNYAcfr.js` (operating) and `scripts/processNYRevenueAcfr.js` (revenue) to cover **FY2003–FY2014** in addition to the existing FY2015–FY2024, reusing the proven v2.11 loaders unchanged in shape. All extraction + transcription + tie-check happens here, proven via `--dry-run` with NO production DB writes. The live write happens in Wave 2 (104-04).

Purpose: Retire NY's deeper-history extraction risk at $0 (pdftotext only, no AI). The recon (103-DEEPEN-SOURCES.md) already bookend-tie-confirmed the window ends; this plan extracts + ties the 12 added years per the exact-tie-else-skip+log policy (D-03/D-02).
Output: `scripts/processNYAcfr.js`, `scripts/processNYRevenueAcfr.js` extended for FY2003–FY2014, both `--dry-run` clean, plus `104-DEEPEN-GAPLOG.md` recording any skipped year.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-CONTEXT.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md

<interfaces>
<!-- Existing NY loader shape (read the files; do NOT re-architect). -->
From scripts/processNYAcfr.js (operating — EXTEND in place):
  - STATE_NAME='New York'; STATE_ABBR='NY'; UNITS = 1_000_000 (NY ACFR prints MILLIONS → ×1,000,000).
  - function nyUrl(fy): already emits `comprehensive-annual-financial-report-{YYYY}.pdf` for fy<=2021 and `annual-comprehensive-financial-report-{YYYY}.pdf` for fy>=2022, under base `https://www.osc.ny.gov/files/reports/finance/pdf/`. Confirm by reading — the FY2003–FY2014 added years all fall in the fy<=2021 (CAFR word-order) branch, so NO nyUrl change should be needed.
  - const SOURCES = Object.fromEntries([2015..2024].map(fy => [fy, {url: nyUrl(fy), date:`${fy}-03-31`}])) — ADD 2003..2014 to this year array.
  - const EXPENDITURES = { <fy>: { total, confidence:'actual', categories:[{name,total}] } } in raw MILLIONS — ADD blocks for 2003..2014.
  - main() has a hardcoded years array [2015..2024] AND a srcPayload.fiscal_years [2015..2024] — ADD 2003..2014 to BOTH.
  - validate(fy): category sum must tie to printed Total at its gate or process.exit(2). buildTree multiplies by UNITS.
  - flags: --dry-run (short-circuits before any RPC/UPDATE) and --fy <YYYY>.
From scripts/processNYRevenueAcfr.js (revenue — same shape):
  - const REVENUE = { <fy>: {...} } in raw MILLIONS; clampForRender(amount)=Math.max(amount,0); negative cats get label `${name} (net loss — shown at 0)` and render at 0 with root total preserving the net.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend NY SOURCES + year arrays to FY2003-FY2014 (both loaders)</name>
  <files>scripts/processNYAcfr.js, scripts/processNYRevenueAcfr.js</files>
  <read_first>
    - scripts/processNYAcfr.js (the operating loader — read nyUrl(), SOURCES, the main() years array, srcPayload.fiscal_years)
    - scripts/processNYRevenueAcfr.js (the revenue loader — same)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (NY row: FY2003–FY2014 added, comprehensive-annual-financial-report-{YYYY}.pdf naming, millions, FY2003 General Total revenues $29,250M bookend)
  </read_first>
  <action>
    In BOTH scripts/processNYAcfr.js and scripts/processNYRevenueAcfr.js, prepend the years 2003,2004,2005,2006,2007,2008,2009,2010,2011,2012,2013,2014 to: (a) the SOURCES year array, (b) the hardcoded years array in main(), and (c) srcPayload.fiscal_years. Leave the existing 2015..2024 entries exactly as-is (never-overwrite — RECON-05). Do NOT change UNITS (stays 1_000_000), nyUrl(), validate(), buildTree(), or clampForRender().
    First confirm nyUrl(2003) resolves to `https://www.osc.ny.gov/files/reports/finance/pdf/comprehensive-annual-financial-report-2003.pdf` (and likewise through 2014) by reading the function; if the fy<=2021 branch already produces this, no nyUrl edit is required. If nyUrl does NOT cover these years, extend its branch to emit the comprehensive-annual word-order naming for fy<=2021. Keep date=`${fy}-03-31` (NY FYE Mar 31).
  </action>
  <verify>
    <automated>node -e "process.argv;" ; grep -q "2003" scripts/processNYAcfr.js && grep -q "2003" scripts/processNYRevenueAcfr.js && echo OK</automated>
  </verify>
  <done>Both NY loaders list FY2003–FY2014 in SOURCES, main() years, and srcPayload.fiscal_years; the FY2015–FY2024 entries are unchanged; nyUrl resolves the added years to the comprehensive-annual-financial-report-{YYYY}.pdf pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe + tie-check FY2003-FY2014 GF General-column blocks (operating + revenue)</name>
  <files>scripts/processNYAcfr.js, scripts/processNYRevenueAcfr.js, .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md</files>
  <read_first>
    - scripts/processNYAcfr.js (the EXPENDITURES{fy} block shape for an existing year, e.g. FY2015 — copy the format verbatim)
    - scripts/processNYRevenueAcfr.js (the REVENUE{fy} block shape)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (NY columns: General | Federal Special Revenue | Other Governmental | Eliminations | Total — read GENERAL column with `pdftotext -table`, NOT -layout)
  </read_first>
  <action>
    For each FY2003–FY2014: download the NY ACFR PDF from nyUrl(fy) into _acfr-tmp/ny/ (skip download if cached). Extract the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances with `pdftotext -table` (narrow with -f/-l to the statement pages; -layout misaligns the columns). Read the GENERAL column (first numeric data column). Transcribe verbatim category names + printed (millions) figures into EXPENDITURES{fy} (expenditures-by-function) in processNYAcfr.js and REVENUE{fy} (revenues-by-source) in processNYRevenueAcfr.js, matching the existing FY2015 block format. This is the State (finance/reports) ACFR — NOT the NYSLRS pension ACFR.
    Tie policy (D-03 exact-else-skip): each added FY's transcribed category sum MUST tie to the printed Total (operating → "Total expenditures"; revenue → "Total revenues") at the loader's validate() gate. Do NOT widen the gate tolerance to admit a year. If a year does not cleanly -table-extract, lacks a resolvable PDF, or fails the tie, REMOVE that year from the SOURCES/years arrays and record it in 104-DEEPEN-GAPLOG.md (state=NY, FY, reason) — holes are allowed (D-02), never force-load. Anchor: FY2003 GENERAL Total revenues = $29,250M → $29,250,000,000 after ×UNITS.
    Negative GF categories (NY has market-loss investment-income years) render via the existing clampForRender + `(net loss — shown at 0)` label, root total preserving the net (ACFR-08). $0 spend — pdftotext only, no AI.
  </action>
  <verify>
    <automated>node scripts/processNYAcfr.js --dry-run && node scripts/processNYRevenueAcfr.js --dry-run</automated>
  </verify>
  <done>Both NY loaders run --dry-run; every retained FY2003–FY2014 prints a PASS tie-check (FY2003 revenue ties to $29,250,000,000); any year that failed extraction/tie is absent from SOURCES and listed in 104-DEEPEN-GAPLOG.md with a reason; any negative category prints "(net loss — shown at 0)"; no DB write occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| downloaded ACFR PDF → loader | untrusted/maybe-missing remote PDF crosses into the extractor |
| loader → treasury.budgets | NOT crossed in this plan (dry-run only); 104-04 crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01-A | Tampering | wrong units (millions read as thousands → 1000× understated) | mitigate | UNITS=1_000_000 untouched; FY2003 must tie to $29,250,000,000; validate() exits 2 otherwise |
| T-104-01-B | Information disclosure | wrong fund column / wrong statement / NYSLRS pension ACFR extracted | mitigate | per-FY tie-to-printed-Total gate; GENERAL column pinned; finance-not-pension URL; non-tying years skipped+logged |
| T-104-01-C | Tampering | a non-tying year force-loaded by loosening tolerance | mitigate | D-03 exact-else-skip — gate tolerance NOT widened; failing years go to the gap log |
| T-104-01-SC | Tampering | npm/pip installs | accept | no new packages — edits existing scripts using vendored @supabase/supabase-js; pdftotext is a pre-existing system tool |
</threat_model>

<verification>
- `node scripts/processNYAcfr.js --dry-run` and `node scripts/processNYRevenueAcfr.js --dry-run`: all retained FY2003–FY2014 PASS the tie-check; FY2003 revenue = $29,250,000,000 (millions confirmed).
- FY2015–FY2024 entries unchanged (diff shows only additions).
- 104-DEEPEN-GAPLOG.md exists and lists any skipped NY year with a reason (or notes "no NY gaps").
- No row written to treasury.budgets by this plan.
</verification>

<success_criteria>
NY's operating + revenue loaders are extended to FY2003–FY2014 (existing FY2015–FY2024 undisturbed), every retained added year passes an exact tie-check on `--dry-run`, non-tying/non-extracting years are skipped + logged, millions scaling holds, and the P2 clamp is wired for negative years. Zero production DB writes. $0 spend.
</success_criteria>

<output>
Create `.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-01-SUMMARY.md` when done — record each added FY's extracted GENERAL-column Total (revenue + operating), the tie result, the millions conversion shown, any negative category clamped, and the list of any skipped years written to 104-DEEPEN-GAPLOG.md.
</output>
