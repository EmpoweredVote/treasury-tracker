---
phase: 104-deepen-the-4-pilots
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/processFLAcfr.js
  - scripts/processFLRevenueAcfr.js
  - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
autonomous: true
requirements: [DEEP-01, RECON-05, ACFR-08]

must_haves:
  truths:
    - "processFLAcfr.js + processFLRevenueAcfr.js SOURCES/years/fiscal_years arrays extended with FY2021 (1 added year) using the same fye-{YYYY}-... pattern, WITHOUT altering the existing FY2022-FY2024 entries"
    - "FY2021 GENERAL FUND block (operating + revenue) is transcribed from the FL ACFR and ties EXACTLY to the printed Total at validate()"
    - "FY2021's negative 'Investment earnings (losses)' line (-$398,287K) fires the P2 clamp — renders at 0 with '(net loss — shown at 0)' label, root total preserving the net (ACFR-08)"
    - "Both loaders run --dry-run clean with NO production DB writes in this plan"
  artifacts:
    - path: "scripts/processFLAcfr.js"
      provides: "FL GF operating loader window extended to FY2021-FY2024 (added FY2021), General Fund column, thousands"
      contains: "2021"
    - path: "scripts/processFLRevenueAcfr.js"
      provides: "FL GF revenue-by-source loader extended to FY2021-FY2024 with P2 clamp firing on FY2021"
      contains: "clampForRender"
  key_links:
    - from: "scripts/processFLRevenueAcfr.js"
      to: "P2 clamp"
      via: "clampForRender + '(net loss — shown at 0)' label on FY2021 negative investment earnings"
      pattern: "clampForRender"
    - from: "scripts/processFLAcfr.js"
      to: "treasury_sync_budget_tree RPC"
      via: "p_dataset_type='operating', p_fiscal_year=2021, keyed (municipality_id, 2021, 'operating')"
      pattern: "treasury_sync_budget_tree"
---

<objective>
Deepen Florida's ACFR window backward by one year — extend `scripts/processFLAcfr.js` (operating) and `scripts/processFLRevenueAcfr.js` (revenue) to add **FY2021** alongside the existing FY2022–FY2024, using the same `fye-{YYYY}-...` URL pattern. FY2021 is the durable FL old-end (FY≤2020 is not durably sourceable — deferred). FY2021 carries a NEGATIVE "Investment earnings (losses)" line (−$398,287K) that MUST fire the P2 clamp (ACFR-08, the OH FY2022 precedent). All extraction + tie-check + clamp confirmation happens here via `--dry-run`, NO production DB writes. Live write is Wave 2 (104-04).

Purpose: Add FL's one durable extra year at $0 and prove the clamp fires honestly. Recon (103-DEEPEN-SOURCES.md) bookend-tied FY2021 ($46,989,188K GF Total revenues).
Output: `scripts/processFLAcfr.js`, `scripts/processFLRevenueAcfr.js` extended for FY2021, both `--dry-run` clean with the clamp confirmed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-CONTEXT.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md

<interfaces>
<!-- Existing FL loader shape (read the files; do NOT re-architect). -->
From scripts/processFLAcfr.js (operating — EXTEND in place):
  - STATE_NAME='Florida'; STATE_ABBR='FL'; UNITS = 1_000 (thousands → dollars).
  - const FL_BASE = 'https://www.myfloridacfo.com/docs-sf/default-source/transparency-docs/cafr'.
  - const SOURCES = Object.fromEntries([2022,2023,2024].map(fy => [fy, {url:`${FL_BASE}/fye-${fy}-state-of-florida-annual-comprehensive-financial-report.pdf`, date:`${fy}-06-30`}])) — ADD 2021 to this year array (same pattern works for FY2021 per recon).
  - const EXPENDITURES = { <fy>: {...} } in raw thousands — ADD a 2021 block. main() years [2022,2023,2024] AND srcPayload.fiscal_years [2022,2023,2024] — ADD 2021 to BOTH.
  - validate(fy) ties to printed Total or exit(2); buildTree ×UNITS. flags: --dry-run, --fy.
From scripts/processFLRevenueAcfr.js (revenue — same shape): const REVENUE={<fy>:{...}}; clampForRender(amount)=Math.max(amount,0); negative cats labelled `${name} (net loss — shown at 0)`, printed at 0, root total preserving the net. A `[Note: ... true value: ... (net loss — shown at 0)]` line prints for each negative cat.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FY2021 to FL SOURCES + year arrays (both loaders)</name>
  <files>scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js</files>
  <read_first>
    - scripts/processFLAcfr.js (FL_BASE, SOURCES year array, main() years, srcPayload.fiscal_years)
    - scripts/processFLRevenueAcfr.js (same)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (FL row: +FY2021, same fye-{YYYY}-... pattern, FY2021 negative investment earnings −$398,287K, FY2021 GF Total revenues $46,989,188K bookend)
  </read_first>
  <action>
    In BOTH scripts/processFLAcfr.js and scripts/processFLRevenueAcfr.js, add 2021 to: the SOURCES year array (currently [2022,2023,2024]), the main() years array, and srcPayload.fiscal_years. The fye-${fy}-state-of-florida-annual-comprehensive-financial-report.pdf pattern already produces the correct FY2021 URL — no FL_BASE/pattern change needed. Leave the FY2022–FY2024 entries unchanged (RECON-05). Do NOT change UNITS (stays 1_000), validate(), buildTree(), or clampForRender().
  </action>
  <verify>
    <automated>grep -q "2021" scripts/processFLAcfr.js && grep -q "2021" scripts/processFLRevenueAcfr.js && echo OK</automated>
  </verify>
  <done>Both FL loaders list FY2021 in SOURCES, main() years, and srcPayload.fiscal_years; FY2022–FY2024 entries unchanged; FY2021 URL resolves via the existing fye-{YYYY}-... pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe FY2021 GENERAL FUND block + confirm tie + P2 clamp</name>
  <files>scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js, .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md</files>
  <read_first>
    - scripts/processFLAcfr.js (the EXPENDITURES{fy} block format for FY2022)
    - scripts/processFLRevenueAcfr.js (the REVENUE{fy} block format for FY2022 + the clampForRender path)
    - .planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-DEEPEN-SOURCES.md (FL FY2021: GENERAL FUND column, negative Investment earnings (losses) −$398,287K, $46,989,188K Total revenues)
  </read_first>
  <action>
    Download the FY2021 FL ACFR from the FY2021 SOURCES url into _acfr-tmp/fl/. Extract the Governmental Funds Statement of Revenues, Expenditures and Changes in Fund Balances with `pdftotext -table`; read the GENERAL FUND column. Transcribe verbatim category names + printed (thousands) figures into EXPENDITURES{2021} (processFLAcfr.js) and REVENUE{2021} (processFLRevenueAcfr.js), matching the existing FY2022 block format. Record the negative "Investment earnings (losses)" line as a real negative value (−398287 in thousands) — do NOT pre-clamp it in the data; the clampForRender path handles render.
    Tie policy (D-03 exact-else-skip): FY2021's category sum MUST tie EXACTLY to the printed Total at validate() (revenue → "Total revenues" $46,989,188K → $46,989,188,000; operating → "Total expenditures"). The negative line is included in the net total per the ACFR. If FY2021 fails to extract or tie, remove it and log to 104-DEEPEN-GAPLOG.md (state=FL, FY2021, reason). Confirm the revenue --dry-run prints the `[Note: Investment earnings (losses) true value: -398,287,000 (net loss — shown at 0)]` line and that the rendered category is 0 while the root total preserves the net (ACFR-08). $0 spend — pdftotext only, no AI.
  </action>
  <verify>
    <automated>node scripts/processFLAcfr.js --dry-run --fy 2021 && node scripts/processFLRevenueAcfr.js --dry-run --fy 2021</automated>
  </verify>
  <done>Both FL loaders run --dry-run for FY2021 with a PASS tie-check (revenue ties to $46,989,188,000); the revenue loader prints the negative Investment earnings "(net loss — shown at 0)" note and renders it at 0 with the root total preserving the net; no DB write occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FL ACFR PDF → loader | untrusted/maybe-missing remote PDF crosses into the extractor |
| loader → treasury.budgets | NOT crossed in this plan (dry-run only); 104-04 crosses it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03-A | Tampering | negative investment-income line dropped or shown as a positive/abs value | mitigate | transcribe the real negative; clampForRender renders at 0 with signed label; root total nets it (ACFR-08); --dry-run note asserted |
| T-104-03-B | Information disclosure | wrong fund column / wrong statement extracted | mitigate | per-FY tie-to-printed-Total gate; GENERAL FUND column pinned; FY2021 must tie to $46,989,188,000 |
| T-104-03-C | Tampering | a non-tying FY2021 force-loaded by loosening tolerance | mitigate | D-03 exact-else-skip — gate NOT widened; failing year goes to gap log |
| T-104-03-SC | Tampering | npm/pip installs | accept | no new packages — edits existing scripts; pdftotext pre-existing |
</threat_model>

<verification>
- `node scripts/processFLAcfr.js --dry-run --fy 2021` and `node scripts/processFLRevenueAcfr.js --dry-run --fy 2021`: FY2021 PASSES the tie-check; revenue = $46,989,188,000.
- Revenue --dry-run prints the Investment earnings "(net loss — shown at 0)" note; rendered category = 0; root total preserves the net.
- FY2022–FY2024 entries unchanged (diff shows only FY2021 additions).
- No row written to treasury.budgets by this plan.
</verification>

<success_criteria>
FL's operating + revenue loaders are extended to add FY2021 (existing FY2022–FY2024 undisturbed), FY2021 passes an exact tie-check on `--dry-run`, and the P2 clamp fires honestly on FY2021's negative investment earnings. Zero production DB writes. $0 spend.
</success_criteria>

<output>
Create `.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-03-SUMMARY.md` when done — record FY2021's extracted GENERAL FUND Total (revenue + operating), the tie result, the clamp behavior on the negative investment-earnings line, and confirm FY2022–FY2024 untouched.
</output>
