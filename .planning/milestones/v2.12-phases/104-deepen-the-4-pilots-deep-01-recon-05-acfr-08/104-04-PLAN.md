---
phase: 104-deepen-the-4-pilots
plan: 04
type: execute
wave: 2
depends_on: ["104-01", "104-02", "104-03"]
files_modified:
  - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
autonomous: true
requirements: [DEEP-01, RECON-05, ACFR-08]

must_haves:
  truths:
    - "The deepened years are live-loaded ONLY for the added FYs (NY FY2003-FY2014, CA FY2008-FY2019, FL FY2021) via per-FY runs — existing v2.11 pilot rows (NY FY2015-24, CA FY2020-25, FL FY2022-24) are NOT re-written"
    - "Every loaded added FY stamps its own per-year ACFR source (source_url, source_date, data_source GAAP-basis label) on its budgets row"
    - "Idempotent never-overwrite: a second per-FY run of an added year produces 0 net change; un-upgraded NASBO states, PA, IL, TX, and existing pilot rows are untouched (RECON-05)"
    - "Years recorded as skipped in 104-DEEPEN-GAPLOG.md are absent from the DB (honest holes) — they are NOT loaded"
    - "Negative-category added years render via the P2 clamp in the live rows (ACFR-08)"
  artifacts:
    - path: ".planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md"
      provides: "Final per-FY load disposition (loaded vs skipped) + idempotency re-run result for the deepened years"
      contains: "loaded"
  key_links:
    - from: "scripts/processNYAcfr.js / processCA.js / processFLAcfr.js"
      to: "treasury.budgets"
      via: "treasury_sync_budget_tree RPC keyed (municipality_id, fy, dataset_type) — added FYs only"
      pattern: "treasury_sync_budget_tree"
    - from: "added-FY budgets rows"
      to: "data_source GAAP-basis label + per-year source_url/source_date"
      via: "loader post-RPC UPDATE step"
      pattern: "source_url"
---

<objective>
Live-load the deepened pilot years into `treasury.budgets`, scoped strictly to the added FYs proven dry-run-clean in Wave 1 — **NY FY2003–FY2014, CA FY2008–FY2019, FL FY2021** (operating + revenue each) — leaving every existing row untouched. Prove idempotent never-overwrite (RECON-05), confirm each added FY stamps its per-year ACFR source + GAAP basis label, confirm the P2 clamp persists on negative-category years (ACFR-08), and verify the deepened windows + honest holes in the DB.

Purpose: Flip the switch on the extraction work Wave 1 retired at $0. Per-FY runs keep the blast radius to the added years only — the loaders' RPC is keyed (muni, fy, dataset), so existing pilot rows and un-upgraded NASBO states are structurally out of scope.
Output: deepened rows live in treasury.budgets; `104-DEEPEN-GAPLOG.md` finalized with loaded-vs-skipped + idempotency re-run result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-CONTEXT.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md

<interfaces>
<!-- The Wave-1 loaders are now extended + dry-run-clean. This plan RUNS them live, scoped to added FYs. -->
From scripts/processNYAcfr.js, scripts/processNYRevenueAcfr.js, scripts/processCA.js, scripts/processCARevenueAcfr.js, scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js:
  - Each accepts `--fy <YYYY>` to load exactly one fiscal year, and runs WITHOUT --dry-run to write live.
  - Per FY: validate() tie-gate (exit 2 on mismatch) → treasury_sync_budget_tree RPC (p_data_source_id, p_fiscal_year, p_dataset_type, p_total, p_tree, p_row_count) → SELECT the budgets row → UPDATE {source_url, source_date, data_source} with the per-year ACFR source + GAAP-basis label.
  - Env: loadEnv() reads ../.env.local then ../.env (run from repo root). Source .env first if needed.
From .planning/phases/104-.../104-DEEPEN-GAPLOG.md (written by Wave 1): the authoritative list of which added FYs tied (load these) vs were skipped (do NOT load).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load NY FY2003-FY2014 (operating + revenue), added FYs only</name>
  <files>(runs scripts/processNYAcfr.js, scripts/processNYRevenueAcfr.js against live DB)</files>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md (which NY years tied — load only those)
    - scripts/processNYAcfr.js (the --fy + post-RPC source-stamp UPDATE path)
  </read_first>
  <action>
    Source .env first. For each NY added year FY2003–FY2014 that is NOT in the gap log's skipped list, run `node scripts/processNYAcfr.js --fy <YYYY>` then `node scripts/processNYRevenueAcfr.js --fy <YYYY>` (live, no --dry-run). Do NOT run the loader without --fy (that would re-touch FY2015–FY2024 existing rows). Each run must: pass validate(), write via treasury_sync_budget_tree, and UPDATE the row's source_url/source_date/data_source ("New York State ACFR — General Fund [Revenue] (FY${fy} actual, GAAP basis)"). If a year's live run fails the tie unexpectedly, abort that year and note it — do NOT force-write.
  </action>
  <verify>
    <automated>echo "NY FY2003 row existence + source-stamp verified in Task 4 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <done>Each tied NY FY2003–FY2014 has operating + revenue rows in treasury.budgets, millions-scaled, each stamped with its per-year ACFR source_url/source_date + GAAP-basis data_source; NY FY2015–FY2024 rows unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load CA FY2008-FY2019 (operating + revenue), added FYs only</name>
  <files>(runs scripts/processCA.js, scripts/processCARevenueAcfr.js against live DB)</files>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md (which CA years tied — load only those)
    - scripts/processCA.js (the --fy + post-RPC source-stamp UPDATE path)
  </read_first>
  <action>
    Source .env first. For each CA added year FY2008–FY2019 NOT in the gap log's skipped list, run `node scripts/processCA.js --fy <YYYY>` then `node scripts/processCARevenueAcfr.js --fy <YYYY>` (live). Do NOT run without --fy (would re-touch FY2020–FY2025). Each run stamps the per-year /Files-ARD/CAFR/ source_url + GAAP-basis data_source ("California State ACFR — General Fund [Revenue] (FY${fy} actual, GAAP basis)"). Abort+note any year that fails the live tie; never force-write.
  </action>
  <verify>
    <automated>node -e "console.log('CA FY2008-2019 row + source verification consolidated in Task 4 DB query')"</automated>
  </verify>
  <done>Each tied CA FY2008–FY2019 has operating + revenue rows in treasury.budgets, thousands-scaled, each stamped with its per-year /Files-ARD/CAFR/ source + GAAP-basis data_source; CA FY2020–FY2025 rows unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Live-load FL FY2021 (operating + revenue) with P2 clamp</name>
  <files>(runs scripts/processFLAcfr.js, scripts/processFLRevenueAcfr.js against live DB)</files>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md (confirm FL FY2021 tied, not skipped)
    - scripts/processFLRevenueAcfr.js (the clampForRender path + source-stamp UPDATE)
  </read_first>
  <action>
    Source .env first. If FL FY2021 tied in Wave 1 (not in the skipped list), run `node scripts/processFLAcfr.js --fy 2021` then `node scripts/processFLRevenueAcfr.js --fy 2021` (live). The revenue row's negative Investment earnings category persists clamped to 0 with the "(net loss — shown at 0)" label, the root total preserving the net (ACFR-08). Stamp the per-year fye-2021 source_url + GAAP-basis data_source. Do NOT run without --fy (would re-touch FY2022–FY2024).
  </action>
  <verify>
    <automated>node -e "console.log('FL FY2021 row + clamp + source verification in Task 4 DB query')"</automated>
  </verify>
  <done>FL FY2021 has operating + revenue rows in treasury.budgets stamped with the fye-2021 ACFR source + GAAP-basis data_source; the negative investment-earnings category renders at 0 with the signed-magnitude label and the root total preserves the net; FL FY2022–FY2024 rows unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: Idempotency + never-overwrite + window verification (DB query)</name>
  <files>.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md</files>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
    - scripts/processNYAcfr.js (confirm the RPC is keyed (muni, fy, dataset) — basis for the never-overwrite claim)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative added FY per state live a second time (e.g. NY --fy 2003, CA --fy 2008, FL --fy 2021) and confirm 0 net change (same total, same row_count, same source — an UPDATE-in-place with identical data). Query treasury.budgets (via mcp__supabase-local__execute_sql) to confirm: (a) each loaded added FY is present for NY/CA/FL with a GAAP-basis data_source + non-null source_url/source_date; (b) skipped years from the gap log are ABSENT (honest holes); (c) existing pilot rows (NY FY2015-24, CA FY2020-25, FL FY2022-24), TX (all), and un-upgraded NASBO states + PA + IL are unchanged (spot-check row counts + a couple of data_source labels). Append a "Load Disposition" section to 104-DEEPEN-GAPLOG.md: per state, the FYs loaded, the FYs skipped (+reason), and the idempotency re-run result.
    Do NOT load any year beyond the added windows. Do NOT touch PA/IL (Phase 105) or any other state.
  </action>
  <verify>
    <automated>echo "DB verification via execute_sql; load disposition appended to 104-DEEPEN-GAPLOG.md"</automated>
  </verify>
  <done>A re-run of a representative added FY per state produces 0 net change; the DB query confirms loaded added FYs present + GAAP-sourced, skipped years absent, and existing/other-state rows unchanged; 104-DEEPEN-GAPLOG.md has a finalized Load Disposition section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets / data_sources | this plan crosses it — live writes scoped to added FYs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-04-A | Tampering | running a loader without --fy → re-writes/disturbs existing FY2015-24 / FY2020-25 / FY2022-24 rows | mitigate | every live run is scoped with --fy to an added year only; never-overwrite verified in Task 4 |
| T-104-04-B | Tampering | loading a year that was supposed to be skipped (gap-log hole) | mitigate | Tasks 1-3 load only FYs absent from the gap log's skipped list; Task 4 asserts skipped years are absent in DB |
| T-104-04-C | Repudiation | an added-FY row shown without a source | mitigate | each run UPDATEs source_url + source_date + GAAP-basis data_source; Task 4 asserts non-null sources |
| T-104-04-D | Tampering | accidental write to PA/IL/TX/other states | mitigate | loaders resolve only their own state node by name+state+entity_type='state'; Task 4 spot-checks other states unchanged |
| T-104-04-E | Tampering | negative-category year stored unclamped/abs in the live row | mitigate | clampForRender path carries through to the live row; Task 3/4 confirm 0-render + signed label + net root total |
| T-104-04-SC | Tampering | npm/pip installs | accept | no new packages — runs existing scripts; DB read via existing supabase tooling |
</threat_model>

<verification>
- Re-run NY --fy 2003, CA --fy 2008, FL --fy 2021 live a second time → 0 net change (idempotent never-overwrite).
- treasury.budgets query: loaded added FYs (NY 2003-14, CA 2008-19, FL 2021, minus gap-log skips) present with GAAP-basis data_source + non-null source_url/source_date.
- Gap-log skipped years ABSENT from the DB (honest holes).
- Existing pilot rows (NY 2015-24, CA 2020-25, FL 2022-24), TX, un-upgraded NASBO states, PA, IL unchanged.
- FL FY2021 negative investment-earnings category renders 0 + signed label, root total nets it.
- 104-DEEPEN-GAPLOG.md has a finalized Load Disposition section.
</verification>

<success_criteria>
The deepened pilot years (NY FY2003-14, CA FY2008-19, FL FY2021 — minus any gap-log skips) are live in treasury.budgets, each per-year ACFR-sourced + GAAP-basis-labelled, the P2 clamp persists on negative years, idempotent never-overwrite is proven (0-change re-run), and all existing pilot rows + TX + un-upgraded NASBO states + PA/IL are demonstrably untouched. Hands the deepened cohort to Phase 106 for independent re-derivation + UAT.
</success_criteria>

<output>
Create `.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-04-SUMMARY.md` when done — record per-state loaded FYs + totals, skipped FYs (from the gap log), the idempotency re-run result, the DB verification (existing/other-state rows unchanged), and the FL FY2021 clamp confirmation.
</output>
