---
phase: 105-pa-il-acfr-upgrade
plan: 03
type: execute
wave: 2
depends_on: ["105-01", "105-02"]
files_modified:
  - .planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md
autonomous: true
requirements: [ACFR-06, ACFR-07, ACFR-08, RECON-05]

must_haves:
  truths:
    - "PA + IL state nodes show ACFR-sourced GF revenue-by-source (dataset_type='revenue') AND GAAP spending-by-function (dataset_type='operating'), each FY tying to the ACFR GF column total, every row GAAP-basis-labelled + per-year source_url/source_date stamped"
    - "Each state's pre-existing NASBO operating rows (PA FY2023-24, IL FY2023-24) are REPLACED in place by the ACFR operating rows (same (muni, fy, 'operating') key) — not duplicated, not left alongside a stale NASBO label"
    - "Idempotent never-overwrite: a second live run of a representative FY per state produces 0 net change (Loaded 0 rows); un-upgraded NASBO states, the CA/TX/NY/FL ACFR rows, and the deepened pilot rows are all untouched (RECON-05)"
    - "The accept-and-relabel scope divergence (PA ~2.0×, IL ~1.5× NASBO) is confirmed at load: the loaded GF totals are recorded against NASBO and the GAAP basis label is verified on the live rows (D-04) — the node is not silently inflated"
    - "Any negative GENERAL FUND category year renders via the P2 clamp in the live rows (clamped to 0, signed magnitude in label, parent total nets it) (ACFR-08)"
    - "Money In auto-enables on PA + IL once the revenue rows land (data-driven, no frontend change)"
  artifacts:
    - path: ".planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md"
      provides: "Per-state per-FY load disposition (loaded vs skipped), NASBO-replacement confirmation, accept-relabel divergence record, idempotency re-run result, DB verification of untouched cohort"
      contains: "Load Disposition"
  key_links:
    - from: "scripts/processPAAcfr.js / processPARevenueAcfr.js / processILAcfr.js / processILRevenueAcfr.js"
      to: "treasury.budgets (PA + IL state nodes)"
      via: "treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) — NASBO operating replaced in place, revenue inserted new"
      pattern: "treasury_sync_budget_tree"
    - from: "loaded PA/IL budgets rows"
      to: "GAAP-basis data_source + per-year source_url/source_date"
      via: "loader post-RPC source-stamp UPDATE"
      pattern: "source_url"
---

<objective>
Live-load the PA + IL ACFR loaders built in Wave 1 into `treasury.budgets` — running each loader (operating + revenue) WITHOUT `--dry-run` across its dry-run-tied FY window (PA FY2016–FY2025, IL FY2021–FY2025) — so the PA + IL state nodes show ACFR-sourced GF revenue-by-source + GAAP spending-by-function. Confirm the NASBO operating rows (PA/IL FY2023-24) are replaced in place (same (muni, fy, 'operating') key, RECON-05), confirm the accept-and-relabel scope divergence at load (PA ~2.0×, IL ~1.5× NASBO — D-04, not a silent inflation), confirm the P2 clamp persists on any negative-category year (ACFR-08), prove idempotent never-overwrite (0-change re-run), and verify in the DB that the existing CA/TX/NY/FL + deepened-pilot + un-upgraded-NASBO rows are untouched.

Purpose: Flip the switch on the Wave-1 extraction at $0. The RPC is keyed (muni, fy, dataset_type) so the blast radius is PA + IL only; operating REPLACES NASBO in place, revenue is net-new. Money In auto-enables data-drivenly once revenue lands.
Output: PA + IL ACFR rows live in treasury.budgets; `105-PA-IL-LOADLOG.md` with the per-state load disposition, NASBO-replacement + accept-relabel + idempotency + untouched-cohort confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/103-recon-deeper-history-urls-pa-il-acfr-source-location-recon-0/103-PA-IL-SOURCES.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-04-PLAN.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-VERIFICATION.md

<interfaces>
<!-- Wave-1 loaders, now dry-run-tied, RUN live here scoped per-FY. -->
From scripts/processPAAcfr.js, processPARevenueAcfr.js, processILAcfr.js, processILRevenueAcfr.js (built in 105-01 / 105-02):
  - Each accepts `--fy <YYYY>` (one FY) or no flag (full window), and runs WITHOUT --dry-run to write live.
  - Per FY: validate() tie-gate (exit 2 on mismatch) → treasury_sync_budget_tree RPC keyed (muni, fy, dataset_type) → SELECT the budgets row → UPDATE {source_url, source_date, data_source = GAAP-basis label}.
  - operating loaders write dataset_type='operating' (REPLACES the same-key NASBO row in place); revenue loaders write dataset_type='revenue' (net-new, no prior PA/IL revenue row).
  - Env: loadEnv() reads ../.env.local then ../.env (run from repo root). Source .env first.
NASBO baseline (from 104-VERIFICATION.md): PA + IL each currently have 2 NASBO operating rows (FY2023–24, data_source 'NASBO State Expenditure Report') and NO revenue rows. PA NASBO GF FY2023 ~$40.8B / FY2024 ~$44.864B; IL NASBO GF FY2023 ~$43.693B / FY2024 ~$48.563B (loadStateGF.mjs controlTotalGF). The ACFR GF totals will be materially larger (PA ~$95B/$91B, IL ~$74B/$78B) — that is the expected TX-trap divergence (federal/intergovernmental inside the GAAP GF), accepted-and-relabelled per D-04.
DB access for verification: mcp__supabase-local__execute_sql (per feedback_supabase_migration_mcp — no manual Dashboard).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load PA (operating + revenue) across the tied FY window, NASBO replaced in place</name>
  <files>(runs scripts/processPAAcfr.js, scripts/processPARevenueAcfr.js against live DB)</files>
  <read_first>
    - .planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-01-SUMMARY.md (the PA FY window actually transcribed + any honest holes)
    - scripts/processPAAcfr.js + scripts/processPARevenueAcfr.js (the --fy + post-RPC source-stamp path; confirm RPC keyed (muni, fy, dataset))
  </read_first>
  <action>
    Source .env first. Before writing, query treasury.budgets (mcp__supabase-local__execute_sql) for the PA state node's current rows and RECORD the pre-load NASBO operating rows (PA FY2023, FY2024: total + data_source) — this is the never-overwrite baseline and the accept-relabel comparator. Then for each PA FY present in 105-01-SUMMARY (FY2016–FY2025 minus honest holes), run `node scripts/processPAAcfr.js --fy <YYYY>` then `node scripts/processPARevenueAcfr.js --fy <YYYY>` (live, no --dry-run). Each run must pass validate(), write via treasury_sync_budget_tree (operating REPLACES the NASBO row at (PA, fy, 'operating'); revenue inserts net-new at (PA, fy, 'revenue')), and stamp source_url/source_date/data_source (GAAP basis label). If a year's live run fails the tie unexpectedly, abort that year + note it — never force-write. Record the loaded PA GF operating + revenue totals against the pre-load NASBO totals to confirm the accept-and-relabel divergence (~2.0×) is the expected federal-inside-GAAP-GF mechanism (D-04), NOT a transcription error inflating the node.
  </action>
  <verify>
    <automated>echo "PA row existence + NASBO-replacement + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, treasury.budgets for the PA node has, for each loaded FY, an operating row with data_source "Pennsylvania State ACFR — General Fund (FY{fy} actual, GAAP basis)" (NOT "NASBO State Expenditure Report") and a revenue row "…General Fund Revenue…" — both with non-null source_url/source_date.
    - PA FY2024 revenue row total = 91,293,027,000 and FY2023 = 95,231,042,000 (thousands ×1000); PA FY2023/FY2024 operating rows tie to their printed General-Fund Total expenditures.
    - The PA FY2023 + FY2024 NASBO operating rows are GONE (replaced in place, not duplicated): exactly ONE operating row per (PA, fy), now GAAP-labelled.
    - The pre-load NASBO totals + the loaded ACFR GF totals are both recorded in the load log with the ~2.0× divergence explained (federal/intergovernmental inside the GAAP GF — D-04 accept-and-relabel).
  </acceptance_criteria>
  <done>Each transcribed PA FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-basis-labelled + per-year-sourced; the FY2023/FY2024 NASBO operating rows are replaced in place (no duplicates); the accept-relabel divergence is recorded against the pre-load NASBO totals.</done>
</task>

<task type="auto">
  <name>Task 2: Live-load IL (operating + revenue) across the tied FY window, NASBO replaced in place, P2 clamp persisted</name>
  <files>(runs scripts/processILAcfr.js, scripts/processILRevenueAcfr.js against live DB)</files>
  <read_first>
    - .planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-02-SUMMARY.md (the IL FY window actually transcribed + any honest holes, e.g. FY2022)
    - scripts/processILAcfr.js + scripts/processILRevenueAcfr.js (the --fy + clampForRender + source-stamp path)
  </read_first>
  <action>
    Source .env first. Query treasury.budgets for the IL node's current rows and RECORD the pre-load NASBO operating rows (IL FY2023, FY2024: total + data_source) as the never-overwrite baseline + accept-relabel comparator. Then for each IL FY present in 105-02-SUMMARY (FY2021–FY2025 minus honest holes), run `node scripts/processILAcfr.js --fy <YYYY>` then `node scripts/processILRevenueAcfr.js --fy <YYYY>` (live). Operating REPLACES the NASBO row at (IL, fy, 'operating') for FY2023/FY2024; FY2021/FY2022/FY2025 operating + all revenue are net-new. Each run stamps source_url/source_date/data_source (GAAP basis label). If any FY shows a negative GENERAL FUND category, confirm it persists clamped to 0 with the "(net loss — shown at 0)" label and the root total nets it (ACFR-08). Abort+note any year failing the live tie; never force-write. Record loaded IL GF totals against pre-load NASBO totals to confirm the ~1.5× accept-relabel divergence (federal inside GAAP GF — D-04).
  </action>
  <verify>
    <automated>echo "IL row existence + NASBO-replacement + clamp + source-stamp verified in Task 3 via mcp__supabase-local execute_sql"</automated>
  </verify>
  <acceptance_criteria>
    - After load, the IL node has, for each loaded FY, an operating row "Illinois State ACFR — General Fund (FY{fy} actual, GAAP basis)" and a revenue row "…General Fund Revenue…", both non-null source_url/source_date.
    - IL FY2025 revenue row total = 78,342,927,000 and FY2023 = 73,827,795,000.
    - IL FY2023 + FY2024 NASBO operating rows are GONE (replaced in place); exactly ONE operating row per (IL, fy), GAAP-labelled; no "NASBO" data_source remains on the IL node.
    - Any negative IL GENERAL FUND category year renders at 0 with the signed-magnitude label, root total nets it.
    - The ~1.5× accept-relabel divergence is recorded against the pre-load NASBO totals (federal inside GAAP GF — D-04).
  </acceptance_criteria>
  <done>Each transcribed IL FY has operating + revenue rows in treasury.budgets, thousands-scaled, GAAP-basis-labelled + per-year-sourced; FY2023/FY2024 NASBO operating rows replaced in place; any negative category clamped per ACFR-08; the accept-relabel divergence recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Idempotency + never-overwrite + Money-In + cohort-untouched verification (DB query)</name>
  <files>.planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md</files>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-04-PLAN.md (the idempotency-proof + DB-verification recipe to mirror)
    - scripts/processPAAcfr.js (confirm RPC keyed (muni, fy, dataset) — the never-overwrite basis)
  </read_first>
  <action>
    Idempotency proof: re-run ONE representative loaded FY per state per dataset live a second time (e.g. PA --fy 2024 operating + revenue, IL --fy 2025 operating + revenue) and confirm 0 net change (same total, same row_count, same source — UPDATE-in-place with identical data → "Loaded 0 rows"). Query treasury.budgets via mcp__supabase-local__execute_sql to confirm: (a) every loaded PA/IL FY has BOTH an operating + a revenue row, GAAP-basis-labelled, non-null source_url/source_date; (b) PA/IL have NO remaining "NASBO State Expenditure Report" operating rows (NASBO fully replaced in place — exactly one operating row per (state, fy)); (c) honest-hole FYs from the SUMMARYs are ABSENT; (d) Money In auto-enable — PA + IL each have ≥1 dataset_type='revenue' row (the data-driven trigger for the v2.11 Money In view + ?dataset=revenue deep-link); (e) the cohort is untouched: CA/TX/NY/FL ACFR rows + deepened-pilot windows (NY 2003-24, CA 2008-25, FL 2021-24, TX 2015-24) unchanged, and a sample un-upgraded NASBO state (e.g. Georgia) + a prior ACFR state (e.g. Ohio) unchanged — spot-check row counts + a couple of data_source labels. Append a "Load Disposition" section to 105-PA-IL-LOADLOG.md: per state, FYs loaded, FYs skipped (+reason), the pre-load-NASBO-vs-loaded-ACFR totals with the accept-relabel divergence note, the NASBO-replacement confirmation, and the idempotency re-run result. Do NOT load any year beyond the tied windows; do NOT touch any state other than PA/IL.
  </action>
  <verify>
    <automated>echo "DB verification via mcp__supabase-local execute_sql; Load Disposition appended to 105-PA-IL-LOADLOG.md"</automated>
  </verify>
  <acceptance_criteria>
    - A second live run of PA --fy 2024 (operating + revenue) and IL --fy 2025 (operating + revenue) each reports "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
    - DB query: every loaded PA/IL FY has both an operating + revenue row, all GAAP-basis-labelled with non-null source_url/source_date; PA/IL have ZERO remaining "NASBO" operating rows; exactly one operating row per (state, fy).
    - PA + IL each have ≥1 revenue row (Money In auto-enable confirmed data-drivenly).
    - Honest-hole FYs (from 105-01/105-02 SUMMARYs) are ABSENT from the DB.
    - CA/TX/NY/FL ACFR + deepened-pilot rows unchanged; Georgia (NASBO) + Ohio (prior ACFR) unchanged (row counts + sample labels match the 104-VERIFICATION baseline).
    - 105-PA-IL-LOADLOG.md has a finalized "Load Disposition" section incl. the accept-relabel divergence record + NASBO-replacement confirmation + idempotency result.
  </acceptance_criteria>
  <done>A representative-FY re-run per state produces 0 net change; the DB query confirms PA/IL fully ACFR-sourced (operating + revenue, GAAP-labelled, NASBO replaced in place, Money In enabled), honest holes absent, and the rest of the cohort unchanged; 105-PA-IL-LOADLOG.md has a finalized Load Disposition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| loader → treasury.budgets / data_sources | this plan crosses it — live writes scoped to PA + IL nodes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03-A | Tampering | NASBO operating row left alongside the ACFR row (duplicate) or ACFR row inserted under a new key instead of replacing NASBO in place → two operating rows / stale NASBO label | mitigate | RPC keyed (muni, fy, 'operating') → UPDATE-in-place; Task 3 asserts exactly one operating row per (state, fy) and ZERO remaining NASBO labels |
| T-105-03-B | Tampering | accidental write to a state other than PA/IL (CA/TX/NY/FL ACFR rows, deepened pilots, un-upgraded NASBO) | mitigate | each loader resolves only its own node by name+state+entity_type='state'; Task 3 spot-checks CA/TX/NY/FL + Georgia + Ohio unchanged vs the 104-VERIFICATION baseline |
| T-105-03-C | Repudiation | a loaded PA/IL row shown without a source | mitigate | each run UPDATEs source_url + source_date + GAAP-basis data_source; Task 3 asserts non-null sources on every loaded row |
| T-105-03-D | Tampering | negative-category year stored unclamped/abs in the live row | mitigate | clampForRender carries to the live row; Task 2/3 confirm 0-render + signed label + net root total (ACFR-08) |
| T-105-03-E | Information Disclosure | silently inflating PA (~2.0×) / IL (~1.5×) without recording the divergence misleads the citizen | mitigate | Tasks 1–2 record pre-load NASBO vs loaded ACFR totals + the federal-inside-GAAP-GF explanation; the GAAP basis label is asserted on the live rows (D-04 accept-and-relabel) |
| T-105-03-F | Tampering | loading a year beyond the tied window or a non-tying honest-hole FY | mitigate | Tasks 1–2 load only FYs present in the SUMMARYs; validate() exit-2 gate; Task 3 asserts honest holes absent |
| T-105-03-SC | Tampering | npm/pip installs | accept | no new packages — runs existing Wave-1 scripts; DB read via existing mcp__supabase-local tooling |
</threat_model>

<verification>
- Re-run PA --fy 2024 + IL --fy 2025 (operating + revenue) live a second time → "Loaded 0 rows" / 0 net change (idempotent never-overwrite).
- treasury.budgets: every loaded PA/IL FY has operating + revenue rows, GAAP-basis-labelled, non-null source_url/source_date; PA FY2024 revenue 91,293,027,000 / FY2023 95,231,042,000; IL FY2025 revenue 78,342,927,000 / FY2023 73,827,795,000.
- PA/IL have ZERO remaining NASBO operating rows; exactly one operating row per (state, fy) — NASBO replaced in place (RECON-05).
- PA + IL each have ≥1 revenue row → Money In auto-enabled (data-driven, no frontend change).
- Accept-relabel divergence (PA ~2.0×, IL ~1.5× NASBO) recorded against pre-load NASBO totals (D-04).
- Any negative GENERAL FUND category renders 0 + signed label, root total nets it (ACFR-08).
- CA/TX/NY/FL ACFR + deepened-pilot rows + Georgia (NASBO) + Ohio (prior ACFR) unchanged.
- 105-PA-IL-LOADLOG.md has a finalized Load Disposition section.
</verification>

<success_criteria>
PA + IL state nodes are live on full State-ACFR GAAP (revenue-by-source + GAAP spending-by-function), each transcribed FY ACFR-sourced + GAAP-basis-labelled + per-year-stamped, the NASBO operating rows replaced in place (RECON-05), the accept-and-relabel scope divergence recorded at load (D-04, not a silent inflation), the P2 clamp persisting on any negative year (ACFR-08), idempotent never-overwrite proven (0-change re-run), Money In auto-enabled on PA/IL, and the rest of the cohort (CA/TX/NY/FL + deepened pilots + un-upgraded NASBO) demonstrably untouched. Hands PA + IL to Phase 106 for independent re-derivation + cohort audit + Chris UAT.
</success_criteria>

<output>
Create `.planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-03-SUMMARY.md` when done — record per-state loaded FYs + totals, skipped FYs (honest holes), the NASBO-replacement confirmation, the pre-load-NASBO-vs-loaded-ACFR accept-relabel divergence, the idempotency re-run result, the Money-In auto-enable confirmation, the DB verification (rest of cohort unchanged), and any negative-category clamp confirmation.
</output>
