---
phase: 106-verification-source-chain-audit-uat-ver-03-ver-04
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase106-cohort-audit.mjs
  - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-COHORT-AUDIT.md
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "The full 50-node state cohort source-chain audit is clean: 0 NULL-basis, 0 fragile/residue, 0 out-of-window, 0 dup, 0 orphan"
    - "Every displayed state-node row carries a basis label (data_source + source_url + source_date)"
    - "The deepened pilot windows (CA FY2008-2025, NY FY2003-2024, FL FY2021-2024) and the new PA (FY2016-2025) + IL (FY2021-2025) windows pass the out-of-window invariant against their actual loaded bounds"
    - "Un-upgraded NASBO states still pass — NASBO provenance, operating-only, untouched"
    - "Re-running a representative v2.12 loader produces 0 net change (idempotency)"
    - "Every logged Phase-104 deepening hole matches what is actually missing in the DB and renders honestly (D-06)"
  artifacts:
    - path: "scripts/verify-phase106-cohort-audit.mjs"
      provides: "50-node cohort source-chain audit adapted for the v2.12-augmented windows"
      contains: "WINDOW_BOUNDS"
    - path: ".planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-COHORT-AUDIT.md"
      provides: "The cohort audit report: per-invariant PASS/FAIL, idempotency result, hole-verdict reconciliation"
  key_links:
    - from: "scripts/verify-phase106-cohort-audit.mjs"
      to: "treasury.budgets + treasury.data_sources"
      via: "read-only invariant queries across all entity_type=state nodes"
      pattern: "budgets"
    - from: "106-COHORT-AUDIT.md hole-verdict section"
      to: "104-DEEPEN-GAPLOG.md"
      via: "reconcile logged holes against actual DB gaps"
      pattern: "gap"
---

<objective>
Run the full 50-node state cohort source-chain audit on the v2.12-augmented cohort (VER-03 parts b+c): assert the Phase 102 invariant set (0 NULL / fragile / residue / out-of-window / dup / orphan; every row basis-labelled; un-upgraded NASBO states untouched) over the deepened CA/NY/FL windows + the new PA/IL nodes, confirm loader idempotency (0-change re-run), and reconcile the logged Phase-104 deepening holes against what is actually missing in the DB (D-06).

Purpose: Prove the whole cohort's source chain stayed clean and residue-free after the v2.12 loads — the Phase 102 7/7-invariant precedent re-run on the larger surface.
Output: `scripts/verify-phase106-cohort-audit.mjs` + `106-COHORT-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md
@.planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md
@.planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md
@.planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-VERIFICATION.md
@scripts/verify-phase102-cohort-audit.mjs

<interfaces>
<!-- verify-phase102-cohort-audit.mjs is the structural template (7 invariants INV-1..INV-7,
     read-only, exit 0/2). Reuse it. The Phase 106 changes are window bounds + PA/IL + a hole-verdict step. -->

WINDOW_BOUNDS must be UPDATED for the v2.12 loads (the 102 bounds are now stale for the deepened pilots):
  CA  FY2008–FY2025   (was 2020–2025 in 102 — deepened in Phase 104)
  NY  FY2003–FY2024   (was 2015–2024 in 102 — deepened in Phase 104)
  FL  FY2021–FY2024   (was 2022–2024 in 102 — deepened in Phase 104)
  TX  FY2015–FY2024   (unchanged)
  MN  FY2008–FY2025 / OH FY2020–FY2025 / VA FY2022–FY2025 (unchanged)
  PA  FY2016–FY2025   (NEW — Phase 105)
  IL  FY2021–FY2025   (NEW — Phase 105)
  All other NASBO states: FY2023–FY2024

ACFR-GAAP-labelled states (INV-6 must now include PA + IL alongside CA/TX/NY/FL/MN/OH/VA):
  PA + IL operating+revenue rows must carry an ACFR-GAAP provenance label ("...State ACFR — General Fund (FY{fy} actual, GAAP basis)").

Live DB municipality_ids: PA = d4a4aadc-f91e-45e4-852f-2cf21e177de5 ; IL = ac8b3dee-b431-48d0-9f59-deea46c85948.

Row-count expectations (from load logs) to confirm in the report:
  CA 18+18 (op+rev FY2008-2025); NY 22 op + 22 rev (FY2003-2024); FL 4 op + 4 rev (FY2021-2024);
  PA 10 op + 10 rev (FY2016-2025); IL 5 op + 5 rev (FY2021-2025).
  GA (NASBO control): 2 rows, NASBO label, operating-only — untouched.

Idempotency: re-running processPAAcfr.js / processILAcfr.js (or revenue variants) for one representative FY must report "Loaded 0 rows" (never-overwrite via treasury_sync_budget_tree). Source-safety: NEVER use treasury_sync_city_budget (not source-safe — overwrites + keeps stale label).

D-06 hole-verdict: 104-DEEPEN-GAPLOG.md records 0 gaps (25/25 added FYs retained). The audit must CONFIRM the DB matches — i.e. the loaded windows are contiguous within their bounds with no unrecorded gaps, and any FY absent from the DB (e.g. NY ≤FY2002, CA FY2002-2007, FL ≤FY2020) is absent BY DESIGN per the gap log's "no durable URL / deferred" disposition, and the live node renders the non-contiguous overall range honestly (per-FY rows, basis label + source chip, no interpolation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt the cohort audit for the v2.12-augmented windows and run the 7-invariant set</name>
  <read_first>
    - scripts/verify-phase102-cohort-audit.mjs (template — INV-1..INV-7, WINDOW_BOUNDS, ACFR_STATES sets, read-only, exit 0/2)
    - .planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md (PA/IL row counts, municipality_ids, NASBO-replaced confirmation, cohort-untouched table)
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md (deepened CA/NY/FL windows + added-row counts)
    - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md (the invariant set + un-upgraded-NASBO-still-passes requirement)
  </read_first>
  <action>
    Create scripts/verify-phase106-cohort-audit.mjs from verify-phase102-cohort-audit.mjs (reuse its createClient env-load, the 7 invariants, and the exit-0/2 contract). Apply these changes:

    1. Update WINDOW_BOUNDS to the v2.12 loaded windows: CA 2008–2025, NY 2003–2024, FL 2021–2024 (deepened), and ADD PA 2016–2025 + IL 2021–2025. TX/MN/OH/VA unchanged; all other NASBO states 2023–2024.
    2. Add PA + IL to the ACFR-GAAP set so INV-6 (ACFR-GAAP label) covers their operating+revenue rows alongside CA/TX/NY/FL/MN/OH/VA.
    3. Keep INV-1 (every state row has data_source + source_url + source_date), INV-2 (0 residue stale *-gf-* data_sources), INV-3 (0 out-of-window FYs against the updated bounds), INV-4 (0 dup (muni,fy,dataset) combos), INV-5 (0 orphan data_source_id), INV-7 (NASBO states carry NASBO provenance, operating-only, untouched — GA checked dynamically as the control).

    Run it read-only against the live treasury schema. The script must exit 0 only if all 7 invariants PASS. If any invariant FAILs, that is a source-chain defect — per D-05 it is fixed in-phase (re-run the guarded loader / correct the data_sources row via the source-safe path, NEVER treasury_sync_city_budget) before sign-off, then re-run to confirm. Capture the per-invariant output for the report.
  </action>
  <verify>
    <automated>node scripts/verify-phase106-cohort-audit.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase106-cohort-audit.mjs exists; makes no writes (read-only).
    - WINDOW_BOUNDS contains CA 2008-2025, NY 2003-2024, FL 2021-2024, PA 2016-2025, IL 2021-2025.
    - INV-6 set includes PA and IL.
    - All 7 invariants print PASS and the script exits 0 (or any failure is fixed-in-phase via the source-safe path and the re-run exits 0).
    - PA shows 10 op + 10 rev rows, IL 5 op + 5 rev rows, all ACFR-GAAP labelled, 0 NASBO labels; GA still 2 NASBO operating-only rows untouched.
  </acceptance_criteria>
  <done>The 7-invariant cohort audit runs read-only over the v2.12-augmented cohort and exits 0; any defect found is fixed in-phase and re-confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm idempotency, reconcile the D-06 hole verdict, and write 106-COHORT-AUDIT.md</name>
  <read_first>
    - .planning/phases/104-deepen-the-4-pilots-deep-01-recon-05-acfr-08/104-DEEPEN-GAPLOG.md (the gap log D-06 reconciles against — 0 recorded gaps; deferred FYs: NY ≤FY2002, CA FY2002-2007, FL ≤FY2020)
    - .planning/phases/105-pa-il-acfr-upgrade-acfr-06-acfr-07-acfr-08-recon-05/105-PA-IL-LOADLOG.md (idempotency re-run precedent: "Loaded 0 rows")
    - .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-CONTEXT.md (D-06 hole-verdict rule; D-05 idempotency confirmation requirement)
    - The Task 1 audit output
  </read_first>
  <action>
    (a) IDEMPOTENCY: re-run one representative v2.12 loader per new state — processPAAcfr.js for a single PA FY and processILAcfr.js for a single IL FY (use each loader's existing guarded never-overwrite path via treasury_sync_budget_tree; source the .env first; main tree only; NEVER treasury_sync_city_budget). Confirm each reports "Loaded 0 rows" / 0 net change. Record the result. If a re-run produces a non-zero change, that is a never-overwrite defect → fix in-phase before sign-off.

    (b) D-06 HOLE-VERDICT RECONCILIATION: query the DB for the actual present FYs per deepened/new state and diff against 104-DEEPEN-GAPLOG.md. Confirm: (i) every FY the gap log says is LOADED is present; (ii) every FY absent from the DB is absent BY DESIGN per a recorded disposition (NY ≤FY2002 = no durable URL; CA FY2002-2007 = deferred variant-naming; FL ≤FY2020 = not durably sourceable) — NOT an unrecorded gap; (iii) the overall per-state range renders honestly (per-FY rows, basis label + source chip make the window self-evident — no interpolation, no implied continuity across a deferred boundary). Record the verdict: an honest, disclosed hole is a PASS; do NOT re-litigate gaps 104 judged unrecoverable.

    (c) WRITE 106-COHORT-AUDIT.md: headline verdict (N/7 invariants), the per-invariant PASS/FAIL table with row counts (PA 10+10, IL 5+5, deepened CA/NY/FL counts, GA control untouched), the idempotency result, and the D-06 hole-reconciliation section. Note the deferred-not-defect items (CA FY2002-2007 etc.) so they are not re-litigated. This file + 106-REDERIVATION.md are the two inputs the UAT-prep plan and the gsd-verifier read for VER-03.
  </action>
  <verify>
    <automated>node scripts/verify-phase106-cohort-audit.mjs; test -f .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-COHORT-AUDIT.md && grep -qiE "idempoten|hole|invariant" .planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-COHORT-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - 106-COHORT-AUDIT.md exists with: per-invariant PASS/FAIL table, row-count confirmations, idempotency result ("Loaded 0 rows" per representative PA + IL loader re-run), and the D-06 hole-verdict reconciliation.
    - Idempotency re-run produced 0 net change for both PA and IL representative FYs (or a defect was fixed in-phase and re-confirmed 0).
    - The hole reconciliation confirms 0 unrecorded DB gaps and lists the deferred-by-design FYs (NY ≤FY2002, CA FY2002-2007, FL ≤FY2020) as PASS-honest holes, not defects.
    - The audit script exits 0.
  </acceptance_criteria>
  <done>106-COHORT-AUDIT.md records 7/7 invariants clean, idempotency 0-change, and the D-06 hole verdict reconciled against the actual DB; defects (if any) fixed in-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → treasury schema (read) | Read-only invariant queries via service key (env-only). The audit makes NO writes. |
| in-phase fix path → treasury.budgets (write, only if a defect is found) | A D-05 fix re-runs the loader's guarded never-overwrite RPC; the source-unsafe RPC must be avoided. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-04 | Tampering | in-phase D-05 fix write to treasury.budgets | mitigate | Any fix uses the loader's guarded never-overwrite path (treasury_sync_budget_tree) — NEVER treasury_sync_city_budget (project memory: not source-safe, overwrites rows + keeps stale data_source label). Idempotency re-run confirms the write was non-destructive. |
| T-106-05 | Information Disclosure | SUPABASE_SERVICE_KEY | accept | Read from env only; never inlined in script or report. Standing practice. |
| T-106-06 | Repudiation | audit verdict honesty | mitigate | The audit script exits 2 on any failing invariant (cannot silently "pass"); the report records the raw per-invariant output; the idempotency re-run is observable ("Loaded 0 rows"). |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — reuses @supabase/supabase-js (already in package.json). No install task; no [ASSUMED]/[SUS] packages. |

Read-only audit; the only write path is a conditional D-05 in-phase fix, gated to the source-safe never-overwrite RPC (T-106-04).
</threat_model>

<verification>
- scripts/verify-phase106-cohort-audit.mjs exits 0 with all 7 invariants PASS over the v2.12-augmented windows.
- Idempotency re-run of a representative PA + IL loader reports 0 net change.
- 106-COHORT-AUDIT.md reconciles the D-06 hole verdict against the actual DB (0 unrecorded gaps).
</verification>

<success_criteria>
- VER-03 parts (b)+(c): full 50-node cohort source-chain audit clean (0 NULL/fragile/residue/out-of-window/dup/orphan), every displayed row basis-labelled, un-upgraded NASBO states still pass; idempotency + hole-verdict confirmed.
</success_criteria>

<output>
Create `.planning/phases/106-verification-source-chain-audit-uat-ver-03-ver-04/106-02-SUMMARY.md` when done.
</output>
