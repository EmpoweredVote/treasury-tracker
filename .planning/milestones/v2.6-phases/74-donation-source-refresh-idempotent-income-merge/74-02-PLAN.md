---
phase: 74-donation-source-refresh-idempotent-income-merge
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/loadEVDonations.js
autonomous: false
requirements: [EVDATA-01, EVDATA-02, EVDATA-03]
user_setup:
  - service: Supabase + donation platform exports
    why: "Live load writes the current-FY EV revenue dataset from the real exports and verifies it in the production app."
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env (Supabase service role) — already present locally; never log or commit"
    dashboard_config:
      - task: "Ensure data/ev-sources/{givebutter,patreon,benevity}.csv are the current-FY exports from 74-01 Task 1 (re-export if stale)"
        location: "Each platform's export UI → data/ev-sources/"

must_haves:
  truths:
    - "The current fiscal year EV revenue dataset reflects the combined, deduplicated GROSS donations from GiveButter, Patreon, and Benevity"
    - "Re-running loadEVDonations.js against the same exports leaves the DB byte-identical (idempotent) — verified by a second run"
    - "GiveButter is not double-counted: export aggregate + webhook delta, never both for the same donation (D-04)"
    - "The live GiveButter donation counter still updates (webhook path intact) after the reload"
    - "The EV page in the live app shows the refreshed Money In totals by source, every figure attributable to a platform export"
  artifacts:
    - path: "scripts/loadEVDonations.js"
      provides: "the loader, exercised live against real exports (no new file; may gain small fixes found during live run)"
      contains: "data/ev-sources"
  key_links:
    - from: "loadEVDonations live run"
      to: "treasury.budgets (EV revenue, current FY)"
      via: "createBudget + insertCategories writing the per-source gross tree"
      pattern: "revenue"
    - from: "ev-accounts API"
      to: "EV page Money In"
      via: "production API serves the refreshed revenue dataset to the live app"
      pattern: "Money In"
---

<objective>
Run `scripts/loadEVDonations.js` against the real current-FY exports, load the EV revenue dataset, and verify in the live app that donation income is current, deduplicated, and not double-counted — then confirm idempotency (a second run changes nothing) and that the live GiveButter counter still works.

Purpose: turn the tested loader (74-01) into actual refreshed numbers on treasurytracker.empowered.vote, and prove the no-double-count + idempotency contracts hold against real data, not just fixtures.
Output: current-FY EV revenue rows reflecting combined platform gross by source; a human-confirmed live-app check; any small loader fixes surfaced by real data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/74-donation-source-refresh-idempotent-income-merge/74-CONTEXT.md
@.planning/phases/74-donation-source-refresh-idempotent-income-merge/74-01-SUMMARY.md
@scripts/loadEVDonations.js
@docs/ev-donation-sources.md

<interfaces>
<!-- Verification targets. The live app reads EV via the production ev-accounts API, NOT a direct treasury.budgets probe — always confirm against the API/app, not just the DB (see feedback_app_url memory). -->

Live read path: treasurytracker.empowered.vote → ev-accounts-api.onrender.com → treasury.budgets (EV revenue dataset)
DB verify: treasury.budgets for the Empowered Vote municipality_id, dataset_type='revenue', current FY
  budget_line_items.source ∈ {'csv','givebutter_webhook'} — the dedup seam to confirm
CLI: node scripts/loadEVDonations.js --fy <current year> [--dry-run]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run then live-load the current FY; confirm sane gross-by-source + fees</name>
  <files>scripts/loadEVDonations.js</files>
  <read_first>
    - scripts/loadEVDonations.js (the loader from 74-01)
    - docs/ev-donation-sources.md (column mapping + dedup contract)
    - .planning/phases/74-donation-source-refresh-idempotent-income-merge/74-01-SUMMARY.md (what was built + any caveats)
  </read_first>
  <action>
    Source the gitignored `.env` for `SUPABASE_SERVICE_KEY`. Confirm `data/ev-sources/{givebutter,patreon,benevity}.csv` are the current-FY exports.

    Dry-run first: `node scripts/loadEVDonations.js --fy 2026 --dry-run`. Confirm the printed tree has Donations → {Give Butter, Patreon, Benevity} with plausible per-source gross totals, the per-source fee totals print, and `exportAsOf` is a sane recent date. Sanity-check the GiveButter gross against the figure the old combined sheet showed (it should be in the same ballpark, allowing for new donations since the sheet was last hand-updated).

    Then live-load: `node scripts/loadEVDonations.js --fy 2026`. The reload clears prior csv-sourced income rows, preserves webhook rows, applies the D-04 dedup, and writes the per-source gross tree. If real data surfaces a parsing edge case (a column variant, a refund/negative row, a $0 row), fix it minimally in the loader and re-run; record the fix in the summary.

    DB-verify: for the Empowered Vote municipality_id, confirm the current-FY revenue budget exists with Donations→{Give Butter,Patreon,Benevity} categories whose amounts equal the per-source gross, total_budget = sum of those, data_source set, and no donor name/email stored in any line item.
  </action>
  <verify>
    <automated>node scripts/loadEVDonations.js --fy 2026 --dry-run</automated>
    <human-check>Supabase: SELECT name, amount FROM budget_categories for the EV current-FY revenue budget — confirm Give Butter/Patreon/Benevity per-source gross + a Donations parent total; confirm no line item carries a donor name/email.</human-check>
  </verify>
  <acceptance_criteria>
    - Dry-run prints Donations→{Give Butter,Patreon,Benevity} with plausible per-source gross + fee totals + a recent exportAsOf, zero writes.
    - After the live run, the EV current-FY revenue budget exists with per-source gross category amounts summing to total_budget.
    - No `budget_line_items` row for the EV revenue dataset contains a donor name or email (aggregate-only, D-05).
    - Any loader fix made during the live run is recorded in the summary.
  </acceptance_criteria>
  <done>Current-FY EV donation income is live, aggregated gross by source, fees captured, no donor PII stored.</done>
</task>

<task type="auto">
  <name>Task 2: Prove idempotency + no GiveButter double-count against real data</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - scripts/loadEVDonations.js (dedup + clear-and-reload logic)
    - supabase/functions/givebutter-webhook/index.ts (webhook delta path)
  </read_first>
  <action>
    Idempotency: capture the current-FY revenue total + per-source amounts. Re-run `node scripts/loadEVDonations.js --fy 2026` against the same files. Re-capture and confirm the totals + per-source amounts are identical (no accumulation, no duplicate categories/line items).

    No double-count: confirm the GiveButter displayed total equals the export aggregate plus only the webhook rows dated after `exportAsOf` — NOT the export plus all webhook rows. Inspect `budget_line_items` for the EV revenue dataset: webhook rows (source='givebutter_webhook') dated ≤ exportAsOf are absent/superseded; rows > exportAsOf remain. Cross-check the GiveButter number against the platform export's own gross total for the FY (they should match within the live-delta of donations arriving after the export).
  </action>
  <verify>
    <automated>node scripts/loadEVDonations.js --fy 2026 --dry-run</automated>
    <human-check>Run the loader twice; confirm identical totals. Query budget_line_items for the EV revenue dataset and confirm no webhook row dated ≤ exportAsOf coexists with the export aggregate (no double-count).</human-check>
  </verify>
  <acceptance_criteria>
    - A second identical run yields identical revenue total + per-source amounts (idempotent — no growth, no duplicate rows).
    - GiveButter displayed total = export gross + webhook rows dated strictly after exportAsOf; no webhook row ≤ exportAsOf is also counted.
    - The GiveButter figure reconciles to the platform export's FY gross within the post-export live delta.
  </acceptance_criteria>
  <done>Idempotency and no-double-count are demonstrated against real exports, not just fixtures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-app verification — refreshed Money In + live counter still works (Chris)</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - .planning/phases/74-donation-source-refresh-idempotent-income-merge/74-CONTEXT.md (live host; ev-accounts API read path)
  </read_first>
  <what-built>
    The current-FY EV revenue dataset now reflects combined, deduplicated GROSS donations by source (Give Butter / Patreon / Benevity) from the platform exports, with the live GiveButter webhook providing the post-export delta.
  </what-built>
  <how-to-verify>
    1. Open the Empowered Vote page on treasurytracker.empowered.vote (production reads via the ev-accounts API — allow for its cache/deploy).
    2. Confirm Money In for the current year shows the three sources with the refreshed gross totals matching the dry-run/DB figures.
    3. Confirm the figures look current (reflect recent donations, not the stale hand-entered sheet).
    4. Confirm the live donation counter still increments on a new GiveButter donation (or that the webhook path is intact) — the reload must not have broken the Donations→Give Butter category the webhook looks up.
  </how-to-verify>
  <acceptance_criteria>
    - The EV page shows current-year Money In by source matching the loaded figures.
    - The numbers reflect current data (not the old sheet snapshot).
    - The live GiveButter counter / webhook path is confirmed intact (Donations→Give Butter category resolves).
    - Chris signs off.
  </acceptance_criteria>
  <verify>
    <human-check>See how-to-verify; Chris confirms the refreshed Money In totals and the live counter in the production app.</human-check>
  </verify>
  <done>Refreshed donation income is live and correct in the app, the live counter works, and Chris has signed off — EVDATA-01/02/03 satisfied.</done>
  <resume-signal>Type "approved" once the live app shows the refreshed, deduplicated Money In and the counter still works, or describe what's off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| platform exports → live DB | Real donor data parsed locally; only per-source aggregates written. Raw files gitignored. |
| loader → Supabase (write) | Service-role key (gitignored) writes the EV revenue dataset; single-operator, main tree. |
| webhook → revenue budget | Live writer the loader must reconcile (D-04), not clobber. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-05 | Information Disclosure | donor PII in live write | mitigate | Aggregate-only write confirmed in Task 1 acceptance (no name/email column); raw exports gitignored. |
| T-74-06 | Tampering / data integrity | double-count or non-idempotent reload | mitigate | Task 2 proves a second run is identical and GiveButter is counted once (export + post-asOf delta only). |
| T-74-07 | Availability | reload breaks the live webhook | mitigate | Task 3 confirms the Donations→Give Butter category survives the reload and the counter still works. |
</threat_model>

<verification>
- Current-FY EV revenue dataset loaded from real exports: gross by source, fees captured, no donor PII stored.
- Idempotency proven (second run identical) and GiveButter double-count ruled out (export + post-asOf delta only).
- Live app shows refreshed Money In by source; the live GiveButter counter/webhook path intact; Chris signed off.
</verification>

<success_criteria>
- EVDATA-01/02/03 satisfied end-to-end: current-FY donation income from GiveButter, Patreon, and Benevity is live, combined, deduplicated, and idempotent.
- The reconciled GiveButter figure matches the platform export within the post-export live delta.
- The refreshed totals are confirmed in the production app with the live counter still working.
</success_criteria>

<output>
Create `.planning/phases/74-donation-source-refresh-idempotent-income-merge/74-02-SUMMARY.md` when done — record the loaded per-source gross + fees, the idempotency + no-double-count verification, any loader fixes, and the Chris live-app sign-off.
</output>
