---
phase: 75-bank-truth-reconciliation
plan: 03
type: execute
wave: 3
depends_on: ["75-02"]
files_modified:
  - scripts/loadEVDonations.js
  - scripts/loadEVDonations.test.mjs
  - scripts/loadEVFinances.js
  - docs/ev-donation-sources.md
autonomous: false
requirements: [EVDATA-06]
user_setup:
  - service: Manual / off-platform income ledger
    why: "EVDATA-06 needs a place to record checks/grants and any unmatched bank deposit (from 75-02). Bank interest is re-homed from the bank export."
    dashboard_config:
      - task: "If there is off-platform income for FY2026 (checks, grants, or an unmatched deposit flagged by reconcileEV), add rows to data/ev-sources/manual.csv using the header documented in Task 1 (date,source,amount,note). May be left empty/header-only if none."
        location: "data/ev-sources/manual.csv (gitignored)"
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env (service role) — used for the live revenue reload in Task 4."

must_haves:
  truths:
    - "Manual / off-platform income (checks, grants, in-kind-excluded) is recorded in data/ev-sources/manual.csv and loaded by loadEVDonations.js into the revenue tree tagged source='manual', deduplicated/idempotent on re-import (EVDATA-06)"
    - "Bank interest income is re-homed: it comes from the bank export's 'Credit Interest' deposits (via the shared classifyDeposit), NOT from data/ev-finances.csv — Interest → Bank Interest stays in the revenue tree, now bank-sourced (D-06)"
    - "data/ev-finances.csv is fully retired as a data source: loadEVDonations no longer reads it for carry-forward, and loadEVFinances.js no longer writes the operating dataset (the bank owns expenses via loadEVBank.js) (D-08/D-09)"
    - "In-kind / non-cash gifts are NOT counted in cash income or balance this phase (deferred, D-10) — documented as the next follow-up with the show-separately design"
    - "Re-running loadEVDonations against the same exports + manual.csv yields identical revenue state (idempotent), and still preserves the givebutter_webhook live delta"
  artifacts:
    - path: "scripts/loadEVDonations.js"
      provides: "income loader now sources non-platform income from manual.csv (Direct/manual) + bank interest, not ev-finances.csv"
      contains: "manual.csv"
    - path: "scripts/loadEVDonations.test.mjs"
      provides: "tests for manual.csv parsing, manual source tagging, bank-interest carry-forward, idempotency"
      contains: "manual"
  key_links:
    - from: "data/ev-sources/manual.csv"
      to: "revenue tree (Donations → Direct / manual)"
      via: "loadEVDonations reads manual.csv, tags line items source='manual'"
      pattern: "manual"
    - from: "bank 'Credit Interest' deposits"
      to: "revenue tree (Interest → Bank Interest)"
      via: "classifyDeposit(kind='interest') → carry-forward, replacing the ev-finances.csv source"
      pattern: "Bank Interest"
---

<objective>
Close out EVDATA-06 and finish retiring the legacy ledger. Add a manual / off-platform income path: `data/ev-sources/manual.csv` (date, source, amount, note) read by `loadEVDonations.js` and written into the revenue tree tagged `source='manual'`, idempotently. Re-home **bank interest** so it comes from the bank export's `Credit Interest` deposits (via the shared `classifyDeposit` from 75-02) instead of `data/ev-finances.csv`. Then fully retire `data/ev-finances.csv`: `loadEVDonations` stops reading it, and `loadEVFinances.js` stops writing the operating dataset (the bank is the expense authority via `loadEVBank.js`). In-kind gifts are deferred (D-10) but documented as the next follow-up.

Purpose: this makes EVDATA-06 true, removes the last double-write risk (two operating writers), and re-homes interest to its real source — leaving exactly three current sources (bank expenses, platform income, manual income) with no stale ledger in the loop.
Output: manual.csv path + tests, interest re-home, loadEVFinances retirement, docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md
@scripts/loadEVDonations.js
@scripts/loadEVFinances.js
@scripts/lib/evBankDeposits.js
@docs/ev-donation-sources.md

<interfaces>
<!-- The current non-platform income path to REPLACE. -->

scripts/loadEVDonations.js currently has carryForwardFromSheet(sheetPath, fy) which reads data/ev-finances.csv for
  'Income' rows, skips Patreon/Give Butter/Benevity, maps Category 'Interest' → Interest/Bank Interest else → Donations/Direct.
  buildDonationTree(bySource, carryForward) accepts carry-forward [{parent,name,amount}].
  insertCategories(...) currently hardcodes line-item source='csv'.

Replace carryForwardFromSheet with TWO sources:
  (a) manual.csv  → Direct (and any operator-labeled) income, source='manual'
  (b) bank export → Credit Interest deposits → Interest/Bank Interest, source='bank'
  using classifyDeposit + extractDeposits from scripts/lib/evBankDeposits.js (75-02) and the bank file in data/ev-sources/.

scripts/loadEVFinances.js: still writes the operating (expense) dataset from data/ev-finances.csv. The bank now owns
  operating expenses (scripts/loadEVBank.js, source='bank'). Two operating writers = conflict → retire the loadEVFinances operating write.

manual.csv schema (Task 1): header `date,source,amount,note`; date M/D/YYYY or ISO; amount plain decimal; source = display label
  (e.g. "Check — Acme Grant"); idempotent because the whole FY revenue budget is clear-and-reloaded each run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define manual.csv + re-home non-platform income in loadEVDonations.js</name>
  <files>scripts/loadEVDonations.js, docs/ev-donation-sources.md</files>
  <read_first>
    - scripts/loadEVDonations.js (carryForwardFromSheet, buildDonationTree, insertCategories, main)
    - scripts/lib/evBankDeposits.js (classifyDeposit, extractDeposits — for bank interest)
    - .planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md (D-06 bank interest re-home, D-08/09 retire sheet, D-10 in-kind deferred)
  </read_first>
  <behavior>
    - New `carryForwardManual(dir, fy)`: if `data/ev-sources/manual.csv` exists, parse header `date,source,amount,note`, FY-filter by date year, and return [{parent:'Donations', name: <source label or 'Direct'>, amount, tag:'manual'}] for each cash row. Absent file → []. (No in-kind rows; in-kind deferred D-10.)
    - New `carryForwardInterest(dir, fy)`: read the bank export, `extractDeposits(rows, fy)`, keep `classifyDeposit(d.desc).kind === 'interest'`, sum → [{parent:'Interest', name:'Bank Interest', amount, tag:'bank'}] (omit if 0).
    - main() builds carry-forward from `carryForwardManual` + `carryForwardInterest` (NOT carryForwardFromSheet); the rest of the revenue build is unchanged.
    - Line items carry their source tag: manual rows → 'manual', interest rows → 'bank', platform rows → 'csv' (existing). insertCategories must accept a per-row source instead of the hardcoded 'csv'.
    - Idempotent: clear-and-reload of the FY revenue budget preserves the givebutter_webhook delta exactly as today.
  </behavior>
  <action>
    In `scripts/loadEVDonations.js`: add `carryForwardManual(dir, fy)` and `carryForwardInterest(dir, fy)` (import `classifyDeposit`, `extractDeposits` from `./lib/evBankDeposits.js` and reuse `readCsvRows`, `money`). Thread a `source` tag through the carry-forward records and through `buildDonationTree` → `insertCategories` so each line item is inserted with its own source ('manual' | 'bank' | 'csv') instead of the hardcoded `source: 'csv'`. In `main()`, replace the `carryForwardFromSheet(path.join(..., 'ev-finances.csv'), fy)` call with `carryForwardManual(dir, fy)` concatenated with `carryForwardInterest(dir, fy)`. Remove (or export-and-mark-unused) `carryForwardFromSheet` — it must no longer be invoked. Do not change the GiveButter dedup / webhook-delta logic.

    Document in `docs/ev-donation-sources.md`: the `manual.csv` header + semantics (cash off-platform income only; in-kind excluded, deferred per D-10), and that Interest now comes from the bank export's `Credit Interest` deposits, not the retired sheet.
  </action>
  <verify>
    <automated>node --check scripts/loadEVDonations.js &amp;&amp; node scripts/loadEVDonations.js --fy 2026 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/loadEVDonations.js` exits 0.
    - `loadEVDonations.js` no longer calls `carryForwardFromSheet` and contains `carryForwardManual` + `carryForwardInterest`; it imports from `./lib/evBankDeposits.js`.
    - `insertCategories` inserts line items with a per-row `source` ('manual'/'bank'/'csv'), not a hardcoded 'csv'.
    - `--dry-run` shows Interest → Bank Interest sourced from the bank deposits (≈ the FY2026 Credit Interest total) and any manual.csv rows under Donations.
    - `docs/ev-donation-sources.md` documents manual.csv + the interest re-home + in-kind deferral.
  </acceptance_criteria>
  <done>Non-platform income now comes from manual.csv (manual) + bank interest (bank); ev-finances.csv is no longer read by the income loader.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the operating write in loadEVFinances.js (bank owns expenses)</name>
  <files>scripts/loadEVFinances.js</files>
  <read_first>
    - scripts/loadEVFinances.js (main — the operating/expense build + write block)
    - scripts/loadEVBank.js (the authoritative operating writer, source='bank')
    - .planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md (D-08/D-09 ev-finances.csv fully retired)
  </read_first>
  <behavior>
    - loadEVFinances.js no longer writes ANY budget dataset (revenue already retired in Phase 74; now operating too).
    - No second writer of the operating dataset remains — loadEVBank.js is the sole operating writer.
  </behavior>
  <action>
    In `scripts/loadEVFinances.js`, remove the operating-dataset build + write path (the `buildTree(...)` → `clearExistingBudget(...,'operating')`/delete → `createBudget(...,'operating',...)` → `insertCategories` block). Update the file header + footer comments to state the script is retired: EV expenses load from the bank via `scripts/loadEVBank.js`, donation income via `scripts/loadEVDonations.js`, manual income via `manual.csv`, and the reconciled summary via `scripts/reconcileEV.js`; `data/ev-finances.csv` is no longer a data source (D-09). If `main()` ends up empty, make it a clear no-op that prints the retirement notice and exits 0 (do not delete the file — keep import-safe helpers others may use). Do not touch the webhook, the RPC, or loadEVBank.js.
  </action>
  <verify>
    <automated>node --check scripts/loadEVFinances.js &amp;&amp; node scripts/loadEVFinances.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/loadEVFinances.js` exits 0.
    - `loadEVFinances.js main()` no longer calls `createBudget(... 'operating' ...)` — grep returns 0 operating write call sites.
    - Running `node scripts/loadEVFinances.js` performs no DB write and prints the retirement notice.
    - Header/footer comments point to loadEVBank.js / loadEVDonations.js / reconcileEV.js and state ev-finances.csv is retired.
  </acceptance_criteria>
  <done>loadEVFinances.js writes nothing; loadEVBank.js is the sole operating writer — no double-write path remains.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — manual.csv parsing, source tagging, bank-interest carry-forward, idempotency</name>
  <files>scripts/loadEVDonations.test.mjs</files>
  <read_first>
    - scripts/loadEVDonations.js (carryForwardManual, carryForwardInterest, buildDonationTree, the source threading)
    - scripts/loadEVDonations.test.mjs (existing tests to extend, keep them green)
    - scripts/lib/evBankDeposits.js (classifyDeposit/extractDeposits used by interest carry-forward)
  </read_first>
  <action>
    Extend `scripts/loadEVDonations.test.mjs` (node:test, fixtures, no network). Add assertions:
    1. carryForwardManual parses `date,source,amount,note`, FY-filters by year, and returns Donations rows tagged 'manual' (absent file → []).
    2. carryForwardInterest, given fixture bank rows with a 'Credit Interest' deposit in-FY and a platform payout deposit, returns exactly one Interest/Bank Interest row tagged 'bank' equal to the interest sum (ignores the platform payout).
    3. buildDonationTree threads the source tag so manual line items carry source='manual' and interest items source='bank'.
    4. Idempotency: building the tree twice from the same bySource + carry-forward yields identical totals.
    Keep all pre-existing tests passing.
  </action>
  <verify>
    <automated>node --test scripts/loadEVDonations.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/loadEVDonations.test.mjs` reports all tests passing, 0 failing (old + new).
    - A test proves a 'Credit Interest' deposit becomes Interest/Bank Interest (tag 'bank') and a platform payout deposit is excluded.
    - A test proves manual.csv rows are tagged source='manual'.
  </acceptance_criteria>
  <done>The manual + bank-interest income path and source tagging are proven offline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live revenue reload + verify sources; confirm single operating writer</name>
  <files>scripts/loadEVDonations.js</files>
  <read_first>
    - scripts/loadEVDonations.js (the live load path)
    - .planning/phases/75-bank-truth-reconciliation/75-CONTEXT.md (verification expectations)
  </read_first>
  <action>
    Operator populates `data/ev-sources/manual.csv` (header-only if no off-platform income for FY2026). Run `SUPABASE_SERVICE_KEY=... node scripts/loadEVDonations.js --fy 2026` to reload revenue with the new manual + bank-interest sources. Verify via `mcp__supabase-local__execute_sql` that the FY2026 EV revenue budget contains: a `Donations` parent (with Give Butter/Patreon/Benevity + any manual rows tagged source='manual'), an `Interest → Bank Interest` leaf sourced from the bank (source='bank'), and that no `givebutter_webhook` row was lost. Confirm the FY2026 EV `operating` budget still equals the bank load from loadEVBank.js (loadEVFinances.js did not overwrite it). Re-run loadEVDonations once more → identical revenue (idempotent).
  </action>
  <verify>
    <human-check>execute_sql shows revenue with manual (source='manual') + bank interest (source='bank') rows, webhook delta intact; operating still matches the bank load; re-run is idempotent.</human-check>
  </verify>
  <acceptance_criteria>
    - FY2026 EV revenue contains Interest → Bank Interest with source='bank' and (if manual.csv had rows) Donations rows with source='manual'.
    - No givebutter_webhook line item was deleted by the reload (live delta preserved).
    - FY2026 EV operating total still equals the loadEVBank.js value (no loadEVFinances overwrite).
    - A second loadEVDonations run leaves revenue identical (idempotent).
  </acceptance_criteria>
  <done>Manual + bank-interest income live and idempotent; bank remains the sole operating writer; EVDATA-06 satisfied.</done>
  <resume-signal>Type "reloaded" once revenue shows the manual + bank-interest sources and operating is untouched, or describe what's off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual.csv (operator-entered) → loader | Operator-authored cash entries; gitignored folder; aggregated into the revenue tree (no donor PII required — labels are operator text). |
| bank export → loader (interest) | Bank credit rows re-used for interest income; already gitignored. |
| loader → Supabase (write) | Revenue reload via gitignored service-role key; single-operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-01 | Tampering / data integrity | two operating writers (sheet + bank) | mitigate | loadEVFinances.js operating write removed (Task 2); loadEVBank.js is the sole operating writer. |
| T-75-03-02 | Tampering / data integrity | double-count interest (sheet + bank) | mitigate | Interest sourced ONLY from bank Credit Interest deposits; ev-finances.csv no longer read (D-06/D-09). |
| T-75-03-03 | Tampering / data integrity | webhook delta lost on reload | mitigate | Clear-and-reload preserves source='givebutter_webhook' rows (unchanged dedup path); asserted in Task 4. |
| T-75-03-04 | Information Disclosure | manual.csv / service key | mitigate | data/ev-sources/ gitignored; manual labels are operator text (no required PII); key from gitignored .env, never logged. |
| T-75-03-SC | Tampering | npm installs | accept | No new installs. |
</threat_model>

<verification>
- node --check + node --test green for loadEVDonations (+ existing tests) and loadEVFinances.
- Non-platform income re-homed: manual.csv → source='manual', bank Credit Interest → Interest/Bank Interest source='bank'; ev-finances.csv not read.
- loadEVFinances.js writes nothing; bank is the sole operating writer (no double-write).
- Live FY2026 revenue reload shows the new sources, preserves the webhook delta, is idempotent; operating untouched.
- In-kind deferred (D-10), documented as the next follow-up.
</verification>

<success_criteria>
- EVDATA-06 implemented: off-platform / manual income recorded via manual.csv and included in totals; bank interest re-homed; ev-finances.csv fully retired with no double-write path.
</success_criteria>

<output>
Create `.planning/phases/75-bank-truth-reconciliation/75-03-SUMMARY.md` when done — record the manual.csv schema, the interest re-home, the loadEVFinances retirement, the in-kind deferral note, and the live-reload verification.
</output>
