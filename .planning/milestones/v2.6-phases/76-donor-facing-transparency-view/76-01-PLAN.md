---
phase: 76-donor-facing-transparency-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260621000000_org_financial_summary_add_goal.sql
  - scripts/reconcileEV.js
  - data/ev-sources/goal.json
autonomous: false
requirements: [EVVIEW-04]
user_setup:
  - service: Supabase (treasury schema, production project kxsdzaojfaibhuzmclfq)
    why: "The donor view reads goal_amount/goal_label via the live ev-accounts API. The two new columns must land on the same prod project the API reads (verified prod = kxsdzaojfaibhuzmclfq)."
    dashboard_config:
      - task: "Apply the migration via mcp__supabase-local__apply_migration (DDL), then verify columns with mcp__supabase-local__execute_sql. Do NOT ask Chris to paste into the Dashboard (per feedback_supabase_migration_mcp)."
        location: "MCP supabase-local tools"
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env — present locally; used by reconcileEV.js to upsert the goal value."

must_haves:
  truths:
    - "treasury.org_financial_summary has two new nullable columns: goal_amount numeric and goal_label text"
    - "The fundraising goal is a MANUAL value sourced from a committed file data/ev-sources/goal.json (no GiveButter/live-API call) — D-01"
    - "reconcileEV.js reads the manual goal for the target FY and includes goal_amount + goal_label in the upserted org_financial_summary row; absent goal file leaves both columns null (no error)"
    - "Re-running reconcileEV.js with the same goal.json changes nothing (idempotent upsert by municipality_id,fiscal_year)"
    - "After running the loader, the FY2026 EV row has a non-null goal_amount and the live API returns it"
  artifacts:
    - path: "supabase/migrations/20260621000000_org_financial_summary_add_goal.sql"
      provides: "DDL adding goal_amount + goal_label to treasury.org_financial_summary"
      contains: "goal_amount"
    - path: "data/ev-sources/goal.json"
      provides: "the manual fundraising goal (amount + label) for EV, per fiscal year"
      contains: "goal_amount"
    - path: "scripts/reconcileEV.js"
      provides: "goal merge into the upserted summary row"
      contains: "goal_amount"
  key_links:
    - from: "data/ev-sources/goal.json"
      to: "org_financial_summary.goal_amount/goal_label → ev-accounts API → donor view progress bar"
      via: "reconcileEV.js reads goal.json for the FY and upserts it onto the summary row"
      pattern: "goal_amount"
---

<objective>
Give Empowered Vote's reconciled summary a fundraising-goal field so the Phase 76 donor view can show "progress toward the goal." Per CONTEXT D-01, the goal is a MANUAL value (not a GiveButter/live-API pull this milestone), stored as two new nullable columns (`goal_amount`, `goal_label`) on the existing `treasury.org_financial_summary` table and populated by `reconcileEV.js` from a small committed file `data/ev-sources/goal.json`. Progress itself is computed downstream from `income_net` (D-02) — this plan only supplies the goal target.

Purpose: the API (already built by EV-Accounts, `GET /api/treasury/orgs/:id/financial-summary`) uses `SELECT *` + `?? null`, so the moment these columns exist and the loader populates them, the goal serves automatically with zero API change. This plan delivers the data half of EVVIEW-04.
Output: one applied + verified migration, a committed goal file, an idempotent loader change, and a populated FY2026 goal row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md
@supabase/migrations/20260620000000_create_org_financial_summary.sql
@scripts/reconcileEV.js

<interfaces>
EV entity: treasury.municipalities row name='Empowered Vote', entity_type='nonprofit', id ee6f34f7-bd85-4387-8d71-4c2ed8cb8fdf.
Existing table: treasury.org_financial_summary (migration 20260620000000) — upsert keyed (municipality_id, fiscal_year); sourcing columns source_name/source_url/source_date; grants SELECT to anon+authenticated.
reconcileEV.js: computeSummary(dir, fy) builds the row object; upsertSummary(sb, muniId, s) writes it via .upsert(row, { onConflict: 'municipality_id,fiscal_year' }).
Progress metric (downstream, D-02): income_net (already on the row) ÷ goal_amount, capped at 100% (D-03). This plan does NOT compute progress — it only supplies goal_amount/goal_label.
Goal-met behavior (downstream, D-03): cap at 100% + celebrate. No overage. Not implemented here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — add goal_amount + goal_label columns</name>
  <files>supabase/migrations/20260621000000_org_financial_summary_add_goal.sql</files>
  <read_first>
    - supabase/migrations/20260620000000_create_org_financial_summary.sql (table shape, COMMENT style, dollars-not-cents convention, the UNIQUE(municipality_id,fiscal_year) key)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (D-01 manual goal, D-02 progress = income_net)
  </read_first>
  <action>
    Create `supabase/migrations/20260621000000_org_financial_summary_add_goal.sql`. Use `ALTER TABLE treasury.org_financial_summary ADD COLUMN IF NOT EXISTS goal_amount numeric;` and `ADD COLUMN IF NOT EXISTS goal_label text;` (both nullable — a row with no active goal is valid). Add a `COMMENT ON COLUMN` for each: goal_amount = "active fundraising goal in DOLLARS, manual value from data/ev-sources/goal.json (D-01); progress = income_net / goal_amount capped at 100% (D-02/D-03)"; goal_label = "human label for the active goal (e.g. campaign name)". No new grants needed — column adds inherit the table's existing anon/authenticated SELECT grant.
  </action>
  <acceptance_criteria>
    - File `supabase/migrations/20260621000000_org_financial_summary_add_goal.sql` exists and contains `ADD COLUMN IF NOT EXISTS goal_amount` and `ADD COLUMN IF NOT EXISTS goal_label`
    - Timestamp `20260621000000` is newer than the latest existing migration in supabase/migrations/
    - Both columns are nullable (no NOT NULL, no DEFAULT that would break existing rows)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply the migration to the prod project + verify</name>
  <files>supabase/migrations/20260621000000_org_financial_summary_add_goal.sql</files>
  <read_first>
    - supabase/migrations/20260621000000_org_financial_summary_add_goal.sql
  </read_first>
  <action>
    Apply the migration with mcp__supabase-local__apply_migration (DDL path, per feedback_supabase_migration_mcp — do NOT ask Chris to paste into the Dashboard). Then verify with mcp__supabase-local__execute_sql querying information_schema.columns for table_name='org_financial_summary' AND column_name IN ('goal_amount','goal_label'). Confirm the target project is the prod project the ev-accounts API reads (kxsdzaojfaibhuzmclfq).
  </action>
  <acceptance_criteria>
    - mcp__supabase-local__execute_sql against information_schema.columns returns BOTH goal_amount and goal_label for treasury.org_financial_summary
    - The columns exist on project kxsdzaojfaibhuzmclfq (the project the live API reads)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create the manual goal file</name>
  <files>data/ev-sources/goal.json</files>
  <read_first>
    - data/ev-sources/ (confirm directory + sibling manual/source files for naming consistency)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (D-01)
  </read_first>
  <action>
    Create `data/ev-sources/goal.json` as a small committed, git-reviewable file holding the manual goal per fiscal year. Shape: an array of objects `[{ "fiscal_year": 2026, "goal_amount": <number in dollars>, "goal_label": "<label>" }]` (array keyed by FY so future years append without overwriting). CHECKPOINT: ask Chris for the actual FY2026 goal_amount and goal_label before finalizing the value (do not invent a number — the figure is donor-facing and must be real). If Chris is unavailable at execution time, write the file with a clearly-marked placeholder and set this task autonomous:false so it is not silently shipped.
  </action>
  <acceptance_criteria>
    - File `data/ev-sources/goal.json` exists and parses as valid JSON
    - Contains an entry with fiscal_year 2026, a numeric goal_amount, and a non-empty goal_label
    - The goal_amount is a real figure confirmed by Chris (not an unconfirmed placeholder) OR the task is flagged autonomous:false pending his input
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire the goal into reconcileEV.js (read goal.json → upsert columns)</name>
  <files>scripts/reconcileEV.js</files>
  <read_first>
    - scripts/reconcileEV.js (computeSummary builds the row; upsertSummary writes it; findFile helper; round2)
    - data/ev-sources/goal.json
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (D-01)
  </read_first>
  <action>
    In `scripts/reconcileEV.js`, add a pure helper `readGoal(dir, fy)` that reads `data/ev-sources/goal.json` (resolve relative to the same source dir the other files use), finds the entry where `fiscal_year === fy`, and returns `{ goal_amount, goal_label }` or `{ goal_amount: null, goal_label: null }` when the file is missing or has no matching FY (never throw on absence — null is valid). Merge its result into the object returned by `computeSummary(dir, fy)` (so the summary carries goal_amount/goal_label). In `upsertSummary`, add `goal_amount: s.goal_amount` and `goal_label: s.goal_label` to the `row` object. Extend `printSummary` to log the goal line (e.g. "Goal: $X (<label>)" or "Goal: none"). Do not alter the existing income/recon/runway math.
  </action>
  <acceptance_criteria>
    - reconcileEV.js contains a `readGoal` function that returns nulls (no throw) when goal.json is absent or lacks the FY
    - The upserted `row` in upsertSummary includes `goal_amount` and `goal_label`
    - Running the reconcileEV dry-run prints a Goal line
    - No change to the numeric outputs of monthlyBurn/runway/buildIncome/reconcile (existing tests/dry-run figures unchanged)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run the loader to populate FY2026 goal + confirm API serves it</name>
  <files>scripts/reconcileEV.js</files>
  <read_first>
    - scripts/reconcileEV.js (how it is invoked — npm script / node entry, --fy flag, source dir)
    - .planning/phases/76-donor-facing-transparency-view/76-CONTEXT.md (live API contract under "API endpoint — BUILT")
  </read_first>
  <action>
    Run reconcileEV.js for FY2026 against data/ev-sources/ to upsert the row including the goal (source the gitignored .env for SUPABASE_SERVICE_KEY first). Then confirm end-to-end: query the live API `GET https://ev-accounts-api.onrender.com/api/treasury/orgs/ee6f34f7-bd85-4387-8d71-4c2ed8cb8fdf/financial-summary?fiscal_year=2026` (or the dev-proxied path) and verify the JSON includes a non-null goal_amount + goal_label matching goal.json. Re-run the loader once more and confirm the row is unchanged (idempotent).
  </action>
  <acceptance_criteria>
    - The live API response for the EV org id (FY2026) includes goal_amount (non-null, matches goal.json) and goal_label
    - income_net is present on the same response (the progress numerator, already populated by Phase 75)
    - Running reconcileEV.js twice produces identical row values (idempotent — no duplicate, no drift)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Migration applied to kxsdzaojfaibhuzmclfq; goal_amount + goal_label columns confirmed via execute_sql.
- data/ev-sources/goal.json committed with a real FY2026 goal (Chris-confirmed) or flagged pending.
- reconcileEV.js reads the goal and upserts it; absent file → null, no error; idempotent re-run.
- Live API returns goal_amount + goal_label + income_net for the EV org row.
</verification>

<must_haves_check>
EVVIEW-04 data half delivered: a real, sourced fundraising goal exists on the EV summary row and is reachable through the live API alongside income_net (the progress numerator). The visual progress bar + cap-at-100% celebration is 76-03.
</must_haves_check>
