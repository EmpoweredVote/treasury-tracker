---
phase: 69-utah-city-budgets-load
plan: 02
type: execute
wave: 2
depends_on: ["69-01"]
files_modified: []
autonomous: false
requirements: [UCITY-01, UCITY-02]
user_setup:
  - service: Transparent Utah BigQuery
    why: "Live BigQuery reads for the 9-city op+rev sweep (UCITY-01/02)."
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env (Supabase service role) — already present locally; never log or commit"
    dashboard_config:
      - task: "Confirm a live gcloud session (CLI cred + ADC) before the sweep; refresh interactively if a query throws invalid_grant/invalid_rapt (Workspace reauth policy)"
        location: "Real terminal: prepend gcloud SDK bin to PATH, then `gcloud auth login` AND `gcloud auth application-default login` (docs/utah-bigquery-access.md)"

must_haves:
  truths:
    - "The 9 non-canary Utah cities each show operating (EX) + revenue (RV) for FY2014–FY2025"
    - "Every loaded row carries data_source_name 'Transparent Utah' and a non-null durable source_url"
    - "The fund1→org1→cat1 icicle renders for each of the 9 cities"
    - "Any pre-existing custom-source city row is left unchanged (never-overwrite)"
    - "Provo all-funds operating total reconciles within tolerance of a published reference"
  artifacts:
    - path: "scripts/loadUtahTransparency.js"
      provides: "the refactored 3-level loader (consumed unchanged from 69-01)"
      contains: "GROUP BY fund1, org1, cat1"
  key_links:
    - from: "loadUtahTransparency.js"
      to: "treasury_sync_city_budget"
      via: "per-entity, per-FY RPC write with Transparent Utah attribution"
      pattern: "treasury_sync_city_budget"
    - from: "treasury.budgets"
      to: "production /api/treasury/cities"
      via: "first budget row makes the city visible in the app"
      pattern: "municipality_id"
---

<objective>
Sweep the remaining 9 Utah cities (after the SLC canary in 69-01) through the refactored loader: live-load operating + revenue for FY2014–FY2025, all-funds, durably sourced, with the fund1→org1→cat1 tree. Reconcile Provo's all-funds operating total against a published reference (the second of the ≥2-city spot-checks for SC#4).

Purpose: completes UCITY-01 (operating, EX) + UCITY-02 (revenue, RV) for all 10 cities.
Output: operating + revenue rows for Layton, Lehi, Ogden, Orem, Provo, Sandy, St. George, West Jordan, West Valley City across FY2014–FY2025 — each city now visible in the app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-utah-city-budgets-load/69-CONTEXT.md
@docs/utah-entity-mapping.md
@docs/utah-bigquery-access.md
@.planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md

<interfaces>
<!-- This plan does NOT modify the loader — it runs the refactored 69-01 loader as-is. -->

CLI (defaults to types EX,RV → operating + revenue; fund1→org1→cat1 tree after 69-01):
  node scripts/loadUtahTransparency.js --entity "<exact entity_name>" --fy 2014 ... --fy 2025

Exact entity_name strings (from docs/utah-entity-mapping.md — match EXACTLY, never LIKE; decoys exist):
  Layton City | Lehi City | Ogden City | Orem City | Provo City
  Sandy City | St. George City | West Jordan City | West Valley City
  (Salt Lake City already loaded in 69-01 — do NOT reload here.)

Never-overwrite guard (carried lock): a (muni, fy, dataset) row from a DIFFERENT data_source SKIPs untouched.
Source attribution (carried lock): data_source_name='Transparent Utah'; source_url=https://transparent.utah.gov; source_date once per run.
FY range (D-69-04): FY2014–FY2025 inclusive; EXCLUDE FY2026 (partial in-progress year).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live-load operating + revenue FY2014–FY2025 for the 9 non-canary cities</name>
  <files>scripts/loadUtahTransparency.js</files>
  <read_first>
    - scripts/loadUtahTransparency.js (the refactored loader — read to confirm the fund tree + CLI flags)
    - .planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md (SLC canary outcome, any SKIPs, the FY-flag invocation pattern that worked)
    - docs/utah-entity-mapping.md (the 9 exact entity_name strings + their counties for Phase 70; match EXACTLY)
    - docs/utah-bigquery-access.md (gcloud PATH/ADC, reauth caveat, $0 dry-run discipline)
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-02 all-funds; D-69-04 FY range; carried-forward locks)
  </read_first>
  <action>
    Source the gitignored `.env` for `SUPABASE_SERVICE_KEY` and prepend the gcloud SDK bin to PATH; confirm a live ADC session (re-auth interactively if a query throws `invalid_grant`/`invalid_rapt`).

    For each of the 9 cities — `Layton City`, `Lehi City`, `Ogden City`, `Orem City`, `Provo City`, `Sandy City`, `St. George City`, `West Jordan City`, `West Valley City` — run the loader once with all 12 complete fiscal years FY2014–FY2025 (EXCLUDE FY2026, D-69-04), letting it default to types EX,RV: `node scripts/loadUtahTransparency.js --entity "<exact name>" --fy 2014 --fy 2015 --fy 2016 --fy 2017 --fy 2018 --fy 2019 --fy 2020 --fy 2021 --fy 2022 --fy 2023 --fy 2024 --fy 2025`. Match `entity_name` EXACTLY (never `LIKE`) — decoys like North Ogden City / North Salt Lake / South Salt Lake / Washington Terrace City exist in the table.

    Do NOT reload Salt Lake City (done in 69-01). Keep all-funds basis — no fund filter, no governmental/enterprise split (D-69-02). Keep each query column-projected + entity/FY/type-filtered to stay $0. The never-overwrite guard stays on; the 9 cities are expected all-new (confirm none pre-exist from a custom source) — log any SKIP and leave that row untouched.

    Optionally dry-run one mid-size city (e.g. `Ogden City --fy 2024 --dry-run`) before the live sweep to re-confirm the fund-top tree shape, then proceed.
  </action>
  <verify>
    <automated>node scripts/loadUtahTransparency.js --entity "Ogden City" --fy 2024 --dry-run</automated>
    <human-check>Supabase: for each of the 9 municipality_ids, SELECT fiscal_year, dataset_type, data_source FROM treasury.budgets — confirm operating+revenue rows across FY2014–FY2025, every row data_source='Transparent Utah', no FY2026 rows.</human-check>
  </verify>
  <acceptance_criteria>
    - DB query: `SELECT municipality_id, count(*) FROM treasury.budgets WHERE municipality_id IN (<the 9 city ids>) AND dataset_type IN ('operating','revenue') AND fiscal_year BETWEEN 2014 AND 2025 GROUP BY municipality_id` returns a non-zero count for all 9 cities, covering both dataset_types.
    - DB query: every row from this run carries `data_source = 'Transparent Utah'` and a non-null `source_url`.
    - DB query: no `treasury.budgets` row for any of the 9 cities has `fiscal_year = 2026`.
    - The Ogden dry-run prints a 3-level tree with `fund1` names at the top (General Fund, Power, Water, etc.), zero writes.
    - Every loader invocation used an EXACT entity_name from docs/utah-entity-mapping.md (no LIKE match).
    - Any never-overwrite SKIP is logged with the pre-existing data_source and the row left unchanged.
  </acceptance_criteria>
  <done>All 9 non-canary cities have operating + revenue for FY2014–FY2025, fund-top tree, Transparent-Utah-sourced, FY2026 excluded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 10-city app spot-check + Provo ACFR reconciliation (SC#1, SC#4)</name>
  <read_first>
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-02 all-funds reconciliation basis; SC#1 + SC#4)
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md (Provo FY2024 dry-run baseline: EX→operating $346,484,274.68, RV→revenue $285,684,200.65 on the org1 tree — the fund-tree total for the same FY should match the same grand total, since totals are column-independent)
    - docs/utah-entity-mapping.md (Provo City → Utah County)
  </read_first>
  <action>Checkpoint — the operator spot-checks all 10 Utah cities in the live app and reconciles Provo all-funds operating total against its published ACFR (steps in how-to-verify). No code or DB writes in this task.</action>
  <files>(verification only — no files modified)</files>
  <what-built>
    All 10 Utah cities (SLC from 69-01 + these 9) now have operating + revenue for FY2014–FY2025 from Transparent Utah, with a fund1→org1→cat1 icicle, every row durably sourced. Each city becomes visible in the live app once its first budget row lands (production reads ev-accounts-api `/api/treasury/cities`).
  </what-built>
  <how-to-verify>
    1. In the live app (treasurytracker.empowered.vote), confirm all 10 Utah cities appear under "Utah" in the city picker.
    2. Open 2–3 cities (e.g. Provo, Ogden, West Valley City): confirm a Money Out / operating icicle renders with fund names at the top level (General Fund, Power, Water…), a Money In / revenue tab renders, and the source chip shows Transparent Utah.
    3. Provo ACFR reconciliation (the 2nd of the ≥2-city SC#4 spot-checks): note Provo's loaded all-funds operating total for a recent complete FY and compare against Provo's published ACFR all-funds expenditure figure for the same FY. Cross-check the fund-tree grand total for Provo FY2024 against the 68-03 dry-run baseline ($346.5M EX / $285.7M RV) — totals are column-independent so they should match. Expect explainable variance vs the ACFR, not penny-exact.
    4. Flag any city whose tree is empty, whose total is implausible, or whose source chip is missing.
  </how-to-verify>
  <acceptance_criteria>
    - All 10 Utah cities appear in the live city picker under "Utah".
    - The ≥2 spot-checked cities render an operating icicle with fund1 names at the top and a revenue tab, each showing a Transparent Utah source chip.
    - Provo's loaded all-funds operating total reconciles within explainable tolerance of its published ACFR (or the variance is documented), and the Provo FY2024 fund-tree grand total matches the 68-03 baseline.
    - No city has an empty tree or a missing source chip.
  </acceptance_criteria>
  <verify>
    <human-check>See the how-to-verify steps above; confirm the loaded total reconciles within explainable tolerance of the published reference.</human-check>
  </verify>
  <done>All 10 Utah cities render with sourced op+rev and Provo reconciles within explainable tolerance (SC#1 + SC#4).</done>
  <resume-signal>Type "approved" once all 10 cities render with sourced op+rev and Provo reconciles, or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → BigQuery (read) | Public CC BY 4.0 dataset via live ADC; entity_name from the curated mapping (no untrusted input). |
| local script → Supabase (write) | Fixed RPCs, gitignored service-role key, serial single-operator main-tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Tampering | entity_name selection across 9 cities | mitigate | Match EXACTLY from docs/utah-entity-mapping.md (never LIKE) — decoys (North/South Ogden, Washington Terrace) would load the wrong entity. |
| T-69-06 | Tampering | never-overwrite guard | mitigate | Guard stays ON; any different-source (muni,fy,dataset) row SKIPs untouched (SC#3); logged. |
| T-69-07 | Information Disclosure | Supabase service key + ADC | mitigate | Never log/echo creds; `.env` gitignored; secrets stay out of summaries/commits. |
| T-69-08 | Denial of Service (cost) | 9-city × 12-FY × 2-type BigQuery sweep | mitigate | Column-project + entity/FY/type filter per query; dry-run confirms $0; stays inside the 1 TB/month free tier. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; no legitimacy gate needed. |
</threat_model>

<verification>
- All 9 non-canary cities have op+rev rows for FY2014–FY2025, Transparent-Utah-sourced, FY2026 absent (SC#1).
- All 10 cities render in the live app with the fund-top icicle + source chips (SC#1).
- Provo reconciles within explainable tolerance of its ACFR (SC#4, 2nd spot-check).
- Never-overwrite guard honored across the sweep (SC#3).
</verification>

<success_criteria>
- UCITY-01 (operating/EX) + UCITY-02 (revenue/RV) satisfied for all 10 Utah cities, FY2014–FY2025, durably sourced.
- ≥2 cities reconciled (SLC in 69-01 + Provo here) for SC#4.
</success_criteria>

<output>
Create `.planning/phases/69-utah-city-budgets-load/69-02-SUMMARY.md` when done — record per-city per-FY op+rev totals, the Provo reconciliation figure + variance, any never-overwrite SKIPs, and the app spot-check result.
</output>
