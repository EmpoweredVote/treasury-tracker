---
phase: 69-utah-city-budgets-load
plan: 03
type: execute
wave: 3
depends_on: ["69-01", "69-02"]
files_modified:
  - scripts/loadUTPopulation.js
autonomous: true
requirements: [UCITY-01]
user_setup: []

must_haves:
  truths:
    - "Each of the 10 Utah cities has a non-zero population and a population_year"
    - "Per-capita ($/resident) renders for each city, labeled with its Census source year"
    - "No city's non-zero population is ever lowered to 0"
  artifacts:
    - path: "scripts/loadUTPopulation.js"
      provides: "Census-vintage population loader for the 10 Utah cities"
      contains: "EXPECTED_CITIES"
  key_links:
    - from: "loadUTPopulation.js"
      to: "treasury.municipalities"
      via: "UPDATE population, population_year WHERE name=? AND state='UT'"
      pattern: "population_year"
---

<objective>
Build a Utah population loader (`scripts/loadUTPopulation.js`, mirroring `loadTXPopulation.js`/`loadORPopulation.js`) that sets a single recent Census vintage population + `population_year` for all 10 Utah cities (D-69-03), enabling per-capita display (SC#2). Runs after 69-01 + 69-02 so all 10 municipality rows already exist.

Purpose: per-capita ($/resident) is part of UCITY-01 parity and SC#2 — the budget loaders create cities with population 0; this sets the real figure.
Output: all 10 Utah cities carry a non-zero population + population_year; per-capita renders in the app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-utah-city-budgets-load/69-CONTEXT.md
@docs/utah-entity-mapping.md
@scripts/loadORPopulation.js
@scripts/loadTXPopulation.js

<interfaces>
<!-- The population-loader pattern to mirror (loadTXPopulation.js / loadORPopulation.js). -->

Census Population Estimates place file (single vintage, D-69-03):
  URL pattern: https://www2.census.gov/programs-surveys/popest/datasets/2020-2024/cities/totals/sub-est2024_<STATEFIPS>.csv
  Utah STATEFIPS = 49  → sub-est2024_49.csv
  CSV columns (verified for TX/OR analogs, vintage 2024): SUMLEV at col 0 ('162' = incorporated place),
    NAME at col 8, POPESTIMATE2024 at col 15. POP_YEAR = 2024.
  normalizeCensusName strips a trailing " city"/" town"/" village" (lowercase) from NAME →
    "Salt Lake City city" → "Salt Lake City"; "West Valley City city" → "West Valley City";
    "St. George city" → "St. George". These must equal municipalities.name EXACTLY.

DB update contract (idempotent, never-lower-to-0):
  UPDATE treasury.municipalities SET population=?, population_year=2024 WHERE name=? AND state='UT'
  Skip if current population already equals the target with the same population_year.
  NEVER write population=0 over a non-zero value (D-69-03 lock).

Exact municipalities.name for the 10 cities (from docs/utah-entity-mapping.md — these are the DB names,
i.e. the entity_name minus the trailing " City" for most, but SLC + West Valley City keep "City"):
  Layton | Lehi | Ogden | Orem | Provo | Salt Lake City | Sandy | St. George | West Jordan | West Valley City
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create loadUTPopulation.js for the 10 Utah cities</name>
  <files>scripts/loadUTPopulation.js</files>
  <read_first>
    - scripts/loadORPopulation.js (closest analog — copy its download/redirect-follow + CSV-column-guard + idempotent-update structure)
    - scripts/loadTXPopulation.js (second analog — the EXPECTED_CITIES + KNOWN_VALUES sanity-check pattern)
    - docs/utah-entity-mapping.md (the 10 city DB names; note SLC + West Valley City retain "City")
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-03: single vintage, store population_year, never lower a non-zero to 0)
  </read_first>
  <action>
    Create `scripts/loadUTPopulation.js` modeled on `scripts/loadORPopulation.js`. Set `CSV_URL` to the Utah place file `https://www2.census.gov/programs-surveys/popest/datasets/2020-2024/cities/totals/sub-est2024_49.csv` (state FIPS 49), `CSV_PATH` to a temp path, and `POP_YEAR = 2024` (Claude's discretion per D-69-03 — the latest reliable Census Population Estimates vintage; switch to a newer vintage only if available at run time).

    Set `EXPECTED_CITIES` to the 10 DB municipality names EXACTLY as they appear in treasury.municipalities: `Layton`, `Lehi`, `Ogden`, `Orem`, `Provo`, `Salt Lake City`, `Sandy`, `St. George`, `West Jordan`, `West Valley City`. Reuse `normalizeCensusName` (strips a trailing lowercase " city"/" town"/" village") — verify it correctly yields "Salt Lake City" from "Salt Lake City city" and "West Valley City" from "West Valley City city" (it strips only the final lowercase " city", leaving the proper-noun "City"). If the CSV name for any city does not normalize to the exact DB name, add a small explicit name-map rather than loosening the matcher.

    Keep the CSV-column guard (SUMLEV col 0, NAME col 8, POPESTIMATE2024 col 15) and abort with a clear message if the format drifts. Filter rows to `SUMLEV === '162'` (incorporated place). Populate `KNOWN_VALUES` with the 2024 figures read from the actual Utah CSV during this task (do not hardcode guesses — read them from the downloaded file) for the >1% drift sanity warning.

    Implement the idempotent UPDATE exactly like the analog: for each expected city, look up current population/population_year; SKIP if already equal to the target with population_year=2024; otherwise `UPDATE treasury.municipalities SET population=?, population_year=2024 WHERE name=? AND state='UT'`. NEVER write a 0 over a non-zero value (D-69-03). Support a `--dry-run` flag that prints intended updates with zero writes.
  </action>
  <verify>
    <automated>node --check scripts/loadUTPopulation.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/loadUTPopulation.js` exits 0.
    - Grep: the file sets `CSV_URL` to the `sub-est2024_49.csv` Utah place file and `POP_YEAR = 2024`.
    - Grep: `EXPECTED_CITIES` contains all 10 names including the literal `'Salt Lake City'` and `'West Valley City'`.
    - The CSV-column guard and the never-lower-to-0 / idempotent-skip logic are present (mirrors the analog).
    - A `--dry-run` flag exists and performs zero DB writes.
  </acceptance_criteria>
  <done>loadUTPopulation.js exists, parses the Utah Census place file, maps all 10 cities to their DB names, and updates population idempotently without ever lowering a non-zero to 0.</done>
</task>

<task type="auto">
  <name>Task 2: Dry-run then live-load population for all 10 Utah cities</name>
  <files>scripts/loadUTPopulation.js</files>
  <read_first>
    - scripts/loadUTPopulation.js (the loader from Task 1)
    - .planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md and 69-02-SUMMARY.md (confirm all 10 municipality rows now exist from the budget loads)
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (SC#2 per-capita)
  </read_first>
  <action>
    Source the gitignored `.env` for `SUPABASE_SERVICE_KEY`. First run `node scripts/loadUTPopulation.js --dry-run` and confirm it resolves all 10 cities from the Census CSV with plausible Utah populations (e.g. Salt Lake City ~200K, Provo ~115K, West Valley City ~140K, St. George ~100K) and that every EXPECTED_CITY was found (no "Missing cities" abort).

    Then run `node scripts/loadUTPopulation.js` (live). Confirm all 10 cities update (or SKIP if already set) with population_year=2024 and that no city is left at population 0. The 10 municipality rows must already exist (created by the budget loads in 69-01 + 69-02) — if any city is not found in the DB, stop and report (the budget sweep for that city did not complete).

    Verify via Supabase that each of the 10 Utah cities now has a non-zero `population` and `population_year = 2024`.
  </action>
  <verify>
    <automated>node scripts/loadUTPopulation.js --dry-run</automated>
    <human-check>Supabase: SELECT name, population, population_year FROM treasury.municipalities WHERE state='UT' AND name IN (the 10 cities) — confirm all 10 have non-zero population and population_year=2024.</human-check>
  </verify>
  <acceptance_criteria>
    - The dry-run resolves all 10 cities from the CSV with no "Missing cities" abort and plausible populations.
    - DB query: `SELECT count(*) FROM treasury.municipalities WHERE state='UT' AND name IN (<10 cities>) AND population > 0 AND population_year = 2024` returns 10.
    - No Utah city among the 10 has population = 0 after the run.
    - Re-running the loader reports SKIP for already-set cities (idempotent).
  </acceptance_criteria>
  <done>All 10 Utah cities carry a non-zero Census-2024 population + population_year, so per-capita renders (SC#2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → Census (read) | Downloads a public Census Population Estimates CSV over HTTPS; no auth, no untrusted input beyond the fixed file. |
| local script → Supabase (write) | UPDATEs treasury.municipalities population columns via the gitignored service-role key; serial single-operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-09 | Tampering | population overwrite | mitigate | Idempotent UPDATE keyed on exact name+state='UT'; never write 0 over a non-zero value (D-69-03 lock); dry-run first. |
| T-69-10 | Tampering | Census CSV format drift | mitigate | Column guard (SUMLEV/NAME/POPESTIMATE2024 positions) aborts on drift; KNOWN_VALUES >1% drift warning. |
| T-69-11 | Information Disclosure | Supabase service key | mitigate | Never log/echo the key; `.env` gitignored; out of summaries/commits. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (`@supabase/supabase-js` already present); Node built-ins only; no legitimacy gate needed. |
</threat_model>

<verification>
- `node --check` passes for loadUTPopulation.js.
- All 10 Utah cities have non-zero population + population_year=2024 (SC#2).
- Loader is idempotent and never lowers a non-zero population to 0.
</verification>

<success_criteria>
- D-69-03 satisfied: single Census vintage population + population_year for all 10 cities; per-capita renders (SC#2).
</success_criteria>

<output>
Create `.planning/phases/69-utah-city-budgets-load/69-03-SUMMARY.md` when done — record the per-city populations + vintage, and the per-capita confirmation.
</output>
