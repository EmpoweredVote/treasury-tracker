---
phase: 69-utah-city-budgets-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadUtahTransparency.js
  - scripts/loadUtahTransparency.test.mjs
autonomous: false
requirements: [UCITY-01, UCITY-02]
user_setup:
  - service: Transparent Utah BigQuery
    why: "Live BigQuery reads for the SLC canary load (UCITY-01/02). Access granted + verified 2026-06-19; this re-confirms a live session before any write."
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env (Supabase service role) — already present locally; never log or commit"
    dashboard_config:
      - task: "Ensure a live gcloud session exists (CLI cred + ADC) before running the live load — the empowered.vote Workspace reauth policy expires both creds; refresh interactively if a query throws invalid_grant/invalid_rapt"
        location: "Real terminal: prepend the gcloud SDK bin to PATH, then `gcloud auth login` AND `gcloud auth application-default login` (see docs/utah-bigquery-access.md)"

must_haves:
  truths:
    - "fetchFromBigQuery aggregates fund1, org1, cat1 and returns a 3-level tree"
    - "Salt Lake City operating (EX) and revenue (RV) load for FY2014–FY2025, fund1 at the top of the icicle"
    - "Every loaded SLC row carries data_source_name 'Transparent Utah' and a non-null durable source_url"
    - "SLC all-funds operating total reconciles within tolerance of a published ACFR figure"
    - "The never-overwrite guard leaves any pre-existing custom-source SLC row unchanged"
  artifacts:
    - path: "scripts/loadUtahTransparency.js"
      provides: "3-level fund→org→cat loader (D-69-01)"
      contains: "GROUP BY fund1, org1, cat1"
    - path: "scripts/loadUtahTransparency.test.mjs"
      provides: "offline unit tests for the 3-level tree"
      contains: "fund1"
  key_links:
    - from: "fetchFromBigQuery"
      to: "buildTree"
      via: "rows keyed fund1/org1/cat1 → topCol fund1, subCol org1, itemCol cat1"
      pattern: "topCol.*fund1"
    - from: "importEntityData"
      to: "treasury_sync_city_budget"
      via: "RPC write with tree + total + Transparent Utah attribution"
      pattern: "treasury_sync_city_budget"
---

<objective>
Refactor `scripts/loadUtahTransparency.js` to build a genuine 3-level icicle tree — top=`fund1`, sub=`org1`, leaf=`cat1` (D-69-01) — then prove it on the Salt Lake City canary: dry-run, live-load FY2014–FY2025 operating + revenue, and reconcile the all-funds operating total against SLC's published ACFR (SC#1, SC#3, SC#4).

Purpose: SLC is the largest entity (125 funds) and the hardest stress test of the fund-top tree, plus a SC#4 reconciliation target. Proving it here de-risks the 9-city sweep (69-02).
Output: Refactored loader + updated unit tests + SLC operating/revenue rows for FY2014–FY2025 in treasury.budgets, all carrying Transparent Utah attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-utah-city-budgets-load/69-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/utah-entity-mapping.md
@docs/utah-bigquery-access.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md

<interfaces>
<!-- Current loader contract (scripts/loadUtahTransparency.js, post-68-03). The refactor changes the fetch SQL + the buildTree mapping; the RPC write, never-overwrite guard, and source attribution stay intact. -->

Exports (keep names stable — unit tests import them):
  amt(v) -> Number                       // money/number parser; "(200)"→-200, 0/null→0
  typeToDataset(type) -> 'operating'|'revenue'|'salaries'|null   // EX→operating, RV→revenue
  neverOverwriteDecision(existingSource, runSource='Transparent Utah') -> 'skip'|'refresh'
  buildTree(rows, opts) -> { tree, total }   // opts: { topCol, subCol, itemCol }
  DATA_SOURCE_NAME = 'Transparent Utah'
  entitySourceUrl() -> 'https://transparent.utah.gov'

Compact tree shape buildTree emits (must stay 3 levels, ≤3 — ground rule 3):
  [{ n: top, a, c: [{ n: sub, a, i: [{ d: leaf, a, aa }] }] }]   // sorted desc, zero rows skipped, negatives retained

DB write contract (unchanged):
  treasury_ensure_municipality(p_name, p_state='UT', p_entity_type='city', p_population=0) -> municipalityId
  treasury_sync_city_budget(p_municipality_id, p_fiscal_year, p_dataset_type,
                            p_total, p_tree, p_row_count,
                            p_data_source_name, p_source_url, p_source_date) -> result
  findConflictingBudget(municipalityId, fy, datasetType, sourceName)  // returns existing row only if a DIFFERENT source → SKIP

CLI: node scripts/loadUtahTransparency.js --entity "<exact entity_name>" --fy <y> [--fy <y2> ...] [--type EX|RV] [--dry-run] [--source-date YYYY-MM-DD]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor loader + tests to the fund1→org1→cat1 three-level tree (D-69-01)</name>
  <files>scripts/loadUtahTransparency.js, scripts/loadUtahTransparency.test.mjs</files>
  <read_first>
    - scripts/loadUtahTransparency.js (the file being modified — read fully first)
    - scripts/loadUtahTransparency.test.mjs (the test file being modified)
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-01 is the exact spec; D-69-02 all-funds basis)
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md (why org1-as-top was too wide; the bugs already fixed)
    - docs/utah-entity-mapping.md (tree-column section: function1 unusable, fund1 legible)
  </read_first>
  <behavior>
    - buildTree with rows having fund1/org1/cat1 and opts {topCol:'fund1', subCol:'org1', itemCol:'cat1'} produces top nodes named after fund1, sub nodes named after org1, leaf items {d: cat1, a, aa}.
    - Grand total equals the sum of all non-zero rows (negative offsets retained, zero rows skipped) — column-independent, unchanged from current behavior.
    - Top-level nodes sorted by amount descending; each parent total equals the sum of its children.
    - A row with null fund1 falls back to a 'Unknown' top; null org1 → 'General' sub.
    - amt, typeToDataset, neverOverwriteDecision behavior unchanged (existing assertions still pass).
  </behavior>
  <action>
    Change `fetchFromBigQuery` so its SQL selects three grouped columns and returns three keyed columns. The query must be `SELECT COALESCE(fund1,'Unknown') AS fund1, COALESCE(org1,'General') AS org1, COALESCE(cat1,'General') AS cat1, SUM(amount) AS amount FROM \`ut-sao-transparency-prod.transaction.transaction\` WHERE entity_name = @entity AND fiscal_year = @fy AND type = @type GROUP BY fund1, org1, cat1`. Pass `fiscal_year` as a Number (INT64) via params {entity, fy: Number(fiscalYear), type}. Return rows mapped to `{ fund1, org1, cat1, amount, fiscal_year, type }` (drop the old function1/cat1 aliasing and the `topCol === 'cat1' ? 'org1' : 'cat1'` sub-selection logic). Retire the `--source-column` default of `org1` from 68-03; this loader now hard-targets the fund tree.

    Update both `buildTree` call sites — `importEntityData` and the dry-run/summary path in `main` — to pass `{ topCol: 'fund1', subCol: 'org1', itemCol: 'cat1' }` instead of `{ topCol: 'function1', subCol: 'cat1', itemCol: 'org1' }`. Keep `buildTree` generic (it already reads topCol/subCol/itemCol from opts and emits the {n,a,c}/{n,a,i} shape) — no change to its internals beyond confirming the leaf uses `row[itemCol]`.

    Keep all-funds basis (D-69-02): do NOT add a fund filter or split governmental vs enterprise into separate datasets — `fund1` at the top gives that separation for free. Keep the dataset_type values 'operating'/'revenue'.

    Update `scripts/loadUtahTransparency.test.mjs`: change the FIXTURE rows to carry `fund1`/`org1`/`cat1` keys, and update the buildTree assertions to call `buildTree(FIXTURE, { topCol:'fund1', subCol:'org1', itemCol:'cat1' })`. Assert top nodes are fund names, sub nodes are org names, leaf items use cat1 as `d`. Preserve the existing total/sorting/zero-skip/negative-retain/Unknown-fallback assertions. Keep the amt/typeToDataset/neverOverwriteDecision test blocks unchanged.

    Do not touch `treasury_sync_city_budget`/`treasury_ensure_municipality` call shapes, the never-overwrite guard, or `entitySourceUrl()`.
  </action>
  <verify>
    <automated>node --check scripts/loadUtahTransparency.js && node --test scripts/loadUtahTransparency.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/loadUtahTransparency.js` exits 0.
    - `node --test scripts/loadUtahTransparency.test.mjs` reports all tests passing, 0 failing.
    - Grep: `scripts/loadUtahTransparency.js` contains the literal `GROUP BY fund1, org1, cat1`.
    - Grep: both buildTree call sites pass `topCol: 'fund1'` (no remaining `topCol: 'function1'`); run `grep -c "topCol: 'function1'"` → 0.
    - Grep: the test file references `fund1` in the FIXTURE and the buildTree opts.
  </acceptance_criteria>
  <done>The loader builds a fund1→org1→cat1 three-level tree, all offline unit tests pass, and the old function1/org1-top mapping is gone.</done>
</task>

<task type="auto">
  <name>Task 2: SLC canary — dry-run, live-load FY2014–FY2025 op+rev, DB-verify sourcing</name>
  <files>scripts/loadUtahTransparency.js</files>
  <read_first>
    - scripts/loadUtahTransparency.js (the refactored loader from Task 1)
    - docs/utah-bigquery-access.md (gcloud PATH prepend, ADC, dry-run-to-confirm-$0, reauth caveat)
    - docs/utah-entity-mapping.md (exact entity_name: `Salt Lake City`, govt_lvl City, FY2014–FY2026; EXCLUDE FY2026)
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-04 FY range + SLC-first canary; carried-forward source attribution locks)
  </read_first>
  <action>
    First confirm a live BigQuery session (the empowered.vote Workspace reauth policy expires both the CLI cred and ADC; if any query throws `invalid_grant`/`invalid_rapt`, the operator must re-run `gcloud auth login` and `gcloud auth application-default login` in a real terminal per docs/utah-bigquery-access.md). Source the gitignored `.env` for `SUPABASE_SERVICE_KEY` and prepend the gcloud SDK bin to PATH before invoking the loader.

    Dry-run SLC for one recent year to confirm the 3-level tree shape and a sane total, zero writes: `node scripts/loadUtahTransparency.js --entity "Salt Lake City" --fy 2025 --dry-run`. Confirm fund1 names appear at the top level (General Fund, Power, Water, Airport, Debt Service, etc.) and the printed top-level count reflects SLC's many funds. Stay $0 — the query is column-projected + entity/FY/type filtered (a broad unfiltered single-FY scan is ~20–26 GB; the filtered query is a tiny fraction of the 1 TB/month free tier).

    Then live-load Salt Lake City operating + revenue for the complete years FY2014 through FY2025 (EXCLUDE FY2026 — partial in-progress year, D-69-04). Pass all 12 fiscal years in one invocation: `node scripts/loadUtahTransparency.js --entity "Salt Lake City" --fy 2014 --fy 2015 --fy 2016 --fy 2017 --fy 2018 --fy 2019 --fy 2020 --fy 2021 --fy 2022 --fy 2023 --fy 2024 --fy 2025`. The loader defaults to types EX,RV (operating + revenue). The never-overwrite guard (`findConflictingBudget`) stays on; SLC is expected all-new — if any FY/dataset SKIPs because a different `data_source` pre-exists, record it and leave it untouched (carried-forward lock; confirms SC#3).

    Verify in the DB via Supabase that SLC has operating + revenue rows across FY2014–FY2025 and that every row carries `data_source_name = 'Transparent Utah'` with a non-null `source_url`. SLC must NOT have any FY2026 row from this run.
  </action>
  <verify>
    <automated>node scripts/loadUtahTransparency.js --entity "Salt Lake City" --fy 2025 --dry-run</automated>
    <human-check>Supabase query: for the Salt Lake City municipality_id, SELECT fiscal_year, dataset_type, data_source, source_url FROM treasury.budgets — confirm operating+revenue rows for FY2014–FY2025, every row data_source='Transparent Utah' with non-null source_url, and NO FY2026 row.</human-check>
  </verify>
  <acceptance_criteria>
    - The dry-run prints a 3-level tree with `fund1` names at the top level for Salt Lake City FY2025, zero DB writes.
    - DB query: `SELECT count(*) FROM treasury.budgets WHERE municipality_id = <SLC id> AND dataset_type IN ('operating','revenue') AND fiscal_year BETWEEN 2014 AND 2025` returns rows for both dataset_types across the available FYs.
    - DB query: every Salt Lake City budget row from this run has `data_source = 'Transparent Utah'` and a non-null `source_url` (the durable `https://transparent.utah.gov` portal).
    - DB query: no Salt Lake City `treasury.budgets` row exists for `fiscal_year = 2026`.
    - Any never-overwrite SKIP (different pre-existing data_source) is logged and the pre-existing row is unchanged.
  </acceptance_criteria>
  <done>Salt Lake City shows operating + revenue for FY2014–FY2025 with the fund-top tree, every row Transparent-Utah-sourced, FY2026 excluded, never-overwrite guard honored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SLC ACFR reconciliation (SC#4) — confirm all-funds operating total is sane</name>
  <read_first>
    - .planning/phases/69-utah-city-budgets-load/69-CONTEXT.md (D-69-02 all-funds reconciliation basis; SC#4)
    - .planning/STATE.md (prior CA ACFR-variance precedent: all-governmental-funds vs gov-activities is a documented, explainable variance, not penny-exact)
  </read_first>
  <action>Checkpoint — the operator reconciles the loaded SLC all-funds operating total against SLC published ACFR (steps in how-to-verify). No code or DB writes in this task.</action>
  <files>(verification only — no files modified)</files>
  <what-built>
    SLC operating (EX) + revenue (RV) loaded for FY2014–FY2025 from Transparent Utah on an all-funds basis, with a fund1→org1→cat1 icicle. The loaded all-funds operating total for a recent FY (e.g. FY2024 or FY2025) is the figure to reconcile against SLC's published ACFR.
  </what-built>
  <how-to-verify>
    1. Note the loaded SLC all-funds operating total for a recent complete FY (read it from the loader output or a DB query on treasury.budgets total_budget for that FY/operating).
    2. Open Salt Lake City's published ACFR for the same fiscal year (SLC posts ACFRs on the city finance/comptroller site; the government-wide Statement of Activities total expenses or the all-funds expenditures total is the basis-matched comparator).
    3. Confirm the loaded total is in the same ballpark — an explainable variance is expected (all-funds includes proprietary/internal-service funds; basis differences documented across prior CA county reconciliations). Penny-exact is NOT required.
    4. If the loaded total is wildly off (orders of magnitude, or implausible for SLC), flag it — that signals a tree/aggregation or FY-mapping bug to fix before the 9-city sweep.
  </how-to-verify>
  <acceptance_criteria>
    - The loaded SLC all-funds operating total for the chosen FY is within an explainable tolerance of the published ACFR all-funds expenditure figure for the same FY, OR any variance is documented with its basis explanation.
    - No order-of-magnitude discrepancy remains unexplained.
  </acceptance_criteria>
  <verify>
    <human-check>See the how-to-verify steps above; confirm the loaded total reconciles within explainable tolerance of the published reference.</human-check>
  </verify>
  <done>SLC all-funds operating total reconciled within explainable tolerance of the published ACFR (SC#4); the 9-city sweep is unblocked.</done>
  <resume-signal>Type "approved" once the SLC total reconciles sanely against the ACFR, or describe the discrepancy to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → BigQuery (read) | Reads a public CC BY 4.0 dataset; credential is live ADC (gitignored, interactive). No untrusted input — entity_name is from the curated mapping doc. |
| local script → Supabase (write) | Writes via two fixed RPCs using the gitignored service-role key. Single-operator, serial, main-tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure | gitignored `.env` (Supabase service key) + live ADC | mitigate | Never log or echo the key/ADC token; `.env` stays gitignored; do not paste secrets into summaries or commits. |
| T-69-02 | Tampering | never-overwrite guard (`findConflictingBudget`) | mitigate | Guard stays ON; any different-source (muni,fy,dataset) row SKIPs untouched — confirmed in Task 2 acceptance + SC#3. |
| T-69-03 | Tampering | BigQuery SQL inputs (entity_name/fy/type) | mitigate | Parameterized query (`@entity/@fy/@type`); entity_name matched EXACTLY from docs/utah-entity-mapping.md (never LIKE — decoys like North Salt Lake exist). |
| T-69-04 | Denial of Service (cost) | unfiltered BigQuery scan | mitigate | Column-project + filter entity/FY/type; `--dry-run` confirms $0 before live reads; stays inside 1 TB/month free tier. |
| T-69-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan (`@google-cloud/bigquery` + `@supabase/supabase-js` already in package.json from Phase 68); no legitimacy gate needed. |
</threat_model>

<verification>
- `node --check` + `node --test` pass for the refactored loader.
- SLC operating + revenue rows present for FY2014–FY2025, fund-top tree, every row Transparent-Utah-sourced, FY2026 absent.
- SLC all-funds operating total reconciles within explainable tolerance of the published ACFR (SC#4).
- Never-overwrite guard honored (SC#3).
</verification>

<success_criteria>
- D-69-01 refactor complete: `GROUP BY fund1, org1, cat1`, buildTree mapped fund1→org1→cat1, tests green.
- Salt Lake City canary loaded for FY2014–FY2025 op+rev, durably sourced, FY2026 excluded.
- ACFR reconciliation passes (SC#4) — the 9-city sweep (69-02) is unblocked.
</success_criteria>

<output>
Create `.planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md` when done — record the refactor, the SLC totals per FY, the ACFR reconciliation figure + variance, and any never-overwrite SKIPs.
</output>
