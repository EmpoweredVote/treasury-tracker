---
phase: 73-utah-verification-source-chain-audit-uat
plan: "73-02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [UVER-01]
must_haves:
  truths:
    - "Across the full Utah backfill cohort (Phase 69 city op/rev, Phase 70 county-gov op/rev, Phase 71 city salaries, Phase 72 enrichment), every backfilled budget/salary row carries source attribution: data_source='Transparent Utah' + source_url='https://transparent.utah.gov' (D-73-04, D-73-05)"
    - "The bare-domain source_url 'https://transparent.utah.gov' is the accepted durable human-page bar (uniform across budgets AND salaries) — it is NOT flagged as fragile; zero NULL source_url on Transparent Utah rows (D-73-04, D-73-05)"
    - "Zero residue: the Phase 70-02 phantom county rows are gone, Phase 72 universal enrichment holds at 4,476 rows / 0 duplicate name_keys, no orphaned/placeholder/test rows from the backfill (D-73-05)"
    - "Salaries names-free PII guard re-verified: 0 PII tokens (vendor_name, title, hourly_rate, gender, name) across the serialized hierarchy of all 120 Utah salary rows (D-73-06)"
    - "The 3,536 Phase-72 enrichment rows are bleed-safe (0 $-leaks, 0 city-name leaks); the 4 pre-existing non-P72 $-leak rows are reported as a documented follow-up, NOT fixed (D-73-07)"
    - "Read-only only; production DB; no live BigQuery; $0 (D-73-08, D-73-11, D-73-12)"
  artifacts:
    - path: ".planning/phases/73-utah-verification-source-chain-audit-uat/73-02-SUMMARY.md"
      provides: "The source-chain durability + bleed-safety audit result: per-phase cohort counts, NULL-source_url breakdown, fragility scan, residue scan, PII re-verification, enrichment bleed scan — the UVER-01 part-B evidence record"
      contains: "per-phase cohort counts (69: 240, 70: 120, 71: 120, 72: 3,536), Transparent Utah NULL-source_url count (0), fragile-URL count (0), residue count (0), PII-token count (0 across 120 salary rows), P72 $-leak/city-name-leak count (0), the 4 documented pre-existing $-leak rows as a follow-up"
  key_links:
    - from: "the full Utah backfilled cohort (treasury.budgets + category_enrichment for the 10 cities + 5 county govs)"
      to: "durable human-page source attribution (https://transparent.utah.gov on every budget + salary row), zero residue, names-free + bleed-safe"
      via: "read-only attribution + NULL-source_url + fragility + residue + PII + bleed scans (reuse scripts/audit_task1.mjs + audit_task2.mjs parameterized to the Utah ids)"
      pattern: "scan attribution + NULL source_url → fragility + zero-residue scan → PII + bleed re-verify → document the durable source chain"
---

<objective>
Satisfy UVER-01 part B: a full-cohort source-chain durability + bleed-safety audit of the Utah backfill —
every backfilled budget/salary row carries durable human-page source attribution (the uniform
https://transparent.utah.gov bare domain, which passes the durability bar), zero residue, salaries are
names-free, and Phase-72 enrichment is bleed-safe. Read-only, production DB, $0. (Reconciliation is 73-01;
UAT is 73-03.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/73-utah-verification-source-chain-audit-uat/73-CONTEXT.md
@.planning/phases/69-utah-city-budgets-load/69-02-SUMMARY.md
@.planning/phases/70-utah-county-budgets-linking/70-02-SUMMARY.md
@.planning/phases/71-utah-city-salaries-compensation/71-01-SUMMARY.md
@.planning/phases/72-utah-enrichment-parity/72-01-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- PRODUCTION DB read-only via repo .env, schema treasury. NEVER mcp__supabase-local. NEVER query
     BigQuery live (rollup already loaded; cost incident). Columns: source_url, data_source, total_budget,
     hierarchy; exact-count head queries (1000-row cap). No writes. -->

Durable source (D-73-04): data_source='Transparent Utah', source_url='https://transparent.utah.gov' on EVERY budget + salary row. The bare domain is a stable, version-independent, citizen-openable page — it PASSES the durability bar. Uniform single-domain attribution is the Utah norm (unlike CA's /d/ deep links + publicpay split), NOT a gap.
Cohort (D-73-05): Phase 69 (10 cities op/rev = 240 budget rows) + Phase 70 (5 county-gov op/rev = 120 budget rows) + Phase 71 (120 salary rows = 10 cities × 12 FY, dataset_type='salaries') + Phase 72 (3,536 universal category_enrichment rows; total category_enrichment = 4,476, 0 duplicates).
Residue checks (D-73-05): the 70-02 phantom entity_type='city' "<X> County" rows MUST be gone; the 72 universal-enrichment count MUST hold at 4,476 with 0 duplicate name_keys; no orphan/placeholder/test rows.
PII guard (D-73-06): salaries store ONLY org1/cat1/SUM (D-71-01) — 0 PII tokens in any stored salary hierarchy. Reuse the test-guard pattern from scripts/loadUtahTransparency.test.mjs.
Bleed (D-73-07): the 3,536 P72 rows = 0 $-leaks + 0 city-name leaks. The 4 pre-existing non-P72 rows (parking meter, harbor and port enterprise fund, sewer enterprise fund, solid waste enterprise fund; generated 2026-03-28) carry $-figures — DOCUMENT as a follow-up, do NOT fix.
Reuse scripts/audit_task1.mjs + audit_task2.mjs parameterized to the Utah municipality ids.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit source attribution + NULL source_url + per-phase cohort counts across the full Utah cohort</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - .planning/phases/73-utah-verification-source-chain-audit-uat/73-CONTEXT.md (D-73-04, D-73-05)
    - scripts/audit_task1.mjs (reusable attribution / NULL-source_url scan)
    - .planning/phases/72-utah-enrichment-parity/72-01-SUMMARY.md (4,476-row baseline + dedup fix)
  </read_first>
  <action>
    Source .env. Resolve the full Utah cohort municipality ids: the 10 cities (entity_type='city', display
    names: Salt Lake City, West Valley City, Provo, Orem, Ogden, Layton, Lehi, Sandy, St. George, West
    Jordan) + the 5 county governments (entity_type='county': Salt Lake, Utah, Davis, Weber, Washington).
    With READ-ONLY probes (schema treasury): count per-phase cohort rows — city op/rev (Phase 69, expect
    240), county-gov op/rev (Phase 70, expect 120), salaries (Phase 71, expect 120), and universal
    enrichment (Phase 72, expect 3,536 of the 4,476 universal total). For budgets + salaries, count rows by
    source attribution: rows carrying data_source='Transparent Utah' + source_url='https://transparent.utah.gov',
    and any NULL-source_url rows. Confirm NULL-source_url Transparent Utah rows = 0. No BigQuery, no writes.
  </action>
  <acceptance_criteria>
    - Per-phase cohort counts recorded (69 city op/rev ≈240, 70 county-gov op/rev ≈120, 71 salaries ≈120, 72 enrichment ≈3,536); category_enrichment universal total = 4,476.
    - NULL-source_url Transparent Utah budget/salary rows = 0; every budget + salary row carries data_source='Transparent Utah' + source_url='https://transparent.utah.gov'.
  </acceptance_criteria>
  <done>Source attribution coverage + the NULL-source_url breakdown + per-phase cohort counts across the full Utah cohort are established read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Fragility + zero-residue scan, PII re-verification, and enrichment bleed scan</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - The Task 1 attribution results
    - scripts/audit_task2.mjs (fragility / residue scan patterns)
    - scripts/loadUtahTransparency.test.mjs (the names-free PII-guard assertion pattern)
  </read_first>
  <action>
    With READ-ONLY probes:
    (a) Fragility — assert the distinct Utah source_url ('https://transparent.utah.gov') is a durable human
        page and NOT fragile/version-specific (no export tokens, session URLs, /resource/*.json or BigQuery
        endpoints, version/date query params). Count fragile URLs (must be 0). The bare domain passes (D-73-04).
    (b) Zero residue — confirm: the 70-02 phantom entity_type='city' "<X> County" rows are GONE (0 rows
        matching entity_type='city' AND name LIKE '% County' in the Utah set); the Phase 72 universal
        category_enrichment count holds at 4,476 with 0 duplicate name_keys; no Utah budget row with a
        null/zero total where it should have data; no FY2026 rows; no duplicate (municipality_id,
        fiscal_year, dataset_type) rows. Record counts (all must be 0 / as-expected).
    (c) PII re-verification (D-73-06) — for the 120 salary rows (dataset_type='salaries'), serialize each
        stored hierarchy and assert NO PII token appears (vendor_name, title, hourly_rate, gender, employee
        name, or any free-text name field); the stored tree is org1/cat1/SUM only. Count PII tokens (must be 0).
    (d) Enrichment bleed (D-73-07) — confirm the 3,536 P72 universal rows carry 0 $-figures and 0 city-name
        leaks. Separately, enumerate the 4 pre-existing non-P72 $-leak rows (parking meter, harbor and port
        enterprise fund, sewer enterprise fund, solid waste enterprise fund) so they are accounted for as a
        DOCUMENTED follow-up — do NOT modify them (D-73-07, D-73-08).
  </action>
  <acceptance_criteria>
    - Fragile/version-specific source_url count = 0 (the bare transparent.utah.gov domain passes).
    - Zero-residue scan: phantom county rows = 0, enrichment holds at 4,476 / 0 duplicates, 0 FY2026 rows, 0 orphan/placeholder/duplicate rows.
    - PII re-verification: 0 PII tokens across all 120 salary-row hierarchies.
    - P72 enrichment bleed: 0 $-leaks + 0 city-name leaks across 3,536 rows; the 4 pre-existing non-P72 $-leak rows enumerated as a documented follow-up (not modified).
  </acceptance_criteria>
  <done>The Utah source chain is durable (bare domain passes, no fragile links), residue-free, names-free, and P72-bleed-safe; the 4 pre-existing leaks are documented.</done>
</task>

<task type="auto">
  <name>Task 3: Document the audit + write 73-02-SUMMARY (UVER-01 part-B evidence)</name>
  <files>.planning/phases/73-utah-verification-source-chain-audit-uat/73-02-SUMMARY.md</files>
  <read_first>
    - The Task 1 + Task 2 results
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 73-02-SUMMARY.md: per-phase cohort counts (69/70/71/72); Transparent Utah NULL-source_url count
    (0); fragile-URL count (0, with the bare-domain durability rationale); residue count (0, with the
    phantom-county + 4,476-enrichment confirmations); PII-token count (0 across 120 salary rows); P72
    enrichment $-leak/city-name-leak count (0); and the 4 pre-existing non-P72 $-leak rows listed as a
    documented follow-up. State UVER-01 part B satisfied (durable source chain, zero residue, names-free,
    bleed-safe). Note any anomaly as a documented follow-up. Confirm read-only, no BigQuery, $0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/73-utah-verification-source-chain-audit-uat/73-02-SUMMARY.md';const t=fs.readFileSync(p,'utf8');const ok=/UVER-01/.test(t)&&/residue/i.test(t)&&/fragil/i.test(t)&&/transparent\.utah\.gov/i.test(t)&&/PII/i.test(t);console.log('SUMMARY has UVER-01 + residue + fragility + transparent.utah.gov + PII:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - 73-02-SUMMARY.md records per-phase counts, NULL-source_url=0, fragile=0 (bare domain passes), residue=0, PII=0, P72 bleed=0, and the 4 documented pre-existing $-leak rows; UVER-01 part B stated.
    - Verify probe passes (SUMMARY references UVER-01, residue, fragility, transparent.utah.gov, PII).
  </acceptance_criteria>
  <done>The source-chain durability + bleed-safety audit is documented as the UVER-01 part-B evidence record.</done>
</task>

</tasks>

<verification>
- Every backfilled Utah budget/salary row carries durable attribution (transparent.utah.gov); NULL-source_url = 0.
- No fragile/version-specific source_url (bare domain passes); zero residue (phantom county rows gone, enrichment holds at 4,476/0 dups, no FY2026/orphan/duplicate rows).
- Salaries names-free (0 PII tokens across 120 rows); P72 enrichment bleed-safe (0 $/city-name leaks); 4 pre-existing $-leak rows documented as a follow-up (not fixed).
- Read-only, production DB, no live BigQuery, $0; 73-02-SUMMARY.md is the UVER-01 part-B evidence.
</verification>

<success_criteria>
- UVER-01 part B satisfied: full-cohort source-chain durability + bleed-safety audit passes — durable human-page attribution everywhere, zero residue, names-free salaries, bleed-safe P72 enrichment.
- No DB writes / source changes (D-73-08); production DB (D-73-11); no live BigQuery + $0 (D-73-12).
- 73-02-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/73-utah-verification-source-chain-audit-uat/73-02-SUMMARY.md` when done.
</output>
