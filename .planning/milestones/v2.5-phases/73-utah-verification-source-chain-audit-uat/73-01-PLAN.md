---
phase: 73-utah-verification-source-chain-audit-uat
plan: "73-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [UVER-01]
must_haves:
  truths:
    - "Provo (city) AND Salt Lake County (county government) are each reconciled against their published ACFR on a basis-matched comparison, for the most recent FY with both a published ACFR and a loaded Transparent Utah row (D-73-01, D-73-03)"
    - "Salt Lake County is the milestone's first county-government ACFR reconciliation (Phase 70-02 deferred all county recon to Phase 73); its variance gets extra care (county all-funds vs ACFR governmental/proprietary split) (D-73-01)"
    - "Each reconciliation passes within a documented, explainable tolerance (fund scope / enterprise inclusion / bond proceeds / inter-fund gross-ups / restatement / timing) — NOT penny-exact; differences are explained, not merely flagged (D-73-02)"
    - "Read-only only: no DB writes, no source-file changes; production DB; no live BigQuery; $0 (free ACFR PDFs via WebFetch) (D-73-08, D-73-11, D-73-12)"
  artifacts:
    - path: ".planning/phases/73-utah-verification-source-chain-audit-uat/73-01-SUMMARY.md"
      provides: "The ACFR reconciliation result: per-entity chosen FY, loaded Transparent Utah figure, basis-matched ACFR line + source, delta + explanation — the UVER-01 part-A evidence record"
      contains: "per-entity table (entity, type, FY, loaded figure, ACFR line + URL, delta abs + %, basis explanation, PASS/EXPLAINED), the tolerance reasoning, the county all-funds-vs-ACFR basis statement, any documented follow-up"
  key_links:
    - from: "loaded Transparent Utah figures (treasury.budgets.total_budget) for Provo + Salt Lake County"
      to: "published ACFR basis-matched lines (official Provo / Salt Lake County ACFR PDFs)"
      via: "read-only DB probe + WebFetch of the official ACFR; basis-matched comparison within a documented tolerance"
      pattern: "probe loaded figures + pick comparable FY → fetch ACFR + reconcile on the all-funds line → document delta + explanation"
---

<objective>
Satisfy UVER-01 part A: reconcile Provo (city) and Salt Lake County (county government) against their
published ACFRs on a basis-matched comparison, for the most recent fiscal year with both a published ACFR
and a loaded Transparent Utah row. Salt Lake County is the milestone's first county-government ACFR
reconciliation. Document each delta with an explainable tolerance (not penny-exact). Read-only, production
DB, $0. (The source-chain audit is 73-02; UAT is 73-03.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/73-utah-verification-source-chain-audit-uat/73-CONTEXT.md
@.planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md
@.planning/phases/69-utah-city-budgets-load/69-02-SUMMARY.md
@.planning/phases/70-utah-county-budgets-linking/70-02-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- PRODUCTION DB read-only via repo .env SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, schema treasury.
     NEVER mcp__supabase-local (stale). NEVER query BigQuery live (rollup already loaded; cost incident).
     Probe columns: total_budget, hierarchy, source_url, data_source; exact-count head queries (avoid
     1000-row cap). ACFR PDFs via WebFetch (free). No writes. -->

Basis (D-73-02): Transparent Utah BigQuery data = all-governmental-funds + proprietary (no fund filter; fund1 tops the tree). ACFR governmental + proprietary statements are separate — pick the comparable line (total expenditures/revenues across the funds the load includes), same FY, document the basis.
Year (D-73-03): most recent FY with BOTH a published ACFR AND a loaded Transparent Utah row. Loaded coverage = FY2014–FY2025 (FY2026 intentionally excluded). ACFRs lag ~1 year.
Sample (D-73-01): Provo (city) + Salt Lake County (county government). Provo is already penny-exact vs the 68-03 independent baseline (op $346,484,274.68 / rev $285,684,200.65 FY2024) and operator-approved in 69-02 — the clean cross-read. Salt Lake County (~$1.46B recent-FY operating) is the FIRST Utah county-gov ACFR recon.
Note: SLC's known FY-to-FY volatility (airport-driven, e.g. op $1.81B FY23 → $887M FY24 → $1.53B FY25) is the canonical explainable-lumpiness example, but SLC is NOT in this sample (already operator-reconciled in 69-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe loaded Transparent Utah figures + select the basis-matched comparison year for Provo + Salt Lake County</name>
  <files>(no writes — read-only probe)</files>
  <read_first>
    - .planning/phases/73-utah-verification-source-chain-audit-uat/73-CONTEXT.md (D-73-01..03, D-73-08, D-73-11, D-73-12)
    - .planning/phases/69-utah-city-budgets-load/69-02-SUMMARY.md (Provo reconciliation method + FY2024 baseline)
    - .planning/phases/70-utah-county-budgets-linking/70-02-SUMMARY.md (Salt Lake County loaded totals)
  </read_first>
  <action>
    Source .env. With a READ-ONLY probe (schema treasury), pull Provo's and Salt Lake County's loaded
    operating + revenue total_budget by fiscal year (use total_budget; exact-count/head where counting).
    Provo is entity_type='city' under the display name "Provo"; Salt Lake County is entity_type='county'
    under "Salt Lake County". For each, identify the candidate fiscal years available (FY2014–FY2025) to
    pair against a published ACFR in Task 2 — prefer the most recent FY likely to have a published ACFR
    (expect FY2023 or FY2024). Record per entity: the available loaded FYs and the chosen-candidate-FY
    op/rev totals as the reconciliation reference figures. No writes. No BigQuery.
  </action>
  <acceptance_criteria>
    - Provo + Salt Lake County loaded op/rev totals by FY are recorded read-only (total_budget), with each entity keyed to its correct entity_type (city / county) and display name.
    - The candidate reconciliation FY per entity is identified (most recent FY likely to have a published ACFR).
  </acceptance_criteria>
  <done>Provo + Salt Lake County loaded Transparent Utah figures are established read-only, ready to pair with published ACFRs.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch published ACFRs and reconcile Provo + Salt Lake County on a basis-matched line ($0)</name>
  <files>(no writes)</files>
  <read_first>
    - The Task 1 loaded figures
    - .planning/phases/69-utah-city-budgets-load/69-01-SUMMARY.md (SLC basis/volatility characteristics — the explainable-lumpiness reference)
  </read_first>
  <action>
    For Provo and Salt Lake County, WebFetch the official published ACFR for the most recent FY that also
    has a loaded Transparent Utah row (D-73-03). Locate the basis-matched line (D-73-02): total
    expenditures (operating) and total revenues across the funds the load includes (all-governmental +
    proprietary, since the load applies no fund filter). For Salt Lake County, take EXTRA care mapping the
    county's all-funds load to the ACFR's separate governmental-vs-proprietary statements — this is the
    milestone's first county recon and the most likely place for an explainable basis gap. Compare each
    loaded total_budget to the ACFR line for the SAME FY. Compute the delta (absolute + %) and EXPLAIN it
    (fund scope, enterprise inclusion, bond proceeds, inter-fund gross-ups, restatement, timing) — the
    reconciliation passes within a documented, explainable tolerance, not penny-exact. Record per entity:
    chosen FY, loaded figure, ACFR line + page/URL, delta, and the explanation. If an entity cannot be
    reconciled (no published ACFR for a shared FY, or an unexplainable gap), document it as a follow-up
    rather than forcing a pass. $0 (WebFetch only).
  </action>
  <acceptance_criteria>
    - Provo AND Salt Lake County are each reconciled to a published ACFR basis-matched line for a shared FY; delta + explanation recorded; the ACFR source URL captured.
    - The Salt Lake County all-funds-vs-ACFR basis mapping is explicitly documented (governmental vs proprietary).
    - Any non-reconcilable entity is documented as a follow-up (not forced to pass); $0 (WebFetch only).
  </acceptance_criteria>
  <done>Provo + Salt Lake County are reconciled (or explicitly documented as a follow-up) on a basis-matched line within an explained tolerance.</done>
</task>

<task type="auto">
  <name>Task 3: Document the reconciliation + write 73-01-SUMMARY (UVER-01 part-A evidence)</name>
  <files>.planning/phases/73-utah-verification-source-chain-audit-uat/73-01-SUMMARY.md</files>
  <read_first>
    - The Task 2 reconciliation results
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Write 73-01-SUMMARY.md: a per-entity table (entity, type, chosen FY, loaded Transparent Utah figure,
    basis-matched ACFR line + URL, delta absolute + %, basis explanation, PASS/EXPLAINED or FOLLOW-UP),
    plus the overall tolerance reasoning and the basis statement (all-governmental-funds + proprietary vs
    ACFR fund statements), with a dedicated note on the Salt Lake County first-county basis mapping. State
    UVER-01 part A satisfied (the sample reconciles on a basis-matched comparison within a documented
    tolerance). Note any documented follow-ups. Confirm read-only, no BigQuery, $0.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/73-utah-verification-source-chain-audit-uat/73-01-SUMMARY.md';const t=fs.readFileSync(p,'utf8');const ok=/UVER-01/.test(t)&&/basis/i.test(t)&&/Provo/i.test(t)&&/Salt Lake County/i.test(t)&&(t.match(/PASS|EXPLAINED|FOLLOW-UP/gi)||[]).length>=2;console.log('SUMMARY has UVER-01 + basis + Provo + Salt Lake County + >=2 verdicts:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - 73-01-SUMMARY.md records Provo + Salt Lake County with chosen FY, loaded vs ACFR figures, delta, and explanation; the tolerance reasoning + county basis mapping are documented; UVER-01 part A stated.
    - Verify probe passes (SUMMARY references UVER-01, basis, both entities, ≥2 verdicts).
  </acceptance_criteria>
  <done>The ACFR reconciliation is documented as the UVER-01 part-A evidence record.</done>
</task>

</tasks>

<verification>
- Provo + Salt Lake County reconciled on a basis-matched ACFR line for a shared FY, within a documented explainable tolerance (not penny-exact).
- Differences explained (fund/enterprise/restatement/timing); the county all-funds-vs-ACFR basis mapping documented; any non-reconcilable entity documented as a follow-up.
- Read-only, production DB, no live BigQuery, $0; 73-01-SUMMARY.md is the UVER-01 part-A evidence.
</verification>

<success_criteria>
- UVER-01 part A satisfied: Provo + Salt Lake County reconcile against published ACFRs on a basis-matched comparison, documented with explainable tolerance; first Utah county-gov ACFR recon completed.
- No DB writes / source changes (D-73-08); production DB (D-73-11); no live BigQuery + $0 (D-73-12).
- 73-01-SUMMARY.md documents the result.
</success_criteria>

<output>
Create `.planning/phases/73-utah-verification-source-chain-audit-uat/73-01-SUMMARY.md` when done.
</output>
