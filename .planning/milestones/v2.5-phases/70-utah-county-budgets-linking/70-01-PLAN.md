---
phase: 70-utah-county-budgets-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seedCountyLinks.js
autonomous: false
requirements: [UCO-02]
user_setup:
  - service: Supabase (production treasury schema)
    why: "Seeds the 5 Utah county entities and sets municipalities.county_id on the 10 cities (UCO-02). Read-only state-node verification + idempotent writes."
    env_vars:
      - name: SUPABASE_SERVICE_KEY
        source: "gitignored .env (Supabase service role) — already present locally; never log or commit"

must_haves:
  truths:
    - "seedCountyLinks.js accepts an explicit --cities member-list flag that bypasses the CA SCO Socrata fetch when provided"
    - "The CA Socrata membership path is unchanged when --cities is absent (no regression)"
    - "The Utah state node already exists (entity_type=state, state=UT) and is reused as the breadcrumb parent — NOT re-created"
    - "All 5 Utah county entities (Salt Lake, Utah, Davis, Weber, Washington) exist with entity_type=county and state=UT"
    - "All 10 Utah cities have county_id set to the correct one of the 5 counties — no orphans, no mis-links"
    - "The county→city breadcrumb chip and the Cities-in-County panel render in the live app"
  artifacts:
    - path: "scripts/seedCountyLinks.js"
      provides: "generalized county seed + link with an explicit --cities member-list path (D-70-02)"
      contains: "cities"
  key_links:
    - from: "seedCountyLinks.js --cities"
      to: "municipalities.county_id"
      via: "explicit member list → .in('name', cityNames) → UPDATE county_id"
      pattern: "county_id"
    - from: "city row (state=UT)"
      to: "Utah state node (entity_type=state)"
      via: "breadcrumb resolves US→Utah via the shared state='UT' field; county via county_id"
      pattern: "state"
---

<objective>
Generalize `scripts/seedCountyLinks.js` with an explicit `--cities` member-list path (D-70-02) so it works for Utah (no CA SCO Socrata source exists), then seed the 5 Utah county entities (entity_type=county) and link all 10 Utah cities via `county_id` — making the US→Utah→county→city breadcrumb chip and the Cities-in-County panel render. The Utah state node already exists (DB-confirmed) and is reused as the parent, NOT re-created.

Purpose: satisfies UCO-02 and unblocks 70-02 — the county entities MUST exist (entity_type=county) before the budget load runs, otherwise loadUtahTransparency.js would INSERT a duplicate city-typed phantom (the D-70-04 bug, fixed in 70-02).
Output: a generalized seeder, 5 seeded `county` entities, and `county_id` set on all 10 cities.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-utah-county-budgets-linking/70-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/utah-entity-mapping.md

<interfaces>
<!-- Current seedCountyLinks.js contract (scripts/seedCountyLinks.js). The change adds a --cities path; everything else (county-entity seed, idempotent county_id linking, --force guard, --dry-run, CA SCO path) stays intact. -->

CLI (CA, unchanged):
  node scripts/seedCountyLinks.js --county "Orange" [--state CA] [--dry-run] [--force]
    → membership derived from SCO ByTheNumbers Socrata (county field, dataset ju3w-4gxp)

CLI (NEW — Utah path):
  node scripts/seedCountyLinks.js --county "Salt Lake" --state UT --cities "Salt Lake City,Sandy City,West Jordan City,West Valley City" [--dry-run]
    → membership comes from the --cities list; the SCO fetch is bypassed entirely

Internals:
  fetchCountyCityNames(county) -> string[]   // SCO Socrata fetch — CALL ONLY when --cities absent
  countyEntityName = `${county} County`      // so --county "Salt Lake" → entity "Salt Lake County"
  Step 1: treasury_ensure_municipality(p_name=countyEntityName, p_state, p_entity_type='county', p_population=0) — reuse if present
  Step 3: lookup munis WHERE state=<state> AND name IN (cityNames); set county_id where NULL or already this county; --force repoints

DB facts (verified live 2026-06-19):
  - Utah state node ALREADY EXISTS: treasury.municipalities name='Utah', state='UT', entity_type='state',
    id 740cffee-3111-44c0-9473-a77acb6c42f8 — DO NOT create or re-seed it.
  - The 10 Utah city municipality rows carry the FULL Transparent Utah entity_name WITH the "City" suffix:
    Layton City, Lehi City, Ogden City, Orem City, Provo City, Salt Lake City, Sandy City, St. George City,
    West Jordan City, West Valley City (confirmed in scripts/loadUTPopulation.js DB_NAME map).
    The --cities list MUST use these exact suffixed names or the .in('name', …) match will miss.

Authoritative city→county membership (docs/utah-entity-mapping.md, D-70-02) — all 10 map to exactly one county, no orphans:
  Salt Lake County  → Salt Lake City, Sandy City, West Jordan City, West Valley City
  Utah County       → Provo City, Orem City, Lehi City
  Davis County      → Layton City
  Weber County      → Ogden City
  Washington County → St. George City
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an explicit --cities member-list path to seedCountyLinks.js (D-70-02)</name>
  <files>scripts/seedCountyLinks.js</files>
  <read_first>
    - scripts/seedCountyLinks.js (the file being modified — read fully first)
    - scripts/seedMACountyLinks.js (non-SCO / explicit-membership linking precedent referenced by D-70-02)
    - .planning/phases/70-utah-county-budgets-linking/70-CONTEXT.md (D-70-02 — membership comes from the mapping doc, NOT any API)
    - docs/utah-entity-mapping.md (the authoritative city→county membership table)
  </read_first>
  <behavior>
    - When `--cities "A,B,C"` is passed, the script splits on commas, trims each name, drops empties, and uses that list as the member-city set — `fetchCountyCityNames` (the SCO Socrata fetch) is NOT called.
    - When `--cities` is absent, behavior is byte-for-byte the existing CA path: `fetchCountyCityNames(county)` runs against SCO Socrata (no regression).
    - The county-entity seed (Step 1), the idempotent county_id linking (Step 3), the --force repoint guard, and --dry-run all behave identically regardless of which membership source is used.
    - `--state UT` flows through to both the county-entity `treasury_ensure_municipality` call and the city lookup `.eq('state', …)`.
  </behavior>
  <action>
    Add a `cities: { type: 'string' }` entry to the `parseArgs` options block. Read it into a local (e.g. `const cityListArg = values.cities`). In `main`, replace the unconditional `const cityNames = await fetchCountyCityNames(county);` with a branch: if `cityListArg` is a non-empty string, set `cityNames = cityListArg.split(',').map(s => s.trim()).filter(Boolean)` and log that membership came from the explicit list (e.g. `Member cities from --cities flag (N): …`); otherwise call `fetchCountyCityNames(county)` exactly as today and keep its existing "Member cities from SCO" log. Keep the existing empty-list guard (`if (cityNames.length === 0) { … process.exit(1) }`) applying to both paths. Do not change `fetchCountyCityNames`, Step 1, Step 3, the summary block, the --force logic, or the --dry-run logic. Update the top-of-file usage comment to document the `--cities` flag with a Utah example.
  </action>
  <verify>
    <automated>node --check scripts/seedCountyLinks.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/seedCountyLinks.js` exits 0.
    - Grep: `scripts/seedCountyLinks.js` contains a `cities:` option in the parseArgs block and a `.split(',')` over the cities arg.
    - Grep: `fetchCountyCityNames` is still defined and is still called on the no-`--cities` branch (CA path intact).
    - The top-of-file usage comment shows a `--cities` example with `--state UT`.
  </acceptance_criteria>
  <done>seedCountyLinks.js takes an explicit `--cities` member list that bypasses the SCO fetch, with the CA Socrata path unchanged when the flag is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the Utah state parent exists, then seed 5 counties + link 10 cities</name>
  <files>scripts/seedCountyLinks.js</files>
  <read_first>
    - scripts/seedCountyLinks.js (the generalized seeder from Task 1)
    - .planning/phases/70-utah-county-budgets-linking/70-CONTEXT.md (D-70-02 membership; Utah state node already exists — do not re-seed; --force guard kept)
    - docs/utah-entity-mapping.md (exact county entity_names + member-city table)
  </read_first>
  <action>
    First confirm (read-only) the Utah state node already exists — query Supabase for `treasury.municipalities` WHERE name='Utah' AND state='UT' AND entity_type='state'; it must return the row id `740cffee-3111-44c0-9473-a77acb6c42f8`. Do NOT create or re-seed the state node. Source the gitignored `.env` for `SUPABASE_SERVICE_KEY` before running the seeder.

    Dry-run each of the 5 counties once to confirm the member-city resolution and that each county entity would be reused/created and the right cities would be linked — zero writes:
      node scripts/seedCountyLinks.js --county "Salt Lake" --state UT --cities "Salt Lake City,Sandy City,West Jordan City,West Valley City" --dry-run
    Confirm the dry-run reports the 4 cities under "Would link" (or "Already linked") and zero "Not yet in DB" — a non-empty "Not yet in DB" means a name mismatch (the city rows carry the "City" suffix; fix the --cities argument, do NOT relink by guessing).

    Then live-run all 5 counties (no --dry-run), using the authoritative membership and the suffixed DB names:
      node scripts/seedCountyLinks.js --county "Salt Lake" --state UT --cities "Salt Lake City,Sandy City,West Jordan City,West Valley City"
      node scripts/seedCountyLinks.js --county "Utah" --state UT --cities "Provo City,Orem City,Lehi City"
      node scripts/seedCountyLinks.js --county "Davis" --state UT --cities "Layton City"
      node scripts/seedCountyLinks.js --county "Weber" --state UT --cities "Ogden City"
      node scripts/seedCountyLinks.js --county "Washington" --state UT --cities "St. George City"
    Each run seeds (or reuses) the `<X> County` entity with entity_type=county, state=UT, and sets county_id on its member cities. Do NOT pass --force (no city should already be linked elsewhere; a linkedElsewhere report signals a data problem to investigate, not force through).

    Verify in the DB via Supabase: exactly 5 rows with entity_type='county', state='UT', names "Salt Lake County","Utah County","Davis County","Weber County","Washington County"; and all 10 Utah city rows have county_id pointing at the correct county per the mapping (0 cities with NULL county_id, 0 mis-links).
  </action>
  <verify>
    <human-check>Supabase query: SELECT name, entity_type FROM treasury.municipalities WHERE state='UT' AND entity_type='county' — confirm exactly the 5 expected county rows. Then SELECT c.name AS city, p.name AS county FROM treasury.municipalities c JOIN treasury.municipalities p ON c.county_id = p.id WHERE c.state='UT' AND c.entity_type='city' — confirm all 10 cities map to the correct county per docs/utah-entity-mapping.md, none NULL.</human-check>
  </verify>
  <acceptance_criteria>
    - DB query: `SELECT count(*) FROM treasury.municipalities WHERE state='UT' AND entity_type='county'` returns 5, named Salt Lake/Utah/Davis/Weber/Washington County.
    - DB query: the Utah state node (name='Utah', state='UT', entity_type='state') still has exactly one row with id 740cffee-3111-44c0-9473-a77acb6c42f8 (not duplicated).
    - DB query: all 10 Utah `entity_type='city'` rows have a non-NULL county_id, and each resolves to the county listed in docs/utah-entity-mapping.md (Salt Lake County: 4 cities; Utah County: 3; Davis/Weber/Washington: 1 each).
    - No city is linked to a county it does not belong to.
  </acceptance_criteria>
  <done>The Utah state node is confirmed (not re-seeded), 5 county entities exist with entity_type=county, and all 10 cities carry the correct county_id.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live-app check — breadcrumb chip + Cities-in-County panel render (SC#3)</name>
  <read_first>
    - .planning/phases/70-utah-county-budgets-linking/70-CONTEXT.md (CitiesInCountyPanel + breadcrumb are existing, data-driven — render once county_id is set; counties are directory-only until 70-02 lands budgets)
    - src/components/CitiesInCountyPanel.tsx (the data-driven panel — no change expected this phase)
  </read_first>
  <action>Checkpoint — the operator verifies the county→city linking renders end-to-end in the live app. No code or DB writes in this task.</action>
  <files>(verification only — no files modified)</files>
  <what-built>
    5 Utah county entities (entity_type=county) are seeded and all 10 Utah cities are linked via county_id. The breadcrumb chip on a city page should now show US → Utah → <County> → <City>, and the county page should show a Cities-in-County panel listing its member cities. (County pages are still "directory-only" — no icicle/per-capita yet; that lands in 70-02.)
  </what-built>
  <how-to-verify>
    1. Open the live app (treasurytracker.empowered.vote). Production reads ev-accounts-api.onrender.com/api/treasury/cities — confirm that endpoint returns county_id for the Utah cities (if a city page shows no county chip, the API response is the first thing to check).
    2. Open a Utah city page (e.g. Salt Lake City) and confirm the breadcrumb/context chip shows its county (Salt Lake County) under Utah.
    3. Open a Utah county page (e.g. Salt Lake County) and confirm the Cities-in-County panel lists its member cities (SLC, Sandy City, West Jordan City, West Valley City). A county with one member (Davis → Layton City, Weber → Ogden City, Washington → St. George City) should still list that one city.
    4. Spot-check one city from each county group to confirm no mis-links.
  </how-to-verify>
  <acceptance_criteria>
    - At least one Utah city page shows the correct US→Utah→county→city breadcrumb chip.
    - At least one Utah county page shows the Cities-in-County panel with the correct member cities.
    - No city appears under the wrong county.
  </acceptance_criteria>
  <verify>
    <human-check>See the how-to-verify steps; confirm the breadcrumb chip and Cities-in-County panel render correctly for Utah.</human-check>
  </verify>
  <done>The US→Utah→county→city breadcrumb and Cities-in-County panel render correctly in the live app (SC#3).</done>
  <resume-signal>Type "approved" once the breadcrumb + Cities-in-County panel render correctly, or describe what's wrong to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → Supabase (read + write) | Read-only state-node verification + idempotent county-entity seed and county_id UPDATEs via the gitignored service-role key. Single-operator, serial, main-tree. Membership input is the curated mapping doc, not user/network input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | gitignored `.env` (Supabase service key) | mitigate | Never log or echo the key; `.env` stays gitignored; do not paste secrets into summaries or commits. |
| T-70-02 | Tampering | Utah state node (reused parent) | mitigate | Verify-only (read), never re-seed; confirm the single existing state row id is unchanged — prevents a duplicate state node. |
| T-70-03 | Tampering | existing county_id links / --force | mitigate | Do NOT pass --force on the Utah runs; the idempotent guard only sets county_id where NULL or already this county; a linkedElsewhere report is investigated, not forced. |
| T-70-04 | Tampering | wrong-name links (suffix mismatch) | mitigate | --cities uses the exact suffixed DB names; dry-run "Not yet in DB" must be empty before the live run, proving every city matched. |
| T-70-SC | Tampering | npm installs | accept | No new package installs (`@supabase/supabase-js` already present); no legitimacy gate needed. |
</threat_model>

<verification>
- `node --check scripts/seedCountyLinks.js` passes; the CA SCO path is unchanged when --cities is absent.
- Utah state node confirmed present (not re-seeded); 5 county entities exist (entity_type=county, state=UT).
- All 10 cities carry the correct county_id (no NULLs, no mis-links).
- Breadcrumb chip + Cities-in-County panel render in the live app (SC#3).
</verification>

<success_criteria>
- D-70-02 generalization complete: seedCountyLinks.js takes an explicit `--cities` member list; CA path intact.
- UCO-02 satisfied: Utah state parent confirmed, 5 county nodes seeded, 10 cities linked.
- SC#2 (counties seeded + state parent exists) and SC#3 (cities linked, breadcrumb + panel render) met.
</success_criteria>

<output>
Create `.planning/phases/70-utah-county-budgets-linking/70-01-SUMMARY.md` when done — record the seeder change, the 5 seeded county ids, the per-county city links (and any "Not yet in DB"/linkedElsewhere reports resolved), and the live-app breadcrumb/panel verification result.
</output>
