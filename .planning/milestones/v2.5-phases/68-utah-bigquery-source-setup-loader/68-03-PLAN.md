---
phase: 68-utah-bigquery-source-setup-loader
plan: "68-03"
type: execute
wave: 2
depends_on: ["68-01", "68-02"]
files_modified: [".mcp.json", "docs/utah-entity-mapping.md"]
autonomous: false
requirements: [UTSRC-01, UTSRC-02]
must_haves:
  truths:
    - "A read-only BigQuery MCP server is wired into .mcp.json (Google official server preferred; read-only community server fallback) authenticating via ADC with NO committed key, and can run a SELECT against ut-sao-transparency-prod.transaction.transaction (D-12, D-13, D-14)"
    - "All 15 target entities' EXACT entity_name strings + entity_id + FY coverage are confirmed against the live table and recorded in docs/utah-entity-mapping.md, with each mapped to its Treasury-Tracker municipality (D-16, completes UTSRC-01)"
    - "The clean functional column is decided (function1 vs cat1 vs org1) from the live data and the loader's --source-column default is set accordingly (D-06)"
    - "A pilot-city dry-run via loadUtahTransparency.js produces a sane multi-level function/purpose tree with a plausible total and a durable per-entity source_url, writing ZERO rows (completes UTSRC-02 proof)"
  artifacts:
    - path: ".planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md"
      provides: "The live-validation record: MCP wired, 15-entity mapping, functional-column decision, pilot dry-run proof — the UTSRC-01 mapping + UTSRC-02 proof evidence"
      contains: "MCP server chosen + config + read-only confirmation, the 15 entity_name→municipality mapping table with entity_id + FY reach, the function1/cat1 decision, the pilot dry-run tree + total + source_url + zero-write confirmation"
  key_links:
    - from: "Live ut-sao-transparency-prod.transaction.transaction (post-grant)"
      to: "docs/utah-entity-mapping.md (15 exact entity_names) + loadUtahTransparency.js --source-column default + a verified pilot dry-run"
      via: "read-only BigQuery MCP / bq queries + loadUtahTransparency.js --entity <pilot> --dry-run"
      pattern: "wire MCP → distinct-entity-name recon → pick clean functional column → pilot dry-run proof (zero writes)"
---

<objective>
Complete UTSRC-01 (entity-name mapping) and prove UTSRC-02 against live data: wire a read-only BigQuery MCP,
confirm the 15 entities' exact entity_name strings, decide the clean functional tree column, and run a
pilot-city dry-run through loadUtahTransparency.js producing a sane function/purpose tree with zero writes.
GATED on 68-01 (access granted) and 68-02 (loader built). $0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-RESEARCH.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-01-SUMMARY.md
@.planning/phases/68-utah-bigquery-source-setup-loader/68-02-SUMMARY.md
@docs/utah-bigquery-access.md
@scripts/loadUtahTransparency.js

<interfaces>
<!-- Requires: 68-01 access granted (verify query returns rows) + 68-02 loader built.
  Target entities (15):
    Cities (10): Layton, Lehi, Ogden, Orem, Provo, Salt Lake City, Sandy, St. George, West Jordan, West Valley City
    Counties (5): Salt Lake, Utah, Davis, Weber, Washington
  Exact entity_name strings are UNKNOWN until queried (e.g. "Salt Lake City" vs "Salt Lake City Corporation",
    county naming) — that discovery IS the UTSRC-01 deliverable.
  Recon query (run live):
    SELECT entity_name, MIN(fiscal_year) miny, MAX(fiscal_year) maxy, COUNT(*) n
    FROM `ut-sao-transparency-prod.transaction.transaction`
    WHERE LOWER(entity_name) LIKE '%<token>%' GROUP BY entity_name ORDER BY n DESC
  Column-cleanliness probe: SELECT function1, COUNT(*) ... GROUP BY function1  vs  same for cat1 — pick the
    one with clean, populated, citizen-legible categories for a city's EX rows.
  MCP candidates (D-13): Google official BigQuery MCP server (docs.cloud.google.com/bigquery/docs/use-bigquery-mcp)
    OR read-only community ergut/mcp-bigquery-server. Config in .mcp.json (project-scoped); creds via ADC; NEVER commit a key.
  Pilot dry-run: node scripts/loadUtahTransparency.js --entity "<exact name>" --fy <recent> --dry-run -->
</interfaces>
</context>

<tasks>

<task type="manual">
  <name>Task 1: Wire a read-only BigQuery MCP into .mcp.json and confirm it reads the table</name>
  <files>.mcp.json</files>
  <read_first>
    - docs/utah-bigquery-access.md (the working access recipe + granted account)
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md (D-12, D-13, D-14)
  </read_first>
  <action>
    Add a read-only BigQuery MCP server to .mcp.json (project-scoped). Try the Google official BigQuery MCP
    server first (enabled via the BigQuery API, OAuth2/IAM — uses the same ADC); if it requires IAM/billing the
    sandbox grant can't satisfy, fall back to a read-only community server (ergut/mcp-bigquery-server, SELECT-only).
    Authenticate via ADC / the granted account — do NOT embed or commit any service-account key (gitignore any
    key file). Confirm the MCP is read-only and can run a trivial SELECT against
    ut-sao-transparency-prod.transaction.transaction.
  </action>
  <acceptance_criteria>
    - .mcp.json contains a BigQuery MCP server entry; no secret/key value is committed in it.
    - The MCP can execute a SELECT against the Utah table and is read-only (no write/DDL tools exposed or they are disabled).
  </acceptance_criteria>
  <done>A read-only BigQuery MCP is available for recon (D-12) — credentials kept local.</done>
</task>

<task type="manual">
  <name>Task 2: Reconnaissance — confirm the 15 exact entity_name strings + map to municipalities</name>
  <files>docs/utah-entity-mapping.md</files>
  <read_first>
    - .planning/phases/68-utah-bigquery-source-setup-loader/68-CONTEXT.md (target entities)
    - docs/utah-bigquery-access.md
  </read_first>
  <action>
    Using the MCP (or bq with the SDK on PATH), for each of the 10 cities + 5 counties run the recon query to
    find the EXACT entity_name string(s), entity_id, and FY coverage (min/max year, row count). Resolve
    ambiguities (e.g. "Salt Lake City" vs "...Corporation"; county entity naming; ensure city vs county-gov
    rows are distinguished via govt_lvl/entity_name). Record docs/utah-entity-mapping.md: a table of
    target → exact entity_name → entity_id → FY range → row count → planned Treasury-Tracker municipality name
    + county linkage (the county linkage is consumed by Phase 70). Flag any target not found.
  </action>
  <acceptance_criteria>
    - docs/utah-entity-mapping.md lists all 15 targets with their exact entity_name, entity_id, FY coverage, and mapped municipality; any not-found target is flagged.
  </acceptance_criteria>
  <done>UTSRC-01 mapping complete — the exact entity identifiers downstream load phases need are recorded.</done>
</task>

<task type="manual">
  <name>Task 3: Decide the clean functional tree column + pilot-city dry-run proof</name>
  <files>scripts/loadUtahTransparency.js, docs/utah-entity-mapping.md, .planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md</files>
  <read_first>
    - scripts/loadUtahTransparency.js (the --source-column default from 68-02)
    - docs/utah-entity-mapping.md (the exact entity_names)
    - $HOME/.claude/get-shit-done/templates/summary.md
  </read_first>
  <action>
    Probe function1 vs cat1 (vs org1) on a city's EX rows for cleanliness/legibility and set
    loadUtahTransparency.js's --source-column default to the winner (D-06). Pick the cleanest-data city as the
    pilot (D-10) and run: node scripts/loadUtahTransparency.js --entity "<exact name>" --fy <recent year> --dry-run.
    Confirm the dry-run prints a sane multi-level function/purpose tree, a plausible operating total (sanity-check
    against any public figure if available), a durable per-entity source_url (finalize the source_url pattern in
    the loader — D-08), and writes ZERO rows. Write 68-03-SUMMARY.md: MCP choice + config, the 15-entity mapping
    summary, the function1/cat1 decision, and the pilot dry-run tree/total/source_url/zero-write proof. State
    UTSRC-01 + UTSRC-02 fully satisfied and the loader is ready for Phase 69 (city loads).
  </action>
  <verify>
    <automated>node --check scripts/loadUtahTransparency.js</automated>
  </verify>
  <acceptance_criteria>
    - loadUtahTransparency.js --source-column default is set to the chosen clean functional column.
    - The pilot dry-run output shows a 2-3 level function/purpose tree, a plausible total, a durable per-entity source_url, and zero DB writes.
    - 68-03-SUMMARY.md documents the MCP, the mapping, the column decision, and the dry-run proof; states UTSRC-01 + UTSRC-02 satisfied.
  </acceptance_criteria>
  <done>The loader is live-proven on a pilot city with the right tree column and durable sourcing — UTSRC-01 + UTSRC-02 complete; ready for Phase 69.</done>
</task>

</tasks>

<verification>
- Read-only BigQuery MCP wired in .mcp.json, reads the table, no committed key (Task 1).
- 15 exact entity_names + entity_id + FY coverage mapped in docs/utah-entity-mapping.md (Task 2).
- Clean functional column chosen; pilot dry-run produces a sane tree + durable source_url + zero writes (Task 3).
</verification>

<success_criteria>
- UTSRC-01 fully satisfied: access verified (68-01) + all 15 entity_name strings confirmed and mapped + read-only BigQuery MCP wired.
- UTSRC-02 fully satisfied: the loader (built in 68-02) is live-proven on a pilot city with the chosen function/purpose column and durable per-entity sourcing, zero writes.
- $0; the loader is ready for Phase 69 (Utah city budgets load).
</success_criteria>

<output>
Create `.planning/phases/68-utah-bigquery-source-setup-loader/68-03-SUMMARY.md` when done.
</output>
