---
phase: 62-acfr-verification-source-chain-audit-uat
plan: "62-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "The LA County government entity + the 4 Phase-58 sample cities (Burbank, Glendale, Pasadena, Santa Monica) each reconcile to a published ACFR / adopted budget on a basis-matched line, for a year where both an ACFR and a loaded SCO row exist (D-01, D-03)"
    - "Each comparison is basis-matched and documented: the ACFR statement/line chosen, the SCO basis (all-governmental-funds), the FY, both figures, the delta, and a written explanation of any residual (basis scope / fund inclusion / restatement / timing) — pass is explainable reconciliation, not penny-exact (D-02)"
    - "ACFR source PDFs are fetched from official city/county sites via free WebFetch; every figure cited carries its document + page/statement reference; $0 spend (D-10)"
    - "Verification is read-only — no DB writes, no source-file changes; any unexplainable residual is documented as a follow-up, not fixed here (D-08)"
  artifacts:
    - path: ".planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md"
      provides: "The documented ACFR reconciliation table (5 entities) with basis notes, chosen years, figures, deltas, and explanations — the VER-03 part-A evidence record"
      contains: "one row per entity: ACFR doc + statement line, FY, ACFR figure, loaded SCO figure, delta, basis-match explanation, PASS/EXPLAINED verdict"
  key_links:
    - from: "treasury.budgets (LA County gov + 4 sample cities, operating/revenue)"
      to: "published ACFR / adopted-budget statements for the same FY"
      via: "basis-matched line selection (all-governmental-funds SCO total vs ACFR governmental+proprietary funds or government-wide statement)"
      pattern: "compare on the same basis; document scope difference; pass = reconcilable within an explained tolerance"
---

<objective>
Satisfy the ACFR-reconciliation clause of VER-03 (Success Criterion #1): independently reconcile the
loaded parity totals for the **LA County government entity + Burbank, Glendale, Pasadena, Santa Monica**
(Chris's sample, D-01) against **published ACFRs / adopted budgets**, on a **basis-matched comparison**
(D-02), and document it. SCO ByTheNumbers reports all-governmental-funds; ACFRs split governmental vs
proprietary funds — so the work is to pick the comparable line, compare for a year both sources cover,
and *explain* any residual. Pass = reconcilable on a basis-matched footing within a documented,
explainable tolerance — NOT penny-exact. Read-only, $0 (free official PDFs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md
@.planning/phases/58-la-county-parity-backfill/58-04-SUMMARY.md
@.planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Read-only production DB probe harness — reuse the Phase 61 .env-loading + service-key pattern exactly.
     Schema `treasury`. NEVER use mcp__supabase-local (stale, D-09). NEVER write. -->

DB access (production Treasury DB ONLY):
  repo .env / .env.local → SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (service key; anon does not bypass RLS).
  createClient(URL, SERVICE_KEY, { db: { schema: 'treasury' } }); read-only .select() only.

Loaded SCO figures already documented (use as the DB-side comparator, re-probe to confirm live):
  LA County gov: FY2003 op $13,664M / rev $14,139M; FY2024 op $37,577M / rev $39,322M (58-04-SUMMARY).
  Burbank FY2003 op $426.5M / rev $488.2M; Glendale $451.9M / $522.2M; Pasadena $458.1M / $526.8M;
  Santa Monica $332.7M / $363.5M (58-04-SUMMARY) — these are FY2003; for ACFR matching use the most
  recent overlapping FY (D-03), re-probing the actual loaded figure for that year.

Basis: SCO ByTheNumbers = all-governmental-funds (GF + special revenue + debt service + capital
  projects + enterprise/proprietary/internal-service). ACFR statements separate governmental vs
  proprietary funds. Government-wide Statement of Activities is the broadest single comparator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe loaded SCO figures + select the basis-matched comparison year per entity</name>
  <files>(no files written — read-only DB probes)</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-01, D-02, D-03 — read in full)
    - .planning/phases/58-la-county-parity-backfill/58-04-SUMMARY.md (loaded figures + LA County entity id)
    - .planning/phases/61-enrichment-parity/61-01-PLAN.md (Task 2 verify block — the .env service-key probe snippet to reuse)
  </read_first>
  <action>
    Using read-only probes against the PRODUCTION Treasury DB (schema `treasury`, service key), for each
    of the 5 entities — LA County government (id f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1) and the 4 cities
    Burbank, Glendale, Pasadena, Santa Monica — list the loaded `treasury.budgets` rows for datasets
    operating + revenue across all loaded fiscal years, recording per (entity, FY, dataset): the total
    figure, `data_source`, and `source_url`.

    Then select, per entity, the **candidate reconciliation FY** = the most recent fiscal year for which
    a loaded SCO row exists AND a published ACFR is likely available (ACFRs lag ~1 year, so expect
    FY2022 or FY2023; do not assume FY2024). Record the candidate FY and its loaded op + rev totals.
    These loaded totals are the DB-side comparator for Task 2.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data:m}=await s.from('municipalities').select('id,name').or('name.eq.Burbank,name.eq.Glendale,name.eq.Pasadena,name.eq.Santa Monica').eq('state','CA');console.log('sample cities found:',(m||[]).map(x=>x.name));const ids=(m||[]).map(x=>x.id).concat(['f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1']);const {data:b}=await s.from('budgets').select('municipality_id,fiscal_year,dataset_type,total_amount').in('municipality_id',ids).in('dataset_type',['operating','revenue']);console.log('op/rev rows for sample:',(b||[]).length);process.exit((m||[]).length===4&&(b||[]).length>0?0:1);})"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 entities resolve in `treasury.municipalities` (4 cities by name+state, LA County by id); the verify probe finds the 4 sample cities and >0 op/rev rows (exits 0).
    - For each entity, loaded op + rev totals are recorded for every loaded FY, with `data_source` + `source_url`.
    - A candidate reconciliation FY is chosen per entity (most recent FY with a loaded row, ACFR-plausible — FY2022/FY2023 expected, not assumed FY2024), with its loaded op + rev totals recorded.
    - No writes performed (read-only `.select()` only).
  </acceptance_criteria>
  <done>
    Loaded SCO op/rev figures for the 5 sample entities are probed live and a basis-matched candidate FY with its loaded totals is selected per entity — ready to compare against published ACFRs in Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fetch published ACFRs and reconcile each entity on a basis-matched line ($0)</name>
  <files>(no repo files written — findings recorded in the SUMMARY in Task 3)</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-02 basis-matching HARD, D-03 year selection, D-10 free sources)
    - .planning/phases/60-statewide-ca-salaries-sweep/60-03-SUMMARY.md (independent-re-aggregation reconciliation method — the methodological template)
    - The loaded-figure + candidate-FY record from Task 1
  </read_first>
  <action>
    For each of the 5 entities, locate and fetch (via WebFetch, free official sources only — city/county
    finance department sites, the city ACFR/CAFR landing page, or the state's published filings; NO paid
    sources, D-10) the published **ACFR (Annual Comprehensive Financial Report)** or, if no ACFR is
    published for the candidate FY, the **adopted budget** for that FY. If the candidate FY's ACFR is not
    yet published, fall back to the most recent published year and re-probe the loaded SCO figure for
    that same year (the comparison must be same-FY on both sides).

    Perform the basis-matched comparison (D-02): SCO ByTheNumbers is all-governmental-funds (incl.
    enterprise/proprietary). Select the comparable ACFR line — typically the **government-wide Statement
    of Activities total expenses** (broadest, closest to SCO's all-funds scope) and/or the **total
    expenditures across all governmental funds** plus proprietary funds. Record per entity:
      (1) ACFR document title + URL + the exact statement/line used, and the FY.
      (2) The published ACFR figure on that line.
      (3) The loaded SCO figure for the same FY (from Task 1).
      (4) The delta (absolute + %).
      (5) A written basis explanation of the residual — fund-scope difference, enterprise-fund inclusion,
          restatement, or timing. A large residual that is fully explained by a known basis difference is
          a PASS; an unexplainable residual is documented as a follow-up flag (D-08), not fixed here.

    Verdict per entity: PASS (reconciles within an explained tolerance) or FLAG (unexplained residual,
    documented for follow-up). Do NOT write to the DB or change any source file.
  </action>
  <acceptance_criteria>
    - All 5 entities have a fetched published ACFR or adopted-budget source (official, free), cited by title + URL + statement line + FY.
    - Each comparison is same-FY on both sides and uses a basis-matched comparator line (government-wide or all-governmental+proprietary), with the SCO all-funds basis stated.
    - Each entity records: ACFR figure, loaded SCO figure, delta (abs + %), and a written basis explanation of the residual.
    - Each entity gets a PASS or FLAG verdict; any FLAG names the specific unexplained residual for follow-up (no fix attempted — D-08).
    - $0 spend: only free WebFetch reads of official documents; no paid source/API.
  </acceptance_criteria>
  <done>
    All 5 sample entities are reconciled against published ACFRs/adopted budgets on a basis-matched, same-FY line with documented deltas and explanations; verdicts assigned; $0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the reconciliation + write the SUMMARY (VER-03 part-A evidence)</name>
  <files>.planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/62-acfr-verification-source-chain-audit-uat/62-CONTEXT.md (D-02 — pass criteria)
    - $HOME/.claude/get-shit-done/templates/summary.md
    - The reconciliation findings from Task 2
  </read_first>
  <action>
    Write `62-01-SUMMARY.md` containing the documented reconciliation: a table with one row per entity
    (entity | FY | ACFR doc + statement line | ACFR figure | loaded SCO figure | delta abs/% | basis
    explanation | PASS/FLAG), followed by a per-entity narrative of the basis match. State the overall
    ACFR-reconciliation verdict for SC#1: it PASSES when all 5 entities reconcile within a documented,
    explainable tolerance on a basis-matched footing (D-02). List any FLAGs as explicit follow-up
    candidates (not fixed this phase, D-08). Record that this satisfies the ACFR-reconciliation clause of
    VER-03 (the source-chain clause is covered by Plan 62-02).
  </action>
  <acceptance_criteria>
    - `62-01-SUMMARY.md` exists with the 5-entity reconciliation table (all columns populated) + per-entity basis narrative.
    - The SC#1 ACFR verdict is stated with its pass rationale (basis-matched, explainable tolerance — not penny-exact).
    - Any unexplained residual is listed as a documented follow-up (no fix attempted).
    - The summary states VER-03 part-A (ACFR reconciliation) is satisfied and references Plan 62-02 for the source-chain clause.
  </acceptance_criteria>
  <done>
    The ACFR reconciliation is fully documented in 62-01-SUMMARY.md with a basis-matched table, per-entity explanations, and an explicit SC#1 verdict — the VER-03 part-A evidence record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published ACFR PDF (external) → reconciliation claim | An ACFR line read on the wrong basis or wrong FY produces a false PASS or a false FLAG. |
| repo .env service key → production Treasury DB | Read-only here; a stray write would corrupt verified production data. |
| free WebFetch → paid source drift | Fetching a paywalled/paid document would breach the $0 free-source rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-A | Tampering / false verdict | basis mismatch (governmental-funds-only ACFR line vs all-funds SCO) | mitigate | D-02 HARD: comparator line is explicitly basis-matched (government-wide or all-governmental+proprietary); the residual is explained in writing; pass is explainable-tolerance, not penny-exact. Same-FY enforced both sides (D-03). |
| T-62-01-B | Tampering | accidental DB write during probing | mitigate | D-08 read-only: probes use `.select()` only; no upsert/update/delete; verify snippets are read-only. |
| T-62-01-C | Info disclosure / cost | fetching a paid/paywalled ACFR source | mitigate | D-10: official free city/county finance sites only; WebFetch reads, no paid API; $0. |
| T-62-01-D | Spoofing | wrong-year or wrong-entity ACFR | mitigate | Per-entity ACFR doc title + URL + FY + statement line recorded; same-FY comparison enforced; LA County matched by entity id, cities by name+state. |
</threat_model>

<verification>
- 5 entities (LA County gov + Burbank/Glendale/Pasadena/Santa Monica) each reconciled against a published ACFR/adopted budget on a basis-matched, same-FY line.
- Each comparison documents ACFR line + figure, loaded SCO figure, delta, and a written basis explanation; verdict PASS or FLAG.
- Pass = reconcilable within a documented, explainable tolerance (D-02), not penny-exact.
- $0 spend (free official sources only); read-only (no DB writes, no source changes).
- 62-01-SUMMARY.md records the table + verdict as the VER-03 part-A evidence.
</verification>

<success_criteria>
- ACFR-reconciliation clause of VER-03 satisfied: LA County gov + 4 sample cities reconcile on a basis-matched comparison, documented (SC#1).
- Basis differences explicitly explained; pass criterion is explainable reconciliation, not exact match (D-02).
- Free official sources only, $0 spend (D-10); read-only, no writes (D-08).
- 62-01-SUMMARY.md is the documented evidence record; source-chain clause handed to Plan 62-02.
</success_criteria>

<output>
Create `.planning/phases/62-acfr-verification-source-chain-audit-uat/62-01-SUMMARY.md` when done.
</output>
