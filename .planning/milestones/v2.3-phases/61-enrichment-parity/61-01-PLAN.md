---
phase: 61-enrichment-parity
plan: "61-01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [ENR-01]
must_haves:
  truths:
    - "Every parity-loaded top-level category name_key (operating + revenue from Phases 58/59, salaries from Phase 60) not already covered (universal NULL set OR already city-scoped) receives plain-language enrichment, authored inline at $0 API cost"
    - "Enrichment depth, field richness, and universal-vs-city-scoped placement match the LA County / OC baseline"
    - "Generated text is strictly category-level — no city-specific names, dollar figures, or facts are ever stored in a universal (municipality_id IS NULL) record"
    - "A citizen viewing any Phase 58/59/60 city's operating, revenue, or salaries budget sees plain-language category names + descriptions render automatically"
  artifacts:
    - path: "treasury.category_enrichment (Phase 61 gap rows)"
      provides: "Plain-language plain_name/short_description/description/tags/confidence/evidence_summary for the parity-loaded gap set across op/rev/salaries"
      contains: "rows upserted on conflict (name_key, municipality_id), mirroring the enrichCategories.js write shape exactly"
  key_links:
    - from: "treasury.category_enrichment (name_key, municipality_id)"
      to: "cohort budget_categories (name_key = normalized parent|name or name) across datasets operating, revenue, salaries"
      via: "ev-accounts treasuryService.ts reads enrichment by (name_key, this-city) OR (name_key, NULL universal)"
      pattern: "name_key = lower(trim(parent_name))|lower(trim(name)) OR lower(trim(name)); two-tier fallback city-scoped then universal"
---

<objective>
Satisfy ENR-01 — every parity-loaded budget category (Phase 58 LA County 88 cities + LA County
government op/rev backfill, Phase 59 the 7 thin other-county/unlinked cities op/rev, AND Phase 60
salaries-dataset categories for ~98 CA cities) carries standardized, bleed-safe, plain-language
enrichment consistent with the OC/LA County baseline — authored **INLINE at $0** (D-01). The paid
`enrichCategories.js` Anthropic SDK path is NOT run; the script is read for its exact write shape and
gap logic only.

This is a direct re-application of the Phase 54 OC enrichment pattern (gap-set GATE → author inline →
verify), generalized to the Phase 58/59/60 cohort and extended to the salaries dataset (D-07, Chris
2026-06-16). Output: new `treasury.category_enrichment` rows for the uncovered gap set, upserted on
conflict `(name_key, municipality_id)`, universal-first for max reuse, strictly category-level
(bleed-safe per D-04). No frontend work — enrichment renders automatically via the ev-accounts
two-tier join.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-enrichment-parity/61-CONTEXT.md
@.planning/milestones/v2.2-phases/54-orange-county-entity-linking-enrichment/54-02-PLAN.md
@.planning/milestones/v2.2-phases/54-orange-county-entity-linking-enrichment/54-02-SUMMARY.md
@scripts/enrichCategories.js
@scripts/loadCASalaries.js
@docs/socal-county-onboarding.md

<interfaces>
<!-- Contracts extracted from scripts/enrichCategories.js. REFERENCE ONLY — do NOT run its paid path. -->

category_enrichment write shape (saveEnrichment, lines 381–402) — mirror exactly:
  row = {
    name_key,                 // normalize(parent_name)|normalize(name) IF parent_name else normalize(name)
    municipality_id,          // NULL = universal (inherited by all cities); city id = that city only
    plain_name,               // 2–5 word citizen-friendly name (not the raw fund/department name)
    short_description,        // one sentence — what does this fund/department pay for?
    description,              // 2–3 sentences — what it is, why it exists, how it is funded
    tags,                     // array of 3–6 topic tags
    source,                   // probe LA/OC first; MATCH the stored value (Phase 54 used 'ai')
    confidence,               // 'high' | 'medium' | 'low'
    evidence_summary,         // brief note on basis/uncertainty
    generated_at,             // ISO timestamp
  }
  upsert onConflict: 'name_key,municipality_id'   (schema `treasury`)

normalize() (152–154):  (name || '').toLowerCase().trim()
  → top-level name_key = `${lower.trim(name)}`; child = `${lower.trim(parent_name)}|${lower.trim(name)}`

Gap-detection logic to REPLICATE (do NOT run the script — re-implement the filter via probes):
  - getExistingEnrichments(muniId) (236–242): name_keys where municipality_id = muniId OR IS NULL.
  - getUniversalEnrichments() (247–253): name_keys where municipality_id IS NULL.
  - getBudgetCategories(muniId) (181–231): top-level categories (DEPTH '0' → parent_id IS NULL) for
    budgets at fiscal_year = YEAR. For this phase iterate datasets operating + revenue + salaries.
  GAP per city = its top-level name_keys NOT in getExistingEnrichments(thatCity). After a universal
  row is authored, that name_key is covered for ALL remaining cities — author universals first.

Salaries tree (loadCASalaries.js ~380–448): dataset_type 'salaries'; top-level (depth 0) = DEPARTMENT
names (generic, universal-eligible); depth-1 = `position (count)` (city-specific count → NOT enriched).
Enrich salaries at top-level only (DEPTH 0), consistent with the op/rev convention.

DB access (production Treasury DB ONLY — NOT mcp__supabase-local, which is stale):
  repo .env / .env.local → SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (service key required; anon key
  does not bypass RLS → writes fail silently). schema `treasury`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the parity-loaded enrichment gap set (op + revenue + salaries) is authored INLINE at $0 — no paid API call.</decision>
  <context>
    D-01 (locked) requires the executing agent to author enrichment text itself inline (no billed
    @anthropic-ai/sdk calls) and write rows directly to category_enrichment. This checkpoint computes
    the gap-set size and the LA/OC baseline, then PAUSES so the operator confirms the $0 plan BEFORE
    authoring and the run cannot silently drift onto the paid path. Empowered Vote has a hard ~$5 gate.
  </context>
  <options>
    <option id="proceed-inline-zero-cost">
      <name>Proceed: author the computed gap set inline at $0</name>
      <pros>Zero API spend; honors D-01; full control over bleed-safety (D-04) and LA-consistency (D-03).</pros>
      <cons>Agent context time spent authoring text per name_key.</cons>
    </option>
    <option id="stop-needs-paid-api">
      <name>STOP — some portion genuinely cannot be authored inline</name>
      <pros>Surfaces any case needing a paid call before spending.</pros>
      <cons>Requires explicit user approval before ANY spend, even under the $5 gate.</cons>
    </option>
  </options>
  <read_first>
    - .planning/phases/61-enrichment-parity/61-CONTEXT.md (D-01..D-11 — read in full)
    - .planning/milestones/v2.2-phases/54-orange-county-entity-linking-enrichment/54-02-PLAN.md (the gated template)
    - scripts/enrichCategories.js (152–154 normalize, 181–253 gap/skip logic, 319–348 output schema, 381–402 write shape)
    - scripts/loadCASalaries.js (~380–448 salaries tree: top-level=department, dataset_type 'salaries')
  </read_first>
  <action>
    Do NOT run enrichCategories.js. Using read-only DB probes against the PRODUCTION Treasury DB
    (repo .env service key, schema `treasury`):
    (1) Record the LA/OC baseline (D-03 bar): enriched name_keys, universal (municipality_id IS NULL)
        vs city-scoped split, depth/level enriched, which fields are populated, and the exact stored
        `source` value (expected 'ai'). This is the consistency bar.
    (2) Enumerate the cohort (D-06): the 88 LA County cities + the LA County government entity (id
        f3db6f9f-2575-48e3-bf42-a1f9dd1ec6a1) from Phase 58, and the 7 thin cities (San Francisco,
        Oakland, Fresno, Riverside, Bakersfield, San Diego, Berkeley) from Phase 59. Confirm the
        Phase 60 salaries cohort (~98 cities) via budgets where dataset_type='salaries'.
    (3) Compute the GAP SET (D-09): for each cohort city, list top-level (parent_id IS NULL)
        budget_categories name_keys for datasets operating, revenue, AND salaries at the latest loaded
        year (FY2024), AND additionally sweep distinct top-level name_keys across all loaded years per
        dataset to catch historical-only names. Subtract name_keys already in
        getExistingEnrichments(thatCity) = (city-scoped OR universal NULL). Report the DISTINCT gap-set
        size, broken out by dataset (operating / revenue / salaries) and by placement
        (universal-eligible generic names vs any requiring city-scoping per D-04). Note how many op/rev
        names are already covered by the Phase 54 universal set (inherited).
    (4) Confirm the entire gap set is authorable inline at $0 (no @anthropic-ai/sdk / ANTHROPIC_API_KEY
        call planned). Then PAUSE for the operator. If any item genuinely needs a paid call, select
        STOP and surface it — do not auto-spend.
  </action>
  <acceptance_criteria>
    - LA/OC baseline recorded: enriched depth/level, populated field set, universal-vs-city-scoped split, and the stored `source` value.
    - Cohort enumerated: 88 LA County cities + LA County gov + 7 thin cities; Phase 60 salaries cohort confirmed via dataset_type='salaries' probe.
    - Gap set computed by replicating the script's skip filter (existing = city-scoped OR universal NULL), at top-level, across operating + revenue + salaries; distinct gap-set count reported, broken out by dataset and by universal-eligible vs city-scoped-required.
    - Plan confirmed authorable inline at $0; no paid API call planned.
    - Operator selects an option; if STOP, the specific item needing spend is surfaced for approval.
  </acceptance_criteria>
  <resume-signal>Select: proceed-inline-zero-cost (confirms $0 inline authoring) or stop-needs-paid-api (describe the item needing spend).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the gap-set enrichment inline ($0) and upsert into category_enrichment</name>
  <files>(no repo files written — DB writes to treasury.category_enrichment only)</files>
  <read_first>
    - .planning/phases/61-enrichment-parity/61-CONTEXT.md (D-02 write shape, D-03 match LA, D-04 bleed-safety, D-05 universal-first, D-07 salaries — read in full)
    - scripts/enrichCategories.js (381–402 saveEnrichment write shape, 319–346 field semantics)
    - .planning/milestones/v2.2-phases/54-orange-county-entity-linking-enrichment/54-02-SUMMARY.md (OC authoring example — depth/richness/placement)
    - The LA/OC baseline record + gap set computed in Task 1
  </read_first>
  <action>
    Do NOT run enrichCategories.js or make any billed API call. For each name_key in the gap set from
    Task 1, the agent AUTHORS the text itself inline: plain_name (2–5 words), short_description (one
    sentence), description (2–3 sentences: what the fund/department is, why it exists, how it is
    funded), tags (3–6), confidence, evidence_summary. Match the LA/OC depth, field richness, and
    placement convention (D-03). Set `source` to the value LA/OC stores (probed in Task 1 — expected
    'ai'). Compute name_key exactly as the script does: top-level = `lower(trim(name))`.

    Placement (D-04 HARD + D-05): generic statewide SCO operating/revenue fund names and generic
    salaries DEPARTMENT names (uniform across cities) → write UNIVERSAL (municipality_id IS NULL) so
    all cities inherit them. Author universals FIRST to maximize reuse. Any text that would reference a
    city-specific name/figure/fact MUST be written CITY-SCOPED (municipality_id = that city's id) — and
    the text itself must still describe the category generically. NEVER store city-specific content in
    a universal record (this is the prior production bleed bug, project_enrichment_scoping_fix).
    Strictly category-level text only — no city names, no dollar figures, no city-specific facts in any
    record. Salaries department descriptions describe the generic municipal department (e.g. "Police
    Department" → citywide public-safety payroll), never a specific city's headcount or dollar total.

    Upsert each row into treasury.category_enrichment on conflict `(name_key, municipality_id)` via the
    service key, mirroring the script's write shape exactly. Idempotent: re-running upserts the same
    rows without duplication.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data}=await s.from('category_enrichment').select('name_key,plain_name,description').is('municipality_id',null).limit(5000);const leak=(data||[]).filter(r=>/\$\s?\d/.test((r.description||'')+(r.plain_name||''))).map(r=>r.name_key);console.log('universal rows:',(data||[]).length,'| dollar-figure leaks in universals:',leak.length,leak.slice(0,10));process.exit(leak.length?1:0);})"</automated>
  </verify>
  <acceptance_criteria>
    - Every gap-set name_key now resolves enrichment via (name_key, city) OR (name_key, NULL): a probe per cohort city returns 0 top-level budget_categories name_keys (across op/rev/salaries) lacking coverage.
    - Authored rows mirror the script write shape: name_key (correctly normalized, top-level), municipality_id, plain_name (2–5 words), short_description (1 sentence), description (2–3 sentences), tags (3–6), source (= LA/OC value), confidence, evidence_summary, generated_at.
    - D-04 bleed-safety: every universal row authored in this task contains NO city-specific names, dollar figures, or city facts (automated check exits 0 — no dollar-figure leak in universals). Any city-specific content lives only in a city-scoped row.
    - $0 cost: no @anthropic-ai/sdk import executed, no ANTHROPIC_API_KEY call made; enrichCategories.js not run.
    - Re-running the upsert produces no duplicate rows (idempotent on (name_key, municipality_id)).
  </acceptance_criteria>
  <done>
    Parity-loaded gap set (op + revenue + salaries) enriched inline at $0; every cohort city's top-level categories resolve enrichment; universals are strictly category-level (bleed-safe); rows match the LA/OC shape/depth/placement; upsert is idempotent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify enrichment coverage + bleed-safety across the cohort (≥3 cities)</name>
  <files>(no files written — DB read-only probes)</files>
  <read_first>
    - .planning/phases/61-enrichment-parity/61-CONTEXT.md (D-03, D-04, D-11)
    - docs/socal-county-onboarding.md (Step 4 enrichment verification)
    - The LA/OC baseline record from Task 1 (consistency target)
  </read_first>
  <action>
    Run read-only DB probes against the production Treasury DB (schema `treasury`) to prove ENR-01:
    (1) Coverage — for each cohort city, every top-level budget_categories name_key across datasets
        operating, revenue, AND salaries at the loaded years resolves to a category_enrichment row via
        (name_key, city) OR (name_key, NULL); report 0 uncovered. Include the LA County government
        entity and at least one salaries-only city.
    (2) Consistency with LA/OC (D-03) — enriched depth, populated-field set, and universal-vs-city
        placement match the baseline captured in Task 1.
    (3) Bleed-safety (D-04) — scan all universal (municipality_id IS NULL) rows touched by this phase
        for city-specific tokens (cohort city names, dollar-sign figures, city-specific proper nouns);
        assert none present. Spot-check across ≥3 distinct cities that no city's text appears on
        another city's categories (success criterion 2).
    (4) Spot-check 2–3 representative gap-set categories per dataset (op, revenue, salaries): confirm
        plain_name is citizen-friendly and the description is generic to the fund/department (no city
        specifics). Record samples in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "import('@supabase/supabase-js').then(async ({createClient})=>{const fs=require('fs');for(const f of ['.env.local','.env']){try{for(const l of fs.readFileSync(f,'utf8').split('\n')){const [k,...v]=l.split('=');if(k&&v.length&&!process.env[k.trim()])process.env[k.trim()]=v.join('=').trim();}}catch{}}const s=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_SERVICE_KEY,{db:{schema:'treasury'}});const {data}=await s.from('category_enrichment').select('name_key,plain_name,description').is('municipality_id',null).limit(5000);const leak=(data||[]).filter(r=>/\$\s?\d/.test((r.description||'')+(r.plain_name||''))).map(r=>r.name_key);console.log('universal rows:',(data||[]).length,'| dollar-figure leaks:',leak.length,leak.slice(0,10));process.exit(leak.length?1:0);})"</automated>
  </verify>
  <acceptance_criteria>
    - Coverage probe: every cohort city has 0 top-level budget_categories name_keys (across op/rev/salaries, at loaded years) lacking a category_enrichment match via (name_key, city) OR (name_key, NULL). LA County gov + ≥1 salaries-only city included.
    - Consistency probe: enriched depth + populated-field set + placement convention match the LA/OC baseline.
    - Bleed-safety probe: 0 universal rows contain a cohort city name, a dollar-amount figure, or city-specific facts (automated check exits 0); spot-checked across ≥3 cities (success criterion 2).
    - Spot-check: 2–3 sampled rows per dataset have citizen-friendly plain_name and strictly category-level description (recorded in the SUMMARY).
  </acceptance_criteria>
  <done>
    ENR-01 proven: every cohort city's operating, revenue, AND salaries categories carry enrichment consistent with LA/OC; universals are strictly category-level with no city-specific bleed (spot-checked ≥3 cities); spot-checks pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-authored text → production category_enrichment | The agent writes plain-language rows to a production table via the service key. Bad placement (city-specific text in a universal) bleeds across all cities. |
| repo .env service key → production Treasury DB | Writes bypass RLS; wrong DB target or anon key would mis-write or silently fail. |
| (forbidden) paid Anthropic SDK path | enrichCategories.js can spend money; D-01 forbids running it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering / Info disclosure | Universal (municipality_id IS NULL) enrichment rows | mitigate | D-04 enforced as task acceptance criteria: universals contain ONLY category-level text; city-specific content is city-scoped. Tasks 2 + 3 automated probe fails on any universal row carrying a dollar figure; manual scan checks city-name/proper-noun tokens across ≥3 cities. Mirrors the Phase 33/54 universal-bleed verifier and the fix in project_enrichment_scoping_fix. |
| T-61-02 | Elevation of cost / Spoofing intent | Accidental run of the paid enrichCategories.js path | mitigate | D-01: paid path never run. Gated by a blocking checkpoint:decision confirming $0 inline authoring; no ANTHROPIC_API_KEY call. autonomous:false. Any item needing paid spend triggers STOP for explicit approval (no auto-spend under the $5 gate). |
| T-61-03 | Tampering | Existing LA / OC / other-city enrichment + already-covered name_keys | mitigate | Gap filter (existing = city-scoped OR universal NULL) authors only the uncovered set; upsert on (name_key, municipality_id) is idempotent and does not clobber unrelated keys. |
| T-61-04 | Information disclosure | Wrong DB target (stale local / social DB) | mitigate | Use repo .env service key against prod Treasury DB only; mcp__supabase-local MUST NOT be used (D-10). Service key required (anon fails silently). |
| T-61-05 | Tampering | Salaries depth-1 city-specific counts leaking into universals | mitigate | Enrich salaries at top-level (DEPTH 0 / department) only (D-07); depth-1 `position (count)` nodes are not enriched, so per-city counts never enter category_enrichment. |
| T-61-SC | Tampering | npm package installs | accept | No new package installs — @supabase/supabase-js already present; the forbidden @anthropic-ai/sdk path is not invoked. |
</threat_model>

<verification>
- Gap set computed by replicating the script's skip filter (existing = city-scoped OR universal NULL), top-level, across operating + revenue + salaries; confirmed $0 inline at the gate.
- Coverage probe: every cohort city (incl. LA County gov + ≥1 salaries-only city) has 0 uncovered top-level name_keys.
- Bleed-safety probe: 0 universal rows carry city-specific tokens / dollar figures (automated check exits 0); spot-checked ≥3 cities.
- Consistency: enriched depth/fields/placement match the LA/OC baseline; source value matches.
- Spot-check: sampled rows per dataset are citizen-friendly + strictly category-level.
</verification>

<success_criteria>
- Every parity-loaded gap-set name_key (op + revenue + salaries) is enriched inline at $0 — no paid API call, enrichCategories.js not run (ENR-01, D-01).
- Rows mirror the enrichCategories.js write shape and match the LA/OC depth/richness/placement (D-02, D-03).
- All universal rows are strictly category-level — no city-specific bleed, spot-checked across ≥3 cities (D-04, success criterion 2).
- Every cohort city's operating, revenue, and salaries top-level categories resolve enrichment via (name_key, city) OR (name_key, NULL).
</success_criteria>

<output>
Create `.planning/phases/61-enrichment-parity/61-01-SUMMARY.md` when done.
</output>
