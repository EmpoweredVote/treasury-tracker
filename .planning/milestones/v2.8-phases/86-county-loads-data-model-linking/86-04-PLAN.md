---
phase: 86-county-loads-data-model-linking
plan: 04
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - scripts/loadOhioAOS.js
  - scripts/loadOhioAOS.test.mjs
autonomous: true
requirements: [OHCO-01]
must_haves:
  truths:
    - "detectLayout distinguishes CITY vs COUNTY workbooks (both share the SOREACIFB_TotalGov / SORDACIFB_TotalGov sheet names) — threaded via an entityType argument from the batch driver — and returns a COUNTY-specific layout profile with the correct header row, entity/county columns, revenue source columns, revenue total column, expenditure function columns, and expenditure total column for each basis"
    - "For the county GAAP workbook the layout is: header row 6, data start row 7, entityCol 1, countyCol 2, revenue source cols 3-15, revenue total col 16, expenditure function cols 17-31, expenditure total col 32 (cols 33+ = Excess/OFS/fund-balance/transfers EXCLUDED, D-02 analog) — verified against the live workbook header row"
    - "County CASH/MOD workbook layout (SORDACIFB_TotalGov) is probed + mapped the same way (its header row + column ranges discovered from the file, NOT assumed equal to the city CASH/MOD layout), with the expenditure total column located by its real position"
    - "enumerateCities + buildRevenueTree + buildExpenditureTree + cityPopulation + cityCounty all produce CORRECT results on county workbooks once the county layout is used: real text labels (e.g. 'Property Taxes', 'Public Safety'), not numeric values; Allen County is enumerated (it was previously dropped as a misread header row) so the GAAP county roster is ~64 incl. Allen"
    - "Offline tests assert the county path: enumerateCities(County GAAP wb) includes 'Allen County' and 'Franklin County'; buildRevenueTree(County GAAP, 'Franklin County') returns text-labeled sources (no numeric node names) and a total matching the workbook 'Total' (col 16); buildExpenditureTree total = col 32; ALL Phase 84/85/86-01 city tests still pass (city layout unchanged)"
  artifacts:
    - path: "scripts/loadOhioAOS.js"
      provides: "detectLayout with a correct county layout profile (GAAP + CASH/MOD), entityType-aware; tree-builders/enumerate now correct for counties"
      contains: "county"
    - path: "scripts/loadOhioAOS.test.mjs"
      provides: "Offline county-layout regression tests (text labels, Allen County recovered, totals match workbook)"
      contains: "Allen County"
  key_links:
    - from: "County GAAP/CASH/MOD workbooks (distinct layout from cities)"
      to: "correct text-labeled county revenue + expenditure trees + accurate totals"
      via: "detectLayout(workbook, entityType='county') → county layout profile → existing tree-builders"
      pattern: "county"
---

<objective>
GAP CLOSURE for Phase 86. The county load (plans 86-01/02) is broken: the county AOS workbooks use a DIFFERENT layout than the city workbooks (header on row 6 not 7; expenditure total at col 32 not 35; county-specific revenue vocabulary), but `detectLayout` applied the city layout. Result: all 66 loaded counties have numeric garbage category labels, wrong operating/expenditure totals, and Allen County was wrongly dropped (its data row was misread as the header).

Fix `detectLayout` so it returns a correct COUNTY layout profile (per basis), threaded via an `entityType` argument. Prove the county tree-builders now produce real text labels + correct totals via `--dry-run` + offline tests. NO writes here — the data reload + re-verification + re-linking are plan 86-05.

Cities are NOT affected (the city workbook genuinely uses row 7; do not change the city layout or any city behavior).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md
@.planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md
@scripts/loadOhioAOS.js
@scripts/loadOhioAOS.test.mjs
@scripts/loadOhioAOSBatch.js

<interfaces>
<!-- The defect is hardcoded city column indices in detectLayout applied to counties. -->

CONFIRMED county GAAP layout (from the live County_2024_GAAP_Summarized.XLSX, SOREACIFB_TotalGov):
- Row 6 = header: col1 "Entity Name", col2 "County", cols 3-15 = revenue sources (Property Taxes, Sales Taxes, Intergovernmental, Revenue In Lieu Of Taxes, Interest, Licenses And Permits, Fines And Forfeitures, Rentals, Charges For Services ×2, Contributions And Donations, Special Assessments, Other Revenues), col16 "Total" (revenue total).
- cols 17-31 = expenditure functions (General Government Legislative And Executive, General Government Judicial, Public Safety, Public Works, Health, Human Services, Conservation And Recreation, Community And Economic Development, Capital Outlay, Intergovernmental, Debt Service Principal Retirement, Debt Service Interest And Fiscal Charges, Debt Service Bond Issuance Costs, Debt Service Other, Other Expenditures), col 32 "Total" (expenditure total).
- cols 33+ = Excess Of Revenues, Sale Of Capital Assets, Inception Of Lease, bonds, Transfers In — OFS / fund-balance, EXCLUDE (mirrors city D-04b).
- Row 7 = first DATA row (Allen County). Data start = row 7. entityCol 1, countyCol 2.
- The city GAAP layout (header row 7, exp total col 35) is WRONG for counties — that is the bug.

County CASH/MOD (SORDACIFB_TotalGov) — DISCOVER its header row + column ranges from the live County_2024_CASH/MOD workbooks the same way (do not assume it equals the city CASH/MOD layout). The city CASH/MOD branch uses header row 6, entityCol 2 — verify whether counties match or differ, and set county CASH/MOD constants accordingly.

How to distinguish city vs county GAAP (same sheet name): thread an `entityType` argument. `loadOhioAOSBatch` already knows `--entity-type county` (86-01). Pass it into the workbook-reading calls. RECOMMENDED: `detectLayout(workbook, entityType = 'city')` returns the city profile by default (preserving every existing caller) and the county profile when entityType==='county'. Then buildRevenueTree/buildExpenditureTree/enumerateCities/cityPopulation/cityCounty must accept + forward entityType (default 'city') OR accept a pre-resolved layout. Pick the smaller-diff approach; keep all city call sites behaving identically.

Files already on disk (gitignored): _oh-recon/County_2024_{GAAP,CASH,MOD}_Summarized.XLSX.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a correct county layout profile to detectLayout (entityType-aware), GAAP + CASH/MOD</name>
  <files>scripts/loadOhioAOS.js</files>
  <read_first>
    - scripts/loadOhioAOS.js (detectLayout GAAP + CASH/MOD branches; how the layout descriptor fields feed buildRevenueTree/buildExpenditureTree/enumerateCities/cityPopulation/cityCounty)
  </read_first>
  <action>
    Make `detectLayout(workbook, entityType = 'city')` return a county-specific profile when entityType==='county':
    - County GAAP (SOREACIFB_TotalGov): headerRow 6, dataStart 7, entityCol 1, countyCol 2, revSourceCols [3..15], revTotalCol 16, expFuncCols [17..31], expTotalCol 32, plus the OI_Demographics offsets for the county file (discover demoHeaderRow/demoDataStart/demoEntityCol/demoCountyCol/demoPopCol from the county workbook).
    - County CASH/MOD (SORDACIFB_TotalGov): discover the header row + column ranges from County_2024_CASH/MOD and set them (do not assume city CASH/MOD values).
    Thread entityType (default 'city') through `enumerateCities`, `buildRevenueTree`, `buildExpenditureTree`, `cityPopulation`, `cityCounty` so they call detectLayout with the right entityType (or accept a resolved layout). Every existing city caller must behave EXACTLY as before (entityType defaults to 'city'). Update `loadOhioAOSBatch` to pass its `--entity-type` value into these calls.
    Verify interactively against _oh-recon/County_2024_GAAP_Summarized.XLSX before moving on (Franklin County revenue sources should be text-labeled; revenue total = col 16 value; expenditure total = col 32 value; Allen County now enumerated).
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/loadOhioAOS.js').then(m=>{const x=m.detectLayout.length; console.log(x>=2)})"` — detectLayout accepts an entityType arg (default 'city')
    - On County_2024_GAAP: enumerateCities includes "Allen County" AND "Franklin County"; buildRevenueTree("Franklin County") nodes are TEXT-labeled (e.g. "Property Taxes") with NO numeric node names; revenue total equals the workbook col-16 "Total"; buildExpenditureTree total equals the col-32 "Total"
    - On County_2024_CASH and _MOD: detectLayout(wb,'county') returns without throwing and enumerateCities returns text-labeled county names
    - Every existing city call path is unchanged (entityType default 'city')
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: County-layout regression tests + city no-regression</name>
  <files>scripts/loadOhioAOS.test.mjs</files>
  <read_first>
    - scripts/loadOhioAOS.test.mjs (existing city + county-01 cases; assert-when-present pattern)
    - scripts/loadOhioAOS.js (the entityType-aware exports)
  </read_first>
  <action>
    Add offline tests (assert-when-present on the gitignored county samples):
    - `enumerateCities(County GAAP wb, 'county')` includes "Allen County" and "Franklin County"; length ≥ 60.
    - `buildRevenueTree(County GAAP wb, "Franklin County", 'county')`: every node name is non-numeric text (regex: NOT /^-?[0-9]+$/); total is a finite number equal to the workbook col-16 value; "Property Taxes" present as a labeled source.
    - `buildExpenditureTree(County GAAP wb, "Franklin County", 'county')`: text labels, total = col-32 value, NO "Inception Of Lease" / "Transfers In" / "Excess" node (OFS excluded).
    - Regression: all prior city tests (Columbus etc.) still pass with the default entityType.
  </action>
  <acceptance_criteria>
    - `node --test scripts/loadOhioAOS.test.mjs` exits 0 with the new county-layout cases AND all prior city cases passing
    - At least one test asserts county tree node names are non-numeric (the exact regression that was broken)
    - At least one test asserts Allen County is enumerated (recovered)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: County dry-run proof (no writes)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (entityType threading into detectLayout/tree-builders)
  </read_first>
  <action>
    Run `node scripts/loadOhioAOSBatch.js --fy 2024 --entity-type county --dry-run` and confirm: ~64 GAAP counties processed INCLUDING Allen County, Franklin County revenue + operating totals are real finite numbers (and cross-read Franklin's published Total Revenues from the workbook col 16 to confirm they match), text-labeled categories, zero writes, zero failures. Capture the corrected county count (incl. Allen) + Franklin totals for the SUMMARY. NO writes.
  </action>
  <acceptance_criteria>
    - The county dry-run exits 0, includes Allen County, zero writes, zero failures
    - Franklin County's dry-run revenue total matches the workbook col-16 "Total" (text labels, not numeric)
    - No DB writes occur in this plan
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test scripts/loadOhioAOS.test.mjs` → all pass incl. new county-layout regression cases (text labels, Allen recovered, totals match workbook cols 16/32).
- `detectLayout(wb,'county')` returns the correct county profile (GAAP header row 6 / exp total col 32; CASH/MOD probed) without changing any city behavior.
- County dry-run for FY2024 shows ~64 counties incl. Allen, correct text labels + totals, zero writes.
- The broken-data reload + independent re-verification + re-linking + verification-record correction are plan 86-05.
</verification>
