---
phase: 86-county-loads-data-model-linking
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - scripts/linkOhioCitiesToCounties.js
  - scripts/ohioCountyResidual.json
must_haves:
  truths:
    - "Every filing Ohio county government is live in production with operating + revenue across FY2016–2025, entity_type='county', stored as '<Name> County', per-capita from OI_Demographics, every row stamped data_source='Ohio Auditor of State Summarized Annual Financial Reports' + county source_url + source_date (OHCO-01); GAAP→CASH→MOD backfill applied per (county, FY)"
    - "Every Ohio city's municipalities.county_id is set to its parent '<County> County' municipality, sourced DIRECTLY from the workbook OI_Demographics County column via cityCounty() (CONTEXT D-05) — no authored map; linking is idempotent (set-if-different) and cities whose County value has no matching loaded county are recorded as a link-residual, never linked to a phantom county"
    - "Counties remain standalone (county_id NULL); the pre-existing Ohio state node (name 'Ohio', entity_type='state') is verified present + data-less; the resulting graph reads US → Ohio → <County> County → <City> for cities (CONTEXT D-06)"
    - "The load + link are source-safe + idempotent: a second run creates 0 new municipalities, changes 0 county_id values, and leaves per-(county,FY,dataset) row counts unchanged; the never-overwrite guard protects any richer pre-existing source (auto-memory project_sync_city_budget_not_source_safe)"
    - "Counties present in OI_Demographics but absent from every basis workbook across all FY are written to a committed scripts/ohioCountyResidual.json (county + reason) and are NOT created as municipalities (no phantom rows; CONTEXT D-08)"
  artifacts:
    - path: "scripts/linkOhioCitiesToCounties.js"
      provides: "The city→county linking pass — reads each OH city's County column from a workbook and sets municipalities.county_id to the matching '<County> County' row; idempotent; records unmatched cities as a link-residual"
      contains: "county_id"
    - path: "scripts/ohioCountyResidual.json"
      provides: "Committed county source-gap residual — counties in OI_Demographics with no financial row in any basis/FY (no phantom municipalities)"
      contains: "reason"
  key_links:
    - from: "County_<FY>_<BASIS> workbooks + the OI_Demographics County column"
      to: "treasury.budgets county rows (entity_type='county') + municipalities.county_id on every OH city"
      via: "loadOhioAOSBatch --entity-type county (live) → then linkOhioCitiesToCounties (cityCounty → '<County> County' → county_id)"
      pattern: "county_id"
---

<objective>
Run the Phase 86 county load **live** for FY2016–2025 (OHCO-01) using the plan 86-01 tooling, then **link every Ohio city to its parent county** by reading the workbook's `OI_Demographics` County column and setting `municipalities.county_id` (OHLINK-01 data layer), and verify the Ohio state node. Finally verify in-phase (county spot-check, linking proof, per-capita, idempotency) and commit `scripts/ohioCountyResidual.json`.

This delivers OHCO-01 (counties loaded, sourced, per-capita) and the data half of OHLINK-01 (state node verified + city→county `county_id` set). The frontend/integration verification (breadcrumb, Cities-in-County panel, selectability) is plan 86-03. The deeper cross-phase audit + UAT are Phase 88.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md
@scripts/loadOhioAOSBatch.js
@scripts/loadOhioAOS.js
@scripts/ohioAosCountyDatasets.json

<interfaces>
<!-- Production writes. The loader/driver (86-01) is the tool; this plan runs it live + adds linking. -->

- Live writes require the gitignored `.env` `SUPABASE_SERVICE_KEY` (SUPABASE_URL=https://kxsdzaojfaibhuzmclfq.supabase.co — the production project) and run SERIALLY on the main tree (CONTEXT D-07). Idempotent + never-overwrite guarded.
- County load: `node scripts/loadOhioAOSBatch.js --fy <YYYY> --entity-type county` for each FY 2016–2025 (the driver downloads County_<FY>_<BASIS> workbooks via the county manifest, GAAP→CASH→MOD per-county assignment, writes via importCity with entityType:'county').
- Linking inputs: `cityCounty(workbook, cityName)` returns the bare county name (e.g. Columbus→"Franklin"); the parent county municipality is stored as "<County> County" ("Franklin County"). The OH city rows already exist (Phase 85, 253 cities). The county rows exist after the county load above.
- DB model: `treasury.municipalities.county_id` FK exists; `entity_type` CHECK allows 'county'/'state'. The Ohio state node already exists (name "Ohio", entity_type='state', pop 11,799,448) — verify, do not recreate. Use `total_budget`/`hierarchy` column names for any budget probes (auto-memory reference_treasury_budgets_probe_columns). Prefer mcp__supabase-local execute_sql for read-only probes (feedback_supabase_migration_mcp).
- A city can appear in multiple FY workbooks; read the County column from any FY that contains the city (prefer the most recent GAAP workbook, fall back to CASH/MOD) — the city→county relationship is stable across years.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire county workbooks + full-range dry-run pre-flight (zero writes)</name>
  <files>scripts/ohioAosCountyDatasets.json</files>
  <read_first>
    - scripts/ohioAosCountyDatasets.json (the FY×basis county URLs)
    - scripts/loadOhioAOSBatch.js (--entity-type county acquisition + dry-run)
  </read_first>
  <action>
    Ensure every available FY2016–2025 county workbook (GAAP/CASH/MOD) is present under gitignored `_oh-recon/` (the driver downloads on demand; pre-fetch surfaces dead URLs). Then, BEFORE any write, run `node scripts/loadOhioAOSBatch.js --fy <YYYY> --entity-type county --dry-run` for each FY 2016–2025: confirm a plausible county count per year (≈60–88 across GAAP+CASH+MOD), the basis distribution, zero failures, zero writes. Capture per-year processed counts + the cross-FY county residual for Task 4. Note any non-resolving manifest URL as a coverage gap for the SUMMARY.
  </action>
  <acceptance_criteria>
    - Every available county workbook opens with exceljs (detectLayout returns without throwing)
    - A dry-run for each FY 2016–2025 (--entity-type county) exits 0 with zero writes and zero unhandled failures
    - Per-year county counts + basis distribution captured for the SUMMARY; any dead URL noted
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live county load FY2016–2025 (serial, idempotent, sourced, entity_type='county')</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (live path; --entity-type county; failures log)
    - scripts/loadOhioAOS.js (importCity entityType threading → p_entity_type)
    - auto-memory project_utah_loader_entity_type_and_display_names (entity_type='county' mandatory)
  </read_first>
  <action>
    With the gitignored `.env` SUPABASE_SERVICE_KEY sourced, run the county load LIVE (no --dry-run) on the main tree, SERIALLY, for every FY 2016 → 2025: `node scripts/loadOhioAOSBatch.js --fy <YYYY> --entity-type county`. Each county writes operating + revenue under its assigned basis with the county source_url, as `entity_type='county'`, stored "<Name> County". Per-county errors append to a failures log without aborting. Capture per-FY processed/written counts.
  </action>
  <acceptance_criteria>
    - The live county load completes for FY2016–2025; counties exist as entity_type='county' named "<Name> County" with operating + revenue rows, each stamped the Ohio AOS data_source + a non-null county source_url + source_date
    - No phantom city rows were created for counties (verify: every "<Name> County" row is entity_type='county', not 'city')
    - CASH/MOD-loaded counties carry their basis (≥0 is acceptable if all counties file GAAP; report the distribution)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: City→county linking from the source County column (scripts/linkOhioCitiesToCounties.js)</name>
  <files>scripts/linkOhioCitiesToCounties.js</files>
  <read_first>
    - scripts/loadOhioAOS.js (cityCounty, enumerateCities, getSupabase)
    - scripts/seedMACountyLinks.js (the Phase 25 city→county county_id linking precedent — set-if-different idempotent update pattern)
    - .planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md (D-05 sourced linking, no authored map; D-06 direction)
  </read_first>
  <action>
    Create `scripts/linkOhioCitiesToCounties.js` (ESM, reuse getSupabase + the never-overwrite-style guarded update). For every OH city municipality:
    - Read its County value from a county-bearing workbook via `cityCounty(workbook, cityName)` (prefer the most recent FY GAAP city workbook; fall back to CASH/MOD). Map to the parent municipality named `"<County> County"`, `entity_type='county'`, `state='OH'`.
    - If the parent county municipality exists and the city's `county_id` differs, UPDATE `municipalities.county_id` (idempotent set-if-different). If the county value is blank or no matching county municipality exists, record the city in a link-residual (do NOT create a phantom county, do NOT set a bogus id).
    - Support `--dry-run` (print the would-set links + residual, zero writes). Counties keep `county_id` NULL.
    Run it live after the dry-run looks right. Capture linked-count + link-residual for the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/linkOhioCitiesToCounties.js --dry-run` prints a plausible link count (≈253 cities) + any unmatched-city residual, zero writes
    - After the live run, a DB probe shows the large majority of OH cities have a non-null county_id pointing to an entity_type='county' OH municipality (e.g. Columbus.county_id → "Franklin County")
    - Counties have county_id NULL; no phantom county municipality was created by the linker
    - Re-running the linker changes 0 county_id values (idempotent)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: In-phase verification + commit scripts/ohioCountyResidual.json</name>
  <files>scripts/ohioCountyResidual.json</files>
  <read_first>
    - .planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md (D-08 residual; D-06 graph)
    - feedback_supabase_migration_mcp (use mcp__supabase-local execute_sql for read-only probes)
    - reference_treasury_budgets_probe_columns (total_budget/hierarchy column names)
  </read_first>
  <action>
    Run read-only verification probes + an idempotency re-run:
    - **County spot-check (OHCO-01):** read back one county's stored FY2024 operating + revenue totals (e.g. Franklin County) and confirm they match the workbook figures; confirm county rows have non-null source_url + the Ohio AOS data_source + non-zero population (per-capita).
    - **Coverage:** count distinct OH counties with budgets + per-(FY,dataset) row counts; confirm the count is in the expected ≈60–88 range.
    - **Linking proof (OHLINK-01):** confirm Columbus.county_id → "Franklin County"; count OH cities with non-null county_id; confirm at least one full chain (city → its county → Ohio state node) resolves.
    - **State node:** confirm the "Ohio" entity_type='state' node exists and has no budget datasets.
    - **Idempotency:** re-run one FY of the county load + the linker; confirm 0 new municipalities, 0 changed county_id, unchanged row counts.
    - **Residual:** write + commit `scripts/ohioCountyResidual.json` (object with note + `counties` array of `{name, reason}`) for counties in OI_Demographics absent from every basis/FY workbook; confirm none exist as municipalities.
  </action>
  <acceptance_criteria>
    - A sampled county's stored FY2024 totals match the workbook; county rows are sourced + have non-zero population
    - Columbus.county_id resolves to the "Franklin County" entity_type='county' row; the majority of OH cities have non-null county_id
    - The "Ohio" state node exists, entity_type='state', with zero budget datasets
    - Idempotency re-run: 0 new munis, 0 changed county_id, unchanged per-(FY,dataset) county row counts
    - `scripts/ohioCountyResidual.json` committed; listed counties (if any) do not exist as municipalities
  </acceptance_criteria>
</task>

</tasks>

<verification>
- County load ran serially FY2016–2025; counties live as entity_type='county' ("<Name> County"), sourced + per-capita, GAAP→CASH→MOD backfill applied (OHCO-01).
- Every (most) OH city has county_id → its "<County> County" parent, sourced from the OI_Demographics County column (no authored map); counties standalone; Ohio state node verified data-less → US→Ohio→County→City graph (OHLINK-01 data layer).
- Idempotent + source-safe: re-run creates 0 munis, changes 0 county_id, unchanged row counts.
- `scripts/ohioCountyResidual.json` committed; no phantom county municipalities.
- Frontend/integration verification (breadcrumb, Cities-in-County panel, selectability) is plan 86-03; cross-phase audit + UAT are Phase 88.
</verification>
