---
phase: 86-county-loads-data-model-linking
plan: 05
type: execute
wave: 2
gap_closure: true
depends_on: ["86-04"]
files_modified:
  - scripts/ohioCountyResidual.json
  - .planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md
autonomous: false
requirements: [OHCO-01, OHLINK-01]
must_haves:
  truths:
    - "The broken county budget data (numeric labels + wrong operating totals from the pre-86-04 load) is DELETED before reload — because the never-overwrite guard would otherwise SKIP same-data_source rows; only OH entity_type='county' budgets from data_source='Ohio Auditor of State Summarized Annual Financial Reports' are removed (cities untouched)"
    - "All county governments are reloaded live FY2016-2025 with the corrected county layout (86-04): real text category labels, correct revenue (col 16) + operating/expenditure (col 32) totals, per-capita, sourced; Allen County is now loaded (previously wrongly dropped) so the GAAP county roster is ~64 and overall coverage approaches all 88 counties"
    - "Independent re-verification confirms (read back from DB, not loader self-report): zero county budget_categories with numeric depth-0 link_key/name; a spot-checked county's stored revenue + operating totals match the workbook col-16 / col-32 figures; county rows are sourced + per-capita"
    - "City→county linking is re-run: with Allen County now present, Lima + Delphos link normally; the authored override (Germantown/Ironton) still applies only where the source county is blank; the link-residual is recomputed and ohioCountyResidual.json is rewritten (Allen removed — it is NOT a source gap)"
    - "86-VERIFICATION.md is corrected to a final accurate verdict for the county half (OHCO-01) + linking (OHLINK-01), replacing the superseded PASS, with DB-derived evidence"
  artifacts:
    - path: "scripts/ohioCountyResidual.json"
      provides: "Corrected county residual (Allen removed — now loaded; only genuinely-absent counties, if any, remain)"
      contains: "counties"
    - path: ".planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md"
      provides: "Corrected final phase-86 verdict with independent DB evidence for the fixed county data"
      contains: "OHCO-01"
  key_links:
    - from: "Corrected county layout (86-04) + delete of broken county budgets"
      to: "accurate text-labeled county budgets + complete county coverage + city→county links"
      via: "delete broken county rows → loadOhioAOSBatch --entity-type county live → linkOhioCitiesToCounties re-run → DB re-verification"
      pattern: "county"
---

<objective>
GAP CLOSURE for Phase 86 (data half). With the county layout fixed (86-04), delete the broken county budget data, reload all county governments live for FY2016-2025 with correct labels + totals (recovering Allen County), re-run city→county linking, independently re-verify against the DB, rewrite the county residual, and correct 86-VERIFICATION.md.

This is the production-write half of the gap closure. The never-overwrite guard protects same-source rows, so the broken county budgets MUST be explicitly deleted before reload — a plain re-run would skip them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-county-loads-data-model-linking/86-CONTEXT.md
@.planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md
@scripts/loadOhioAOSBatch.js
@scripts/loadOhioAOS.js
@scripts/linkOhioCitiesToCounties.js

<interfaces>
<!-- Production writes. Target: SUPABASE_URL=https://kxsdzaojfaibhuzmclfq.supabase.co (.env). -->

- Live writes need the gitignored .env SUPABASE_SERVICE_KEY, run SERIALLY on the main tree. Idempotent + never-overwrite guarded.
- DELETE scope (critical): remove ONLY the broken county budgets — `treasury.budgets` rows whose municipality is OH + entity_type='county' + data_source='Ohio Auditor of State Summarized Annual Financial Reports', plus their `budget_categories` (cascade or explicit). Do NOT touch city budgets, the Ohio state node's General Fund data, or county municipality rows / county_id. Prefer mcp__supabase-local execute_sql for the delete + verification (auto-memory feedback_supabase_migration_mcp). Use treasury.budgets columns total_budget/hierarchy for probes (auto-memory reference_treasury_budgets_probe_columns).
- Reload: `node scripts/loadOhioAOSBatch.js --fy <YYYY> --entity-type county` for FY2016-2025 (now uses the corrected county layout from 86-04). Re-link: `node scripts/linkOhioCitiesToCounties.js` (idempotent; with Allen County present, Lima + Delphos resolve).
- Allen County: previously in ohioCountyResidual.json as "absent" — it is NOT absent (it was a misread header). After reload it exists; remove it from the residual.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the broken county budget data (cities untouched)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - .planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md (the defect scope)
    - feedback_supabase_migration_mcp (use mcp__supabase-local for the delete + checks)
  </read_first>
  <action>
    Count, then DELETE only the broken county budgets: `treasury.budgets` where municipality_id ∈ (OH entity_type='county') AND data_source='Ohio Auditor of State Summarized Annual Financial Reports', plus their `budget_categories` rows (verify the FK cascade or delete categories explicitly first). Confirm BEFORE + AFTER counts: OH city budget rows unchanged, Ohio state-node General Fund rows unchanged, county municipality rows + county_id values unchanged (only their budgets/categories removed). Capture the deleted count for the SUMMARY.
  </action>
  <acceptance_criteria>
    - Only OH county budgets from the Ohio AOS data_source are deleted; OH city budget count + Ohio state-node dataset count are identical before/after
    - County municipality rows (and any city county_id) are NOT deleted
    - Post-delete: 0 OH county budgets remain for that data_source (clean slate for reload)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reload all counties FY2016-2025 with the corrected layout (live)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (--entity-type county live path, now using corrected detectLayout)
    - scripts/loadOhioAOS.js (corrected county layout from 86-04)
  </read_first>
  <action>
    With .env sourced, run `node scripts/loadOhioAOSBatch.js --fy <YYYY> --entity-type county` LIVE, SERIALLY, for FY2016-2025. Each county writes operating + revenue with real text labels + correct totals + per-capita + county source_url, entity_type='county', "<Name> County". Allen County now loads. Capture per-FY processed/written counts + the basis distribution.
  </action>
  <acceptance_criteria>
    - All FY2016-2025 county loads complete; Allen County present; counties have text-labeled categories + correct revenue (col16) + operating (col32) totals
    - No phantom city rows; county rows sourced + per-capita
    - Per-FY counts captured for the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-run city→county linking + rewrite residual</name>
  <files>scripts/ohioCountyResidual.json</files>
  <read_first>
    - scripts/linkOhioCitiesToCounties.js (idempotent re-run; Allen now present)
    - scripts/ohioCountyResidual.json (remove Allen)
  </read_first>
  <action>
    Re-run `node scripts/linkOhioCitiesToCounties.js` live. With Allen County loaded, Lima + Delphos should now link via the source County column (the authored override remains for genuinely-blank-source cities Germantown/Ironton). Recompute the county source-gap residual and rewrite `scripts/ohioCountyResidual.json` — remove Allen County (not absent), list only counties genuinely absent from all workbooks (likely none). Capture the final linked count.
  </action>
  <acceptance_criteria>
    - Lima + Delphos now have county_id set (Allen County); link count increases accordingly (target 253/253 minus any still genuinely blank)
    - ohioCountyResidual.json no longer lists Allen County; lists only genuinely-absent counties (if any), each with a reason
    - Linker remains idempotent (a second run = 0 changes)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Independent DB re-verification + correct 86-VERIFICATION.md</name>
  <files>.planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/86-county-loads-data-model-linking/86-VERIFICATION.md (superseded banner to replace with the corrected verdict)
    - reference_treasury_budgets_probe_columns (total_budget/hierarchy)
  </read_first>
  <action>
    Run read-only DB probes (mcp__supabase-local) and record results in a corrected 86-VERIFICATION.md (replace the superseded PASS banner with a final accurate verdict):
    - **Zero numeric labels:** count OH county budget_categories with depth-0 link_key/name matching ^-?[0-9]+$ → must be 0.
    - **Totals spot-check:** a sampled county's (e.g. Franklin County FY2024) stored revenue total = workbook col-16 value AND stored operating total = workbook col-32 value (read back from DB).
    - **Coverage:** distinct OH counties with budgets (incl. Allen); per-(FY,dataset) row counts.
    - **Sourcing + per-capita:** county rows have non-null source_url + Ohio AOS data_source + non-zero population.
    - **Linking:** Columbus→Franklin County; Lima/Delphos→Allen County; count cities with county_id.
    - **Idempotency:** re-run one FY county load + the linker → 0 new munis, 0 changed county_id, unchanged row counts.
    Write the corrected verdict (OHCO-01 + OHLINK-01) with this DB-derived evidence.
  </action>
  <acceptance_criteria>
    - 0 OH county budget_categories have numeric depth-0 labels (the original defect is gone)
    - A sampled county's stored revenue + operating totals match the workbook col-16 / col-32 figures
    - 86-VERIFICATION.md final verdict reflects the corrected county data + linking with DB evidence (no stale "Allen absent" / superseded PASS claims)
    - Idempotency re-run: 0 new munis, 0 changed links, unchanged county row counts
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Broken county budgets deleted (cities/state-node untouched); all counties reloaded FY2016-2025 with correct text labels + totals (rev col16 / exp col32) + per-capita + sourcing; Allen County recovered (OHCO-01 now met for counties).
- City→county linking re-run: Lima + Delphos link to Allen County; residual rewritten (Allen removed); linker idempotent (OHLINK-01).
- Independent DB re-verification: 0 numeric county labels, sampled totals match workbook, coverage + sourcing confirmed; 86-VERIFICATION.md corrected to a final accurate verdict.
- After this plan, Phase 87 (enrichment) can be planned over the corrected city + county vocabulary.
</verification>
