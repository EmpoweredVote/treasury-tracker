---
phase: 85-city-loads
plan: 02
type: execute
wave: 2
depends_on: ["85-01"]
files_modified:
  - scripts/ohioCityResidual.json
  - scripts/loadOhioAOSBatch.js
autonomous: false
requirements: [OHCITY-01, OHCITY-02]
must_haves:
  truths:
    - "Every GAAP-filing Ohio city is live in production for the full FY2016–2025 range with operating + revenue, every row stamped data_source='Ohio Auditor of State Summarized Annual Financial Reports' + the per-FY+basis source_url + source_date (OHCITY-01); the load ran serially via loadOhioAOSBatch across all available FY (CONTEXT D-01)"
    - "Cities absent from the GAAP workbook are backfilled from the CASH then MOD workbook for that FY with the basis recorded per (city, FY) — a city can be GAAP in one year and CASH in another; the GAAP→CASH→MOD precedence (CONTEXT D-02) holds across every loaded year (OHCITY-02)"
    - "Per-capita renders: each loaded city/FY has the population set from that FY's OI_Demographics tab (CONTEXT D-06), so the app computes per-capita without a missing-population gap"
    - "Cities present in OI_Demographics but absent from EVERY basis workbook across ALL FY are written to a committed scripts/ohioCityResidual.json (city + reason) and are NOT created as municipalities — no phantom rows (CONTEXT D-03; OHCITY-02)"
    - "The load is idempotent and source-safe: a second full run creates 0 new municipalities, leaves per-(city,FY,dataset) row counts unchanged, and the never-overwrite guard skips any (muni,FY,dataset) already owned by a different/richer data_source"
  artifacts:
    - path: "scripts/ohioCityResidual.json"
      provides: "Committed source-gap residual — the durable record of demographics-only Ohio cities with no financial data in any basis/FY workbook (no phantom municipalities)"
      contains: "reason"
    - path: "scripts/loadOhioAOSBatch.js"
      provides: "The plan 85-01 batch driver, run live for FY2016–2025 (plus any minimal live-path fix surfaced during the real run)"
      contains: "loadOhioAOSBatch"
  key_links:
    - from: "FY2016–2025 Ohio AOS all-cities workbooks (GAAP + CASH/MOD)"
      to: "treasury.budgets operating + revenue rows for every filing Ohio city, sourced + per-capita"
      via: "loadOhioAOSBatch live run (GAAP→CASH→MOD per-city assignment) → importCity → treasury_sync_city_budget with never-overwrite guard"
      pattern: "loadOhioAOSBatch"
---

<objective>
Run the Phase 85 batch loader **live** against production Supabase to load every filing Ohio city — operating + revenue — across the full FY2016–2025 range (CONTEXT D-01), with GAAP primary and CASH/MOD backfill per (city, FY) (OHCITY-02). Then verify the load in-phase: DB row/municipality counts, a published-report spot-check (Columbus FY2024 exact), per-capita population set, every row sourced, and idempotency (a second run changes nothing). Finally write the committed `scripts/ohioCityResidual.json` documenting demographics-only cities (no phantom municipalities).

This delivers OHCITY-01 (GAAP cities loaded, sourced, per-capita) and OHCITY-02 (CASH/MOD backfill + documented source-gap residual). The batch driver (plan 85-01) does the work; this plan acquires all the data, runs it live, verifies, and records the residual. The deeper cross-phase source-chain audit + UAT are Phase 88 — this plan only does the in-phase spot-check (ROADMAP SC#1–4).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/85-city-loads/85-CONTEXT.md
@scripts/loadOhioAOSBatch.js
@scripts/loadOhioAOS.js
@scripts/ohioAosDatasets.json

<interfaces>
<!-- The batch driver (plan 85-01) is the tool; this plan supplies data + runs it live + verifies. -->

- Live writes require the gitignored `.env` `SUPABASE_SERVICE_KEY` and run SERIALLY on the main tree (CONTEXT D-05). Writes are safe to repeat — the never-overwrite guard protects any richer pre-existing source and importCity/importDataset are idempotent.
- `scripts/ohioAosDatasets.json` (Phase 84) — the FY2016–2025 × {GAAP,CASH,MOD} manifest (floor 2016, all URLs HTTP-200 confirmed). `resolveSourceUrl(fy, basis)` reads it. This is the committed provenance for source_url — verify the URLs still resolve; leave the file unchanged unless a URL moved.
- `loadOhioAOSBatch({ fy, dryRun, limit, fileGaap, fileCash, fileMod })` → `{ fy, processed, assigned:{GAAP,CASH,MOD}, residual:[...], failures:[...] }`. The CLI driver downloads missing workbooks into gitignored `_oh-recon/` via the manifest.
- Verification probes are read-only against production (Supabase MCP execute_sql or a small node read script). Per auto-memory `feedback_supabase_migration_mcp`, prefer the mcp__supabase-local tools for DB reads/checks.
- Residual: the `residual` array returned by the batch run (union across FY) is the input to `scripts/ohioCityResidual.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire all FY2016–2025 × {GAAP,CASH,MOD} workbooks into _oh-recon/ (pre-flight)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/ohioAosDatasets.json (the 30 FY×basis entries + URLs; floor 2016)
    - scripts/loadOhioAOSBatch.js (the acquisition/download path added in 85-01)
  </read_first>
  <action>
    Ensure every available FY2016–2025 basis workbook is present locally in gitignored `_oh-recon/`. For each manifest entry, download `City_<FY>_<BASIS>_Summarized.XLSX` from its URL if missing (the batch driver does this on demand, but pre-fetching surfaces any dead URL before the live run). Confirm each downloaded file opens with exceljs and `detectLayout` recognises it (SOREACIFB for GAAP, SORDACIFB for CASH/MOD). Record any manifest URL that no longer resolves as a data-availability note for the SUMMARY (do NOT silently skip — a missing year is a coverage gap to report). Do not commit the XLSX files (gitignored); `scripts/ohioAosDatasets.json` stays the committed provenance — verify its URLs, leave it unchanged unless one moved.
  </action>
  <acceptance_criteria>
    - Every manifest FY×basis workbook with a live URL exists under `_oh-recon/` and opens with exceljs (detectLayout returns without throwing)
    - Any non-resolving manifest URL is captured in a note for the SUMMARY (coverage gap), not silently dropped
    - No XLSX file is staged for commit; `scripts/ohioAosDatasets.json` is unchanged (or a moved-URL fix only)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-range dry-run across FY2016–2025 (pre-flight, zero writes)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (loadOhioAOSBatch return shape + CLI)
    - .planning/phases/85-city-loads/85-CONTEXT.md (D-02 precedence; D-03 residual)
  </read_first>
  <action>
    Before any write, run the batch loader in `--dry-run` for EACH fiscal year 2016 through 2025 and eyeball the per-FY summaries: confirm a plausible city count per year (≈200–235 for recent GAAP years; older years may differ), the GAAP→CASH→MOD basis distribution looks right (most GAAP in recent years), Columbus FY2024 totals match the Phase 84 recon ($2.166B revenue / Total Expenditures operating), and no city errors out (`failures` empty). Capture, per year, the processed count + basis distribution + residual count for the SUMMARY. Accumulate the cross-FY residual (cities in OI_Demographics absent from every financial tab across ALL years) for Task 4.
  </action>
  <acceptance_criteria>
    - A dry-run for each FY 2016–2025 exits 0 with zero writes and zero unhandled per-city failures
    - Columbus FY2024 dry-run shows revenue ≈ $2.166B and the operating total = the Total Expenditures figure
    - Each year's processed count + basis distribution + residual count is captured for the SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Live load FY2016–2025 (serial, idempotent, source-safe)</name>
  <files>scripts/loadOhioAOSBatch.js</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (live write path; failures-log append)
    - scripts/loadOhioAOS.js (importCity → treasury_ensure_municipality + importDataset never-overwrite guard)
    - auto-memory project_sync_city_budget_not_source_safe (the guard is mandatory — already inside importDataset)
  </read_first>
  <action>
    With the gitignored `.env` `SUPABASE_SERVICE_KEY` sourced, run the batch loader LIVE (no `--dry-run`) on the main tree, SERIALLY, for every fiscal year 2016 → 2025:
    `node scripts/loadOhioAOSBatch.js --fy <YYYY>` for each year (a thin wrapper loop is fine; keep it serial — no parallel RPC fan-out). Each (city, FY) writes operating + revenue under its assigned basis with the per-FY+basis source_url. The never-overwrite guard skips any (muni,FY,dataset) already owned by a different data_source; per-city errors append to `scripts/load-ohio-cities.failures.txt` without aborting the run. Capture per-FY processed/written counts + any failures for the SUMMARY.

    NOTE (autonomous:false): this is the production-write task — pause for the user before the first live FY if there is any ambiguity about the target Supabase project; otherwise proceed, since writes are idempotent + guarded.
  </action>
  <acceptance_criteria>
    - The live run completes for all of FY2016–2025; every processed city/FY has operating + revenue rows in production, each stamped data_source='Ohio Auditor of State Summarized Annual Financial Reports' + a non-null source_url + source_date
    - `scripts/load-ohio-cities.failures.txt` contains no unexplained per-city failures (any entry is triaged in the SUMMARY)
    - No city is loaded under a basis other than its GAAP→CASH→MOD assignment; CASH/MOD-loaded cities have their basis recorded
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: In-phase verification (spot-check, sourcing, per-capita, idempotency) + write scripts/ohioCityResidual.json</name>
  <files>scripts/ohioCityResidual.json</files>
  <read_first>
    - scripts/loadOhioAOSBatch.js (the residual array from the runs)
    - .planning/phases/85-city-loads/85-CONTEXT.md (D-03 residual file shape; D-06 per-capita)
    - feedback_supabase_migration_mcp (prefer mcp__supabase-local tools for the read-only DB probes)
  </read_first>
  <action>
    Run read-only verification probes against production and an idempotency re-run:
    - **Spot-check (SC#2):** confirm the STORED Columbus FY2024 revenue total ≈ $2.166B and Income Taxes ≈ $1.145B and operating Police ≈ $810M match the published-report values — cross-read the stored tree, not just the loader output.
    - **Coverage (SC#1):** count distinct OH cities with budgets and the per-(FY,dataset) row counts; confirm the count is in the expected ≈235 range for recent GAAP years and that every loaded row has a non-null source_url + data_source.
    - **Per-capita (SC#2/D-06):** confirm loaded city/FY rows have a non-zero population set (sample a few across years) so per-capita renders.
    - **Backfill (SC#3/OHCITY-02):** confirm at least one CASH/MOD-basis city is present with its basis recorded.
    - **Idempotency (SC#4):** re-run one FY live and confirm 0 new municipalities + unchanged per-(city,FY,dataset) row counts.
    - **Residual (SC#4/D-03):** write `scripts/ohioCityResidual.json` from the accumulated cross-FY residual — an object with a note + a `cities` array of `{ name, reason }` (e.g. "demographics-only: no SOREACIFB/SORDACIFB row in any FY 2016–2025"). Confirm none of these names exist as municipalities in production (no phantom rows). Commit this file.
  </action>
  <acceptance_criteria>
    - Stored Columbus FY2024 revenue ≈ $2.166B, Income Taxes ≈ $1.145B, Police ≈ $810M (read back from the DB, within tolerance)
    - Every loaded OH city budget row has a non-null source_url + data_source='Ohio Auditor of State Summarized Annual Financial Reports'; sampled rows have a non-zero population (per-capita renders)
    - ≥1 CASH/MOD-basis city present with basis recorded (backfill proven live)
    - The idempotency re-run creates 0 new municipalities and leaves per-(FY,dataset) row counts unchanged
    - `scripts/ohioCityResidual.json` exists, is committed, lists each demographics-only city + reason, and none of those names exist as OH municipalities
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Live load ran serially for FY2016–2025; every filing city has sourced operating + revenue with per-FY+basis source_url + source_date (OHCITY-01).
- GAAP→CASH→MOD per-(city,FY) backfill holds; ≥1 CASH/MOD city present with basis recorded; mixed basis across years allowed (OHCITY-02).
- Stored Columbus FY2024 totals match the published report (read back from DB); sampled rows have population set (per-capita renders, D-06).
- `scripts/ohioCityResidual.json` is committed and documents demographics-only cities; no phantom municipalities created (D-03).
- Idempotency: a second run creates 0 new munis and changes no row counts; the never-overwrite guard protects richer pre-existing sources.
- Deeper cross-phase source-chain audit + UAT are Phase 88 — this plan does the in-phase spot-check only.
</verification>
