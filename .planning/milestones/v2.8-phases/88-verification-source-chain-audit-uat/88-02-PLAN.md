---
phase: 88-verification-source-chain-audit-uat
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
autonomous: false
files_modified:
  - scripts/seedOHState.js
  - .planning/phases/88-verification-source-chain-audit-uat/88-02-AUDIT.md
requirements: [OHVER-01]
must_haves:
  truths:
    - "Full-cohort source-chain audit across ALL loaded Ohio rows (4,880 city + 1,736 county budget rows + 51 universal enrichment rows + the 10 state-node rows) confirms: 0 NULL source_url / source_date / data_source, 0 fragile links (every AOS source_url resolves to a live ohioauditor.gov file; enrichment rows carry source='ai' + evidence), and 0 residue (no orphan budgets, no stale/duplicate (muni,fy,dataset) rows, no phantom municipalities)"
    - "Independent re-derivation (CONTEXT D-88-06, the Phase 86 lesson): for the sample entities + ≥3 random others incl. ≥1 county and ≥1 CASH/MOD entity, a handful of category amounts AND the dataset totals are re-computed straight from the workbook and MATCH the stored DB values (catches any residual label/column/row mismapping) — 0 numeric-garbage category labels cohort-wide"
    - "In-phase fix #1 (D-88-04): the 10 'Ohio' state-node General Fund NULL source_url + source_date rows are stamped with the Ohio OBM state-budget source; after the fix the FULL OH cohort is literally 0-NULL source_url"
    - "In-phase fix #2 (D-88-05): Ironton population backfilled from a sourced Census figure (was 0 → per-capita now renders); any other population=0 OH entity is fixed the same way or documented; idempotent set-if-different"
    - "A written 88-02-AUDIT.md records the audit queries + results, the re-derivation samples, both fixes (before/after), and the OHVER-01 part B verdict; re-running the fixes is idempotent (0 further changes)"
  artifacts:
    - path: ".planning/phases/88-verification-source-chain-audit-uat/88-02-AUDIT.md"
      provides: "The full-cohort source-chain audit + independent re-derivation + in-phase-fix record (OHVER-01 part B)"
      contains: "source-chain"
    - path: "scripts/seedOHState.js"
      provides: "Updated to stamp the OH state-node General Fund rows with the OBM source_url + source_date (fix #1), idempotent"
      contains: "source_url"
  key_links:
    - from: "All loaded Ohio rows (budgets + enrichment + state node)"
      to: "a clean, durable, residue-free source chain + accurate stored figures"
      via: "audit queries + independent workbook re-derivation + two approved in-phase fixes"
      pattern: "source-chain"
---

<objective>
OHVER-01 part B: prove the full Ohio cohort's source chain is durable, complete, and residue-free, and that the stored figures are accurate (independently re-derived, not loader-trusted — the Phase 86 county near-miss lesson). Then apply the two Chris-approved in-phase fixes and re-verify.

Production writes are limited to the two approved fixes (state-node source stamp; Ironton population backfill); everything else is read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/88-verification-source-chain-audit-uat/88-CONTEXT.md
@.planning/milestones/v2.7-phases/83-verification-source-chain-audit-uat/83-02-SUMMARY.md
@scripts/seedOHState.js
@scripts/loadOhioAOS.js

<interfaces>
<!-- Production target kxsdzaojfaibhuzmclfq (.env). Only 2 writes; rest read-only. mcp__supabase-local preferred. -->
- Cohort (re-verify counts): 253 cities / 4,880 rows; 88 counties / 1,736 rows; 51 universal enrichment rows; 10 state-node General Fund rows. Use total_budget/hierarchy column names.
- Source-chain audit queries (read-only): NULL source_url/source_date/data_source by entity_type; duplicate (municipality_id, fiscal_year, dataset_type); orphan budgets (no municipality) / orphan budget_categories (no budget); numeric-garbage depth-0 labels cohort-wide (regex ^-?[0-9]+$ → must be 0 after the 86 fix); enrichment rows with NULL plain_name/description.
- Fragile-link check: every distinct AOS source_url should match the committed manifests (scripts/ohioAosDatasets.json / ohioAosCountyDatasets.json) and resolve HTTP 200 (the manifests were probed; spot-re-probe a sample). Durable-human-page bar (CONTEXT D-88-03).
- Independent re-derivation (D-88-06): use scripts/loadOhioAOS.js detectLayout/buildRevenueTree/buildExpenditureTree on the _oh-recon workbooks to recompute totals + a few category amounts for Columbus, Franklin County, + ≥3 random entities (incl. ≥1 county + ≥1 CASH/MOD), compare to stored DB values.
- Fix #1 (state node): the 10 rows (data_source 'Ohio General Fund Operating Budget' / 'Ohio General Fund Revenue') have NULL source_url + source_date. seedOHState.js / processOH.js loaded them — read those for the intended OBM source; use the canonical Ohio OBM operating-budget page (https://obm.ohio.gov / budget.ohio.gov). Stamp source_url + source_date (the budget publication/retrieval date). Idempotent.
- Fix #2 (Ironton): municipalities.population for Ironton (OH, city) is 0. Set from a sourced U.S. Census figure (2020 decennial / latest ACS — record the figure + source). Check other OH entities for population=0 and fix-or-document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-cohort source-chain audit (read-only)</name>
  <files>.planning/phases/88-verification-source-chain-audit-uat/88-02-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v2.7-phases/83-verification-source-chain-audit-uat/83-02-SUMMARY.md (the VA audit checks + format)
    - 88-CONTEXT.md (D-88-03 durability bar; the known state-node NULL gap)
  </read_first>
  <action>
    Run the read-only audit queries (mcp__supabase-local) over the full OH cohort and record results in 88-02-AUDIT.md: (a) NULL source_url/source_date/data_source by entity_type (expect 0 for cities+counties, 10 for the state node pre-fix); (b) duplicate (muni,fy,dataset); (c) orphan budgets / orphan budget_categories; (d) numeric-garbage depth-0 labels cohort-wide (must be 0); (e) enrichment rows with NULL plain_name/description (must be 0); (f) fragile links — every distinct AOS source_url is in the committed manifest + spot-re-probe ~5 URLs for HTTP 200. Flag every finding; the only expected one is the 10 state-node NULLs (fixed in Task 3).
  </action>
  <acceptance_criteria>
    - 88-02-AUDIT.md records every query + result; cities+counties show 0 NULL/fragile/residue; the 10 state-node NULLs are the only NULL finding
    - 0 duplicate (muni,fy,dataset) rows; 0 orphan budgets/categories; 0 numeric-garbage labels cohort-wide; 0 enrichment rows missing text
    - A sample of ~5 AOS source_urls re-probed HTTP 200 (durable)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Independent re-derivation of stored figures (the Phase 86 lesson)</name>
  <files>.planning/phases/88-verification-source-chain-audit-uat/88-02-AUDIT.md</files>
  <read_first>
    - project_ohio_aos_county_vs_city_layout (why totals-only / loader-self-report is insufficient)
    - scripts/loadOhioAOS.js (detectLayout/buildRevenueTree/buildExpenditureTree for re-derivation)
  </read_first>
  <action>
    For Columbus, Franklin County, and ≥3 random OH entities (incl. ≥1 county and ≥1 CASH/MOD-basis entity), independently recompute from the _oh-recon workbooks (via the loader's pure tree-builders): the operating + revenue totals AND 2-3 named category amounts each, then compare to the stored DB values. Record matches/deltas in 88-02-AUDIT.md. Any mismatch is a finding (the Phase 86 county defect was exactly this class). Confirm category labels are real text everywhere sampled.
  </action>
  <acceptance_criteria>
    - ≥5 entities (incl. ≥1 county + ≥1 CASH/MOD) have independently re-derived totals + sample category amounts MATCHING the stored DB values
    - All sampled category labels are real text (no numeric garbage)
    - Any mismatch is recorded as a finding with root-cause (none expected after the 86 fix)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Apply the two approved in-phase fixes + re-verify (live)</name>
  <files>scripts/seedOHState.js, .planning/phases/88-verification-source-chain-audit-uat/88-02-AUDIT.md</files>
  <read_first>
    - scripts/seedOHState.js / scripts/processOH.js (the intended OBM state-budget source)
    - 88-CONTEXT.md (D-88-04 state-node stamp; D-88-05 Ironton population)
  </read_first>
  <action>
    With .env sourced (production): 
    Fix #1 — stamp the 10 'Ohio' state-node General Fund rows' source_url + source_date with the canonical Ohio OBM operating-budget source (confirm/record the exact URL; update seedOHState.js so it's reproducible + idempotent, then apply). 
    Fix #2 — set Ironton's municipalities.population from a sourced Census figure (record figure + source); fix any other OH population=0 entity the same way or document why deferred. 
    Re-verify (mcp__supabase-local): full OH cohort now 0 NULL source_url/source_date; Ironton population > 0 (per-capita renders); re-run is idempotent (0 further changes). Record before/after in 88-02-AUDIT.md with the OHVER-01 part B verdict.
  </action>
  <acceptance_criteria>
    - After fix #1: 0 OH budget rows (any entity_type) have NULL source_url or source_date; the 10 state-node rows carry the OBM source_url + a source_date
    - After fix #2: Ironton population > 0 from a cited Census figure; no OH city/county with population=0 remains undocumented
    - Both fixes are idempotent (re-run = 0 changes); 88-02-AUDIT.md ends with the OHVER-01 part B PASS verdict + before/after evidence
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full-cohort source chain is durable + complete + residue-free: after the state-node stamp, 0 NULL source_url/source_date/data_source across ALL OH rows; 0 duplicate/orphan/residue; 0 numeric-garbage labels; enrichment intact; sampled source_urls resolve.
- Independent re-derivation (not loader self-report) confirms stored totals + sample category amounts match the workbooks for ≥5 entities incl. a county + a CASH/MOD entity (the Phase 86 lesson applied).
- Ironton population backfilled (per-capita renders); both in-phase fixes idempotent; 88-02-AUDIT.md records everything with the OHVER-01 part B verdict.
- UAT + sign-off is plan 88-03.
</verification>
