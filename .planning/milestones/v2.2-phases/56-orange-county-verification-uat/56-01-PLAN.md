---
phase: 56-orange-county-verification-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase56.mjs
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Running `node scripts/verify-phase56.mjs` exits 0 with all 7 automated DB gaps PASS"
    - "The probe confirms 34 OC cities are linked to the Orange County entity via county_id"
    - "The probe confirms operating + revenue + salaries coverage for the OC city set"
    - "The probe confirms Anaheim/Santa Ana custom-sourced rows (source_url IS NULL) are intact"
    - "The probe confirms 9 known-good city/year/dataset totals match exactly"
  artifacts:
    - path: "scripts/verify-phase56.mjs"
      provides: "Read-only DB-probe verification script for Phase 56 automatable assertions (gaps 56-01-01..07)"
      min_lines: 120
      contains: "Accept-Profile"
  key_links:
    - from: "scripts/verify-phase56.mjs"
      to: "treasury.municipalities / treasury.budgets (PostgREST)"
      via: "native node:https request with Accept-Profile: treasury header, creds from .env"
      pattern: "Accept-Profile.*treasury"
---

<objective>
Create `scripts/verify-phase56.mjs` — the read-only DB-probe that automates every machine-checkable VER-01 assertion against the production Treasury DB, then run it to confirm it exits 0.

Purpose: This is the Wave 0 automated harness for Phase 56 (per 56-VALIDATION.md). It is the precondition for the manual ACFR reconciliation (Plan 02) and closeout (Plan 03) — those plans trust the DB totals only because this probe confirms coverage, attribution, custom-row preservation, and known exact values.
Output: `scripts/verify-phase56.mjs` (new), exiting 0 with 7 automated gaps PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-orange-county-verification-uat/56-RESEARCH.md
@.planning/phases/56-orange-county-verification-uat/56-PATTERNS.md

<interfaces>
<!-- The probe is a standalone ESM script. Mirror scripts/verify-phase34.mjs EXACTLY for the
     env-load, pass()/fail() accumulator, native node:https request helper (agent:false),
     content-range count extraction, and the summary + exit 0/1 block. 56-PATTERNS.md contains
     copy-ready excerpts (lines 26-198) — use them; do not re-derive. -->

Constants (from 56-RESEARCH.md §DB Probe Design / 56-PATTERNS.md lines 211-232):
  OC_COUNTY_ID    = '65e7c643-5829-4821-9537-f8595bce61ab'
  ANAHEIM_ID      = '7fbdd013-69c9-41fb-a87d-c9ca7b3cdeb5'
  SANTA_ANA_ID    = '2dc65052-aa62-4a3c-a5c0-eea78dfe9ad3'
  IRVINE_ID       = '17f0abc4-751f-4609-adcd-d6274ed33269'
  HB_ID           = 'd0b51865-2581-4091-8d4c-18e2a2750657'
  NEWPORT_ID      = 'a091a210-e017-47df-ba65-5e2bf43c95c8'
  VILLA_PARK_ID   = 'ce99c02d-b889-4c38-832d-face172b5a8c'
  LAGUNA_WOODS_ID = '3a25551e-5a40-40a7-ac72-3e6938695f40'

KNOWN_TOTALS (gap 56-01-06 exact-match assertions, from 56-RESEARCH.md §Known DB Values):
  Irvine     FY2024 operating 656013821
  HB         FY2019 operating 323441057
  Anaheim    FY2024 operating 1640316917
  Anaheim    FY2025 operating 490937159
  Santa Ana  FY2024 operating 414022680
  Santa Ana  FY2024 revenue   400947213
  Newport    FY2024 operating 444327078
  Villa Park FY2024 operating 6111009
  Laguna Wds FY2024 operating 10051862

Env: SUPABASE_URL + (SUPABASE_SERVICE_KEY || SUPABASE_SERVICE_ROLE_KEY), read from ../.env.local then ../.env.
PostgREST header for schema selection: Accept-Profile: treasury.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold verify-phase56.mjs from the verify-phase34 pattern</name>
  <files>scripts/verify-phase56.mjs</files>
  <read_first>
    - scripts/verify-phase34.mjs (the exact analog — env-load, pass()/fail() accumulator, native node:https helper, content-range count, summary + exit 0/1)
    - .planning/phases/56-orange-county-verification-uat/56-PATTERNS.md (copy-ready excerpts, lines 26-256)
  </read_first>
  <action>
    Create `scripts/verify-phase56.mjs` as a standalone Node ESM script. Copy verbatim from verify-phase34.mjs: the import block (node:fs, node:https request as httpsRequest, node:http request as httpRequest, node:path resolve/dirname, node:url fileURLToPath, __dirname), the `loadEnv()` function (reads ../.env.local then ../.env, does not overwrite already-set process.env keys), and the `results[]` / `pass(gapId, description)` / `fail(gapId, description, detail)` accumulator. Read SUPABASE_URL and SUPABASE_SERVICE_KEY||SUPABASE_SERVICE_ROLE_KEY into locals; if either is missing, call fail() with a clear "DB check skipped — missing env" message naming the missing vars (do NOT echo any key value). Write a header comment listing gap coverage 56-01-01 through 56-01-07 (automated) and noting 56-02-01 + 56-03-01 are human-only. Add the constants block (OC_COUNTY_ID, the 7 sampled city IDs, KNOWN_TOTALS) from the interfaces section. Add a `dbGet(path, {head})` helper using native node:https/http `request` with headers apikey + Authorization Bearer + `Accept-Profile: treasury` + (`Accept: application/json` for GET or `Prefer: count=exact` for HEAD), and `agent: false` (disable keep-alive — required to avoid Windows open-handle hangs per verify-phase34 line 197). For HEAD, resolve the parsed `content-range` total (split on `/`, index 1, parseInt, -1 on parse failure); for GET, collect chunks and JSON.parse the body.
  </action>
  <verify>
    <automated>node -c scripts/verify-phase56.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -c scripts/verify-phase56.mjs` reports no syntax errors.
    - File defines loadEnv(), pass(), fail(), a dbGet helper using node:https/node:http with `agent: false` and `Accept-Profile: treasury`.
    - File reads creds from `.env`; no SUPABASE key value is interpolated into any console.log/fail-detail string.
    - Constants block contains OC_COUNTY_ID and all 7 sampled city IDs and a 9-entry KNOWN_TOTALS list matching 56-RESEARCH.md §Known DB Values.
  </acceptance_criteria>
  <done>Script scaffold exists, parses cleanly, has env-load + accumulator + dbGet helper + constants, leaks no secret.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the 7 automated DB gap checks and the summary/exit block</name>
  <files>scripts/verify-phase56.mjs</files>
  <read_first>
    - scripts/verify-phase56.mjs (the file from Task 1)
    - .planning/phases/56-orange-county-verification-uat/56-RESEARCH.md (§DB Probe Design — Automatable Assertions table + Key DB Query Patterns + Known DB Values)
    - .planning/phases/56-orange-county-verification-uat/56-PATTERNS.md (lines 235-256 PostgREST query patterns; lines 174-207 summary/exit + section-heading patterns)
  </read_first>
  <action>
    Implement each gap as a check that calls pass()/fail(). First resolve the OC city id set: HEAD `municipalities?county_id=eq.{OC_COUNTY_ID}&entity_type=eq.city&select=id` and GET the same to collect the id list for the IN() clauses. Gap 56-01-01: assert that count = 34 (city set linked to OC entity). Gap 56-01-02: count of `budgets?municipality_id=in.(ids)&fiscal_year=gte.2003&fiscal_year=lte.2024&dataset_type=eq.operating` is >= 33*22 = 726 (>=, not ==, because Anaheim/Santa Ana custom years and known Phase 53 source gaps shift the exact total). Gap 56-01-03: same with dataset_type=eq.revenue, >= 726. Gap 56-01-04: count of `budgets?municipality_id=in.(ids)&source_url=not.is.null&source_url=not.like.*%2Fd%2F*&dataset_type=eq.operating` equals 0 (every non-null ByTheNumbers source_url contains the durable `/d/` dataset path). Gap 56-01-05: GET Anaheim rows where source_url is null → assert exactly 4 (FY2025-26 operating+revenue); GET Santa Ana rows where source_url is null → assert >= 8 (FY2023-26 operating+revenue custom). Gap 56-01-06: for each of the 9 KNOWN_TOTALS, GET `budgets?municipality_id=eq.{id}&fiscal_year=eq.{fy}&dataset_type=eq.{type}&select=total_budget` and assert the returned total_budget strictly equals the expected integer; fail listing every mismatch with expected-vs-actual. Gap 56-01-07: count distinct municipality_id in `budgets?municipality_id=in.(ids)&dataset_type=eq.salaries` equals 34 (GET select=municipality_id, dedupe in JS). Then append the summary block (per-gap PASS/FAIL list, "N passed, M failed", the human-only note pointing to 56-VERIFICATION.md) and `process.exit(failCount === 0 ? 0 : 1)`. Use the section-heading console.log style from 56-PATTERNS.md. Do NOT print any URL containing the apikey/Authorization; build request paths without embedding creds in the path.
  </action>
  <verify>
    <automated>node scripts/verify-phase56.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/verify-phase56.mjs` exits 0.
    - Output shows 7 PASS lines (56-01-01 through 56-01-07) and "7 passed, 0 failed".
    - 56-01-01 prints count = 34; 56-01-02 and 56-01-03 print counts >= 726; 56-01-04 prints 0 non-durable rows; 56-01-05 prints Anaheim 4 / Santa Ana >= 8 custom rows; 56-01-06 prints 9 exact matches; 56-01-07 prints 34 distinct salaried cities.
    - The process exits cleanly (no hang) — `agent: false` is set on every request.
    - grep of script output contains no JWT/service-key substring (no secret leaked to stdout).
  </acceptance_criteria>
  <done>All 7 automated VER-01 DB assertions pass; script exits 0; no secret printed; process terminates without hanging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local script → production Treasury DB (PostgREST) | Read-only HTTP requests authenticated with the service-role key from `.env`. The key is a high-value secret; the DB is production. |
| `.env` file → process memory → stdout/logs | The service key is loaded from disk into env and used in request headers. It must never reach stdout, the git index, or the SUMMARY. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Information Disclosure | service-role key in `.env` | mitigate | Read creds into locals only; never interpolate the key into any console.log, fail() detail, or request path; build PostgREST paths without embedding creds. Acceptance criteria assert no key substring in stdout. |
| T-56-02 | Tampering | production `treasury` tables | mitigate | Probe issues only HEAD and GET (read-only). No POST/PATCH/DELETE/RPC. The script's purpose is assertion, never write. |
| T-56-03 | Information Disclosure | `verify-phase56.mjs` committed to git | mitigate | Script contains only hard-coded UUIDs + public dataset path fragments; zero secrets. Creds come exclusively from `.env` at runtime (already git-ignored per prior verify-phase scripts). |
| T-56-04 | Denial of Service | open https handles on Windows | accept | `agent: false` closes connections immediately (verify-phase34 precedent); read-only probe makes a bounded number of requests. No high-severity risk. |

No threat reaches `high` severity: the script is read-only, secrets stay out of logs/git, and the request surface is a fixed set of bounded PostgREST reads.
</threat_model>

<verification>
- `node scripts/verify-phase56.mjs` exits 0 with 7 PASS lines.
- The script makes only HEAD/GET requests (read-only) against schema `treasury`.
- No SUPABASE key value appears anywhere in stdout.
</verification>

<success_criteria>
- `scripts/verify-phase56.mjs` exists and is the Wave 0 automated harness for Phase 56.
- All 7 automatable VER-01 gaps (56-01-01..07) pass; exit 0.
- 56-VALIDATION.md `wave_0_complete` precondition is satisfiable (the probe exists and passes).
</success_criteria>

<output>
Create `.planning/phases/56-orange-county-verification-uat/56-01-SUMMARY.md` when done.
</output>
