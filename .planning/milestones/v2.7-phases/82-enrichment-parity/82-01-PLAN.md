---
phase: 82-enrichment-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/vaEnrichment82.mjs
  - scripts/loadVAEnrichment82.mjs
  - scripts/loadVAEnrichment82.test.mjs
autonomous: false
requirements: [VAENR-01]
user_setup:
  - service: Supabase (production)
    why: "Task 3 dry-run derives the worklist read-only from production budget_categories. Task 4 (the --apply delete-then-insert) WRITES universal category_enrichment rows to production. Both reuse the gitignored .env / .env.local service key already present locally."
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "gitignored .env / .env.local (Supabase service role) — already present locally; never log or commit. SUPABASE_SERVICE_KEY also accepted (same fallback as loadUtahEnrichment72.mjs)."

must_haves:
  truths:
    - "Every one of the 73 live Virginia category name_keys (operating depth-0 + depth-1, revenue depth-0 + depth-1, including the 12 Virginia state-node keys) has exactly one universal (municipality_id IS NULL) enrichment row, authored from an explicit hand-written map — 100% coverage, enforced by a dry-run assertion that ABORTS if any live VA key is unmapped (D-82-01, D-82-02, D-82-04, SC#1)"
    - "Every authored row is bleed-safe by construction — no VA locality name, no dollar figure, no entity-specific fact in any plain_name/short_description/description; a $-leak guard AND a VA-locality-name guard ABORT the run on any violation (D-82-05, SC#2)"
    - "Authoring is inline at $0 — no paid AI API path exists in the loader (the only cost is zero, same as Phase 61/66/72); within the $5 AI gate (SC#1)"
    - "Writes use delete-then-insert over the authored name_keys (NOT upsert): the (name_key, municipality_id) index is NULLS DISTINCT so upsert would INSERT duplicate universal rows. Re-running --apply reproduces the same rows (idempotent) (D-82-06)"
    - "The 7 shared universal keys (public safety, public works, education, community development, fines and forfeitures, revenue from use of money and property, miscellaneous) are overwritten with entity- AND state-neutral text that reads correctly for a VA county, a CA city, and an MA town; the prior miscellaneous='Information Technology' universal row is corrected to a revenue catch-all; a CA+MA no-regression spot-check passes (D-82-03)"
    - "The two parent-specific 'interest' composites carry distinct, parent-correct descriptions: general property taxes|interest (interest/penalty on delinquent tax) vs revenue from use of money and property|interest (interest earned on invested cash) (D-82-08)"
    - "The enrichment name_key written for each node EXACTLY matches the join key the app/API uses for that node: depth-0 = plain link_key; depth-1 = the full parent|child composite link_key — confirmed against live budget_categories and the app's enrichment fetch in src/data/dataLoader.ts / src/App.tsx"
  artifacts:
    - path: "data/vaEnrichment82.mjs"
      provides: "VA_ENRICHMENT — an explicit object keyed by exact name_key (all 73 live VA keys), each value { plain_name, short_description, description, tags:[...], confidence }, in the caParityEnrichment61 CONCEPTS voice; entity- AND state-neutral, no locality names, no $ figures. Also exports the canonical EXPECTED_KEYS list (the 73 keys) for the coverage assertion + tests."
      contains: "VA_ENRICHMENT"
    - path: "scripts/loadVAEnrichment82.mjs"
      provides: "Live-worklist VA enrichment loader mirroring loadUtahEnrichment72.mjs: derives the distinct operating depth-0/1 + revenue depth-0/1 link_keys for VA entities from production, looks each up in VA_ENRICHMENT, ABORTS if any live key is unmapped (100% coverage gate), runs the $-leak + locality-name guards, and on --apply performs delete-then-insert of universal rows (dry-run by default). Exports a pure resolve/build helper for offline tests."
      contains: "VA_ENRICHMENT"
    - path: "scripts/loadVAEnrichment82.test.mjs"
      provides: "Offline node:test suite: VA_ENRICHMENT covers every EXPECTED_KEY (73); no $-figure and no VA locality name anywhere in the map; the two 'interest' composites differ; miscellaneous plain_name != 'Information Technology'; every row has the required fields with municipality_id implied null + source 'ai'."
      contains: "EXPECTED_KEYS"
  key_links:
    - from: "loadVAEnrichment82.mjs (live worklist: operating+revenue, depth 0 and 1)"
      to: "treasury.category_enrichment (universal rows)"
      via: "exact name_key (link_key) → VA_ENRICHMENT[name_key] → delete-then-insert (municipality_id IS NULL)"
      pattern: "explicit-map-coverage"
    - from: "VA_ENRICHMENT name_key"
      to: "app render (category.enrichment.plainName)"
      via: "API server-side join by name_key (city-scoped first, then NULL universal), surfaced via src/data/dataLoader.ts → src/App.tsx"
      pattern: "two-tier-universal-join"
---

<objective>
Author standardized, plain-language, **bleed-safe** category enrichment for **every** Virginia budget category loaded in Phases 80–81 — all 73 distinct `name_key`s across operating (function + `function|activity`) and revenue (source + `source|subsource`), including the 12 Virginia state-node keys — reusing the Phase 61/66/72 inline-authoring pattern at **$0**.

Because Virginia's taxonomy is **fixed and standardized statewide** by the APA Comparative Report, author an **explicit hand-written map** (`data/vaEnrichment82.mjs`) keyed by exact `name_key` — one accurate row per key — rather than Phase 72's heuristic router (which existed only for Utah's unbounded messy fund names). The loader derives the live worklist, **asserts 100% coverage** of that map against the live keys, and writes universal rows via **delete-then-insert** (NULLS DISTINCT). All rows are universal (`municipality_id IS NULL`) and bleed-safe by construction (no locality names, no `$` figures), and deliberately entity- AND state-neutral so the 7 shared keys also read correctly for CA/MA/etc. This is a **data-only** phase: no app or API code changes (VAENR-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/82-enrichment-parity/82-CONTEXT.md
@.planning/phases/82-enrichment-parity/82-RESEARCH.md
@.planning/REQUIREMENTS.md
@scripts/loadUtahEnrichment72.mjs
@scripts/loadUtahEnrichment72.test.mjs
@data/caParityEnrichment61.mjs
@scripts/enrichCategories.js
@scripts/loadVAComparativeReport.js
@src/data/dataLoader.ts

<interfaces>
<!-- New work mirrors the proven Phase 72 enrichment loader, simplified to an explicit map. NOTHING about
     existing CA/SoCal/Utah enrichment data files or loaders changes — vaEnrichment82.mjs is additive.
     The only existing rows touched are the 7 SHARED universal keys, intentionally overwritten with
     entity/state-neutral text per D-82-03 ("Improve + fix"). -->

LIVE VOCABULARY (verified 2026-06-23 — the loader re-derives it live and asserts coverage against the map):
  VA entities: state='VA' (independent cities, counties, towns + the 'Virginia' state node).
  73 distinct name_keys total:
    operating depth-0 (functions):           10   (9 locality + 'virginia general fund budget' state header)
    operating depth-1 (function|activity):    28   (22 locality + 6 state-node program composites)
    revenue  depth-0 (sources):                8   (7 locality + 'virginia general fund revenue' state header)
    revenue  depth-1 (source|subsource):      27   (23 locality + 4 state-node composites)
  link_key format: depth-0 = plain (e.g. "public safety", "general property taxes");
                   depth-1 = "parent|child" (e.g. "public safety|fire and rescue services").
  Exact key list is enumerated in 82-RESEARCH.md and MUST be reproduced verbatim in EXPECTED_KEYS
  (byte-for-byte, including: "non- departmental" (space after hyphen), "merchants' capital" (apostrophe),
  "coal, oil, and gas taxes" (internal commas), "parks, recreation, and cultural|...").

EXPLICIT MAP (data/vaEnrichment82.mjs — NEW):
  VA_ENRICHMENT — object keyed by exact name_key → { plain_name, short_description, description, tags:[...], confidence }.
    Voice = the citizen-friendly CONCEPTS voice in data/caParityEnrichment61.mjs (purpose + what it pays for +
    how it's funded). Entity- AND state-neutral: works for a city, a county, AND a town; NO "the city" framing
    where a county/town also uses the key — prefer "the locality" / "local government". NO locality names, NO $ figures.
    Functions: general government administration, judicial administration, public safety, public works,
      health and human services, education, parks/recreation/cultural, community development, non- departmental.
    Activities: each depth-1 composite gets a row scoped to that activity under its function (e.g.
      public safety|fire and rescue services = fire/EMS; public works|sanitation and waste removal = trash/recycling/landfill).
    Revenue: general property taxes (+ its 8 subsources, incl. two distinct senses where 'interest' = penalty interest),
      other local taxes (+ 13 subsources), permits/privilege fees/regulatory licenses, fines and forfeitures,
      charges for services, revenue from use of money and property (+ interest = investment interest, rental/sale),
      miscellaneous (a genuine revenue catch-all — NOT "Information Technology").
    State node: virginia general fund budget + 6 program areas (education, general government, health and human
      services, natural resources and commerce, other programs, public safety and corrections); virginia general
      fund revenue + 4 sources (corporate income tax, individual income tax, other taxes and fees, sales and use tax).
      Frame as STATE-level program areas / statewide taxes, not local departments.
  EXPECTED_KEYS — exported array of the 73 key strings (single source of truth for the coverage gate + tests).

LOADER (scripts/loadVAEnrichment82.mjs — NEW; mirror loadUtahEnrichment72.mjs structure, simpler):
  CLI: node scripts/loadVAEnrichment82.mjs           # dry-run: derive worklist, assert coverage, print plan, NO writes
       node scripts/loadVAEnrichment82.mjs --apply    # delete-then-insert universal rows into treasury.category_enrichment
  Worklist derivation (live; paginate past the 1000-row cap; chunk .in() id lists ~25-30):
    VA_IDS = municipalities where state='VA' (all entity types incl. the state node row).
    For dataset_type in (operating, revenue): collect DISTINCT depth-0 link_keys AND depth-1 link_keys.
  Coverage gate (the core difference from Phase 72): for every distinct live link_key, require VA_ENRICHMENT[key]
    to exist. If ANY live key is unmapped → print the missing keys and ABORT (no fallback). Also warn (non-fatal)
    if VA_ENRICHMENT has keys not present in the live worklist (stale entries).
  Row shape (identical to Utah): { name_key, municipality_id:null, plain_name, short_description, description,
    tags, source:'ai', confidence, evidence_summary (Phase-82 generic note), generated_at }.
  Guards BEFORE any write: (a) $-leak — abort if /\$\s?\d/ in description+plain_name+short_description of any row;
    (b) locality-name — abort if any authored text contains a VA locality name (case-insensitive) drawn from the
    live municipalities.name list for state='VA' (fetched once; guards against accidental entity facts).
  Write (on --apply): delete-then-insert over the authored keys — for each chunk of name_keys,
    delete().is('municipality_id', null).in('name_key', chunk); then insert(rows) in batches of ~200.
    (NULLS DISTINCT means upsert can't overwrite a NULL-municipality row — see memory category-enrichment-nulls-distinct.)
  Logging (dry-run + apply): VA entity count, per-bucket worklist sizes, total keys, coverage result (missing count
    must be 0), $-leak count (0), locality-leak count (0), rows to author. Write data/va-enrichment-82.expanded.json.
  Supabase client: createClient(URL, SERVICE_ROLE_KEY||SERVICE_KEY, { db:{ schema:'treasury' } }); load .env.local/.env.
  GENERATED_AT: env override (e.g. PHASE82_TS) else a fixed ISO literal '2026-06-23T00:00:00.000Z'.

APP JOIN (read-only confirmation — do NOT change): the budget API joins category_enrichment by name_key,
  city-scoped row first then NULL universal; src/data/dataLoader.ts fetches the tree and src/App.tsx reads
  category.enrichment.{plainName,shortDescription,description}. The depth-1 join key is the full parent|child
  composite link_key — the name_keys written here match it (confirmed against live budget_categories 2026-06-23).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author data/vaEnrichment82.mjs — explicit 73-key VA enrichment map + EXPECTED_KEYS (bleed-safe, state-neutral, $0)</name>
  <files>data/vaEnrichment82.mjs</files>
  <read_first>
    - data/caParityEnrichment61.mjs (CONCEPTS shape + the exact citizen-friendly voice to match: { plain_name, short_description, description, tags, confidence })
    - .planning/phases/82-enrichment-parity/82-RESEARCH.md (the verbatim 73-key vocabulary list to reproduce in EXPECTED_KEYS + map keys)
    - .planning/phases/82-enrichment-parity/82-CONTEXT.md (D-82-01..08 — authoring model, scope, disambiguation, neutrality rules)
    - scripts/loadVAComparativeReport.js (what each VA function/activity/revenue source actually covers, to write accurate descriptions)
  </read_first>
  <behavior>
    - Export VA_ENRICHMENT: an object with exactly the 73 keys from 82-RESEARCH.md (byte-for-byte). Each value is { plain_name, short_description, description, tags:[...], confidence }. Voice = the caParityEnrichment61 CONCEPTS voice.
    - Text is entity- AND state-neutral: usable for a VA city, county, AND town, and (for the 7 shared keys) for CA/MA entities too. Prefer "the locality" / "local government"; do NOT assume a city. NO locality name, NO $ figure, NO entity-specific fact.
    - depth-1 composite rows describe the specific activity/subsource under its parent (e.g. public safety|inspections = building/health/code inspections; other local taxes|consumer utility taxes = tax on utility bills).
    - The two "interest" composites are written distinctly (D-82-08): general property taxes|interest = interest/penalty charged on delinquent property tax; revenue from use of money and property|interest = interest earned on the locality's invested cash and bank balances.
    - miscellaneous (revenue depth-0) = a small catch-all of revenue that doesn't fit the named sources (refunds, recoveries, sundry receipts) — explicitly NOT "Information Technology".
    - State-node keys (virginia general fund budget/revenue + composites) framed as Virginia STATE program areas / statewide taxes.
    - Export EXPECTED_KEYS: an array of the 73 key strings — the single source of truth for the loader's coverage gate and the test.
    - Module is pure data — no DB, no network, importable offline.
  </behavior>
  <action>
    1. Create data/vaEnrichment82.mjs. Define EXPECTED_KEYS as the 73 key strings copied verbatim from 82-RESEARCH.md (mind: "non- departmental", "merchants' capital", "coal, oil, and gas taxes", commas/apostrophes inside leaves).
    2. Author VA_ENRICHMENT with one row per EXPECTED_KEY, matching the field shape + voice of CONCEPTS in caParityEnrichment61.mjs. Keep descriptions concept-level (what the function/activity/source is and how it's funded), entity- and state-neutral.
    3. Write the two "interest" composites distinctly and the miscellaneous revenue catch-all per <behavior>.
    4. Add a module-level comment documenting: all rows universal + bleed-safe by construction, $0 inline (no AI API), Phase-82 provenance, and that EXPECTED_KEYS is the coverage source of truth.
  </action>
  <verify>
    <automated>node --check data/vaEnrichment82.mjs && node -e "import('./data/vaEnrichment82.mjs').then(m=>{const k=m.EXPECTED_KEYS,e=m.VA_ENRICHMENT;if(k.length!==73){console.error('EXPECTED_KEYS len',k.length);process.exit(1)}for(const key of k){const r=e[key];if(!r||!r.plain_name||!r.description||!r.short_description){console.error('missing/incomplete row:',key);process.exit(1)}}const all=JSON.stringify(e);if(/\$\s?\d/.test(all)){console.error('DOLLAR LEAK');process.exit(1)}if(e['miscellaneous'].plain_name==='Information Technology'){console.error('miscellaneous still IT');process.exit(1)}console.log('rows',Object.keys(e).length,'expected',k.length)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check data/vaEnrichment82.mjs` exits 0.
    - EXPECTED_KEYS has exactly 73 entries; VA_ENRICHMENT has a complete row ({plain_name, short_description, description, tags, confidence}) for every EXPECTED_KEY.
    - No `$\d` appears anywhere in the module (bleed/$-leak guard).
    - `VA_ENRICHMENT['miscellaneous'].plain_name` is NOT 'Information Technology' and reads as a revenue catch-all.
    - `VA_ENRICHMENT["general property taxes|interest"].description` differs from `VA_ENRICHMENT["revenue from use of money and property|interest"].description`.
  </acceptance_criteria>
  <done>data/vaEnrichment82.mjs exports a complete, bleed-safe, state-neutral explicit map for all 73 VA keys plus EXPECTED_KEYS, in the proven CONCEPTS voice, with zero $-figures, the miscellaneous label corrected, and the two interest senses disambiguated.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/loadVAEnrichment82.mjs + offline tests — live worklist, 100% coverage gate, guards, delete-then-insert</name>
  <files>scripts/loadVAEnrichment82.mjs, scripts/loadVAEnrichment82.test.mjs</files>
  <read_first>
    - scripts/loadUtahEnrichment72.mjs (the structure to mirror: .env load, live-worklist pagination, $-leak guard, delete-then-insert for NULL-municipality writes, dry-run vs --apply, expanded-JSON writer)
    - scripts/loadUtahEnrichment72.test.mjs (offline test shape — node:test, import pure helpers, no network)
    - data/vaEnrichment82.mjs (VA_ENRICHMENT + EXPECTED_KEYS from Task 1)
    - src/data/dataLoader.ts (CONFIRM the enrichment join: name_key = link_key, depth-1 = parent|child composite; do NOT change app code)
    - .planning/phases/82-enrichment-parity/82-CONTEXT.md (D-82-05/06/07 guards + write rules)
  </read_first>
  <behavior>
    - Mirror loadUtahEnrichment72.mjs: load .env.local/.env; createClient with treasury schema; --apply flag; dry-run writes nothing.
    - Derive the live worklist for VA entities (municipalities where state='VA', all entity types): for dataset_type in (operating, revenue) collect DISTINCT depth-0 link_keys AND depth-1 link_keys. Paginate past the 1000-row cap; chunk .in() id lists.
    - Coverage gate: for every distinct live link_key, require VA_ENRICHMENT[key]. Collect all unmapped keys; if any → print them and process.exit(1) (no fallback). Warn (non-fatal) on map keys absent from the live worklist.
    - Build universal rows (municipality_id:null) from VA_ENRICHMENT in the Utah row shape; set evidence_summary to a Phase-82 generic note (mention inline-authored, fixed VA taxonomy, bleed-safe); generated_at from PHASE82_TS env or the fixed literal.
    - Guards before any write: (a) $-leak — abort on any /\$\s?\d/ in row text; (b) locality-name — fetch the VA municipalities.name list once, lowercase, and abort if any authored row text contains one (skip ultra-generic tokens that are also common words if needed, but VA locality names are distinctive).
    - Write on --apply: delete-then-insert — for each chunk (~100) of authored name_keys: delete().is('municipality_id',null).in('name_key',chunk); then insert(rows) in batches (~200). Idempotent.
    - Print: VA entity count, per-bucket worklist sizes, total distinct keys, missing-coverage count (must be 0), $-leak count (0), locality-leak count (0), rows authored. Write data/va-enrichment-82.expanded.json.
    - Export a pure helper (e.g. buildRows(liveKeys) → { rows, missing }) so tests exercise coverage + row-building offline without the DB. Guard main() so importing the module does not hit the network (mirror the Utah `entry.endsWith(...)` pattern).
    - Tests (scripts/loadVAEnrichment82.test.mjs, node:test, offline): VA_ENRICHMENT covers every EXPECTED_KEY (73); buildRows(EXPECTED_KEYS).missing is empty; buildRows on a synthetic unmapped key reports it missing; no $-figure and no sample VA locality name (e.g. 'Alexandria','Fairfax','Arlington','Richmond','Roanoke','Falls Church') anywhere in the map text; the two 'interest' composites differ; miscellaneous plain_name != 'Information Technology'; every built row has municipality_id===null and source==='ai'.
  </behavior>
  <action>
    1. Copy the loadUtahEnrichment72.mjs skeleton (env load, supabase client, pagination, batched delete-then-insert, $-leak guard, dry-run/--apply, expanded-JSON writer, main()-guard). Import VA_ENRICHMENT + EXPECTED_KEYS from data/vaEnrichment82.mjs.
    2. Replace the Utah cohort + resolver with: VA cohort (state='VA', all entity types) + the live distinct-link_key worklist for operating/revenue depth 0 and 1.
    3. Implement and EXPORT buildRows(liveKeys): map each key via VA_ENRICHMENT, collect missing keys, return { rows, missing }. main() calls it, prints coverage, and ABORTS if missing.length>0.
    4. Add the locality-name guard (fetch VA municipalities.name, lowercase, substring check over row text) and keep the $-leak guard; both abort before any write.
    5. Implement delete-then-insert on --apply over the authored name_keys; keep dry-run write-free. Wire the data/va-enrichment-82.expanded.json writer.
    6. Write scripts/loadVAEnrichment82.test.mjs (node:test) per <behavior>; tests import VA_ENRICHMENT/EXPECTED_KEYS/buildRows only — no network.
  </action>
  <verify>
    <automated>node --check scripts/loadVAEnrichment82.mjs && node --test scripts/loadVAEnrichment82.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/loadVAEnrichment82.mjs` exits 0; `node --test scripts/loadVAEnrichment82.test.mjs` reports all passing, 0 failing.
    - The script imports VA_ENRICHMENT + EXPECTED_KEYS, exports buildRows, and (on --apply) writes via delete-then-insert with `.is('municipality_id', null)` — never upsert. Every built row has `municipality_id: null` and `source: 'ai'`.
    - Test proves: full coverage of EXPECTED_KEYS; a synthetic unmapped key is reported missing; no $-figure and no sample VA locality name in any row text; the two 'interest' composites differ; miscellaneous != 'Information Technology'.
    - Importing the module does not perform DB/network I/O (main() is entry-guarded).
  </acceptance_criteria>
  <done>scripts/loadVAEnrichment82.mjs derives the live VA worklist, enforces 100% map coverage (abort on any unmapped key), runs the $-leak + locality-name guards, and idempotently writes universal rows via delete-then-insert on --apply — proven by offline coverage/bleed tests.</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run — prove 100% coverage, $-leak=0, locality-leak=0, zero writes BEFORE applying</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - scripts/loadVAEnrichment82.mjs (the loader from Task 2)
    - scripts/loadUtahEnrichment72.mjs (reference for expected dry-run output shape)
  </read_first>
  <action>
    Source the gitignored .env / .env.local and run the dry-run (NO --apply — zero writes):
      node scripts/loadVAEnrichment82.mjs
    Confirm the printed report: VA entity count > 0; the operating + revenue depth-0/1 worklists are non-empty; total distinct live keys = 73 (or, if the live count has shifted since recon, the missing-coverage list explains every delta); missing-coverage count = 0; $-leak count = 0; locality-leak count = 0. Inspect data/va-enrichment-82.expanded.json. If coverage shows missing keys (a live key not in the map), ADD those keys to data/vaEnrichment82.mjs (Task 1) and re-run — do NOT add a fallback. If a $-leak or locality-leak fires, fix the offending row in Task 1.
  </action>
  <verify>
    <automated>node scripts/loadVAEnrichment82.mjs</automated>
    <human-check>Dry-run prints non-empty op/rev worklists, missing-coverage=0 (100% of live keys mapped), $-leak=0, locality-leak=0, and zero writes; expanded JSON lists ~73 authored rows.</human-check>
  </verify>
  <acceptance_criteria>
    - Dry-run completes with zero DB writes.
    - Missing-coverage count = 0 (every live VA link_key resolves to a VA_ENRICHMENT row).
    - `$-leak` count = 0 and `locality-leak` count = 0.
    - data/va-enrichment-82.expanded.json is written and lists the authored rows (~73) with their name_keys.
  </acceptance_criteria>
  <done>A live, write-free dry-run proves the full VA worklist maps at 100% coverage with a clean $-leak=0 and locality-leak=0 — the authoring is correct and complete before any production write.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Apply to production + in-app render + bleed-safety + CA/MA no-regression spot-check</name>
  <files>(production write — no repo files modified)</files>
  <read_first>
    - scripts/loadVAEnrichment82.mjs
    - .planning/phases/82-enrichment-parity/82-CONTEXT.md (D-82-03 shared-key policy; SC#1/#2/#3)
  </read_first>
  <action>Checkpoint — the operator runs the real write and verifies render + bleed-safety + no-regression in the live app. No code changes in this task.</action>
  <what-built>
    The --apply run delete-then-inserts universal (municipality_id NULL) enrichment rows for all 73 VA function/activity/source keys into treasury.category_enrichment, overwriting the 7 shared universals (incl. the corrected miscellaneous row). The app's two-tier join now renders plain-language names + descriptions for every VA city, county, and town that shares a key — at $0.
  </what-built>
  <how-to-verify>
    1. Source .env / .env.local and run: `node scripts/loadVAEnrichment82.mjs --apply`. Confirm it reports rows written (~73), missing-coverage=0, $-leak=0, locality-leak=0. Re-run once to confirm idempotency (same row count, no duplicates).
    2. In the live app (treasurytracker.empowered.vote), open one VA CITY (e.g. Alexandria), one VA COUNTY (e.g. Fairfax County), and one VA TOWN: confirm functions (Public Safety, Public Works, Health and Human Services, …) and their drill-down activities show plain-language names + descriptions on both Money Out and Money In; confirm the Virginia state node renders its program-area enrichment.
    3. BLEED CHECK (SC#2): confirm no locality's text appears on another's categories and no locality name / $ figure appears in any enrichment description across the three VA entities. County functions read with locality-neutral framing (not "city department").
    4. NO-REGRESSION CHECK (D-82-03): open one CA city + one MA town; confirm the 7 shared keys (public safety, public works, education, community development, fines and forfeitures, revenue from use of money and property, miscellaneous) still read sensibly, and that `miscellaneous` now reads as a revenue catch-all (NOT "Information Technology").
    5. Confirm $0 spend (no paid API was called).
  </how-to-verify>
  <acceptance_criteria>
    - `--apply` writes the universal rows via delete-then-insert idempotently (re-run = same rows, no duplicate name_keys), with missing-coverage=0, $-leak=0, locality-leak=0.
    - Live spot-check across a VA city + county + town shows plain-language function + activity enrichment with NO cross-entity bleed, no locality names, no $ figures (SC#2, SC#3).
    - CA + MA no-regression check passes; `miscellaneous` reads as revenue, not "Information Technology" (D-82-03).
    - $0 spend confirmed (SC#1).
  </acceptance_criteria>
  <verify>
    <human-check>See how-to-verify: production write clean (coverage=0 missing, $-leak=0, locality-leak=0), VA city/county/town render bleed-safe, CA+MA no-regression passes, $0.</human-check>
  </verify>
  <done>Universal VA enrichment is live: all 73 function/activity/source keys carry bleed-safe, state-neutral plain language, spot-checked across a VA city, county, and town, with the shared keys improved (miscellaneous fixed) and CA/MA confirmed non-regressed, at $0 — VAENR-01 satisfied.</done>
  <resume-signal>Type "approved" once --apply has run clean (coverage=0 missing, $-leak=0, locality-leak=0), the VA city/county/town render + bleed checks pass, and the CA+MA no-regression check passes — or describe what's wrong to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explicit map → public app | Universal (NULL-municipality) enrichment text rendered to every entity sharing a key — across VA AND CA/MA/UT/TX/OR. The ONLY safety against cross-entity/cross-state bleed is that the text is concept-level (function/activity/source) with no entity facts and is state-neutral. |
| local script → Supabase (write) | Idempotent delete-then-insert of universal category_enrichment rows via the service key (gitignored). Single-operator, serial, main-tree. |
| local script → Supabase (read) | Read-only worklist derivation from production budget_categories for VA entities + a read of VA municipality names for the locality-name guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Information Disclosure | Cross-entity/cross-state text bleed via universal rows (the prior NULL-municipality incident, memory project_enrichment_scoping_fix) | mitigate | All text concept-level, no locality names / no $ figures, state-neutral; $-leak guard + locality-name guard ABORT the run; Task 1/2 tests assert no $-figure and no sample VA locality name; Task 4 bleed spot-check across a VA city/county/town + CA/MA no-regression (SC#2, D-82-03). |
| T-82-02 | Tampering | Overwriting working shared universals (CA/MA render) | mitigate | Authored text is a strict improvement (state-neutral; miscellaneous corrected); Task 4 REQUIRES a CA+MA no-regression spot-check before sign-off; delete-then-insert touches only municipality_id IS NULL rows for the 73 authored keys (city-scoped rows are a different conflict key, untouched). |
| T-82-03 | Information Disclosure | Supabase service key in .env | mitigate | Never log/echo the key; .env stays gitignored; no secrets in summaries/commits. |
| T-82-04 | Denial of Service (cost) | Unexpected paid AI API spend | mitigate | No AI API path exists in the loader — authoring is fully inline/static ($0), same as Phase 61/66/72. SC#1 + the $5 cost gate. |
| T-82-05 | Tampering | Wrong name_key → enrichment silently never joins, OR a live key left unmapped | mitigate | name_keys are the live link_keys (verified against budget_categories); the dry-run 100% coverage gate ABORTS on any unmapped live key (no silent fallback); Task 4 live spot-check proves rows render. |
| T-82-06 | Tampering | NULLS DISTINCT → duplicate universal rows on re-run | mitigate | Writes use delete-then-insert (not upsert) over the authored keys; idempotency re-checked in Task 4 (memory category-enrichment-nulls-distinct). |
</threat_model>

<verification>
- `node --check` + `node --test` pass for data/vaEnrichment82.mjs and scripts/loadVAEnrichment82.{mjs,test.mjs}; coverage + bleed/$-leak + interest-disambiguation + miscellaneous-fix assertions green.
- Dry-run (Task 3) derives the live VA worklist, shows 100% coverage (0 missing), $-leak=0, locality-leak=0, zero writes.
- `--apply` (Task 4) writes universal rows idempotently via delete-then-insert; live spot-check across a VA city + county + town shows bleed-safe plain-language enrichment; CA+MA no-regression passes.
- VAENR-01 satisfied: standardized, bleed-safe universal enrichment authored for the full VA function/activity/source vocabulary (73 keys incl. the state node), no locality-name leaks, inline at $0.
</verification>

<success_criteria>
- SC#1 (roadmap): Universal category_enrichment rows authored for the VA function/activity vocabulary, inline at $0 — all 73 live keys covered (100% coverage gate), no paid API, within the $5 gate.
- SC#2 (roadmap): No locality-name leaks / cross-entity bleed — concept-level, state-neutral text; $-leak + locality-name guards; bleed spot-check across a VA city/county/town; CA+MA no-regression.
- SC#3 (roadmap): Enrichment renders in-app for a sample city, county, and town — confirmed live at treasurytracker.empowered.vote (data-only phase; existing API join surfaces the universal rows).
- D-82-01/02: explicit hand-authored map; depth-0 AND depth-1 enriched; 100% coverage by assertion, not fallback.
- D-82-03: shared keys improved (state-neutral; miscellaneous corrected); CA+MA no-regression required.
- D-82-04: 12 Virginia state-node keys included.
- D-82-06: delete-then-insert (NULLS DISTINCT); idempotent.
- D-82-08: two parent-specific "interest" composites carry distinct descriptions.
</success_criteria>

<output>
Create `.planning/phases/82-enrichment-parity/82-01-SUMMARY.md` when done — record the explicit map (73 keys, key-group counts), the loader design (live worklist + 100% coverage gate + $-leak/locality guards + delete-then-insert), the confirmed app join (name_key = link_key; depth-1 = parent|child composite), the dry-run coverage/leak result (Task 3), the live --apply result + VA city/county/town render + bleed-safety + CA/MA no-regression spot-check (Task 4), and confirmation of $0 spend.
</output>
