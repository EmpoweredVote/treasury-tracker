---
phase: 81.5-ev-micro-donation-transparency
plan: 02
subsystem: frontend
tags: [react, tsx, nonprofit, ev-financials, micro-donation, transparency]

# Dependency graph
requires:
  - phase: 81.5-01
    provides: item_count=9 + _evMicro JSON persisted on Donations category

provides:
  - MicroDonationCallout component reads _evMicro from Donations category
  - Callout mounted on EV nonprofit view only (entity_type='nonprofit')

affects: [ev-financials-frontend]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lean carrier consumer: frontend parses _evMicro JSON from category.description; no backend change"
    - "Graceful absent: component renders static headline when aggregates missing/unparseable"
    - "No new fetch: uses already-loaded revenueData to find Donations category"

key-files:
  created:
    - src/components/dashboard/MicroDonationCallout.tsx
  modified:
    - src/App.tsx

key-decisions:
  - "Find Donations by linkKey='donations' OR name.toLowerCase()='donations' — dual-match is robust to future link_key changes"
  - "Render callout always when nonprofit + revenueData present — even on non-revenue dataset tabs (the headline is entity-level context, not tab-specific)"
  - "Graceful absent path renders static headline + invite without fabricated numbers — handles edge where revenueData lacks _evMicro (e.g. older FY snapshots)"

# Metrics
duration: 14min
completed: 2026-06-23
---

# Phase 81.5 Plan 02: EV Micro-Donation Callout (UI Half)

**MicroDonationCallout renders on the EV nonprofit view: locked "free for everyone" headline + honest 9-people/$10-month stat from persisted aggregates + soft "Join them" invite linking to DonateModal; deployed to production; zero PII.**

## Performance

- **Duration:** ~14 min
- **Started:** 2026-06-23T07:13:59Z
- **Completed:** 2026-06-23T07:28:22Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- Built `MicroDonationCallout` component reading `_evMicro` from the Donations budget category's `description` field (lean carrier from Phase 81.5-01)
- Locked headline (verbatim, Chris's words): "These tools are free for everyone, and always will be. We are currently sustained by like-minded people who give a few dollars a month."
- Quiet honest stat: "9 people — typically around $10/month" from persisted aggregates (no fabrication)
- Soft invite: "Join them" button opens existing DonateModal; "$2/month helps — only if you can"
- Graceful absent: if `_evMicro` absent/unparseable or zero supporters, renders static headline only (no fabricated numbers)
- Gated to `entity_type === 'nonprofit'` — invisible on all municipal/county/state/federal pages
- No new fetch — reads from `revenueData.categories` already loaded for the summary panel
- Deployed: live bundle `index-DXgUgJiX.js` at treasurytracker.empowered.vote confirmed to contain all new strings

## Task Commits

1. **Task 1 — MicroDonationCallout component** - `55697f7` (feat)
2. **Task 2 — Mount on EV financials view** - `eef6191` (feat)

## Files Created/Modified

- `src/components/dashboard/MicroDonationCallout.tsx` - New component; parses `_evMicro`; renders locked headline + dynamic stat + soft invite; graceful absent path
- `src/App.tsx` - Import + mount MicroDonationCallout below OrgTransparencyPanel; nonprofit-only gate; searches revenueData categories by linkKey/name for Donations

## Decisions Made

- **Dual-match for Donations category:** `linkKey === 'donations' || name.toLowerCase() === 'donations'` — dual-match is defensive against future link_key changes; also searches subcategories as a fallback
- **Graceful absent = static headline + invite (not null):** The locked headline is always true and never fabricated, so it's safe to show even when aggregates are absent; the dynamic stat and count are gated on `hasAggregates`
- **Place callout in space-y-6 block below OrgTransparencyPanel:** Reads as "how we stay free" context after the goal-progress display, before the dataset tabs — natural narrative flow

## Deviations from Plan

None — plan executed exactly as written. All three tasks completed.

## Known Stubs

None. The callout reads live from `revenueData.categories`, which is loaded from the API on entity+year selection. The `_evMicro` JSON is persisted data from Phase 81.5-01, not a stub.

## Threat Flags

None. No new network endpoints, auth paths, or file access patterns introduced. The callout is a pure read of already-loaded client-side state.

## Self-Check: PASSED

- `src/components/dashboard/MicroDonationCallout.tsx` - FOUND (created)
- `src/App.tsx` - FOUND (modified, +25 lines)
- Commit `55697f7` - FOUND (feat(81.5-02): MicroDonationCallout component)
- Commit `eef6191` - FOUND (feat(81.5-02): mount MicroDonationCallout on EV nonprofit revenue view)
- `npm run build` - PASS (zero TypeScript errors, zero new warnings)
- Live bundle `index-DXgUgJiX.js` contains: "These tools are free for everyone" - FOUND
- Live bundle contains: "recurring_supporters", "Join them", "only if you can" - FOUND
- PII check in live bundle: 0 donor PII strings - PASS

## Awaiting Human Sign-Off

See `81.5-VERIFICATION.md` for the human UAT checklist. Chris needs to:
1. Visit https://treasurytracker.empowered.vote/?entity=empowered-vote-ca&year=2026&dataset=revenue
2. Confirm headline, stat, and invite render correctly
3. Confirm callout absent on non-EV entities
4. Give final copy/UX judgment

---
*Phase: 81.5-ev-micro-donation-transparency*
*Completed: 2026-06-23*
