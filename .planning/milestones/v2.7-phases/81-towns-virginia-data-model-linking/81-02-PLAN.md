---
phase: 81-towns-virginia-data-model-linking
plan: 02
type: execute
wave: 2
depends_on: [81-01]
files_modified:
  - data/vaTownCounties.json
  - scripts/seedVirginiaDataModel.js
requirements: [VALINK-01]
must_haves:
  truths:
    - "A Virginia state node exists: a municipalities row name='Virginia', state='VA', entity_type='state', population from the latest Census VA total (sourced), with NO budget datasets — a navigation hub only (CONTEXT D-08). The APA source has no VA state-level budget."
    - "An authored, committed, sourced town→county map data/vaTownCounties.json covers all 37 towns; every value resolves to an existing VA county municipality ('<name> County'); _meta records the official source URL + retrieval date; towns spanning multiple counties use the seat county with a note (CONTEXT D-06)"
    - "Each town municipality row has county_id set to its parent county municipality's id (CONTEXT D-07). Independent cities keep county_id NULL (standalone — VA cities are not inside counties); counties keep county_id NULL"
    - "Seeding is idempotent: re-running creates no duplicate Virginia node, sets county_id only when different, and changes nothing on a second run"
  artifacts:
    - path: "data/vaTownCounties.json"
      provides: "Authored + sourced town→county lookup for all 37 VA towns, with _meta source attribution"
      contains: "_meta"
    - path: "scripts/seedVirginiaDataModel.js"
      provides: "Idempotent seeder: Virginia state node + town county_id linking from vaTownCounties.json"
      contains: "entity_type"
  key_links:
    - from: "data/vaTownCounties.json (town → '<name> County')"
      to: "town municipalities.county_id → parent county municipality id"
      via: "seedVirginiaDataModel.js resolves each county row by name+state and sets the town's county_id idempotently"
      pattern: "seedVirginiaDataModel"
    - from: "Census VA state total"
      to: "a Virginia state navigation node (entity_type='state', no datasets)"
      via: "treasury_ensure_municipality name='Virginia' state='VA' entity_type='state'"
      pattern: "seedVirginiaDataModel"
---

<objective>
Put the Virginia **data model** in place (the non-UI half of VALINK-01): seed a **Virginia state navigation node** (no budget data) and **link every town to its parent county** via `county_id`, using an authored + sourced `data/vaTownCounties.json` map (the APA report carries no town→county column). Independent cities and counties stay standalone. Everything is idempotent. The frontend that consumes this model (picker filter, hub page, county-page towns, breadcrumb) is plan 81-03.
</objective>

<task type="execute">
<name>Author the sourced town→county map</name>
<read_first>
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-06 — sourcing rule + the 37-town list in code_context; primary/seat county for split towns)
- scripts/loadVAComparativeReportBatch.js (enumerateRoster — the authoritative 37-town names, and the county display-name convention "<name> County")
- C:/Users/Chris/.claude/projects/C--treasury-tracker/memory/feedback_no_research_subagents.md (inline-authored-data-file + fetched-official-source rule — model memory is not a source)
</read_first>
<action>
Create `data/vaTownCounties.json`: a top-level `_meta` object (`source`: the official reference URL used — e.g. U.S. Census Bureau place→county relationship data or a Commonwealth of Virginia / Census incorporated-places list; `retrieved`: date; `note`: split-town handling) and a `towns` map keyed by the exact bare town name from `enumerateRoster().towns` (all 37) → the parent county's **display name** ("<name> County") so it joins directly to the seeded county municipalities. Resolve each town's county from the fetched official source (NOT from memory — honor the sourcing rule). For a town spanning more than one county, use its seat/primary county and add a per-entry note. Verify every county value matches a real VA county the Phase 80 load created (95 "<name> County" rows).
</action>
<acceptance_criteria>
- data/vaTownCounties.json parses as JSON and has a `towns` object with exactly 37 keys matching enumerateRoster().towns
- Every `towns` value is of the form "<name> County" and corresponds to a county loaded in Phase 80
- `_meta.source` is a real official URL and `_meta.retrieved` is set
</acceptance_criteria>
</task>

<task type="execute">
<name>Idempotent seeder: Virginia state node + town county_id linking</name>
<read_first>
- scripts/loadVAComparativeReport.js (getSupabase, treasury_ensure_municipality usage in importLocality ~417)
- scripts/seedMACountyLinks.js (the Phase 25/MA county_id linking precedent — set-if-different idempotency)
- data/vaTownCounties.json (the map authored above)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-07, D-08)
</read_first>
<action>
Create `scripts/seedVirginiaDataModel.js` (ESM, mirrors the getSupabase + treasury_ensure_municipality pattern). It (1) ensures the **Virginia state node** via `treasury_ensure_municipality` with `p_name='Virginia'`, `p_state='VA'`, `p_entity_type='state'`, `p_population` = the latest Census Virginia total (hard-code the sourced value + cite it in a comment) — no datasets; (2) reads `data/vaTownCounties.json`, looks up each county municipality by `name='<name> County'` + `state='VA'` + `entity_type='county'`, looks up each town by bare `name` + `state='VA'` + `entity_type='town'`, and sets the town's `county_id` to the county's id **only when it differs** (idempotent UPDATE on treasury.municipalities). Skip + warn (do not abort) if a town isn't loaded yet or its county is missing; print a summary (state node id, towns linked, towns skipped). Support `--dry-run` (resolve + report, no writes). Independent cities and counties are left untouched (county_id stays NULL).
</action>
<acceptance_criteria>
- `node scripts/seedVirginiaDataModel.js --dry-run` resolves the Virginia node + maps all linkable towns to a county id, reporting any skips, with zero writes
- After a live run: a `municipalities` row name='Virginia', state='VA', entity_type='state' exists (exactly one); querying it returns no budget datasets
- After a live run: each loaded town's `county_id` equals its parent county's id per vaTownCounties.json; sampled independent cities (e.g. Alexandria) and counties have county_id NULL
- Re-running the seeder writes nothing new (no duplicate Virginia node; county_id unchanged) — idempotent
</acceptance_criteria>
</task>

<verification>
- `node scripts/seedVirginiaDataModel.js --dry-run` → Virginia node resolved + all towns mapped, 0 writes
- Live: one Virginia state node (no datasets); towns linked to parent county; cities/counties county_id NULL; idempotent re-run
- data/vaTownCounties.json: 37 sourced entries, all counties real
</verification>
