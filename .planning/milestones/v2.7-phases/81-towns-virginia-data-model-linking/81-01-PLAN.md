---
phase: 81-towns-virginia-data-model-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/loadVAComparativeReportBatch.js
  - scripts/loadVAComparativeReport.js
  - scripts/loadVAComparativeReport.test.mjs
autonomous: true
requirements: [VALOAD-03]
must_haves:
  truths:
    - "The batch driver loads all 37 reporting towns (§5) when run with --entity-type town: each town is loaded bare (displayName = matchName), entity_type='town', sectionIndex=2, reusing the proven importLocality write path + never-overwrite guard + per-FY sourcing — NO new parse/RPC logic (CONTEXT D-01/D-02)"
    - "Town per-capita works: town population is read from Exhibit A (header row 7, name col 4, population col 2 'Population (Note 1-B)'), section-scoped to §2, because Exhibit H has NO town section — cities/counties keep the Exhibit H path unchanged (CONTEXT D-03)"
    - "Towns are loaded for FY2023 and FY2024-amended, every row carrying data_source='Virginia APA Comparative Report' + the per-FY source_url from vaApaDatasets.json + source_date; re-running writes the same trees/totals and changes nothing (idempotent never-overwrite, CONTEXT D-04)"
    - "An absent town (zero op+rev in the report) skips cleanly — no $0 budget, no phantom municipality — and is recorded, mirroring the Phase 80 absent-locality handling (CONTEXT D-05)"
    - "Bare town display names are safe: no town collides with an independent city, and the 6 town/county name overlaps (Bedford, Culpeper, Orange, Pulaski, Tazewell, Wise) do not collide because counties carry the 'County' suffix"
  artifacts:
    - path: "scripts/loadVAComparativeReportBatch.js"
      provides: "Batch driver extended with a town branch in the work-list builder (--entity-type town)"
      contains: "town"
    - path: "scripts/loadVAComparativeReport.js"
      provides: "localityPopulation extended (or a townPopulationFromExhibitA helper) so towns get population from Exhibit A"
      contains: "Exhibit A"
    - path: "scripts/loadVAComparativeReport.test.mjs"
      provides: "Offline tests for town segmentation count, town population from Exhibit A, and bare-name safety"
      contains: "town"
  key_links:
    - from: "VA APA Comparative Report XLSX §5 towns + Exhibit A population"
      to: "37 town municipalities with operating+revenue trees + per-capita, FY2023 + FY2024"
      via: "enumerateRoster().towns → work-list town branch → importLocality (section 2) + Exhibit A population fallback"
      pattern: "loadVAComparativeReportBatch --entity-type town"
---

<objective>
Load all **37 reporting Virginia towns** (§5 of the APA Comparative Report) with operating (expenditure function→activity) + revenue (source→sub-source) + per-capita, for FY2023 and FY2024-amended, idempotently and fully sourced — completing the data half of the phase (VALOAD-03).

The work is deliberately tiny because Phases 79–80 already built everything: `enumerateRoster()` already returns the town list, `ENTITY_TYPE_SECTION.town === 2`, section-scoped homonym-safe lookup, the never-overwrite guard, per-FY sourcing, and the absent-skip all exist. The only **new** logic is (1) a `town` branch in the batch work-list builder and (2) a **town population fallback to Exhibit A**, because Exhibit H has no town section. Prove the whole town roster parses via `--dry-run` + offline tests, then live-load both FYs.
</objective>

<task type="execute">
<name>Add a town branch to the batch work-list builder</name>
<read_first>
- scripts/loadVAComparativeReportBatch.js (the work-list builder lines ~100–116 and the roster log line 115; enumerateRoster already returns roster.towns)
- scripts/loadVAComparativeReport.js (importLocality signature + ENTITY_TYPE_SECTION; confirm town:2)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-01, D-02)
</read_first>
<action>
In `loadVAComparativeReportBatch` (scripts/loadVAComparativeReportBatch.js), after the existing `if (entityTypes.includes('county'))` block, add an `if (entityTypes.includes('town'))` block that pushes one work item per `roster.towns` name: `{ matchName: name, displayName: name, entityType: 'town', sectionIndex: 2 }` — BARE display name (CONTEXT D-02: no town↔city collisions; counties carry the "County" suffix so the 6 town/county overlaps don't collide). Update the roster log line (currently "…towns (towns out of scope — Phase 81)") to drop the out-of-scope note and report the town count. Leave the CLI `--entity-type` default as `['city','county']` so towns are opt-in via `--entity-type town`. Do NOT change the city/county branches.
</action>
<acceptance_criteria>
- scripts/loadVAComparativeReportBatch.js contains `entityTypes.includes('town')` and pushes items with `entityType: 'town'` and `sectionIndex: 2`
- The roster log line no longer says "towns out of scope"
- `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2024-comparative-report.xlsx --fy 2024 --entity-type town --dry-run` prints 37 town work items and exits 0 with zero writes
</acceptance_criteria>
</task>

<task type="execute">
<name>Add a town population fallback to Exhibit A</name>
<read_first>
- scripts/loadVAComparativeReport.js (localityPopulation ~306–323; headerCells, findHeaderRow, findLocalityRowInSection, cellNum)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-03 — Exhibit H has no town section; Exhibit A header row 7, name col 4, population col 2 "Population (Note 1-B)")
</read_first>
<action>
Make town population resolve from **Exhibit A** since Exhibit H has no town rows. Smallest change: in `localityPopulation`, when the Exhibit H lookup yields null (locality absent from Exhibit H) fall back to Exhibit A — find the header row containing "No.", locate the locality row in the given `sectionIndex` matching on the **Exhibit A name column (col 4, "Locality / City of:")** rather than col 2, and read the **"Population (Note 1-B)" column (col 2)** via `cellNum`. Because the existing section-scoped lookup assumes the name in col 2, implement the Exhibit A scan inline (or in a small `townPopulationFromExhibitA(workbook, name, sectionIndex)` helper) that segments by the col-1 "No."-reset and matches the name in col 4. Cities and counties must keep returning their Exhibit H population unchanged (the fallback only fires when Exhibit H returns null). Return null (not throw) if the town is absent from Exhibit A too.
</action>
<acceptance_criteria>
- `localityPopulation(wb, '<town>', 2)` returns a finite population for a town present in Exhibit A (e.g. Blacksburg, Leesburg) where the unmodified Exhibit-H path returned null
- `localityPopulation(wb, 'Alexandria', 0)` and a county at section 1 still return their Exhibit H values (unchanged)
- A town genuinely absent from both exhibits returns null without throwing
</acceptance_criteria>
</task>

<task type="execute">
<name>Extend offline tests for towns</name>
<read_first>
- scripts/loadVAComparativeReport.test.mjs (existing 12 tests — segmentation, homonym, county naming patterns)
- scripts/loadVAComparativeReportBatch.js (enumerateRoster)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (specifics — per-capita-for-towns proof)
</read_first>
<action>
Add offline tests (using the recon workbook `_va-recon/fy2024-comparative-report.xlsx`, matching how the existing tests load it): (1) `enumerateRoster().towns.length === 37` and includes "Abingdon" and "Wytheville", excludes "Total"/"Grand Total"; (2) town population fallback — `localityPopulation(wb, 'Blacksburg', 2)` (or another town confirmed in Exhibit A) is a finite number > 0; (3) bare-name safety — assert no town name equals any city name from `enumerateRoster().cities` (the collision check), and that a town/county overlap name (e.g. "Orange") resolves to a town expenditure tree at section 2 distinct from "Orange County" at section 1. Keep all prior tests passing.
</action>
<acceptance_criteria>
- `node --test scripts/loadVAComparativeReport.test.mjs` exits 0 with all tests passing (prior 12 + the new town tests)
- The town-population test asserts a finite > 0 value (proves D-03)
- The bare-name test asserts zero town↔city collisions
</acceptance_criteria>
</task>

<task type="execute">
<name>Live-load towns for FY2023 + FY2024-amended and verify</name>
<read_first>
- scripts/vaApaDatasets.json (per-FY xlsxUrl + the amended FY2024 entry)
- scripts/loadVAComparativeReportBatch.js (CLI; --entity-type town, --fy, --file)
- .planning/phases/80-city-county-loads/80-VERIFICATION.md (the FY2024-amended choice + absent-locality handling precedent)
- .planning/phases/81-towns-virginia-data-model-linking/81-CONTEXT.md (D-04, D-05)
</read_first>
<action>
With the gitignored `.env` SUPABASE_SERVICE_KEY sourced, run the batch driver `--entity-type town` once per FY against the same XLSX files Phase 80 used: FY2024 against the **amended** workbook (the report Phase 80 adopted) and FY2023 against its workbook, each with the matching `--source-url` from vaApaDatasets.json. Run live (no `--dry-run`). Then verify via live probes: town municipalities exist with `entity_type='town'`, operating + revenue rows for both FYs, `data_source='Virginia APA Comparative Report'` with non-null `source_url` on every town row, at least one town with non-null population/per-capita, and a re-run that writes nothing new (idempotent). Record the loaded town count and any absent towns (zero-data late-filers) the way Phase 80 recorded its gaps. Stay SERIAL on the main tree; if the SCO/Supabase write flakes, retry per-FY.
</action>
<acceptance_criteria>
- Live `treasury.budgets`: ~37 towns × {operating, revenue} for FY2024 (+ FY2023 where the town filed), 0 NULL/empty source_url on town rows, uniform data_source
- At least one town has a non-null population (per-capita renders) — proves the Exhibit A fallback works in the live load
- A second run of the same commands reports skipped(never-overwrite) / changes the row + municipality counts by 0 (idempotent)
- One town's FY2024 operating total is cross-read against the published report and matches; any absent towns are listed
</acceptance_criteria>
</task>

<verification>
- `node --test scripts/loadVAComparativeReport.test.mjs` → all pass (prior + new town tests)
- `node scripts/loadVAComparativeReportBatch.js --file _va-recon/fy2024-comparative-report.xlsx --fy 2024 --entity-type town --dry-run` → 37 towns parsed, 0 writes
- Live probe: town rows present for both FYs, every row sourced, ≥1 town with population, idempotent re-run
</verification>
