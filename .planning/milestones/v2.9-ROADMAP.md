# v2.9 Minnesota Local Government Expansion — Roadmap (Phases 89–93)

**Shipped:** 2026-06-28  ·  **Status:** ✅ Complete (MNVER-01/02 verified, Chris UAT all-pass)

**Milestone goal:** Bring every Minnesota city + county government onto Treasury Tracker at parity from the single uniform Minnesota Office of the State Auditor "City/County Finances Report" raw XLSX (`cired_YY_data.xlsx` / `county_YY_data.xlsx`, free, no auth) — two-level revenue-by-source + expenditure-by-function trees (real icicle drill-down, resolving the Ohio flat-source limitation), per-capita, every figure sourced to osa.state.mn.us. The 5 ranked-choice-voting cities (Minneapolis, St. Paul, St. Louis Park, Bloomington, Minnetonka) were the mission-aligned verification anchors.

**Constraints:** One uniform free source (osa.state.mn.us, no auth, $0); general-government scope (`Governmental Funds` sheet — enterprise funds deferred); GAAP/Cash basis per-entity via `GAAPInd`; no salaries (`Employee Data` sheet deferred); XLSX-era FY range; city→county linking from the built-in `ParentEntityName` column; RCV = selection rationale + verification anchor (no new UI); every figure durably sourced.

**Critical path:** 89 → 90 → 91 → 92 → 93.

## Result (cohort)
**858 cities** (20,414 budget rows, ~FY2012–2023) + **87 counties** (1,380 rows, ~FY2013–2021) + **Minnesota state node** + **136 universal enrichment rows**. Full cohort 0-NULL source across all entity types. 2-level icicle drill-down renders cohort-wide (100% depth-1, 99.8% depth-2).

## Phases

### Phase 89: MN OSA Source + Loader (MNSRC-01, MNSRC-02) — ✅
Reusable `loadMNOSA.js` builds 2-level revenue-by-source + expenditure-by-function trees from the OSA `Governmental Funds` sheet for both city + county layouts (county layout verified independently — the Ohio county-layout lesson), per-entity GAAP/Cash basis via `GAAPInd`, per-FY source manifest (`mnOsaDatasets.json`), idempotent never-overwrite guard, offline tests.

### Phase 90: City Loads (MNCITY-01, MNCITY-02) — ✅
All ~858 MN cities loaded operating + revenue across the XLSX-era FY range; per-capita from the built-in `Population` column; GAAP/Cash basis recorded (`mnCityBasis.json`); cross-FY source-gap residual documented (`mnCityResidual.json`, no phantom municipalities); idempotent.

### Phase 91: County Loads + Data Model & Linking (MNCO-01, MNLINK-01) — ✅
All 87 county governments loaded operating + revenue, per-capita, sourced; Minnesota state node established; cities linked to parent county via the source `ParentEntityName` column (`linkMNCitiesToCounties.js`); US→Minnesota→county→city breadcrumb + Cities-in-County panel render.

### Phase 92: Enrichment Parity (MNENR-01) — ✅
115 hand-authored, state-neutral, bleed-safe last-segment CONCEPTS covering all 136 live composite keys; loader (`loadMNEnrichment92.mjs`) with 100% coverage gate (abort, no fallback) + $-leak + locality-name guards + delete-then-insert (NULLS-DISTINCT-safe); $0 inline-authored; 16 offline tests.

### Phase 93: Verification + Source-Chain Audit + UAT (MNVER-01, MNVER-02) — ✅
ACFR reconciliation (Hennepin County FY2021 exact within 0.07%; Minneapolis FY2023 +15% explained — OSA city entity includes the Park & Recreation Board); full-cohort source-chain audit clean; independent workbook re-derivation of 5 entities (exact, 0 mismatches); 2-level icicle drill-down confirmed; **state-node placeholder estimates replaced with real State-of-MN ACFR GAAP actuals (FY2023–2025)**; live-app UAT 17/17 pass, Chris sign-off 2026-06-27.

## Key decisions / lessons
- MN OSA is the official state compilation of each entity's annual financial report → stored figures track published ACFRs closely (counties near-exact); city deltas explained by OSA reporting-entity scope (component units like the Minneapolis Park Board) + functional reclassification.
- The 2-level OSA trees resolve the Ohio AOS flat-source icicle limitation.
- **Surfaced:** the all-50-states seed loaded unsourced "best guess" estimate data for state General Fund nodes (47 states unsourced; OH + VA falsely stamped with a source) → chartered as new milestone **v2.10 State General Fund Sourcing**.

## Deferred (v2.9)
Salaries (`Employee Data`), enterprise funds (`Enterprise Funds`), pre-2015 CSV/ZIP history, townships/special districts (MNSAL-01/MNENT-01/MNHIST-01/MNTWN-01); MN state-node history FY2021/2022 + deeper (→ v2.10).
