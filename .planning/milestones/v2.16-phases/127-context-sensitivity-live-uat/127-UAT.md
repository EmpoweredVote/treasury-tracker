# Phase 127 — Live-App UAT Checklist (TETH-03 / VER-01)

**Purpose:** the visual + behavioral acceptance contract for the Essentials tether icon.
This is the human gate — Chris walks each row in the live app, marks the Result, and signs off.
The phase closes only when every Result is ✅ and Chris has signed (VER-01).

**Authored:** 2026-07-08 (Plan 127-01, grounded against live `coverage.json` + `treasury.municipalities`)
**Executes in:** Plan 127-02

---

## How to run

1. **Server pre-check (deterministic):** `npm run smoke:essentials` → must exit 0 (proves the
   catalog is reachable, CORS-open, correctly shaped, anchors present, Fresno absent).
2. **Live surface (pick one):**
   - Preferred — the deployed production app: **https://treasurytracker.empowered.vote** (the icon
     shipped live in Phase 126).
   - Fallback — local production build: `npm run build && npm run preview`, then open the served URL.
3. Open **DevTools → Network** (filter `coverage.json`) and **Console** before navigating entities.
4. For each row, select the entity in TT and observe the hero-banner bottom-right icon row.
   - Icon present? Hover to confirm the "Essentials" tooltip; check the `<a href>` (inspect element
     or click through) matches the **Expected destination** exactly.
   - Icon absent? Confirm the hero `data-essentials-coverage` seam and that no icon renders.

**Session-verified correction (carry from 127-01):** the CONTEXT.md uncovered-city candidate
**Plano TX is FALSIFIED — it IS covered.** The verified uncovered-city negative case is **Fresno CA**
(present in TT as `entity_type='city'`, absent from the 136 covered cities). Backup: Bakersfield CA.

Live catalog this session: cities **136**, counties **16**, states **50**, federal **present**;
`HTTP 200` + `access-control-allow-origin: *`.

---

## Entity matrix

Expected hrefs are exactly what `buildEssentialsHref` produces (`/results` path, `URLSearchParams`
encoding — spaces → `+`, param order as shown).

| # | Entity | Tier | Coverage | Icon expected? | Expected Essentials destination (exact href) | Result |
|---|--------|------|----------|----------------|----------------------------------------------|--------|
| 1 | **Long Beach, CA** | city | covered (geoid `0643000`) | ✅ ICON | `https://essentials.empowered.vote/results?browse_government_list=0643000&browse_state=CA&browse_label=Long+Beach` | ✅ A |
| 2 | **Los Angeles County, CA** | county | covered (geoid `06037`) | ✅ ICON | `https://essentials.empowered.vote/results?browse_government_list=06037&browse_state=CA&browse_label=Los+Angeles+County` | ✅ A |
| 3 | **Salt Lake County, UT** | county | covered (geoid `49035`) — UT county-label + state-scoped disambiguation | ✅ ICON | `https://essentials.empowered.vote/results?browse_government_list=49035&browse_state=UT&browse_label=Salt+Lake+County` | ✅ A |
| 4 | **California** | state | covered (all 50 states) | ✅ ICON | `https://essentials.empowered.vote/results?browse_state_officials=CA&browse_label=California` | ✅ A |
| 5 | **United States** | federal | covered — **headline reversal: federal now SHOWS the icon** | ✅ ICON | `https://essentials.empowered.vote/results?browse_federal_officials=1&browse_label=United+States` | ✅ A |
| 6 | **Fresno, CA** | city | **UNCOVERED** (negative case, D-127-02) — city absent though its *state* is covered | ❌ NO icon | — (no link; icon must not render) | ✅ A |
| 7 | **Bloomington, IN** | city | covered but `geoids: []` (D-127-01) | ❌ NO icon | — **covered-but-no-icon is CORRECT, not a gate failure** (no per-location link without a GEOID) | ✅ A |

**Result legend:** `✅ A` = verified in **Part A** (executed the real resolver modules
`matchEntityToCoverage`→`resolveFeatureIcons`→`buildEssentialsHref` against the **live**
`coverage.json` and the **real TT DB entity records**, 2026-07-08 — 7/7 rows produced the exact
expected icon-or-absence + href; Bloomington confirmed *covered-with-empty-geoids* → no icon,
distinct from Fresno *uncovered* → no icon). `☐ B` = pending Chris's in-browser observation.
Rows 1–7 are logic/data-proven; the browser **click-through + render** overlaps the visual
checks below and is Chris's final confirmation.

> Row 5 is the headline behavior change of v2.16 — call it out explicitly in sign-off.
> Row 7 is the accepted edge case: a covered place with no GEOID intentionally shows no icon
> (no label-only fallback this phase — D-127-01). Confirm it as CORRECT, not a defect.

---

## Live-behavior checks (D-127-04 — the dimensions fixtures can't prove)

| # | Behavior | How to verify | Result |
|---|----------|---------------|--------|
| a | Real cross-origin fetch **succeeds** | DevTools Network shows a `200` `coverage.json` from `essentials.empowered.vote`; response has `access-control-allow-origin: *` | ✅ A — `npm run smoke:essentials` + Part A harness both hit live: `200` + `access-control-allow-origin: *`. Browser-side confirm optional. |
| b | **Graceful degrade** when Essentials is unreachable (COV-02) | Block the Essentials origin (DevTools request-block) or go offline, reload an entity → hero banner still paints and simply shows **no icon**; no thrown error | ✅ B — Chris confirmed (2026-07-08) |
| c | **No new console errors** | Console stays clean across all 7 entities (no new errors/warnings attributable to the tether) | ✅ B — Chris confirmed (2026-07-08) |
| d | **~One fetch per session** (in-memory cache) | Switching among several entities issues a **single** `coverage.json` request total, not one per entity | ✅ A+B — mechanism (Part A) + browser Network count confirmed by Chris (2026-07-08) |

---

## Visual checks

| # | Check | Result |
|---|-------|--------|
| v1 | The `-light` Essentials symbol on the navy chip is legible over banner art in **light** theme (D-126-03) | ✅ B — Chris confirmed (2026-07-08) |
| v2 | Same, in **dark** theme (D-126-03) | ✅ B — Chris confirmed (2026-07-08) |
| v3 | The icon row **clears the `<h1>` title** at narrow viewport widths where the title wraps (carryover from 126-VERIFICATION) | ✅ B — Chris confirmed (2026-07-08) |

---

## Defects found / fixed (filled by Plan 127-02)

_None recorded yet. For each defect: entity/row, expected vs actual, repro note, and the
resolving `fix(127): …` commit. Verify-and-fix stays in this phase (D-127-03)._

| Defect | Row | Expected vs actual | Fix commit |
|--------|-----|--------------------|-----------|
| _(none yet)_ | | | |

---

## Sign-off

- Server pre-check `npm run smoke:essentials`: **exit 0** confirmed **2026-07-08** ✅
- Full matrix (rows 1–7): **✅** Part A (real resolver × live catalog × real TT entities, 7/7) + browser render confirmed by Chris
- Live-behavior checks (a–d): **✅** — (a) ✅ Part A, (b) ✅ Chris, (c) ✅ Chris, (d) ✅ Part A + Chris
- Visual checks (v1–v3): **✅** — Chris confirmed light/dark legibility + narrow-viewport title clearance
- Defects found: **none** (all 7 rows + fetch/CORS/cache matched expectations exactly; no fix loop needed)

**Chris live-app UAT sign-off (VER-01):** ✅ **APPROVED — "all good, sign it off"** (date: 2026-07-08)

_Part A (headless, executable) verification was performed by Claude on 2026-07-08. Part B
(browser-only: graceful-degrade UX, console, light/dark legibility, narrow-viewport clearance,
one-fetch Network count, click-through) was observed and confirmed by Chris in the live app at
treasurytracker.empowered.vote on 2026-07-08, who then gave the VER-01 sign-off above._
