---
context: default
status: in_progress
last_updated: 2026-06-07
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Workflow StructuredOutput failure — background workflow agents repeatedly failed with "subagent completed without calling StructuredOutput" even with tiny schemas and short prompts. **Do NOT use the Workflow tool or Agent tool for this task.** Do all work directly in the main conversation.
- [ ] CONSTRAINT: API cost — two failed workflow runs cost ~$5-15 in subagent tokens. User is an unfunded nonprofit. All remaining work must be done in the main conversation, not via spawned agents.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Workflow/Agent for state budget loading | Agents time out or exhaust context before calling StructuredOutput; kills entire workflow; high token cost | blocking | Do all work in main conversation with Write + Bash tools directly |
| Parentheses for negative numbers in JS | `(130_000_000)` is positive in JS — accounting notation doesn't work. MBT credit items in processMIRevenue.js had this bug | advisory | Use `-130_000_000` (minus sign) for all negative amounts |
| Hardcoding budget numbers from memory without confidence label | LLM knowledge can be wrong; risks all-funds vs general-fund confusion | advisory | Mark all estimated data with `confidence: 'estimated'`; mark confirmed data with `confidence: 'confirmed'` |

<current_state>
Building state-level General Fund budget data for all 50 US states in Treasury Tracker.

**DB state:**
- All 50 states have municipality rows (entity_type='state') and data_source stubs in the DB
- 12 states now have revenue + operating data loaded:
  TX, NY, FL, PA, IL, OH, GA, NC, MI, NJ, VA

**Scripts written and loaded this session:**
- processMI.js + processMIRevenue.js (fixed negative MBT values + FY2025 Business Taxes total)
- processNJ.js + processNJRevenue.js
- processVA.js + processVARevenue.js

**Compact script format established** — use single-line boilerplate pattern (see processNJ.js as template). ~60% smaller than full-length TX/CA scripts but equally correct.

**NOT yet done:** 37 states have no process scripts at all.
</current_state>

<completed_work>
This session (resumed from prior checkpoint):
- Seeded Michigan via seedMIState.js (was missing from DB despite bulk seeder)
- Fixed processMIRevenue.js: changed `(N)` to `-N` for 4 negative MBT values across FY2023-2026; removed FY2025 "Other Business Taxes" placeholder ($500M inflated)
- Wrote processMI.js (operating budget, all years estimated)
- Loaded TX, NY, FL, PA, IL, OH, GA, NC (revenue + operating — scripts were pre-written)
- Loaded MI revenue + operating
- Wrote + loaded NJ revenue + operating
- Wrote + loaded VA revenue + operating

Prior session:
- Designed and iterated on state budget workflow (3 attempts, all failed on StructuredOutput)
- Workflow agents wrote scripts for 9 states: TX, NY, FL, PA, IL, OH, GA, NC, MI (revenue only)
- Wrote seedRemainingStates.js and ran it — all 40 remaining states seeded in DB
</completed_work>

<remaining_work>
**37 states remaining, in population order:**
WA, AZ, MA, TN, IN, MO, MD, WI, CO, MN, SC, AL, LA, KY, OR, OK, CT, UT, IA, NV, AR, MS, KS, NM, NE, ID, WV, HI, NH, ME, RI, MT, DE, SD, ND, AK, VT, WY

For each state: write processXXRevenue.js + processXX.js, then run both scripts.

**Key structural quirks by state:**
- WA: NO income tax — B&O gross receipts tax + sales tax dominant
- WY, SD, NV, TN (until 2021), FL: no income tax
- MT, OR, NH, DE, AK: no sales tax
- AZ: income tax rate cut to flat 2.5% starting FY2024 — revenue declining
- MA: Millionaires Tax surtax (4% on income >$1M) starts FY2024/FY2025
- IL: pensions are massive (~20% of GF spending)
- TX, VA: biennial budgets (split evenly by year)
- KY, IN: relatively flat budgets with modest growth
- OR, ME, NH: no sales tax — rely on income tax
</remaining_work>

<decisions_made>
- FY range: FY2022–2026 for all states (matching California pattern)
- Data strategy: Web-research + hardcode — mark `confidence: 'estimated'` or `'confirmed'`
- Script pattern: Each state gets processXXRevenue.js + processXX.js (seed already done for all 50)
- Do NOT use Workflow tool or Agent tool — work directly in main conversation
- Compact script format: single-line boilerplate, data-heavy — use processNJ.js as template
- States with no income tax: omit income tax category
- States with no sales tax (OR, MT, NH, DE, AK): omit sales tax category
- Michigan operating budget: all years 'estimated' — SFA State Budget Overview Dec 2025 is source
</decisions_made>

<blockers>
- None currently
</blockers>

## Required Reading (in order)
1. `scripts/processNJ.js` — the compact script template to use for all remaining states
2. `scripts/processCARevenue.js` — full-length revenue template (reference only; use NJ compact format)
3. This file — full context

## Infrastructure State
- DB: All 50 states have municipality rows + data_source stubs
- Loaded: TX, NY, FL, PA, IL, OH, GA, NC, MI, NJ, VA (revenue + operating)
- Env: .env exists at repo root (not .env.local) — scripts load from ../.env correctly
- Script pattern: compact format (see processNJ.js), `confidence: 'estimated'` label, same RPC call pattern

<next_action>
1. Write processWARevenue.js — WA has NO income tax; revenue = Sales Tax (~42%) + B&O Tax (~27%) + Property Tax (~10%) + Other (~21%). Totals ~$27-33B FY2022-2026.
2. Write processWA.js — WA spending categories: K-12 Education, Higher Ed, Health/HHS, Transportation (GF portion), Corrections, General Government.
3. Run both: node scripts/processWARevenue.js && node scripts/processWA.js
4. Continue: AZ, MA, TN, IN, MO, MD, WI, CO, MN... in population order
</next_action>
