---
context: default
status: in_progress
last_updated: 2026-06-07
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: Workflow StructuredOutput failure — background workflow agents repeatedly failed with "subagent completed without calling StructuredOutput" even with tiny schemas and short prompts. **Do NOT use the Workflow tool for this task.** Do all work directly in the main conversation.
- [ ] CONSTRAINT: API cost — two failed workflow runs cost ~$5-15 in subagent tokens. User is an unfunded nonprofit. All remaining work must be done in the main conversation, not via spawned agents.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Workflow agents for state budget loading | Agents time out or exhaust context before calling StructuredOutput; kills entire workflow | blocking | Do all work in main conversation with WebSearch + Write + Bash tools directly |
| Hardcoding budget numbers from memory | LLM knowledge of subcategory breakdowns is unreliable; risks all-funds vs general-fund confusion | advisory | Do one targeted WebSearch per state to verify numbers from official .gov source |

<current_state>
Building state-level General Fund budget data for all 50 US states in Treasury Tracker.

**DB state (all seeded):**
- All 50 states now have municipality rows (entity_type='state') and data_source stubs in the DB
- seedRemainingStates.js ran successfully — 40 states registered this session

**Scripts written (by workflow agents, need verification before running):**
- Seed scripts: TX, NY, FL, PA, IL, OH, GA, NC, MI (+ CA pre-existing)
- Process scripts (operating): TX, NY, FL, PA, IL, OH, GA, NC (MI missing processMI.js)
- Revenue scripts: TX, NY, FL, PA, IL, OH, GA, NC, MI

**NOT yet loaded to DB:** None of the workflow-written process scripts have been run yet.
The workflow agents wrote the scripts but failed before executing them.

**Remaining work:** 39 states have no process scripts at all (NJ, VA, WA, AZ, MA, TN, IN, MO, MD, WI, CO, MN, SC, AL, LA, KY, OR, OK, CT, UT, IA, NV, AR, MS, KS, NM, NE, ID, WV, HI, NH, ME, RI, MT, DE, SD, ND, AK, VT, WY).
</current_state>

<completed_work>
- Designed and iterated on state budget workflow (3 attempts, all failed on StructuredOutput)
- Workflow agents wrote scripts for 9 states: TX, NY, FL, PA, IL, OH, GA, NC, MI
- Wrote seedRemainingStates.js and ran it — all 40 remaining states seeded in DB
- Wrote .claude/workflows/state-budgets-all-50.js (do not use — see blocking constraint)
- Decided on correct approach: do work in main conversation, not via workflow agents
</completed_work>

<remaining_work>
1. Verify quality of workflow-written scripts (spot-check TX, NY, FL revenue data)
2. Run seed scripts for the 9 states that have them: TX, NY, FL, PA, IL, OH, GA, NC, MI
   (seedRemainingStates.js already seeded them — just need to run the individual seed scripts OR verify they're in DB)
3. Write processMI.js (operating budget — MI revenue script exists but operating is missing)
4. Run all existing process scripts to load data: TX, NY, FL, PA, IL, OH, GA, NC, MI
5. Write process scripts for 39 remaining states (one targeted WebSearch per state, then write)
6. Run all new process scripts

**Order for process scripts (largest first):**
NJ, VA, WA, AZ, MA, TN, IN, MO, MD, WI, CO, MN, SC, AL, LA, KY, OR, OK, CT, UT, IA, NV, AR, MS, KS, NM, NE, ID, WV, HI, NH, ME, RI, MT, DE, SD, ND, AK, VT, WY
</remaining_work>

<decisions_made>
- FY range: FY2022–2026 for all states (matching California pattern)
- Data strategy: Web-research + hardcode (like processCARevenue.js) — NOT runtime downloads
- Script pattern: Each state gets seedXXState.js + processXXRevenue.js + processXX.js
- Do NOT use Workflow tool — work directly in main conversation
- One targeted WebSearch per state to find official data source and verify numbers
- States with no income tax (TX, FL, WA, WY, SD, NV): omit income tax category
- States with no sales tax (OR, MT, NH, DE, AK): omit sales tax category
</decisions_made>

<blockers>
- processMI.js (operating budget) missing — needs to be written
- None of the workflow-written scripts have been verified for accuracy yet
</blockers>

## Required Reading (in order)
1. `scripts/processCARevenue.js` — the exact template all state revenue scripts follow
2. `scripts/seedCAState.js` — the exact template all state seed scripts follow
3. This file — full context

## Infrastructure State
- DB: All 50 states have municipality rows + data_source stubs
- Scripts written but NOT yet run: processTX.js, processTXRevenue.js, processNY.js, processNYRevenue.js, processFL.js, processFLRevenue.js, processPA.js, processPARevenue.js, processIL.js, processILRevenue.js, processOH.js, processOHRevenue.js, processGA.js, processGARevenue.js, processNC.js, processNCRevenue.js, processMIRevenue.js
- Env: .env exists at repo root (not .env.local) — scripts load from ../.env correctly

<next_action>
1. Spot-check processTXRevenue.js data against comptroller.texas.gov (verify GR totals are general revenue, not all-funds)
2. Run: node scripts/processTXRevenue.js --dry-run
3. If passes: node scripts/processTXRevenue.js
4. Continue through FL, PA, IL, OH, GA, NC, MI
5. Then write + run process scripts for the 39 remaining states one at a time
</next_action>
